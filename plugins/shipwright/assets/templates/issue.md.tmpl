<!-- task-id: {{TASK_ID}} -->
<!-- generated-by: test-readiness/test-publish -->

> **Milestone:** {{MILESTONE_NAME}}
> **Layer:** `{{LAYER}}` · **Bucket:** `{{BUCKET}}` · **Criticality:** `{{CRITICALITY}}`

## Expected outcome

{{EXPECTED_OUTCOME}}

## Files to touch

{{FILES_TO_TOUCH}}

## Acceptance criteria

{{ACCEPTANCE_CRITERIA}}

## Audit decisions

*For audit tasks only — omit this section for non-audit tasks. Each item being audited gets one row. This task is **not done** until every row has a filled decision.*

| Item | Decision | Criterion |
|---|---|---|
{{AUDIT_DECISION_ROWS}}

## Verification command

Run this to confirm done:

```
{{VERIFY_COMMAND}}
```

Output must show: {{VERIFY_EXPECTATION}}

## Context

- **Inventory entry:** [{{INVENTORY_LINK_TEXT}}]({{INVENTORY_LINK_URL}})
- **Blueprint section:** [{{BLUEPRINT_LINK_TEXT}}]({{BLUEPRINT_LINK_URL}})
- **Migration bucket:** [{{MIGRATION_LINK_TEXT}}]({{MIGRATION_LINK_URL}})

## Dependencies

{{DEPENDENCIES}}

## Closing checklist

Before closing this issue (or merging the PR that closes it):

- [ ] All acceptance-criteria checkboxes above are ticked
- [ ] `Verification command` was run; output pasted in the closing PR description (or linked via a CI run URL)
- [ ] CI is green on the PR (gated by branch protection once enabled)
- [ ] No source files outside `Files to touch` were modified, or the deviation is justified in the PR description

---

<details>
<summary>Why this task exists</summary>

{{RATIONALE}}

This issue is part of the **Test Readiness Roadmap**. See the tracking issue {{TRACKING_REF}} for the full picture: where the suite is now, where it needs to be, and the sequenced milestones to close the gap.

The five cross-cutting requirements this task must respect:

1. **Local-runnable** — no required external network. Use the local substitute prescribed in the blueprint.
2. **Canary-eligible** *(if applicable)* — same code must run with `TEST_TARGET_URL` set to a deployed URL.
3. **Fast** — must meet the layer's speed budget. Current target: {{SPEED_TARGET}}.
4. **Canonical layer** — assert only what this layer is responsible for. Do not re-assert what a lower layer already proves.
5. **No prod-write paths** *(canary-eligible tasks only)* — canary spec files must contain no writes to persistent state that are not cleaned up unconditionally. Verify with:
   ```
   grep -En "(\.create|\.update|\.delete|\.insert|\.upsert|\.patch|\.save|\bPOST\b|\bPUT\b|\bPATCH\b|\bDELETE\b)" <spec-files>
   ```
   Every match must be classified as setup-with-teardown or canary-scoped. Any unclassified match means the test is not canary-eligible and must be marked `rebuild`. See `canary-execution/SKILL.md` for the full classification procedure.

</details>
