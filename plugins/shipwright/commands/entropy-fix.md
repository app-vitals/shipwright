---
name: entropy-fix
description: "Read entropy-report.md and queue pr_worthy violations as task-store tasks, one task per golden principle, with per-finding HITL classification. Requires /entropy-scan to have been run first. Flags: --dry-run (preview tasks without queueing them), --rule {id} (queue one specific rule only)."
---

Invoke the `entropy-fix` skill.
