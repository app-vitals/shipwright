# Test Readiness Plan

> Phase 4 synthesis. The executable roadmap from where we are now to a test suite trustworthy enough for autonomous programming.
>
> Generated by `/test-roadmap` on 2026-07-27 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-20260727`), consuming `test-inventory.md` (refreshed this
> same pass at `03af837f`), `test-system.md` (refreshed this same pass at `75ffaa35`), and
> `test-migration.md` (refreshed this same pass at `779274c7`).

## Task-store reconciliation (Process step 4) — read this before Section 5

Queried task-store directly for every task in this repo, regardless of status, paginated
past the 500-per-page default (`total: 694`, fetched both pages). Filtered to
`test-t-<NNN>-shipwright` IDs: the highest existing number is **`test-t-078-shipwright`**.
**`starting_offset = 79`** — this cycle's genuinely-new tasks (if any) would start at
`T-079`, not `T-001`.

**Six test-readiness tasks already exist in task-store from a prior cycle's `/test-fix`
run** (dated on or before 2026-07-25/26, prior to this branch), all still tracked
independently of which docs branch is merged to `main`:

| Task | Status | Notes |
|---|---|---|
| `test-t-071-shipwright` | `deploying` (stale status — work is actually merged) | Verified directly: `mcp-server/src/main.smoke.test.ts` exists on `main` (commit `7b7ae8cf`, PR #2178). This is the known `task_stuck_deploying_status_gap` pattern — the PR shipped but the task-store record never transitioned past `deploying`. **Not re-tasked this cycle.** Flagged in Open risks for a task-store hygiene fix, not a docs-pipeline fix. |
| `test-t-076-shipwright` | `deploying` (stale status — work is actually merged) | Verified directly: `admin/src/agent-type-manifest-loader.integration.test.ts` exists on `main` (commit `15663f3c`, PR #2207), splitting the real-disk I/O cases out of the `.unit.test.ts` file exactly as scoped. Same stale-`deploying` pattern as T-071. **Not re-tasked.** |
| `test-t-073-shipwright` | `pending`, `hitl: true` | **Still genuinely open** — verified `site/src/lib/html-escape.ts` has no test file on `main`. This is a scoping decision (special-case a `bun:test` unit file under `site/src/lib/` vs. accept indirect e2e coverage), already correctly routed to HITL by the prior cycle's `test-fix`. This cycle's own Phase 3 independently re-found the same gap (see `test-migration.md`'s Net-new section) — **not re-tasked**, carried forward under its existing ID below. |
| `test-t-074-shipwright` | `blocked`, `hitl: true`, `blockedReason: requires_postgres_infra_access` | **Still genuinely open** — verified `docs/test-readiness/speed-baseline.md` is still dated 2026-07-15. This is the speed-baseline re-measurement item; the prior cycle's `test-fix` already applied the SKILL's 3-consecutive-cycle carried-forward-measurement-item rule (its own description cites 5 consecutive cycles: 2026-07-16, -19, -21, -23, -25) and correctly escalated to HITL rather than re-carrying it silently. **Not re-tasked**, carried forward under its existing ID below. This supersedes this cycle's own `test-migration.md` Method-note discussion, which (written before this reconciliation step ran) under-stated that the escalation had already happened — corrected here. |
| `test-t-077-shipwright` | `pending`, `hitl: false` | **Still genuinely open** — verified no test for `agent/src/check-review.ts`'s `isBundleComplete` fail-open `.catch(() => true)` catch path exists in `agent/src/check-review.unit.test.ts` today. This is a finer-grained finding than this cycle's own Phase 1/3 passes surfaced (they classified `check-review.ts` at the file level, not down to this specific catch-path branch) — carried forward under its existing ID, not re-derived. |
| `test-t-078-shipwright` | `pending`, `hitl: false` | **Still genuinely open** — verified no test proves `check-review.ts`'s `isAuthorAllowed` default closure calls `hasSynced()` (not just `.get()`) on a truly fresh, never-synced `agentAuthorAllowlistRef`; also verified the ref itself isn't yet injectable into `buildProductionDeps` the way `isBundleComplete` already is. Same finer-grained-than-this-cycle's-own-Phase-1/3 situation as T-077 — carried forward under its existing ID. |

**Net effect on Section 5 below:** this cycle mints **zero new T-NNN task-store records**.
Every real gap this cycle's own Phase 1–3 passes found (the Agent Type manifest system's
test coverage, the `html-escape.ts` gap, the `generate-agent-type-schema.ts` gap) is
either already fully covered (verified directly against `main`, not assumed) or already
tracked by an existing, still-open task-store record at a finer grain than this cycle's
own audit reached. The one item this cycle's own passes found that has **no** existing
task-store record — `scripts/generate-agent-type-schema.ts`'s missing test — is new
information relative to the six tasks above and gets a fresh task below, using the
correctly-reserved `T-079`.

## 1. Where we are now

### Layer coverage map

| Layer | Existing tests | Inventory items (canonical layer) | Coverage gap |
|---|---|---|---|
| Unit | 119 | ~90+ rows tagged unit across all categories | 1 real gap (`site/src/lib/html-escape.ts`, tracked T-073/HITL), 1 medium-but-likely-benign gap (`scripts/generate-agent-type-schema.ts`, new T-079 below), 2 finer-grained consumer-contract gaps inside `agent/src/check-review.ts` (tracked T-077, T-078) |
| Integration | 57 | ~55+ rows tagged integration (service-boundary + external) | 0 net-new this cycle — `admin/src/agent-type-manifest-loader.ts` (new) landed with both unit and integration coverage (T-076 already split the real-disk cases out, merged) |
| Smoke | 43 | ~20 rows tagged smoke (HTTP routes + route-equivalents) | 0 — `mcp-server/src/main.ts`'s fail-closed guard gap (last cycle's T-071) is now resolved and merged |
| E2E | 23 (20 `site/*.spec.ts` + 3 `*/e2e/*.e2e.ts`) | 1 genuinely browser-driven journey (marketing site) + 3 e2e/smoke-composed journeys proven at integration layer | 0 — the 3 new `site/tests/*.spec.ts` files (docs-cron-jobs, docs-nav, story) all landed with their corresponding page/nav content in the same commits |
| Content (5th layer) | 28 | ~5 grouped rows (commands/agents/skills/references/docs) | 0 — fully covered; skill/command/agent file count unchanged since 2026-07-21, 2 new content tests landed alongside the Agent Type manifest system's own docs |

**Total: 270 test files**, up from 249 at the 2026-07-21 cycle (+21). Every test-file item
flagged as a real gap at the 2026-07-21 cycle (`mcp-server/src/main.ts`) is now resolved
and merged. Of this cycle's 9 new non-test source files, 7 shipped with matching test
coverage in the same PRs; 2 remain genuinely gapped
(`scripts/generate-agent-type-schema.ts`, `site/src/lib/html-escape.ts`) — the latter
already tracked (`test-t-073-shipwright`, HITL), the former new this cycle (T-079 below).
See `test-migration.md`'s Summary and Net-new sections for full detail.

### Local-runnable status

- **Tests that run locally with zero external network:** effectively 100% of the 270
  files — Phase 3's isolation-contract grep sweep found zero real violations
  (`mock.module(`, `global.fetch =`, `global.console =`, stray `DATABASE_URL` in unit
  tests), unchanged from every prior cycle.
- **Tests blocked on external services:** none.

### Canary status

**Not applicable.** Root `CLAUDE.md` declares `## Deploy model: direct` — no
staging/production GitHub Environments, no deploy→canary→promote pipeline. Phase 1
skipped canary-eligibility tagging; Phase 2's canary execution contract documents what
would need to change (a staging target) before this applies; Phase 3 confirms no canary
plumbing exists to audit. This phase carries the same finding forward. See Milestone 3
below.

### Speed status (current p95 per layer)

`docs/test-readiness/speed-baseline.md` (on `main` since 2026-07-15) shows every measured
layer comfortably within budget as of its last real measurement: unit 1.05-2.50s per
package (target <30s suite), integration 0.15-5.35s (target <3min suite), smoke
3.24-4.62s (target <30s/<60s suite), e2e 4-40s via real CI runs (target <10min), full
suite 34.23s wall-clock (target <15min full-PR budget). **This measurement is now 12 days
stale and has been carried forward, unactioned, across 5 consecutive `/test-migration`
cycles** (2026-07-16, -19, -21, -23, -25) plus this one (6th) — already correctly
escalated to a `blocked`/`hitl:true` task-store record (`test-t-074-shipwright`,
`blockedReason: requires_postgres_infra_access`) by a prior cycle's `test-fix`, not a new
finding this cycle. No re-measurement is possible from this docs-pipeline sandbox (no
`DATABASE_URL_SHIPWRIGHT_*` wired). See Open risks.

### "Rebuild" debt

| Bucket | Count | Effort |
|---|---|---|
| Rebuild | 0 | — |

### "Delete (redundant)" count

**Zero.** No higher-layer test was found re-asserting a business rule already proved at a lower layer.

## 2. Where we want to be

- **Framework matrix** — already fully realized: `bun test` (unit/integration/content),
  `bun test` + Hono `app.request()` (smoke), Playwright (E2E, 3 surfaces). No framework
  changes needed.
- **Coverage targets** — single 80/80 line/function CI floor (`scripts/check-coverage.ts`),
  already enforced; 100% of `critical`-tier inventory items covered at the prescribed
  layer except the two finer-grained `check-review.ts` consumer-contract gaps (T-077,
  T-078, both already tracked); `high`-tier is fully covered except `html-escape.ts`
  (T-073, HITL-gated scoping decision) and the medium-tier `generate-agent-type-schema.ts`
  gap (T-079, new).
- **Canary suite** — not applicable under the current `direct` deploy model; deferred
  until a staged-deploy model exists.
- **Speed budgets per layer** — Unit <50ms p95 / Integration <2s / Smoke <5s / E2E <30s /
  full PR pipeline <15min, per `speed-budgets/SKILL.md` defaults, unmodified. Measured
  once and within budget; re-measurement blocked on infra access (T-074, already
  HITL-escalated).
- **Zero `rebuild` / `delete (redundant)` buckets** — already true today.

## 3. The gap

The gap remains small and is now almost entirely composed of already-tracked, already-HITL-triaged
items rather than net-new discoveries:

- **No missing layers.** All five layers in active use with the correct framework per layer.
- **Wrong-layer tests:** 0.
- **External-only deps with no local substitute:** none.
- **Net-new coverage gaps (from Phase 3), reconciled against existing task-store records:**
  - `scripts/generate-agent-type-schema.ts` (medium tier, no existing task-store record) —
    genuinely new this cycle. See T-079, Milestone 4.
  - `site/src/lib/html-escape.ts` (high tier) — already tracked as `test-t-073-shipwright`,
    `hitl: true`, a scoping decision (special-case a unit test under `site/src/lib/` vs.
    rely on indirect e2e coverage). Carried forward under its existing ID, not re-tasked.
  - `agent/src/check-review.ts`'s `isBundleComplete` fail-open catch path (critical tier) —
    already tracked as `test-t-077-shipwright`. Carried forward, not re-tasked.
  - `agent/src/check-review.ts`'s `isAuthorAllowed` never-synced consumer contract (high
    tier) — already tracked as `test-t-078-shipwright`. Carried forward, not re-tasked.
- **Infra gap: none new.** All infrastructure items (runner config, CI shape, Postgres
  containers, `Clock` helpers, recorded-fixture doubles, coverage gate) confirmed
  unchanged and correct this cycle.
- **Carried-forward gap: speed-baseline re-measurement.** Already HITL-escalated
  (`test-t-074-shipwright`, `blocked`) by a prior cycle per the SKILL's 3-cycle
  carried-forward-measurement-item rule — this cycle correctly does not re-escalate a
  second time, just references the existing record. See Open risks.
- **Task-store hygiene gap (not a test-coverage gap): two `deploying`-status records
  (`test-t-071-shipwright`, `test-t-076-shipwright`) whose underlying work is already
  merged to `main`.** This is the `task_stuck_deploying_status_gap` pattern — not
  something this docs-only pipeline can fix (no write access to correct task-store
  status beyond what `test-fix` itself does), flagged in Open risks for the deploy/PR
  reconciler to pick up.

### Speed delta

| Layer | Current (measured, `speed-baseline.md`, 2026-07-15) | Target | Ratio |
|---|---|---|---|
| Unit | 1.05–2.50s per package | <50ms per-test p95 / <30s suite target | within suite-target budget; per-test p95 not separately broken out in the baseline doc (aggregate-only) |
| Integration | 0.15–5.35s per package | <2s per-test p95 / <3min suite target | within suite-target budget; per-test p95 not separately broken out |
| Smoke | 3.24–4.62s per package | <5s per-test p95 / <60s suite target | within budget on both dimensions |
| E2E | 4–40s per surface (CI-sourced) | <30s per-test p95 / <10min suite target | within budget; site's 40s Playwright-smoke step is a job-level step duration (includes multiple specs), not a single-test p95 — same caveat `speed-baseline.md` itself notes |
| Full suite | 34.23s wall-clock | <15min full-PR budget | ~26x headroom |

No layer is flagged over its hard cap based on the last real measurement. The
measurement is now a **12-day-stale** point-in-time snapshot spanning four full
test-readiness cycles' worth of file-count growth (224 → 243 → 249 → 270), still small
and low-complexity per cycle (thin smoke/unit files, no new heavy integration suites), but
the staleness itself is the tracked item — see `test-t-074-shipwright` (already
`blocked`/HITL) rather than a fresh flag here.

## 4. Roadmap (five milestones)

### Milestone 1 — Infrastructure baseline
**DOD:** Runners, local substitutes, CI pipeline shape in place. No new feature tests written yet — just the rails.

**Already satisfied — zero new tasks this cycle.** The naming convention + runner-exclusion
config remain correct and unchanged (verified in Phase 2). The branch-protection
verification item (T-051, from an earlier cycle) is now `done` — confirmed via a fresh
task-store query this cycle, resolving the "escalate at the next run if still pending"
note the 2026-07-21 plan left. The carried-forward speed-baseline re-measurement item
(`test-t-074-shipwright`) remains open but is **already** a Milestone-1 task-store record
from a prior cycle's `test-fix` — referenced here for continuity, not re-emitted:

| Task | Files to touch | Layer | Bucket origin | Expected outcome | Verification command |
|---|---|---|---|---|---|
| `test-t-074-shipwright` (carried forward, not re-tasked) | `docs/test-readiness/speed-baseline.md`, `docs/test-readiness/test-system.md` | infra | reuse (measurement refresh) | Real per-layer p95/wall-clock numbers captured against a Postgres-enabled run, replacing the 2026-07-15 baseline | `DATABASE_URL_SHIPWRIGHT_ADMIN=... DATABASE_URL_SHIPWRIGHT_TASK_STORE=... DATABASE_URL_SHIPWRIGHT_CHAT=... bun test --coverage` (or CI equivalent), capture wall-clock + per-package timings |

### Milestone 2 — Critical-path coverage
**DOD:** 100% of `critical` tier inventory items have passing tests at the prescribed layer.

**One item, already tracked — not re-tasked.** `agent/src/check-review.ts`'s
`isBundleComplete` fail-open `.catch(() => true)` catch path (critical tier per Phase 1)
has zero test coverage today, verified directly. This is a finer-grained finding than
this cycle's own Phase 1/3 passes reached (they classified `check-review.ts` at the file
level) — already correctly captured by a prior cycle's `test-fix` as
`test-t-077-shipwright`, `hitl: false`, `pending`:

| Task | Files to touch | Layer | Bucket origin | Expected outcome | Verification command |
|---|---|---|---|---|---|
| `test-t-077-shipwright` (carried forward, not re-tasked) | `agent/src/check-review.unit.test.ts` | unit | net-new | `isBundleComplete`'s fail-open catch path proven via a rejecting-dependency test case, modeled on the two adjacent bundle-gate cases at lines 631-664 | `bun test agent/src/check-review.unit.test.ts` |

### Milestone 3 — Canary suite live
**DOD:** canary not applicable -- deploy_model=direct

Root `CLAUDE.md` declares `direct` — no staging/production GitHub Environments, no
deploy→canary→promote pipeline. Per the SKILL, zero tasks are emitted for this milestone;
no canary-plumbing work is generated for a pipeline that doesn't exist. See
`test-system.md`'s Canary execution contract section for what would need to change
before this applies.

### Milestone 4 — High-tier coverage
**DOD:** 100% of `high` tier inventory items covered.

**Two items: one already tracked, one genuinely new this cycle.**

1. **`test-t-078-shipwright`** (carried forward, not re-tasked) — `agent/src/check-review.ts`'s
   `isAuthorAllowed` default closure only calls `.get()` on the author-allowlist ref, never
   `.hasSynced()`; no test proves the never-synced-vs-synced-empty fail-open equivalence at
   the consumer level (only at the ref level, per `agent-author-allowlist-ref.unit.test.ts`).
   Requires threading the ref as an injectable dependency into `buildProductionDeps` first
   (mirroring `isBundleComplete`'s existing injectability) — scoped as one task since the
   test can't be written correctly without that change.
2. **T-079** (new) — `scripts/generate-agent-type-schema.ts` has no test file. Thin CLI
   wrapper around `admin/src/agent-type-registry.ts`'s already-unit-tested
   `buildAgentTypeJsonSchema()`; the untested surface is just the `writeFileSync` call and
   the `import.meta.main` CLI-entry guard. Same shape as the repo's other pre-existing,
   also-untested `generate-*-spec.ts` codegen scripts (carried as a non-gap in every prior
   cycle) — flagged here as a **one-time class-level triage decision**, not a bespoke
   per-script task: does this repo want thin-codegen-wrapper scripts tested at all, and if
   so, what's the minimal shape (e.g. inject a `writeFile` fn, assert it's called with the
   right path/content)? If the class-level answer is "yes, test them," this task also
   covers writing the actual test; if "no, these are conventionally untested wrappers,"
   this task's outcome is a one-line note in `test-system.md`'s Shared-helpers or
   Gaps-summary section recording that decision so it isn't re-litigated file-by-file each
   cycle.

| Task | Files to touch | Layer | Bucket origin | Expected outcome | Verification command | Depends on |
|---|---|---|---|---|---|---|
| `test-t-078-shipwright` (carried forward, not re-tasked) | `agent/src/check-review.ts`, `agent/src/check-review.unit.test.ts` | unit | net-new | `isAuthorAllowed` ref made injectable into `buildProductionDeps`; never-synced fail-open equivalence explicitly tested in a fresh (non-shared-`beforeEach`) describe block | `bun test agent/src/check-review.unit.test.ts` | — |
| T-079 | `scripts/generate-agent-type-schema.ts` (test decision), `docs/test-readiness/test-system.md` (record the class-level decision) | unit (if tested) / infra (if documented as a convention) | net-new | Either a `scripts/generate-agent-type-schema.unit.test.ts` asserting `writeFileSync` is called with the right path/content given an injected writer, OR a documented, class-level "thin codegen wrappers are conventionally untested" decision covering this file and its siblings | `bun test scripts/generate-agent-type-schema.unit.test.ts` (if the test-writing path is chosen) | — |

**Note on `site/src/lib/html-escape.ts`:** this Phase 1/3-flagged high-tier gap is **not**
listed as a Milestone 4 task here — it's already tracked as `test-t-073-shipwright`,
correctly HITL-gated by a prior cycle (no `site/` unit-test precedent to mirror; the fix
approach itself, not just the test content, is a human call). Referenced in Section 3
above and Open risks below; not re-emitted as a new task.

### Milestone 5 — Cleanup
**DOD:** Zero tests in `rebuild` or `delete (redundant)` buckets. CI green.

**Already satisfied — zero tasks.** Phase 3 found zero `rebuild` and zero `delete
(redundant)` items this cycle, same as every prior cycle. No new generalizable learning
surfaced this cycle warranting a `# Harness TODO` — the process items this cycle
surfaced (two stale `deploying`-status task-store records, and this cycle's own Phase 1/3
passes reaching a coarser grain than a prior cycle's `test-fix` on `check-review.ts`) are
task-store/process hygiene notes, not skill/plugin defects, and are captured in Open risks
below instead.

## 5. Task list

Flat, ordered, agent-executable. **Zero new task-store records are minted by this
cycle's Section 4 above beyond T-079** — every other roadmap item is a carried-forward
reference to an existing, still-open task-store record (confirmed live via direct query,
not assumed), per the Task-store reconciliation section at the top of this document.
T-079 correctly starts from `starting_offset = 79` (max existing `test-t-NNN-shipwright`
found was `078`, queried across all 694 tasks in the repo regardless of status).

| ID | Milestone | Files to touch | Layer | Bucket origin | Expected outcome | Verification command | Depends on |
|---|---|---|---|---|---|---|---|
| `test-t-074-shipwright` (ref only) | 1 | `docs/test-readiness/speed-baseline.md`, `test-system.md` | infra | reuse (measurement refresh) | Real per-layer p95/wall-clock numbers, replacing the 2026-07-15 baseline | `bun test --coverage` with all three `DATABASE_URL_SHIPWRIGHT_*` set | — |
| `test-t-077-shipwright` (ref only) | 2 | `agent/src/check-review.unit.test.ts` | unit | net-new | `isBundleComplete` fail-open catch path proven | `bun test agent/src/check-review.unit.test.ts` | — |
| `test-t-078-shipwright` (ref only) | 4 | `agent/src/check-review.ts`, `agent/src/check-review.unit.test.ts` | unit | net-new | `isAuthorAllowed` ref injectable; never-synced equivalence tested | `bun test agent/src/check-review.unit.test.ts` | — |
| `test-t-073-shipwright` (ref only) | 4 | `docs/test-readiness/test-system.md` (+ optionally `site/src/lib/html-escape.unit.test.ts`) | unit (pending scoping) | net-new | Scoping decision on `site/`-under-`bun:test` precedent, then act on it | `bun test site/src/lib/html-escape.unit.test.ts` (if chosen) | — |
| **T-079 (new)** | 4 | `scripts/generate-agent-type-schema.ts` (test decision), `test-system.md` | unit / infra | net-new | Class-level test/no-test decision for thin codegen-wrapper scripts, applied to this file | `bun test scripts/generate-agent-type-schema.unit.test.ts` (if chosen) | — |

No task in this cycle exceeds the ~1000-line cap, touches more than one functional
concern, or represents an N-service repeated operation — no task splitting was required.
Phase 5 (`test-fix`) should **not** create new task-store records for the four
carried-forward references above (T-074, T-077, T-078, T-073 by their existing IDs) —
they already exist and are already correctly triaged (2 blocked/HITL on infra or scoping
grounds, 2 plain-pending agent-executable). Phase 5 should create exactly one new record
for T-079.

## 6. Open risks

- **Two task-store records are stuck at `deploying` status despite their PRs already
  being merged to `main`.** `test-t-071-shipwright` (PR #2178, `mcp-server/src/main.ts`
  fail-closed guard test) and `test-t-076-shipwright` (PR #2207, manifest-loader
  unit/integration split) both show `status: deploying` in task-store today, but neither
  has a live branch on `origin` and both PRs are confirmed merged via `git log`. This
  matches the known `task_stuck_deploying_status_gap` pattern (per project memory) — not
  something a docs-readiness pipeline session can or should fix directly (no task-store
  write access beyond what `test-fix` performs); flagged for the deploy pipeline's PR-state
  reconciler to pick up. Not re-tasked or re-escalated here — purely a status-hygiene note.
- **This cycle's own Phase 1/3 passes reached a coarser classification grain than a prior
  cycle's `test-fix` did for `agent/src/check-review.ts`.** T-077 and T-078 (both
  pre-existing) identify specific untested branches (`isBundleComplete`'s fail-open catch,
  `isAuthorAllowed`'s never-synced consumer contract) that this cycle's Phase 1 inventory
  and Phase 3 migration audit classified only at the whole-file level. This is not a
  regression — the file-level classification is still correct, just less granular — but
  worth noting as a limitation of this pipeline's per-cycle sampling depth (Phase 1's
  SKILL explicitly caps reads and prefers breadth over depth for large repos). No action
  needed; the finer-grained tasks are already tracked and correctly reference their exact
  line numbers.
- **Speed baseline is now 12 days stale and already HITL-escalated** (`test-t-074-shipwright`,
  `blocked`, `blockedReason: requires_postgres_infra_access`) after 5+ consecutive cycles
  carrying the item forward. Per the SKILL, this should not be escalated a second time —
  it already has a `hitl: true` record. If it is still open after several more cycles with
  no infra access materializing, a human should consider whether this measurement can ever
  realistically be refreshed from an agent-driven pipeline, or whether it needs a
  dedicated CI job instead (a different kind of fix than another docs-cycle escalation).
- **`test-t-073-shipwright`'s scoping decision (`site/src/lib/html-escape.ts`) remains
  open and HITL-gated.** No new information this cycle changes the calculus described in
  its own task body — still no `site/`-under-`bun:test` precedent to mirror, still a
  genuine human call between special-casing one unit-test file vs. relying on indirect
  e2e coverage via whichever `*.spec.ts` renders a `set:html`-using component. Not
  re-escalated, just carried forward.
- **Recorded-fixture coverage breadth** (Phase 2's gaps-summary item 2: ~9 remaining
  `Http*Client` modules that could extend the `Recorded*Client` pattern) is a
  design-level backlog item, not a migration-bucket verdict on any existing file — no
  task emitted this cycle since Phase 3 found no test currently missing coverage because
  of it. Revisit if a future inventory pass tags one of those clients as critical/high
  with no test.
- **`agent/src/piper-voice.ts` reclassification watch item** (Phase 1/2: unit today,
  integration once Piper synthesis lands per PPR-1.2) — no action needed until that
  lands, still not landed as of this cycle; flagged so a future Phase 3 doesn't silently
  miss the transition.
- **Recorded-fixture maintenance loop remains deferred** (`repo-config/SKILL.md` §5) —
  correctly not scheduled this cycle; sequence after fixture-authoring work matures, per
  Phase 2's explicit guidance.
- **Two unmerged sibling refresh branches exist** (`docs/test-readiness-refresh-20260723`,
  `-20260726`) with their own inventory/migration/roadmap edits that never landed on
  `main`. This cycle's artifacts are based on `main` alone and don't reconcile against
  those branches' content — flagged in case a human wants to compare or salvage anything
  from them before they age out (worktrees older than 14 days are removed automatically
  per this workspace's own convention).

---

## Handoff

This document is intended for execution by:
- An engineer working through the task list manually, OR
- `shipwright /dev-task` consuming the task list as a queue.
