# Test Readiness Plan

> Phase 4 synthesis. The executable roadmap from where we are now to a test suite trustworthy enough for autonomous programming.
>
> Generated by `/test-roadmap` on 2026-08-22 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-20260822`), consuming `test-inventory.md` (refreshed at
> `ea846283`), `test-system.md` (refreshed at `ea846283`), and `test-migration.md`
> (refreshed at this cycle's HEAD, `ea846283`).

## Coverage Gate

The headline machine-checkable verdict — computed by `scripts/check-coverage-gate.ts`
(CGT-1.2), rendered verbatim below.

Inputs sourced per Process step 8: `linePct`/`lineSource` from this cycle's Step 4c CI
pull ([run 32550364804](https://github.com/app-vitals/shipwright/actions/runs/32550364804)
@ `ea846283`); `featurePct`/`featureSource` carried forward from `test-inventory.md`'s
`feature_coverage_pct` (unchanged this cycle, see that document's Features section).
Computed via:
```bash
echo '{"featurePct": 100, "featureSource": "docs/test-readiness/test-inventory.md", "linePct": 91.28, "lineSource": "CI run https://github.com/app-vitals/shipwright/actions/runs/32550364804 @ ea846283"}' \
  | bun run scripts/check-coverage-gate.ts
```

| Field | Value |
|---|---|
| Feature coverage | 100% (target 90%) — source: docs/test-readiness/test-inventory.md |
| Line coverage | 91.28% (target 90%) — source: CI run https://github.com/app-vitals/shipwright/actions/runs/32550364804 @ ea846283 |
| **Verdict** | **READY** |

**Stage-3 coverage-check promotion — code-side change landed; branch-protection swap
still needs human confirmation.** `line_coverage_pct` (91.28) remains above the 90%
threshold `repo-config/SKILL.md`'s 3-stage coverage-check lifecycle gates on. **T-083**
("Promote coverage required check from never-decrease to hard ≥90% gate") is now
**`done`** per direct task-store query this cycle (was `pending` last cycle) — its PR
raised `scripts/check-coverage.ts`'s `THRESHOLD_LINES` from 89 to 90 and, per that
change's own header comment, deleted the now-superseded
`scripts/check-coverage-no-decrease.ts` stage-2 gate entirely (`2ab1a4cb`). **However,
that same header comment explicitly states the branch-protection required-check swap
itself is "separate and human-driven, outside the scope of this code-side change"** — this
cycle could not verify via `gh api repos/app-vitals/shipwright/branches/main/protection`
(403, insufficient token scope) whether the required-status-check config was actually
updated to require the new 90% line gate instead of (or in addition to) the now-deleted
never-decrease check. **Flagging as a new Open risk below** — a human should confirm
branch protection matches the code-side change, since a stale required-check name would
silently stop blocking merges on the retired job.

## Status of the prior cycle's plan (2026-08-18) and everything since

Queried task-store directly (not assumed from the old doc; direct `GET /tasks/{id}`
lookups against each of the 8 known IDs from the prior cycle, plus a fresh full-repo scan
for the offset query below), current status of every relevant ID as of this cycle
(2026-08-22):

| Task | Status | Title |
|---|---|---|
| `test-t-076-shipwright` | **deploying** (unchanged) | Split `agent-type-manifest-loader.unit.test.ts`'s real-disk cases into a new `.integration.test.ts` file |
| `test-t-077-shipwright` | **deploying** (unchanged) | `check-review.ts`'s `isBundleComplete` fail-open catch-path test |
| `test-t-078-shipwright` | **deploying** (unchanged) | Make `isAuthorAllowed`'s ref dependency injectable + never-synced equivalence test |
| `test-t-079-shipwright` | **deploying** (unchanged) | `scripts/generate-agent-type-schema.ts` test-coverage class-level decision — human note on record (Dan, 2026-07-28) confirming this is a deliberate no-test-needed convention, already reflected in `test-system.md` |
| `test-t-080-shipwright` | **deployed** (unchanged) | HITL dev-loop bootstrap's full clone→seed→boot→poll sequence, injected `gh` exec fake |
| `test-t-081-shipwright` | **done** (unchanged) | `TaskService.claim()`'s concurrent-race close (real Postgres) |
| `test-t-082-shipwright` | **deploying** (unchanged) | `traceReviewCandidacyDecision`'s exclusion checks proven correct in 3+-condition combination |
| `test-t-083-shipwright` | **done** (moved from `pending`) | Promote coverage required check from never-decrease to hard ≥90% gate |

**T-083 moved from `pending` to `done` this cycle** — the code-side change landed
(`5bb3cf6d` raises `THRESHOLD_LINES` to 90; `2ab1a4cb` deletes the superseded
`check-coverage-no-decrease.ts` stage-2 script + tests + CI job). See the Coverage Gate
section above for the caveat this cycle surfaced: T-083's own source-file comment says
the *branch-protection* required-check swap is a separate, human-driven step outside its
scope, and this cycle could not verify that step completed (403 on the branch-protection
API). Recommend a human confirm it directly before treating T-083 as fully closed
end-to-end.

**No status movement on T-076 through T-079 or T-082 this cycle.** T-076 through T-079
remain parked at `deploying` — now the **9th consecutive cycle** with zero movement, all
four confirmed merged 2026-07-25 through 2026-07-28 (now 3.5-4 weeks ago) with
`deployedAt: null` still unset. This cycle re-confirmed all four merge commits remain
ancestors of current `origin/main`. **Still not force-patched this cycle** — same
reasoning as every prior cycle: this docs-refresh pipeline has no first-party mechanism
to confirm a specific Helm/image build actually rolled out, and blindly patching risks
marking genuinely-stuck work as resolved without evidence. Escalated again in Open risks
below — now a 9-cycle-old, unresolved-by-this-pipeline pattern that likely warrants
direct human investigation rather than further passive re-confirmation each cycle.

**Task-store query for the starting T-NNN offset (per SKILL Process step 4), run before
minting any new IDs:** queried `$SHIPWRIGHT_TASK_STORE_URL/tasks?repo=app-vitals%2Fshipwright&limit=500`
— `total: 911`, requiring one additional page (`&offset=500`, +411 records, 911 total
scanned, matching `.total`). Filtered to `test-t-<NNN>-shipwright` IDs across both pages:
highest is `test-t-083-shipwright` (confirmed present, now `done`). **`starting_offset =
84`** — this cycle's new task begins at **T-084**.

**This cycle mints exactly one new task: T-084.** Phase 3 (`test-migration.md`) found
one genuine Promote/deepen item this cycle — `agent-slack-membership-ref.unit.test.ts`'s
process-wide-singleton test has no `afterEach` reset, a real isolation-contract gap (see
Method note step 1 there). Zero rebuild items; one trivial/optional carried-forward
Net-new item (`admin/src/clock.ts`, explicitly not worth a task per every prior cycle's
same finding). The Coverage Gate's stage-3 promotion trigger (Process step 9) found
T-083 already minted and now `done` — no duplicate emitted for it.

## 1. Where we are now

### Layer coverage map

| Layer | Existing tests | Inventory items | Coverage gap |
|---|---|---|---|
| Unit | 134 `.unit.test.ts` | Dominant category-1 surface (`lib/`, decision/classification logic, mutable-box refs, crypto helpers, parsing/validation, coverage-gate tooling) | None on layer/framework — but see the new isolation-contract Promote/deepen item below (not a missing-coverage gap, a test-quality gap) |
| Integration | 67 `.integration.test.ts` | Every category-2/5 service-boundary and external-integration item | None — Okta auth, GitHub App provisioning, and PR audit-trail events (PSA-1.2) all landed with matching integration coverage this cycle |
| Smoke | 43 `.smoke.test.ts` | Every category-3 HTTP route across 5 services | None — new `GET /prs/:id/events` route landed with matching smoke coverage this cycle |
| E2E | 21 `site/tests/*.spec.ts` + 3 `{admin,metrics}/e2e/*.e2e.ts` | 4 of 5 identified journeys are browser-driven or partially so (journey #5, HITL bootstrap, is integration-composed, not E2E) | None |
| Content | 41 `.content.test.ts` | `plugins/shipwright/{commands,agents,skills,references}/*` | None — unchanged this cycle |
| Shell (unofficial 6th layer) | 2 `test-*.sh` bash assertion suites | `.github/workflows/lib/chart-tag-utils.sh`, `chart-drift-check.sh` | None — coverage exists; only a documentation-completeness question is open (see Open risks) |
| Compile-time-only (unformalized) | 1 `*.check.ts` (`blocked-by-type-parity.check.ts`) | `admin/src/blocked-by-type-parity.check.ts` | None — wired into CI's `typecheck` step, confirmed passing; only a taxonomy question open (see Open risks) |

**Total: 309 test files** (285 `bun test`-scanned + 21 `site/tests/*.spec.ts` + 3
Playwright `*.e2e.ts`), net +2 from the 2026-08-18 pass's 307: +5 new
(`okta-auth-client.integration.test.ts`, `github-app-provisioning-client.{unit,integration}.test.ts`,
`agent-slack-membership-ref.unit.test.ts`, `pr-transition-diff.unit.test.ts`) minus −2
deleted (`check-coverage-no-decrease.{unit,integration}.test.ts`, paired with their
source file's removal). 4 of the 5 new files landed as already-correct, already-passing
coverage in the same commits as their production change; one
(`agent-slack-membership-ref.unit.test.ts`) has a real isolation-contract gap — see
below and `test-migration.md`'s Method note step 1.

**`## Features` section in `test-inventory.md` unchanged this cycle:** 23 features,
`feature_coverage_pct` still 100% (23/23) — no new feature introduced (all 5 new units
fold into existing features). This is the source for the Coverage Gate block above.

### Local-runnable status

**100%** of the 285 `bun test`-scanned files run locally with zero external network.
Phase 3's isolation-contract grep sweep (`mock.module(`, `global.fetch =`,
`global.console =`, stray `DATABASE_URL` inside `*.unit.test.ts`) found zero *literal*
violations this cycle (41 `mock.module(` matches, all in comments, unchanged; 3
pre-existing non-violation `DATABASE_URL` string-literal matches, unchanged). **However,
this cycle's Phase 3 pass found a real isolation-contract hazard outside the sweep's
existing pattern set**: `agent-slack-membership-ref.unit.test.ts`'s singleton-scoped test
mutates the shared, process-wide `agentSlackMembershipRef` export with no `afterEach`
reset — the same class of cross-suite leak risk the `global.*`-override rule exists to
prevent, just via a named-export mutation instead of a literal `global.` assignment (see
Milestone 1's naming/config note is unaffected; this is a test-code issue, not a
runner-config issue). No test is blocked on an external service. New integration tests
(`okta-auth-client.integration.test.ts`, `github-app-provisioning-client.integration.test.ts`)
use injected-`fetchFn` cassette replay, not live network — fully local-runnable,
consistent with every other integration test in this suite.

### Canary status

**Not applicable.** Root `CLAUDE.md` declares `## Deploy model: direct` — no
staging/production GitHub Environments, no deploy→canary→promote pipeline. Unchanged
finding across every cycle to date. See Milestone 3.

### Speed status (Tier 1 aggregate vs. budget; Tier 2 not triggered)

**Tier 1 — aggregate (measured this pass, via live CI history):**

| Aggregate wall-clock | Budget | Ratio |
|---|---|---|
| 2m38s ([run 32550364804](https://github.com/app-vitals/shipwright/actions/runs/32550364804), 2026-08-22T03:56:54Z, commit `ea846283` — this cycle's exact HEAD) | <15 min (Full PR pipeline) | ~18% of budget |

Fully green run — no failing jobs. `bun test` step itself: 41.53s. 7,190 tests pass, 0
fail, across 286 files. Coverage held at 91.28% lines / 90.00% functions (threshold
90%/89%, line threshold raised by T-083/CLG-1.1 this cycle). Consistent with every prior
cycle's ~13-18% readings — not trending toward the escalation trigger. Test-count growth
trajectory: 5,762 (2026-07-28) → 6,041-6,042 (2026-08-01) → 6,070 (2026-08-05) → 6,071
(2026-08-06) → 6,177 (2026-08-09) → 6,288 (2026-08-11) → 6,501 (2026-08-14) → 6,864
(2026-08-16) → 6,920 (2026-08-18) → **7,190 (2026-08-22, this pass)** — steady, expected
growth (+270 tests over the 4-day gap since 2026-08-18).

**Tier 2 triggered:** no — the escalation formula (aggregate wall-clock >7.5 min,
sustained across 2 consecutive measurements) is nowhere near tripped (~18% of budget,
well below the 50% escalation threshold). No per-layer p95 table is populated below, per
the SKILL's explicit guidance against fabricating per-layer numbers when only the Tier 1
aggregate is available.

### "Rebuild" / "Delete (redundant)" debt

| Bucket | Count | Effort |
|---|---|---|
| Rebuild | 0 | — |
| Promote / deepen | 1 | trivial (add `afterEach` reset) |
| Delete (redundant) | 0 | — |
| Net-new | 1 | trivial, optional |

Rebuild and Delete remain zero this cycle. **Promote/deepen moves from 0 to 1** —
`agent-slack-membership-ref.unit.test.ts`'s process-wide-singleton test needs a paired
cleanup (see Local-runnable status above and `test-migration.md`'s Risk callouts). This
ends the empty-Promote/deepen steady state that had held since 2026-08-16 — a healthy
sign the audit is still catching real, if small, gaps rather than just re-confirming a
static baseline. No higher-layer test re-asserts a business rule already proved at a
lower layer; no test's layer/framework/fundamentals were found wrong — the one
Promote/deepen item is a test-isolation defect, not a layer/framework mismatch. Net-new
remains at 1 — only the long-standing, explicitly-optional `admin/src/clock.ts` item.

## 2. Where we want to be

- **Framework matrix** — already fully realized: `bun test` (unit/integration/smoke/content),
  Playwright (E2E, 3 surfaces), plain `bash` (the shell layer). No framework changes
  needed.
- **Local-substitute map** — every dependency named in the inventory has a working local
  substitute (recorded-fixture doubles for internal/third-party HTTP, real Postgres via
  CI service container for the three Prisma-backed services, a real locally-launched
  Chromium binary for `agent/src/browser.ts`). No blockers.
- **Coverage targets** — single 90/89 line/function CI floor (`scripts/check-coverage.ts`,
  raised from 80/80 by MTC-1.7, to 89% lines by MTC-1.7's follow-up, then to 90% lines by
  T-083/CLG-1.1 this cycle), code-side change already enforced (branch-protection swap
  status: see Coverage Gate section and Open risks). Tiered targets (critical ~95%, high
  ~80%, medium spot-check) direct prioritization, not a second CI gate.
- **Canary suite definition** — not applicable under the current `direct` deploy model;
  deferred until a staged-deploy model exists.
- **Speed budgets per layer** — Unit <50ms p95 / Integration <2s / Smoke <5s / E2E <30s /
  full PR pipeline <15min. Tier 1 aggregate confirms comfortable headroom (~18% of
  budget); no per-layer breakdown needed since Tier 2 has never triggered.
- **Zero `rebuild` / `delete (redundant)` buckets** — still true today. **`promote/deepen`
  bucket returns to 1 this cycle** (`agent-slack-membership-ref.unit.test.ts`'s missing
  `afterEach` reset) after one cycle at zero — see Milestone 2/gap section below.
- **Every critical- and high-tier inventory item covered at its prescribed layer,
  including a genuine concurrent-caller regression test wherever a claim/lock race was
  closed, and combination coverage across interacting exclusion checks in
  candidate-selection logic** — the claim-race standard was already fully met as of last
  cycle; **the combination-coverage standard for `check-review.ts`'s exclusion checks is
  now also fully met (T-082 resolved this cycle)**; the HITL bootstrap sequencing
  standard is likewise now fully met (T-080 resolved this cycle). No open depth gaps
  remain in this category.

## 3. The gap

- **No missing layers.** All six layers (five `bun test`-declared plus the unofficial
  shell layer) in active use with the correct framework per layer, plus one
  deliberately-unformalized compile-time-only pattern (`blocked-by-type-parity.check.ts`)
  that has no coverage gap despite not fitting the existing taxonomy.
- **Wrong-layer tests:** 0.
- **External-only deps with no local substitute:** none.
- **One new depth gap this cycle: `agent-slack-membership-ref.unit.test.ts`'s
  process-wide singleton test has no `afterEach` reset (T-084).** Found by reading the
  file directly, not inferred — no other file in this baseline imports the singleton, so
  it produces no observable failure yet, but it's the exact shape of leak this repo's
  isolation contract exists to prevent. Trivial fix (add a cleanup hook or move the
  assertion onto a factory-created instance). Per project memory, PR #2788 already
  addresses this and may land before T-084 is dispatched — see Milestone 1/2 and Open
  risks.
- **New this cycle (2026-08-22): four of five new functional surfaces landed with
  matching coverage at build time.** Okta OIDC auth (`okta-auth-client.ts`), GitHub App
  manifest-flow provisioning (`github-app-provisioning-client.ts`), and PR audit-trail
  events (`pr-transition-diff.ts`, PSA-1.2) all shipped with unit/integration/smoke
  coverage in the same commits — no gap, no task needed for those three. The 8-commit
  review/patch-candidacy fix cluster (`check-review.ts`, `compute-unaddressed-findings.ts`,
  `review.md`) likewise landed with matching test extensions across all 8 commits
  (spot-checked 3 of 8 directly) — no gap.
- **`scripts/check-coverage-no-decrease.ts` deleted this cycle, paired cleanly with its
  tests** — `2ab1a4cb` removed the script and both its test files in the same commit, no
  orphaned test left behind. Confirms T-083's stage-3 coverage-promotion is code-complete
  (branch-protection swap status remains open, see Coverage Gate section).
- **2026-08-16 cycle's finding (preserved for continuity): the dual coverage-gate tooling
  landed with matching coverage at build time.** `check-coverage-gate.ts`,
  `check-coverage-no-decrease.ts` (since deleted, see above), `coverage-tool-dispatch.ts`,
  and `check-coverage.ts`'s `CoverageParser` refactor all shipped with test coverage in
  the same commits — no gap, no task needed.
- **1 carried-forward, low-priority, non-gap item:** `admin/src/clock.ts` has no
  dedicated test file — consistent with the established convention shared by all sibling
  `Clock` implementations across the repo (near-zero logic, proven via consumers). Not
  scored as a gap; no task minted.
- **Infra gap: none net-new** beyond T-084 (the new Promote/deepen item, see Milestone 1/2).
  Runner config, CI shape, Postgres containers, `Clock` helpers, recorded-fixture
  doubles, PR template all confirmed unchanged and correct. The isolation-contract
  audit's own pattern set has a documented blind spot (singleton-mutation leaks vs.
  literal `global.*` overrides) — see Open risks for a recommendation to widen it.

### Speed delta

**Tier 1 — aggregate-vs-budget (leads; always populated):**

| Aggregate wall-clock | Budget | Ratio / time-to-close |
|---|---|---|
| 2m38s | <15 min (Full PR pipeline) | ~18% of budget — no time-to-close concern |

**Tier 2 — per-layer ratio (conditional on Tier 2 having triggered):**

**Tier 2 triggered:** no. Per-layer numbers are not fabricated here — Tier 1's aggregate
is the complete, sufficient measurement this cycle. Growth trajectory (283 → 286 files
per CI, 6,920 → 7,190 tests) is steady with no budget concern at any point measured.

## 4. Roadmap (five milestones)

### Milestone 1 — Infrastructure baseline
**DOD:** Runners, local substitutes, CI pipeline shape in place. No new tests yet — just the rails.

**T-083 (auto-emitted stage-3 coverage-check promotion) is now `done` — its code-side
half landed this cycle.** No new Milestone 1 task minted. Naming convention +
runner-exclusion config (`docs/test-readiness/naming.md`, `bunfig.toml`'s
`pathIgnorePatterns`) is established and re-verified unchanged this cycle. CI pipeline
shape matches `test-system.md`'s documented target exactly. Postgres service containers,
`Clock` injection, and recorded-fixture doubles are all confirmed in place. T-074
(speed-baseline Postgres refresh) remains `cancelled` — no successor task needed given
Tier 1's CI-sourced measurement is a complete, sufficient answer.

**T-083's branch-protection swap remains an open, human-executed follow-up — not
re-opened as a new task** (its code-side scope is complete; the settings swap was always
documented as out of scope for the task itself, per `check-coverage.ts`'s own header
comment). See Open risks for the verification step a human should run.

`recorded_fixture_automation` is undeclared in root `CLAUDE.md` (re-verified this cycle —
no `## Recorded-fixture automation` section present, defaults to `planned` per this
cycle's read mechanism) — no repo-config declaration opts this repo out of the
recorded-fixture-refresh-loop pattern. That loop remains correctly **deferred, not
scheduled** (`test-system.md`'s Repo configuration section) — sequenced after the manual,
hand-authored-fixture workflow matures, not alongside it. No further M1 task minted for
it this cycle.

### Milestone 2 — Critical-path coverage
**DOD:** 100% of `critical` tier inventory items have passing tests at the prescribed layer.

**One task this cycle: T-084.** `agent-slack-membership-ref.ts` is ranked `critical` in
`test-inventory.md` (a Slack access-control boundary) — its test file exists and covers
the right behaviors, but the singleton-scoped test's missing `afterEach` reset means the
*coverage's own isolation* isn't sound, which this milestone's DOD implicitly requires
("passing tests" that don't leak state into sibling suites). Not a missing-coverage gap
(the assertions are correct) — a test-hygiene fix.

| Task | Files to touch | Layer | Bucket origin | Expected outcome | Verification command | Depends on |
|---|---|---|---|---|---|---|
| T-084 | `agent/src/agent-slack-membership-ref.unit.test.ts` | unit | Promote/deepen | Add an `afterEach` (or equivalent) that resets `agentSlackMembershipRef` to its fail-open default (`{restrict: false, emails: []}`) after the singleton-scoped `describe` block's test, closing the cross-suite leak risk flagged in `test-migration.md`'s Method note step 1 | `bun test agent/src/agent-slack-membership-ref.unit.test.ts` passes; grep confirms no other test file's assertions changed when run in a different file order (`bun test --shuffle` or equivalent, if available) | none |

Sizing check: single file, single concern (add a cleanup hook), trivially under the
1000-line cap. **Before dispatching T-084, check whether PR #2788 (per project memory)
has already landed** — it's tracked as addressing this exact leak; if it has, this task
is a duplicate and should be closed/skipped rather than re-done.

### Milestone 3 — Canary suite live
**DOD:** canary not applicable -- deploy_model=direct

Zero tasks, same as every prior cycle. See `test-system.md`'s Canary execution contract
section for what would need to change before this applies.

### Milestone 4 — High-tier coverage
**DOD:** 100% of `high` tier inventory items covered.

**Already satisfied — zero real-gap tasks.** T-080, the sole item that occupied this
milestone since 2026-07-30 (journey #5, local HITL dev-loop bootstrap's full clone → seed
→ boot → poll sequence), is now resolved (PR #2644) — the pure-planning half
(`computeMissingClones`, `computeProvisionPlan`, `buildHitlConfig`, `buildPreflightSteps`,
`buildServiceSpecs`) is unit-tested via `hitl.bootstrap.unit.test.ts`, and the composed
sequencing/failure-propagation behavior is integration-tested via
`hitl.integration.test.ts`. This closes the repo's longest-standing open test-readiness
item — carried forward across every cycle since 2026-07-30.

`admin/src/clock.ts` remains the sole other Net-new item from Phase 3 — carried forward,
unchanged, explicitly a non-gap (established `Clock`-file convention, near-zero logic,
proven via consumers). No task minted for it, same as every prior cycle.

### Milestone 5 — Cleanup
**DOD:** Zero tests in `rebuild` or `delete (redundant)` buckets. CI passes.

**Already satisfied — zero tasks.** Phase 3 found zero `rebuild` and zero `delete
(redundant)` items this cycle, the same steady state as every prior pass. No new
generalizable learning surfaced this cycle warranting a `# Harness TODO`.

## 5. Task list

Flat, ordered, agent-executable. **One genuinely new task this cycle: T-084** (Phase 3's
Promote/deepen finding — `agent-slack-membership-ref.unit.test.ts`'s missing `afterEach`
reset). T-083 (the auto-emitted stage-3 coverage-check promotion task) moved to `done`
this cycle, not re-minted.

| ID | Milestone | Files to touch | Layer | Bucket origin | Expected outcome | Verification command | Depends on |
|---|---|---|---|---|---|---|---|
| T-084 | 2 | `agent/src/agent-slack-membership-ref.unit.test.ts` | unit | Promote/deepen | Add an `afterEach` resetting `agentSlackMembershipRef` to its fail-open default after the singleton-scoped test | `bun test agent/src/agent-slack-membership-ref.unit.test.ts` | none |

**Before dispatching T-084, confirm PR #2788 hasn't already landed this exact fix** (per
project memory it's tracked as addressing the same singleton test leak) — if it has,
close T-084 as a duplicate rather than re-doing the work.

**T-083 moved to `done` this cycle** — its code-side change (raise `THRESHOLD_LINES` to
90, delete `check-coverage-no-decrease.ts`) landed. Its branch-protection swap remains an
open, human-executed follow-up, not a re-opened task — see Open risks.

**Resolved (re-verified via direct task-store query this cycle, not carried forward from
the prior doc):** T-073 through T-075, T-080, T-081, T-082 all confirmed at their
terminal/near-terminal status from the 2026-08-18 doc, unchanged (`done`/`cancelled`/
`deployed`/`deploying` as applicable — see the Status table above for exact values).
T-076 (`agent-type-manifest-loader` real-disk split — **deploying**), T-077
(`check-review.ts` fail-open catch-path test — **deploying**), T-078 (injectable ref +
never-synced equivalence test — **deploying**), T-079 (`generate-agent-type-schema.ts`
coverage class-level decision — **deploying**) remain parked, unchanged since 2026-08-06,
all four confirmed merged 2026-07-25 through 2026-07-28 with `deployedAt: null` still
unset — **9 consecutive cycles with zero status movement**, see Open risks below for the
escalation.

## 6. Open risks

- **T-076 through T-079 have now shown zero status movement for 9 consecutive
  test-readiness cycles (parked at `deploying`).** All four merged 2026-07-25 through
  2026-07-28 (now 3.5-4 weeks ago) with `deployedAt: null` still unset, and none carries a
  `note` field explaining a specific transient failure to reconcile against (unlike T-081,
  which did, and turned out to be a stale-record case rather than a genuine block; T-079
  does carry an unrelated human note recording a scope decision, not a deploy-status
  explanation). This cycle re-confirmed the four merge commits (`15663f3c`, `778b3ba1`,
  `45435444`, `9a1bee28`) remain ancestors of current `origin/main` — the code is
  unambiguously present in the current tree, and this repo's `deploy_model: direct` means
  every merge to main ships via the next Helm chart/image build with no staging gate to
  get stuck behind. This docs-refresh pipeline still has no first-party way to confirm a
  specific build actually rolled out, so this cycle again did **not** force-patch these
  four to `done`. **Escalating again — now a 9-cycle-old (~4-week) stale-status
  pattern**: recommend a human (or the `deploy`/`patch` pipeline's own diagnostics)
  directly check GitHub Actions deploy-workflow runs following each of the four merge
  commits (PRs #2207, #2227, #2228, #2264) to confirm an actual image build/rollout
  completed — if so, the fix is the same stale-record PATCH T-081 got; if not, this is a
  real, month-old deploy-pipeline stall that predates and is unrelated to any recent
  cycle's work. **Given the pattern is now 9 cycles deep with zero movement, this docs
  pipeline recommends the next human touchpoint treat this as a standing item to actively
  resolve, not just re-confirm.**
- **New this cycle: T-083's branch-protection swap needs direct human verification.**
  T-083's code-side change (raise `check-coverage.ts`'s line threshold to 90, delete the
  superseded never-decrease script) landed and moved the task to `done`, but its own
  source comment explicitly scopes the branch-protection required-check swap as a
  separate, human-driven step. This cycle could not verify that step via
  `gh api repos/app-vitals/shipwright/branches/main/protection` (403, insufficient token
  scope) — recommend a human run that check (or the equivalent GitHub UI view) directly
  and confirm the required status check now enforces the hard ≥90% gate rather than a
  stale reference to the deleted never-decrease job.
- **New this cycle: the isolation-contract audit's grep pattern set has a documented blind
  spot.** `agent-slack-membership-ref.unit.test.ts`'s singleton-mutation leak (T-084)
  doesn't match any of the three existing signals (`mock.module(`, `global.fetch =`,
  `global.console =`) because it mutates a named export, not a literal `global.`
  assignment. Recommend a future cycle add a fourth signal scanning for direct
  `.set(`/`.reset(`/mutation calls on this repo's known singleton-ref exports
  (`agentRposRef`, `agentAuthorAllowlistRef`, `agentSlackMembershipRef`, and future
  `agent-*-ref.ts` additions) with no paired `afterEach`/`afterAll` in the same file —
  this is a Phase 3 process improvement, not itself a task against the codebase.
- **The compile-time-only "type-parity check" pattern (`blocked-by-type-parity.check.ts`)
  still needs a taxonomy decision.** Not a coverage gap — confirmed wired into CI and
  passing — but it doesn't fit any of the five CLAUDE.md-declared suffixes or the
  unofficial sixth "shell" layer. `test-system.md` deliberately declines to formalize a
  seventh layer on a single instance; revisit if a second such file appears.
- **The bash "shell" layer still needs a formal documentation decision.** Carried forward
  unchanged across several cycles now (first flagged 2026-07-31, re-verified present and
  unchanged this cycle): `.github/workflows/lib/chart-tag-utils.sh` / `chart-drift-check.sh`
  and their `test-*.sh` siblings are genuinely tested code units that fall entirely
  outside the `bun test` runner and the five-suffix convention declared in root
  `CLAUDE.md`. Coverage itself is not in question — the open question is purely whether a
  human wants to formally document this as a sixth layer in `CLAUDE.md`'s `## Test
  conventions` table and `docs/test-readiness/naming.md`, or confirm it should stay an
  unofficial exception. Not urgent; not blocking any task above.
- **Recorded-fixture coverage breadth** (~9 remaining `Http*Client` modules that could
  extend the `Recorded*Client` pattern) remains a Phase 2 design-level backlog item, not
  a migration-bucket verdict on any existing file. No task emitted this cycle since
  Phase 3 found no test currently missing coverage because of it.
- **`agent/src/piper-voice.ts` reclassification watch item** (unit today, integration
  once Piper synthesis lands) — still not landed as of this cycle; flagged so a future
  Phase 3 doesn't silently miss the transition.
- **Recorded-fixture maintenance loop remains deferred** (`repo-config/SKILL.md` §5) —
  correctly not scheduled this cycle; sequence after fixture-authoring work matures. Not
  wired into T-084 or any other task this cycle — T-084 is a test-isolation fix, not a
  credential-provisioning automation task, so it doesn't trip the "never automate a
  never-proven capability" failure mode this note exists to block.

---

## Handoff

This document is intended for execution by:
- An engineer working through the task list manually, OR
- `shipwright /dev-task` consuming the task list as a queue.
