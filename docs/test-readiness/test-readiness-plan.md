# Test Readiness Plan

> Phase 4 synthesis. The executable roadmap from where we are now to a test suite trustworthy enough for autonomous programming.
>
> Generated by `/test-roadmap` on 2026-09-21 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-2026-09-21`), consuming `test-inventory.md` (commit
> `c728d5afc`), `test-system.md` (commit `e8d9ec61a`), and `test-migration.md` (commit
> `0618d393d`) — all three refreshed this cycle against a small 3-day/18-commit top-up (6
> non-routine commits after excluding chart/plugin-version-sync and docs-freshness-anchor
> churn) since the 2026-09-18 baseline `eff5b503`, landing at this cycle's exact HEAD
> `08cab0b33`. Headline delta: a one-off PR-origin historical backfill script
> (`agent/src/pr-origin-backfill.ts`, POB-1.1), a dashboard KPI swap, Sessions-list
> Follow-button wiring, and `dev-task`-only resume wiring — all either landed with matching
> test coverage in the same commit/PR or fold into already-designed patterns. Phase 3's one
> genuine finding this cycle: `plugins/shipwright/commands/dev-task.content.test.ts` lacks
> a sweeping regression-guard assertion that every `worktree add`/`worktree remove`
> invocation in `dev-task.md` uses the absolute `${VAR:-$HOME/...}` fallback pattern — the
> first non-empty Promote/deepen finding in eight cycles, closing a bug class that had
> recurred 5+ times before being root-caused (fix: PR #3545). This synthesis mints **one
> new task, T-085**.

## Coverage Gate

The headline machine-checkable verdict — computed by `scripts/check-coverage-gate.ts`
(CGT-1.2), rendered verbatim below.

Inputs sourced per Process step 8: `linePct`/`lineSource` from this cycle's live CI pull
([run 35557736074](https://github.com/app-vitals/shipwright/actions/runs/35557736074)
@ `08cab0b33`, this cycle's exact HEAD, fetched directly via `gh run list`/`gh run view --log`
per `test-migration.md`'s Speed measurement section); `featurePct`/`featureSource` from
`test-inventory.md`'s `feature_coverage_pct` (unchanged this cycle — 100%, 24/24 features,
no new feature since Sessions landed 2026-09-18). Computed via:
```bash
echo '{"featurePct": 100, "featureSource": "docs/test-readiness/test-inventory.md", "linePct": 92.46, "lineSource": "CI run https://github.com/app-vitals/shipwright/actions/runs/35557736074 @ 08cab0b33"}' \
  | bun run scripts/check-coverage-gate.ts
```
Actually run (not hand-computed):
```json
{"feature_coverage_pct":100,"feature_coverage_source":"docs/test-readiness/test-inventory.md","line_coverage_pct":92.46,"line_coverage_source":"CI run https://github.com/app-vitals/shipwright/actions/runs/35557736074 @ 08cab0b33","verdict":"READY","targets":{"feature":90,"line":90}}
```

| Field | Value |
|---|---|
| Feature coverage | 100% (target 90%) — source: docs/test-readiness/test-inventory.md |
| Line coverage | 92.46% (target 90%) — source: CI run https://github.com/app-vitals/shipwright/actions/runs/35557736074 @ 08cab0b33 |
| **Verdict** | **READY** |

**Stage-3 coverage-check promotion — `done`, unchanged this cycle.** `line_coverage_pct`
(92.46, essentially flat vs. 2026-09-18's 92.47%) remains above the 90% threshold
`repo-config/SKILL.md`'s 3-stage coverage-check lifecycle gates on. **T-083** ("Promote
coverage required check from never-decrease to hard ≥90% gate") remains **`done`** per
direct task-store query this cycle — no further action needed, no new stage-3 emission
(Process step 9's dedup scan found T-083 already filed and `done`).

## Status of the prior cycle's plan (2026-09-18) and everything since

Queried task-store directly (`$SHIPWRIGHT_TASK_STORE_URL/tasks?repo=app-vitals%2Fshipwright&limit=500`,
paginated across 3 pages — `offset=0`, `offset=500`, `offset=1000` — 1,266 tasks total
scanned this cycle across all repos in the store). Filtered to `test-t-<NNN>-shipwright`
IDs: the highest existing ID is **`test-t-084-shipwright`** — no `test-t-085` or higher
exists yet, confirming the prior cycle's reservation was never consumed. **`starting_offset
= 85`** — this cycle's one new task begins at **T-085**.

Re-verified the status of every previously-tracked ID (not assumed from the old doc):

| Task | Status | Title |
|---|---|---|
| `test-t-076-shipwright` | **deployed** (unchanged) | Split `agent-type-manifest-loader.unit.test.ts`'s real-disk cases into a new `.integration.test.ts` file |
| `test-t-077-shipwright` | **deployed** (unchanged) | `check-review.ts`'s `isBundleComplete` fail-open catch-path test |
| `test-t-078-shipwright` | **deployed** (unchanged) | Make `isAuthorAllowed`'s ref dependency injectable + never-synced equivalence test |
| `test-t-079-shipwright` | **deployed** (unchanged) | `scripts/generate-agent-type-schema.ts` test-coverage class-level decision |
| `test-t-080-shipwright` | **deployed** (unchanged) | HITL dev-loop bootstrap's full clone→seed→boot→poll sequence, injected `gh` exec fake |
| `test-t-081-shipwright` | **done** (unchanged) | `TaskService.claim()`'s concurrent-race close (real Postgres) |
| `test-t-082-shipwright` | **deployed** (unchanged) | `traceReviewCandidacyDecision`'s exclusion checks proven correct in 3+-condition combination |
| `test-t-083-shipwright` | **done** (unchanged) | Promote coverage required check from never-decrease to hard ≥90% gate |
| `test-t-084-shipwright` | **deployed** (unchanged) | Add `afterEach` reset to `agentSlackMembershipRef` singleton test |

**All 9 previously-tracked IDs remain at a terminal status** (`deployed`/`done`). No
regression, no status churn since the 2026-09-18 pass. Nothing to carry forward from the
open-task perspective — every prior-cycle task this pipeline ever minted is now closed out.

**One new task minted this cycle: T-085 (`test-t-085-shipwright`, once queued by
`/test-fix`).** Sourced directly from `test-migration.md`'s single Promote/deepen finding
(see Method note step 3 and the Tests — bucketed → Promote/deepen table in that document) —
`plugins/shipwright/commands/dev-task.content.test.ts` needs one new `it()` block that
regex-extracts every `git -C ... worktree add`/`worktree remove` invocation in
`dev-task.md` and asserts each uses the `${SHIPWRIGHT_REPO_DIR:-$HOME/src}` /
`${SHIPWRIGHT_WORKTREE_DIR:-$HOME/worktrees}` (or equivalent absolute-fallback) pattern,
never a bare relative default. No production code change — the underlying bug (PR #3545)
is already fixed; this closes the regression-guard gap so the same bug class (already
recurred 5+ times per this repo's own incident history) can't silently reintroduce itself.

## 1. Where we are now

### Layer coverage map

| Layer | Existing tests | Inventory items | Coverage gap |
|---|---|---|---|
| Unit | 187 `.unit.test.ts` | Dominant category-1 surface (`lib/`, decision/classification logic, mutable-box refs, crypto helpers, parsing/validation, coverage-gate tooling, Sessions' `session-rollup.ts`, the new `pr-origin-backfill.ts`) | None |
| Integration | 83 `.integration.test.ts` | Every category-2/5 service-boundary and external-integration item, incl. Sessions' `session-service.ts`/`session-retention-reaper.ts`, the Prisma 7 `prisma-client.ts` extraction (3 services) | None |
| Smoke | 54 `.smoke.test.ts` | Every category-3 HTTP route across 5 services, incl. `routes/sessions.ts` + admin Sessions UI wire-contract coverage | None |
| E2E | 26 `site/tests/*.spec.ts` + 7 `{admin,metrics}/e2e/*.e2e.ts` | 4 of 5 identified journeys are browser-driven or partially so (journey #5, HITL bootstrap, is integration-composed, not E2E) | None |
| Content | 46 `.content.test.ts` | `plugins/shipwright/{commands,agents,skills,references,test}/*` plus root-level content tests | **1 depth gap — see Promote/deepen below (T-085)** |
| Shell (unofficial 6th layer) | 2 `test-*.sh` bash assertion suites | `.github/workflows/lib/chart-tag-utils.sh`, `chart-drift-check.sh` | None — only a documentation-completeness question open (see Open risks) |
| Compile-time-only (unformalized) | 1 `*.check.ts` (`blocked-by-type-parity.check.ts`) | `admin/src/blocked-by-type-parity.check.ts` | None — wired into CI's `typecheck` step; only a taxonomy question open (see Open risks) |

**Total: 403 test files** (370 `bun test`-scanned: 187 unit + 83 integration + 54 smoke +
46 content, matching this cycle's live CI run's own "370 files" scan report exactly; + 26
`site/tests/*.spec.ts` + 7 Playwright `*.e2e.ts`) — up +2 from the 2026-09-18 pass's 401:
`agent/src/pr-origin-backfill.unit.test.ts` and `agent/src/sessions.unit.test.ts`, both new
this cycle, both verified Reuse-as-is.

**`## Features` section in `test-inventory.md`: 24 features, unchanged (100%, 24/24).**
No new feature this cycle — the new inventory row (`pr-origin-backfill.ts`) folds into the
existing "Agent — work selection & dispatch" feature alongside its sibling `pr-census.ts`.

### Local-runnable status

**100%** of the `bun test`-scanned files run locally with zero external network. This
cycle's isolation-contract grep sweep (`mock.module(`, `global.fetch =`, `global.console =`,
stray `DATABASE_URL` inside `*.unit.test.ts`) found zero real violations — consistent with
every prior cycle. No test is blocked on an external service.

### Canary status

**Not applicable.** Root `CLAUDE.md` declares `## Deploy model: direct` — no
staging/production GitHub Environments, no deploy→canary→promote pipeline. Unchanged
finding across every cycle to date. See Milestone 3.

### Speed status (Tier 1 aggregate vs. budget; Tier 2 not triggered)

**Tier 1 — aggregate (measured this pass, via live CI history, fetched directly via
`gh run list`/`gh run view --log`):**

| Aggregate wall-clock | Budget | Ratio |
|---|---|---|
| 1m37s ([run 35557736074](https://github.com/app-vitals/shipwright/actions/runs/35557736074), 2026-09-21T03:30:17Z–03:32:11Z, commit `08cab0b33` — this cycle's exact HEAD) | <15 min (Full PR pipeline) | ~11% of budget |

Fully green run — no failing jobs. `bun test` step itself: 23.04s. **9,271 tests across
370 files** (+52 tests / +2 files vs. the 2026-09-18 pass, proportionate to this cycle's
small 7-commit delta — in sharp contrast to the prior pass's +1,349-test outlier jump).
Coverage held essentially flat: **92.46%** lines (35715/38629), **91.01%** functions
(2965/3258), against the 90%/89% thresholds — `✅ Coverage gate passed`, logged directly.
Test-count growth trajectory: …→ 7,870 (2026-09-02) → 9,219 (2026-09-18) → **9,271
(2026-09-21, this pass)** — no anomaly, no padding.

**Tier 2 triggered:** no — the escalation formula (aggregate wall-clock >7.5 min, sustained
across 2 consecutive measurements) is nowhere near tripped (~11% of budget, well below the
50% escalation threshold). No per-layer p95 table is populated below, per the SKILL's
explicit guidance against fabricating per-layer numbers when only the Tier 1 aggregate is
available.

### "Rebuild" / "Delete (redundant)" debt

| Bucket | Count | Effort |
|---|---|---|
| Rebuild | 0 | — |
| Promote / deepen | 1 | trivial (T-085) |
| Delete (redundant) | 0 | — |
| Net-new | 1 | trivial, optional |

Rebuild and Delete remain zero this cycle. **Promote/deepen gains its first finding in
eight cycles** — `dev-task.content.test.ts`'s missing sweeping worktree-path-fallback
regression guard (see T-085 above and in the Task list below). Net-new remains at 1 — only
the long-standing, explicitly-optional `admin/src/clock.ts` item, unchanged.

## 2. Where we want to be

- **Framework matrix** — already fully realized: `bun test` (unit/integration/smoke/content),
  Playwright (E2E, 3 surfaces), plain `bash` (the shell layer). No framework changes
  needed.
- **Local-substitute map** — every dependency named in the inventory has a working local
  substitute (recorded-fixture doubles for internal/third-party HTTP, real Postgres via
  CI service container for the three Prisma-backed services, a real locally-launched
  Chromium binary for `agent/src/browser.ts`). No blockers.
- **Coverage targets** — single 90/89 line/function CI floor (`scripts/check-coverage.ts`),
  fully enforced (branch-protection swap status: see Coverage Gate section). Tiered targets
  (critical ~95%, high ~80%, medium spot-check) direct prioritization, not a second CI
  gate.
- **Canary suite definition** — not applicable under the current `direct` deploy model;
  deferred until a staged-deploy model exists.
- **Speed budgets per layer** — Unit <50ms p95 / Integration <2s / Smoke <5s / E2E <30s /
  full PR pipeline <15min. Tier 1 aggregate confirms comfortable headroom (~11% of
  budget); no per-layer breakdown needed since Tier 2 has never triggered.
- **Zero `rebuild` / `delete (redundant)` buckets, and every Promote/deepen finding closed
  promptly** — Rebuild/Delete remain zero; the one open Promote/deepen finding (T-085) is
  the only item standing between this cycle and a fully clean state.
- **Every critical- and high-tier inventory item covered at its prescribed layer, and every
  content-layer regression-guard assertion sweeping enough to close a recurring bug class
  rather than pinning one historical instance** — the T-085 finding is exactly this
  standard being applied for the first time to a content-layer test; closing it completes
  the standard's application across all layers, not just unit/integration.

## 3. The gap

- **No missing layers.** All six layers (five `bun test`-declared plus the unofficial
  shell layer) in active use with the correct framework per layer, plus one
  deliberately-unformalized compile-time-only pattern (`blocked-by-type-parity.check.ts`)
  that has no coverage gap despite not fitting the existing taxonomy.
- **Wrong-layer tests:** 0.
- **External-only deps with no local substitute:** none.
- **New this cycle: one content-layer depth gap.** `plugins/shipwright/commands/dev-task.content.test.ts`
  has a narrower existing assertion (line ~712, checking for raw `{repo}`-style relative
  paths) that does **not** cover every `worktree add`/`worktree remove` invocation's
  fallback-default pattern — the exact gap that let the PR #3545 bug (relative
  `${SHIPWRIGHT_REPO_DIR:-repos}` default landing worktrees nested inside
  `repos/<repo>/worktrees/...` instead of the top-level `worktrees/` dir) ship, and that
  had already recurred 5+ times across prior sessions before being root-caused. Closing
  this (T-085) converts a one-off pinned regression test into a sweeping structural
  guarantee. Everything else in this cycle's inventory delta (the dashboard KPI swap, the
  Sessions-list Follow button wiring, `dev-task`-only resume wiring) landed with matching
  coverage at the correct layer in the same commit/PR — no gap.
- **1 carried-forward, low-priority, non-gap item:** `admin/src/clock.ts` has no
  dedicated test file — consistent with the established convention shared by all sibling
  `Clock` implementations across the repo (near-zero logic, proven via consumers). Not
  scored as a gap; no task minted.
- **T-083's stage-3 coverage-promotion, including the branch-protection swap, remains
  fully confirmed done** — see Coverage Gate section above. No remaining open question on
  this item.
- **Infra gap: none.** Runner config, CI shape, Postgres containers, `Clock` helpers,
  recorded-fixture doubles, PR template all confirmed unchanged and correct this cycle.

### Speed delta

**Tier 1 — aggregate-vs-budget (leads; always populated):**

| Aggregate wall-clock | Budget | Ratio / time-to-close |
|---|---|---|
| 1m37s | <15 min (Full PR pipeline) | ~11% of budget — no time-to-close concern |

**Tier 2 — per-layer ratio (conditional on Tier 2 having triggered):**

**Tier 2 triggered:** no. Per-layer numbers are not fabricated here — Tier 1's aggregate
is the complete, sufficient measurement this cycle. Growth trajectory (370 files, 9,219 →
9,271 tests since the 2026-09-18 baseline) is proportionate to this cycle's small
7-commit delta with no budget concern.

## 4. Roadmap (five milestones)

### Milestone 1 — Infrastructure baseline
**DOD:** Runners, local substitutes, CI pipeline shape in place. No new tests yet — just the rails.

**Already satisfied — zero tasks this cycle.** T-083 (auto-emitted stage-3 coverage-check
promotion) remains `done`. Naming convention + runner-exclusion config
(`docs/test-readiness/naming.md`, `bunfig.toml`'s `pathIgnorePatterns`) re-verified
unchanged. CI pipeline shape matches `test-system.md`'s documented target exactly. Postgres
service containers, `Clock` injection, and recorded-fixture doubles all confirmed in place.

### Milestone 2 — Critical-path coverage
**DOD:** 100% of `critical` tier inventory items have passing tests at the prescribed layer.

**Already satisfied — zero tasks this cycle.** No open items carried in from the
2026-09-18 pass (it closed clean apart from the reservation now consumed by T-085, which
is a content/high-tier item, not critical). No new critical-tier gap found this cycle —
`agent/src/pr-origin-backfill.ts` (medium tier, mirrors its sibling `pr-census.ts`) has
matching coverage at its prescribed layer, landed in the same PR as the production code.

### Milestone 3 — Canary suite live
**DOD:** canary not applicable -- deploy_model=direct

Zero tasks, same as every prior cycle. See `test-system.md`'s Canary execution contract
section for what would need to change before this applies.

### Milestone 4 — High-tier coverage
**DOD:** 100% of `high` tier inventory items covered.

**One task this cycle: T-085.** `plugins/shipwright/commands/dev-task.content.test.ts`
is an already-`Reuse`-bucketed, high-value content test guarding a command file with a
5+-recurrence bug-class history — deepening it with a sweeping regression-guard assertion
is squarely a high-tier-coverage-completeness action, not a new gap in the strict
critical/high tiering sense (the file already has passing coverage; this closes a depth
gap in that coverage). All other prior high-tier items (T-080's HITL bootstrap sequencing
among them) remain resolved, unchanged since their respective resolution cycles.

`admin/src/clock.ts` remains the sole other Net-new item from Phase 3 — carried forward,
unchanged, explicitly a non-gap. No task minted for it, same as every prior cycle.

### Milestone 5 — Cleanup
**DOD:** Zero tests in `rebuild` or `delete (redundant)` buckets. CI passes.

**Already satisfied — zero tasks.** Phase 3 found zero `rebuild` and zero `delete
(redundant)` items this cycle, the same steady state as every prior pass.

## 5. Task list

Flat, ordered, agent-executable. Format: `T-NNN | M# | files | layer | bucket | outcome | verify`.

| ID | Milestone | File(s) | Layer | Bucket | Expected outcome | Verify |
|---|---|---|---|---|---|---|
| **T-085** | M4 | `plugins/shipwright/commands/dev-task.content.test.ts` (test-only; no production file touched — the underlying bug is already fixed by PR #3545) | content | Promote / deepen | New `it()` block regex-extracts every `git -C ... worktree add`/`worktree remove` invocation from `plugins/shipwright/commands/dev-task.md` and asserts each one's default value uses the absolute-fallback form (`${SHIPWRIGHT_REPO_DIR:-$HOME/src}` / `${SHIPWRIGHT_WORKTREE_DIR:-$HOME/worktrees}` or equivalent), never a bare relative default (`${VAR:-repos}` / `${VAR:-worktrees}`). Mirrors the file's existing narrower assertion at line ~712, widened from one historical instance to every invocation in the file — a future PR that reintroduces a relative default anywhere in `dev-task.md` now fails CI instead of silently recurring for a 6th time. | `bun test plugins/shipwright/commands/dev-task.content.test.ts` |

**Total open tasks in this plan's flat task list: 1.**

**Resolved (re-verified via direct task-store query this cycle):** all 9 previously-tracked
IDs (T-076 through T-084) remain at a terminal status (`deployed`/`done`) — see the Status
section above. None require any action this cycle.

## 6. Open risks

- **New this cycle — none.** No new open risk surfaced. T-085 is a well-scoped, trivial,
  fully-specified task, not an open question requiring human judgment.
- **The isolation-contract audit's grep pattern set still has a documented blind spot.**
  The one known instance of this leak class (T-084, `agent-slack-membership-ref.unit.test.ts`)
  was fixed back in the 2026-08-23 cycle via a manual diff read, not an automated sweep —
  the pattern set still can't catch a recurrence of this leak class on its own. Recommend a
  future cycle add a fourth signal scanning for direct `.set(`/`.reset(`/mutation calls on
  this repo's known singleton-ref exports (`agentReposRef`, `reviewAuthorAllowlistRef`,
  `agentSlackMembershipRef`, `loopJobsRef`, `patchAuthorAllowlistRef`, and future
  `agent-*-ref.ts` additions) with no paired `afterEach`/`afterAll` in the same file — a
  Phase 3 process improvement, not itself a task against the codebase. Carried forward
  unchanged, no new instance found this cycle.
- **The compile-time-only "type-parity check" pattern (`blocked-by-type-parity.check.ts`)
  still needs a taxonomy decision.** Not a coverage gap — confirmed wired into CI and
  passing — but it doesn't fit any of the five CLAUDE.md-declared suffixes or the
  unofficial sixth "shell" layer. `test-system.md` deliberately declines to formalize a
  seventh layer on a single instance; revisit if a second such file appears.
- **The bash "shell" layer still needs a formal documentation decision.** Carried forward
  unchanged across several cycles (first flagged 2026-07-31, re-verified present and
  unchanged this cycle): `.github/workflows/lib/chart-tag-utils.sh` / `chart-drift-check.sh`
  and their `test-*.sh` siblings are genuinely tested code units that fall entirely
  outside the `bun test` runner and the five-suffix convention declared in root
  `CLAUDE.md`. Coverage itself is not in question — the open question is purely whether a
  human wants to formally document this as a sixth layer in `CLAUDE.md`'s `## Test
  conventions` table and `docs/test-readiness/naming.md`, or confirm it should stay an
  unofficial exception. Not urgent; not blocking any task above.
- **Recorded-fixture coverage breadth** (remaining `Http*Client` modules that could extend
  the `Recorded*Client` pattern) remains a Phase 2 design-level backlog item, not a
  migration-bucket verdict on any existing file. No task emitted this cycle since Phase 3
  found no test currently missing coverage because of it.
- **`agent/src/piper-voice.ts` reclassification watch item** (unit today, integration once
  Piper synthesis lands) — still not landed as of this cycle; flagged so a future Phase 3
  doesn't silently miss the transition.
- **`admin/src/session-scope.ts`'s visibility predicate remains not yet wired into most
  Session routes.** Documented, intentional interim state per
  `docs/session-notifications.md` — the predicate itself is fully unit-tested, so this is
  an application-logic sequencing gap, not a test-coverage gap. No task minted (this
  pipeline doesn't mint tasks for application logic, only test coverage) — carried forward
  so a human continues tracking the wiring-completion timeline.
- **Recorded-fixture maintenance loop remains deferred** (`repo-config/SKILL.md` §5) —
  correctly not scheduled this cycle; sequence after fixture-authoring work matures.

---

## Handoff

This document is intended for execution by:
- An engineer working through the task list manually, OR
- `shipwright /dev-task` consuming the task list as a queue, OR
- `/shipwright:test-fix` (Phase 5), which queues every row in the Task list section above
  as a task-store task.
