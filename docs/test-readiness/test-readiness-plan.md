# Test Readiness Plan

> Phase 4 synthesis. The executable roadmap from where we are now to a test suite trustworthy enough for autonomous programming.
>
> Generated by `/test-roadmap` on 2026-09-23 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-20260923`), consuming `test-inventory.md`, `test-system.md`,
> and `test-migration.md` — all three freshly regenerated this run against this cycle's exact
> HEAD, commit `38a492e1a`. This is a small, quiet cycle: a 2-day/30-commit top-up (6
> non-routine commits after excluding chart/plugin-version-sync churn) since the 2026-09-21
> baseline (`08cab0b33`). Headline finding: **every single non-routine commit this window
> landed with matching, same-commit, correctly-layered test coverage** — the
> `run_in_background: false` background-dispatch-pinning cluster across `patch.md`,
> `dev-task.md`, `review.md`, the new `research.md`, and `agent/workspace/CLAUDE.md.template`,
> plus the T-085 regression-guard widening (now closed). Phase 3's bucket tables are clean
> across the board: zero Promote/deepen, zero Rebuild, zero Delete (redundant), and the sole
> Net-new item remains the long-standing, explicitly-optional `admin/src/clock.ts` trivial
> case. **This synthesis mints zero new tasks.**

## Coverage Gate

The headline machine-checkable verdict — computed by `scripts/check-coverage-gate.ts`,
rendered verbatim below.

Inputs sourced per Process step 8: `linePct`/`lineSource` from this cycle's live CI pull
([run 35707088252](https://github.com/app-vitals/shipwright/actions/runs/35707088252) @
`38a492e1a`, this cycle's exact HEAD, fetched directly via `gh run list`/`gh run view --json
jobs` per `test-migration.md`'s Speed measurement section); `featurePct`/`featureSource` from
`test-inventory.md`'s `feature_coverage_pct` (100%, 24/24 features, unchanged this cycle — no
new feature since Sessions landed 2026-09-18). Computed via:

```bash
echo '{"featurePct": 100, "featureSource": "docs/test-readiness/test-inventory.md", "linePct": 92.46, "lineSource": "CI run https://github.com/app-vitals/shipwright/actions/runs/35707088252 @ 38a492e1a"}' \
  | bun run scripts/check-coverage-gate.ts
```

Actually run (not hand-computed):
```json
{"feature_coverage_pct":100,"feature_coverage_source":"docs/test-readiness/test-inventory.md","line_coverage_pct":92.46,"line_coverage_source":"CI run https://github.com/app-vitals/shipwright/actions/runs/35707088252 @ 38a492e1a","verdict":"READY","targets":{"feature":90,"line":90}}
```

| Field | Value |
|---|---|
| Feature coverage | 100% (target 90%) — source: docs/test-readiness/test-inventory.md |
| Line coverage | 92.46% (target 90%) — source: CI run https://github.com/app-vitals/shipwright/actions/runs/35707088252 @ 38a492e1a |
| **Verdict** | **READY** |

**Stage-3 coverage-check promotion — `done`, unchanged this cycle.** `line_coverage_pct`
(92.46%, byte-identical to the 2026-09-21 pass — no first-party source-line diff in this
window) remains above the 90% threshold `repo-config/SKILL.md`'s 3-stage coverage-check
lifecycle gates on. Per Process step 9, reused the step-4 task-store query result set and
scanned it for a task matching the stage-3 promotion pattern: **`test-t-083-shipwright`**
("Promote coverage required check from never-decrease to hard ≥90% gate") is already filed
and at status **`done`** — no duplicate emission.

## Status of the prior cycle's plan (2026-09-21) and everything since

Queried task-store directly (`$SHIPWRIGHT_TASK_STORE_URL/tasks?repo=app-vitals%2Fshipwright&limit=500`,
paginated across 3 pages — `offset=0`, `offset=500`, `offset=1000` — 1,285 tasks total
scanned this cycle for this repo). Filtered to `test-t-<NNN>-shipwright` IDs: the highest
existing ID is **`test-t-085-shipwright`** — confirming the prior cycle's T-085 reservation
was consumed. **`starting_offset = 86`** for this cycle (no new task needed it).

Re-verified the status of every previously-tracked ID (not assumed from the old doc):

| Task | Status | Title |
|---|---|---|
| `test-t-076-shipwright` | **deployed** (unchanged) | Split `agent-type-manifest-loader.unit.test.ts`'s real-disk cases into a new `.integration.test.ts` file |
| `test-t-077-shipwright` | **deployed** (unchanged) | `check-review.ts`'s `isBundleComplete` fail-open catch-path test |
| `test-t-078-shipwright` | **deployed** (unchanged) | Make `isAuthorAllowed`'s ref dependency injectable + never-synced equivalence test |
| `test-t-079-shipwright` | **deployed** (unchanged) | `scripts/generate-agent-type-schema.ts` test-coverage class-level decision |
| `test-t-080-shipwright` | **deployed** (unchanged) | HITL dev-loop bootstrap's full clone→seed→boot→poll sequence, injected `gh` exec fake |
| `test-t-081-shipwright` | **done** (unchanged) | `TaskService.claim()`'s concurrent-race close (real Postgres) |
| `test-t-082-shipwright` | **deployed** (unchanged) | `traceReviewCandidacyDecision`'s exclusion checks proven correct in 3+-condition combination |
| `test-t-083-shipwright` | **done** (unchanged) | Promote coverage required check from never-decrease to hard ≥90% gate |
| `test-t-084-shipwright` | **deployed** (unchanged) | Add `afterEach` reset to `agentSlackMembershipRef` singleton test |
| `test-t-085-shipwright` | **deployed** (unchanged) | Add sweeping worktree add/remove absolute-fallback regression guard to `dev-task.content.test.ts` |

**All 10 previously-tracked IDs remain at a terminal status** (`deployed`/`done`). No
regression, no status churn since the 2026-09-21 pass. **No new task minted this cycle** —
every bucket in `test-migration.md`'s Tests/Infrastructure tables is empty except the
non-actionable Net-new carry-forward (see below).

## 1. Where we are now

### Layer coverage map

| Layer | Existing tests | Inventory items | Coverage gap |
|---|---|---|---|
| Unit | 187 `.unit.test.ts` | Dominant category-1 surface (`lib/`, decision/classification logic, mutable-box refs, crypto helpers, parsing/validation, coverage-gate tooling), plus the new `admin/src/agent-type-registry.ts` (already covered, pre-existing) | None |
| Integration | 83 `.integration.test.ts` | Every category-2/5 service-boundary and external-integration item | None |
| Smoke | 54 `.smoke.test.ts` | Every category-3 HTTP route across 5 services | None |
| E2E | 26 `site/tests/*.spec.ts` + 7 `{admin,metrics}/e2e/*.e2e.ts` | 4 of 5 identified journeys are browser-driven or partially so (journey #5, HITL bootstrap, is integration-composed, not E2E) | None |
| Content | 47 `.content.test.ts` (+1 vs. 2026-09-21: new `research.content.test.ts`) | `plugins/shipwright/{commands,agents,skills,references,test}/*` plus root-level content tests | None — the one open depth gap (dev-task worktree-fallback sweep, T-085) closed this cycle |
| Shell (unofficial 6th layer) | 3 `test-*.sh` bash assertion suites (`test-auto-bump-chart.sh`, `test-check-chart-drift.sh`, `test-sync-plugin-version.sh` — direct `find .github/workflows -name "test-*.sh"` this cycle) | `.github/workflows/lib/chart-tag-utils.sh`, `chart-drift-check.sh` (`test-sync-plugin-version.sh` covers plugin-version-sync logic, not separately itemized in the inventory's shell-layer callout) | None — only a documentation-completeness question open (see Open risks) |
| Compile-time-only (unformalized) | 1 `*.check.ts` (`blocked-by-type-parity.check.ts`) | `admin/src/blocked-by-type-parity.check.ts` | None — wired into CI's `typecheck` step; only a taxonomy question open (see Open risks) |

**Total: 371 `bun test`-scanned files** (187 unit + 83 integration + 54 smoke + 47 content —
matches this cycle's live CI run's own summary line, "Ran 9288 tests across 371 files."
exactly) **+ 26 site E2E + 7 admin/metrics E2E = 404 test files** — up +1 vs. the 2026-09-21
pass's 403, tracking the one new content file (`research.content.test.ts`).

**`## Features` section in `test-inventory.md`: 24 features, unchanged (100%, 24/24).** One
new inventory row this cycle (`admin/src/agent-type-registry.ts`) folds into the existing
"Admin — agent CRUD & provisioning" feature, already covered by pre-existing tests — no new
feature.

### Local-runnable status

**100%** of the `bun test`-scanned files run locally with zero external network. This
cycle's isolation-contract grep sweep (`mock.module(`, `global.fetch =`, `global.console =`,
stray `DATABASE_URL` inside `*.unit.test.ts`) found zero real violations — byte-identical
counts to the 2026-09-21 pass. No test is blocked on an external service.

### Canary status

**Not applicable.** Root `CLAUDE.md` declares `## Deploy model: direct` — no
staging/production GitHub Environments, no deploy→canary→promote pipeline. Unchanged finding
across every cycle to date. See Milestone 3.

### Speed status (Tier 1 aggregate vs. budget; Tier 2 not triggered)

**Tier 1 — aggregate (measured this pass, via live CI history, fetched directly via
`gh run list`/`gh run view --json jobs`):**

| Aggregate wall-clock | Budget | Ratio |
|---|---|---|
| 1m35s ([run 35707088252](https://github.com/app-vitals/shipwright/actions/runs/35707088252), 2026-09-22T08:52:21Z–08:53:56Z, commit `38a492e1a` — this cycle's exact HEAD) | <15 min (Full PR pipeline) | ~11% of budget |

Fully green run. `bun test` step itself: 26.69s. **9,288 tests across 371 files** (+17
tests / +1 file vs. the 2026-09-21 pass, proportionate to this cycle's small 6-commit
delta). Coverage held essentially flat: **92.46%** lines (35715/38629), **91.01%** functions
(2965/3258), against the 90%/89% thresholds — `✅ Coverage gate passed`, logged directly,
byte-identical to the 2026-09-21 pass's reading (expected: this window's non-routine commits
are all content-layer test/markdown changes or dependency-lockfile bumps with no first-party
source-line diff). Test-count growth trajectory: …→ 9,219 (2026-09-18) → 9,271 (2026-09-21)
→ **9,288 (2026-09-23, this pass)** — no anomaly, no padding.

**Tier 2 triggered:** no — the escalation formula (aggregate wall-clock >7.5 min, sustained
across 2 consecutive measurements) is nowhere near tripped (~11% of budget, well below the
50% escalation threshold). No per-layer p95 table is populated below, per the SKILL's
explicit guidance against fabricating per-layer numbers when only the Tier 1 aggregate is
available.

### "Rebuild" / "Delete (redundant)" debt

| Bucket | Count | Effort |
|---|---|---|
| Rebuild | 0 | — |
| Promote / deepen | 0 | — (T-085 closed this cycle) |
| Delete (redundant) | 0 | — |
| Net-new | 1 | trivial, optional, non-actionable (see below) |

Every bucket is clean this cycle. The one and only carried-forward item is
`admin/src/clock.ts` — no dedicated test file, consistent with every sibling `Clock`
implementation across the repo (near-zero logic, proven via consumers), Phase 3's
criticality for it is **low** (not `critical`/`high`), and it is explicitly flagged
"trivial, optional." It does not map to Milestone 2 (critical) or Milestone 4 (high) and is
not infra/delete — consistent with every prior cycle, **no task is minted for it** (see
Milestone 4 and Open risks).

## 2. Where we want to be

- **Framework matrix** — already fully realized: `bun test` (unit/integration/smoke/content),
  Playwright (E2E, 2 surfaces: `site/*.spec.ts`, `admin/e2e/*.e2e.ts`), plain `bash` (the
  shell layer). No framework changes needed.
- **Local-substitute map** — every dependency named in the inventory has a working local
  substitute (recorded-fixture doubles for internal/third-party HTTP, real Postgres via CI
  service container for the three Prisma-backed services, a real locally-launched Chromium
  binary for `agent/src/browser.ts`). No blockers.
- **Coverage targets** — single 90/89 line/function CI floor (`scripts/check-coverage.ts`),
  fully enforced (stage-3 branch-protection promotion confirmed `done` — see Coverage Gate
  section). Tiered targets (critical ~95%, high ~80%, medium spot-check) direct
  prioritization, not a second CI gate.
- **Canary suite definition** — not applicable under the current `direct` deploy model;
  deferred until a staged-deploy model exists.
- **Speed budgets per layer** — Unit <50ms p95 / Integration <2s / Smoke <5s / E2E <30s /
  full PR pipeline <15min. Tier 1 aggregate confirms comfortable headroom (~11% of budget);
  no per-layer breakdown needed since Tier 2 has never triggered.
- **Zero `rebuild` / `delete (redundant)` buckets, and every Promote/deepen finding closed
  promptly** — fully achieved this cycle: all buckets are empty.
- **Every critical- and high-tier inventory item covered at its prescribed layer** — fully
  achieved: no open critical/high gap found this cycle.

## 3. The gap

- **No missing layers.** All six layers (five `bun test`-declared plus the unofficial shell
  layer) in active use with the correct framework per layer, plus one
  deliberately-unformalized compile-time-only pattern (`blocked-by-type-parity.check.ts`)
  that has no coverage gap despite not fitting the existing taxonomy.
- **Wrong-layer tests:** 0.
- **External-only deps with no local substitute:** none.
- **No new gap this cycle.** The sole open item carried from the 2026-09-21 pass
  (`dev-task.content.test.ts`'s missing sweeping worktree-fallback regression guard) closed
  via T-085 (`test-t-085-shipwright`, now `deployed`) — re-verified directly this cycle via
  `grep -n "worktree add\|worktree remove" plugins/shipwright/commands/dev-task.md`: all
  current invocation lines pass the widened assertion. Every one of this cycle's 6
  non-routine commits (the `run_in_background: false` background-dispatch-pinning cluster)
  landed with matching, same-commit, correctly-layered test coverage — no new Promote/deepen
  finding.
- **1 carried-forward, low-priority, non-gap item:** `admin/src/clock.ts` has no dedicated
  test file — consistent with the established convention shared by all sibling `Clock`
  implementations across the repo (near-zero logic, proven via consumers). Not scored as a
  gap; no task minted.
- **T-083's stage-3 coverage-promotion, including the branch-protection swap, remains fully
  confirmed done** — see Coverage Gate section above. No remaining open question on this
  item.
- **Infra gap: none.** Runner config, CI shape, Postgres containers, `Clock` helpers,
  recorded-fixture doubles, PR template all confirmed unchanged and correct this cycle
  (`test-migration.md`'s Infrastructure — bucketed section: Reuse as-is only, zero
  Promote/Rebuild/Delete/Net-new).

### Speed delta

**Tier 1 — aggregate-vs-budget (leads; always populated):**

| Aggregate wall-clock | Budget | Ratio / time-to-close |
|---|---|---|
| 1m35s | <15 min (Full PR pipeline) | ~11% of budget — no time-to-close concern |

**Tier 2 — per-layer ratio (conditional on Tier 2 having triggered):**

**Tier 2 triggered:** no. Per-layer numbers are not fabricated here — Tier 1's aggregate is
the complete, sufficient measurement this cycle. Growth trajectory (371 files, 9,271 → 9,288
tests since the 2026-09-21 baseline) is proportionate to this cycle's small 6-commit delta
with no budget concern.

## 4. Roadmap (five milestones)

### Milestone 1 — Infrastructure baseline
**DOD:** Runners, local substitutes, CI pipeline shape in place. No new tests yet — just the rails.

**Already satisfied — zero tasks this cycle.** T-083 (auto-emitted stage-3 coverage-check
promotion) remains `done`. Naming convention + runner-exclusion config
(`docs/test-readiness/naming.md`, `bunfig.toml`'s `pathIgnorePatterns`) re-verified
unchanged. CI pipeline shape matches `test-system.md`'s documented target exactly. Postgres
service containers, `Clock` injection, and recorded-fixture doubles all confirmed in place.
`recorded_fixture_automation` is undeclared in root `CLAUDE.md` (defaults to `planned`), but
the recorded-fixture-refresh loop remains explicitly deferred per
`test-system.md`'s Repo configuration section ("deferred / not implemented... do not build it
until the manual, hand-authored-fixture workflow above is running and tuned") — no task
emitted for it, consistent with every prior cycle; Phase 3's infra bucket found no gap
requiring it regardless.

### Milestone 2 — Critical-path coverage
**DOD:** 100% of `critical` tier inventory items have passing tests at the prescribed layer.

**Already satisfied — zero tasks this cycle.** No open items carried in. No new critical-tier
gap found — this cycle's one new inventory row (`admin/src/agent-type-registry.ts`, `high`
tier, not critical) was already covered by pre-existing tests (originating PRs #2191/#2207,
2026-07-24) before Phase 1 even gave it its own row.

### Milestone 3 — Canary suite live
**DOD:** canary not applicable -- deploy_model=direct

Zero tasks, same as every prior cycle. See `test-system.md`'s Canary execution contract
section for what would need to change before this applies.

### Milestone 4 — High-tier coverage
**DOD:** 100% of `high` tier inventory items covered.

**Already satisfied — zero tasks this cycle.** T-085 (the sole prior-cycle high-tier
content-layer depth gap) is now `deployed` and re-verified closed directly against the
current `dev-task.md`. No new high-tier gap found this cycle.

`admin/src/clock.ts` remains the sole Net-new item from Phase 3 — `low` criticality, carried
forward, unchanged, explicitly a non-gap. No task minted for it, same as every prior cycle
(it does not qualify for this milestone's `high`-tier walk).

### Milestone 5 — Cleanup
**DOD:** Zero tests in `rebuild` or `delete (redundant)` buckets. CI passes.

**Already satisfied — zero tasks.** Phase 3 found zero `rebuild` and zero `delete
(redundant)` items this cycle, the same steady state as every prior pass.

## 5. Task list

Flat, ordered, agent-executable. Format: `T-NNN | M# | files | layer | bucket | outcome | verify`.

**No open tasks this cycle.** Every migration bucket (Tests and Infrastructure) came back
empty except the non-actionable, `low`-criticality `admin/src/clock.ts` Net-new carry-forward
(see Milestone 4 and Open risks) — not eligible for any of the five milestone walks per
Process step 5, so no `T-NNN` row is emitted for it. `starting_offset` for the next cycle
that does mint a task remains **T-086**.

**Resolved (re-verified via direct task-store query this cycle):** all 10 previously-tracked
IDs (`test-t-076-shipwright` through `test-t-085-shipwright`) remain at a terminal status
(`deployed`/`done`) — see the Status section above. None require any action this cycle.

## 6. Open risks

- **New this cycle — none.** No new open risk surfaced. T-085 closed clean with no follow-on
  question.
- **The isolation-contract audit's grep pattern set still has a documented blind spot.** The
  one known instance of this leak class (T-084, `agent-slack-membership-ref.unit.test.ts`)
  was fixed back in the 2026-08-23 cycle via a manual diff read, not an automated sweep — the
  pattern set still can't catch a recurrence of this leak class on its own. Recommend a
  future cycle add a fourth signal scanning for direct `.set(`/`.reset(`/mutation calls on
  this repo's known singleton-ref exports (`agentReposRef`, `reviewAuthorAllowlistRef`,
  `agentSlackMembershipRef`, `loopJobsRef`, `patchAuthorAllowlistRef`, and future
  `agent-*-ref.ts` additions) with no paired `afterEach`/`afterAll` in the same file — a
  Phase 3 process improvement, not itself a task against the codebase. Carried forward
  unchanged, no new instance found this cycle.
- **The compile-time-only "type-parity check" pattern (`blocked-by-type-parity.check.ts`)
  still needs a taxonomy decision.** Not a coverage gap — confirmed wired into CI and
  passing — but it doesn't fit any of the five CLAUDE.md-declared suffixes or the unofficial
  sixth "shell" layer. `test-system.md` deliberately declines to formalize a seventh layer on
  a single instance; revisit if a second such file appears.
- **The bash "shell" layer still needs a formal documentation decision, and this cycle found
  a small accuracy gap worth a human look.** `.github/workflows/lib/chart-tag-utils.sh` /
  `chart-drift-check.sh` and their `test-*.sh` siblings are genuinely tested code units that
  fall entirely outside the `bun test` runner and the five-suffix convention declared in root
  `CLAUDE.md`. Carried forward unchanged across several cycles (first flagged 2026-07-31).
  **New observation this cycle:** a direct `find .github/workflows -name "test-*.sh"` turned
  up a third file, `test-sync-plugin-version.sh`, not individually named in
  `test-inventory.md`'s shell-layer callout (which cites only the chart-tag-utils/
  chart-drift-check pair). Coverage itself is not in question — this is a documentation
  precision note for a future `test-inventory` pass, not a Phase 4 task. Not urgent; not
  blocking anything above.
- **Recorded-fixture coverage breadth** (remaining `Http*Client` modules that could extend
  the `Recorded*Client` pattern) remains a Phase 2 design-level backlog item, not a
  migration-bucket verdict on any existing file. No task emitted this cycle since Phase 3
  found no test currently missing coverage because of it.
- **`agent/src/piper-voice.ts` reclassification watch item** (unit today, integration once
  Piper synthesis lands) — still not landed as of this cycle; flagged so a future Phase 3
  doesn't silently miss the transition.
- **`admin/src/session-scope.ts`'s visibility predicate remains not yet wired into most
  Session routes.** Documented, intentional interim state per
  `docs/session-notifications.md` — the predicate itself is fully unit-tested, so this is an
  application-logic sequencing gap, not a test-coverage gap. No task minted (this pipeline
  doesn't mint tasks for application logic, only test coverage) — carried forward so a human
  continues tracking the wiring-completion timeline.
- **Recorded-fixture maintenance loop remains deferred** (`repo-config/SKILL.md` §5) —
  correctly not scheduled this cycle; sequence after fixture-authoring work matures.

---

## Handoff

This document is intended for execution by:
- An engineer working through the task list manually, OR
- `shipwright /dev-task` consuming the task list as a queue, OR
- `/shipwright:test-fix` (Phase 5), which queues every row in the Task list section above as
  a task-store task — this cycle, that means **zero tasks to queue**; `test-fix` should be a
  clean no-op run.
