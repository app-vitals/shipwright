# Test Readiness Plan

> Phase 4 synthesis. The executable roadmap from where we are now to a test suite trustworthy enough for autonomous programming.
>
> Generated by `/test-roadmap` on 2026-08-11 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-20260811`), consuming `test-inventory.md` (refreshed at
> `0428fe71`), `test-system.md` (refreshed at `0428fe71`), and `test-migration.md`
> (refreshed at this cycle's HEAD, `0428fe71`).

## Status of the prior cycle's plan (2026-08-09) and everything since

Queried task-store directly (not assumed from the old doc; direct `GET /tasks/{id}`
lookups against each of the 8 known IDs), current status of every relevant ID as of this
cycle:

| Task | Status | Title |
|---|---|---|
| `test-t-073-shipwright` | **done** | `site/src/lib/html-escape.ts` coverage scoping decision |
| `test-t-074-shipwright` | **cancelled** | Refresh the speed baseline against a real Postgres-enabled environment |
| `test-t-075-shipwright` | **deployed** | Naming convention + runner-exclusion re-verification |
| `test-t-076-shipwright` | **deploying** | Split `agent-type-manifest-loader.unit.test.ts`'s real-disk cases into a new `.integration.test.ts` file |
| `test-t-077-shipwright` | **deploying** | `check-review.ts`'s `isBundleComplete` fail-open catch-path test |
| `test-t-078-shipwright` | **deploying** | Make `isAuthorAllowed`'s ref dependency injectable + never-synced equivalence test |
| `test-t-079-shipwright` | **deploying** | `scripts/generate-agent-type-schema.ts` test-coverage class-level decision |
| `test-t-080-shipwright` | **pending** | HITL dev-loop bootstrap's full clone→seed→boot→poll sequence, injected `gh` exec fake |

**No status movement on T-073 through T-079 since the 2026-08-09 pass.** T-076 through
T-079 remain parked at `deploying` — this is now the **5th consecutive cycle** these four
IDs have shown zero movement, which is now a stronger signal worth a direct human check on
why the deploy pipeline hasn't advanced them (outside this pipeline's own remit, but the
streak itself is new information this cycle). **T-080 is still `pending`**, unchanged since
it was queued 2026-07-30 — see Milestone 4 below, this task still carries forward.

**Task-store query for the starting T-NNN offset (per SKILL Process step 4), run before
minting any new IDs:** queried `$SHIPWRIGHT_TASK_STORE_URL/tasks?repo=app-vitals%2Fshipwright&limit=500`
— `total: 830`, requiring one additional page (`&offset=500`, +330 records, 830 total
scanned, matching `.total`). Filtered to `test-t-<NNN>-shipwright` IDs across both pages:
highest is `test-t-080-shipwright`. **`starting_offset = 81`** — this cycle's new tasks
begin at **T-081**.

**This cycle mints exactly one new task, T-081** — Phase 3's one genuinely new,
newly-identified gap (`task-store/src/task-service.ts`'s `claim()` concurrent-race
regression test). See Milestone 2 below.

**Two items closed out this cycle vs. the 2026-08-09 plan, not carried forward as open
work:**
1. **CRF-1.1** (`pull-request-service.ts`'s `claim()` concurrent-claim race) —
   **confirmed resolved.** New `pull-request-service.integration.test.ts` has a dedicated
   real-Postgres `Promise.allSettled`-raced concurrent-claim test. No longer tracked as a
   gap anywhere in this cycle's roadmap (it was not carried in the 2026-08-09 plan's task
   list either, since it hadn't landed yet at that point — this is the confirmation that
   it landed correctly).
2. **DRO-1.2** (`check-patch.ts`'s `isSupersededBySelfReview` self-review-supersession
   predicate) — **confirmed resolved.** `check-patch.unit.test.ts` has 4 dedicated cases
   proving both directions of the predicate. No longer tracked as a gap.
3. **The `pr-state-reconciler.ts` orphan-reconciliation removal** — carried forward as an
   *ambiguous item needing human-confirmed rationale* in both the 2026-08-09 inventory and
   design doc (not itself a task, but an open caution in Open risks). This cycle's Phase 3
   directly investigated it and confirmed it is a deliberate, reviewed, well-tested clean
   removal (PR #2527, "refactor: remove orphaned-task reconciler (DTC-1.1)", merged
   2026-08-10, superseded by `StaleClaimReaper` + the ready-set FIFO + `dev-task.md`'s
   Step 4 Branch/PR Reality Check). **Removed from Open risks this cycle** — resolved, not
   carried forward.

## 1. Where we are now

### Layer coverage map

| Layer | Existing tests | Inventory items | Coverage gap |
|---|---|---|---|
| Unit | 125 `.unit.test.ts` | Dominant category-1 surface (`lib/`, decision/classification logic, mutable-box refs, crypto helpers, parsing/validation) | None — every inventory item mapped to unit has a matching test file |
| Integration | 62 `.integration.test.ts` | Every category-2/5 service-boundary and external-integration item | One real gap: `task-service.ts`'s `claim()` concurrent-race case (see Milestone 2) |
| Smoke | 43 `.smoke.test.ts` | Every category-3 HTTP route across 5 services | None |
| E2E | 21 `site/tests/*.spec.ts` + 3 `{admin,metrics}/e2e/*.e2e.ts` | 4 of 5 identified journeys are browser-driven or partially so (journey #5, HITL bootstrap, is integration-composed, not E2E) | None at E2E; journey #5's integration-harness half is a carried-forward gap (see Milestone 4) |
| Content | 38 `.content.test.ts` | `plugins/shipwright/{commands,agents,skills,references}/*` | None — +2 this cycle (`unblock.content.test.ts`, `test-readiness/SKILL.content.test.ts`), both backing landed production content |
| Shell (unofficial 6th layer) | 2 `test-*.sh` bash assertion suites | `.github/workflows/lib/chart-tag-utils.sh`, `chart-drift-check.sh` | None — coverage exists; only a documentation-completeness question is open (see Open risks) |

**Total: 292 test files** (268 `bun test`-scanned + 21 `site/tests/*.spec.ts` + 3
Playwright `*.e2e.ts`), +5 from the 2026-08-09 pass. Growth this cycle is 5 new files
(`lib/http.unit.test.ts`, `unblock.content.test.ts`, `compute-review-verdict.unit.test.ts`,
`test-readiness/SKILL.content.test.ts`, `pull-request-service.integration.test.ts`), all
backing landed production changes and already-correct, already-passing coverage — none are
open work items.

### Local-runnable status

**100%** of the 268 `bun test`-scanned files run locally with zero external network.
Phase 3's isolation-contract grep sweep (`mock.module(`, `global.fetch =`,
`global.console =`, stray `DATABASE_URL` inside `*.unit.test.ts`) found zero real
violations this cycle (37 `mock.module(` matches, all in comments; 3 pre-existing
non-violation `DATABASE_URL` string-literal matches, unchanged). No test is blocked on an
external service.

### Canary status

**Not applicable.** Root `CLAUDE.md` declares `## Deploy model: direct` — no
staging/production GitHub Environments, no deploy→canary→promote pipeline. Unchanged
finding across every cycle to date. See Milestone 3.

### Speed status (Tier 1 aggregate vs. budget; Tier 2 not triggered)

**Tier 1 — aggregate (measured this pass, via live CI history):**

| Aggregate wall-clock | Budget | Ratio |
|---|---|---|
| 2m20s ([run 31461392600](https://github.com/app-vitals/shipwright/actions/runs/31461392600), 2026-08-11T05:21:52Z, commit `0428fe71` — this cycle's exact HEAD) | <15 min (Full PR pipeline) | ~16% of budget |

Fully green run — no failing jobs. `bun test` step itself: 38.35s. 6,288 tests pass, 0
fail, across 269 files. Consistent with every prior cycle's ~13-17% readings — not
trending toward the escalation trigger. Test-count growth trajectory: 5,762
(2026-07-28) → 6,041-6,042 (2026-08-01) → 6,070 (2026-08-05) → 6,071 (2026-08-06) → 6,177
(2026-08-09) → 6,288 (2026-08-11, this pass) — steady, expected growth (+111 tests).

**Tier 2 triggered:** no — the escalation formula (aggregate wall-clock >7.5 min,
sustained across 2 consecutive measurements) is nowhere near tripped (~16% of budget,
well below the 50% escalation threshold). No per-layer p95 table is populated below, per
the SKILL's explicit guidance against fabricating per-layer numbers when only the Tier 1
aggregate is available.

### "Rebuild" / "Delete (redundant)" debt

| Bucket | Count | Effort |
|---|---|---|
| Rebuild | 0 | — |
| Delete (redundant) | 0 | — |
| Net-new | 3 | trivial-to-small (see Milestones 2 and 4) |

Rebuild and Delete both zero, same as every prior pass. No higher-layer test re-asserts a
business rule already proved at a lower layer; no test's layer/framework/fundamentals
were found wrong. Zero false-confidence coverage identified. Net-new is 3, up from 2 in
the 2026-08-09 plan — one item (`task-service.ts`'s claim-race test) is genuinely new
this cycle; the other two (HITL bootstrap integration harness, `admin/src/clock.ts`) are
carried forward unchanged.

## 2. Where we want to be

- **Framework matrix** — already fully realized: `bun test` (unit/integration/smoke/content),
  Playwright (E2E, 3 surfaces), plain `bash` (the shell layer). No framework changes
  needed.
- **Local-substitute map** — every dependency named in the inventory has a working local
  substitute (recorded-fixture doubles for internal/third-party HTTP, real Postgres via
  CI service container for the three Prisma-backed services, a real locally-launched
  Chromium binary for `agent/src/browser.ts`). No blockers.
- **Coverage targets** — single 80/80 line/function CI floor (`scripts/check-coverage.ts`),
  already enforced; tiered targets (critical ~95%, high ~80%, medium spot-check) direct
  prioritization, not a second CI gate.
- **Canary suite definition** — not applicable under the current `direct` deploy model;
  deferred until a staged-deploy model exists.
- **Speed budgets per layer** — Unit <50ms p95 / Integration <2s / Smoke <5s / E2E <30s /
  full PR pipeline <15min. Tier 1 aggregate confirms comfortable headroom (~16% of
  budget); no per-layer breakdown needed since Tier 2 has never triggered.
- **Zero `rebuild` / `delete (redundant)` buckets** — already true today.
- **Every critical- and high-tier inventory item covered at its prescribed layer,
  including a genuine concurrent-caller regression test wherever a claim/lock race was
  closed** — this cycle's `pull-request-service.ts` CRF-1.1 close meets this standard
  fully; its `task-service.ts` twin does not yet (mocked single-caller coverage only) —
  the one gap this cycle's roadmap targets.

## 3. The gap

- **No missing layers.** All six layers (five `bun test`-declared plus the unofficial
  shell layer) in active use with the correct framework per layer.
- **Wrong-layer tests:** 0.
- **External-only deps with no local substitute:** none.
- **One real, newly-identified net-new gap this cycle:** `task-store/src/task-service.ts`'s
  `claim()` concurrent-race regression test. The `AND "claimedBy" IS NULL` WHERE-clause
  tightening (the task-layer twin of CRF-1.1) has only single-caller, mocked-double
  coverage (`task-service.unit.test.ts`'s `makeClaimPrismaDouble()` tests); no
  real-Postgres test races two concurrent `claim()` calls against the same seeded row the
  way `pull-request-service.integration.test.ts`'s new test does. The fix's entire value
  is in the concurrent-access case, which current coverage cannot exercise. This corrects
  Phase 2's gap #15, which had grouped this with the (now-resolved) `pull-request-service.ts`
  sibling as a single already-resolved item — direct inspection this cycle found the
  task-service half genuinely still open.
- **One real gap, carried forward unchanged since 2026-07-30 (T-080, still `pending`):**
  journey #5 (local HITL dev-loop bootstrap) has its pure-planning half
  (`computeMissingClones`, `computeProvisionPlan`) fully unit-tested, but the full clone →
  seed → boot → poll sequence has no integration-level test exercising it end-to-end with
  an injected `gh` exec fake.
- **Two gaps resolved and closed out this cycle:** CRF-1.1 (`pull-request-service.ts`'s
  claim-race close) and DRO-1.2 (`check-patch.ts`'s self-review-supersession predicate) —
  both now have dedicated, verified test coverage landed in the same commits as the
  production changes. See Status of the prior cycle's plan above.
- **One ambiguous item resolved and closed out this cycle:** the `pr-state-reconciler.ts`
  orphan-reconciliation removal is confirmed a deliberate, reviewed, well-tested clean
  removal (PR #2527) — no longer an open caution.
- **This cycle's other real source changes, all confirmed already resolved:** the new
  `lib/http.ts` shared `readJson` dedup (+ its 3 call-site route switches),
  `compute-review-verdict.ts`'s `computeVerdict`/`validateReviewVerdict` (DRO-1.1), and the
  new `unblock.md` command (+ its content test). Every touched row has matching test
  coverage landed in the same commits as the production changes — no task needed for any
  of them.
- **1 carried-forward, low-priority, non-gap item:** `admin/src/clock.ts` has no
  dedicated test file — consistent with the established convention shared by all sibling
  `Clock` implementations across the repo (near-zero logic, proven via consumers). Not
  scored as a gap; no task minted.
- **Infra gap: none net-new.** Runner config, CI shape, Postgres containers, `Clock`
  helpers, recorded-fixture doubles, coverage gate, PR template all confirmed unchanged
  and correct.

### Speed delta

**Tier 1 — aggregate-vs-budget (leads; always populated):**

| Aggregate wall-clock | Budget | Ratio / time-to-close |
|---|---|---|
| 2m20s | <15 min (Full PR pipeline) | ~16% of budget — no time-to-close concern |

**Tier 2 — per-layer ratio (conditional on Tier 2 having triggered):**

**Tier 2 triggered:** no. Per-layer numbers are not fabricated here — Tier 1's aggregate
is the complete, sufficient measurement this cycle. Growth trajectory (269 → 292 files,
6,177 → 6,288 tests) is steady with no budget concern at any point measured.

## 4. Roadmap (five milestones)

### Milestone 1 — Infrastructure baseline
**DOD:** Runners, local substitutes, CI pipeline shape in place. No new tests yet — just the rails.

**Already satisfied — zero tasks.** Naming convention + runner-exclusion config
(`docs/test-readiness/naming.md`, `bunfig.toml`'s `pathIgnorePatterns`) is established and
re-verified unchanged this cycle. CI pipeline shape matches `test-system.md`'s documented
target exactly. Postgres service containers, `Clock` injection, and recorded-fixture
doubles are all confirmed in place. T-074 (speed-baseline Postgres refresh) remains
`cancelled` — no successor task needed given Tier 1's CI-sourced measurement is a
complete, sufficient answer.

`recorded_fixture_automation` is undeclared in root `CLAUDE.md` (re-verified this cycle —
no `## Recorded-fixture automation` section present, defaults to `planned` per this
cycle's read mechanism) — no repo-config declaration opts this repo out of the
recorded-fixture-refresh-loop pattern. That loop remains correctly **deferred, not
scheduled** (`test-system.md`'s Repo configuration section) — sequenced after the manual,
hand-authored-fixture workflow matures, not alongside it. No M1 task minted for it this
cycle.

### Milestone 2 — Critical-path coverage
**DOD:** 100% of `critical` tier inventory items have passing tests at the prescribed layer.

**One new task this cycle — T-081.** `task-store/src/task-service.ts` is a `critical`-tier
inventory row (Prisma-backed service, `task-service.ts`/`pull-request-service.ts`/`token-service.ts`
row). Its `claim()` concurrent-race regression test is the one item standing between this
milestone's 100% DOD and today's actual state — every other critical-tier item, including
this cycle's `pull-request-service.ts` CRF-1.1 close and `check-patch.ts`'s DRO-1.2 close,
already has matching coverage landed in the same commits as the production change.

| Task | Files to touch | Layer | Bucket origin | Expected outcome | Verification command |
|---|---|---|---|---|---|
| T-081 | `task-store/src/task-service.integration.test.ts` (new) | integration | net-new | A dedicated real-Postgres concurrent-claim regression test for `TaskService.claim()`, mirroring `pull-request-service.integration.test.ts`'s existing `Promise.allSettled`-raced pattern exactly (two concurrent callers claim the same seeded row; assert exactly one resolves and the other throws `ConflictError`) — proving Postgres, not JS, arbitrates the race for tasks the same way it now does for PRs | `bun test task-store/src/task-service.integration.test.ts` |

Sizing check: single concern (one new test file, one service, one race property), no
"part 1 of 2," well under the 1000-line cap — the harness pattern to port already exists
verbatim in `pull-request-service.integration.test.ts` per `test-system.md`'s Shared
helpers section, making this a same-shape port, not new design work. Independently
deployable — the PR can merge standalone with no dependency on any other task.

### Milestone 3 — Canary suite live
**DOD:** canary not applicable -- deploy_model=direct

Zero tasks, same as every prior cycle. See `test-system.md`'s Canary execution contract
section for what would need to change before this applies.

### Milestone 4 — High-tier coverage
**DOD:** 100% of `high` tier inventory items covered.

**One open task, carried forward from 2026-07-30 (T-080, still `pending`, no new task
minted this cycle).** Journey #5 (local HITL dev-loop bootstrap, `high`-adjacent — Phase 1
ranks the journey itself `medium`, but it gates the team's own primary local dev-loop)
crossed the journey threshold in a prior cycle when `scripts/hitl.ts` gained an auto-clone
preflight and `agent-policy.md` template seeding. The pure-planning half
(`computeMissingClones`, `computeProvisionPlan`) is fully unit-tested; the orchestration
sequence itself (clone → seed → boot → poll → dispatch) has no test exercising it
end-to-end.

| Task | Files to touch | Layer | Bucket origin | Expected outcome | Verification command |
|---|---|---|---|---|---|
| T-080 | `scripts/hitl.ts`, `scripts/hitl.integration.test.ts` | integration | net-new | An injected `gh`/process-exec seam (if not already present) wired into `scripts/hitl.ts`'s orchestration function, plus one sequencing test exercising clone→seed→boot→poll with the fake, proving the full sequence composes correctly (not just its individual pure-planning pieces) | `bun test scripts/hitl.integration.test.ts` |

`admin/src/clock.ts` remains the sole other Net-new item from Phase 3 — carried forward,
unchanged, explicitly a non-gap (established `Clock`-file convention, near-zero logic,
proven via consumers). No task minted for it, same as every prior cycle.

### Milestone 5 — Cleanup
**DOD:** Zero tests in `rebuild` or `delete (redundant)` buckets. CI passes.

**Already satisfied — zero tasks.** Phase 3 found zero `rebuild` and zero `delete
(redundant)` items this cycle, the same steady state as every prior pass. No new
generalizable learning surfaced this cycle warranting a `# Harness TODO`.

## 5. Task list

Flat, ordered, agent-executable. **One genuinely new task this cycle** (T-081, minted at
the `starting_offset = 81` derived from the task-store scan above); T-080 (minted
2026-07-30, still `pending`) carries forward unchanged as the other open item.

| ID | Milestone | Files to touch | Layer | Bucket origin | Expected outcome | Verification command | Depends on |
|---|---|---|---|---|---|---|---|
| T-080 | 4 | `scripts/hitl.ts`, `scripts/hitl.integration.test.ts` | integration | net-new | HITL dev-loop bootstrap's full clone→seed→boot→poll sequence proven correct end-to-end via an injected `gh` exec fake | `bun test scripts/hitl.integration.test.ts` | — |
| T-081 | 2 | `task-store/src/task-service.integration.test.ts` (new) | integration | net-new | `TaskService.claim()`'s concurrent-race close proven correct against real Postgres, mirroring `pull-request-service.integration.test.ts`'s existing harness | `bun test task-store/src/task-service.integration.test.ts` | — |

No task in this cycle exceeded the ~1000-line cap, touched more than one functional
concern, or represented an N-service repeated operation — both T-080 and T-081 are single
tasks against a single file's/service's own test surface, with no ordering dependency
between them (they touch unrelated services and can run fully in parallel).

**Resolved (unchanged since the 2026-08-06 pass, re-verified via direct task-store
query, not carried forward from the prior doc):** T-073
(`site/src/lib/html-escape.ts` coverage-scoping decision — **done**), T-074
(speed-baseline Postgres-enabled refresh — **cancelled**, superseded by Tier 1's
successful CI-sourced measurement), T-075 (naming-convention re-verification —
**deployed**), T-076 (`agent-type-manifest-loader` real-disk split — **deploying**),
T-077 (`check-review.ts` fail-open catch-path test — **deploying**), T-078 (injectable
ref + never-synced equivalence test — **deploying**), T-079
(`generate-agent-type-schema.ts` coverage class-level decision — **deploying**). No
status movement on any of these since the 2026-08-06 pass — T-076 through T-079 have now
shown zero movement for **5 consecutive cycles**, a stronger signal this cycle that a
human should check the deploy pipeline directly, even though fixing it is outside this
pipeline's own scope.

## 6. Open risks

- **T-080's scope depends on whether `scripts/hitl.ts` already exposes an injectable
  exec seam.** Not directly re-verified this pass (Phase 3 stopped at confirming the
  planning-helper unit tests exist, unchanged since 2026-08-06). If the seam doesn't exist
  yet, T-080 implicitly bundles "add the injection point" with "add the sequencing test."
  Flagging so whoever picks up T-080 checks this first rather than assuming either shape.
- **T-076 through T-079 have now shown zero status movement for 5 consecutive
  test-readiness cycles (parked at `deploying`).** This is stronger evidence this cycle
  than last that something in the deploy pipeline for these four PRs is stuck, not just
  slow. Recommend a direct human check on why these four haven't advanced — outside this
  pipeline's remit to fix, but worth surfacing explicitly given the now-5-cycle streak.
- **Content-layer file count grew by two this cycle (36 → 38 `.content.test.ts` files:
  `unblock.content.test.ts`, `test-readiness/SKILL.content.test.ts`), still not
  independently full-audited.** Both new files back landed production content
  (`unblock.md`'s 30th-command addition, `test-readiness/SKILL.md`'s 67-line extension).
  `security-scan/SKILL.md`'s 130-line extension has no confirmed corresponding growth in
  its own content-test file — flagged for a light spot-check next cycle, not elevated to
  a gap since content-layer coverage for skills is asserted at the file level, not
  per-section. Phase 1 still batch-classifies `plugins/shipwright/{commands,agents,skills}/*`
  by convention rather than confirming each file individually — the convention has held
  in every spot-check to date.
- **The bash "shell" layer still needs a formal documentation decision.** Carried forward
  unchanged across several cycles now (first flagged 2026-07-31, re-verified present and
  unchanged this cycle — zero `.github/workflows/` changes since the 2026-08-09 baseline):
  `.github/workflows/lib/chart-tag-utils.sh` / `chart-drift-check.sh` and their
  `test-*.sh` siblings are genuinely tested code units that fall entirely outside the
  `bun test` runner and the five-suffix convention declared in root `CLAUDE.md`. Coverage
  itself is not in question — the open question is purely whether a human wants to
  formally document this as a sixth layer in `CLAUDE.md`'s `## Test conventions` table
  and `docs/test-readiness/naming.md`, or confirm it should stay an unofficial exception.
  Not urgent; not blocking any task above.
- **Recorded-fixture coverage breadth** (~9 remaining `Http*Client` modules that could
  extend the `Recorded*Client` pattern) remains a Phase 2 design-level backlog item, not
  a migration-bucket verdict on any existing file. No task emitted this cycle since
  Phase 3 found no test currently missing coverage because of it.
- **`agent/src/piper-voice.ts` reclassification watch item** (unit today, integration
  once Piper synthesis lands) — still not landed as of this cycle; flagged so a future
  Phase 3 doesn't silently miss the transition.
- **Recorded-fixture maintenance loop remains deferred** (`repo-config/SKILL.md` §5) —
  correctly not scheduled this cycle; sequence after fixture-authoring work matures. Per
  the SKILL's guidance, this is deliberately not wired into T-080, T-081, or any other
  task this cycle — neither open task is a credential-provisioning automation task, so
  neither trips the "never automate a never-proven capability" failure mode this note
  exists to block.

---

## Handoff

This document is intended for execution by:
- An engineer working through the task list manually, OR
- `shipwright /dev-task` consuming the task list as a queue.
