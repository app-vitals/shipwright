# Test Readiness Plan

> Phase 4 synthesis. The executable roadmap from where we are now to a test suite trustworthy enough for autonomous programming.
>
> Generated by `/test-roadmap` on 2026-07-23 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-20260723`), consuming `test-inventory.md` (refreshed at
> `e441fec0`), `test-system.md` (refreshed at `c5723e88`), and `test-migration.md`
> (refreshed at `0c84bbd4`).

## Status of the prior cycle's Milestone 1 / Milestone 2 tasks

Queried task-store directly (not assumed from the prior doc):

| Task | Status | Title |
|---|---|---|
| `test-t-051-shipwright` | **done** (resolved 2026-07-22) | Verify main's required status checks match `ci.yml`'s job names |
| `test-t-067-shipwright` | deployed | Fold the existing speed baseline into `test-system.md` |
| `test-t-068-shipwright` | deployed | Naming convention + runner-exclusion verification |
| `test-t-069-shipwright` | deployed | `AgentWorkQueueService` integration coverage |
| `test-t-070-shipwright` | deployed | `admin/src/server.ts` thin composition smoke test |

**T-051 is resolved.** It moved from `pending` to `done` since the 2026-07-21 pass — task
notes confirm branch protection was updated directly via `gh api PUT` under HITL
(`hitl: true`, completed 2026-07-22T02:07:59Z), correcting job names that didn't match
the task description's assumed yaml IDs (e.g. `admin-docker-build` → actual check-run
name `admin docker build`). This closes the item that had been carried forward,
unescalated risk, across the 2026-07-19 and 2026-07-21 cycles — it did **not** need the
"escalate to a human" step the 2026-07-21 plan pre-committed to, because the HITL
resolution had already happened by the time this cycle's carry-forward check would have
triggered it.

**T-071 (`mcp-server/src/main.ts` fail-closed auth-guard test) is still not queued to
task-store** — no `test-t-071-shipwright` record exists (confirmed via a fresh query
across all `test-t-NNN-shipwright` IDs this cycle; max existing ID is still `070`). The
underlying gap the 2026-07-21 plan wrote this task up for is **still open** and now
**unresolved for a second consecutive cycle** — see Milestone 2 and Open risks below.

## 1. Where we are now

### Layer coverage map

| Layer | Existing tests | Inventory items (canonical layer) | Coverage gap |
|---|---|---|---|
| Unit | 113 | ~85+ rows tagged unit across all categories | 1 net-new gap — `lib/agent-default-tools.ts` (new this cycle, pure constant data, no test yet); `admin/src/clock.ts` and `mcp-server/src/clock.ts` remain optional/low, consistent with sibling `Clock` files having no dedicated test |
| Integration | 56 | ~50+ rows tagged integration (service-boundary + external) | 0 net-new — `agent/src/cron-handler.integration.test.ts` (new this cycle) already lands correct coverage for the generic cron-dispatch progress-push wiring |
| Smoke | 42 | ~18 rows tagged smoke (HTTP routes + route-equivalents) | 1 carried-forward gap — `mcp-server/src/main.ts`'s fail-closed auth guard, unresolved for a **second consecutive cycle**; tracked as T-071 (Milestone 2) |
| E2E | 23 (20 `site/*.spec.ts` + 3 `*/e2e/*.e2e.ts`) | 1 genuinely browser-driven journey (marketing site) + 3 e2e/smoke-composed journeys proven at integration layer | 0 — no new browser-driven journeys this cycle; the site-spec count grew by 3 genuinely new specs plus a correction of a stale prior-cycle undercount (see `test-migration.md` Method note) |
| Content (5th layer) | 26 | ~4 grouped rows (commands/agents/skills/references) | 0 — fully covered; new `readme.content.test.ts` this cycle is a regression guard (MMA-1.2), not a coverage gap; skill/command/agent file count unchanged |

Also flagged: `site/src/lib/html-escape.ts` (new this cycle, pure unit-eligible logic) has
no dedicated test — but `site/` has no `*.test.ts`/unit convention in this repo
(`bunfig.toml` excludes `site/**` from the root `bun test` scan), so this is a scoping
question, not a straightforward unit-layer gap. See Milestone 4 / Section 5.

**Total: 260 test files**, up from 249 at the last pass (+11: 3 genuinely new test files
— `admin/src/agent-members.unit.test.ts`, `agent/src/cron-handler.integration.test.ts`,
`readme.content.test.ts` — plus 3 new `site/` Playwright specs plus a +5 correction of a
stale undercount inherited from the 2026-07-21 doc, see `test-migration.md`). T-051 (the
sole carried-forward item from the last two cycles) is now **resolved** (see Status
section above). `mcp-server/src/main.ts` remains a **real, untested, security-relevant
gap**, now unresolved for a second consecutive cycle since its module-level fail-closed
guard on `SHIPWRIGHT_MCP_SERVER_TOKEN` still has zero test coverage. Two new low-priority
net-new gaps surfaced this cycle: `lib/agent-default-tools.ts` (trivial, unit-eligible)
and `site/src/lib/html-escape.ts` (trivial but scoping-blocked). See Milestone 2 / 4 and
Section 5 below.

### Local-runnable status

- **Tests that run locally with zero external network:** effectively 100% of the 260 files — Phase 3's isolation-contract grep sweep found zero real violations (`mock.module(`, `global.fetch =`, `global.console =`, stray `DATABASE_URL` in unit tests), byte-for-byte unchanged from the 2026-07-21 pass.
- **Tests blocked on external services:** none.

### Canary status

**Not applicable.** Root `CLAUDE.md` declares `## Deploy model: direct` — no staging/production GitHub Environments, no deploy→canary→promote pipeline. Phase 1 skipped canary-eligibility tagging; Phase 2's canary execution contract documents what would need to change (a staging target) before this applies; Phase 3 confirms no canary plumbing exists to audit. This phase carries the same finding forward. See Milestone 3 below.

### Speed status (current p95 per layer)

`docs/test-readiness/speed-baseline.md` (on `main` since 2026-07-15, and correctly
cross-referenced into `test-system.md`'s Speed budgets section since the 2026-07-19 cycle
via T-067) shows every measured layer comfortably within budget: unit 1.05-2.50s per
package (target <30s suite), integration 0.15-5.35s (target <3min suite), smoke
3.24-4.62s (target <30s/<60s suite), e2e 4-40s via real CI runs (target <10min), full
suite 34.23s wall-clock (target <15min full-PR budget). No layer is over its hard cap.
This measurement predates this cycle's 3-test-file delta (all small, single-concern
files: an in-memory-fake unit test, an injected-double integration test, and a
`readFileSync` content test) — unlikely to move the needle materially, per Phase 3's
Speed measurement section — so no re-measurement is warranted this cycle either.

### "Rebuild" debt

| Bucket | Count | Effort |
|---|---|---|
| Rebuild | 0 | — |

### "Delete (redundant)" count

**Zero.** No higher-layer test was found re-asserting a business rule already proved at a lower layer.

## 2. Where we want to be

- **Framework matrix** — already fully realized: `bun test` (unit/integration/content), `bun test` + Hono `app.request()` (smoke), Playwright (E2E, 3 surfaces). No framework changes needed.
- **Coverage targets** — single 80/80 line/function CI floor (`scripts/check-coverage.ts`), already enforced; 100% of `critical`-tier inventory items covered at the prescribed layer once T-071 lands. `high`-tier is already fully covered. `admin/src/clock.ts`, `lib/agent-default-tools.ts`, and `site/src/lib/html-escape.ts` are the remaining gaps, all optional/low or medium, not critical/high.
- **Canary suite** — not applicable under the current `direct` deploy model; deferred until a staged-deploy model exists.
- **Speed budgets per layer** — Unit <50ms p95 / Integration <2s / Smoke <5s / E2E <30s / full PR pipeline <15min, per `speed-budgets/SKILL.md` defaults, unmodified. Already measured and within budget, already cross-referenced in `test-system.md` — no further doc work needed.
- **Zero `rebuild` / `delete (redundant)` buckets** — already true today.

## 3. The gap

The gap remains small — the suite continues to track its own blueprint closely, though
one real gap is now overdue. Concretely:

- **No missing layers.** All five layers in active use with the correct framework per layer.
- **Wrong-layer tests:** 0.
- **External-only deps with no local substitute:** none.
- **Net-new coverage gaps (from Phase 3):** 1 real gap (carried, now overdue), 2 new trivial/low-priority gaps, 1 unchanged non-gap. `admin/src/clock.ts` remains a non-gap, matching established repo convention of leaving trivial `Clock` implementations untested directly. `mcp-server/src/main.ts` is still a **real gap, now unresolved for a second consecutive cycle**: its fail-closed guard (`if (!token) throw ...`, `main.ts:27-32`) is security-critical (the sole place `SHIPWRIGHT_MCP_SERVER_TOKEN` is read) and runs at module load, before `createApp` is invoked — `index.smoke.test.ts` only imports `createApp` directly and never executes `main.ts`, so the guard has zero test coverage. `lib/agent-default-tools.ts` (new this cycle, pure constant data) and `site/src/lib/html-escape.ts` (new this cycle, pure logic but blocked on a `site/`-unit-convention scoping decision) are two new low-priority gaps. See T-071 in Milestone 2 and the new items in Milestone 4 / Section 5.
- **Infra gap: none.** All infrastructure items (runner config, CI shape, Postgres containers, `Clock` helpers, recorded-fixture doubles, coverage gate) confirmed unchanged and correct.
- **Resolved this cycle: T-051 branch-protection verification.** `test-t-051-shipwright` moved to `done` on 2026-07-22 — the live GitHub branch-protection required-checks list for `main` was verified and corrected via HITL (`gh api` PUT), closing an item carried across two prior cycles. See Status section above.

### Speed delta

| Layer | Current (measured, `speed-baseline.md`, 2026-07-15) | Target | Ratio |
|---|---|---|---|
| Unit | 1.05–2.50s per package | <50ms per-test p95 / <30s suite target | within suite-target budget; per-test p95 not separately broken out in the baseline doc (aggregate-only) |
| Integration | 0.15–5.35s per package | <2s per-test p95 / <3min suite target | within suite-target budget; per-test p95 not separately broken out |
| Smoke | 3.24–4.62s per package | <5s per-test p95 / <60s suite target | within budget on both dimensions |
| E2E | 4–40s per surface (CI-sourced) | <30s per-test p95 / <10min suite target | within budget; site's 40s Playwright-smoke step is a job-level step duration (includes multiple specs), not a single-test p95 — same caveat `speed-baseline.md` itself notes |
| Full suite | 34.23s wall-clock | <15min full-PR budget | ~26x headroom |

No layer is flagged over its hard cap. The measurement is a point-in-time snapshot from
2026-07-15 and now predates three full test-readiness cycles' worth of file-count growth
(224 → 243 → 249 → 260), but the growth per cycle has been small and low-complexity (thin
smoke/unit/content files, no new heavy integration suites), so no re-measurement is
warranted yet — flagged in Open risks as a "revisit when growth becomes non-trivial"
watch item, same guidance as prior cycles.

## 4. Roadmap (five milestones)

### Milestone 1 — Infrastructure baseline
**DOD:** Runners, local substitutes, CI pipeline shape in place; the live branch-protection required-checks list is verified against `ci.yml`'s actual job names. No new feature tests written yet — just the rails.

**Already satisfied — zero tasks this cycle.** T-051 (the sole carried-forward M1 item
across the 2026-07-19 and 2026-07-21 cycles) is now **`done`** in task-store, resolved
2026-07-22 via HITL. This cycle's own new-file delta (`admin/src/agent-members.ts`,
`lib/agent-default-tools.ts`, `site/src/lib/html-escape.ts`) required no naming or
runner-exclusion changes — confirmed in Phase 1/2 (the one new source+test pair uses the
existing `.unit.test.ts` suffix with zero `bunfig.toml` changes) — so no fresh
verification task is needed this cycle either.

### Milestone 2 — Critical-path coverage
**DOD:** 100% of `critical` tier inventory items have passing tests at the prescribed layer.

**One task, carried forward and now overdue.** `mcp-server/src/main.ts` — tagged
`critical` per Phase 1's inventory criticality — remains a real, security-relevant gap
with zero test coverage (Phase 3's reconciled verdict, unchanged from the 2026-07-21
pass; see Section 3 above): its fail-closed `SHIPWRIGHT_MCP_SERVER_TOKEN` guard runs at
module load and is never exercised by `index.smoke.test.ts` or any other test in the
repo. **This is the second consecutive cycle this gap has gone unqueued to task-store**
(no `test-t-071-shipwright` record exists) — see Open risks below.

1. **T-071 — `mcp-server/src/main.ts` fail-closed auth guard smoke-adjacent test.** One test asserting the process refuses to start (the module throws) when `SHIPWRIGHT_MCP_SERVER_TOKEN` is unset, e.g. via a dynamic `import()` of `main.ts` in a subprocess or with the env var deleted before import. Mirrors the shape of the prior cycles' T-069/T-070 net-new coverage tasks. Unchanged in scope from the 2026-07-21 plan.

| Task | Files to touch | Layer | Bucket origin | Expected outcome | Verification command |
|---|---|---|---|---|---|
| T-071 | `mcp-server/src/main.smoke.test.ts` (new) | smoke | net-new | `mcp-server/src/main.ts`'s fail-closed guard confirmed to throw/refuse to start when `SHIPWRIGHT_MCP_SERVER_TOKEN` is unset | `bun test mcp-server/src/main.smoke.test.ts` |

### Milestone 3 — Canary suite live
**DOD:** canary not applicable -- deploy_model=direct

Root `CLAUDE.md` declares `direct` — no staging/production GitHub Environments, no deploy→canary→promote pipeline. Per the SKILL, zero tasks are emitted for this milestone; no canary-plumbing work is generated for a pipeline that doesn't exist. See `test-system.md`'s Canary execution contract section for what would need to change before this applies.

### Milestone 4 — High-tier coverage
**DOD:** 100% of `high` tier inventory items covered.

**Two new tasks, both medium/low priority.** No `high`-tier gaps this cycle, but two
`medium`/`low`-tier net-new items surfaced in Phase 1/3 that don't fit Milestone 2
(not `critical`) or Milestone 5 (not rebuild/delete):

1. **T-072 — `lib/agent-default-tools.ts` unit test.** New this cycle: pure constant
   data (`DEFAULT_ADMIN_AGENT_TOOLS`, `DEFAULT_AGENT_TOOLS`), consumed by
   `admin/src/agents-api.ts` and `scripts/seed-dev-agent.ts`. Trivial effort — assert
   array membership/contents so a future accidental edit is caught.
2. **T-073 — `site/src/lib/html-escape.ts` coverage scoping decision.** New this cycle:
   pure `escapeHtml` helper with no dedicated test today. Blocked on a human scoping
   decision, not a straightforward test-writing task — `site/` has no `*.test.ts`/unit
   convention in this repo (`bunfig.toml` excludes `site/**` from the root `bun test`
   scan); the helper is currently only exercised indirectly via whichever page-level
   Playwright spec renders a component using `set:html`. This task's "implementation" is
   a decision + doc update (either special-case allow a `bun:test` unit file under
   `site/src/lib/`, or explicitly accept indirect e2e coverage as sufficient), not new
   test code by default.

| Task | Files to touch | Layer | Bucket origin | Expected outcome | Verification command |
|---|---|---|---|---|---|
| T-072 | `lib/agent-default-tools.unit.test.ts` (new) | unit | net-new | `DEFAULT_ADMIN_AGENT_TOOLS`/`DEFAULT_AGENT_TOOLS` array contents asserted | `bun test lib/agent-default-tools.unit.test.ts` |
| T-073 | `docs/test-readiness/test-system.md` (scoping decision documented) + optionally `site/src/lib/html-escape.unit.test.ts` (new, only if the decision is to special-case allow it) | unit (pending scoping) | net-new | Either a dedicated unit test lands, or the decision to rely on indirect e2e coverage is explicitly documented so it isn't silently re-flagged as an unaddressed gap next cycle | `bun test site/src/lib/html-escape.unit.test.ts` (if the test-writing path is chosen) |

### Milestone 5 — Cleanup
**DOD:** Zero tests in `rebuild` or `delete (redundant)` buckets. CI green.

**Already satisfied — zero tasks.** Phase 3 found zero `rebuild` and zero `delete (redundant)` items this cycle, same as all four prior passes. No new generalizable learning surfaced this cycle warranting a `# Harness TODO` — the one open process item (T-071's now-two-cycle unqueued status) is a task-store hygiene note, not a skill/plugin defect, and is captured in Open risks below instead.

## 5. Task list

Flat, ordered, agent-executable. T-071 carries forward from the 2026-07-21 plan, unchanged
in scope — a fresh task-store query this cycle confirms `test-t-071-shipwright` still does
not exist (max existing `test-t-NNN-shipwright` ID found is still `070`, queried across all
`test-t-*-shipwright`-pattern tasks regardless of status). T-072 and T-073 are genuinely
new this cycle, using the next two unclaimed numbers in sequence.

| ID | Milestone | Files to touch | Layer | Bucket origin | Expected outcome | Verification command | Depends on |
|---|---|---|---|---|---|---|---|
| T-071 | 2 | `mcp-server/src/main.smoke.test.ts` (new) | smoke | net-new | `mcp-server/src/main.ts`'s fail-closed `SHIPWRIGHT_MCP_SERVER_TOKEN` guard confirmed to throw when the token is unset | `bun test mcp-server/src/main.smoke.test.ts` | — |
| T-072 | 4 | `lib/agent-default-tools.unit.test.ts` (new) | unit | net-new | `DEFAULT_ADMIN_AGENT_TOOLS`/`DEFAULT_AGENT_TOOLS` array contents asserted | `bun test lib/agent-default-tools.unit.test.ts` | — |
| T-073 | 4 | `docs/test-readiness/test-system.md` + optionally `site/src/lib/html-escape.unit.test.ts` | unit (pending scoping) | net-new | `site/`'s unit-test scoping question resolved and documented, or a dedicated test lands | `bun test site/src/lib/html-escape.unit.test.ts` (if applicable) | — |

No task in this cycle exceeds the ~1000-line cap, touches more than one functional concern, or represents an N-service repeated operation — no task splitting was required. Phase 5 (`test-fix`) should confirm `test-t-071-shipwright` genuinely does not exist before creating it (it has now been written up in two consecutive roadmap docs without being queued — see Open risks), and should create fresh `test-t-072-shipwright`/`test-t-073-shipwright` records for the two new tasks.

## 6. Open risks

- **T-051 is resolved — no further risk.** Closed 2026-07-22 via HITL, branch protection updated directly via `gh api PUT`. Included here only for continuity with the prior two cycles' risk entries.
- **T-071 has now been written up in two consecutive roadmap docs (2026-07-21, 2026-07-23) without ever being queued to task-store.** No `test-t-071-shipwright` record exists as of this cycle's fresh query. Unlike T-051 (which needed elevated GitHub API scope this sandbox doesn't have), T-071 has **no external blocker** — it's a self-contained smoke test an agent can write and land directly. Its non-queuing across two cycles looks like a Phase 5 (`test-fix`) process gap, not a genuine access blocker: this cycle's `test-fix` run (Phase 5 below) should queue it as the priority item. If T-071 is still unqueued at the *next* `/test-roadmap` run, escalate — a critical-tier security gap sitting unactioned for three cycles with no external blocker would be a process failure worth surfacing to a human directly, not silently carrying forward a fourth time.
- **Speed baseline is a point-in-time measurement (2026-07-15), now three cycles stale.** It predates 2026-07-19's 19-file delta, 2026-07-21's 6-file delta, and this cycle's 11-file delta (243 → 249 → 260 files). No single delta is large enough to plausibly threaten the <15min full-PR budget (per `test-system.md`'s own override-condition analysis), but the baseline itself doesn't reflect current file count and the gap is widening. Not urgent enough to re-measure this cycle; worth a fresh capture the next time suite growth is non-trivial or a flake/slowness pattern is reported.
- **Recorded-fixture coverage breadth** (Phase 2's gaps-summary item 2: ~9 remaining `Http*Client` modules that could extend the `Recorded*Client` pattern) is a design-level backlog item, not a migration-bucket verdict on any existing file — no task emitted this cycle since Phase 3 found no test currently missing coverage because of it. Revisit if a future inventory pass tags one of those clients as critical/high with no test.
- **`agent/src/piper-voice.ts` reclassification watch item** (Phase 1/2: unit today, integration once Piper synthesis lands per PPR-1.2) — no action needed until that lands, still not landed as of this cycle; flagged so a future Phase 3 doesn't silently miss the transition.
- **Recorded-fixture maintenance loop remains deferred** (`repo-config/SKILL.md` §5) — correctly not scheduled this cycle; sequence after fixture-authoring work matures, per Phase 2's explicit guidance.
- **`site/`'s unit-test scoping question (T-073) is a new open design question**, not just a task — someone needs to decide whether `bun:test` unit files are ever appropriate under `site/src/lib/` given the current `bunfig.toml` exclusion of `site/**`, or whether pure-logic helpers there are expected to rely on indirect e2e coverage permanently. Flagging so this doesn't get treated as a mechanical "just add a test file" task without that decision first.

---

## Handoff

This document is intended for execution by:
- An engineer working through the task list manually, OR
- `shipwright /dev-task` consuming the task list as a queue.
