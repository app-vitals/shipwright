# Test Readiness Plan

> Phase 4 synthesis. The executable roadmap from where we are now to a test suite trustworthy enough for autonomous programming.
>
> Generated by `/test-roadmap` on 2026-09-02 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-20260902`), consuming `test-inventory.md`, `test-system.md`,
> and `test-migration.md` — all three regenerated this cycle at commit `c1c81d4e`,
> reconciling a 51-non-routine-commit delta since the 2026-08-30 baseline (`ac0ec6a8`) —
> the largest window measured to date: six new feature clusters (unified agent-creation +
> Slack/GitHub provisioning-service extraction UAP-1.x/2.1, an installable PWA shell + Web
> Push notifications CFB-4.2, a TaskEvent audit-log write-through TCS-1.x, a Slack Bolt 5.x
> upgrade + `SlackProgress` migration to `agents.sessions.setStatus` SSB-1.1/SAV-1.1, a
> chat reply-notification webhook + stall-reaper CFB-4.3, and a wide PR/task-linking
> migration off the deprecated stored `taskId` field PTL-1.x/2.x). Phase 3 found every
> real source change already carries matching test coverage at the correct layer, landed
> in the same commits/PRs — so this synthesis again mints **zero new tasks**.

## Coverage Gate

The headline machine-checkable verdict — computed by `scripts/check-coverage-gate.ts`
(CGT-1.2), rendered verbatim below.

Inputs sourced per Process step 8: `linePct`/`lineSource` from this cycle's Step 4c CI
pull ([run 33595391033](https://github.com/app-vitals/shipwright/actions/runs/33595391033)
@ `c1c81d4e`, this cycle's exact HEAD); `featurePct`/`featureSource` from
`test-inventory.md`'s `feature_coverage_pct` (regenerated this cycle, unchanged value —
100%, 23/23 features, no new feature introduced despite 19 new inventory rows — all fold
into existing features). Computed via:
```bash
echo '{"featurePct": 100, "featureSource": "docs/test-readiness/test-inventory.md", "linePct": 91.58, "lineSource": "CI run https://github.com/app-vitals/shipwright/actions/runs/33595391033 @ c1c81d4e"}' \
  | bun run scripts/check-coverage-gate.ts
```

| Field | Value |
|---|---|
| Feature coverage | 100% (target 90%) — source: docs/test-readiness/test-inventory.md |
| Line coverage | 91.58% (target 90%) — source: CI run https://github.com/app-vitals/shipwright/actions/runs/33595391033 @ c1c81d4e |
| **Verdict** | **READY** |

**Stage-3 coverage-check promotion — `done`, unchanged this cycle.** `line_coverage_pct`
(91.58) remains above the 90% threshold `repo-config/SKILL.md`'s 3-stage coverage-check
lifecycle gates on. **T-083** ("Promote coverage required check from never-decrease to
hard ≥90% gate") remains **`done`** per direct task-store query this cycle
(`starting_offset` re-derivation below), its `note` field unchanged since the
2026-08-20 completion, still confirming the branch-protection swap is complete and no
further action is needed. No new stage-3 emission this cycle (Process step 9's dedup
scan found T-083 already filed and `done`).

## Status of the prior cycle's plan (2026-08-30) and everything since

Queried task-store directly (not assumed from the old doc; a fresh full-repo scan across
all three pages for the offset query below, then filtered to each of the 9 known IDs from
the prior cycle), current status of every relevant ID as of this cycle (2026-09-02):

| Task | Status | Title |
|---|---|---|
| `test-t-076-shipwright` | **deploying** (unchanged) | Split `agent-type-manifest-loader.unit.test.ts`'s real-disk cases into a new `.integration.test.ts` file |
| `test-t-077-shipwright` | **deploying** (unchanged) | `check-review.ts`'s `isBundleComplete` fail-open catch-path test |
| `test-t-078-shipwright` | **deploying** (unchanged) | Make `isAuthorAllowed`'s ref dependency injectable + never-synced equivalence test |
| `test-t-079-shipwright` | **deploying** (unchanged) | `scripts/generate-agent-type-schema.ts` test-coverage class-level decision — human note on record (Dan, 2026-07-28) confirming this is a deliberate no-test-needed convention, already reflected in `test-system.md` |
| `test-t-080-shipwright` | **deployed** (unchanged) | HITL dev-loop bootstrap's full clone→seed→boot→poll sequence, injected `gh` exec fake |
| `test-t-081-shipwright` | **done** (unchanged) | `TaskService.claim()`'s concurrent-race close (real Postgres) |
| `test-t-082-shipwright` | **deploying** (unchanged) | `traceReviewCandidacyDecision`'s exclusion checks proven correct in 3+-condition combination |
| `test-t-083-shipwright` | **done** (unchanged) | Promote coverage required check from never-decrease to hard ≥90% gate |
| `test-t-084-shipwright` | **deployed** (unchanged) | Add `afterEach` reset to `agentSlackMembershipRef` singleton test |

**No status movement on any of the 9 tracked IDs this cycle.** T-076 through T-079 and
T-082 remain parked at `deploying` — now the **15th consecutive cycle** with zero
movement for T-076–T-079, all four confirmed merged 2026-07-25 through 2026-07-28 (now
~5.6 weeks ago) with `deployedAt: null` still unset. This cycle re-confirmed all four
merge commits (`15663f3c`, `778b3ba1`, `45435444`, `9a1bee28`) remain ancestors of current
`origin/main` (re-verified via `git merge-base --is-ancestor` against this cycle's exact
HEAD, `c1c81d4e`). **Still not force-patched this cycle** — same reasoning as every prior
cycle: this docs-refresh pipeline has no first-party mechanism to confirm a specific
Helm/image build actually rolled out, and blindly patching risks marking genuinely-stuck
work as resolved without evidence. Escalated again in Open risks below — now a
**15-cycle-old, unresolved-by-this-pipeline pattern**, matching known project history
(task-store status can get stuck at `deploying`/`deployedAt: null` indefinitely after a
real merge+ship, per prior investigation of this exact class of stale record) — this
strongly suggests the same known status-tracking gap rather than a new distinct issue,
but this pipeline still lacks the access to confirm and patch it directly.

**Task-store query for the starting T-NNN offset (per SKILL Process step 4), run before
minting any new IDs:** queried `$SHIPWRIGHT_TASK_STORE_URL/tasks?repo=app-vitals%2Fshipwright&limit=500`
— `total: 1036`, requiring two additional pages (`&offset=500` and `&offset=1000`, 1036
total scanned across 3 pages, matching `.total`). Filtered to `test-t-<NNN>-shipwright`
IDs across all three pages: highest is still `test-t-084-shipwright` (confirmed present,
unchanged status `deployed`). **`starting_offset = 85`** — this cycle's new task, if any,
would begin at **T-085**. Per Phase 3's findings (zero Promote/deepen, zero rebuild, zero
net-new beyond the same carried-forward optional item), **no new task is minted this
cycle** — T-085 remains reserved but unused.

**Zero new tasks minted this cycle.** Phase 3 (`test-migration.md`, regenerated this
cycle against the 2026-08-30→2026-09-02 delta — the largest window measured to date, 51
non-routine commits) found zero genuine Promote/deepen items, zero rebuild items, zero
delete/trim items — every genuinely new source row across the six feature clusters
(UAP provisioning-service extraction, PWA/push, TaskEvent audit log, Slack Bolt
5.x/`SlackProgress` migration, chat reply-notifier/stall-reaper, plus the wide PTL
migration cluster) landed with matching test coverage in the same commits/PRs. Two novel
dependency-runtime regression files (`slack-bolt-client.integration.test.ts`,
`slack-socket-mode-ping.integration.test.ts`) were correctly bucketed Reuse, not flagged
as a gap — see `test-migration.md`'s Summary note for why no inventory row applies to
them by design. One deliberate, well-justified indirect-coverage acceptance
(`scripts/build-pwa-icons.ts`, mirroring the already-accepted `hitl-log-file-tee-target.ts`
pattern) was correctly bucketed Reuse, not Net-new. One trivial/optional carried-forward
Net-new item (`admin/src/clock.ts`, explicitly not worth a task per every prior cycle's
same finding). The Coverage Gate's stage-3 promotion trigger (Process step 9) found T-083
already minted and still `done` — no duplicate emitted for it.

## 1. Where we are now

### Layer coverage map

| Layer | Existing tests | Inventory items | Coverage gap |
|---|---|---|---|
| Unit | 157 `.unit.test.ts` | Dominant category-1 surface (`lib/`, decision/classification logic, mutable-box refs, crypto helpers, parsing/validation, coverage-gate tooling, VAPID/push-payload builders, `task-transition-diff.ts`, `slack-thread-status-tracker.ts`) | None |
| Integration | 71 `.integration.test.ts` | Every category-2/5 service-boundary and external-integration item, incl. the new provisioning-service extraction, TaskEvent write-through, and the two novel dependency-runtime regression files (`slack-bolt-client.integration.test.ts`, `slack-socket-mode-ping.integration.test.ts`) | None |
| Smoke | 47 `.smoke.test.ts` | Every category-3 HTTP route across 5 services, incl. the new PWA-shell/push-route wire-contract coverage | None |
| E2E | 24 `site/tests/*.spec.ts` + 7 `{admin,metrics}/e2e/*.e2e.ts` | 4 of 5 identified journeys are browser-driven or partially so (journey #5, HITL bootstrap, is integration-composed, not E2E); admin e2e now covers the Slack-progress UI states (`chat-degraded.e2e.ts`, `chat-progress.e2e.ts`) alongside the existing chat/mobile-responsive assertions | None |
| Content | 43 `.content.test.ts` | `plugins/shipwright/{commands,agents,skills,references,test}/*` plus root-level content tests | None — unchanged this cycle |
| Shell (unofficial 6th layer) | 2 `test-*.sh` bash assertion suites | `.github/workflows/lib/chart-tag-utils.sh`, `chart-drift-check.sh` | None — coverage exists; only a documentation-completeness question is open (see Open risks) |
| Compile-time-only (unformalized) | 1 `*.check.ts` (`blocked-by-type-parity.check.ts`) | `admin/src/blocked-by-type-parity.check.ts` | None — wired into CI's `typecheck` step, confirmed passing; only a taxonomy question open (see Open risks) |

**Total: 349 test files, up from the 2026-08-30 pass's 324** (net +26/−1: 26 files added —
19 backing this cycle's six new feature clusters, 4 first-time unit-test files for
already-inventoried source rows (`cron-handler.unit.test.ts`,
`message-service.unit.test.ts`, `task-store-provider.unit.test.ts`,
`http-chat-client.unit.test.ts`), and 2 novel dependency-runtime regression files with no
first-party source row (`slack-bolt-client.integration.test.ts`,
`slack-socket-mode-ping.integration.test.ts`) — 1 removed: a rename
(`check-dependabot-triage.unit.test.ts` → `check-dependency-bot-triage.unit.test.ts`),
not a real deletion; see `test-migration.md`'s Method note and Summary for the full
accounting).

**`## Features` section in `test-inventory.md` unchanged this cycle:** 23 features,
`feature_coverage_pct` still 100% (23/23) — no new feature introduced. This is the source
for the Coverage Gate block above.

### Local-runnable status

**100%** of the `bun test`-scanned files run locally with zero external network. Phase
3's isolation-contract grep sweep (`mock.module(`, `global.fetch =`, `global.console =`,
stray `DATABASE_URL` inside `*.unit.test.ts`) found zero *literal* violations as of its
last run (unchanged from every prior pass) — see `test-migration.md`'s Isolation contract
audit. No test is blocked on an external service.

### Canary status

**Not applicable.** Root `CLAUDE.md` declares `## Deploy model: direct` — no
staging/production GitHub Environments, no deploy→canary→promote pipeline. Unchanged
finding across every cycle to date. See Milestone 3.

### Speed status (Tier 1 aggregate vs. budget; Tier 2 not triggered)

**Tier 1 — aggregate (measured this pass, via live CI history):**

| Aggregate wall-clock | Budget | Ratio |
|---|---|---|
| 2m34s ([run 33595391033](https://github.com/app-vitals/shipwright/actions/runs/33595391033), 2026-09-02T05:36:58Z, commit `c1c81d4e` — this cycle's exact HEAD) | <15 min (Full PR pipeline) | ~17% of budget |

Fully green run — no failing jobs. `bun test` step itself: 46.95s. 7,870 tests pass, 0
fail, across 319 files. Coverage held at 91.58% lines / 90.25% functions (threshold
90%/89%). Consistent with every prior cycle's ~13-18% readings — not trending toward the
escalation trigger. Test-count growth trajectory: 5,762 (2026-07-28) → 6,041-6,042
(2026-08-01) → 6,070 (2026-08-05) → 6,071 (2026-08-06) → 6,177 (2026-08-09) → 6,288
(2026-08-11) → 6,501 (2026-08-14) → 6,864 (2026-08-16) → 6,920 (2026-08-18) → 7,190
(2026-08-22) → 7,211 (2026-08-23) → 7,228 (2026-08-25) → 7,326 (2026-08-28) → 7,444
(2026-08-30) → **7,870 (2026-09-02, this pass)** — +426 tests, the largest single-cycle
jump on record, tracking this cycle's unusually large 51-non-routine-commit delta 1:1
(see `test-migration.md`'s Method note) — no anomaly, no padding.

**Tier 2 triggered:** no — the escalation formula (aggregate wall-clock >7.5 min,
sustained across 2 consecutive measurements) is nowhere near tripped (~17% of budget,
well below the 50% escalation threshold). No per-layer p95 table is populated below, per
the SKILL's explicit guidance against fabricating per-layer numbers when only the Tier 1
aggregate is available.

### "Rebuild" / "Delete (redundant)" debt

| Bucket | Count | Effort |
|---|---|---|
| Rebuild | 0 | — |
| Promote / deepen | 0 | — |
| Delete (redundant) | 0 | — |
| Net-new | 1 | trivial, optional |

Rebuild, Promote/deepen, and Delete all remain zero this cycle — a sixth consecutive
clean cycle. No higher-layer test re-asserts a business rule already proved at a lower
layer; no test's layer/framework/fundamentals were found wrong. Net-new remains at 1 —
only the long-standing, explicitly-optional `admin/src/clock.ts` item.
`scripts/build-pwa-icons.ts` (new this cycle) was correctly bucketed Reuse via an
accepted indirect-coverage pattern, not Net-new — see `test-migration.md`'s Method note.

## 2. Where we want to be

- **Framework matrix** — already fully realized: `bun test` (unit/integration/smoke/content),
  Playwright (E2E, 3 surfaces), plain `bash` (the shell layer). No framework changes
  needed.
- **Local-substitute map** — every dependency named in the inventory has a working local
  substitute (recorded-fixture doubles for internal/third-party HTTP, real Postgres via
  CI service container for the three Prisma-backed services, a real locally-launched
  Chromium binary for `agent/src/browser.ts`). No blockers.
- **Coverage targets** — single 90/89 line/function CI floor (`scripts/check-coverage.ts`,
  raised from 80/80 by MTC-1.7, to 89% lines by MTC-1.7's follow-up, then to 90% lines by
  T-083/CLG-1.1 this cycle), code-side change already enforced (branch-protection swap
  status: see Coverage Gate section and Open risks). Tiered targets (critical ~95%, high
  ~80%, medium spot-check) direct prioritization, not a second CI gate.
- **Canary suite definition** — not applicable under the current `direct` deploy model;
  deferred until a staged-deploy model exists.
- **Speed budgets per layer** — Unit <50ms p95 / Integration <2s / Smoke <5s / E2E <30s /
  full PR pipeline <15min. Tier 1 aggregate confirms comfortable headroom (~18% of
  budget); no per-layer breakdown needed since Tier 2 has never triggered.
- **Zero `rebuild` / `delete (redundant)` / `promote-deepen` buckets** — still true today,
  a third consecutive clean cycle.
- **Every critical- and high-tier inventory item covered at its prescribed layer,
  including a genuine concurrent-caller regression test wherever a claim/lock race was
  closed, and combination coverage across interacting exclusion checks in
  candidate-selection logic** — all standards remain fully met, unchanged this cycle. No
  open depth gaps remain in this category.

## 3. The gap

- **No missing layers.** All six layers (five `bun test`-declared plus the unofficial
  shell layer) in active use with the correct framework per layer, plus one
  deliberately-unformalized compile-time-only pattern (`blocked-by-type-parity.check.ts`)
  that has no coverage gap despite not fitting the existing taxonomy.
- **Wrong-layer tests:** 0.
- **External-only deps with no local substitute:** none.
- **No open depth gaps carried in from the prior cycle** — the 2026-08-30 cycle closed
  clean (0 open Promote/deepen items).
- **New this cycle (2026-09-02): all genuinely new source rows across the six feature
  clusters in the 51-non-routine-commit delta landed with matching coverage in the same
  commits/PRs.** UAP-1.1/1.2's `SlackProvisioningService`/`GithubProvisioningService`
  extraction (pure extraction, no behavior change), CFB-4.2's PWA shell + Web Push
  (9 new test files, incl. an injected-`fetchImpl`-seam pattern verified directly against
  the isolation contract), TCS-1.1/1.3's TaskEvent audit-log write-through (mirrors the
  existing `pr-transition-diff.ts` pattern) plus a new DB CHECK constraint and
  `claim-invariant-reconciler.ts` self-heal pass, SSB-1.1/SAV-1.1's Slack Bolt 5.x upgrade
  + `SlackProgress`/`slack-thread-status-tracker.ts` migration to
  `agents.sessions.setStatus` (superseding prior thread-message-editing logic), and
  CFB-4.3's chat reply-notifier webhook + stall-reaper — every one landed with matching
  test coverage at the correct canonical layer in the same commit/PR — no gap, no task
  needed for any of them. The wide PTL-1.x/2.x migration cluster (6 PRs moving PR/task
  linking off the deprecated stored `taskId` field) touched 5+ already-inventoried rows
  with in-place test extensions, no new row, no gap. Two novel dependency-runtime
  regression files (`slack-bolt-client.integration.test.ts`,
  `slack-socket-mode-ping.integration.test.ts`) test installed `@slack/bolt`/
  `@slack/socket-mode` package behavior directly, with no first-party inventory row by
  design — not a gap, correctly scoped. See `test-migration.md`'s Method note for the
  full per-item accounting.
- **T-083's stage-3 coverage-promotion, including the branch-protection swap, remains
  fully confirmed done** — see Coverage Gate section above. No remaining open question on
  this item.
- **1 carried-forward, low-priority, non-gap item:** `admin/src/clock.ts` has no
  dedicated test file — consistent with the established convention shared by all sibling
  `Clock` implementations across the repo (near-zero logic, proven via consumers). Not
  scored as a gap; no task minted.
- **Infra gap: none.** Runner config, CI shape, Postgres containers, `Clock` helpers,
  recorded-fixture doubles, PR template all confirmed unchanged and correct. The
  isolation-contract audit's own pattern set still has a documented blind spot
  (singleton-mutation leaks vs. literal `global.*` overrides, as an automated sweep) —
  carried forward, see Open risks.

### Speed delta

**Tier 1 — aggregate-vs-budget (leads; always populated):**

| Aggregate wall-clock | Budget | Ratio / time-to-close |
|---|---|---|
| 2m34s | <15 min (Full PR pipeline) | ~17% of budget — no time-to-close concern |

**Tier 2 — per-layer ratio (conditional on Tier 2 having triggered):**

**Tier 2 triggered:** no. Per-layer numbers are not fabricated here — Tier 1's aggregate
is the complete, sufficient measurement this cycle. Growth trajectory (319 files,
7,444 → 7,870 tests since the 2026-08-30 baseline, the largest single-cycle jump on
record) is steady with no budget concern at any point measured.

## 4. Roadmap (five milestones)

### Milestone 1 — Infrastructure baseline
**DOD:** Runners, local substitutes, CI pipeline shape in place. No new tests yet — just the rails.

**T-083 (auto-emitted stage-3 coverage-check promotion) remains `done`.** No new
Milestone 1 task minted. Naming convention + runner-exclusion config
(`docs/test-readiness/naming.md`, `bunfig.toml`'s `pathIgnorePatterns`) is established and
re-verified unchanged this cycle. CI pipeline shape matches `test-system.md`'s documented
target exactly. Postgres service containers, `Clock` injection, and recorded-fixture
doubles are all confirmed in place. T-074 (speed-baseline Postgres refresh) remains
`cancelled` — no successor task needed given Tier 1's CI-sourced measurement is a
complete, sufficient answer.

**T-083's branch-protection swap remains confirmed complete** (see Coverage Gate section)
— no open follow-up remains for this item.

`recorded_fixture_automation` is undeclared in root `CLAUDE.md` (re-verified this cycle —
no `## Recorded-fixture automation` section present, defaults to `planned` per this
cycle's read mechanism) — no repo-config declaration opts this repo out of the
recorded-fixture-refresh-loop pattern. That loop remains correctly **deferred, not
scheduled** (`test-system.md`'s Repo configuration section) — sequenced after the manual,
hand-authored-fixture workflow matures, not alongside it. No further M1 task minted for
it this cycle.

### Milestone 2 — Critical-path coverage
**DOD:** 100% of `critical` tier inventory items have passing tests at the prescribed layer.

**Already satisfied — zero tasks this cycle.** No open items were carried in from the
2026-08-30 pass (it closed clean). No new critical-tier gap found this cycle — the new
`critical`-tier rows this cycle (`agent/src/claim-invariant-reconciler.ts`,
`admin/src/slack-provisioning-service.ts`/`github-provisioning-service.ts`) all have
matching coverage at their prescribed layer, landed in the same PR as the production code.

### Milestone 3 — Canary suite live
**DOD:** canary not applicable -- deploy_model=direct

Zero tasks, same as every prior cycle. See `test-system.md`'s Canary execution contract
section for what would need to change before this applies.

### Milestone 4 — High-tier coverage
**DOD:** 100% of `high` tier inventory items covered.

**Already satisfied — zero real-gap tasks.** All prior high-tier items (T-080's HITL
bootstrap sequencing among them) remain resolved, unchanged since their respective
resolution cycles.

`admin/src/clock.ts` remains the sole other Net-new item from Phase 3 — carried forward,
unchanged, explicitly a non-gap (established `Clock`-file convention, near-zero logic,
proven via consumers). No task minted for it, same as every prior cycle.

### Milestone 5 — Cleanup
**DOD:** Zero tests in `rebuild` or `delete (redundant)` buckets. CI passes.

**Already satisfied — zero tasks.** Phase 3 found zero `rebuild` and zero `delete
(redundant)` items this cycle, the same steady state as every prior pass. No new
generalizable learning surfaced this cycle warranting a `# Harness TODO`.

## 5. Task list

**Zero new tasks minted this cycle.** Phase 3 (`test-migration.md`) found zero
Promote/deepen, zero rebuild, zero delete/trim, and zero net-gap items beyond the same
carried-forward optional `admin/src/clock.ts` entry (explicitly not worth a task, per
every prior cycle's identical finding). `starting_offset` was computed as **T-085**
(see Status section above) but is unused this cycle — no row to emit.

T-083 (the auto-emitted stage-3 coverage-check promotion task) remains `done`, with its
branch-protection question confirmed complete (see Coverage Gate section). T-084 remains
`deployed`, unchanged.

**Resolved (re-verified via direct task-store query this cycle, not carried forward from
the prior doc):** T-080, T-081, T-083, T-084 all confirmed at their terminal/near-terminal
status, unchanged (`deployed`/`done`/`deployed` as applicable — see the Status table above
for exact values). T-076 (`agent-type-manifest-loader` real-disk split — **deploying**),
T-077 (`check-review.ts` fail-open catch-path test — **deploying**), T-078 (injectable ref
+ never-synced equivalence test — **deploying**), T-079 (`generate-agent-type-schema.ts`
coverage class-level decision — **deploying**), T-082 (`traceReviewCandidacyDecision`
combinatorial-exclusion test — **deploying**) remain parked, unchanged since 2026-08-06
(T-076–T-079) / 2026-08-16 (T-082), all four of T-076–T-079 confirmed merged 2026-07-25
through 2026-07-28 with `deployedAt: null` still unset — **15 consecutive cycles with zero
status movement**, see Open risks below for the escalation.

## 6. Open risks

- **T-076 through T-079 have now shown zero status movement for 15 consecutive
  test-readiness cycles (parked at `deploying`).** All four merged 2026-07-25 through
  2026-07-28 (now ~5.6 weeks ago) with `deployedAt: null` still unset, and none carries a
  `note` field explaining a specific transient failure to reconcile against (unlike T-081
  and T-083, both of which did, and both turned out to be stale-record cases rather than a
  genuine block; T-079 does carry an unrelated human note recording a scope decision, not
  a deploy-status explanation). This cycle re-confirmed the four merge commits
  (`15663f3c`, `778b3ba1`, `45435444`, `9a1bee28`) remain ancestors of current
  `origin/main` (re-verified against this cycle's exact HEAD, `c1c81d4e`) — the code is
  unambiguously present in the current tree, and this repo's `deploy_model: direct` means
  every merge to main ships via the next Helm chart/image build with no staging gate to
  get stuck behind. This docs-refresh pipeline still has no first-party way to confirm a
  specific build actually rolled out, so this cycle again did **not** force-patch these
  four to `done`. **Escalating again — now a 15-cycle-old (~5.6-week) stale-status
  pattern**: recommend a human (or the `deploy`/`patch` pipeline's own diagnostics)
  directly check GitHub Actions deploy-workflow runs following each of the four merge
  commits (PRs #2207, #2227, #2228, #2264) to confirm an actual image build/rollout
  completed — if so, the fix is the same stale-record PATCH T-081 and T-083 both got.
  **Given the pattern is now 15 cycles deep with zero movement, this docs pipeline
  recommends the next human touchpoint treat this as a standing item to actively resolve
  via the same PATCH mechanism, not just re-confirm.**
- **The isolation-contract audit's grep pattern set still has a documented blind spot.**
  The one known instance of this leak class (T-084, `agent-slack-membership-ref.unit.test.ts`)
  was fixed back in the 2026-08-23 cycle, but it was found by a manual diff read, not an
  automated sweep — the pattern set still can't catch a recurrence of this leak class on
  its own. Recommend a future cycle add a fourth signal scanning for direct
  `.set(`/`.reset(`/mutation calls on this repo's known singleton-ref exports
  (`agentReposRef`, `agentAuthorAllowlistRef`, `agentSlackMembershipRef`, `loopJobsRef`,
  and future `agent-*-ref.ts` additions) with no paired `afterEach`/`afterAll` in the same
  file — this is a Phase 3 process improvement, not itself a task against the codebase.
  Carried forward unchanged, no new instance found this cycle.
- **The compile-time-only "type-parity check" pattern (`blocked-by-type-parity.check.ts`)
  still needs a taxonomy decision.** Not a coverage gap — confirmed wired into CI and
  passing — but it doesn't fit any of the five CLAUDE.md-declared suffixes or the
  unofficial sixth "shell" layer. `test-system.md` deliberately declines to formalize a
  seventh layer on a single instance; revisit if a second such file appears.
- **The bash "shell" layer still needs a formal documentation decision.** Carried forward
  unchanged across several cycles now (first flagged 2026-07-31, re-verified present and
  unchanged this cycle): `.github/workflows/lib/chart-tag-utils.sh` / `chart-drift-check.sh`
  and their `test-*.sh` siblings are genuinely tested code units that fall entirely
  outside the `bun test` runner and the five-suffix convention declared in root
  `CLAUDE.md`. Coverage itself is not in question — the open question is purely whether a
  human wants to formally document this as a sixth layer in `CLAUDE.md`'s `## Test
  conventions` table and `docs/test-readiness/naming.md`, or confirm it should stay an
  unofficial exception. Not urgent; not blocking any task above.
- **Recorded-fixture coverage breadth** (~9 remaining `Http*Client` modules that could
  extend the `Recorded*Client` pattern) remains a Phase 2 design-level backlog item, not
  a migration-bucket verdict on any existing file. No task emitted this cycle since
  Phase 3 found no test currently missing coverage because of it.
- **`agent/src/piper-voice.ts` reclassification watch item** (unit today, integration
  once Piper synthesis lands) — still not landed as of this cycle; flagged so a future
  Phase 3 doesn't silently miss the transition.
- **Recorded-fixture maintenance loop remains deferred** (`repo-config/SKILL.md` §5) —
  correctly not scheduled this cycle; sequence after fixture-authoring work matures. No
  task this cycle touches credential-provisioning automation, so this note doesn't apply
  to anything minted this cycle (nothing was minted).

---

## Handoff

This document is intended for execution by:
- An engineer working through the task list manually, OR
- `shipwright /dev-task` consuming the task list as a queue.
