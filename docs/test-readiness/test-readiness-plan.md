# Test Readiness Plan

> Phase 4 synthesis. The executable roadmap from where we are now to a test suite trustworthy enough for autonomous programming.
>
> Generated by `/test-roadmap` on 2026-07-25 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-20260725`), consuming `test-inventory.md` (refreshed at
> `24bf90f3`), `test-system.md` (refreshed at `24bf90f3`), and `test-migration.md`
> (refreshed at `24bf90f3`).

`deploy_model` = **`direct`** (root `CLAUDE.md`'s `## Deploy model` section) — Milestone 3
is not applicable this cycle; see below. `recorded_fixture_automation` is **undeclared**
in root `CLAUDE.md` (no `## Recorded-fixture automation` section present) — defaults to
`planned` per the SKILL's read-with-fallback rule; no recorded-fixture-refresh tasks are
generated this cycle regardless, since none were flagged as a migration gap (the loop
itself remains design-deferred per `test-system.md`'s Repo configuration section, not a
Phase 4 task).

## Status of the prior cycle's (2026-07-21) Milestone tasks

Queried directly against task-store (all 675 tasks in `app-vitals/shipwright`, regardless
of status, paginated past the 500-row default limit):

| Task | Status | Title |
|---|---|---|
| `test-t-051-shipwright` | **done** (merged 2026-07-22) | Verify main's branch-protection required-checks against `ci.yml` |
| `test-t-071-shipwright` | **deploying** (merged 2026-07-23, PR #2178) | `mcp-server/src/main.ts` fail-closed auth guard smoke test |

**Correction to this cycle's `test-migration.md`:** Phase 3's Net-new section carries
`mcp-server/src/main.ts` forward as "unchanged, still open" — this is **stale**. Direct
verification against the current worktree tree confirms `mcp-server/src/main.smoke.test.ts`
exists (added by PR #2178, commit `7b7ae8cf`, merged 2026-07-23) and asserts the
fail-closed guard throws when `SHIPWRIGHT_MCP_SERVER_TOKEN` is unset. Task-store confirms
`test-t-071-shipwright` merged the same day. This item is **resolved**, not a gap — it is
excluded from this cycle's Milestone 2 task list below (see Open risks for the process
note on why Phase 3 missed this). T-051 is fully resolved (branch protection updated live
via `gh api`) and likewise carries forward no task this cycle.

## 1. Where we are now

### Layer coverage map

| Layer | Existing tests | Inventory items (canonical layer) | Coverage gap |
|---|---|---|---|
| Unit | 117 | ~90+ rows tagged unit across all categories | 0 net-new; `admin/src/clock.ts` remains optional/low, consistent with sibling `Clock` files having no dedicated test |
| Integration | 56 | ~55+ rows tagged integration (service-boundary + external) | 0 net-new — 1 **Promote** finding: `admin/src/agent-type-manifest-loader.unit.test.ts` has 2/14 cases doing real file I/O that belong in a sibling `.integration.test.ts` file |
| Smoke | 43 | ~20 rows tagged smoke (HTTP routes + route-equivalents) | 0 net-new — `mcp-server/src/main.ts`'s fail-closed guard gap (open at the 2026-07-21/23 passes) is now **resolved** (T-071, PR #2178) |
| E2E | 23 (20 `site/*.spec.ts` + 3 `*/e2e/*.e2e.ts`) | 1 genuinely browser-driven journey (marketing site) + 4 e2e/smoke-composed journeys proven at integration layer | 0 — no new browser-driven journeys this cycle; `admin/e2e/agents-page.e2e.ts`'s required-`typeName`-picker assertion re-validated against the new provisioning flow, confirmed passing |
| Content (5th layer) | 28 | ~4 grouped rows (commands/agents/skills/references) | 0 — fully covered; command count now 28 (up from 19 two cycles ago), all with matching `.content.test.ts` |

**Total: 267 test files**, up from 249 at the 2026-07-21 pass (+18: the Agent Type
manifest system landing end-to-end this cycle — 4 new test files plus in-place growth in
7 existing files; see `test-migration.md`'s Summary for the full breakdown).

### Local-runnable status

- **Tests that run locally with zero external network:** effectively 100% of the 267
  files — Phase 3's isolation-contract grep sweep (`mock.module(`, `global.fetch =`,
  `global.console =`, stray `DATABASE_URL` in unit tests) found zero real violations
  across the full current suite, despite +18 new/grown files this cycle.
- **Tests blocked on external services:** none.

### Canary status

**Not applicable.** Root `CLAUDE.md` declares `## Deploy model: direct` — no
staging/production GitHub Environments, no deploy→canary→promote pipeline. See Milestone
3 below.

### Speed status (current p95 per layer)

`docs/test-readiness/speed-baseline.md` (on `main` since 2026-07-15, PR #1580) is the
only real measurement that exists, and it is now **10 days stale** relative to this
cycle's substantial in-place logic growth (`agent/src/check-*.ts`, `task-store/src/task-service.ts`,
and the entire Agent Type manifest system). Per-layer figures at last measurement: unit
1.05–2.50s per package (suite target <30s), integration 0.15–5.35s (target <3min), smoke
3.24–4.62s (target <30s/<60s), e2e 4–40s via CI (target <10min), full suite 34.23s
wall-clock (target <15min). No layer was over its hard cap **as measured**, but this
pass — like the four before it — could not re-measure: **no local Postgres in this
sandbox** (`DATABASE_URL_SHIPWRIGHT_*` unset), so a `bun test` run here would silently
skip every DB-backed integration test and produce misleading timings. Static review
(line counts, import/mock shape) found no evidence of a hard-cap breach this cycle, but
static review cannot detect a test that has silently grown slow within its existing
budget headroom.

**This measurement-only item has been carried forward unactioned across 5 consecutive
`/test-migration` cycles** (2026-07-16, 2026-07-19, 2026-07-21, 2026-07-23, 2026-07-25),
crossing the SKILL's 3-cycle escalation threshold. Per the SKILL's Milestone 1
carried-forward-item rule, this is placed as **the first Milestone 1 task** below (T-074),
ahead of the naming-convention task, by construction — not left to be noticed in prose.

### "Rebuild" debt

| Bucket | Count | Effort |
|---|---|---|
| Rebuild | 0 | — |

### "Delete (redundant)" count

**Zero.** No higher-layer test was found re-asserting a business rule already proved at a
lower layer. No test currently passing (green) received a `delete` or `rebuild` verdict
this pass, same as all four prior passes.

## 2. Where we want to be

- **Framework matrix** — already fully realized: `bun test` (unit/integration/smoke/content),
  Playwright (E2E, 3 surfaces: site, admin, metrics). No framework changes needed.
- **Coverage targets** — single 80/80 line/function CI floor (`scripts/check-coverage.ts`),
  already enforced repo-wide; 100% of `critical`- and `high`-tier inventory items covered
  at the prescribed layer, with one file-placement correction outstanding (the
  manifest-loader Promote split) and one measurement-infra gap (the Postgres/speed item).
- **Canary suite** — not applicable under the current `direct` deploy model; deferred
  until a staged-deploy model exists.
- **Speed budgets per layer** — Unit <50ms p95 / Integration <2s / Smoke <5s / E2E <30s /
  full PR pipeline <15min, per `speed-budgets/SKILL.md` defaults, unmodified. **Currently
  unverifiable from this sandbox** — the baseline exists but is stale and cannot be
  refreshed without a Postgres-enabled environment (see Milestone 1, T-074).
- **Zero `rebuild` / `delete (redundant)` buckets** — already true today.

## 3. The gap

The gap remains small — the suite continues to track its own blueprint closely — but
this cycle surfaces one process gap that dominates the roadmap's shape:

- **No missing layers.** All five layers in active use with the correct framework per
  layer.
- **Wrong-layer tests:** 1 (`admin/src/agent-type-manifest-loader.unit.test.ts`'s 2 of 14
  cases do real filesystem I/O inside a `.unit.test.ts`-suffixed file — a file-placement
  issue, not a logic defect; see Milestone 1).
- **External-only deps with no local substitute:** none.
- **Net-new coverage gaps (from Phase 3, corrected):** 0 real gaps. `mcp-server/src/main.ts`
  (previously the sole real Net-new gap across 2 cycles) is confirmed resolved this cycle
  (T-071, PR #2178) — Phase 3's doc text is stale on this point; see Status section above.
  `admin/src/clock.ts` remains a non-gap by established repo convention.
- **Infra gap: none new.** All infrastructure items (runner config, CI shape, Postgres
  service containers *in CI*, `Clock` helpers, recorded-fixture doubles, coverage gate)
  confirmed unchanged and correct.
- **Measurement-infra gap (the dominant item this cycle): no local Postgres available to
  this pipeline's sandbox**, meaning the speed baseline that underpins the entire Speed
  budgets section of `test-system.md` cannot be refreshed by this pipeline's own passes —
  only by a run inside CI or an environment with `DATABASE_URL_SHIPWRIGHT_*` wired. This
  has now escalated past the 3-cycle threshold (5 cycles unactioned) and is Milestone 1's
  first task.
- **Canary:** not applicable — `deploy_model: direct`.

### Speed delta

| Layer | Current (measured, `speed-baseline.md`, 2026-07-15 — now 10 days stale) | Target | Ratio |
|---|---|---|---|
| Unit | 1.05–2.50s per package | <50ms per-test p95 / <30s suite target | within suite-target budget as last measured; per-test p95 not separately broken out (aggregate-only); **staleness makes this ratio unverified for the current 267-file suite** |
| Integration | 0.15–5.35s per package | <2s per-test p95 / <3min suite target | within suite-target budget as last measured; same staleness caveat |
| Smoke | 3.24–4.62s per package | <5s per-test p95 / <60s suite target | within budget on both dimensions as last measured; same staleness caveat |
| E2E | 4–40s per surface (CI-sourced) | <30s per-test p95 / <10min suite target | within budget as last measured; site's 40s step is a job-level step duration, not a single-test p95 |
| Full suite | 34.23s wall-clock | <15min full-PR budget | ~26x headroom as last measured — but this predates the Agent Type manifest system (+18 files) and two other cycles' growth |

**The actionable number:** the baseline is 10 days and roughly 18+ test files stale. It
is not flagged as a hard-cap breach, but it is no longer a number this pipeline can stand
behind without a fresh measurement — which requires Postgres, which requires running
outside this sandbox. Time-to-close: one CI-or-equivalent run with
`DATABASE_URL_SHIPWRIGHT_*` wired (T-074, Milestone 1).

## 4. Roadmap (five milestones)

### Milestone 1 — Infrastructure baseline
**DOD:** A real Postgres-backed per-layer speed measurement has replaced the 10-day-stale
baseline; the naming convention and runner-exclusion config are confirmed current; the
one file-placement (layer) mismatch is corrected. No new feature tests written yet — just
the rails.

**T-074 is placed first, by construction**, per the SKILL's mandatory rule for a
measurement-only item carried forward 3+ consecutive cycles unactioned (this one: 5
cycles — 2026-07-16, 2026-07-19, 2026-07-21, 2026-07-23, 2026-07-25).

1. **T-074 — Refresh the speed baseline against a real Postgres-enabled environment.**
   Escalated per the 3-cycle carry-forward rule; this is the first Milestone 1 task, ahead
   of the naming-convention re-verification. Not agent-executable inside this
   docs-pipeline sandbox (no `DATABASE_URL_SHIPWRIGHT_*` wired here) — requires either a
   CI job or a human/agent session with Postgres available. Run a real `bun test
   --coverage` (or equivalent `time`-wrapped per-layer run) with all three
   `DATABASE_URL_SHIPWRIGHT_*` vars set, capture per-layer p95/wall-clock numbers the same
   way `speed-baseline.md` (PR #1580) originally did, and overwrite that file's numbers +
   the "measured" date. Cross-reference the refreshed numbers into `test-system.md`'s
   Speed budgets section (mirrors T-067's original fold-in).
2. **T-075 — Naming convention + runner-exclusion re-verification.** `docs/test-readiness/naming.md`
   is confirmed current and complete this cycle (suffix table, reserved `.canary.` suffix,
   `bunfig.toml` cross-reference all match the live repo state) — no fresh file-naming
   rule is needed. This task is the **verification** the SKILL mandates every M1 pass
   carry regardless: run each entry point in isolation and confirm it discovers only its
   own layer's files, since the Agent Type manifest system added 4 new test files this
   cycle that must have landed inside the existing convention, not a parallel one.
3. **T-076 — Split `admin/src/agent-type-manifest-loader.unit.test.ts`'s 2 real-disk
   cases into a new `.integration.test.ts` file.** The Promote finding from
   `test-migration.md`: 2 of 14 cases (`"loads the real committed coding manifest from
   disk via the default reader"`, `"listTypes discovers the real committed agent-types/
   dir via the default list fn"`) construct `AgentTypeRegistry` with no injected
   `read`/`list`, falling through to `defaultManifestReader`/`defaultTypeDirLister` and
   hitting the real filesystem — a genuine I/O-boundary crossing inside a file whose
   `.unit.test.ts` suffix promises none. Small effort: a file split + 2 test relocations,
   no logic changes, no new fixtures (the component's prescribed canonical layer is
   integration per `test-system.md` line 493). Placed in M1 (infra/file-placement
   correction) rather than M2, since it's a naming/runner-exclusion-adjacent correctness
   issue, not new coverage.

**Recorded-fixture automation:** `recorded_fixture_automation` is undeclared in root
`CLAUDE.md`, defaulting to `planned` — no exception applies. No recorded-fixture-refresh
tasks are generated this cycle regardless, because none were flagged as a migration gap;
the loop itself remains explicitly design-deferred per `test-system.md`'s Repo
configuration section (§"Recorded-fixture maintenance loop — deferred") pending the
hand-authored workflow maturing first. Nothing to schedule.

**Pairing-rule check:** none of T-074/T-075/T-076 create or modify a CI workflow file
(`.github/workflows/*`) — T-074 changes a docs artifact (`speed-baseline.md`) via a run
that happens outside this sandbox, T-075 is verification-only, T-076 is a test-file
relocation. No paired branch-protection task applies this cycle.

| Task | Files to touch | Layer | Bucket origin | Expected outcome | Verification command |
|---|---|---|---|---|---|
| T-074 | `docs/test-readiness/speed-baseline.md`, `docs/test-readiness/test-system.md` (Speed budgets cross-reference) | infra | reuse (measurement refresh) | Real per-layer p95/wall-clock numbers captured against a Postgres-enabled run, replacing the 10-day-stale 2026-07-15 baseline | `DATABASE_URL_SHIPWRIGHT_ADMIN=... DATABASE_URL_SHIPWRIGHT_TASK_STORE=... DATABASE_URL_SHIPWRIGHT_CHAT=... bun test --coverage` (or CI equivalent), capture wall-clock + per-package timings |
| T-075 | (verification only — no source changes expected) | infra | reuse | Each runner (Jest/Bun `testMatch`, Playwright `testMatch`, coverage `exclude`) confirmed to discover only its own layer's files, including this cycle's 4 new Agent Type manifest test files | `bun test --filter unit` / `--filter integration` / `--filter smoke` each return zero cross-layer files; `cd site && npx playwright test --list` returns only `*.spec.ts` |
| T-076 | `admin/src/agent-type-manifest-loader.unit.test.ts`, `admin/src/agent-type-manifest-loader.integration.test.ts` (new) | integration | promote | The 2 real-disk-I/O cases relocated to a `.integration.test.ts` sibling; the remaining 12 injected-DI cases stay in `.unit.test.ts` | `bun test admin/src/agent-type-manifest-loader.unit.test.ts admin/src/agent-type-manifest-loader.integration.test.ts` |

### Milestone 2 — Critical-path coverage
**DOD:** 100% of `critical` tier inventory items have passing tests at the prescribed layer.

**Already satisfied — zero tasks.** Phase 3 (corrected, per the Status section above)
found every critical-tier inventory item covered. `mcp-server/src/main.ts` — the one
`critical`-tier item flagged as a real gap across the 2026-07-21 and 2026-07-23 cycles —
is confirmed resolved this cycle (`mcp-server/src/main.smoke.test.ts`, PR #2178, merged
2026-07-23; task-store `test-t-071-shipwright` status `deploying`). No net-new
critical-tier gap this cycle.

### Milestone 3 — Canary suite live
**DOD:** canary not applicable -- deploy_model=direct

Root `CLAUDE.md` declares `direct` — no staging/production GitHub Environments, no
deploy→canary→promote pipeline. Per the SKILL, zero tasks are emitted for this milestone;
no canary-plumbing work is generated for a pipeline that doesn't exist. See
`test-system.md`'s Canary execution contract section for what would need to change before
this applies.

### Milestone 4 — High-tier coverage
**DOD:** 100% of `high` tier inventory items covered.

**Already satisfied — zero tasks.** No net-new high-tier (or medium-tier) gaps this
cycle. `admin/src/clock.ts` remains optional-low and explicitly not tasked, per Phase 3's
non-gap assessment.

### Milestone 5 — Cleanup
**DOD:** Zero tests in `rebuild` or `delete (redundant)` buckets. CI green.

**Already satisfied — zero tasks.** Phase 3 found zero `rebuild` and zero `delete
(redundant)` items this cycle, same as all four prior passes. No new generalizable
plugin/skill-level learning surfaced this cycle warranting a `# Harness TODO` — the one
real process observation (Phase 3's stale carry-forward claim about `mcp-server/src/main.ts`,
corrected in this document's Status section) is a one-off staleness in that pass's own
diff-based method, not a repeatable pattern needing a skill-level fix; captured in Open
risks instead rather than a plugin-code change.

## 5. Task list

Flat, ordered, agent-executable. **Starting offset:** queried task-store directly across
all 675 tasks in `app-vitals/shipwright` (paginated past the 500-row default limit — page
1: 500 rows, page 2: 175 rows). Max existing `test-t-<NNN>-shipwright` ID found: `073`
(`test-t-073-shipwright`, status `pending`, unrelated to this cycle's gaps — a
pre-existing coverage-scoping decision for `site/src/lib/html-escape.ts`, left untouched).
`starting_offset = 74` → this cycle's tasks are numbered `T-074` onward, continuing rather
than restarting at `T-001`.

| ID | Milestone | Files to touch | Layer | Bucket origin | Expected outcome | Verification command | Depends on |
|---|---|---|---|---|---|---|---|
| T-074 | 1 | `docs/test-readiness/speed-baseline.md`, `docs/test-readiness/test-system.md` | infra | reuse (measurement refresh) | Real per-layer speed measurement against a Postgres-enabled environment, replacing the 10-day-stale baseline | `DATABASE_URL_SHIPWRIGHT_*` wired, `bun test --coverage`, capture + record timings | — |
| T-075 | 1 | (verification only) | infra | reuse | Naming convention + runner-exclusion config confirmed current against the +18-file delta | `bun test --filter unit`/`integration`/`smoke` each isolated; `npx playwright test --list` in `site/` | — |
| T-076 | 1 | `admin/src/agent-type-manifest-loader.unit.test.ts`, `admin/src/agent-type-manifest-loader.integration.test.ts` (new) | integration | promote | 2 real-disk cases relocated out of the `.unit.test.ts` file into a correctly-suffixed `.integration.test.ts` sibling | `bun test admin/src/agent-type-manifest-loader.unit.test.ts admin/src/agent-type-manifest-loader.integration.test.ts` | — |

No task in this cycle exceeds the ~1000-line cap, touches more than one functional
concern, or represents an N-service repeated operation — no task splitting was required.
No task creates or modifies a CI workflow file, so no pairing-rule branch-protection tasks
apply this cycle. Phase 5 (`test-fix`) should create fresh `test-t-074-shipwright` /
`test-t-075-shipwright` / `test-t-076-shipwright` records (none exist yet, confirmed via
the same task-store query) — not assume any of these three IDs are already claimed.

## 6. Open risks

- **T-074 cannot be completed inside this docs-pipeline sandbox.** No local Postgres is
  available here (`DATABASE_URL_SHIPWRIGHT_*` unset) — this is the same constraint that
  has blocked direct re-measurement for 5 consecutive `/test-migration` passes. T-074
  needs either a CI job with the Postgres service containers already wired (per
  `ci.yml`'s existing shape) or a human/agent session running outside this sandbox with
  the three DB env vars set. Flagging explicitly so this doesn't silently become a 6th
  unactioned cycle if T-074 sits `pending` past its natural completion path — if it's
  still open at the *next* `/test-roadmap` run, escalate to a human via HITL rather than
  re-carrying it a second time under a new number.
- **Phase 3's `test-migration.md` contains one confirmed-stale finding this cycle.** Its
  Net-new section (and Method note, and Speed measurement section) still describe
  `mcp-server/src/main.ts` as "unchanged, still open" — but direct verification against
  the current worktree (file exists, task-store confirms merged PR #2178) shows this gap
  closed as of 2026-07-23, two days before this Phase 3 pass ran. This document corrects
  the finding (see Status section) rather than propagating it into a duplicate task. No
  plugin-code action needed — recorded here per `feedback_actionable_followups_need_durable_tracking`-style
  practice, as a one-off diff-method miss (T-071 merged the same day as Phase 3's stated
  baseline commit `24bf90f3`'s ancestry, likely just outside the diff window Phase 3's
  method used) rather than a repeatable pattern needing a skill fix.
- **Recorded-fixture coverage breadth** (`test-system.md`'s gaps-summary item 5: ~9
  remaining `Http*Client` modules that could extend the `Recorded*Client` pattern) is a
  design-level backlog item, not a migration-bucket verdict on any existing file — no
  task emitted this cycle since Phase 3 found no test currently missing coverage because
  of it. Revisit if a future inventory pass tags one of those clients as critical/high
  with no test.
- **`scripts/hitl.ts` criticality/layer remains an open human-review question** (grew to
  739 lines this cycle with no criticality re-tag; `test-system.md`'s Ambiguous items
  section flags this explicitly). No test-system or roadmap action until a human signs
  off on whether "medium, dev-only" still holds at this size.
- **`agent/src/piper-voice.ts` reclassification watch item** (unit today, integration
  once Piper synthesis lands) — no action needed until that lands, still not landed as of
  this cycle.
- **Recorded-fixture maintenance loop remains deferred** (`repo-config/SKILL.md` §5) —
  correctly not scheduled this cycle; sequence after fixture-authoring work matures, per
  `test-system.md`'s explicit guidance. Consistent with `recorded_fixture_automation`
  being undeclared (defaults to `planned`, but nothing to schedule regardless since no
  gap triggers it).
- **`agent-types/coding/manifest.yaml`'s criticality tier (critical vs. high)** is an open
  human-review item per `test-inventory.md`'s Ambiguous items — no roadmap action needed
  either way since it already has full content-layer coverage; flagged for completeness
  only.
- **`test-t-073-shipwright` (site/src/lib/html-escape.ts coverage scoping) remains
  `pending`** in task-store, pre-dating this cycle and unrelated to any gap in this
  cycle's `test-migration.md` — left untouched, not re-derived or duplicated here.

---

## Handoff

This document is intended for execution by:
- An engineer working through the task list manually, OR
- `shipwright /dev-task` consuming the task list as a queue.
