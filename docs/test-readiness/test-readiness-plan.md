# Test Readiness Plan

> Phase 4 synthesis. The executable roadmap from where we are now to a test suite trustworthy enough for autonomous programming.
>
> Generated by `/test-roadmap` on 2026-08-18 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-20260818`), consuming `test-inventory.md` (refreshed at
> `f901803d`), `test-system.md` (refreshed at `f901803d`), and `test-migration.md`
> (refreshed at this cycle's HEAD, `f901803d`).

## Coverage Gate

The headline machine-checkable verdict — computed by `scripts/check-coverage-gate.ts`
(CGT-1.2), rendered verbatim below.

Inputs sourced per Process step 8: `linePct`/`lineSource` from this cycle's Step 4c CI
pull ([run 32103420774](https://github.com/app-vitals/shipwright/actions/runs/32103420774)
@ `f901803d`); `featurePct`/`featureSource` carried forward from `test-inventory.md`'s
`feature_coverage_pct` (unchanged this cycle, see that document's Features section).
Computed via:
```bash
echo '{"featurePct": 100, "featureSource": "docs/test-readiness/test-inventory.md", "linePct": 90.95, "lineSource": "CI run https://github.com/app-vitals/shipwright/actions/runs/32103420774 @ f901803d"}' \
  | bun run scripts/check-coverage-gate.ts
```

| Field | Value |
|---|---|
| Feature coverage | 100% (target 90%) — source: docs/test-readiness/test-inventory.md |
| Line coverage | 90.95% (target 90%) — source: CI run https://github.com/app-vitals/shipwright/actions/runs/32103420774 @ f901803d |
| **Verdict** | **READY** |

**Stage-3 coverage-check promotion — already minted, still pending.** `line_coverage_pct`
(90.95) remains above the 90% threshold `repo-config/SKILL.md`'s 3-stage coverage-check
lifecycle gates on, same as last cycle. **T-083** (minted 2026-08-16, "Promote coverage
required check from never-decrease to hard ≥90% gate") was queried directly this cycle —
`status: pending`, unchanged since minting. No duplicate task minted (per Process step 9's
existing-task check). See Milestone 1 and Open risks below for T-083's dispatch status.

## Status of the prior cycle's plan (2026-08-16) and everything since

Queried task-store directly (not assumed from the old doc; direct `GET /tasks/{id}`
lookups against each of the 8 known IDs from the prior cycle, plus a fresh full-repo scan
for the offset query below), current status of every relevant ID as of this cycle
(2026-08-18):

| Task | Status | Title |
|---|---|---|
| `test-t-076-shipwright` | **deploying** (unchanged) | Split `agent-type-manifest-loader.unit.test.ts`'s real-disk cases into a new `.integration.test.ts` file |
| `test-t-077-shipwright` | **deploying** (unchanged) | `check-review.ts`'s `isBundleComplete` fail-open catch-path test |
| `test-t-078-shipwright` | **deploying** (unchanged) | Make `isAuthorAllowed`'s ref dependency injectable + never-synced equivalence test |
| `test-t-079-shipwright` | **deploying** (unchanged) | `scripts/generate-agent-type-schema.ts` test-coverage class-level decision |
| `test-t-080-shipwright` | **deployed** (unchanged) | HITL dev-loop bootstrap's full clone→seed→boot→poll sequence, injected `gh` exec fake |
| `test-t-081-shipwright` | **done** (unchanged) | `TaskService.claim()`'s concurrent-race close (real Postgres) |
| `test-t-082-shipwright` | **deploying** (unchanged) | `traceReviewCandidacyDecision`'s exclusion checks proven correct in 3+-condition combination |
| `test-t-083-shipwright` | **pending** (unchanged since minting 2026-08-16) | Promote coverage required check from never-decrease to hard ≥90% gate |

**No status movement on any of the 8 carried-forward IDs this cycle.** T-076 through
T-079 remain parked at `deploying` — now the **8th consecutive cycle** with zero
movement, all four confirmed merged 2026-07-25 through 2026-07-28 (3-3.5 weeks ago) with
`deployedAt: null` still unset. This cycle re-confirmed all four merge commits remain
ancestors of current `origin/main`. **Still not force-patched this cycle** — same
reasoning as every prior cycle: this docs-refresh pipeline has no first-party mechanism
to confirm a specific Helm/image build actually rolled out, and blindly patching risks
marking genuinely-stuck work as resolved without evidence. Escalated again in Open risks
below — now an 8-cycle-old, unresolved-by-this-pipeline pattern.

**T-083 (minted 2026-08-16, the coverage-gate stage-3 promotion task) is still `pending`
— not yet dispatched by `/shipwright:dev-task`.** No PR/branch activity observed against
`feat/test-t-083-coverage-gate-promotion` beyond the branch name recorded at minting.
Not re-minted or duplicated this cycle (Process step 9's existing-task check correctly
found it via the task-store query below and did not create a second copy).

**Task-store query for the starting T-NNN offset (per SKILL Process step 4), run before
minting any new IDs:** queried `$SHIPWRIGHT_TASK_STORE_URL/tasks?repo=app-vitals%2Fshipwright&limit=500`
— `total: 887`, requiring one additional page (`&offset=500`, +387 records, 887 total
scanned, matching `.total`). Filtered to `test-t-<NNN>-shipwright` IDs across both pages:
highest is `test-t-083-shipwright` (already minted last cycle, confirmed still present).
**`starting_offset = 84`** — this cycle's new tasks (none minted, see below) would begin
at **T-084**.

**This cycle mints zero new tasks.** Phase 3 (`test-migration.md`) found zero
Promote/deepen items, zero rebuild items, and only one trivial/optional carried-forward
Net-new item (`admin/src/clock.ts`, explicitly not worth a task per every prior cycle's
same finding) — no roadmap task material. The Coverage Gate's stage-3 promotion trigger
(Process step 9) found T-083 already minted and pending, so it did not mint a duplicate.

## 1. Where we are now

### Layer coverage map

| Layer | Existing tests | Inventory items | Coverage gap |
|---|---|---|---|
| Unit | 132 `.unit.test.ts` | Dominant category-1 surface (`lib/`, decision/classification logic, mutable-box refs, crypto helpers, parsing/validation, coverage-gate tooling) | None — every inventory item mapped to unit has a matching test file |
| Integration | 66 `.integration.test.ts` | Every category-2/5 service-boundary and external-integration item | None — new `PrFinding` model (PFL-1.1/PFL-2.1) landed with matching integration coverage this cycle |
| Smoke | 44 `.smoke.test.ts` | Every category-3 HTTP route across 5 services | None — new `POST /prs/:id/findings` route and `?hitl` filter both landed with matching smoke coverage this cycle |
| E2E | 21 `site/tests/*.spec.ts` + 3 `{admin,metrics}/e2e/*.e2e.ts` | 4 of 5 identified journeys are browser-driven or partially so (journey #5, HITL bootstrap, is integration-composed, not E2E) | None |
| Content | 41 `.content.test.ts` | `plugins/shipwright/{commands,agents,skills,references}/*` | None — unchanged this cycle |
| Shell (unofficial 6th layer) | 2 `test-*.sh` bash assertion suites | `.github/workflows/lib/chart-tag-utils.sh`, `chart-drift-check.sh` | None — coverage exists; only a documentation-completeness question is open (see Open risks) |
| Compile-time-only (unformalized) | 1 `*.check.ts` (`blocked-by-type-parity.check.ts`) | `admin/src/blocked-by-type-parity.check.ts` | None — wired into CI's `typecheck` step, confirmed passing; only a taxonomy question open (see Open risks) |

**Total: 307 test files** (283 `bun test`-scanned + 21 `site/tests/*.spec.ts` + 3
Playwright `*.e2e.ts`), +2 from the 2026-08-16 pass: `task-store/src/pr-finding.integration.test.ts`,
backing the new `PrFinding` model (PFL-1.1/PFL-2.1) — already-correct, already-passing
coverage landed in the same commits as the production change (see `test-migration.md`'s
Method note steps 1-4) — and `task-store/src/state-filter.smoke.test.ts`, backing the
new `?hitl` filter, whose count was previously omitted from the smoke bucket.

**`## Features` section in `test-inventory.md` unchanged this cycle:** 23 features,
`feature_coverage_pct` still 100% (23/23) — no new feature introduced (`PrFinding` folds
into the existing "Task store — task/PR lifecycle" feature). This is the source for the
Coverage Gate block above.

### Local-runnable status

**100%** of the 283 `bun test`-scanned files run locally with zero external network.
Phase 3's isolation-contract grep sweep (`mock.module(`, `global.fetch =`,
`global.console =`, stray `DATABASE_URL` inside `*.unit.test.ts`) found zero real
violations this cycle (41 `mock.module(` matches, all in comments, unchanged; 3
pre-existing non-violation `DATABASE_URL` string-literal matches, unchanged). No test is
blocked on an external service. The new `pr-finding.integration.test.ts` requires
`DATABASE_URL_SHIPWRIGHT_TASK_STORE_TEST` and skips gracefully when absent
(`describeOrSkip`), consistent with every other integration test in this suite —
"local-runnable" in the sense the whole suite already uses (real Postgres available
locally or in CI, gracefully skipped otherwise).

### Canary status

**Not applicable.** Root `CLAUDE.md` declares `## Deploy model: direct` — no
staging/production GitHub Environments, no deploy→canary→promote pipeline. Unchanged
finding across every cycle to date. See Milestone 3.

### Speed status (Tier 1 aggregate vs. budget; Tier 2 not triggered)

**Tier 1 — aggregate (measured this pass, via live CI history):**

| Aggregate wall-clock | Budget | Ratio |
|---|---|---|
| 2m11s ([run 32103420774](https://github.com/app-vitals/shipwright/actions/runs/32103420774), 2026-08-18T05:34:39Z, commit `f901803d` — this cycle's exact HEAD) | <15 min (Full PR pipeline) | ~15% of budget |

Fully green run — no failing jobs. `bun test` step itself: 34.59s. 6,920 tests pass, 0
fail, across 283 files. Coverage held at 90.95% lines / 89.68% functions (threshold
90%/89%, line threshold raised by CLG-1.1). Consistent with every prior cycle's ~13-18% readings — not trending
toward the escalation trigger. Test-count growth trajectory: 5,762 (2026-07-28) →
6,041-6,042 (2026-08-01) → 6,070 (2026-08-05) → 6,071 (2026-08-06) → 6,177 (2026-08-09) →
6,288 (2026-08-11) → 6,501 (2026-08-14) → 6,864 (2026-08-16) → 6,920 (2026-08-18, this
pass) — steady, expected growth (+56 tests over the 1-day gap since 2026-08-16).

**Tier 2 triggered:** no — the escalation formula (aggregate wall-clock >7.5 min,
sustained across 2 consecutive measurements) is nowhere near tripped (~15% of budget,
well below the 50% escalation threshold). No per-layer p95 table is populated below, per
the SKILL's explicit guidance against fabricating per-layer numbers when only the Tier 1
aggregate is available.

### "Rebuild" / "Delete (redundant)" debt

| Bucket | Count | Effort |
|---|---|---|
| Rebuild | 0 | — |
| Promote / deepen | 0 | — |
| Delete (redundant) | 0 | — |
| Net-new | 1 | trivial, optional |

Rebuild, Promote/deepen, and Delete all zero this cycle — the empty-Promote/deepen steady
state (first reached 2026-08-16) holds for a second consecutive cycle. No higher-layer
test re-asserts a business rule already proved at a lower layer; no test's
layer/framework/fundamentals were found wrong. Zero false-confidence coverage
identified. Net-new remains at 1 — only the long-standing, explicitly-optional
`admin/src/clock.ts` item.

## 2. Where we want to be

- **Framework matrix** — already fully realized: `bun test` (unit/integration/smoke/content),
  Playwright (E2E, 3 surfaces), plain `bash` (the shell layer). No framework changes
  needed.
- **Local-substitute map** — every dependency named in the inventory has a working local
  substitute (recorded-fixture doubles for internal/third-party HTTP, real Postgres via
  CI service container for the three Prisma-backed services, a real locally-launched
  Chromium binary for `agent/src/browser.ts`). No blockers.
- **Coverage targets** — single 90/89 line/function CI floor (`scripts/check-coverage.ts`,
  raised from 80/80 by MTC-1.7, then to 90% lines by CLG-1.1), already enforced.
  Tiered targets (critical ~95%, high ~80%, medium spot-check) direct prioritization, not a
  second CI gate. **New this cycle: a dual feature+line `coverage_gate` block**
  (`scripts/check-coverage-gate.ts`) combines Phase 1's new `feature_coverage_pct` with
  the line-coverage floor into a single self-consistency-checked verdict — see Coverage Gate
  section above.
- **Canary suite definition** — not applicable under the current `direct` deploy model;
  deferred until a staged-deploy model exists.
- **Speed budgets per layer** — Unit <50ms p95 / Integration <2s / Smoke <5s / E2E <30s /
  full PR pipeline <15min. Tier 1 aggregate confirms comfortable headroom (~16% of
  budget); no per-layer breakdown needed since Tier 2 has never triggered.
- **Zero `rebuild` / `delete (redundant)` buckets** — already true today. **Zero
  `promote/deepen` bucket, new this cycle** — the first cycle since this document's
  inception with this bucket empty.
- **Every critical- and high-tier inventory item covered at its prescribed layer,
  including a genuine concurrent-caller regression test wherever a claim/lock race was
  closed, and combination coverage across interacting exclusion checks in
  candidate-selection logic** — the claim-race standard was already fully met as of last
  cycle; **the combination-coverage standard for `check-review.ts`'s exclusion checks is
  now also fully met (T-082 resolved this cycle)**; the HITL bootstrap sequencing
  standard is likewise now fully met (T-080 resolved this cycle). No open depth gaps
  remain in this category.

## 3. The gap

- **No missing layers.** All six layers (five `bun test`-declared plus the unofficial
  shell layer) in active use with the correct framework per layer, plus one
  deliberately-unformalized compile-time-only pattern (`blocked-by-type-parity.check.ts`)
  that has no coverage gap despite not fitting the existing taxonomy.
- **Wrong-layer tests:** 0.
- **External-only deps with no local substitute:** none.
- **Zero depth gaps this cycle.** The one Promote/deepen item carried from the prior cycle
  (`agent/src/check-review.unit.test.ts`'s combinatorial exclusion-check coverage)
  resolved via T-082 (PR #2641) — 3 new test cases stack 3+ of the fixed exclusion
  conditions in single fixtures, closing the gap Phase 2's original gap #18 flagged.
- **One real gap, resolved this cycle (was carried forward since 2026-07-30, T-080):**
  journey #5 (local HITL dev-loop bootstrap) previously had only its pure-planning half
  (`computeMissingClones`, `computeProvisionPlan`) unit-tested. T-080 (PR #2644) split
  `scripts/hitl.ts` into pure planners + a thin injected executor, now covered by
  `hitl.bootstrap.unit.test.ts` (unit) + `hitl.integration.test.ts` (integration) — a
  deliberate unit-layer classification for the planner half, recorded in
  `.claude/shipwright/test-readiness-decisions.md`, not the originally-prescribed pure
  integration layer. See Milestone 4 for the full rationale.
- **New this cycle (2026-08-18): the `PrFinding` ledger feature landed with matching
  coverage at build time.** `pull-request-service.ts`'s `appendFinding()` and the new
  `POST /prs/:id/findings` route both shipped with unit + integration + smoke coverage
  in the same commits (PFL-1.1/PFL-2.1) — no gap, no task needed. The `?hitl` task-list
  filter (HTF-1.1) and the ephemeral-storage/cache-pinning fix (`agent-manifest.ts` +
  `setup.ts`) likewise landed with matching test extensions — no gap, no task needed.
- **2026-08-16 cycle's finding (preserved for continuity): the dual coverage-gate tooling
  landed with matching coverage at build time.** `check-coverage-gate.ts`,
  `check-coverage-no-decrease.ts`, `coverage-tool-dispatch.ts`, and `check-coverage.ts`'s
  `CoverageParser` refactor all shipped with test coverage in the same commits — no gap,
  no task needed.
- **1 carried-forward, low-priority, non-gap item:** `admin/src/clock.ts` has no
  dedicated test file — consistent with the established convention shared by all sibling
  `Clock` implementations across the repo (near-zero logic, proven via consumers). Not
  scored as a gap; no task minted.
- **Infra gap: none net-new** beyond the auto-emitted stage-3 coverage-check promotion
  task (see Coverage Gate section and Milestone 1). Runner config, CI shape, Postgres
  containers, `Clock` helpers, recorded-fixture doubles, PR template all confirmed
  unchanged and correct.

### Speed delta

**Tier 1 — aggregate-vs-budget (leads; always populated):**

| Aggregate wall-clock | Budget | Ratio / time-to-close |
|---|---|---|
| 2m11s | <15 min (Full PR pipeline) | ~15% of budget — no time-to-close concern |

**Tier 2 — per-layer ratio (conditional on Tier 2 having triggered):**

**Tier 2 triggered:** no. Per-layer numbers are not fabricated here — Tier 1's aggregate
is the complete, sufficient measurement this cycle. Growth trajectory (282 → 283 files,
6,864 → 6,920 tests) is steady with no budget concern at any point measured.

## 4. Roadmap (five milestones)

### Milestone 1 — Infrastructure baseline
**DOD:** Runners, local substitutes, CI pipeline shape in place. No new tests yet — just the rails.

**No new task this cycle — T-083 (auto-emitted stage-3 coverage-check promotion,
Process step 9) already minted 2026-08-16, still `pending`.** Naming convention +
runner-exclusion config (`docs/test-readiness/naming.md`, `bunfig.toml`'s
`pathIgnorePatterns`) is established and re-verified unchanged this cycle. CI pipeline
shape matches `test-system.md`'s documented target exactly. Postgres service containers,
`Clock` injection, and recorded-fixture doubles are all confirmed in place. T-074
(speed-baseline Postgres refresh) remains `cancelled` — no successor task needed given
Tier 1's CI-sourced measurement is a complete, sufficient answer.

| Task | Files to touch | Layer | Bucket origin | Expected outcome | Verification command | Depends on |
|---|---|---|---|---|---|---|
| T-083 (carried forward, `status: pending`, not yet dispatched) | `.github/branch-protection` config (repo settings, not a source file) | infra | (auto-emitted, not migration-bucket-derived) | Promote the `coverage_gate` required status check from never-decrease (`RAT-1.3`, stage 2) to a hard ≥90% gate — `coverage_gate.line_coverage_pct` (90.95%) remains above the 90% threshold `repo-config/SKILL.md`'s 3-stage lifecycle gates stage-3 promotion on | Branch protection settings show the coverage check enforcing a hard ≥90% floor, not just never-decrease; confirm via `gh api repos/app-vitals/shipwright/branches/main/protection` | `RAT-1.3` |

Sizing check: single concern (one branch-protection config change), no source files
touched, well under the 1000-line cap. Not independently deployable in the code sense
(it's a repo-settings change, not a PR) — flagged as a repo-config execution step per
`repo-config/SKILL.md`'s existing convention for branch-protection tasks (see Open risks
for the human-execution note).

`recorded_fixture_automation` is undeclared in root `CLAUDE.md` (re-verified this cycle —
no `## Recorded-fixture automation` section present, defaults to `planned` per this
cycle's read mechanism) — no repo-config declaration opts this repo out of the
recorded-fixture-refresh-loop pattern. That loop remains correctly **deferred, not
scheduled** (`test-system.md`'s Repo configuration section) — sequenced after the manual,
hand-authored-fixture workflow matures, not alongside it. No further M1 task minted for
it this cycle.

### Milestone 2 — Critical-path coverage
**DOD:** 100% of `critical` tier inventory items have passing tests at the prescribed layer.

**Already satisfied — zero tasks.** T-082, the sole item that occupied this milestone
last cycle (`agent/src/check-review.ts`'s combinatorial exclusion-check depth gap), is now
resolved (PR #2641) — every critical-tier item has matching coverage landed in the same
commits as its production change.

### Milestone 3 — Canary suite live
**DOD:** canary not applicable -- deploy_model=direct

Zero tasks, same as every prior cycle. See `test-system.md`'s Canary execution contract
section for what would need to change before this applies.

### Milestone 4 — High-tier coverage
**DOD:** 100% of `high` tier inventory items covered.

**Already satisfied — zero real-gap tasks.** T-080, the sole item that occupied this
milestone since 2026-07-30 (journey #5, local HITL dev-loop bootstrap's full clone → seed
→ boot → poll sequence), is now resolved (PR #2644) — the pure-planning half
(`computeMissingClones`, `computeProvisionPlan`, `buildHitlConfig`, `buildPreflightSteps`,
`buildServiceSpecs`) is unit-tested via `hitl.bootstrap.unit.test.ts`, and the composed
sequencing/failure-propagation behavior is integration-tested via
`hitl.integration.test.ts`. This closes the repo's longest-standing open test-readiness
item — carried forward across every cycle since 2026-07-30.

`admin/src/clock.ts` remains the sole other Net-new item from Phase 3 — carried forward,
unchanged, explicitly a non-gap (established `Clock`-file convention, near-zero logic,
proven via consumers). No task minted for it, same as every prior cycle.

### Milestone 5 — Cleanup
**DOD:** Zero tests in `rebuild` or `delete (redundant)` buckets. CI passes.

**Already satisfied — zero tasks.** Phase 3 found zero `rebuild` and zero `delete
(redundant)` items this cycle, the same steady state as every prior pass. No new
generalizable learning surfaced this cycle warranting a `# Harness TODO`.

## 5. Task list

Flat, ordered, agent-executable. **Zero genuinely new tasks this cycle** — T-083 (the
auto-emitted stage-3 coverage-check promotion task) was already minted 2026-08-16 and
remains `pending`, not re-minted. No Phase 3 migration-bucket item produced a task this
cycle (zero Promote/deepen, zero rebuild, one trivial/optional Net-new not worth a task).

| ID | Milestone | Files to touch | Layer | Bucket origin | Expected outcome | Verification command | Depends on |
|---|---|---|---|---|---|---|---|
| T-083 (carried forward, `pending`) | 1 | `.github/branch-protection` config (repo settings) | infra | (auto-emitted stage-3 coverage promotion) | Promote coverage required check from never-decrease to hard ≥90% gate | `gh api repos/app-vitals/shipwright/branches/main/protection` | `RAT-1.3` |

T-083 is a repo-settings change, not a source-code PR — sizing rules (line cap, single
concern) don't apply in the usual sense, but it is a single, independently-actionable
concern with one real predecessor dependency (`RAT-1.3`, the stage-2 never-decrease check
that must exist before it can be promoted — already merged via PR #2657). **Status as of
this cycle: still `pending`, not yet dispatched** — see Open risks below.

**Resolved (re-verified via direct task-store query this cycle, not carried forward from
the prior doc):** T-073 through T-075, T-080, T-081, T-082 all confirmed at their
terminal/near-terminal status from the 2026-08-16 doc, unchanged (`done`/`cancelled`/
`deployed`/`deploying` as applicable — see the Status table above for exact values).
T-076 (`agent-type-manifest-loader` real-disk split — **deploying**), T-077
(`check-review.ts` fail-open catch-path test — **deploying**), T-078 (injectable ref +
never-synced equivalence test — **deploying**), T-079 (`generate-agent-type-schema.ts`
coverage class-level decision — **deploying**) remain parked, unchanged since 2026-08-06,
all four confirmed merged 2026-07-25 through 2026-07-28 with `deployedAt: null` still
unset — **8 consecutive cycles with zero status movement**, see Open risks below for the
escalation.

## 6. Open risks

- **T-076 through T-079 have now shown zero status movement for 8 consecutive
  test-readiness cycles (parked at `deploying`).** All four merged 2026-07-25 through
  2026-07-28 (3-3.5 weeks ago) with `deployedAt: null` still unset, and none carries a
  `note` field explaining a specific transient failure to reconcile against (unlike T-081,
  which did, and turned out to be a stale-record case rather than a genuine block). This
  cycle re-confirmed the four merge commits (`15663f3c`, `778b3ba1`, `45435444`,
  `9a1bee28`) remain ancestors of current `origin/main` — the code is unambiguously
  present in the current tree, and this repo's `deploy_model: direct` means every merge to
  main ships via the next Helm chart/image build with no staging gate to get stuck behind.
  This docs-refresh pipeline still has no first-party way to confirm a specific build
  actually rolled out, so this cycle again did **not** force-patch these four to `done`.
  **Escalating again — now an 8-cycle-old (3.5+ week) stale-status pattern**: recommend a
  human (or the `deploy`/`patch` pipeline's own diagnostics) directly check GitHub Actions
  deploy-workflow runs following each of the four merge commits (PRs #2207, #2227, #2228,
  #2264) to confirm an actual image build/rollout completed — if so, the fix is the same
  stale-record PATCH T-081 got; if not, this is a real, month-old deploy-pipeline stall
  that predates and is unrelated to any recent cycle's work.
- **T-083 (minted 2026-08-16) has now been `pending` for 2 consecutive cycles without
  dispatch.** Not itself concerning yet (2 cycles / ~2 days is not the 8-cycle pattern
  above), but flagging early: if T-083 is still `pending` after several more cycles,
  investigate whether `/shipwright:dev-task` is correctly picking up this task — recall
  it's a repo-settings change, not a normal source-code PR (see the note below), which may
  mean it needs manual/human execution rather than autonomous dispatch.
- **The compile-time-only "type-parity check" pattern (`blocked-by-type-parity.check.ts`)
  needs a taxonomy decision, new this cycle.** Not a coverage gap — confirmed wired into
  CI and passing — but it doesn't fit any of the five CLAUDE.md-declared suffixes or the
  unofficial sixth "shell" layer. `test-system.md` deliberately declines to formalize a
  seventh layer on a single instance; revisit if a second such file appears.
- **The bash "shell" layer still needs a formal documentation decision.** Carried forward
  unchanged across several cycles now (first flagged 2026-07-31, re-verified present and
  unchanged this cycle): `.github/workflows/lib/chart-tag-utils.sh` / `chart-drift-check.sh`
  and their `test-*.sh` siblings are genuinely tested code units that fall entirely
  outside the `bun test` runner and the five-suffix convention declared in root
  `CLAUDE.md`. Coverage itself is not in question — the open question is purely whether a
  human wants to formally document this as a sixth layer in `CLAUDE.md`'s `## Test
  conventions` table and `docs/test-readiness/naming.md`, or confirm it should stay an
  unofficial exception. Not urgent; not blocking any task above.
- **Recorded-fixture coverage breadth** (~9 remaining `Http*Client` modules that could
  extend the `Recorded*Client` pattern) remains a Phase 2 design-level backlog item, not
  a migration-bucket verdict on any existing file. No task emitted this cycle since
  Phase 3 found no test currently missing coverage because of it.
- **`agent/src/piper-voice.ts` reclassification watch item** (unit today, integration
  once Piper synthesis lands) — still not landed as of this cycle; flagged so a future
  Phase 3 doesn't silently miss the transition.
- **Recorded-fixture maintenance loop remains deferred** (`repo-config/SKILL.md` §5) —
  correctly not scheduled this cycle; sequence after fixture-authoring work matures. Per
  the SKILL's guidance, this is deliberately not wired into T-083 or any other task this
  cycle — T-083 is a coverage-check promotion, not a credential-provisioning automation
  task, so it doesn't trip the "never automate a never-proven capability" failure mode
  this note exists to block.
- **T-083 (new this cycle) is a repo-settings change, not a code PR — execution path
  needs a human or the `repo-config` skill's own mechanism, not `shipwright /dev-task`.**
  Promoting a branch-protection required-check from never-decrease to hard-≥90% is a
  GitHub repo-settings mutation (`gh api .../branches/main/protection` or the UI), not a
  source-code change `dev-task` can open a PR for. Flagging so whoever picks this up
  routes it correctly rather than assuming it's a normal code task.

---

## Handoff

This document is intended for execution by:
- An engineer working through the task list manually, OR
- `shipwright /dev-task` consuming the task list as a queue.
