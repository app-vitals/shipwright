# Test Readiness Plan

> Phase 4 synthesis. The executable roadmap from where we are now to a test suite trustworthy enough for autonomous programming.
>
> Generated by `/test-roadmap` on 2026-08-09 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-20260809`), consuming `test-inventory.md` (refreshed at
> `a9f2a534`), `test-system.md` (refreshed at `a9f2a534`), and `test-migration.md`
> (refreshed at `a9f2a534`).

## Status of the prior cycle's plan (2026-08-06) and everything since

The 2026-08-06 plan (this file, before this overwrite) carried forward exactly one open
item into its own task list — T-080 (journey #5's HITL dev-loop bootstrap integration
test), minted 2026-07-30 — and minted no new tasks, since that cycle's Phase 3 top-up
found zero new gaps in its 3-commit / 13-file delta.

Queried directly against task-store (not assumed from the old doc; direct `GET
/tasks/{id}` lookups against each of the 8 known IDs), current status of every relevant
ID as of this cycle:

| Task | Status | Title |
|---|---|---|
| `test-t-073-shipwright` | **done** | `site/src/lib/html-escape.ts` coverage scoping decision |
| `test-t-074-shipwright` | **cancelled** | Refresh the speed baseline against a real Postgres-enabled environment |
| `test-t-075-shipwright` | **deployed** | Naming convention + runner-exclusion re-verification |
| `test-t-076-shipwright` | **deploying** | Split `agent-type-manifest-loader.unit.test.ts`'s real-disk cases into a new `.integration.test.ts` file |
| `test-t-077-shipwright` | **deploying** | `check-review.ts`'s `isBundleComplete` fail-open catch-path test |
| `test-t-078-shipwright` | **deploying** | Make `isAuthorAllowed`'s ref dependency injectable + never-synced equivalence test |
| `test-t-079-shipwright` | **deploying** | `scripts/generate-agent-type-schema.ts` test-coverage class-level decision |
| `test-t-080-shipwright` | **pending** | HITL dev-loop bootstrap's full clone→seed→boot→poll sequence, injected `gh` exec fake |

**T-073 through T-079 are unchanged since the 2026-08-06 pass** — no status movement in
the ~3 days since (T-076/T-077/T-078/T-079 remain parked at `deploying`, same as last
cycle; not re-investigated here since Phase 4 doesn't own deploy-pipeline state — this is
now the 4th consecutive cycle these four IDs have shown no movement, worth a human check
on why the deploy pipeline hasn't advanced them, though that's outside this pipeline's
own remit). **T-080 is still `pending`**, unchanged since it was queued 2026-07-30; it
carries forward into this cycle's task list below, not as a new task (already minted)
but as the one still-open item.

**Next available ID for this cycle's genuinely new tasks would still be T-081** (highest
existing `test-t-NNN-shipwright` record is `test-t-080-shipwright`, confirmed via direct
task-store lookup). **No new tasks are minted this cycle** — Phase 3 (`test-migration.md`,
this cycle's 2026-08-09 top-up over a 50-commit / 97-file delta since the `71b6024e`
baseline, 13 non-doc/non-version-bump/non-chart source files) found one gap (#15, the
HSR-1.1 data-migration coverage) and confirmed it already resolved by a dedicated new
integration test file landed in the same commit series as the production change — same
pattern as every prior cycle. Nothing new for Phase 5 to queue at T-081 or beyond.

## 1. Where we are now

### Layer coverage map

| Layer | Existing tests | Inventory items | Coverage gap |
|---|---|---|---|
| Unit | 123 `.unit.test.ts` | Dominant category-1 surface (`lib/`, decision/classification logic, mutable-box refs, crypto helpers, parsing/validation) | None — every inventory item mapped to unit has a matching test file |
| Integration | 61 `.integration.test.ts` | Every category-2/5 service-boundary and external-integration item | None — including this cycle's HSR-1.1/RCG-1.2/STD-1.x fixes and the new dedicated `hitl-blocked-migration.integration.test.ts`, all landed with tests in the same commits as the changes |
| Smoke | 43 `.smoke.test.ts` | Every category-3 HTTP route across 5 services | None |
| E2E | 21 `site/tests/*.spec.ts` + 3 `{admin,metrics}/e2e/*.e2e.ts` | 4 of 5 identified journeys are browser-driven or partially so (journey #5, HITL bootstrap, is integration-composed, not E2E) | None at E2E; journey #5's integration-harness half is the one real gap (see below) |
| Content | 36 `.content.test.ts` | `plugins/shipwright/{commands,agents,skills,references}/*` | None — +1 this cycle (`plan-session.content.test.ts`, extending coverage of the existing `plan-session.md`, not a new command/skill file) |
| Shell (unofficial 6th layer) | 2 `test-*.sh` bash assertion suites | `.github/workflows/lib/chart-tag-utils.sh`, `chart-drift-check.sh` | None — coverage exists; only a documentation-completeness question is open (see Open risks) |

**Total: 287 test files** (263 `bun test`-scanned + 21 `site/tests/*.spec.ts` + 3
Playwright `*.e2e.ts`), +3 from the 2026-08-06 pass. Growth this cycle is 3 new files
(`scripts/minikube.unit.test.ts`, `task-store/src/hitl-blocked-migration.integration.test.ts`,
`plugins/shipwright/commands/plan-session.content.test.ts`) backing this cycle's real
production changes (HSR-1.1 completion, `scripts/minikube.ts`), plus incremental new test
*cases* within already-existing files (`blocked-by.unit.test.ts`, `ready.unit.test.ts`,
`loop-orchestrator.unit.test.ts`, and the `check-*.unit.test.ts` family's `hitl`→`blocked`
fixture updates) for RCG-1.2/STD-1.x and the field-rename plumbing.

### Local-runnable status

**100%** of the 263 `bun test`-scanned files run locally with zero external network.
Phase 3's isolation-contract grep sweep (`mock.module(`, `global.fetch =`,
`global.console =`, stray `DATABASE_URL` inside `*.unit.test.ts`) found zero real
violations — every match is either a compliance-documenting comment or a guarded
env-var-name assertion. No test is blocked on an external service.

### Canary status

**Not applicable.** Root `CLAUDE.md` declares `## Deploy model: direct` — no
staging/production GitHub Environments, no deploy→canary→promote pipeline. Unchanged
finding across every cycle to date. See Milestone 3.

### Speed status (Tier 1 aggregate vs. budget; Tier 2 not triggered)

**Tier 1 — aggregate (measured this pass, via live CI history):**

| Aggregate wall-clock | Budget | Ratio |
|---|---|---|
| 2m25s ([run 31294850463](https://github.com/app-vitals/shipwright/actions/runs/31294850463), 2026-08-09, commit `a9f2a534` — this cycle's exact HEAD) | <15 min (Full PR pipeline) | ~16% of budget |

This run's `lint / typecheck / test` job passed cleanly: 6,177 pass / 0 fail across 264
files in 40.47s. Fully green run — no failing jobs this time. Consistent with every
prior cycle's ~13-17% readings — not trending toward the escalation trigger. Test-count
growth trajectory: 5,762 (2026-07-28 `speed-baseline.md`) → 5,880 (2026-07-30) →
6,041-6,042 (2026-08-01) → 6,070 (2026-08-05) → 6,071 (2026-08-06) → 6,177 (2026-08-09,
this pass) — steady, expected growth (+106 tests), consistent with this cycle's 3-new-file
delta plus incremental cases for HSR-1.1/RCG-1.2/STD-1.x.

**Tier 2 triggered:** no — the escalation formula (aggregate wall-clock >7.5 min,
sustained across 2 consecutive measurements) is nowhere near tripped (~16% of budget,
well below the 50% escalation threshold). Tier 2 not triggered — suite is within the
Tier 1 aggregate budget; no per-layer p95 table is populated below, per the SKILL's
explicit guidance against fabricating per-layer numbers when only the Tier 1 aggregate
is available.

This remains closed out from the prior 6-cycle carry-forward (2026-07-19 through
2026-07-26) that had escalated local Postgres-blocked remeasurement to task-store as
T-074 — that blocker only ever applied to Tier 2's per-layer breakdown, which was never
triggered. Tier 1's aggregate pull needs only `gh` CLI access to CI history, which this
sandbox has always had. T-074 remains `cancelled` (see Status of the prior cycle's plan
above); no new M1 task is needed for this.

### "Rebuild" / "Delete (redundant)" debt

| Bucket | Count | Effort |
|---|---|---|
| Rebuild | 0 | — |
| Delete (redundant) | 0 | — |
| Net-new | 2 | trivial-to-small |

Both zero, same as every prior pass. No higher-layer test re-asserts a business rule
already proved at a lower layer; no test's layer/framework/fundamentals were found wrong.
Zero false-confidence coverage identified.

## 2. Where we want to be

- **Framework matrix** — already fully realized: `bun test` (unit/integration/smoke/content),
  Playwright (E2E, 3 surfaces), plain `bash` (the new shell layer). No framework changes
  needed.
- **Local-substitute map** — every dependency named in the inventory has a working local
  substitute (recorded-fixture doubles for internal/third-party HTTP, real Postgres via
  CI service container for the three Prisma-backed services, a real locally-launched
  Chromium binary for the new `agent/src/browser.ts`). No blockers.
- **Coverage targets** — single 80/80 line/function CI floor (`scripts/check-coverage.ts`),
  already enforced; tiered targets (critical ~95%, high ~80%, medium spot-check) direct
  prioritization, not a second CI gate.
- **Canary suite definition** — not applicable under the current `direct` deploy model;
  deferred until a staged-deploy model exists (see `test-system.md`'s Canary execution
  contract section for what would need to change).
- **Speed budgets per layer** — Unit <50ms p95 / Integration <2s / Smoke <5s / E2E <30s /
  full PR pipeline <15min. Tier 1 aggregate confirms comfortable headroom (~17% of
  budget); no per-layer breakdown needed since Tier 2 has never triggered.
- **Zero `rebuild` / `delete (redundant)` buckets** — already true today.
- **Every critical- and high-tier inventory item covered at its prescribed layer,
  including newly-introduced guard-condition branches** — this cycle's one production
  fix (RFR-1.1 fresh-author-reply-exception hoist) meets this standard already; the
  standard the one remaining net-new item (journey #5's integration harness) falls short
  of today.

## 3. The gap

- **No missing layers.** All six layers (five `bun test`-declared plus the unofficial
  shell layer) in active use with the correct framework per layer.
- **Wrong-layer tests:** 0.
- **External-only deps with no local substitute:** none.
- **Net-new coverage gap, carried forward unchanged from 2026-07-30 (already tracked as
  T-080, still `pending`):** 1 real gap — journey #5 (local HITL dev-loop bootstrap) has
  its pure-planning half (`computeMissingClones`, `computeProvisionPlan`) fully
  unit-tested, but the full clone → seed → boot → poll sequence has no integration-level
  test exercising it end-to-end with an injected `gh` exec fake. Narrower than Phase 2's
  original framing precisely because the hardest planning logic is already isolated and
  proven. This cycle's 50-commit / 97-file delta (2026-08-09 top-up) introduced no new
  gap here — `scripts/hitl.ts` has had zero scope growth since 2026-08-06.
- **1 real gap flagged and confirmed already resolved this cycle:** HSR-1.1's
  `20260731000000_split_hitl_blocked_signals` data migration (the raw-SQL `UPDATE`
  statements converting pre-migration `hitl`/`hitlNotifiedAt` rows into the new
  `requiresHumanApproval`/`status='blocked'`/`PullRequest.blocked` shape) needed
  dedicated pre→post migration-transition coverage distinct from the post-migration
  unit-layer coverage of the new field shapes. Resolved by a new
  `task-store/src/hitl-blocked-migration.integration.test.ts` (DB-gated, real Postgres),
  landed in the same commit series as the schema change. No task needed.
- **This cycle's other real source changes, all confirmed already resolved:** the
  HSR-1.1 hitl/blocked field-rename plumbing across `agent/src/check-deploy.ts`,
  `check-patch.ts`, `check-review.ts`, `check-helpers.ts`, `admin/src/admin-ui-pages.ts`;
  `task-store/src/ready.ts`'s same-branch exclusivity guard (RCG-1.2); `agent/src/loop-orchestrator.ts`'s
  skip-reason-taxonomy generalization (STD-1.1/1.2/1.4) and clean-409-claim-loss
  re-selection exclusion (RCG-1.2); and new `scripts/minikube.ts` (dev-tooling, mirrors
  `dev-tmux.ts`'s already-designed pattern). Every touched row has matching test
  coverage landed in the same commits as the production changes — no task needed for
  any of them. (Prior cycles' carried-forward items — RFR-1.1 fresh-author-reply
  exception hoist, RVG-1.1 author-reply candidacy retrigger, BBE-1.2 skip-counter
  exemption, `ciFailureSignature` streak auto-block, ORF-1.x/2.x combined-filter
  integration coverage, RRR-1.1 requested-reviewer inclusion — remain resolved and
  unchanged.)
- **1 carried-forward, low-priority, non-gap item:** `admin/src/clock.ts` has no
  dedicated test file — consistent with the established convention shared by all 6
  sibling `Clock` implementations across the repo (near-zero logic, proven via
  consumers). Not scored as a gap.
- **Infra gap: none net-new.** Runner config, CI shape, Postgres containers, `Clock`
  helpers, recorded-fixture doubles, coverage gate, PR template all confirmed unchanged
  and correct.

### Speed delta

**Tier 1 — aggregate-vs-budget (leads; always populated):**

| Aggregate wall-clock | Budget | Ratio / time-to-close |
|---|---|---|
| 2m25s | <15 min (Full PR pipeline) | ~16% of budget — no time-to-close concern |

**Tier 2 — per-layer ratio (conditional on Tier 2 having triggered):**

**Tier 2 triggered:** no. Per-layer numbers are not fabricated here — Tier 1's aggregate
is the complete, sufficient measurement this cycle, corroborated by an independent
`speed-baseline.md` refresh from 2026-07-28 (commit `1406a4dc`, 258 files / 2m01s, 13% of
budget) not re-refreshed this pass — the growth trajectory (258 → 264 files,
5,762 → 6,177 tests) is steady with no budget concern at any point measured.

## 4. Roadmap (five milestones)

### Milestone 1 — Infrastructure baseline
**DOD:** Runners, local substitutes, CI pipeline shape in place. No new tests yet — just the rails.

**Already satisfied — zero tasks.** Naming convention + runner-exclusion config
(`docs/test-readiness/naming.md`, `bunfig.toml`'s `pathIgnorePatterns`) is established
and re-verified unchanged this cycle. CI pipeline shape matches `test-system.md`'s
documented target exactly. Postgres service containers, `Clock` injection, and
recorded-fixture doubles are all confirmed in place. T-074 (speed-baseline Postgres
refresh) remains `cancelled` — no successor task needed given Tier 1's CI-sourced
measurement is a complete, sufficient answer (see Speed status above).

`recorded_fixture_automation` is undeclared in root `CLAUDE.md` (defaults to `planned`
per this cycle's read mechanism) — no repo-config declaration opts this repo out of the
recorded-fixture-refresh-loop pattern. That loop remains correctly **deferred, not
scheduled** (`test-system.md`'s Repo configuration section) — sequenced after the
manual, hand-authored-fixture workflow matures, not alongside it. No M1 task minted for
it this cycle; nothing in Phase 3's findings changes that sequencing call.

### Milestone 2 — Critical-path coverage
**DOD:** 100% of `critical` tier inventory items have passing tests at the prescribed layer.

**Already satisfied — zero tasks.** Every `critical`-tier inventory item has coverage at
its prescribed layer, including this cycle's real production changes (HSR-1.1's
hitl/blocked split completion across `task-store/src/blocked-by.ts` and its
`check-*.ts`/`admin-ui-pages.ts` consumers, `ready.ts`'s same-branch exclusivity guard,
and `loop-orchestrator.ts`'s skip-reason-taxonomy generalization), which landed with
dedicated positive, negative, and regression test cases in the same commits as the
production changes (confirmed directly against test file contents in Phase 3), plus a
new dedicated data-migration integration test for the HSR-1.1 schema transition itself.

### Milestone 3 — Canary suite live
**DOD:** canary not applicable -- deploy_model=direct

Zero tasks, same as every prior cycle. See `test-system.md`'s Canary execution contract
section for what would need to change before this applies.

### Milestone 4 — High-tier coverage
**DOD:** 100% of `high` tier inventory items covered.

**One open task, carried forward from 2026-07-30 (T-080, still `pending`, no new task minted this cycle).** Journey #5 (local HITL dev-loop bootstrap, `high`-adjacent —
Phase 1 ranks the journey itself `medium`, but it gates the team's own primary local
dev-loop) crossed the journey threshold in a recent cycle when `scripts/hitl.ts` gained
an auto-clone preflight and `agent-policy.md` template seeding. The pure-planning half
(`computeMissingClones`, `computeProvisionPlan`) is fully unit-tested; the orchestration
sequence itself (clone → seed → boot → poll → dispatch) has no test exercising it
end-to-end.

| Task | Files to touch | Layer | Bucket origin | Expected outcome | Verification command |
|---|---|---|---|---|---|
| T-080 | `scripts/hitl.ts`, `scripts/hitl.integration.test.ts` | integration | net-new | An injected `gh`/process-exec seam (if not already present) wired into `scripts/hitl.ts`'s orchestration function, plus one sequencing test exercising clone→seed→boot→poll with the fake, proving the full sequence composes correctly (not just its individual pure-planning pieces) | `bun test scripts/hitl.integration.test.ts` |

`admin/src/clock.ts` remains the sole other Net-new item from Phase 3 — carried forward,
unchanged, explicitly a non-gap (established `Clock`-file convention, near-zero logic,
proven via consumers). No task minted for it, same as every prior cycle.

### Milestone 5 — Cleanup
**DOD:** Zero tests in `rebuild` or `delete (redundant)` buckets. CI passes.

**Already satisfied — zero tasks.** Phase 3 found zero `rebuild` and zero `delete
(redundant)` items this cycle, the same steady state as every prior pass. No new
generalizable learning surfaced this cycle warranting a `# Harness TODO`.

## 5. Task list

Flat, ordered, agent-executable. **No genuinely new task this cycle** — T-080 (minted
2026-07-30, still `pending`) carries forward unchanged as the only open item; Phase 3's
2026-08-09 top-up found one gap (#15, HSR-1.1 data-migration coverage) in the 50-commit /
97-file delta since the `71b6024e` baseline, already resolved by a landed test file, so
there is nothing new to number starting at `test-t-081-shipwright`.

| ID | Milestone | Files to touch | Layer | Bucket origin | Expected outcome | Verification command | Depends on |
|---|---|---|---|---|---|---|---|
| T-080 | 4 | `scripts/hitl.ts`, `scripts/hitl.integration.test.ts` | integration | net-new | HITL dev-loop bootstrap's full clone→seed→boot→poll sequence proven correct end-to-end via an injected `gh` exec fake | `bun test scripts/hitl.integration.test.ts` | — |

No task in this cycle exceeded the ~1000-line cap, touched more than one functional
concern, or represented an N-service repeated operation — T-080 is a single task against
a single file's orchestration seam, not a multi-service refactor.

**Resolved (unchanged since the 2026-08-06 pass, re-verified via direct task-store
query, not carried forward from the prior doc):** T-073
(`site/src/lib/html-escape.ts` coverage-scoping decision — **done**), T-074
(speed-baseline Postgres-enabled refresh — **cancelled**, superseded by Tier 1's
successful CI-sourced measurement), T-075 (naming-convention re-verification —
**deployed**), T-076 (`agent-type-manifest-loader` real-disk split — **deploying**),
T-077 (`check-review.ts` fail-open catch-path test — **deploying**), T-078 (injectable
ref + never-synced equivalence test — **deploying**), T-079
(`generate-agent-type-schema.ts` coverage class-level decision — **deploying**). No
status movement on any of these since the 2026-08-06 pass — T-076 through T-079 have now
shown zero movement for 4 consecutive cycles, worth a human check on the deploy pipeline
even though it's outside this pipeline's scope to fix.

## 6. Open risks

- **T-080's scope depends on whether `scripts/hitl.ts` already exposes an injectable
  exec seam.** Phase 3 confirms the pure-planning helpers (`computeMissingClones`,
  `computeProvisionPlan`) are already injectable-I/O by design, mirroring the repo's
  established convention — but whether the *orchestration function* that sequences
  clone→seed→boot→poll already accepts an injected `gh`/process-exec dependency, or
  needs that seam added first, was not directly verified this pass (Phase 3 stopped at
  confirming the planning-helper unit tests exist). If the seam doesn't exist yet, T-080
  implicitly bundles "add the injection point" with "add the sequencing test" — the same
  shape as the prior cycle's T-078, which was an accepted single-task bundle because the
  test couldn't be correctly authored without the design change. Flagging so whoever
  picks up T-080 checks this first rather than assuming either shape.
- **Content-layer file count grew by one this cycle, still not independently
  full-audited.** This cycle's content-layer file count is 36 `.content.test.ts` files
  (+1) — the new file (`plugins/shipwright/commands/plan-session.content.test.ts`) and
  the additive assertions on other existing files (`deploy.content.test.ts`,
  `dev-task.content.test.ts`, `patch.content.test.ts`, `review.content.test.ts`, all for
  their respective `.md` content changes this cycle) are all for **existing** command
  files, not new commands (still 29 `commands/*.md`). Phase 1 still batch-classifies
  `plugins/shipwright/{commands,agents,skills}/*` as content-covered by established
  convention rather than confirming each file individually — the convention has held in
  every spot-check to date, so this isn't scored as a gap, but a full count-and-diff
  still hasn't happened in several cycles — worth doing the next time Phase 1/3 has
  spare scope.
- **The bash "shell" layer still needs a formal documentation decision.** Carried forward
  unchanged across several cycles now (first flagged 2026-07-31, re-verified present and
  unchanged this cycle — zero `.github/workflows/` changes since the `71b6024e`
  baseline, confirmed via `git log 71b6024e..origin/main -- .github/workflows/`
  returning empty): `.github/workflows/lib/chart-tag-utils.sh` / `chart-drift-check.sh`
  and their `test-*.sh` siblings are genuinely tested code units that fall entirely
  outside the `bun test` runner and the five-suffix convention declared in root
  `CLAUDE.md`. Coverage itself is not in question (`test-check-chart-drift.sh` exists
  and passes) — the open question is purely whether a human wants to formally document
  this as a sixth layer in `CLAUDE.md`'s `## Test conventions` table and
  `docs/test-readiness/naming.md`, or confirm it should stay an unofficial exception.
  Not urgent; not blocking any task above.
- **Recorded-fixture coverage breadth** (~9 remaining `Http*Client` modules that could
  extend the `Recorded*Client` pattern) remains a Phase 2 design-level backlog item, not
  a migration-bucket verdict on any existing file. No task emitted this cycle since
  Phase 3 found no test currently missing coverage because of it.
- **`agent/src/piper-voice.ts` reclassification watch item** (unit today, integration
  once Piper synthesis lands) — still not landed as of this cycle; flagged so a future
  Phase 3 doesn't silently miss the transition.
- **Recorded-fixture maintenance loop remains deferred** (`repo-config/SKILL.md` §5) —
  correctly not scheduled this cycle; sequence after fixture-authoring work matures. Per
  the SKILL's guidance, this is deliberately not wired into T-080 or any other task this
  cycle — the HITL bootstrap gap is a sequencing/orchestration test, not a
  credential-provisioning automation task, so it does not trip the "never automate a
  never-proven capability" failure mode this note exists to block.

---

## Handoff

This document is intended for execution by:
- An engineer working through the task list manually, OR
- `shipwright /dev-task` consuming the task list as a queue.
