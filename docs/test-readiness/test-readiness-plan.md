# Test Readiness Plan

> Phase 4 synthesis. The executable roadmap from where we are now to a test suite trustworthy enough for autonomous programming.
>
> Generated by `/test-roadmap` on 2026-09-18 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-20260918`), consuming `test-inventory.md`, `test-system.md`,
> and `test-migration.md` — all three regenerated this cycle at commit `eff5b503`,
> reconciling a 246-non-routine-commit delta spanning a 16-day gap since the true prior
> baseline `fc600c2a` (the previous cycle's header cited `c1c81d4e`, a pre-squash tip that
> was never merged to `main` — see `test-inventory.md`'s Sampling section for the
> anchor-drift correction) — by far the largest window measured to date. Headline new
> surface: a Sessions feature (task grouping, follow/notifications, admin UI), an outbound
> task-store webhook dispatcher, PR-origin/census tracking, a completed author-allowlist
> rename/split cascade, a `plan-session --autonomous` cron phase backed by a new
> `TaskKind` enum, and a Prisma 7 upgrade across admin/task-store/chat. Phase 3 found
> every real source change already carries matching test coverage at the correct layer,
> landed in the same commits/PRs — so this synthesis again mints **zero new tasks**.

## Coverage Gate

The headline machine-checkable verdict — computed by `scripts/check-coverage-gate.ts`
(CGT-1.2), rendered verbatim below.

Inputs sourced per Process step 8: `linePct`/`lineSource` from this cycle's live CI pull
([run 35295193216](https://github.com/app-vitals/shipwright/actions/runs/35295193216)
@ `eff5b503`, this cycle's exact HEAD, fetched directly via `gh run list`/`gh run view --log`);
`featurePct`/`featureSource` from `test-inventory.md`'s `feature_coverage_pct` (regenerated
this cycle — 100%, 24/24 features, one new feature this cycle, Sessions — all other new
inventory rows fold into existing features). Computed via:
```bash
echo '{"featurePct": 100, "featureSource": "docs/test-readiness/test-inventory.md", "linePct": 92.47, "lineSource": "CI run https://github.com/app-vitals/shipwright/actions/runs/35295193216 @ eff5b503"}' \
  | bun run scripts/check-coverage-gate.ts
```
Actually run (not hand-computed):
```json
{"feature_coverage_pct":100,"feature_coverage_source":"docs/test-readiness/test-inventory.md","line_coverage_pct":92.47,"line_coverage_source":"CI run https://github.com/app-vitals/shipwright/actions/runs/35295193216 @ eff5b503","verdict":"READY","targets":{"feature":90,"line":90}}
```

| Field | Value |
|---|---|
| Feature coverage | 100% (target 90%) — source: docs/test-readiness/test-inventory.md |
| Line coverage | 92.47% (target 90%) — source: CI run https://github.com/app-vitals/shipwright/actions/runs/35295193216 @ eff5b503 |
| **Verdict** | **READY** |

**Stage-3 coverage-check promotion — `done`, unchanged this cycle.** `line_coverage_pct`
(92.47) remains above the 90% threshold `repo-config/SKILL.md`'s 3-stage coverage-check
lifecycle gates on. **T-083** ("Promote coverage required check from never-decrease to
hard ≥90% gate") remains **`done`** per direct task-store query this cycle
(`starting_offset` re-derivation below), its `note` field unchanged since the
2026-08-20 completion, still confirming the branch-protection swap is complete and no
further action is needed. No new stage-3 emission this cycle (Process step 9's dedup
scan found T-083 already filed and `done`).

## Status of the prior cycle's plan (2026-09-02) and everything since

Queried task-store directly (not assumed from the old doc; a fresh full-repo scan across
all three pages for the offset query below, then filtered to each of the 9 known IDs from
the prior cycle), current status of every relevant ID as of this cycle (2026-09-18):

| Task | Status | Title |
|---|---|---|
| `test-t-076-shipwright` | **deployed** (resolved this cycle) | Split `agent-type-manifest-loader.unit.test.ts`'s real-disk cases into a new `.integration.test.ts` file |
| `test-t-077-shipwright` | **deployed** (resolved this cycle) | `check-review.ts`'s `isBundleComplete` fail-open catch-path test |
| `test-t-078-shipwright` | **deployed** (resolved this cycle) | Make `isAuthorAllowed`'s ref dependency injectable + never-synced equivalence test |
| `test-t-079-shipwright` | **deployed** (resolved this cycle) | `scripts/generate-agent-type-schema.ts` test-coverage class-level decision |
| `test-t-080-shipwright` | **deployed** (unchanged) | HITL dev-loop bootstrap's full clone→seed→boot→poll sequence, injected `gh` exec fake |
| `test-t-081-shipwright` | **done** (unchanged) | `TaskService.claim()`'s concurrent-race close (real Postgres) |
| `test-t-082-shipwright` | **deployed** (resolved this cycle) | `traceReviewCandidacyDecision`'s exclusion checks proven correct in 3+-condition combination |
| `test-t-083-shipwright` | **done** (unchanged) | Promote coverage required check from never-decrease to hard ≥90% gate |
| `test-t-084-shipwright` | **deployed** (unchanged) | Add `afterEach` reset to `agentSlackMembershipRef` singleton test |

**The 15-cycle-old stuck-at-`deploying` escalation is resolved.** T-076, T-077, T-078,
T-079, and T-082 all flipped from `deploying` to `deployed` on **2026-09-08T18:34:25Z**
(same timestamp across all five — a single reconciliation event, not five independent
transitions), confirmed via direct per-task field inspection of this cycle's task-store
pull (`deployedAt` now populated for all five, previously `null`). This matches every
prior cycle's working theory: these were genuinely shipped, just stuck on a status-
tracking gap rather than a real deployment failure — no manual force-patch was ever
applied by this pipeline, and the eventual self-resolution (whatever reconciler or manual
action fixed it) confirms that theory rather than contradicting it. **Closing this Open
risk** — see the Open risks section below, which now records this as resolved rather
than carrying it forward as unresolved for a 16th cycle.

**Task-store query for the starting T-NNN offset (per SKILL Process step 4), run before
minting any new IDs:** queried `$SHIPWRIGHT_TASK_STORE_URL/tasks?repo=app-vitals%2Fshipwright&limit=500`
— `total: 1259`, requiring two additional pages (`&offset=500` and `&offset=1000`, 1259
total scanned across 3 pages, matching `.total`). Filtered to `test-t-<NNN>-shipwright`
IDs across all three pages: highest is still `test-t-084-shipwright` (confirmed present,
now `deployed`). **`starting_offset = 85`** — this cycle's new task, if any, would begin
at **T-085**. Per Phase 3's findings (zero Promote/deepen, zero rebuild, zero net-new
beyond the same carried-forward optional item), **no new task is minted this cycle** —
T-085 remains reserved but unused.

**Zero new tasks minted this cycle.** Phase 3 (`test-migration.md`, regenerated this
cycle against the `fc600c2a`→`eff5b503` delta — 246 non-routine commits spanning a
16-day gap, by far the largest window measured to date) found zero genuine Promote/deepen
items, zero rebuild items, zero delete/trim items — every genuinely new source row across
this cycle's clusters (Sessions, outbound webhook dispatcher, PR-origin/census, the
author-allowlist rename/split cascade, `plan-session --autonomous`'s `TaskKind` migration,
the Prisma 7 upgrade, and ~10 new pure `lib/`/`plugins/shipwright/scripts/` helpers)
landed with matching test coverage in the same commits/PRs. The one verification finding
worth naming — `task-store/src/pr-origin-derivation.ts` initially looked like it might
lack a dedicated test file — turned out to have comprehensive coverage colocated in a
sibling service's test file on direct inspection, a naming-convention nitpick, not a
coverage gap (see `test-migration.md`'s Method note step 2). One trivial/optional
carried-forward Net-new item (`admin/src/clock.ts`, explicitly not worth a task per every
prior cycle's same finding). The Coverage Gate's stage-3 promotion trigger (Process step
9) found T-083 already minted and still `done` — no duplicate emitted for it.

## 1. Where we are now

### Layer coverage map

| Layer | Existing tests | Inventory items | Coverage gap |
|---|---|---|---|
| Unit | 185 `.unit.test.ts` | Dominant category-1 surface (`lib/`, decision/classification logic, mutable-box refs, crypto helpers, parsing/validation, coverage-gate tooling, VAPID/push-payload builders, Sessions' `session-rollup.ts`, `pr-origin-derivation.ts` — colocated in `pull-request-service.unit.test.ts`) | None |
| Integration | 83 `.integration.test.ts` | Every category-2/5 service-boundary and external-integration item, incl. Sessions' `session-service.ts`/`session-retention-reaper.ts`, the Prisma 7 `prisma-client.ts` extraction (3 services), and the two novel dependency-runtime regression files (`slack-bolt-client.integration.test.ts`, `slack-socket-mode-ping.integration.test.ts`) | None |
| Smoke | 54 `.smoke.test.ts` | Every category-3 HTTP route across 5 services, incl. new `routes/sessions.ts` + admin Sessions UI wire-contract coverage | None |
| E2E | 26 `site/tests/*.spec.ts` + 7 `{admin,metrics}/e2e/*.e2e.ts` | 4 of 5 identified journeys are browser-driven or partially so (journey #5, HITL bootstrap, is integration-composed, not E2E); new `autonomy.spec.ts`/`vs-openhands.spec.ts` this cycle | None |
| Content | 46 `.content.test.ts` (46 total repo-wide incl. `.claude/`/`agent-types/`/`agent/workspace/`; 40 under `plugins/shipwright/`) | `plugins/shipwright/{commands,agents,skills,references,test}/*` plus root-level content tests; +2 new (`dependency-patch`, `dependency-risk-analysis`), −1 deleted (retired `triage-dependency-bot-pr` skill) | None — net +1 this cycle |
| Shell (unofficial 6th layer) | 2 `test-*.sh` bash assertion suites | `.github/workflows/lib/chart-tag-utils.sh`, `chart-drift-check.sh` | None — coverage exists; only a documentation-completeness question is open (see Open risks) |
| Compile-time-only (unformalized) | 1 `*.check.ts` (`blocked-by-type-parity.check.ts`) | `admin/src/blocked-by-type-parity.check.ts` | None — wired into CI's `typecheck` step, confirmed passing; only a taxonomy question open (see Open risks) |

**Total: 401 test files, up from the 2026-09-02 pass's 349** (net +52, spanning the
16-day/246-commit gap since the true prior baseline `fc600c2a` — ~22 new files backing
this cycle's new inventory rows (Sessions alone contributes 7), 2 new content-category
reference-doc tests, 2 new marketing-site specs, 1 new `.claude/` content test, minus 1
deleted (`triage-dependency-bot-pr/SKILL.content.test.ts`, retired alongside its skill) —
see `test-migration.md`'s Summary for the full accounting, ground-truth-verified via
`find` and matching CI's own "368 files" `bun test`-scan report exactly for the four
`bun test` suffixes).

**`## Features` section in `test-inventory.md`: 24 features this cycle, up from 23** —
one new feature, Sessions, `feature_coverage_pct` still 100% (24/24). This is the source
for the Coverage Gate block above.

### Local-runnable status

**100%** of the `bun test`-scanned files run locally with zero external network. This
cycle's isolation-contract grep sweep (`mock.module(`, `global.fetch =`, `global.console =`,
stray `DATABASE_URL` inside `*.unit.test.ts`) found zero real violations — 53
`mock.module(` hits, all re-verified line-by-line as comment-only; 0 global-override
assignments; 6 `DATABASE_URL` references in `*.unit.test.ts` files, all consistent with
the established injected-fake/error-path-testing pattern — see `test-migration.md`'s
Isolation contract audit. No test is blocked on an external service.

### Canary status

**Not applicable.** Root `CLAUDE.md` declares `## Deploy model: direct` — no
staging/production GitHub Environments, no deploy→canary→promote pipeline. Unchanged
finding across every cycle to date. See Milestone 3.

### Speed status (Tier 1 aggregate vs. budget; Tier 2 not triggered)

**Tier 1 — aggregate (measured this pass, via live CI history, fetched directly via
`gh run list`/`gh run view --log`):**

| Aggregate wall-clock | Budget | Ratio |
|---|---|---|
| 1m29s ([run 35295193216](https://github.com/app-vitals/shipwright/actions/runs/35295193216), 2026-09-18T01:24:34Z, commit `eff5b503` — this cycle's exact HEAD) | <15 min (Full PR pipeline) | ~10% of budget |

Fully green run — no failing jobs. `bun test` step itself: 23.38s. 9,219 tests (9,218
pass, 1 skip, 0 fail) across 368 files. Coverage held at 92.47% lines / 91.02% functions
(threshold 90%/89%). Faster than every prior cycle's ~13-18% readings despite the largest
test-count jump on record — not trending toward the escalation trigger; the speed
improvement is normal CI infra variance, not a signal worth investigating on its own.
Test-count growth trajectory: 5,762 (2026-07-28) → 6,041-6,042 (2026-08-01) → 6,070
(2026-08-05) → 6,071 (2026-08-06) → 6,177 (2026-08-09) → 6,288 (2026-08-11) → 6,501
(2026-08-14) → 6,864 (2026-08-16) → 6,920 (2026-08-18) → 7,190 (2026-08-22) → 7,211
(2026-08-23) → 7,228 (2026-08-25) → 7,326 (2026-08-28) → 7,444 (2026-08-30) → 7,870
(2026-09-02) → **9,219 (2026-09-18, this pass)** — +1,349 tests, by far the largest
single-cycle jump on record, tracking this cycle's unusually large 246-commit/16-day gap
(see `test-migration.md`'s Method note) — no anomaly, no padding.

**Tier 2 triggered:** no — the escalation formula (aggregate wall-clock >7.5 min,
sustained across 2 consecutive measurements) is nowhere near tripped (~10% of budget,
well below the 50% escalation threshold). No per-layer p95 table is populated below, per
the SKILL's explicit guidance against fabricating per-layer numbers when only the Tier 1
aggregate is available.

### "Rebuild" / "Delete (redundant)" debt

| Bucket | Count | Effort |
|---|---|---|
| Rebuild | 0 | — |
| Promote / deepen | 0 | — |
| Delete (redundant) | 0 | — |
| Net-new | 1 | trivial, optional |

Rebuild, Promote/deepen, and Delete all remain zero this cycle — a seventh consecutive
clean cycle. No higher-layer test re-asserts a business rule already proved at a lower
layer; no test's layer/framework/fundamentals were found wrong. Net-new remains at 1 —
only the long-standing, explicitly-optional `admin/src/clock.ts` item.
`scripts/build-pwa-icons.ts` (new this cycle) was correctly bucketed Reuse via an
accepted indirect-coverage pattern, not Net-new — see `test-migration.md`'s Method note.

## 2. Where we want to be

- **Framework matrix** — already fully realized: `bun test` (unit/integration/smoke/content),
  Playwright (E2E, 3 surfaces), plain `bash` (the shell layer). No framework changes
  needed.
- **Local-substitute map** — every dependency named in the inventory has a working local
  substitute (recorded-fixture doubles for internal/third-party HTTP, real Postgres via
  CI service container for the three Prisma-backed services, a real locally-launched
  Chromium binary for `agent/src/browser.ts`). No blockers.
- **Coverage targets** — single 90/89 line/function CI floor (`scripts/check-coverage.ts`,
  raised from 80/80 by MTC-1.7, to 89% lines by MTC-1.7's follow-up, then to 90% lines by
  T-083/CLG-1.1 this cycle), code-side change already enforced (branch-protection swap
  status: see Coverage Gate section and Open risks). Tiered targets (critical ~95%, high
  ~80%, medium spot-check) direct prioritization, not a second CI gate.
- **Canary suite definition** — not applicable under the current `direct` deploy model;
  deferred until a staged-deploy model exists.
- **Speed budgets per layer** — Unit <50ms p95 / Integration <2s / Smoke <5s / E2E <30s /
  full PR pipeline <15min. Tier 1 aggregate confirms comfortable headroom (~18% of
  budget); no per-layer breakdown needed since Tier 2 has never triggered.
- **Zero `rebuild` / `delete (redundant)` / `promote-deepen` buckets** — still true today,
  a third consecutive clean cycle.
- **Every critical- and high-tier inventory item covered at its prescribed layer,
  including a genuine concurrent-caller regression test wherever a claim/lock race was
  closed, and combination coverage across interacting exclusion checks in
  candidate-selection logic** — all standards remain fully met, unchanged this cycle. No
  open depth gaps remain in this category.

## 3. The gap

- **No missing layers.** All six layers (five `bun test`-declared plus the unofficial
  shell layer) in active use with the correct framework per layer, plus one
  deliberately-unformalized compile-time-only pattern (`blocked-by-type-parity.check.ts`)
  that has no coverage gap despite not fitting the existing taxonomy.
- **Wrong-layer tests:** 0.
- **External-only deps with no local substitute:** none.
- **No open depth gaps carried in from the prior cycle** — the 2026-09-02 cycle closed
  clean (0 open Promote/deepen items).
- **New this cycle (2026-09-18): all genuinely new source rows across this cycle's
  clusters in the 246-non-routine-commit/16-day delta landed with matching coverage in
  the same commits/PRs.** The Sessions feature (task-store + admin, 7 new test files),
  the outbound task-store webhook dispatcher, `pr-origin-derivation.ts` (POM-1.2, comprehensive
  coverage colocated in a sibling service's test file — a naming-convention nitpick, not
  a gap), the completed author-allowlist rename/split cascade (DBR-1.x–2.4), the
  `plan-session --autonomous`/`TaskKind` migration (a full add→migrate→remove sequence
  completed within this single cycle), the Prisma 7 upgrade (admin/task-store/chat, each
  gaining a `prisma-client.ts` extraction), and ~10 new pure `lib/`/
  `plugins/shipwright/scripts/` helpers — every one landed with matching test coverage at
  the correct canonical layer in the same commit/PR — no gap, no task needed for any of
  them. The retirement of the standalone `triage-dependency-bot-pr(s)` skills/cron/precheck
  (DBR-3.4) is a genuine, cleanly-completed deletion — its content test deleted alongside
  it, no orphaned assertions. See `test-migration.md`'s Method note for the full
  per-item accounting.
- **T-083's stage-3 coverage-promotion, including the branch-protection swap, remains
  fully confirmed done** — see Coverage Gate section above. No remaining open question on
  this item.
- **1 carried-forward, low-priority, non-gap item:** `admin/src/clock.ts` has no
  dedicated test file — consistent with the established convention shared by all sibling
  `Clock` implementations across the repo (near-zero logic, proven via consumers). Not
  scored as a gap; no task minted.
- **Infra gap: none.** Runner config, CI shape, Postgres containers, `Clock` helpers,
  recorded-fixture doubles, PR template all confirmed unchanged and correct. The
  isolation-contract audit's own pattern set still has a documented blind spot
  (singleton-mutation leaks vs. literal `global.*` overrides, as an automated sweep) —
  carried forward, see Open risks.

### Speed delta

**Tier 1 — aggregate-vs-budget (leads; always populated):**

| Aggregate wall-clock | Budget | Ratio / time-to-close |
|---|---|---|
| 1m29s | <15 min (Full PR pipeline) | ~10% of budget — no time-to-close concern |

**Tier 2 — per-layer ratio (conditional on Tier 2 having triggered):**

**Tier 2 triggered:** no. Per-layer numbers are not fabricated here — Tier 1's aggregate
is the complete, sufficient measurement this cycle. Growth trajectory (368 files,
7,870 → 9,219 tests since the 2026-09-02 baseline, by far the largest single-cycle jump
on record) is steady with no budget concern at any point measured — the aggregate got
faster despite the growth.

## 4. Roadmap (five milestones)

### Milestone 1 — Infrastructure baseline
**DOD:** Runners, local substitutes, CI pipeline shape in place. No new tests yet — just the rails.

**T-083 (auto-emitted stage-3 coverage-check promotion) remains `done`.** No new
Milestone 1 task minted. Naming convention + runner-exclusion config
(`docs/test-readiness/naming.md`, `bunfig.toml`'s `pathIgnorePatterns`) is established and
re-verified unchanged this cycle. CI pipeline shape matches `test-system.md`'s documented
target exactly. Postgres service containers, `Clock` injection, and recorded-fixture
doubles are all confirmed in place. T-074 (speed-baseline Postgres refresh) remains
`cancelled` — no successor task needed given Tier 1's CI-sourced measurement is a
complete, sufficient answer.

**T-083's branch-protection swap remains confirmed complete** (see Coverage Gate section)
— no open follow-up remains for this item.

`recorded_fixture_automation` is undeclared in root `CLAUDE.md` (re-verified this cycle —
no `## Recorded-fixture automation` section present, defaults to `planned` per this
cycle's read mechanism) — no repo-config declaration opts this repo out of the
recorded-fixture-refresh-loop pattern. That loop remains correctly **deferred, not
scheduled** (`test-system.md`'s Repo configuration section) — sequenced after the manual,
hand-authored-fixture workflow matures, not alongside it. No further M1 task minted for
it this cycle.

### Milestone 2 — Critical-path coverage
**DOD:** 100% of `critical` tier inventory items have passing tests at the prescribed layer.

**Already satisfied — zero tasks this cycle.** No open items were carried in from the
2026-09-02 pass (it closed clean). No new critical-tier gap found this cycle — the new
`critical`-tier rows this cycle (`agent/src/check-plan.ts`, `github-auth-startup.ts`,
`patch-author-allowlist-ref.ts`, `plugins/shipwright/scripts/is-ci-green.ts`/
`compute-unresolved-comment-check.ts`) all have matching coverage at their prescribed
layer, landed in the same PR as the production code.

### Milestone 3 — Canary suite live
**DOD:** canary not applicable -- deploy_model=direct

Zero tasks, same as every prior cycle. See `test-system.md`'s Canary execution contract
section for what would need to change before this applies.

### Milestone 4 — High-tier coverage
**DOD:** 100% of `high` tier inventory items covered.

**Already satisfied — zero real-gap tasks.** All prior high-tier items (T-080's HITL
bootstrap sequencing among them) remain resolved, unchanged since their respective
resolution cycles.

`admin/src/clock.ts` remains the sole other Net-new item from Phase 3 — carried forward,
unchanged, explicitly a non-gap (established `Clock`-file convention, near-zero logic,
proven via consumers). No task minted for it, same as every prior cycle.

### Milestone 5 — Cleanup
**DOD:** Zero tests in `rebuild` or `delete (redundant)` buckets. CI passes.

**Already satisfied — zero tasks.** Phase 3 found zero `rebuild` and zero `delete
(redundant)` items this cycle, the same steady state as every prior pass. No new
generalizable learning surfaced this cycle warranting a `# Harness TODO`.

## 5. Task list

**Zero new tasks minted this cycle.** Phase 3 (`test-migration.md`) found zero
Promote/deepen, zero rebuild, zero delete/trim, and zero net-gap items beyond the same
carried-forward optional `admin/src/clock.ts` entry (explicitly not worth a task, per
every prior cycle's identical finding). `starting_offset` was computed as **T-085**
(see Status section above) but is unused this cycle — no row to emit.

T-083 (the auto-emitted stage-3 coverage-check promotion task) remains `done`, with its
branch-protection question confirmed complete (see Coverage Gate section). T-084 remains
`deployed`, unchanged.

**Resolved (re-verified via direct task-store query this cycle, not carried forward from
the prior doc):** all 9 tracked IDs now sit at a terminal/near-terminal status. T-076
(`agent-type-manifest-loader` real-disk split), T-077 (`check-review.ts` fail-open
catch-path test), T-078 (injectable ref + never-synced equivalence test), T-079
(`generate-agent-type-schema.ts` coverage class-level decision), and T-082
(`traceReviewCandidacyDecision` combinatorial-exclusion test) all flipped from
`deploying` to **`deployed`** on 2026-09-08T18:34:25Z, closing out the 15-consecutive-
cycle stuck-status escalation carried in every prior doc since 2026-08-06/2026-08-16 —
see Status section above and Open risks below for the resolution detail. T-080, T-081,
T-083, T-084 remain at their already-terminal status (`deployed`/`done`/`deployed` as
applicable), unchanged.

## 6. Open risks

- ~~**T-076 through T-079 (+T-082) had shown zero status movement for 15 consecutive
  test-readiness cycles (parked at `deploying`).**~~ **Resolved this cycle.** All five
  flipped to `deployed` on 2026-09-08T18:34:25Z (a single reconciliation event, confirmed
  via this cycle's direct task-store pull — `deployedAt` now populated for all five,
  previously `null`). No force-patch was ever applied by this pipeline; this appears to
  be a genuine (if very delayed) reconciliation of the same known stuck-status class
  T-081/T-083 hit earlier, not a distinct issue. No further action needed — removed from
  the open-risk list. If a similarly-aged `deploying` record reappears in a future cycle,
  re-escalate using this entry's original reasoning as a template (check deploy-workflow
  runs following the merge commit before assuming it's a real block).
- **New this cycle — `admin/src/session-scope.ts`'s visibility predicate is not yet wired
  into most Session routes.** Documented, intentional interim state per
  `docs/session-notifications.md` and flagged in `test-inventory.md`/`test-system.md`'s
  Ambiguous/gap items — the predicate itself is fully unit-tested, so this is an
  application-logic sequencing gap, not a test-coverage gap. No task minted (this
  pipeline doesn't mint tasks for application logic, only test coverage) — flagged here
  so a human tracks the wiring-completion timeline explicitly rather than it going quiet
  inside an otherwise-100%-covered Sessions feature.
- **The isolation-contract audit's grep pattern set still has a documented blind spot.**
  The one known instance of this leak class (T-084, `agent-slack-membership-ref.unit.test.ts`)
  was fixed back in the 2026-08-23 cycle, but it was found by a manual diff read, not an
  automated sweep — the pattern set still can't catch a recurrence of this leak class on
  its own. Recommend a future cycle add a fourth signal scanning for direct
  `.set(`/`.reset(`/mutation calls on this repo's known singleton-ref exports
  (`agentReposRef`, `reviewAuthorAllowlistRef`, `agentSlackMembershipRef`, `loopJobsRef`,
  and future `agent-*-ref.ts` additions) with no paired `afterEach`/`afterAll` in the same
  file — this is a Phase 3 process improvement, not itself a task against the codebase.
  Carried forward unchanged, no new instance found this cycle.
- **The compile-time-only "type-parity check" pattern (`blocked-by-type-parity.check.ts`)
  still needs a taxonomy decision.** Not a coverage gap — confirmed wired into CI and
  passing — but it doesn't fit any of the five CLAUDE.md-declared suffixes or the
  unofficial sixth "shell" layer. `test-system.md` deliberately declines to formalize a
  seventh layer on a single instance; revisit if a second such file appears.
- **The bash "shell" layer still needs a formal documentation decision.** Carried forward
  unchanged across several cycles now (first flagged 2026-07-31, re-verified present and
  unchanged this cycle): `.github/workflows/lib/chart-tag-utils.sh` / `chart-drift-check.sh`
  and their `test-*.sh` siblings are genuinely tested code units that fall entirely
  outside the `bun test` runner and the five-suffix convention declared in root
  `CLAUDE.md`. Coverage itself is not in question — the open question is purely whether a
  human wants to formally document this as a sixth layer in `CLAUDE.md`'s `## Test
  conventions` table and `docs/test-readiness/naming.md`, or confirm it should stay an
  unofficial exception. Not urgent; not blocking any task above.
- **Recorded-fixture coverage breadth** (~9 remaining `Http*Client` modules that could
  extend the `Recorded*Client` pattern) remains a Phase 2 design-level backlog item, not
  a migration-bucket verdict on any existing file. No task emitted this cycle since
  Phase 3 found no test currently missing coverage because of it.
- **`agent/src/piper-voice.ts` reclassification watch item** (unit today, integration
  once Piper synthesis lands) — still not landed as of this cycle; flagged so a future
  Phase 3 doesn't silently miss the transition.
- **Recorded-fixture maintenance loop remains deferred** (`repo-config/SKILL.md` §5) —
  correctly not scheduled this cycle; sequence after fixture-authoring work matures. No
  task this cycle touches credential-provisioning automation, so this note doesn't apply
  to anything minted this cycle (nothing was minted).

---

## Handoff

This document is intended for execution by:
- An engineer working through the task list manually, OR
- `shipwright /dev-task` consuming the task list as a queue.
