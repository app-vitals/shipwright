# Test Readiness Plan

> Phase 4 synthesis. The executable roadmap from where we are now to a test suite trustworthy enough for autonomous programming.
>
> Generated by `/test-roadmap` on 2026-08-14 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-20260814`), consuming `test-inventory.md` (refreshed at
> `297fb61e`), `test-system.md` (refreshed at `297fb61e`), and `test-migration.md`
> (refreshed at this cycle's HEAD, `297fb61e`).

## Status of the prior cycle's plan (2026-08-11) and everything since

Queried task-store directly (not assumed from the old doc; direct `GET /tasks/{id}`
lookups against each of the 9 known IDs), current status of every relevant ID as of this
cycle:

| Task | Status | Title |
|---|---|---|
| `test-t-073-shipwright` | **done** | `site/src/lib/html-escape.ts` coverage scoping decision |
| `test-t-074-shipwright` | **cancelled** | Refresh the speed baseline against a real Postgres-enabled environment |
| `test-t-075-shipwright` | **deployed** | Naming convention + runner-exclusion re-verification |
| `test-t-076-shipwright` | **deploying** | Split `agent-type-manifest-loader.unit.test.ts`'s real-disk cases into a new `.integration.test.ts` file |
| `test-t-077-shipwright` | **deploying** | `check-review.ts`'s `isBundleComplete` fail-open catch-path test |
| `test-t-078-shipwright` | **deploying** | Make `isAuthorAllowed`'s ref dependency injectable + never-synced equivalence test |
| `test-t-079-shipwright` | **deploying** | `scripts/generate-agent-type-schema.ts` test-coverage class-level decision |
| `test-t-080-shipwright` | **pending** | HITL dev-loop bootstrap's full clone→seed→boot→poll sequence, injected `gh` exec fake |
| `test-t-081-shipwright` | **fixed this cycle: was `blocked`, now `done`** | `TaskService.claim()`'s concurrent-race close (real Postgres) |

**T-081 was found stale and corrected this cycle.** Task-store showed `status: blocked`
with no `pr` field set, but the underlying work landed cleanly: PR **#2560** ("test: add
TaskService.claim() concurrent-race integration test (T-081)"), merged
2026-08-11T08:32:35Z, fully satisfies all 3 of T-081's acceptance criteria per its own PR
body's evidence table (verified directly, not assumed from the PR title). The task
record's `note` field explained the stall: a post-merge CI job (`Build and Push
Task-Store Image`) failed with a transient GHCR "unknown blob" registry error on a
test-file-only diff, and the record was never subsequently reconciled to `done` even
though the actual merge/test outcome was correct. **PATCHed `test-t-081-shipwright` to
`status: done, pr: 2560` this cycle** — closing the loop per the standing instruction not
to leave a resolved gap sitting on a stale record. This also confirms Phase 3's
2026-08-11 finding (T-081's own creation cycle) was accurate: the gap was real, the task
was minted correctly, and the work that closed it was done correctly — only the
record-keeping lagged.

**No status movement on T-073 through T-079 since the 2026-08-11 pass.** T-076 through
T-079 remain parked at `deploying` — this is now the **6th consecutive cycle** these four
IDs have shown zero movement, all four confirmed merged 2026-07-25 through 2026-07-28
(2.5-3 weeks ago) with `deployedAt: null` still unset. Given T-081 just turned out to be a
stale-record case rather than a genuine block, this cycle re-examined whether T-076–T-079
are the same pattern — but unlike T-081, none of the four have a `note` explaining a
specific transient failure, and this pipeline has no mechanism to independently confirm
whether their production code is actually live in a deployed image. **Not corrected this
cycle** (would require verifying actual deploy state, which is `deploy`/`patch` pipeline
territory, not this docs-refresh's remit) — escalated more strongly in Open risks below.
**T-080 is still `pending`**, unchanged since it was queued 2026-07-30 — see Milestone 4
below, this task still carries forward.

**Task-store query for the starting T-NNN offset (per SKILL Process step 4), run before
minting any new IDs:** queried `$SHIPWRIGHT_TASK_STORE_URL/tasks?repo=app-vitals%2Fshipwright&limit=500`
— `total: 860`, requiring one additional page (`&offset=500`, +360 records, 860 total
scanned, matching `.total`). Filtered to `test-t-<NNN>-shipwright` IDs across both pages:
highest is `test-t-081-shipwright`. **`starting_offset = 82`** — this cycle's new tasks
begin at **T-082**.

**This cycle mints exactly one new task, T-082** — Phase 3's one Promote/deepen finding
(`check-review.unit.test.ts`'s combination-coverage depth gap for this cycle's 5
candidate-exclusion fixes). See Milestone 2 below.

**Two items closed out this cycle vs. the 2026-08-11 plan, not carried forward as open
work:**
1. **T-081** (`task-service.ts`'s `claim()` concurrent-race regression test) —
   **confirmed resolved and task-store record corrected** (see above).
2. **Phase 2's gap #19** (`admin/src/blocked-by-type-parity.check.ts`'s new
   compile-time-only pattern) — confirmed not a coverage gap; the check is wired into
   CI's `typecheck` step and passing. Only an open taxonomy question remains (whether to
   formalize a "type-parity" layer), tracked in `test-system.md`, not minted as a task.

## 1. Where we are now

### Layer coverage map

| Layer | Existing tests | Inventory items | Coverage gap |
|---|---|---|---|
| Unit | 127 `.unit.test.ts` | Dominant category-1 surface (`lib/`, decision/classification logic, mutable-box refs, crypto helpers, parsing/validation) | None — every inventory item mapped to unit has a matching test file |
| Integration | 63 `.integration.test.ts` | Every category-2/5 service-boundary and external-integration item | None — `task-service.ts`'s claim-race gap resolved this cycle (T-081); one depth-only Promote/deepen item on `check-review.ts` (see Milestone 2) |
| Smoke | 43 `.smoke.test.ts` | Every category-3 HTTP route across 5 services | None |
| E2E | 21 `site/tests/*.spec.ts` + 3 `{admin,metrics}/e2e/*.e2e.ts` | 4 of 5 identified journeys are browser-driven or partially so (journey #5, HITL bootstrap, is integration-composed, not E2E) | None at E2E; journey #5's integration-harness half is a carried-forward gap (see Milestone 4) |
| Content | 39 `.content.test.ts` | `plugins/shipwright/{commands,agents,skills,references}/*` | None — +1 this cycle (`review-staged/SKILL.content.test.ts`), backing landed production content |
| Shell (unofficial 6th layer) | 2 `test-*.sh` bash assertion suites | `.github/workflows/lib/chart-tag-utils.sh`, `chart-drift-check.sh` | None — coverage exists; only a documentation-completeness question is open (see Open risks) |
| Compile-time-only (new, unformalized) | 1 `*.check.ts` (`blocked-by-type-parity.check.ts`) | `admin/src/blocked-by-type-parity.check.ts` | None — wired into CI's `typecheck` step, confirmed passing; only a taxonomy question open (see Open risks) |

**Total: 296 test files** (272 `bun test`-scanned + 21 `site/tests/*.spec.ts` + 3
Playwright `*.e2e.ts`), +4 from the 2026-08-11 pass. Growth this cycle is 4 new files
(`compute-unaddressed-findings.unit.test.ts`, `diagnose-review-candidacy.unit.test.ts`,
`review-staged/SKILL.content.test.ts`, `task-service.integration.test.ts`), all backing
landed production changes and already-correct, already-passing coverage — none are open
work items.

### Local-runnable status

**100%** of the 272 `bun test`-scanned files run locally with zero external network.
Phase 3's isolation-contract grep sweep (`mock.module(`, `global.fetch =`,
`global.console =`, stray `DATABASE_URL` inside `*.unit.test.ts`) found zero real
violations this cycle (38 `mock.module(` matches, all in comments; 3 pre-existing
non-violation `DATABASE_URL` string-literal matches, unchanged). No test is blocked on an
external service.

### Canary status

**Not applicable.** Root `CLAUDE.md` declares `## Deploy model: direct` — no
staging/production GitHub Environments, no deploy→canary→promote pipeline. Unchanged
finding across every cycle to date. See Milestone 3.

### Speed status (Tier 1 aggregate vs. budget; Tier 2 not triggered)

**Tier 1 — aggregate (measured this pass, via live CI history):**

| Aggregate wall-clock | Budget | Ratio |
|---|---|---|
| 2m39s ([run 31760943817](https://github.com/app-vitals/shipwright/actions/runs/31760943817), 2026-08-14T01:32:53Z, commit `297fb61e` — this cycle's exact HEAD) | <15 min (Full PR pipeline) | ~18% of budget |

Fully green run — no failing jobs. `bun test` step itself: 41.19s. 6,501 tests pass, 0
fail, across 273 files. Coverage held at 88.84% lines / 88.00% functions (threshold
80%/80%). Consistent with every prior cycle's ~13-18% readings — not trending toward the
escalation trigger. Test-count growth trajectory: 5,762 (2026-07-28) → 6,041-6,042
(2026-08-01) → 6,070 (2026-08-05) → 6,071 (2026-08-06) → 6,177 (2026-08-09) → 6,288
(2026-08-11) → 6,501 (2026-08-14, this pass) — steady, expected growth (+213 tests over
the 3-day gap since 2026-08-11).

**Tier 2 triggered:** no — the escalation formula (aggregate wall-clock >7.5 min,
sustained across 2 consecutive measurements) is nowhere near tripped (~18% of budget,
well below the 50% escalation threshold). No per-layer p95 table is populated below, per
the SKILL's explicit guidance against fabricating per-layer numbers when only the Tier 1
aggregate is available.

### "Rebuild" / "Delete (redundant)" debt

| Bucket | Count | Effort |
|---|---|---|
| Rebuild | 0 | — |
| Promote / deepen | 1 | small (see Milestone 2) |
| Delete (redundant) | 0 | — |
| Net-new | 2 | trivial-to-small (see Milestone 4) |

Rebuild and Delete both zero, same as every prior pass. No higher-layer test re-asserts a
business rule already proved at a lower layer; no test's layer/framework/fundamentals
were found wrong. Zero false-confidence coverage identified. Net-new is 2, down from 3 in
the 2026-08-11 plan — `task-service.ts`'s claim-race item resolved this cycle (T-081,
corrected in task-store above); the remaining two (HITL bootstrap integration harness,
`admin/src/clock.ts`) are carried forward unchanged. One new Promote/deepen item this
cycle: `check-review.unit.test.ts`'s combination-coverage depth gap.

## 2. Where we want to be

- **Framework matrix** — already fully realized: `bun test` (unit/integration/smoke/content),
  Playwright (E2E, 3 surfaces), plain `bash` (the shell layer). No framework changes
  needed.
- **Local-substitute map** — every dependency named in the inventory has a working local
  substitute (recorded-fixture doubles for internal/third-party HTTP, real Postgres via
  CI service container for the three Prisma-backed services, a real locally-launched
  Chromium binary for `agent/src/browser.ts`). No blockers.
- **Coverage targets** — single 80/80 line/function CI floor (`scripts/check-coverage.ts`),
  already enforced; tiered targets (critical ~95%, high ~80%, medium spot-check) direct
  prioritization, not a second CI gate.
- **Canary suite definition** — not applicable under the current `direct` deploy model;
  deferred until a staged-deploy model exists.
- **Speed budgets per layer** — Unit <50ms p95 / Integration <2s / Smoke <5s / E2E <30s /
  full PR pipeline <15min. Tier 1 aggregate confirms comfortable headroom (~18% of
  budget); no per-layer breakdown needed since Tier 2 has never triggered.
- **Zero `rebuild` / `delete (redundant)` buckets** — already true today.
- **Every critical- and high-tier inventory item covered at its prescribed layer,
  including a genuine concurrent-caller regression test wherever a claim/lock race was
  closed, and combination coverage across interacting exclusion checks in
  candidate-selection logic** — the claim-race standard is now fully met for both
  `pull-request-service.ts` and `task-service.ts` (T-081 resolved this cycle);
  `check-review.ts`'s 5 candidate-exclusion fixes have solid pairwise coverage but not
  yet a 3+-condition combination case — the one depth gap this cycle's roadmap targets.

## 3. The gap

- **No missing layers.** All six layers (five `bun test`-declared plus the unofficial
  shell layer) in active use with the correct framework per layer, plus one new,
  deliberately-unformalized compile-time-only pattern (`blocked-by-type-parity.check.ts`)
  that has no coverage gap despite not fitting the existing taxonomy.
- **Wrong-layer tests:** 0.
- **External-only deps with no local substitute:** none.
- **One real, newly-identified depth gap this cycle (Promote/deepen, not Net-new):**
  `agent/src/check-review.unit.test.ts` has meaningful pairwise "even when X" combination
  coverage for this cycle's 5 candidate-exclusion correctness fixes (dedup-at-head,
  `reviewedCommitSha`-keyed terminal-skip, review-thread-reply freshness, author-allowlist
  interaction, precedence ordering), but no single test exercises 3+ of these conditions
  simultaneously. This corrects Phase 2's gap #18's "possible coverage gap" framing —
  direct reading of the 98-test-case, 2267-line suite found real, substantive coverage,
  just not a full combinatorial matrix.
- **One real gap, carried forward unchanged since 2026-07-30 (T-080, still `pending`):**
  journey #5 (local HITL dev-loop bootstrap) has its pure-planning half
  (`computeMissingClones`, `computeProvisionPlan`) fully unit-tested, but the full clone →
  seed → boot → poll sequence has no integration-level test exercising it end-to-end with
  an injected `gh` exec fake.
- **One gap resolved this cycle, with a stale task-store record corrected:** T-081
  (`task-service.ts`'s `claim()` concurrent-race regression test) landed via PR #2560
  (merged 2026-08-11) but the task record was stuck at `blocked` due to an unrelated
  transient CI failure on the merge commit. Corrected to `done`/`pr: 2560` this cycle —
  see Status of the prior cycle's plan above.
- **One taxonomy question resolved as "not a gap" this cycle:** Phase 2's gap #19
  (`admin/src/blocked-by-type-parity.check.ts`'s new compile-time-only pattern) — the
  check is wired into CI and passing; only an open naming/documentation question remains,
  tracked in `test-system.md`, no coverage task needed.
- **This cycle's other real source changes, all confirmed already resolved:** the new
  `plugins/shipwright/scripts/diagnose-review-candidacy.ts` (RCO-1.4, covered by its own
  `diagnose-review-candidacy.unit.test.ts`), `task-service.ts`'s filter-honoring +
  assignee-fallback fixes (incremental cases in `task-service.unit.test.ts`),
  `worktree-reaper.ts`'s org/repo-normalization fix, `check-patch.ts`'s stale-CI
  rerun-before-escalate, and two Astro comparison-table content updates. Every touched row
  has matching test coverage landed in the same commits as the production changes — no
  task needed for any of them.
- **1 carried-forward, low-priority, non-gap item:** `admin/src/clock.ts` has no
  dedicated test file — consistent with the established convention shared by all sibling
  `Clock` implementations across the repo (near-zero logic, proven via consumers). Not
  scored as a gap; no task minted.
- **Infra gap: none net-new.** Runner config, CI shape, Postgres containers, `Clock`
  helpers, recorded-fixture doubles, coverage gate, PR template all confirmed unchanged
  and correct.

### Speed delta

**Tier 1 — aggregate-vs-budget (leads; always populated):**

| Aggregate wall-clock | Budget | Ratio / time-to-close |
|---|---|---|
| 2m39s | <15 min (Full PR pipeline) | ~18% of budget — no time-to-close concern |

**Tier 2 — per-layer ratio (conditional on Tier 2 having triggered):**

**Tier 2 triggered:** no. Per-layer numbers are not fabricated here — Tier 1's aggregate
is the complete, sufficient measurement this cycle. Growth trajectory (269 → 273 files,
6,288 → 6,501 tests) is steady with no budget concern at any point measured.

## 4. Roadmap (five milestones)

### Milestone 1 — Infrastructure baseline
**DOD:** Runners, local substitutes, CI pipeline shape in place. No new tests yet — just the rails.

**Already satisfied — zero tasks.** Naming convention + runner-exclusion config
(`docs/test-readiness/naming.md`, `bunfig.toml`'s `pathIgnorePatterns`) is established and
re-verified unchanged this cycle. CI pipeline shape matches `test-system.md`'s documented
target exactly. Postgres service containers, `Clock` injection, and recorded-fixture
doubles are all confirmed in place. T-074 (speed-baseline Postgres refresh) remains
`cancelled` — no successor task needed given Tier 1's CI-sourced measurement is a
complete, sufficient answer.

`recorded_fixture_automation` is undeclared in root `CLAUDE.md` (re-verified this cycle —
no `## Recorded-fixture automation` section present, defaults to `planned` per this
cycle's read mechanism) — no repo-config declaration opts this repo out of the
recorded-fixture-refresh-loop pattern. That loop remains correctly **deferred, not
scheduled** (`test-system.md`'s Repo configuration section) — sequenced after the manual,
hand-authored-fixture workflow matures, not alongside it. No M1 task minted for it this
cycle.

### Milestone 2 — Critical-path coverage
**DOD:** 100% of `critical` tier inventory items have passing tests at the prescribed layer.

**One new task this cycle — T-082.** `agent/src/check-review.ts` is a `critical`-tier
inventory row (part of the `check-*.ts` native-candidate-provider cluster, the
operational core of the review-dispatch loop). Its 5 candidate-exclusion fixes landed
this cycle each with their own guard-condition case, but no test exercises 3+ of them in
combination — this is a depth/DOD-completeness item, not a zero-coverage one. The
`task-service.ts` claim-race item that occupied this milestone last cycle (T-081) is now
resolved and confirmed via corrected task-store record — every other critical-tier item
already has matching coverage landed in the same commits as the production change.

| Task | Files to touch | Layer | Bucket origin | Expected outcome | Verification command |
|---|---|---|---|---|---|
| T-082 | `agent/src/check-review.unit.test.ts` | integration | promote/deepen | 2-3 new multi-condition test cases in `check-review.unit.test.ts` combining 3+ of: stale `commitSha`/`reviewedCommitSha` divergence, review-thread-reply freshness, author-allowlist status, and task-blocked state — proving `traceReviewCandidacyDecision`'s exclusion checks compose correctly in the documented precedence order, not just pairwise | `bun test agent/src/check-review.unit.test.ts` |

Sizing check: single concern (additional cases in one existing test file, one
function's exclusion pipeline), no "part 1 of 2," well under the 1000-line cap — the
fixture-building helpers and `traceReviewCandidacyDecision`'s trace-output assertions
already exist in the same file, so this is additive cases using existing harness, not new
tooling. Independently deployable — the PR can merge standalone with no dependency on any
other task.

### Milestone 3 — Canary suite live
**DOD:** canary not applicable -- deploy_model=direct

Zero tasks, same as every prior cycle. See `test-system.md`'s Canary execution contract
section for what would need to change before this applies.

### Milestone 4 — High-tier coverage
**DOD:** 100% of `high` tier inventory items covered.

**One open task, carried forward from 2026-07-30 (T-080, still `pending`, no new task
minted this cycle).** Journey #5 (local HITL dev-loop bootstrap, `high`-adjacent — Phase 1
ranks the journey itself `medium`, but it gates the team's own primary local dev-loop)
crossed the journey threshold in a prior cycle when `scripts/hitl.ts` gained an auto-clone
preflight and `agent-policy.md` template seeding. The pure-planning half
(`computeMissingClones`, `computeProvisionPlan`) is fully unit-tested; the orchestration
sequence itself (clone → seed → boot → poll → dispatch) has no test exercising it
end-to-end.

| Task | Files to touch | Layer | Bucket origin | Expected outcome | Verification command |
|---|---|---|---|---|---|
| T-080 | `scripts/hitl.ts`, `scripts/hitl.integration.test.ts` | integration | net-new | An injected `gh`/process-exec seam (if not already present) wired into `scripts/hitl.ts`'s orchestration function, plus one sequencing test exercising clone→seed→boot→poll with the fake, proving the full sequence composes correctly (not just its individual pure-planning pieces) | `bun test scripts/hitl.integration.test.ts` |

`admin/src/clock.ts` remains the sole other Net-new item from Phase 3 — carried forward,
unchanged, explicitly a non-gap (established `Clock`-file convention, near-zero logic,
proven via consumers). No task minted for it, same as every prior cycle.

### Milestone 5 — Cleanup
**DOD:** Zero tests in `rebuild` or `delete (redundant)` buckets. CI passes.

**Already satisfied — zero tasks.** Phase 3 found zero `rebuild` and zero `delete
(redundant)` items this cycle, the same steady state as every prior pass. No new
generalizable learning surfaced this cycle warranting a `# Harness TODO`.

## 5. Task list

Flat, ordered, agent-executable. **One genuinely new task this cycle** (T-082, minted at
the `starting_offset = 82` derived from the task-store scan above); T-080 (minted
2026-07-30, still `pending`) carries forward unchanged as the other open item. T-081
resolved and its task-store record corrected — moved to Resolved below.

| ID | Milestone | Files to touch | Layer | Bucket origin | Expected outcome | Verification command | Depends on |
|---|---|---|---|---|---|---|---|
| T-080 | 4 | `scripts/hitl.ts`, `scripts/hitl.integration.test.ts` | integration | net-new | HITL dev-loop bootstrap's full clone→seed→boot→poll sequence proven correct end-to-end via an injected `gh` exec fake | `bun test scripts/hitl.integration.test.ts` | — |
| T-082 | 2 | `agent/src/check-review.unit.test.ts` | integration | promote/deepen | `traceReviewCandidacyDecision`'s exclusion checks proven correct in 3+-condition combination, not just pairwise | `bun test agent/src/check-review.unit.test.ts` | — |

No task in this cycle exceeded the ~1000-line cap, touched more than one functional
concern, or represented an N-service repeated operation — both T-080 and T-082 are single
tasks against a single file's/service's own test surface, with no ordering dependency
between them (they touch unrelated services and can run fully in parallel).

**Resolved (re-verified via direct task-store query this cycle, not carried forward from
the prior doc):** T-073 (`site/src/lib/html-escape.ts` coverage-scoping decision —
**done**), T-074 (speed-baseline Postgres-enabled refresh — **cancelled**, superseded by
Tier 1's successful CI-sourced measurement), T-075 (naming-convention re-verification —
**deployed**), **T-081** (`task-service.ts` concurrent-race close — **done this cycle**,
PR #2560, task-store record corrected from a stale `blocked` state — see Status of the
prior cycle's plan above). T-076 (`agent-type-manifest-loader` real-disk split —
**deploying**), T-077 (`check-review.ts` fail-open catch-path test — **deploying**), T-078
(injectable ref + never-synced equivalence test — **deploying**), T-079
(`generate-agent-type-schema.ts` coverage class-level decision — **deploying**) remain
parked, unchanged since 2026-08-06, all four confirmed merged 2026-07-25 through
2026-07-28 with `deployedAt: null` still unset — **6 consecutive cycles with zero status
movement**, see Open risks below for the strengthened escalation.

## 6. Open risks

- **T-076 through T-079 have now shown zero status movement for 6 consecutive
  test-readiness cycles (parked at `deploying`), and this cycle found a closely related
  case (T-081) that turned out to be a stale record, not a genuine block.** All four
  merged 2026-07-25 through 2026-07-28 (2.5-3 weeks ago) with `deployedAt: null` still
  unset, and — unlike T-081 — none carries a `note` field explaining a specific
  transient failure to reconcile against. This cycle deliberately did **not** force-patch
  these four to `done`, since (a) this docs-refresh pipeline has no independent way to
  confirm their production code is actually live in a deployed image, and (b) blindly
  copying T-081's fix without that confirmation risks marking genuinely-stuck work as
  resolved. **Escalating more strongly this cycle**: recommend a human (or the
  `deploy`/`patch` pipeline's own diagnostics) directly check GitHub Actions on each of
  the four merge commits (PRs #2207, #2227, #2228, #2264) to confirm whether they
  actually deployed, the same way T-081's investigation did — if so, the fix is the same
  stale-record PATCH; if not, this is a real, 3-week-old deploy-pipeline stall.
- **T-080's scope depends on whether `scripts/hitl.ts` already exposes an injectable
  exec seam.** Not directly re-verified this pass (Phase 3 stopped at confirming the
  planning-helper unit tests exist, unchanged since 2026-08-06). If the seam doesn't exist
  yet, T-080 implicitly bundles "add the injection point" with "add the sequencing test."
  Flagging so whoever picks up T-080 checks this first rather than assuming either shape.
- **The compile-time-only "type-parity check" pattern (`blocked-by-type-parity.check.ts`)
  needs a taxonomy decision, new this cycle.** Not a coverage gap — confirmed wired into
  CI and passing — but it doesn't fit any of the five CLAUDE.md-declared suffixes or the
  unofficial sixth "shell" layer. `test-system.md` deliberately declines to formalize a
  seventh layer on a single instance; revisit if a second such file appears.
- **The bash "shell" layer still needs a formal documentation decision.** Carried forward
  unchanged across several cycles now (first flagged 2026-07-31, re-verified present and
  unchanged this cycle): `.github/workflows/lib/chart-tag-utils.sh` / `chart-drift-check.sh`
  and their `test-*.sh` siblings are genuinely tested code units that fall entirely
  outside the `bun test` runner and the five-suffix convention declared in root
  `CLAUDE.md`. Coverage itself is not in question — the open question is purely whether a
  human wants to formally document this as a sixth layer in `CLAUDE.md`'s `## Test
  conventions` table and `docs/test-readiness/naming.md`, or confirm it should stay an
  unofficial exception. Not urgent; not blocking any task above.
- **Recorded-fixture coverage breadth** (~9 remaining `Http*Client` modules that could
  extend the `Recorded*Client` pattern) remains a Phase 2 design-level backlog item, not
  a migration-bucket verdict on any existing file. No task emitted this cycle since
  Phase 3 found no test currently missing coverage because of it.
- **`agent/src/piper-voice.ts` reclassification watch item** (unit today, integration
  once Piper synthesis lands) — still not landed as of this cycle; flagged so a future
  Phase 3 doesn't silently miss the transition.
- **Recorded-fixture maintenance loop remains deferred** (`repo-config/SKILL.md` §5) —
  correctly not scheduled this cycle; sequence after fixture-authoring work matures. Per
  the SKILL's guidance, this is deliberately not wired into T-080, T-082, or any other
  task this cycle — neither open task is a credential-provisioning automation task, so
  neither trips the "never automate a never-proven capability" failure mode this note
  exists to block.

---

## Handoff

This document is intended for execution by:
- An engineer working through the task list manually, OR
- `shipwright /dev-task` consuming the task list as a queue.
