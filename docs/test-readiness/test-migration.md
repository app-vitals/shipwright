# Test Migration

> Phase 3 of the test-readiness pipeline. Reconciles existing tests and infrastructure
> against the Phase 1 inventory ([`test-inventory.md`](./test-inventory.md), refreshed at
> `5eb730db`) and the Phase 2 blueprint ([`test-system.md`](./test-system.md), refreshed at
> `18574f14`), bucketing each existing test file and each inventory item into one of five
> categories.
>
> Generated by `/test-migration` on 2026-07-26 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-20260726`).

## Method note

This pass diffed the current file tree against the 2026-07-21 pass's findings (commit
`82139c09`) and re-verified every open item directly against current repo state, per this
cycle's explicit instruction not to carry forward "missing"/"gap" claims without
re-checking them — the prior doc is 5 days old and this cycle's Phase 2 flagged 3 new
candidate gaps that needed direct verification, not doc-to-doc trust:

1. Enumerated every test file via the Phase 2 framework matrix's glob patterns (`bun
   test`'s default discovery for `*.unit.test.ts` / `*.integration.test.ts` /
   `*.smoke.test.ts` / `*.content.test.ts`, plus `site/tests/*.spec.ts`, plus
   `{admin,metrics}/e2e/**/*.e2e.ts`).
2. Cross-referenced Phase 1's "delta since 2026-07-21" file list (3 new non-test source
   files, 1 deleted, 1 new E2E test file, several materially-changed files) against the
   test-file glob to find newly-introduced coverage gaps, reading actual test bodies
   (not just filenames) for each touched cluster.
3. Directly investigated Phase 2's 3 flagged candidate gaps by reading the relevant
   source and test files line-by-line rather than trusting the Phase 2 doc's framing —
   see "This cycle's 3 flagged gaps, verified" below. Two turned out to be real; one is a
   false premise (already covered, landed 2026-07-24 in a PR that predates this cycle's
   inventory delta).
4. Re-ran the isolation-contract grep sweep (`mock.module(`, `global.fetch =`,
   `global.console =`, `DATABASE_URL` inside `*.unit.test.ts`) across the full current
   suite to catch any regression introduced by unrelated PRs since the last pass.
5. Confirmed `.github/PULL_REQUEST_TEMPLATE.md`, `bunfig.toml`'s `pathIgnorePatterns`, and
   `docs/test-readiness/naming.md` directly rather than trusting the prior doc's snapshot.
6. Read `admin/src/server.smoke.test.ts` directly to confirm test-system.md's carried-forward
   gaps-summary item #6 ("composition entrypoint needs a thin mount-smoke-test") is
   already closed, not an open Phase 3 finding.

**Speed measurement: not re-run this pass**, same reasoning as every prior pass — no local
Postgres in this sandbox (`DATABASE_URL_SHIPWRIGHT_*` unset), so a `bun test` run here
would silently skip every DB-backed integration test and produce misleading timings.
Static review (line counts, import/mock shape) was used instead; no evidence found of any
test breaching a hard cap. A real per-layer measurement continues to exist at
`docs/test-readiness/speed-baseline.md` on `main`, already cross-referenced into
`test-system.md`'s Speed budgets section. See `project_shipwright_metrics_full_suite_flake`
and `project_shipwright_admin_full_suite_flake` in agent memory for the two known
pre-existing full-suite flakes (unrelated to layer/speed, not relevant to this audit).

## This cycle's 3 flagged gaps, verified

Phase 2 (`test-system.md`, this cycle) flagged 3 specific candidate gaps in its "Gaps and
blockers summary." Each was checked directly against current test file contents, not
assumed from the Phase 2 doc's framing:

| # | Flagged gap | Verdict | Evidence |
|---|---|---|---|
| 1 | `agent/src/check-review.ts`'s `isBundleComplete` fail-open `.catch(() => true)` catch path — needs a dedicated integration/unit test case | **Real gap.** No test exercises a *rejecting* `isBundleComplete` dependency passed into `getReviewCandidates`. | `agent/src/check-review.unit.test.ts:631-664` has exactly two `isBundleComplete`-gate test cases — one where the dependency resolves `false` (excluded) and one where it resolves `true` (included). Neither passes a dependency that *rejects*. Separately, `agent/src/check-helpers.unit.test.ts:1265-1277` proves `createBundleCompleteQuery` itself **throws** on a non-OK task-store response (fail-*closed*, correct for the query builder) — but that's the wrong function: the `.catch(() => true)` fail-*open* line lives in `check-review.ts:173-177`'s consumer code, not in `check-helpers.ts`, and nothing exercises that specific consumer-side line. |
| 2 | `agent/src/agent-author-allowlist-ref.ts`'s `hasSynced()` consumer-contract — does `check-review.ts`'s `isAuthorAllowed` default actually check `hasSynced()`, or only `.get()`? | **Real gap** (confirmed, not just Phase 1's "possible" framing — Phase 1 was written before checking the actual call site). | `agent/src/check-review.ts:265-269`: `buildProductionDeps`'s default `isAuthorAllowed` closure reads `agentAuthorAllowlistRef.get().length === 0 \|\| agentAuthorAllowlistRef.get().includes(login)` — it never calls `.hasSynced()`. The ref's own doc comment (`agent-author-allowlist-ref.ts:12-17`) explicitly warns consumers must check `hasSynced()` to distinguish "never synced" from "synced to an empty list," since both return `[]` from `.get()`. The ref itself is thoroughly unit-tested for `hasSynced()` (`agent-author-allowlist-ref.unit.test.ts:19-37`), but the *consumer* test (`check-review.unit.test.ts:678-683`, `"defaults to unfiltered (fail-open) when the ref's allowlist is empty"`) calls `agentAuthorAllowlistRef.set([])` before asserting — which sets `synced = true` — so it only proves the synced-but-empty case, not the never-synced case. Both states currently produce the same (safe) unfiltered behavior by construction, since the closure doesn't branch on `hasSynced()` at all — so this is not a live bug, but the intentional-equivalence assertion Phase 1 called for does not exist as a test. |
| 3 | `admin/src/agents-api.ts`'s `POST /agents` create route — does a smoke test prove the unknown-type-400-before-any-row-written sequencing? | **False premise — already covered.** Landed 2026-07-24 in PR #2200 ("feat: seed new agents from the type manifest at create time"), 2 days before this cycle's Phase 1 inventory even ran. | `admin/src/agents-api.smoke.test.ts:1611-1652`, `"POST /agents with an unknown type returns 400 and creates no agent row"` — asserts `res.status).toBe(400)` **and** spies on `agentService.create()` via a `created: string[]` array asserted `toHaveLength(0)`, proving zero rows are written before the 400. Exactly the sequencing property Phase 2 asked for. |

**Net effect on this cycle's Net-new list:** 2 real net-new test-gap entries (gaps #1 and
#2 above), 1 item removed as a false premise (gap #3, already reuse-as-is).

## Summary

| Bucket | Test count | Infra count | Effort |
|---|---|---|---|
| Reuse as-is | ~268 | 6 | — |
| Promote / deepen | 0 | 0 | — |
| Rebuild | 0 | 0 | — |
| Delete (redundant) | 0 | 0 | — |
| Net-new | 2 | 0 | trivial-to-small (both are single new test cases, not new files) |

**Total existing test files discovered:** 119 unit + 57 integration + 43 smoke + 28
content = 247 `bun test`-scanned files, + 20 `site/tests/*.spec.ts` + 3 Playwright
`*.e2e.ts` (2 `admin/e2e/`, 1 `metrics/e2e/`) = **270 test files total** — up from 249 at
the 2026-07-21 pass (+21: +13 `bun test`-scanned, +8 `site/tests/*.spec.ts`). The
`bun test`-scanned growth is concentrated in `admin/` (agent-type-manifest-loader
integration/unit split, agent-member/agent-type-registry test files) and `scripts/`
(`hitl.unit.test.ts`, `wait-for-agent.unit.test.ts`) landing across the 21-commit window
since the last pass — none of it attributable solely to this cycle's narrow 5-day/22-file
Phase 1 delta. The `site/tests/*.spec.ts` growth (12 → 20) is the marketing-site docs-page
buildout (`docs-admin-console`, `docs-agent-skills`, `docs-commands-reference`,
`docs-cron-jobs`, `docs-day-to-day-operations`, `docs-nav`, `docs-platform`,
`docs-prs-api`, `docs-search`, `docs-task-store-api`, `docs-the-shipwright-loop` — 11 new
docs specs, net +8 after accounting for consolidation), unrelated to this cycle's Phase 1
scope.

**Headline finding:** every source-level addition/change in this cycle's narrow Phase 1
delta already has matching test coverage at the correct layer, **except** the two
consumer-side gaps confirmed above (gap #1 and gap #2) — both are single missing test
*cases* inside already-existing, already-correctly-classified test files, not missing
files or missing layers. The third flagged candidate (gap #3) is a false premise: it was
already resolved 2 days before this cycle's Phase 1 ran, and Phase 2's own "confirm this
exists" framing (rather than "this is missing") was the more accurate read in hindsight.
No isolation-contract regression, no new redundant-coverage pattern, no rebuild
candidates, no trim/delete candidates — the same clean steady state as every prior pass in
this repo's test-migration history.

## Resolved since the 2026-07-21 pass

Verified directly against the current tree (not carried forward from memory):

| Prior item | Verdict then | Status now |
|---|---|---|
| `admin/src/clock.ts` — no dedicated test file, low-priority/optional net-new | non-gap (established `Clock`-file convention) | **Unchanged, still a non-gap.** Confirmed still no `admin/src/clock.ts.unit.test.ts`-equivalent; carried forward below in Net-new for completeness but not counted toward the "real gap" total. |
| `mcp-server/src/main.ts` fail-closed guard — real gap flagged at the 2026-07-19 pass | real gap | **Resolved (confirmed at 2026-07-21 pass, re-verified now).** `mcp-server/src/main.smoke.test.ts` exists and exercises the module. |
| `admin/src/server.ts` composition entrypoint — needs a thin mount-smoke-test (flagged in this cycle's own `test-system.md` gaps-summary item #6, carried forward from an earlier design pass) | open design-level watch item | **Resolved — was already closed before this cycle, `test-system.md` just hadn't dropped the carried-forward note.** `admin/src/server.smoke.test.ts` exists (`describe("server.ts composed app mounts correctly")`) and directly asserts all three sub-apps (`createAdminUIApp`, `createAgentRuntimeApp`, `createAdminApp`) mount and route correctly under one Hono instance. |

## Isolation contract audit

Grepped the full current suite (270 files) for the three hard-rule signals from root
`CLAUDE.md`'s Test isolation section and `references/principles.md`'s `t1_no_global_mocking`:

| Signal | Files matched | Real violations |
|---|---|---|
| `mock.module(` | matches only in header-comment prose (e.g. `"no mock.module(), no global overrides"`) across `admin/src/agent-deletion.unit.test.ts`, `task-store/src/pull-request-service.unit.test.ts`, `agent/src/startup-dm.integration.test.ts`, `agent/src/cron-handler.integration.test.ts`, `agent/src/entrypoint.integration.test.ts`, `agent/src/github-app-auth.integration.test.ts`, and others | **0** — every match is a compliance-documenting comment, none an executable call |
| `global.fetch = ` (assignment) | 0 | **0** |
| `global.console = ` (assignment) | 0 | **0** |
| `DATABASE_URL` inside `*.unit.test.ts` | 3 (same three as every prior pass: `admin/src/main.unit.test.ts`, `admin/src/schema.unit.test.ts`, `scripts/dev-tmux.unit.test.ts`) | **0** — unchanged from prior findings; all assert on the env-var name as a string literal or an early-return guard, none opens a real connection |

No isolation-contract regression found since the 2026-07-21 pass.

## Delete (redundant assertion) analysis

**Zero delete/trim candidates identified**, same finding as every prior pass. No new
duplicate-assertion pattern was introduced by this cycle's delta. The new
`isAuthorAllowed`/`isBundleComplete` gate tests in `check-review.unit.test.ts` each assert
a distinct, non-overlapping property (author-match filtering vs. bundle-completeness
filtering), and the `missingRequiredEnv`/`authorAllowlist` field coverage added across
`admin/src/agents.unit.test.ts` (service layer), `admin/src/agents-api.smoke.test.ts`
(route layer), and `admin/src/agents-api.integration.test.ts` (DB-integration layer) is
correctly non-duplicative per layer — the unit layer proves the derivation logic, the
integration layer proves it against real Postgres rows, and the smoke layer proves the
route surfaces the field in its JSON response, none re-asserting what a lower layer
already owns.

## Tests — bucketed

### Reuse as-is

The large majority of the suite, unchanged in shape from the 2026-07-21 pass aside from
the narrow delta below. Grouped by cluster; each cluster was checked against Phase 1's
delta list, not re-read in full where unchanged.

| Cluster | Test files | Layer(s) | Functional unit | Notes |
|---|---|---|---|---|
| `lib/` | `pricing`, `org-repo`, `request-context`, `sentry`, `web/toolbar`, `claim-ttl`, `github-login` (new since 2026-07-21), `task-store-types` (`.unit.test.ts`) | unit | Cross-service pure helpers | `lib/github-login.unit.test.ts` is this cycle's only new file in this cluster — 8 cases covering valid/invalid GitHub-login-format strings (length boundary, hyphen placement, disallowed characters). `lib/agent-default-tools.ts` and its test were both deleted this cycle (superseded by type-manifest-driven tool seeding) — confirmed zero dangling references. |
| `metrics/src/*` | ~9 top-level + ~7 `lib/` + 2 `providers/` + 1 `dashboard/` | unit/integration/smoke | Formatters, provider selection, HTTP clients, `/metrics/*` routes, request coalescing | Unchanged this cycle — no files in Phase 1's delta touch `metrics/`. |
| `agent/src/*` | ~48 files (incl. new `agent-author-allowlist-ref.unit.test.ts`) | unit/integration/smoke | Work selection, check-\* precondition scripts, Slack Bolt handlers, `claude.ts` spawner, chat/task-store/GitHub clients, chat-poller loop, author-allowlist ref | `check-review.ts`'s new `isBundleComplete` gate and ref-backed `isAuthorAllowed` default both landed with *partial* test coverage in the same commits (happy-path true/false cases) — see Net-new below for the two confirmed-missing consumer-side edge cases. `agent/src/index.ts` (composition entrypoint wiring `agentAuthorAllowlistRef` into `syncConfig()`) remains untested directly, consistent with every other composition/bootstrap file in this cluster (`entrypoint.ts`, `entrypoint-main.ts`) — not counted as a new gap since the wired-in pieces (`agentReposRef.set`, `agentAuthorAllowlistRef.set`) are each tested at their own layer and `index.ts` itself has never had direct test coverage in this repo's history. |
| `admin/src/*` | ~56 files | unit/integration/smoke | Agent/Env/Cron/Tool/Token CRUD, K8s client, provisioning clients, admin UI, chat-token daily rollup, work-queue snapshots, server composition, agent-type manifest resolution | Absorbed the largest test-file growth this cycle: `admin/src/agent-type-manifest-loader.integration.test.ts` (new — was `.unit.test.ts`-only before, now split so `listTypes()`'s real on-disk `agent-types/` read gets one true integration case alongside the fake-lister unit cases), plus `authorAllowlist`/`missingRequiredEnv`/type-aware-create coverage spread correctly across `agents.unit.test.ts` (derivation logic), `agents-api.smoke.test.ts` (route + zero-rows-written sequencing — see gap #3 above), and `agents-api.integration.test.ts` (real-Postgres round-trip). `admin/src/server.smoke.test.ts` confirmed to already prove 3-sub-app composition (resolves this cycle's carried-forward gaps-summary item #6). |
| `task-store/src/*` | ~24 files | unit/integration/smoke | Task/PR/Token services, dependency-resolution rules, routes | Unchanged this cycle — no files in Phase 1's delta touch `task-store/`. |
| `chat/src/*` | 10 files | unit/integration/smoke | Message/thread/token services, routes, OpenAPI schemas, spec generator | Unchanged this cycle. |
| `mcp-server/src/*` | 8 files | unit/integration/smoke | Tool allowlist, tool-caller proxy, MCP wire protocol, inbound bearer auth, Streamable HTTP transport + idle-session reaper, app-factory composition, fail-closed entrypoint guard | Unchanged this cycle — `main.smoke.test.ts` (resolved at the 2026-07-19/21 passes) confirmed still present and correct. |
| `brand/*` | 2 files | unit | Hex-color lint, CSS token generation | Unchanged. |
| `scripts/*` | 16 files (incl. new `wait-for-agent.unit.test.ts`, `hitl.unit.test.ts`) | unit/integration/smoke | Coverage gate, config-docs checker, dev supervisors, seed scripts, version sync, agent-provisioning poll loop | `scripts/wait-for-agent.ts` (new this cycle, replaces `dev-tmux.ts`'s prior implicit agent auto-creation) landed with `scripts/wait-for-agent.unit.test.ts` in the same commit — poll-loop logic tested via injected poll function, not real timers. `scripts/dev-tmux.unit.test.ts` was updated in place (not net-new) to reflect the removed auto-creation path and the new shell-out to `wait-for-agent.ts`. `scripts/hitl.unit.test.ts` predates this cycle's Phase 1 delta (part of the 21-commit window since 2026-07-21 but not part of the narrower 5-day delta) — carried forward as reuse-as-is. |
| `plugins/shipwright/scripts/*` | 15 files | unit | `check-*.ts` cron prechecks, `classify_test_layer.ts`, `check-helpers.ts` | Unchanged this cycle — the plugin-side `check-*.ts` scripts are a separate, older generation from `agent/src`'s native TypeScript candidate providers (which are the ones that changed this cycle); no overlap in the delta. |
| `plugins/shipwright/{commands,agents,skills,references,test}/*` | ~30 files (28 dedicated `*.content.test.ts`, confirmed via direct count this pass) | content/unit/integration | Command/skill/agent markdown prose assertions | `commands/` grew from ~19 to 28 `.md` files per Phase 1's note, but this was **not** independently verified against content-test file count this pass (Phase 1 explicitly flagged this batch as not individually read) — flagged again here as an open verification item for a future pass, not scored as a gap since the repo's established convention (batch-classify as content, one content test per file) has held in every prior spot-check. |
| `site/tests/*.spec.ts` | 20 files | e2e | Marketing pages, incl. 11 `docs/*` page specs | Grown from 12 to 20 since the 2026-07-21 pass (see Summary section) — predates and is unrelated to this cycle's narrow Phase 1 delta; each spec covers a distinct rendered page, no overlap found. |
| `admin/e2e/*.e2e.ts`, `metrics/e2e/*.e2e.ts` | 3 files | e2e | Admin login/agents pages, metrics dashboard | Unchanged this cycle — `admin/e2e/agents-page.e2e.ts` (ADM-3.2, 13 cases) landed in the prior cycle window per Phase 1, not new to this pass but reconfirmed present and correctly scoped (auth redirect, env/cron/token visibility, secret masking, mobile viewport). |

### Promote / deepen

None. No promote-shaped gap was found — every gap identified this pass (see Net-new below)
is a missing *test case* inside an already-correctly-shaped, already-correctly-layered test
file, which is scored as Net-new (new case needed) rather than Promote (the existing shape
is right, it's simply incomplete on one specific input) per the SKILL's bucket definitions.
A reviewer could reasonably argue gap #1 and gap #2 belong in Promote instead of Net-new
since they're additions to existing `describe` blocks, not new files — flagged here for
Phase 4 to resolve either way; the roadmap-level effort estimate is identical (small,
same-file additions) regardless of which of the two buckets they're filed under.

### Rebuild

None. No test file's layer, framework, or fundamentals were found wrong this pass.

### Delete (redundant)

None. See Delete (redundant assertion) analysis above.

### Net-new

Sorted by criticality. Two real gaps confirmed this cycle (both from the Phase 2
flagged-gaps investigation above), plus one carried-forward low-priority item unrelated to
this cycle's delta:

| Inventory item | Prescribed layer | Criticality | Estimated effort |
|---|---|---|---|
| `agent/src/check-review.ts`'s `isBundleComplete` `.catch(() => true)` fail-open consumer path (`check-review.ts:173-177`) — needs a test case where the injected `isBundleComplete` dependency **rejects**, asserting `getReviewCandidates` still includes the PR (proceeds) rather than excluding it | integration (the file's canonical layer per Phase 1; the specific test can be a `.unit.test.ts` case alongside the existing two bundle-gate cases at `check-review.unit.test.ts:631-664`, matching how the file's other dependency-injection tests are already structured) | critical (per Phase 1 — `check-review.ts` is the review-candidate qualification gate; a task-store hiccup here must never silently block review) | trivial — one new `test(...)` block modeled directly on the two adjacent cases, injecting `async () => { throw new Error(...) }` as `isBundleComplete` and asserting `result` still contains the PR |
| `agent/src/agent-author-allowlist-ref.ts`'s `hasSynced()` consumer-contract equivalence at `check-review.ts`'s `isAuthorAllowed` default closure — needs a test case that leaves the ref **never synced** (no `.set()` call at all) alongside the existing synced-but-empty case, to make explicit that both states intentionally produce the same fail-open (unfiltered) behavior | unit (`check-review.unit.test.ts`'s existing `describe("buildProductionDeps isAuthorAllowed default (AAL-2.2)")` block, alongside the case at line 678) | high (per Phase 1 — gates review-candidate selection directly; the ambiguity is about documenting *intentional* equivalence, not fixing a bug, since the current behavior is already the safe default either way) | trivial — one new `test(...)` that does *not* call `agentAuthorAllowlistRef.set(...)` in its own body (relying on a true "never synced" state, which requires either module-reset care or an injectable ref parameter — flag for whoever picks this up to confirm the existing `beforeEach`/`afterEach` `set([])` pattern doesn't defeat the "never synced" precondition, since `set([])` itself flips `hasSynced()` to `true`) |
| `admin/src/clock.ts` (`Clock`/`SystemClock`/`FixedClock` implementation for admin) — carried forward, unrelated to this cycle's Phase 1 delta | unit | low | trivial, optional — carried forward unchanged from every prior pass. Sibling `Clock` files (`agent/src/clock.ts`, `metrics/src/lib/clock.ts`, `plugins/shipwright/scripts/clock.ts`, `mcp-server/src/clock.ts`) all lack a dedicated test file too; near-zero logic (two one-line methods), proven correct via consumers per the repo's established convention. |

**Note on gap #2's test-authoring subtlety:** whoever picks up the second Net-new item
should notice that `check-review.unit.test.ts:668-670`'s `beforeEach(() => {
agentAuthorAllowlistRef.set([]); })` already calls `.set()` before every test in that
`describe` block, which means `hasSynced()` is `true` for every test in the block as
currently written — a literal "never synced" test needs either its own `describe` block
that skips the shared `beforeEach`, or the production closure needs to accept an injectable
ref (mirroring how `isBundleComplete`/`isAuthorAllowed` are already injectable) so the test
can construct a fresh, never-`.set()` ref instance rather than fighting the shared
module-level singleton's `beforeEach` reset. This is a real test-authoring wrinkle, not
just an estimation footnote — flag it explicitly for Phase 4 sequencing.

## Infrastructure — bucketed

### Reuse as-is

- **Bun test runner config** (`bunfig.toml`'s `pathIgnorePatterns`) — re-verified this
  pass: `["**/site/**", "**/metrics/e2e/**", "**/admin/e2e/**", "**/*.canary.test.ts"]`,
  unchanged and correct.
- **CI workflow shape** (`.github/workflows/ci.yml`) — matches Phase 2's documented shape;
  unchanged this cycle.
- **Postgres service container for admin/task-store/chat** — real DB per test run, all
  three `DATABASE_URL_SHIPWRIGHT_*` vars wired in `ci.yml`.
- **`Clock` injection helpers** — five implementations (`agent/src/clock.ts`,
  `metrics/src/lib/clock.ts`, `plugins/shipwright/scripts/clock.ts`, `admin/src/clock.ts`,
  `mcp-server/src/clock.ts`) plus `plugins/shipwright/scripts/test-helpers/doubles.ts` —
  still the strongest, most consistently-applied convention in the repo. No new consumer
  landed this cycle that deviates from the pattern.
- **Recorded-fixture doubles** — `RecordedKubernetesClient`,
  `metrics/src/providers/task-store-recorded.ts`,
  `metrics/src/fixtures/task-store-fixtures.ts`, `agent/src/shipwright-config-client.ts` —
  unchanged this cycle; pattern is stable and extending cleanly to new clients.
- **`scripts/check-coverage.ts`** — the 80/80 lcov-threshold gate, unchanged.
- **`.github/PULL_REQUEST_TEMPLATE.md`** — confirmed present directly (not assumed from
  the prior doc), matches `test-system.md`'s Repo configuration section's note that this
  is resolved, not a gap.

### Promote / deepen

None. The design-level backlog item (extending `Recorded*Client` fixture-double coverage
to the remaining `Http*Client` modules named in `test-system.md`'s Local execution
architecture table) remains a Phase 2 design-level item, not a migration-bucket verdict on
any existing infra file — carried in `test-system.md`'s gaps summary, not restated here as
a bucket entry to avoid double-tracking.

### Rebuild

None.

### Delete (redundant)

None.

### Net-new

None. No new infrastructure gap identified this cycle — the Bun test runner config, CI
workflow shape, Postgres service containers, `Clock` helpers, recorded-fixture doubles,
and coverage gate are all confirmed unchanged and correct (see Reuse as-is above). The new
`admin/src/agent-type-manifest-loader.integration.test.ts` file is a **test file**, already
counted in the Tests section above, not a new piece of shared infrastructure (it reuses
the existing injectable-`TypeDirLister` pattern with zero new fixture/helper machinery).

## Risk callouts

No test currently passing (green) received a `delete` or `rebuild` verdict this pass, so
the SKILL's mandatory risk-callout requirement has no entries to populate — same as every
prior pass in this repo's history.

## Speed measurement

- **Measured:** no local re-measurement this pass (no local Postgres available in this
  sandbox; see Method note above) — same as every prior pass. **This is now the 6th
  consecutive cycle this measurement-only item has gone unactioned** (2026-07-19,
  2026-07-21, 2026-07-23, 2026-07-25, and this 2026-07-26 pass, plus at least one earlier
  pass the 2026-07-21 doc's "fourth pass" framing referenced) — see the escalation in the
  Method note above and the Carried-forward measurement-only item tracking paragraph below.
- **Current per-layer p95:** not measured *by this pass*, but the real, `time`-wrapped
  per-layer measurement at `docs/test-readiness/speed-baseline.md` (committed `8a037efc`,
  PR #1580, merged 2026-07-15) remains valid in direction (no layer found near its budget
  via static review) but is now **11 days stale** relative to this and the prior several
  cycles' substantial in-place logic growth (`loop-orchestrator.ts`, `pr-state-reconciler.ts`,
  `check-review.ts`'s new gates, `admin/src/agents.ts`'s expanded create path) — still
  correctly cross-referenced from `test-system.md`'s Speed budgets section, but the
  staleness itself is part of why this item needs to move to an actual M1 task rather than
  another "not re-run this pass" note.
- **Tests over hard cap:** none identified via static review. `admin/src/admin-ui.smoke.test.ts`
  (5,736 lines) and `admin/src/admin-ui-pages.unit.test.ts` (6,024 lines) remain the two
  largest files by line count — both large due to test-case count (broad UI-form/route
  coverage), not per-test slowness, consistent with every prior pass's finding.
  `agent/src/loop-orchestrator.unit.test.ts` (3,143 lines) and
  `agent/src/check-review.unit.test.ts` (701 lines) were spot-checked this pass given their
  role in this cycle's delta — both remain single-concern-per-test, no shared expensive
  `beforeEach` work, no size/slowness signal.

**Carried-forward measurement-only item tracking (per SKILL failure-modes guidance):** the
"no local Postgres in sandbox" speed-remeasurement gap has now been carried forward
unactioned across **6 consecutive `/test-migration` passes** (2026-07-19, 2026-07-21,
2026-07-23, 2026-07-25, this 2026-07-26 pass, plus at least one earlier pass the
2026-07-21 doc's "fourth pass" framing referenced). The 2026-07-25 pass already flagged
this at cycle 5 and stated Phase 4 must place it in Milestone 1 — this pass re-confirms
that instruction is still open (no evidence a Phase 4/M1 task addressing it has landed;
`speed-baseline.md` is still dated 2026-07-15, unchanged). Per the SKILL's explicit rule
(3+ consecutive cycles), this item **must be surfaced for `/test-roadmap` to place as an
explicit first Milestone 1 (M1) task** — not left to be noticed in prose, and not
re-deferred a 7th time. The underlying blocker (sandbox has no live Postgres) is
infrastructural, not a test-authoring gap; the concrete M1 action is either (a) provision
a throwaway local Postgres in the docs-refresh sandbox before future `/test-migration`
runs, or (b) treat `speed-baseline.md`'s existing CI-sourced measurement as sufficient
going forward, refresh it from a real CI run instead of a local sandbox run, and stop
attempting local re-measurement entirely — Phase 4 should pick one explicitly this cycle.

## Next step

Run `/test-roadmap` to synthesize this migration audit with the Phase 1 inventory and
Phase 2 blueprint into the executable roadmap. Candidate Phase 4 inputs from this
document:

1. **2 real net-new test-case gaps** (both small, same-file additions, not new files):
   `check-review.ts`'s `isBundleComplete` fail-open consumer path (critical), and
   `agent-author-allowlist-ref.ts`'s `hasSynced()` consumer-contract equivalence test
   (high) — the latter has a test-authoring subtlety (shared `beforeEach` `.set([])` call
   defeats a literal "never synced" precondition) that Phase 4 should scope explicitly,
   possibly as "make `isAuthorAllowed`'s ref dependency injectable" rather than "add one
   test case."
2. **1 low-priority/optional carried-forward item** (`admin/src/clock.ts` — still a
   non-gap by the repo's own convention).
3. **1 false-premise flagged gap resolved with zero action needed** (`admin/src/agents-api.ts`'s
   `POST /agents` unknown-type-400 sequencing — already covered, landed 2026-07-24).
4. **1 measurement-only item now at 6 consecutive carried-forward cycles** (local speed
   re-measurement blocked by no sandbox Postgres, `speed-baseline.md` now 11 days stale)
   — the 2026-07-25 pass already said Phase 4 must place this in M1; this pass confirms
   that still hasn't happened and re-escalates. Must land as an explicit M1 task this time,
   not carried forward a 7th time.
5. **0 net-new infra items, 0 rebuild candidates, 0 delete/trim candidates, 0 isolation-contract
   regressions** — the steady-state finding holds for another cycle.
