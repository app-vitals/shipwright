# Test Migration

> Phase 3 of the test-readiness pipeline. Reconciles existing tests and infrastructure
> against the Phase 1 inventory ([`test-inventory.md`](./test-inventory.md), refreshed at
> `a9f2a534`) and the Phase 2 blueprint ([`test-system.md`](./test-system.md), refreshed at
> `a9f2a534`), bucketing each existing test file and each inventory item into one of five
> categories.
>
> Generated by `/test-migration` on 2026-08-09 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-20260809`).

## Method note

This pass diffed the current file tree against the 2026-08-06 pass's findings (baseline
doc committed at `71b6024e`) and re-verified every open item directly against current
repo state, per this cycle's standing instruction not to carry forward "missing"/"gap"
claims without re-checking them:

1. Enumerated every test file via the Phase 2 framework matrix's glob patterns: 123
   `*.unit.test.ts` (+1), 61 `*.integration.test.ts` (+1), 43 `*.smoke.test.ts`
   (unchanged), 36 `*.content.test.ts` (+1) (263 `bun test`-scanned files, up from 260),
   plus 21 `site/tests/*.spec.ts` and 3 Playwright `*.e2e.ts` (2 `admin/e2e/`, 1
   `metrics/e2e/`) = **287 test files total** — +3 from the 2026-08-06 pass, all
   confirmed via `git diff --stat 71b6024e..origin/main --diff-filter=A -- '*.unit.test.ts'
   '*.integration.test.ts' '*.smoke.test.ts' '*.content.test.ts' '*.spec.ts' '*.e2e.ts'`
   (exactly 3 additions, 0 deletions): `scripts/minikube.unit.test.ts`,
   `task-store/src/hitl-blocked-migration.integration.test.ts`,
   `plugins/shipwright/commands/plan-session.content.test.ts`.
2. Cross-referenced Phase 1's delta list (50 commits / 97 files since the `71b6024e`
   baseline, 13 non-doc/non-version-bump/non-chart source files) against the test-file
   glob to confirm each has matching coverage — reading the actual test body (not just
   the filename) for every touched file: `task-store/src/blocked-by.ts` +
   `task-store/src/ready.ts` (HSR-1.1/RCG-1.2), `agent/src/check-deploy.ts`,
   `check-patch.ts`, `check-review.ts`, `check-helpers.ts`, `admin/src/admin-ui-pages.ts`
   (HSR-1.1 rename), `agent/src/loop-orchestrator.ts` (STD-1.1/1.2/1.4 + RCG-1.2), new
   `scripts/minikube.ts`.
3. Directly investigated Phase 2's "Gaps and blockers summary" against current test file
   contents (not the design doc's framing) — see "This cycle's flagged gaps, verified"
   below. One new gap was flagged and verified resolved this cycle (the HSR-1.1
   completion's data-migration coverage); the rest of Phase 1's delta was confined to
   already-integration/unit-tested rows and had matching coverage landed in the same
   commits as the production changes.
4. Re-ran the isolation-contract grep sweep (`mock.module(`, `global.fetch =`,
   `global.console =`, `DATABASE_URL` inside `*.unit.test.ts`) across the full current
   suite to catch any regression introduced by unrelated PRs since the last pass — zero
   new violations, same three pre-existing non-violation `DATABASE_URL` string-literal
   matches as every prior pass (`admin/src/main.unit.test.ts`,
   `admin/src/schema.unit.test.ts`, `scripts/dev-tmux.unit.test.ts`).
5. Confirmed `.github/PULL_REQUEST_TEMPLATE.md`, `bunfig.toml`'s `pathIgnorePatterns`,
   `admin/src/clock.ts` (still no dedicated test file, still a non-gap), and
   `scripts/hitl.ts` (`git log 71b6024e..origin/main -- scripts/hitl.ts` empty — no scope
   growth since 2026-08-06) directly against current tree state rather than trusting the
   prior doc's snapshot. Also confirmed zero `.github/workflows/` changes since the
   baseline (`git log 71b6024e..origin/main -- .github/workflows/` empty) — the shell
   layer's open documentation-decision gap is unchanged, not newly relevant.
6. Pulled a fresh live Tier 1 speed measurement from CI (`gh run view --log` against the
   most recent green `lint / typecheck / test` run on `main`, at this cycle's exact
   HEAD) — see Speed measurement below.
7. Verified task-store state for the carried-forward T-073 through T-080 IDs directly
   via paginated `GET /tasks/{id}` lookups (not trusted from the prior doc's snapshot) —
   see the Status section this doc's sibling `test-readiness-plan.md` maintains; no
   status movement since 2026-08-06.

**Speed measurement: re-run this pass via CI history (Tier 1 only).** No local Postgres
in this sandbox (`DATABASE_URL_SHIPWRIGHT_*` unset, `node_modules` not installed in this
docs-only worktree), so a local `bun test` run was not attempted — Tier 1's aggregate
pull needs only `gh run view --log` against the most recent green `lint / typecheck /
test` job, which this sandbox does have. See Speed measurement section for the numbers.

## This cycle's flagged gaps, verified

Phase 2's (`test-system.md`) "Gaps and blockers summary" is unchanged this cycle — still
the same 14 items as the 2026-08-06 pass. This Phase 3 audit separately self-identified
one additional, already-resolved item (#15, the HSR-1.1 data-migration coverage) while
cross-referencing Phase 1's delta list against current test file contents — it is not a
Phase 2 doc entry. The rest of this cycle's source delta (HSR-1.1's field-rename
plumbing, RCG-1.2, STD-1.1/1.2/1.4, `scripts/minikube.ts`) was confined to
already-integration/unit-tested rows and introduced no new gap. Each of Phase 2's 14
items was re-checked directly against current test file contents rather than trusted
from the prior pass's snapshot:

| # | Flagged item | Verdict | Evidence |
|---|---|---|---|
| 1 | `.github/PULL_REQUEST_TEMPLATE.md` exists | Non-gap, confirmed | File present, unchanged. |
| 2 | Recorded-fixture coverage partial | Design-level backlog item, not a migration-bucket finding | Carried forward in `test-system.md`; not restated as a bucket entry here. |
| 3 | Canary contract deferred | Correct non-applicability, `deploy_model: direct` | Unchanged. |
| 4 | Branch-protection checks need live-API verification | Design-level item, not this phase's scope | Carried forward. |
| 5 | `agent/src/piper-voice.ts` reclassification watch | Still open, no synthesis code landed | Confirmed — file unchanged this cycle, still validates local `.onnx` paths only. |
| 6 | `admin/src/server.ts` composition entrypoint smoke test | Resolved, confirmed present again | `admin/src/server.smoke.test.ts:227` — `describe("server.ts composed app mounts correctly")`. Unchanged since 2026-07-26. |
| 7 | Bash "shell" layer needs a documentation decision | Open human-review item, not a coverage gap | `.github/workflows/lib/chart-tag-utils.sh`, `chart-drift-check.sh`, `test-check-chart-drift.sh` all confirmed present and exercised via `bash <file>.sh` CI steps — coverage exists, only the CLAUDE.md documentation question is open. |
| 8 | `check-review.ts`'s `reviewedCommitSha`-keyed guard divergent-value case | Resolved (carried forward from prior pass, re-verified) | `agent/src/check-review.unit.test.ts:562,590,609` — three divergent-value scenarios covered, unchanged this cycle. |
| 9 | `pr-state-reconciler.ts`'s RDG-1.1 bundle-mate precondition (DIRECT path) | Resolved (carried forward from prior pass, re-verified) | `agent/src/pr-state-reconciler.unit.test.ts:2515,2578,2626,2668` — regression case plus 3 adjacent guard cases, unchanged this cycle. |
| 10 | `browser.ts` / `verify-browser-launch.ts` integration + CI-step coverage | Resolved (carried forward from prior pass, re-verified) | `agent/src/browser.integration.test.ts` (3 cases) + `agent/scripts/verify-browser-launch.ts` wired into `.github/workflows/ci.yml`'s `agent-docker-build` job — unchanged this cycle. |
| 11 | Journey #5 (local HITL dev-loop bootstrap) has no full-sequence coverage shape | Still open — unit half done, integration half not started | `scripts/hitl.unit.test.ts` covers `computeMissingClones`/`computeProvisionPlan` (pure planning). No `scripts/hitl.ts` scope growth since 2026-07-31 (`git log dcec6196..origin/main -- scripts/hitl.ts` is empty) — item is unchanged, not stale. See Net-new below. |
| 12 | **New this cycle** — `pull-request-service.ts`'s `ciFailureSignature` streak-to-auto-block path (CSD-1.1) needs a dedicated integration-test sequence | **Real gap — already resolved** | `task-store/src/pull-request-service.unit.test.ts:139-287` (7 cases: omitted, matches-and-increments, differs-and-resets, no-prior-signature, crosses-threshold, below-threshold, reset-does-not-set-hitl) **plus** `task-store/src/pull-request.integration.test.ts:1552,1596,1622` (3 real-Postgres round-trip cases: 3x-match auto-blocks, differing signature resets the streak, omitted signature leaves fields untouched) — both the unit-level state-machine logic and the integration-level persistence are covered, landed in the same commit series as CSD-1.1's production fix. |
| 13 | **New this cycle** — ORF-1.x/2.x multi-repo/org filter extension needs integration coverage of `buildRepoOrgWhere`'s branch matrix at the service-layer boundary | **Real gap — already resolved** | `task-store/src/lib/repo-org-filter.unit.test.ts` (4 cases: pure-builder unit coverage) **plus** `task-store/src/pull-request.integration.test.ts:1009` (`"list() filters by repo array, org, and combined repo-list + org"` — real-Postgres AND-of-repo-in-and-org-OR case) **plus** `task-store/src/tasks.integration.test.ts:1018` (`"list({ repo: [...], org }) returns only rows intersecting both the repo list and the org"` — 5-case matrix covering in-both/in-repo-only/in-org-only/in-neither). Both service consumers of the shared helper have dedicated integration coverage of the combined-filter case. |
| 14 | **New this cycle** — `check-review.ts`'s requested-reviewer inclusion (RRR-1.1) needs two dedicated test cases (inclusion + allowlist-still-applies) | **Real gap — already resolved** | `agent/src/check-review.unit.test.ts:718-790` — both halves covered: inclusion case (self-authored PR *is* a candidate when currentUser is a requested reviewer) and 4 regression-guard cases proving the author-allowlist exclusion still applies unconditionally regardless of `reviewRequests` (including a self-authored PR with no `reviewRequests` at all, and one whose `reviewRequests` names someone else). |
| 15 | **New this cycle** — HSR-1.1's `20260731000000_split_hitl_blocked_signals` data migration (the raw-SQL `UPDATE` statements that convert pre-migration `hitl`/`hitlNotifiedAt` rows into the new `requiresHumanApproval`/`status='blocked'`/`PullRequest.blocked` shape) needs dedicated pre→post migration-transition coverage, not just post-migration unit coverage of the new field shapes | **Real gap — already resolved** | `task-store/src/hitl-blocked-migration.integration.test.ts` (new this cycle, DB-gated on `DATABASE_URL_SHIPWRIGHT_TASK_STORE_TEST`) — re-creates the dropped pre-migration columns via raw SQL, seeds pre-migration-shape rows, runs the exact three `UPDATE` statements from `migration.sql`, and asserts the resulting post-migration state via the Prisma client before dropping the temporary columns. This is the correct integration-layer treatment (real Postgres, real migration SQL) distinct from `blocked-by.unit.test.ts`'s pure-logic coverage of the new `BlockedByEntry` shape (item context: both layers needed, both present). |

**Net effect on this cycle's Net-new list:** no new gap this cycle. Items #8-#14 (all
carried forward from prior design passes, #12-#14 first flagged as new in the 2026-08-05
pass) remain resolved and unchanged. Item #15 (new this cycle) is also already resolved.
The only genuinely open item is #11 (HITL bootstrap integration harness), unchanged in
scope since 2026-07-31.

Also directly re-verified the source deltas from this and recent prior cycles (not
flagged as a Phase 2 gap, since each row was already fully integration-tested before its
respective change landed). This cycle's real deltas (HSR-1.1 completion, RCG-1.2,
STD-1.1/1.2/1.4, new `scripts/minikube.ts`) are listed first; the rest are carried
forward from prior cycles, unchanged:

- **HSR-1.1 completion** (`task-store/src/blocked-by.ts`'s `BlockedByEntry` union split
  into independent `hitl`/`blocked` gates, plus the `PrRecord.hitl`→`blocked` rename
  across `agent/src/check-deploy.ts`, `check-patch.ts`, `check-review.ts`,
  `check-helpers.ts`, `admin/src/admin-ui-pages.ts`) — `task-store/src/blocked-by.unit.test.ts`
  covers both gates independently and their accumulation (a `status:"blocked"` task with
  its own `hitl:true` gate reports both entries, not just one); `agent/src/check-deploy.unit.test.ts`,
  `check-patch.unit.test.ts`, `check-review.unit.test.ts`, `check-helpers.unit.test.ts`,
  and `admin/src/admin-ui-pages.unit.test.ts` all updated their fixtures/assertions from
  `hitl` to `blocked` in lockstep (confirmed via `grep -n "blocked" <each file>` showing
  matching field/badge-class assertions) — no test still asserts on the old `hitl` field
  name in a way that would mask a regression. See gap #15 above for the dedicated
  data-migration coverage.
- **`task-store/src/ready.ts`'s same-branch exclusivity guard** (RCG-1.2/HSR-1.1) —
  `task-store/src/ready.unit.test.ts`'s `describe("same-branch exclusivity")` block
  covers: exclusion via a fresh `heartbeatAt`-based sibling, exclusion via a fresh
  `claimedAt`-only sibling (no `heartbeatAt`), non-exclusion when the sibling's claim is
  stale (older than `DEFAULT_CLAIM_TTL_MS`), non-exclusion when the sibling is on a
  different branch, and a self-referential edge case (a task isn't excluded by itself
  appearing in the candidate list). All 5 cases landed in the same commit as the
  production change.
- **`agent/src/loop-orchestrator.ts`'s skip-reason-taxonomy generalization** (STD-1.1/1.2/1.4)
  — `agent/src/loop-orchestrator.unit.test.ts` covers: the retagged
  `dev-task:deferred:same-branch-sibling-busy:*` marker (still exempted, now via the
  generic `deferred`-category match rather than the old exact-prefix match), the
  `review:deferred:unresolved-human-feedback:*` marker (STD-1.4, exempted via the same
  generic check with no dedicated handling needed), a generic
  `command:deferred:some-reason` marker (STD-1.1, proves the taxonomy match isn't
  hardcoded to specific commands), and a negative case
  (`command:not-deferred:some-reason` — category segment must be exactly `deferred`, not
  just present, still calls `recordSkip`). Also covers RCG-1.2's clean-409 exclusion
  (`describe`/`test` block at line 2959, "Pre-claim clean-409 exclusion").
- **New `scripts/minikube.ts`** — `scripts/minikube.unit.test.ts` (22 test cases)
  covers the pure `buildMinikubeCommands`/`buildTeardownCommands` builders with no
  Docker/VM/network, same design and coverage shape as the existing
  `scripts/dev-tmux.unit.test.ts` it mirrors.
- **RVG-1.1** (`agent/src/check-review.ts`'s author-reply candidacy retrigger at an
  unchanged commit) — `agent/src/check-review.unit.test.ts:973-1330` covers the full
  matrix: no new comment since `reviewedAt` → still skipped (regression case), a fresh
  author comment after `reviewedAt` → re-admitted as a candidate, a later review
  advancing `reviewedAt` past the reply → does not keep re-triggering on the same
  comment, and a missing/null `record.reviewedAt` watermark → any author comment counts.
  All four cases landed in the same commit as the production change.
- **RFR-1.1** (`agent/src/check-review.ts`'s fresh-author-reply exception, now hoisted
  so it's shared by both the RVD-1.1 live-review dedup check and the RVG-1.1
  terminal-state check) — `agent/src/check-review.unit.test.ts:973-1078` covers both the
  positive case (a clean/no-finding COMMENT review at head, plus a fresh author reply
  after `reviewedAt`, stays a candidate — the PR #2456 trigger shape, where
  `classifyReviewState()` returns non-null and previously hit RVD-1.1's unconditional
  `continue` before ever reaching RVG-1.1) and the regression case (same shape but no
  fresh reply — still skipped). Both cases landed in the same commit as the production
  change. No new gap.
- **BBE-1.2** (`agent/src/loop-orchestrator.ts`'s `SKIP_BLOCK_THRESHOLD` exemption for
  `dev-task:same-branch-sibling-busy:*`) — `agent/src/loop-orchestrator.unit.test.ts:1738-1790`
  covers both the exemption itself (a matching skip-reason calls `skipRun` but not
  `recordSkip`) and the scoping guard (a differently-prefixed skip reason, e.g.
  `deploy:bundle-incomplete:*`, still calls `recordSkip` — the exemption doesn't
  over-broadly swallow unrelated skip reasons). The companion `dev-task.md` marker-emission
  change is covered by `plugins/shipwright/commands/dev-task.content.test.ts`'s new
  assertions.
- **CSI session-id threading** (`claude.ts` → `cron-handler.ts`/`loop-orchestrator.ts`/
  `cron-run-reporter.ts` → `admin/src/agent-cron-runs.ts`) — matching test coverage
  confirmed in `agent/src/claude.unit.test.ts`, `agent/src/cron-handler.integration.test.ts`,
  `agent/src/loop-orchestrator.{unit,integration}.test.ts`, and
  `admin/src/agent-cron-runs.integration.test.ts`.
- **RUC-1.1** (`review.md`'s Step 5.5 GraphQL `reviewThreads` fetch + unaddressed-findings
  hard gate) — `plugins/shipwright/commands/review.content.test.ts:499-664` covers both the
  GraphQL query shape and all 5 gate-behavior cases (forces COMMENT, permits APPROVE when
  clean, etc.).
- **ORF-2.x admin UI** (`admin-ui-pages.ts`'s `renderRepoOrgFilterFields`/`toFilterArray`,
  `admin-ui.ts`'s `c.req.queries("repo")` multiselect wiring) — covered at both unit
  (`admin/src/admin-ui-pages.unit.test.ts:2148,3921` — rendering/selection/XSS-escaping
  logic) and smoke (`admin/src/admin-ui.smoke.test.ts:4059,5486` — repeated-query-param
  round trip) layers.
- **New pure helper** `task-store/src/lib/repo-org-filter.ts` (`buildRepoOrgWhere`) — has
  its own dedicated `repo-org-filter.unit.test.ts`, correctly unit-layered.
- **New/renamed marketing pages** (`service-architecture.astro`, `architecture.astro` →
  `agent-model.astro`) — each has a matching Playwright spec
  (`service-architecture.spec.ts` new, `agent-model.spec.ts` renamed in place).
- **`plugins/shipwright/references/user-guide.md`** (new content-layer doc) — no dedicated
  content test, consistent with the established convention: 0 of 10 files under
  `plugins/shipwright/references/` have a per-file content test (unlike `commands/`/
  `skills/`, which do). Not a gap.
- **`investigate-cron` SKILL.md Step 1c** (CSI-3.1/4.1, direct sessionId-based transcript
  lookup) — `plugins/shipwright/skills/investigate-cron/SKILL.content.test.ts:402-426`
  covers the direct-lookup path and its fallback gating.

## Summary

| Bucket | Test count | Infra count | Effort |
|---|---|---|---|
| Reuse as-is | ~287 | 7 | — |
| Promote / deepen | 0 | 0 | — |
| Rebuild | 0 | 0 | — |
| Delete (redundant) | 0 | 0 | — |
| Net-new | 2 | 0 | trivial-to-small (both are scoping/case additions, not large new files) |

**Total existing test files discovered:** 123 unit + 61 integration + 43 smoke + 36
content = 263 `bun test`-scanned files, + 21 `site/tests/*.spec.ts` + 3 Playwright
`*.e2e.ts` (2 `admin/e2e/`, 1 `metrics/e2e/`) = **287 test files total** — +3 from the
2026-08-06 pass (`scripts/minikube.unit.test.ts`,
`task-store/src/hitl-blocked-migration.integration.test.ts`,
`plugins/shipwright/commands/plan-session.content.test.ts`, all new files backing new
production code, correctly landed alongside their production changes — not new test
*cases* on existing files this time). The 3 new files fold into "Reuse as-is" (they're
already-correct, already-passing coverage for this cycle's delta, not open Net-new
work items) — the Net-new bucket's count of 2 is unchanged from the prior pass and
refers only to the still-open items carried forward below (HITL bootstrap integration
harness + `admin/src/clock.ts`).

**Headline finding:** every real source change in this cycle's 50-commit / 97-file
Phase 1 delta (13 non-doc/non-version-bump/non-chart source files: HSR-1.1 completion
across 5 files, `ready.ts`'s same-branch guard, `loop-orchestrator.ts`'s taxonomy
generalization, and new `scripts/minikube.ts`) already has matching test coverage at the
correct layer, landed in the same commits as the production changes — continuing the
pattern observed in every prior pass. One new gap was flagged this cycle (#15, the
HSR-1.1 data migration's pre→post transition coverage) and found already resolved by a
dedicated new integration test file. No new isolation-contract regression, no new
redundant-coverage pattern, no rebuild candidates, no trim/delete candidates. The only
open item remains the same one carried forward across the last several cycles: the HITL
bootstrap's full integration-harness half (the pure-planning half is already done), plus
the same long-standing low-priority `admin/src/clock.ts` carry-forward.

## Isolation contract audit

Grepped the full current suite (287 files) for the three hard-rule signals from root
`CLAUDE.md`'s Test isolation section and `references/principles.md`'s `t1_no_global_mocking`:

| Signal | Files matched | Real violations |
|---|---|---|
| `mock.module(` | 35 files / 37 matches, all in header-comments (`grep -rn "mock\.module(" --include="*.test.ts"`, e.g. `agent/src/cron-handler.integration.test.ts:7` — `"...mock.module(), no real Slack/Claude calls. Mirrors..."`) | **0** — every match manually confirmed to start with `*` or `//` (a comment line), not an executable call |
| `global.fetch = ` (assignment) | 0 | **0** |
| `global.console = ` (assignment) | 0 | **0** |
| `DATABASE_URL` inside `*.unit.test.ts` | 3 (same three as every prior pass: `admin/src/main.unit.test.ts`, `admin/src/schema.unit.test.ts`, `scripts/dev-tmux.unit.test.ts`) | **0** — unchanged from prior findings; all assert on the env-var name as a string literal or an early-return guard, none opens a real connection |

No isolation-contract regression found since the 2026-07-31 pass. (Note: this pass ran
the broad `--include="*.test.ts"` sweep directly rather than trusting the prior doc's
narrower-count snapshot — 35 files / 37 matches this cycle vs. the prior doc's stated "1
match, narrower grep" and an earlier pass's "17 files, broader sweep." All three sweeps
agree on the substantive finding of zero executable violations; the file-count swings
are grep-scope/glob differences between cycles, not a change in repo state. This cycle's
count (35/37) was independently verified line-by-line, not carried forward from either
prior number.)

## Delete (redundant assertion) analysis

**Zero delete/trim candidates identified**, same finding as every prior pass. No new
duplicate-assertion pattern was introduced by this cycle's delta. This cycle's new
`blocked-by.unit.test.ts` gate-accumulation cases, `ready.unit.test.ts`'s same-branch
exclusivity cases, `loop-orchestrator.unit.test.ts`'s taxonomy-generalization cases, and
the new `hitl-blocked-migration.integration.test.ts`/`minikube.unit.test.ts` files each
assert distinct properties not covered anywhere else, and are correctly non-duplicative
per layer (unit proves pure logic/state machines in isolation; the new integration file
proves the real-Postgres migration-SQL transition the unit tests cannot). Prior cycles'
`ciFailureSignature` cases in `pull-request-service.unit.test.ts` +
`pull-request.integration.test.ts`, the combined-filter cases in
`pull-request.integration.test.ts` + `tasks.integration.test.ts`, and the
requested-reviewer cases in `check-review.unit.test.ts` remain correctly non-duplicative,
unchanged.

## Tests — bucketed

### Reuse as-is

The large majority of the suite, unchanged in shape from the 2026-07-31 pass aside from
the narrow delta below. Grouped by cluster; each cluster was checked against Phase 1's
delta list, not re-read in full where unchanged.

| Cluster | Test files | Layer(s) | Functional unit | Notes |
|---|---|---|---|---|
| `lib/` | `pricing`, `org-repo`, `request-context`, `sentry`, `web/toolbar`, `claim-ttl`, `github-login`, `task-store-types` (`.unit.test.ts`) | unit | Cross-service pure helpers | Unchanged this cycle. |
| `metrics/src/*` | ~9 top-level + ~7 `lib/` + 2 `providers/` + 1 `dashboard/` | unit/integration/smoke | Formatters, provider selection, HTTP clients, `/metrics/*` routes, request coalescing | Unchanged this cycle. |
| `agent/src/*` | ~49 files | unit/integration/smoke | Work selection, check-\* precondition scripts, Slack Bolt handlers, `claude.ts` spawner, chat/task-store/GitHub clients, chat-poller loop, author-allowlist ref, headless-browser launcher | This cycle: `check-deploy.ts`/`check-patch.ts`/`check-review.ts`/`check-helpers.ts`'s `hitl`→`blocked` field rename (HSR-1.1) and `check-review.ts`'s test-only `workspacePath` injection seam, plus `loop-orchestrator.ts`'s skip-reason-taxonomy generalization (STD-1.1/1.2/1.4) and clean-409 re-selection exclusion (RCG-1.2) — all landed with complete coverage, see "verified this cycle's actual source delta" above. Prior cycles: RFR-1.1, RVG-1.1, BBE-1.2, RRR-1.1, CSI-1.1/1.2, unchanged this cycle beyond the rename. |
| `admin/src/*` | ~56 files | unit/integration/smoke | Agent/Env/Cron/Tool/Token CRUD, K8s client, provisioning clients, admin UI, chat-token daily rollup, work-queue snapshots, server composition, agent-type manifest resolution | This cycle: `admin-ui-pages.ts`'s `PrListItem`/badge rendering `hitl`→`blocked` rename (HSR-1.1), covered by updated `admin-ui-pages.unit.test.ts` assertions. Prior cycle's ORF-2.x multiselect UI extension and CSI sessionId passthrough unchanged. |
| `task-store/src/*` | ~26 files (was ~25, +`hitl-blocked-migration.integration.test.ts`) | unit/integration/smoke | Task/PR/Token services, dependency-resolution rules, routes | This cycle: `blocked-by.ts`'s gate-split (HSR-1.1, `blocked-by.unit.test.ts`), `ready.ts`'s same-branch exclusivity guard (RCG-1.2, `ready.unit.test.ts`), `task-service.ts`'s `recordSkip()` now sets `status:'blocked'` instead of `hitl:true` (covered at the integration layer — `skip-tracking.integration.test.ts`'s real-Postgres round-trip, correctly not unit-tested since it's a persistence-layer property) plus a new `requiresHumanApproval` filter (`openapi-schemas.unit.test.ts` + `tasks.openapi.smoke.test.ts`), `pull-request-service.ts`'s `isPrBlocked` dead-code removal and `claim()`'s `claimedBy !== null` 409-guard precondition (`pull-request-service.unit.test.ts`, `pull-request.integration.test.ts`), `routes/tasks.ts`'s `requiresHumanApproval` query param + `stripRemovedFields` guard (`tasks.openapi.smoke.test.ts`, 19 assertions), `routes/prs.ts`'s `PATCH_ALLOWED_FIELDS` rename (`prs.smoke.test.ts`, `prs.openapi.smoke.test.ts`) — all HSR-1.1/RCG-1.2, all with matching coverage confirmed at the correct layer — plus the new dedicated `hitl-blocked-migration.integration.test.ts` covering the pre→post data migration itself (gap #15, already resolved). Prior cycle's CSD-1.1/ORF-1.x unchanged. |
| `chat/src/*` | 10 files | unit/integration/smoke | Message/thread/token services, routes, OpenAPI schemas, spec generator | Unchanged this cycle. |
| `mcp-server/src/*` | 8 files | unit/integration/smoke | Tool allowlist, tool-caller proxy, MCP wire protocol, inbound bearer auth, Streamable HTTP transport + idle-session reaper, app-factory composition, fail-closed entrypoint guard | Unchanged this cycle — no files in Phase 1's delta touch `mcp-server/`. |
| `brand/*` | 2 files | unit | Hex-color lint, CSS token generation | Unchanged. |
| `scripts/*` | 17 files (was 16, +`minikube.unit.test.ts`) | unit/integration/smoke | Coverage gate, config-docs checker, dev supervisors, seed scripts, version sync, agent-provisioning poll loop, Minikube stack orchestration (new) | New this cycle: `scripts/minikube.ts` + `minikube.unit.test.ts` (22 cases), mirrors the existing `dev-tmux.ts` pure-builder pattern. `scripts/hitl.ts`/`hitl.unit.test.ts` unchanged this cycle (`git log 71b6024e..origin/main -- scripts/hitl.ts` empty) — see item #11 in gaps table for what remains open. |
| `plugins/shipwright/scripts/*` | 15 files | unit | `check-*.ts` cron prechecks, `classify_test_layer.ts`, `check-helpers.ts` | Unchanged this cycle. |
| `plugins/shipwright/{commands,agents,skills,references,test}/*` | 36 dedicated `*.content.test.ts` files (was 35, +`plan-session.content.test.ts`) | content/unit/integration | Command/skill/agent markdown prose assertions | New this cycle: `plan-session.content.test.ts` (159 lines, new assertions for `plan-session.md`'s 3-way HITL classification content changes) — existing command file, not a new command (still 29). `deploy.content.test.ts`, `dev-task.content.test.ts`, `patch.content.test.ts`, `review.content.test.ts` all extended for their respective `.md` content changes this cycle (hitl→blocked/`requiresHumanApproval` migration language, skip-reason taxonomy). Prior cycles' `review.content.test.ts`/`patch.content.test.ts`/`investigate-cron/SKILL.content.test.ts` extensions unchanged. `references/user-guide.md` still has no dedicated content test — consistent with the established 0-of-10 convention for `references/` files, not a gap. |
| `site/tests/*.spec.ts` | 21 files (was 20) | e2e | Marketing pages, incl. 11 `docs/*` page specs | `service-architecture.spec.ts` new this cycle, paired with the new `service-architecture.astro` page. `architecture.spec.ts` renamed to `agent-model.spec.ts` in place, paired with the renamed `agent-model.astro` — same coverage, new name. |
| `admin/e2e/*.e2e.ts`, `metrics/e2e/*.e2e.ts` | 3 files | e2e | Admin login/agents pages, metrics dashboard | Unchanged this cycle. |

### Promote / deepen

None. No promote-shaped gap was found this pass.

### Rebuild

None. No test file's layer, framework, or fundamentals were found wrong this pass.

### Delete (redundant)

None. See Delete (redundant assertion) analysis above.

### Net-new

Sorted by criticality. No new net-new items identified this cycle — all three real gaps
this cycle's Phase 2 flagged (items #12-#14) were already resolved by the time this phase
ran. The same two items carried forward from prior cycles remain open:

| Inventory item | Prescribed layer | Criticality | Estimated effort |
|---|---|---|---|
| **Local HITL dev-loop bootstrap** (journey #5) — the full `SHIPWRIGHT_HITL_REPOS` clone → `agent-policy.md` seed → task-store/admin boot → poll → dispatch sequence has no integration-level test exercising the sequence end-to-end with an injected `gh` exec fake, even though the pure planning helpers (`computeMissingClones`, `computeProvisionPlan`) are already fully unit-tested | integration-composed (per Phase 2's Test-data strategy — no browser, single-process orchestration with an injected exec fake) | medium (developer-experience path, not customer-facing, but blocks the team's own local dev-loop if it regresses) | small — the hardest planning logic is already isolated and tested; the remaining work is wiring an injectable `gh`/process-exec seam into `scripts/hitl.ts`'s orchestration function (if not already present) and one sequencing test |
| `admin/src/clock.ts` (`Clock`/`SystemClock`/`FixedClock` implementation for admin) — carried forward, unrelated to this cycle's Phase 1 delta | unit | low | trivial, optional — carried forward unchanged from every prior pass. All 6 sibling `Clock` files across the repo lack a dedicated test file too; near-zero logic (two one-line methods), proven correct via consumers per the repo's established convention. |

## Infrastructure — bucketed

### Reuse as-is

- **Bun test runner config** (`bunfig.toml`'s `pathIgnorePatterns`) — re-verified this
  pass: `["**/site/**", "**/metrics/e2e/**", "**/admin/e2e/**", "**/*.canary.test.ts"]`,
  unchanged and correct.
- **CI workflow shape** (`.github/workflows/ci.yml`) — matches Phase 2's documented
  shape; unchanged this cycle.
- **Postgres service container for admin/task-store/chat** — real DB per test run, all
  three `DATABASE_URL_SHIPWRIGHT_*` vars wired in `ci.yml`.
- **`Clock` injection helpers** — seven implementations across the repo plus
  `plugins/shipwright/scripts/test-helpers/doubles.ts` — still the strongest, most
  consistently-applied convention in the repo. No new consumer landed this cycle that
  deviates from the pattern.
- **Recorded-fixture doubles** — `RecordedKubernetesClient`,
  `metrics/src/providers/task-store-recorded.ts`,
  `metrics/src/fixtures/task-store-fixtures.ts`, `agent/src/shipwright-config-client.ts` —
  unchanged this cycle; pattern is stable and extending cleanly to new clients.
- **`scripts/check-coverage.ts`** — the 80/80 lcov-threshold gate, unchanged.
- **`.github/PULL_REQUEST_TEMPLATE.md`** — confirmed present directly, matches
  `test-system.md`'s Repo configuration section's note that this is resolved, not a gap.
- **`docs/test-readiness/speed-baseline.md`** — last independently refreshed 2026-07-28
  (commit `1406a4dc`); not re-refreshed this cycle but its numbers (258 files, 5,762
  tests, 2m01s) remain directionally consistent with this pass's live CI pull (260-261
  files, 6,041-6,042 tests, 2m17s-2m24s) — steady growth, no budget concern.

### Promote / deepen

None. The design-level backlog item (extending `Recorded*Client` fixture-double coverage
to the remaining `Http*Client` modules named in `test-system.md`'s Local execution
architecture table) remains a Phase 2 design-level item, not a migration-bucket verdict on
any existing infra file — carried in `test-system.md`'s gaps summary, not restated here.

### Rebuild

None.

### Delete (redundant)

None.

### Net-new

None. No new infrastructure gap identified this cycle. The new
`task-store/src/lib/repo-org-filter.ts` pure helper required no new shared
fixture/helper machinery — it's a plain unit-testable function with its own dedicated
unit test file, no infra needed.

## Risk callouts

No test currently passing (green) received a `delete` or `rebuild` verdict this pass, so
the SKILL's mandatory risk-callout requirement has no entries to populate — same as every
prior pass in this repo's history.

## Speed measurement

### Tier 1 — aggregate (measured this pass)

- **Run:** [31294850463](https://github.com/app-vitals/shipwright/actions/runs/31294850463)
  (CI on commit `a9f2a534`, this cycle's exact HEAD), 2026-08-09T04:34:24Z. This run was
  fully green — no failing jobs (unlike some prior cycles' unrelated Docker-layer
  transients).
- **Job wall-clock:** 2m25s (`lint / typecheck / test`, 04:34:39Z–04:37:04Z), of which the
  `bun test` step itself ran 40.47s
- **Tests:** 6,177 tests pass, 0 fail, across 264 files — per the runner's own summary
  line ("Ran 6178 tests across 264 files.")
- **vs. budget:** 2m25s against the <15 min Full-PR-pipeline budget — **~16% of budget,
  well within**, consistent with every prior cycle's ~13-17% readings. Not trending
  toward the escalation trigger. The escalation formula (aggregate wall-clock >7.5 min,
  sustained across 2 consecutive measurements) is nowhere near tripped.

Test-count growth trajectory: 5,762 (2026-07-28 `speed-baseline.md`) → 5,880 (2026-07-30)
→ 6,041-6,042 (2026-08-01) → 6,070 (2026-08-05) → 6,071 (2026-08-06) → 6,177 (2026-08-09,
this pass) — steady, expected growth (+106 tests this cycle), consistent with this
cycle's 3-new-file delta (`minikube.unit.test.ts`'s 22 cases,
`hitl-blocked-migration.integration.test.ts`'s cases, `plan-session.content.test.ts`'s
cases) plus incremental cases added to existing files for HSR-1.1/RCG-1.2/STD-1.x.

### Tier 2 — per-layer breakdown (conditional)

- **Tier 2 triggered:** no — Tier 1's aggregate is at ~16% of the <15 min budget this
  pass, far below the 50% escalation threshold.

Tier 2 not triggered — suite is comfortably within the Tier 1 aggregate budget. Per the
SKILL's explicit guidance, this is a valid, complete outcome, not a gap requiring
escalation. No per-layer numbers fabricated.

## Next step

Run `/test-roadmap` to synthesize this migration audit with the Phase 1 inventory and
Phase 2 blueprint into the executable roadmap. Candidate Phase 4 inputs from this
document:

1. **1 real net-new item** (small effort, unchanged since 2026-07-31): the HITL local
   dev-loop bootstrap (journey #5) needs an integration-level sequencing test with an
   injected `gh` exec fake; the pure planning logic (`computeMissingClones`,
   `computeProvisionPlan`) is already fully unit-tested.
2. **1 low-priority/optional carried-forward item** (`admin/src/clock.ts` — still a
   non-gap by the repo's own convention).
3. **1 real gap flagged and confirmed already resolved this cycle** (#15 — HSR-1.1's
   `20260731000000_split_hitl_blocked_signals` data migration's pre→post transition
   coverage) — landed via a new dedicated `hitl-blocked-migration.integration.test.ts`
   in the same commit series as the schema/field-rename change.
4. **8 carried-forward-from-prior-cycle gaps, all reconfirmed already resolved**
   (`reviewedCommitSha` divergent-value case, RDG-1.1 bundle-mate regression,
   `browser.ts`/`verify-browser-launch.ts` coverage, `ciFailureSignature` streak
   auto-block, ORF-1.x/2.x combined-filter integration coverage, RRR-1.1
   requested-reviewer inclusion, RVG-1.1 author-reply candidacy retrigger, RFR-1.1
   fresh-author-reply exception hoist) — unchanged since 2026-08-06.
5. **This cycle's own real source delta (HSR-1.1 completion, RCG-1.2, STD-1.1/1.2/1.4,
   new `scripts/minikube.ts`), all reconfirmed already resolved** — every touched row has
   matching test coverage landed in the same commits as the production changes, per
   "verified this cycle's actual source delta" above.
6. **Speed measurement re-confirmed in budget** — Tier 1 aggregate ~16% of budget on a
   fresh data point at this cycle's exact HEAD, consistent with the growth trajectory. No
   M1 task needed.
6. **0 net-new infra items, 0 rebuild candidates, 0 delete/trim candidates, 0
   isolation-contract regressions** — the steady-state finding holds for another cycle.
