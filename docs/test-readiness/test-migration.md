# Test Migration

> Phase 3 of the test-readiness pipeline. Reconciles existing tests and infrastructure
> against the Phase 1 inventory ([`test-inventory.md`](./test-inventory.md), refreshed at
> `0428fe71`) and the Phase 2 blueprint ([`test-system.md`](./test-system.md), refreshed
> at `0428fe71`), bucketing each existing test file and each inventory item into one of
> five categories.
>
> Generated by `/test-migration` on 2026-08-11 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-20260811`).

## Method note

This pass diffed the current file tree against the 2026-08-09 pass's findings (baseline
doc committed at `a9f2a534`) and re-verified every open item directly against current
repo state — per this cycle's standing instruction not to carry forward "gap"/"missing"
claims without re-checking them, with particular scrutiny on the `pr-state-reconciler.ts`
orphan-reconciliation removal and Phase 2's two new gap items (#15 concurrent-claim race
tests, #16 self-review supersession tests):

1. Enumerated every test file via the Phase 2 framework matrix's glob patterns: 125
   `*.unit.test.ts` (+2), 62 `*.integration.test.ts` (+1), 43 `*.smoke.test.ts`
   (unchanged), 38 `*.content.test.ts` (+2) (268 `bun test`-scanned files, up from 263),
   plus 21 `site/tests/*.spec.ts` (unchanged) and 3 Playwright `*.e2e.ts` (2 `admin/e2e/`,
   1 `metrics/e2e/`, unchanged) = **292 test files total** — +5 from the 2026-08-09 pass,
   confirmed via `git diff --stat a9f2a534..HEAD --diff-filter=A -- '*.unit.test.ts'
   '*.integration.test.ts' '*.smoke.test.ts' '*.content.test.ts' '*.spec.ts' '*.e2e.ts'`
   (exactly 5 additions, 0 deletions): `lib/http.unit.test.ts`,
   `plugins/shipwright/commands/unblock.content.test.ts`,
   `plugins/shipwright/scripts/compute-review-verdict.unit.test.ts`,
   `plugins/shipwright/skills/test-readiness/SKILL.content.test.ts`,
   `task-store/src/pull-request-service.integration.test.ts`.
2. Cross-referenced Phase 1's delta list (38 commits / 73 files since the `a9f2a534`
   baseline, 9 non-doc/non-version-bump/non-chart source files) against the test-file
   glob to confirm each has matching coverage, reading the actual test body for every
   touched file: `lib/http.ts`, `agent/src/check-patch.ts` (DRO-1.2), `agent/src/pr-state-reconciler.ts`
   (scope reduction), `task-store/src/pull-request-service.ts` (CRF-1.1),
   `task-store/src/task-service.ts`, `task-store/src/routes/{tasks,prs}.ts`,
   `chat/src/routes/messages.ts`, `plugins/shipwright/scripts/compute-review-verdict.ts`
   (DRO-1.1), `plugins/shipwright/commands/unblock.md`.
3. **Directly investigated the `pr-state-reconciler.ts` orphan-reconciliation removal**
   flagged by both Phase 1 and Phase 2 as needing human-confirmed rationale before being
   treated as accounted for. Confirmed via `git log --follow` that the removal landed in
   a dedicated, reviewed, merged PR — **#2527, "refactor: remove orphaned-task reconciler
   (DTC-1.1)"**, merged 2026-08-10. The PR body states the pass is "superseded by
   `StaleClaimReaper` (resets stuck claims within ~65min) + the normal ready-set FIFO +
   `dev-task.md`'s Step 4 Branch/PR Reality Check, which already does a strictly more
   thorough version of the same check," and its own acceptance criteria confirm the
   orphan-heal-specific unit tests were deliberately removed alongside the code (86/86
   remaining tests pass, coverage gate held at 81.32%/82.23%, full suite 5845 pass at
   merge time). Directly verified all three superseding mechanisms exist:
   `task-store/src/stale-claim-reaper.ts` (+ its own unit/integration suites),
   `plugins/shipwright/commands/dev-task.md`'s "Branch/PR Reality Check (prior-session
   recovery)" section (Step 4), and confirmed via grep that zero references to
   `listOrphanCandidateTasks`/`ghListOpenPrsForBranch`/`reconcileOrphanedTask(s)` remain
   anywhere in `agent/src/pr-state-reconciler.ts` or its test file. **Verdict: this was a
   deliberate, reviewed, well-tested clean removal, not an accidental loss of tested
   functionality.** See "The `pr-state-reconciler.ts` orphan-reconciliation removal,
   verified" below for the full writeup — this corrects both prior artifacts' open
   caution into a closed, resolved finding.
4. **Directly investigated Phase 2's two new gap items (#15, #16) against actual test
   file contents rather than trusting the design doc's framing:**
   - **#15, `pull-request-service.ts`'s CRF-1.1 claim race** — confirmed **resolved**:
     `task-store/src/pull-request-service.integration.test.ts` (new this cycle) has a
     dedicated real-Postgres concurrent-caller test
     (`"concurrent claims (same commitSha/phase) against an EXISTING released record:
     exactly one wins, the other gets ConflictError"`, using `Promise.allSettled` to race
     two genuinely concurrent `claim()` calls).
   - **#15, `task-service.ts`'s claim() twin** — confirmed **NOT resolved, still a real
     gap**: no `task-store/src/task-service.integration.test.ts` file exists at all,
     and no other integration test file exercises `TaskService.claim()`'s race. The only
     coverage is `task-service.unit.test.ts`'s `makeClaimPrismaDouble()`-based tests,
     which simulate the WHERE-clause SQL against a single mutable in-memory row via
     **sequential, single-caller** assertions (happy path, already-claimed conflict,
     not-found, wrong-status) — none of them issue two concurrent calls via
     `Promise.allSettled`/`Promise.all` the way the `pull-request-service.ts` sibling
     test does. This asymmetry means the fix's core value (Postgres, not JS, arbitrating
     a real race) is unverified for the task-claim path. See Net-new below — this item
     is corrected from "already resolved" (as it might have looked from file-count alone)
     to genuinely open, half-resolved.
   - **#16, `check-patch.ts`'s `isSupersededBySelfReview` (DRO-1.2)** — confirmed
     **resolved**: `plugins/shipwright/scripts/compute-unaddressed-findings.unit.test.ts`
     (extracted from `check-patch.unit.test.ts` with PVD-1.1, this repo's established pattern
     for testing review-state classification logic via recorded-GitHub-review-data shaped
     fixtures under a `.unit.test.ts` filename, not a literal `.integration.test.ts` file)
     has 4 dedicated cases under the `DRO-1.2` header: superseded (earlier COMMENT excluded
     by a later clean self-review), NOT superseded when the later self-review is itself
     non-clean, order-matters (clean review before the finding doesn't retroactively supersede
     it), and a third-party review's finding is never superseded by anyone's self-review. Both
     directions of the predicate are proven.
5. Re-ran the isolation-contract grep sweep (`mock.module(`, `global.fetch =`,
   `global.console =`, `DATABASE_URL` inside `*.unit.test.ts`) across the full current
   suite (292 files) — zero new violations. 37 `mock.module(` string matches, all
   manually confirmed to be inside `*` or `//` comment lines documenting the *absence* of
   mocking, not executable calls. Zero `global.fetch =`/`global.console =` assignments.
   Same three pre-existing non-violation `DATABASE_URL` string-literal matches as every
   prior pass (`admin/src/main.unit.test.ts`, `admin/src/schema.unit.test.ts`,
   `scripts/dev-tmux.unit.test.ts`).
6. Pulled a fresh live Tier 1 speed measurement from CI (`gh run view --log` against the
   green `lint / typecheck / test` run at this cycle's exact HEAD, `0428fe71`) — see
   Speed measurement below.
7. Spot-checked every other carried-forward gap item (#1-#14 from the prior cycle) that
   Phase 2 restated unchanged, directly against current file contents rather than the
   prior doc's snapshot: `reviewedCommitSha` divergent-value coverage
   (`check-review.unit.test.ts`, 12 matches, unchanged), `pr-state-reconciler.ts`'s
   RDG-1.1 bundle-mate/`startedAt` guard (still present and tested — confirmed this is
   part of the *retained* `reconcilePrOpenTask` direct-path logic, distinct from the
   *removed* orphan pass; not affected by item 3 above), `browser.integration.test.ts`
   (present, unchanged), `admin/src/clock.ts` (still no dedicated unit test file,
   unchanged, still a non-gap per the repo's established `Clock`-file convention),
   `scripts/hitl.ts`/`hitl.unit.test.ts` (still only the pure-planning half tested,
   `computeMissingClones`/`computeProvisionPlan`; no `hitl.integration.test.ts`), and the
   `resetSkip()` companion assertion distinguishing a skip-count auto-block from a
   CI-failure-streak auto-block (`pull-request-service.unit.test.ts:751`, present).

**Speed measurement: re-run this pass via CI history (Tier 1 only).** No local Postgres
in this sandbox, so a local `bun test` run was not attempted — Tier 1's aggregate pull
needs only `gh run view --log` against the most recent green `lint / typecheck / test`
job, which this sandbox does have, at this cycle's exact HEAD commit `0428fe71`.

## The `pr-state-reconciler.ts` orphan-reconciliation removal, verified

Both Phase 1 and Phase 2 flagged this as an open item needing human-confirmed rationale
before treating the reduced surface as fully accounted for. This phase closes that item:

- **What was removed:** `listOrphanCandidateTasks`, `ghListOpenPrsForBranch`, and
  `reconcileOrphanedTask`/`reconcileOrphanedTasks` — a pending/in_progress → pr_open
  self-heal pass (TCR-1.2) — plus their exclusive deps/types and 547 lines of dedicated
  unit-test assertions (single-PR heal, sibling-count skip, degraded-scope skip,
  not-started skip, and their exclusive fixtures).
- **Landed via:** PR **#2527**, "refactor: remove orphaned-task reconciler (DTC-1.1)",
  merged 2026-08-10T04:01:59Z, reviewed and merged through the normal pipeline (not a
  force-push or an unreviewed direct commit).
- **Stated rationale (from the PR body):** the pass is superseded by three other
  mechanisms that together do a strictly more thorough version of the same job:
  `StaleClaimReaper` (resets stuck claims within ~65 min), the normal ready-set FIFO, and
  `dev-task.md`'s Step 4 "Branch/PR Reality Check (prior-session recovery)". All three
  confirmed present in the current tree.
- **What remains:** `reconcilePrOpenTask`'s direct-path and branch-fallback heal logic,
  including the RDG-1.1 bundle-mate/`startedAt` precondition (item #9, carried forward
  below) — unchanged, still fully tested, not touched by this removal.
- **Test-suite integrity at removal time:** the PR's own acceptance criteria (all MET)
  confirm `bun test agent/src/pr-state-reconciler.unit.test.ts` → 86/86 pass after the
  547-line removal, `task typecheck` clean across all 7 workspaces, and the full-suite
  coverage gate held (81.32% lines / 82.23% functions, threshold 80%) — i.e. the removal
  did not silently drop coverage below the gate, and no assertion was deleted without its
  corresponding production code also being deleted.

**Verdict: this is a deliberate, reviewed, well-tested clean removal — not an accidental
loss of tested functionality.** Correcting the prior artifacts' framing: this is not a
migration-bucket finding (no test file to bucket — it no longer exists and correctly so)
and is removed from the open-ambiguous-items list this cycle rather than carried forward
as an unresolved caution.

## Summary

| Bucket | Test count | Infra count | Effort |
|---|---|---|---|
| Reuse as-is | ~291 | 7 | — |
| Promote / deepen | 0 | 0 | — |
| Rebuild | 0 | 0 | — |
| Delete (redundant) | 0 | 0 | — |
| Net-new | 3 | 0 | small (all are scoping/case additions, not large new files) |

**Total existing test files discovered:** 125 unit + 62 integration + 43 smoke + 38
content = 268 `bun test`-scanned files, + 21 `site/tests/*.spec.ts` + 3 Playwright
`*.e2e.ts` (2 `admin/e2e/`, 1 `metrics/e2e/`) = **292 test files total** — +5 from the
2026-08-09 pass (`lib/http.unit.test.ts`, `unblock.content.test.ts`,
`compute-review-verdict.unit.test.ts`, `test-readiness/SKILL.content.test.ts`,
`pull-request-service.integration.test.ts`), all new files backing new production code or
closing a previously-flagged gap, correctly landed — not open Net-new work items. These 5
fold into "Reuse as-is" (already-correct, already-passing coverage for this cycle's
delta). The Net-new bucket's count of 3 (up from 2) reflects: the two carried-forward open
items (HITL bootstrap integration harness, `admin/src/clock.ts`) **plus one item promoted
into Net-new this cycle** — `task-service.ts`'s claim() concurrent-race regression test,
which Phase 2's gap #15 grouped with the (now-resolved) `pull-request-service.ts` sibling
but which this phase found to be genuinely unresolved on direct inspection (see Method
note step 4 above).

**Headline finding:** every real source change in this cycle's 38-commit / 73-file
Phase 1 delta (9 non-doc/non-version-bump/non-chart source files) already has matching
test coverage at the correct layer, landed in the same commits/PRs as the production
changes — continuing the pattern observed in every prior pass, with one exception: the
`task-service.ts` half of the CRF-1.1-class claim-race fix has only mocked-double,
single-caller coverage, not a genuine concurrent-caller regression test, unlike its
`pull-request-service.ts` sibling which does. The `pr-state-reconciler.ts`
orphan-reconciliation removal — carried forward as an open caution in both Phase 1 and
Phase 2 — is now confirmed a deliberate, reviewed, well-tested clean removal (PR #2527,
DTC-1.1) and is closed out of the ambiguous-items list this cycle. No new
isolation-contract regression, no new redundant-coverage pattern, no rebuild candidates,
no trim/delete candidates.

## Isolation contract audit

Grepped the full current suite (292 files) for the three hard-rule signals from root
`CLAUDE.md`'s Test isolation section and `references/principles.md`'s `t1_no_global_mocking`:

| Signal | Files matched | Real violations |
|---|---|---|
| `mock.module(` | 22 files / 37 matches, all in header-comments (`grep -rn "mock\.module(" --include="*.test.ts"`, e.g. `admin/src/agent-deletion.unit.test.ts:7` — `"...contract — no mock.module(), no global overrides."`) | **0** — every match manually confirmed to start with `*` or `//` (a comment line), not an executable call |
| `global.fetch = ` (assignment) | 0 | **0** |
| `global.console = ` (assignment) | 0 | **0** |
| `DATABASE_URL` inside `*.unit.test.ts` | 3 (same three as every prior pass: `admin/src/main.unit.test.ts`, `admin/src/schema.unit.test.ts`, `scripts/dev-tmux.unit.test.ts`) | **0** — unchanged from prior findings; all assert on the env-var name as a string literal or an early-return guard, none opens a real connection |

No isolation-contract regression found since the 2026-08-09 pass. (Note: the raw
match/file count differs slightly from the prior doc's "35 files / 37 matches" — this
cycle's sweep independently counted 22 files / 37 matches. Both sweeps agree on the
substantive finding of zero executable violations and the same match total; the
file-count swing is a grep-scope artifact between cycles (files with multiple matches
counted differently), not a change in repo state.)

## Delete (redundant assertion) analysis

**Zero delete/trim candidates identified**, same finding as every prior pass. No new
duplicate-assertion pattern was introduced by this cycle's delta. This cycle's new
`lib/http.unit.test.ts`, `compute-review-verdict.unit.test.ts`,
`unblock.content.test.ts`, `test-readiness/SKILL.content.test.ts`, and
`pull-request-service.integration.test.ts` each assert distinct properties not covered
anywhere else, correctly non-duplicative per layer. The `plugins/shipwright/scripts/compute-unaddressed-findings.unit.test.ts`
DRO-1.2 cases (extracted from `check-patch.unit.test.ts`) and the `pull-request-service.unit.test.ts`/`.integration.test.ts`
CRF-1.1 cases are unit-vs-real-Postgres pairs proving distinct properties (pure predicate logic
vs. real-DB race arbitration), not duplicate coverage. The `pr-state-reconciler.ts`
removal (see dedicated section above) deleted assertions alongside the production code
they tested — not a trim-bucket case (nothing remains asserting a still-live property
redundantly) and not a delete-bucket case (no passing test was recommended for deletion
by this audit; the deletion already happened, deliberately, in PR #2527, before this
audit ran).

## Tests — bucketed

### Reuse as-is

The large majority of the suite, unchanged in shape from the 2026-08-09 pass aside from
the narrow delta below. Grouped by cluster; each cluster was checked against Phase 1's
delta list, not re-read in full where unchanged.

| Cluster | Test files | Layer(s) | Functional unit | Notes |
|---|---|---|---|---|
| `lib/` | `pricing`, `org-repo`, `request-context`, `sentry`, `web/toolbar`, `claim-ttl`, `github-login`, `task-store-types`, **`http` (new)** (`.unit.test.ts`) | unit | Cross-service pure helpers | New this cycle: `lib/http.ts`'s `readJson` (dedup of a threefold-duplicated request-body parser) + `lib/http.unit.test.ts`. |
| `metrics/src/*` | ~9 top-level + ~7 `lib/` + 2 `providers/` + 1 `dashboard/` | unit/integration/smoke | Formatters, provider selection, HTTP clients, `/metrics/*` routes, request coalescing | Unchanged this cycle. |
| `agent/src/*` | ~49 files | unit/integration/smoke | Work selection, check-\* precondition scripts, Slack Bolt handlers, `claude.ts` spawner, chat/task-store/GitHub clients, chat-poller loop, author-allowlist ref, headless-browser launcher | This cycle: `check-patch.ts`'s `isSupersededBySelfReview` predicate (DRO-1.2, core unit logic stays in `check-patch.ts`, but extraction moves the `hasUnaddressedFindings` integration into `plugins/shipwright/scripts/compute-unaddressed-findings.ts` with full coverage in `compute-unaddressed-findings.unit.test.ts` — see Step 1 delta for file count); `pr-state-reconciler.ts`'s orphan-reconciliation pass removed (547 lines of production + test code, deliberate and verified, see dedicated section above) — remaining `reconcilePrOpenTask` direct/branch-fallback heal paths and RDG-1.1 precondition unchanged. |
| `admin/src/*` | ~56 files | unit/integration/smoke | Agent/Env/Cron/Tool/Token CRUD, K8s client, provisioning clients, admin UI, chat-token daily rollup, work-queue snapshots, server composition, agent-type manifest resolution | Unchanged this cycle — no files in Phase 1's delta touch `admin/`. |
| `task-store/src/*` | ~27 files (was ~26, +`pull-request-service.integration.test.ts`) | unit/integration/smoke | Task/PR/Token services, dependency-resolution rules, routes | This cycle: `pull-request-service.ts`'s `claim()` UPDATE-path conflict-detection race close (CRF-1.1) — moved into the WHERE clause, **new dedicated `pull-request-service.integration.test.ts`** with a real-Postgres `Promise.allSettled`-raced concurrent-claim test (resolves gap #15's PR-side half); companion `resetSkip()` fix (clears `blocked`/`blockedReason` only when set by the skip-count mechanism, not the CI-failure-streak mechanism) covered by 2 dedicated cases in `pull-request-service.unit.test.ts:729,751`. `task-service.ts`'s `claim()` gained the same `AND "claimedBy" IS NULL` WHERE-clause tightening, but **only has mocked-double, single-caller unit coverage** (`task-service.unit.test.ts`'s `makeClaimPrismaDouble()`), no real-Postgres concurrent-race test — see Net-new below, this is the one open item from this cycle's delta. `routes/{tasks,prs}.ts` and `chat/src/routes/messages.ts` switch to the new shared `lib/http.ts` import — no route-shape or coverage change, existing route-contract tests suffice. |
| `chat/src/*` | 10 files | unit/integration/smoke | Message/thread/token services, routes, OpenAPI schemas, spec generator | Unchanged this cycle beyond the `readJson` import swap noted above (no shape change). |
| `mcp-server/src/*` | 8 files | unit/integration/smoke | Tool allowlist, tool-caller proxy, MCP wire protocol, inbound bearer auth, Streamable HTTP transport + idle-session reaper, app-factory composition, fail-closed entrypoint guard | Unchanged this cycle — no files in Phase 1's delta touch `mcp-server/`. |
| `brand/*` | 2 files | unit | Hex-color lint, CSS token generation | Unchanged. |
| `scripts/*` | 17 files | unit/integration/smoke | Coverage gate, config-docs checker, dev supervisors, seed scripts, version sync, agent-provisioning poll loop, Minikube stack orchestration | Unchanged this cycle. `scripts/hitl.ts`/`hitl.unit.test.ts` unchanged — still only the pure-planning half (`computeMissingClones`/`computeProvisionPlan`) tested; see Net-new below for the open integration-harness gap. |
| `plugins/shipwright/scripts/*` | 18 files (was 15, +`compute-review-verdict.unit.test.ts`, +`compute-unaddressed-findings.unit.test.ts`) | unit | `check-*.ts` cron prechecks, `classify_test_layer.ts`, `check-helpers.ts`, **`compute-review-verdict.ts` (DRO-1.1)** and **`compute-unaddressed-findings.ts` (PVD-1.1)** (new) | New this cycle: `compute-review-verdict.ts`'s `computeVerdict`/`validateReviewVerdict` — a pure, fully-enumerable 3-boolean truth-table function mechanizing `review.md` Step 10's verdict logic, plus a regex-based label extractor (`compute-review-verdict.unit.test.ts` covers it); `compute-unaddressed-findings.ts`'s `hasUnaddressedFindings`, `isSelfCleanApprove`, `isAddressedByAuthorReply`, and `isSupersededBySelfReview` — extracted from `check-patch.ts` for CLI reuse by `review.md` Step 9.5, implementing the unaddressed-findings check mechanically with full coverage of all exclusion paths (`compute-unaddressed-findings.unit.test.ts` covers both unit logic and the mechanical integration test harness). |
| `plugins/shipwright/{commands,agents,skills,references,test}/*` | 38 dedicated `*.content.test.ts` files (was 36, +`unblock.content.test.ts`, +`test-readiness/SKILL.content.test.ts`) | content/unit/integration | Command/skill/agent markdown prose assertions | New this cycle: `unblock.content.test.ts` (225 lines, backing the new 30th command `unblock.md` — a human-facing blocked-item triage tool, exempt from the Candidate Selection Contract) and `test-readiness/SKILL.content.test.ts` (97 lines, backing `test-readiness/SKILL.md`'s 67-line extension). `security-scan/SKILL.md`'s 130-line extension has no new dedicated content-test file — its existing `SKILL.content.test.ts` (if any) was not confirmed to have grown; flagged for a light spot-check next cycle, not elevated to a gap since content-layer coverage for skills is asserted at the file level, not per-section. `references/user-guide.md` and the new `references/escalation-pattern.md` still have no dedicated content test — consistent with the established 0-of-N convention for `references/` files, not a gap. |
| `site/tests/*.spec.ts` | 21 files | e2e | Marketing pages, incl. 11 `docs/*` page specs | Unchanged this cycle — no `site/` source changes in Phase 1's delta. |
| `admin/e2e/*.e2e.ts`, `metrics/e2e/*.e2e.ts` | 3 files | e2e | Admin login/agents pages, metrics dashboard | Unchanged this cycle. |

### Promote / deepen

None. No promote-shaped gap was found this pass.

### Rebuild

None. No test file's layer, framework, or fundamentals were found wrong this pass.

### Delete (redundant)

None. See Delete (redundant assertion) analysis above.

### Net-new

Sorted by criticality. One item is new this cycle (promoted out of what Phase 2's gap
#15 had grouped as a single already-resolved item — direct inspection found the
task-service half unresolved); the same two items carried forward from prior cycles
remain open:

| Inventory item | Prescribed layer | Criticality | Estimated effort |
|---|---|---|---|
| **`task-store/src/task-service.ts`'s `claim()` concurrent-race regression test** — the `AND "claimedBy" IS NULL` WHERE-clause tightening (the task-layer twin of CRF-1.1) has only single-caller, mocked-double coverage (`task-service.unit.test.ts`'s `makeClaimPrismaDouble()` tests); no real-Postgres test races two concurrent `claim()` calls against the same seeded row the way `pull-request-service.integration.test.ts`'s new test does. The fix's entire value is in the concurrent-access case, which the current coverage cannot exercise. | integration (real Postgres, mirrors `pull-request-service.integration.test.ts`'s pattern exactly) | critical (per the inventory's own criticality tag for `task-service.ts`) | small — the exact harness (`Promise.allSettled` racing two `claim()` calls against a seeded row, asserting one winner + one `ConflictError`) already exists as a template in `pull-request-service.integration.test.ts`; this is a same-shape port to a new `task-service.integration.test.ts` file, not new design work |
| **Local HITL dev-loop bootstrap** (journey #5) — the full `SHIPWRIGHT_HITL_REPOS` clone → `agent-policy.md` seed → task-store/admin boot → poll → dispatch sequence has no integration-level test exercising the sequence end-to-end with an injected `gh` exec fake, even though the pure planning helpers (`computeMissingClones`, `computeProvisionPlan`) are already fully unit-tested | integration-composed (per Phase 2's Test-data strategy — no browser, single-process orchestration with an injected exec fake) | medium (developer-experience path, not customer-facing, but blocks the team's own local dev-loop if it regresses) | small — the hardest planning logic is already isolated and tested; the remaining work is wiring an injectable `gh`/process-exec seam into `scripts/hitl.ts`'s orchestration function (if not already present) and one sequencing test |
| `admin/src/clock.ts` (`Clock`/`SystemClock`/`FixedClock` implementation for admin) — carried forward, unrelated to this cycle's Phase 1 delta | unit | low | trivial, optional — carried forward unchanged from every prior pass. All sibling `Clock` files across the repo lack a dedicated test file too; near-zero logic (two one-line methods), proven correct via consumers per the repo's established convention. |

## Infrastructure — bucketed

### Reuse as-is

- **Bun test runner config** (`bunfig.toml`'s `pathIgnorePatterns`) — re-verified this
  pass: `["**/site/**", "**/metrics/e2e/**", "**/admin/e2e/**", "**/*.canary.test.ts"]`,
  unchanged and correct.
- **CI workflow shape** (`.github/workflows/ci.yml`) — matches Phase 2's documented
  shape; unchanged this cycle.
- **Postgres service container for admin/task-store/chat** — real DB per test run, all
  three `DATABASE_URL_SHIPWRIGHT_*` vars wired in `ci.yml`.
- **`Clock` injection helpers** — implementations across the repo plus
  `plugins/shipwright/scripts/test-helpers/doubles.ts` — still the strongest, most
  consistently-applied convention in the repo. No new consumer landed this cycle that
  deviates from the pattern.
- **Recorded-fixture doubles** — `RecordedKubernetesClient`,
  `metrics/src/providers/task-store-recorded.ts`,
  `metrics/src/fixtures/task-store-fixtures.ts`, `agent/src/shipwright-config-client.ts` —
  unchanged this cycle; pattern is stable and extending cleanly to new clients.
- **`scripts/check-coverage.ts`** — the 80/80 lcov-threshold gate, unchanged. Confirmed
  passing this cycle's CI run at 81.32%/82.23%.
- **`.github/PULL_REQUEST_TEMPLATE.md`** — confirmed present directly, matches
  `test-system.md`'s Repo configuration section's note that this is resolved, not a gap.
- **`docs/test-readiness/speed-baseline.md`** — not re-refreshed this cycle; its numbers
  remain directionally consistent with this pass's live CI pull (see Speed measurement
  below) — steady growth, no budget concern.

### Promote / deepen

None. The design-level backlog item (extending `Recorded*Client` fixture-double coverage
to the remaining `Http*Client` modules named in `test-system.md`'s Local execution
architecture table) remains a Phase 2 design-level item, not a migration-bucket verdict on
any existing infra file — carried in `test-system.md`'s gaps summary, not restated here.

### Rebuild

None.

### Delete (redundant)

None.

### Net-new

None. No new infrastructure gap identified this cycle. The new
`pull-request-service.integration.test.ts`'s concurrent-caller race harness
(`Promise.allSettled` racing two `claim()` calls) is a reusable pattern for the
`task-service.ts` Net-new item above, but requires no new shared cross-workspace
infrastructure — it's colocated with the service's own integration-test file, per Phase
2's Shared helpers section's own guidance for this exact harness shape.

## Risk callouts

No test currently passing (green) received a `delete` or `rebuild` verdict this pass, so
the SKILL's mandatory risk-callout requirement has no entries to populate for the test
suite as it exists today.

**Adjacent risk note (not a delete/rebuild verdict, but worth flagging given the
magnitude):** the `pr-state-reconciler.ts` orphan-reconciliation removal (PR #2527)
deleted 547 lines of passing, green test assertions alongside their production code.
This audit confirmed the removal was deliberate, reviewed, and the superseding mechanisms
(`StaleClaimReaper`, ready-set FIFO, `dev-task.md` Step 4) are real and tested — so no
action is recommended. Flagging only so a future cycle doesn't need to re-derive this
finding from scratch if the same pattern (large deletion of tested code) recurs elsewhere
without a PR reference this clear.

## Speed measurement

### Tier 1 — aggregate (measured this pass)

- **Run:** [31461392600](https://github.com/app-vitals/shipwright/actions/runs/31461392600)
  (CI on commit `0428fe71`, this cycle's exact HEAD), 2026-08-11T05:21:52Z. Fully green —
  no failing jobs.
- **Job wall-clock:** 2m20s (`lint / typecheck / test`, 05:22:07Z–05:24:27Z), of which the
  `bun test` step itself ran 38.35s.
- **Tests:** 6,288 tests pass, 0 fail, across 269 files — per the runner's own summary
  line ("Ran 6289 tests across 269 files.").
- **vs. budget:** 2m20s against the <15 min Full-PR-pipeline budget — **~16% of budget,
  well within**, consistent with every prior cycle's ~13-17% readings. Not trending
  toward the escalation trigger. The escalation formula (aggregate wall-clock >7.5 min,
  sustained across 2 consecutive measurements) is nowhere near tripped.

Test-count growth trajectory: 5,762 (2026-07-28 `speed-baseline.md`) → 6,041-6,042
(2026-08-01) → 6,070 (2026-08-05) → 6,071 (2026-08-06) → 6,177 (2026-08-09) → 6,288
(2026-08-11, this pass) — steady, expected growth (+111 tests this cycle), consistent
with this cycle's 5-new-file delta plus a handful of incremental cases added to existing
files (DRO-1.2's 4 cases, CRF-1.1's regression tests, `resetSkip()`'s companion
assertion), offset slightly by the ~547-line `pr-state-reconciler.ts` orphan-test
removal (which removed roughly a dozen-plus individual `test()`/`it()` cases, not just
lines — net growth still strongly positive).

### Tier 2 — per-layer breakdown (conditional)

- **Tier 2 triggered:** no — Tier 1's aggregate is at ~16% of the <15 min budget this
  pass, far below the 50% escalation threshold.

Tier 2 not triggered — suite is comfortably within the Tier 1 aggregate budget. Per the
SKILL's explicit guidance, this is a valid, complete outcome, not a gap requiring
escalation. No per-layer numbers fabricated.

## Next step

Run `/test-roadmap` to synthesize this migration audit with the Phase 1 inventory and
Phase 2 blueprint into the executable roadmap. Candidate Phase 4 inputs from this
document:

1. **1 real, newly-identified net-new item**: `task-store/src/task-service.ts`'s
   `claim()` concurrent-race regression test — the task-layer twin of CRF-1.1 has only
   mocked single-caller coverage, not a genuine concurrent-caller Postgres test. Small
   effort — an existing template (`pull-request-service.integration.test.ts`) can be
   ported directly to a new `task-service.integration.test.ts`. This corrects Phase 2's
   gap #15, which had grouped this with the (now-resolved) `pull-request-service.ts`
   sibling as a single already-resolved item.
2. **1 real net-new item, unchanged since 2026-07-31**: the HITL local dev-loop bootstrap
   (journey #5) needs an integration-level sequencing test with an injected `gh` exec
   fake; the pure planning logic is already fully unit-tested.
3. **1 low-priority/optional carried-forward item** (`admin/src/clock.ts` — still a
   non-gap by the repo's own convention).
4. **1 open item resolved and closed out this cycle**: the `pr-state-reconciler.ts`
   orphan-reconciliation removal, carried forward as an ambiguous/unverified item in both
   Phase 1 and Phase 2, is now confirmed a deliberate, reviewed, well-tested removal (PR
   #2527, DTC-1.1) — no Phase 4 action needed, remove from future ambiguous-items
   tracking unless new evidence surfaces.
5. **Phase 2's gap #16 (DRO-1.2 self-review supersession) reconfirmed already resolved**
   — 4 dedicated cases in `plugins/shipwright/scripts/compute-unaddressed-findings.unit.test.ts`
   prove both directions of the predicate (extracted from `check-patch.unit.test.ts` with PVD-1.1).
6. **8 carried-forward-from-prior-cycle gaps, all reconfirmed already resolved**
   (`reviewedCommitSha` divergent-value case, RDG-1.1 bundle-mate regression,
   `browser.ts`/`verify-browser-launch.ts` coverage, `ciFailureSignature` streak
   auto-block, ORF-1.x/2.x combined-filter integration coverage, RRR-1.1
   requested-reviewer inclusion, RVG-1.1 author-reply candidacy retrigger, RFR-1.1
   fresh-author-reply exception hoist) — unchanged since 2026-08-06, not independently
   re-verified line-by-line this cycle beyond the spot-checks in Method note step 7.
7. **Speed measurement re-confirmed in budget** — Tier 1 aggregate ~16% of budget on a
   fresh data point at this cycle's exact HEAD, consistent with the growth trajectory. No
   M1 task needed.
8. **0 net-new infra items, 0 rebuild candidates, 0 delete/trim candidates, 0
   isolation-contract regressions** — the steady-state finding holds for another cycle.
