# Test Migration

> Phase 3 of the test-readiness pipeline. Reconciles existing tests and infrastructure
> against the Phase 1 inventory ([`test-inventory.md`](./test-inventory.md), refreshed at
> `08cab0b33` / commit `c728d5afc`) and the Phase 2 blueprint ([`test-system.md`](./test-system.md),
> refreshed at `08cab0b33` / commit `e8d9ec61a`), bucketing each existing test file and
> each inventory item into one of five categories.
>
> Generated by `/test-migration` on 2026-09-21 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-2026-09-21`).

## Method note (this cycle, 2026-09-21)

This pass covers a small 3-day/18-commit top-up (6 non-routine commits after excluding
chart/plugin-version-sync and docs-freshness-anchor churn — see `test-inventory.md`'s
Sampling section for the full per-commit breakdown) since the 2026-09-18 pass. Every new
source file identified in this cycle's Phase 1 inventory refresh was checked directly via
`ls`/`Glob` for a paired, correctly-suffixed test file, and every touched-in-place row was
checked to confirm its test file was extended in the same commit as the production change
— not inferred from the diff stat alone:

1. **`agent/src/pr-origin-backfill.ts` + CLI wrapper `agent/scripts/backfill-pr-origin.ts`
   (new, POB-1.1)** — a one-off, manually-invoked 90-day-window backfill for
   `PullRequest.origin` on merged PRs predating the POM-1.1/1.2 origin-tracking cutover.
   Paired **`agent/src/pr-origin-backfill.unit.test.ts`** (new, 347 lines) verified
   directly: covers `buildBackfillEntries()`'s cutoff-boundary filtering (exactly-at /
   just-before / just-after cutoff, null-`mergedAt` exclusion), ascending sort by
   `mergedAt`, and — the load-bearing assertion — that classification is delegated to
   `pr-census.ts`'s `classifyPrOrigin()`/`buildCensusEntry()` rather than reimplemented
   (a task-row match beating a dependency-bot-looking author login is pinned explicitly,
   so an accidental reimplementation-drift would fail loudly); `runBackfillForRepo()`'s
   >200-entry chunking into multiple `postCensusBatch` calls and per-repo error isolation
   (a thrown `ghJson`/`postCensusBatch` error is caught and surfaced in the returned
   summary, never thrown); `runBackfill()`'s per-repo continue-on-error orchestration.
   Fully injected `ghJson`/task-store-HTTP doubles, no real fetch, no `gh` CLI, no
   `global.fetch`/`global.*` overrides — matches this repo's unit-test isolation contract
   and mirrors `pr-census.unit.test.ts`'s established fake-deps pattern (itself bucketed
   Reuse in the 2026-09-18 pass, Method note step 3 of that cycle). **Verdict: Reuse
   as-is.** Same prescribed-layer/actual-suffix note as `pr-census.ts`'s own precedent:
   Phase 1 tags this file "integration" (service-boundary category) while the actual test
   file is suffixed `.unit.test.ts` — not a gap, this is the repo's established treatment
   for service-boundary code proven via fully-injected doubles rather than a real
   dependency, carried forward unchanged from the `pr-census.ts` precedent. The thin CLI
   wrapper `backfill-pr-origin.ts` has no dedicated test of its own — accepted thin-wrapper
   pattern, same treatment as `cutover-validate.ts`'s sibling scripts, folds into the same
   row.
2. **`agent/src/sessions.ts`'s periodic prune wiring (DTW-1.3 companion, `#3526`)** — new
   **`agent/src/sessions.unit.test.ts`** (new, 94 lines) is the second of this cycle's two
   new test files. Its own header comment states the regression context directly: before
   this wiring, `prune()` had zero production call sites, so entries only ever expired
   lazily on `get()` — which never happens for a per-dispatch `dev-task:{id}:{nonce}` key
   that is written once and never read again. Covers `pruneSessionsOnce()`'s pass-through
   count and failure-swallowing ("housekeeping never crashes a caller"), and
   `startSessionPruner()`'s immediate-sweep-plus-scheduled-repeat behavior over an
   injected store double and injected `setInterval` — no file I/O, no real timers (the
   file-backed store itself stays covered by the existing `sessions.integration.test.ts`).
   **Verdict: Reuse as-is** — correct canonical unit layer, new file backing new
   production wiring, no `global.*` override.
3. **Touched-in-place rows, verified extended alongside their production change, not
   merely diff-stat-inferred:**
   - `metrics/src/providers/task-store-provider.integration.test.ts` (dashboard KPI swap,
     MPC-1.1, `#3534`) — gained a targeted fixture (two matching PRs + a near-miss for
     each predicate conjunct) after an initial review finding that the prior assertion
     passed regardless of predicate correctness (existence-only signal). Last touched by
     `3cb173830` ("replace Estimation Accuracy KPI with Shipwright PRs Merged count"),
     confirmed via `git log`. **Reuse**, in-place strengthening, no bucket change.
   - `admin/src/admin-ui-sessions-list.smoke.test.ts` (Sessions list Follow button, FLW-1.1,
     `#3533`) — last touched by `f43be09b7` ("wire sessions list Follow button to
     SessionFollowService"), confirmed via `git log`. **Reuse**, in-place extension at the
     already-correct smoke layer for the wired route.
   - `agent/src/check-helpers.unit.test.ts` (dev-task-only resume wiring, DTW-1.3,
     `#3526`) — last touched by `e1c7e7596`, confirmed via `git log`. **Reuse**, in-place
     extension.
   - `plugins/shipwright/commands/dev-task.content.test.ts` (worktree-path fallback fix,
     `#3545`) — **not** touched by the fix commit itself (`79f7fe500`, a 1-line
     `${SHIPWRIGHT_REPO_DIR:-repos}` → `${SHIPWRIGHT_REPO_DIR:-$HOME/src}` doc correction,
     confirmed via `git show --stat`, 1 file changed). Checked directly for a sweeping
     regression-guard assertion (one that would fail if *any* future `worktree add`/
     `worktree remove` line in `dev-task.md` reintroduces a relative-default fallback) —
     **none exists**. The only related assertion (line ~712,
     `"no longer uses raw {repo} for ... {repo} style local paths"`) checks a different,
     narrower naming issue and would not have caught this bug. Per this repo's own
     `.claude/shipwright/*` learning history, this exact class of bug ("worktree add
     relative-path gotcha") had already recurred 5+ times before being root-caused — a
     content-layer sweep assertion is cheap and would close the class permanently rather
     than relying on the next recurrence being caught by a human again. **Verdict:
     Promote / deepen** — first non-empty Promote/deepen finding since the 2026-08-23
     pass, see Tests — bucketed below.
4. **No files deleted this cycle. No new features, no new user journeys** (per
   `test-inventory.md`'s own Sampling note) — so no new Net-new candidates and no new
   Delete/redundancy surface beyond what's already covered by the existing analyses below.

## Prior cycle's method note (2026-09-18, preserved for continuity)

This pass covers a 16-day, 246-non-routine-commit gap since the prior pass (see
`test-inventory.md`'s Sampling section for the anchor-drift correction: the true prior
baseline is `fc600c2a`, not the previous header's `c1c81d4e`). Every new source file
identified in this cycle's Phase 1 inventory refresh was checked directly via `ls`/`Glob`
for a paired, correctly-suffixed test file — not inferred from the diff stat alone:

1. **Sessions (new feature, ~19 PRs)** — `task-store/src/session-service.ts` →
   `session-service.unit.test.ts` + `.integration.test.ts`; `session-rollup.ts` →
   `session-rollup.unit.test.ts`; `session-retention-reaper.ts` →
   `session-retention-reaper.integration.test.ts`; `admin/src/session-alert-sweeper.ts` →
   `session-alert-sweeper.unit.test.ts`; `session-follow-service.ts` →
   `.unit.test.ts` + `.integration.test.ts`; `session-scope.ts` → `.unit.test.ts`. All
   **Reuse** — every file has a paired test at its canonical layer, verified directly.
   Not a coverage gap: `session-scope.ts`'s "not yet wired into most routes" status
   (Phase 1/2 Ambiguous/gap item) is an application-logic wiring gap, not a missing-test
   gap — the predicate itself is fully unit-tested.
2. **`task-store/src/pr-origin-derivation.ts` (POM-1.2) — verified, not a gap.** No
   dedicated `pr-origin-derivation.unit.test.ts` file exists, which read as a possible
   Net-new gap on first pass. Direct inspection of `pull-request-service.unit.test.ts`
   found a full `describe("deriveOrigin() (POM-1.2)", ...)` block (13 cases) colocated
   there, covering all 5 precedence branches (shipwright/ci/dependency_bot/human/unknown)
   plus edge cases (null/empty authorLogin, task-match overriding a bot-looking login,
   `chore/chart-v*`/`chore/plugin-version-v*` headRef patterns, a non-matching prefix
   case) — comprehensive depth, correct canonical unit-equivalent coverage. **Reuse**,
   with a minor organizational note: this repo's dominant convention is 1:1
   file-to-test-file naming (confirmed throughout this document), and this is the one
   new file this cycle that deviates from it (tests colocated in a sibling service's
   test file rather than a same-named file). Not flagged as Promote/deepen since
   correctness/depth is not in question — noted for Phase 4 to optionally suggest an
   extraction for consistency, not required.
3. **`agent/src/check-plan.ts`, `github-auth-startup.ts`+`github-auth-deps.ts`,
   `patch-author-allowlist-ref.ts`, `slack-startup.ts`, `pr-census.ts`** — each has a
   paired `.unit.test.ts`, verified directly. **Reuse** across all five, correct
   canonical layer (unit for the pure/mutable-box files, matching each file's sibling
   precedent — `check-plan.ts` mirrors `check-deploy.ts`'s existing test shape,
   `patch-author-allowlist-ref.ts` mirrors `review-author-allowlist-ref.ts`'s).
4. **`task-store/src/webhook-dispatcher.ts` + `webhook-timeout-buffer-check.ts`,
   `admin/src/agent-work-queue-merge.ts`** — each has a paired `.unit.test.ts`. **Reuse**,
   correct canonical unit layer per Phase 2's local-execution-architecture treatment
   (injected `fetchImpl` for the dispatcher, confirmed via direct read of
   `webhook-dispatcher.unit.test.ts`'s fake — no `global.fetch` override).
5. **Prisma 7 upgrade — new `prisma-client.ts` per service (admin, task-store, chat)** —
   each has both `.unit.test.ts` and `.integration.test.ts` (task-store has unit only,
   verified directly). **Reuse**, pure-extraction pattern, no behavior change.
6. **`lib/cli-flags.ts`, `lib/graceful-shutdown.ts`** — each has a paired
   `.unit.test.ts`. **Reuse**, canonical unit layer.
7. **`plugins/shipwright/scripts/is-ci-green.ts`, `compute-unresolved-comment-check.ts`,
   `resolve-dependency-watched-paths.ts`, `slack-say.ts`, `check-site-docs-freshness.ts`,
   `scripts/find-hidden-dir-tests.ts`** — each has a paired `.unit.test.ts`, verified
   directly. **Reuse** across all six, correct canonical layer.
8. **Author-allowlist rename/split cascade (DBR-1.x–2.4)** — the legacy combined
   `authorAllowlist` column drop (migration `20260903150000_drop_agent_author_allowlist`)
   is covered by updated assertions across `admin/src/agents.unit.test.ts`,
   `agents-api.smoke.test.ts`, `agents-api.integration.test.ts`, `api.smoke.test.ts`,
   `admin-ui.smoke.test.ts` (confirmed absent-field regression assertions, per the
   commit's own description) — **Reuse**, correct in-place migration, no coverage loss.
9. **Content-category:** `plugins/shipwright/references/dependency-risk-analysis.md` +
   `dependency-patch.md` each have a paired `.content.test.ts`; `slack-say` skill has a
   paired `SKILL.md`/no dedicated content test but is covered by
   `slack-say.unit.test.ts` at the script layer (the skill markdown itself is thin
   wiring, consistent with several other thin-skill precedents in this document). **Reuse**.
10. **Everything else this cycle** (~230 further commits: admin Tasks/PRs board redesign,
    dependency-risk-analysis review/patch wiring, TaskKind/autonomousPlanSession
    migration churn, PR origin/census endpoints, docs restructuring, marketing-site
    rewrites, routine dependency bumps) — spot-checked a representative sample per
    cluster; all are correctness fixes/extensions to already-migrated, already-correctly-
    bucketed test files (extended in place, not new files), or doc/content-only changes
    with no test-migration impact. No rebuild, no delete, no new Promote/deepen findings
    beyond item 2's organizational note above.

## Prior cycle's method note (2026-09-02, preserved for continuity)

This pass diffed the current file tree against the 2026-08-30 pass's findings (baseline
doc committed at `ac0ec6a8`) and re-verified the delta directly against current repo
state — 51 non-routine commits since `ac0ec6a8` (113 total, minus chart-version-bump/
plugin-version-sync/doc-only churn). Every new source file identified in this cycle's
Phase 1 inventory refresh has a paired, correctly-suffixed test file landed in the same
PR — verified by direct `ls`/`Glob` against each new row, not inferred from the diff
stat alone:

1. **UAP-1.1/1.2 (#3012) — `SlackProvisioningService`/`GithubProvisioningService`
   extraction (pure extraction, no behavior change, per both files' own header
   comments).** New `admin/src/slack-provisioning-service.unit.test.ts` +
   `.integration.test.ts`, `admin/src/github-provisioning-service.unit.test.ts` +
   `.integration.test.ts` (all new files). **Reuse** — correct canonical
   unit/integration split (pure orchestration logic vs. the OAuth/manifest-flow calls),
   landed alongside the extraction in the same PR, no gap.
2. **CFB-4.2 (#2935/#2964) — PWA shell + Web Push.** New
   `admin/src/pwa.unit.test.ts`, `pwa-https-gating.smoke.test.ts`,
   `pwa-routes.smoke.test.ts`, `pwa-icons.unit.test.ts`, `push-content.unit.test.ts`,
   `push-crypto.unit.test.ts`, `push-sender.unit.test.ts`, `push-service.unit.test.ts`,
   `push-toggle.unit.test.ts` (9 new files). **Reuse** across all — each lands at its
   canonical layer per the Phase 2 blueprint's new Web Push local-execution-architecture
   row (injected `fetchImpl`, never `global.fetch`, verified directly in
   `push-sender.unit.test.ts`'s own header comment and confirmed by the isolation-sweep
   below). `scripts/build-pwa-icons.ts` has no dedicated test file — **Net-new,
   deliberately declined** per Phase 1's documented indirect-coverage acceptance (own
   header frames it as a one-time/on-demand dev script, output covered by
   `pwa-icons.unit.test.ts`); not bucketed as a gap, consistent with the identical,
   already-accepted treatment of `scripts/hitl-log-file-tee-target.ts`.
3. **TCS-1.1/1.3 (#2956/#2983/#2982) — TaskEvent audit log + write-through.** New
   `task-store/src/task-transition-diff.unit.test.ts`; `task-service.unit.test.ts` and
   `stale-claim-reaper.unit.test.ts`/`.integration.test.ts` substantially extended
   (interactive-`$transaction` assertions, reaper-emits-event assertions). New
   `task-store/src/task-claim-status-invariant.integration.test.ts` (110 lines) covers
   the companion DB `CHECK` constraint by asserting a direct raw-SQL row insert that
   violates the invariant is rejected. **Reuse** across all — correct canonical layers,
   landed in the same PRs as the production code, no gap.
4. **New file: `agent/src/claim-invariant-reconciler.ts`.** New
   `agent/src/claim-invariant-reconciler.unit.test.ts`. **Reuse** — correct canonical
   integration-equivalent-but-unit-tested layer, matching the sibling
   `pr-state-reconciler.ts` row's existing precedent (a service-boundary self-heal loop
   whose core decision logic is unit-tested against injected fakes, not a live DB).
5. **SSB-1.1/SAV-1.1 (#3006/#2997/#2949/#2930/#2936/#3003/#3017/#2996) — Slack Bolt 5.x
   + `slack-progress.ts`/`slack-thread-status-tracker.ts`.** New
   `agent/src/slack-progress.unit.test.ts` (extended twice more within this window as
   the SlackProgress→`agents.sessions.setStatus` migration landed in stages), new
   `agent/src/slack-thread-status-tracker.unit.test.ts`. `agent/src/slack.unit.test.ts`
   and `.integration.test.ts` extended for the Bolt 5.x call-shape update and the
   synchronous-throw catch. **Reuse** across all — canonical layers correct, no
   redundant-assertion overlap found between `slack-progress.unit.test.ts` and
   `slack.unit.test.ts` (the former exercises the extracted status-posting logic in
   isolation; the latter exercises Bolt event-handler wiring, a different boundary).
6. **CFB-4.3 (#2981/#2940/#2913) — chat reply-notifier + stall-reaper.**
   New `chat/src/reply-notifier.unit.test.ts`, `chat/src/stall-reaper.unit.test.ts`,
   `lib/chat-notify.unit.test.ts`. **Reuse** across all — `stall-reaper.unit.test.ts`
   mirrors the existing `task-store/src/stale-claim-reaper.unit.test.ts`'s established
   test shape closely enough to confirm it isn't a shallower reimplementation of the
   same pattern (spot-checked: both assert the same three cases — stalled claim reaped,
   fresh claim untouched, and a boundary-exact heartbeat-age case — proportionate depth).
7. **PTL-1.x/2.x migration cluster (6 PRs, no new rows).** `task-store-provider.unit.test.ts`
   gained 218 new lines, `.integration.test.ts` extended;
   `check-helpers.unit.test.ts`/`.integration.test.ts`,
   `patch.content.test.ts`/`review.content.test.ts`,
   `unblock.content.test.ts` all extended in place. **Reuse** across all — in-place
   extensions to already-`Reuse`-bucketed rows, no layer/shape change.
8. **`398c71ed` (DBT-1.1) — dependency-bot-triage rename.**
   `check-dependency-bot-triage.unit.test.ts` (453 lines) is the renamed+extended
   `check-dependabot-triage.unit.test.ts` (was 281 lines) — **Reuse**, a rename carries
   its bucket forward unchanged; the file's prior `Reuse` verdict (2026-08-25 cycle,
   when the underlying script itself was already migrated) still holds.
9. **`ddafdc57` (PFL-4.1) — superseded self-review heuristics removed from
   `compute-unaddressed-findings.ts`.** `compute-unaddressed-findings.unit.test.ts`
   shrank alongside it (net -388 lines from the diff stat) — **not** a coverage-loss
   red flag: the removed test cases exercised heuristic logic that no longer exists in
   production, not logic that still exists but lost its test. Confirmed by reading the
   diff: every removed `it()` block corresponds 1:1 to a removed heuristic function
   (`isSelfCleanApprove`'s superseded branches) rather than a removed assertion against
   still-live code. **Reuse**, correct in-place shrink.
10. Re-ran the isolation-contract grep sweep (`mock.module(`, `global.fetch =`,
    `global.console =`, `DATABASE_URL` inside `*.unit.test.ts`) across every new test
    file from this cycle plus the existing suite — one grep hit
    (`agent/src/slack-progress.unit.test.ts:13`) inspected directly and confirmed a
    **false positive**: a comment ("no mock.module()...") documenting compliance, not a
    violation. **0 real violations**, same result as every prior cycle.
11. Pulled a fresh live Tier 1 speed + Step 4c coverage measurement from CI (`gh run
    view` against the latest green `lint / typecheck / test` run at this cycle's exact
    HEAD, `c1c81d4e`, run
    [33595391033](https://github.com/app-vitals/shipwright/actions/runs/33595391033)) —
    see Speed measurement below.

## Prior cycle's method note (2026-08-30, preserved for continuity)

This pass diffed the current file tree against the 2026-08-28 pass's findings (baseline
doc committed at `5b586beb`) and re-verified the delta directly against current repo
state — 13 non-routine commits since `5b586beb` (28 total, minus chart-version-bump/
plugin-version-sync/doc-only churn):

1. **`a748c46f` (#2906) — generic progress phases (CFB-2.1).** New
   `lib/progress-phases.unit.test.ts` and `agent/src/progress-milestones.unit.test.ts`
   (both new files) cover the closed 11-phase enum and the pure `phaseForBlock()` mapper
   respectively, including a dedicated assertion that `block.input` is never read by the
   mapper (the file's load-bearing anti-leak invariant). `claude.unit.test.ts` extended
   (+222/-lines) to cover the widened `ProgressCallback` firing a phase alongside the
   existing usage-guard behavior. **Reuse**, both new files backing new production code
   in the same PR, correct canonical unit layer — see Phase 1's new rows.
2. **`e12ac33f` (#2893) — `pr-state-reconciler.ts` batch+rotating-cursor scan.**
   `pr-state-reconciler.unit.test.ts` extended (+176/-lines) to cover the new
   `RECONCILER_PR_OPEN_TASK_BATCH_SIZE` cap and the rotating offset cursor's wraparound
   behavior, replacing the old recency-window exclusion tests. In-place extension to an
   already-migrated, already-correct-layer unit row. **Reuse**, no new file.
3. **`b7342b8e` (#2896) — `check-deploy.ts` case-insensitive workflow-name matching.**
   `check-deploy.unit.test.ts` extended (+56/-lines) with mixed-case `"ci"`/`"Deploy"`
   workflow-name fixtures proving `isCiGreen()` and the busy-repo guard both now match
   case-insensitively. **Reuse**, in-place extension.
4. **`93746c6b`/`aeda23a7` (#2899/#2902) — chat reply heartbeat replaces flat 90s
   timeout.** `chat-poller.unit.test.ts` gained the largest single-file extension this
   cycle (+220/-lines), covering the throttled heartbeat-while-working behavior, the
   guard against heartbeating an unclaimed/already-replied message, and the
   interval-driven (not fixed-timer) cadence. `chat/src/message-service.integration.test.ts`
   (+32/-lines) and new **`chat/src/messages.smoke.test.ts`** (111 lines, new file)
   cover the route/service-layer heartbeat persistence and timeout-extension contract at
   the smoke layer — correctly landed alongside the production heartbeat field wiring.
   `chat/src/test-fakes.ts` extended (+24/-lines) to support the new heartbeat-aware fake.
   **Reuse** — the new smoke-test file backs new production wire-contract behavior in the
   same PR, folding into the already-migrated chat-service composite row rather than
   creating a new one (consistent with this row's established treatment of new route
   behavior as extension, not a new functional surface).
5. **`52213413` (#2905) — shared admin page-head extraction (`admin-ui-layout.ts`, new
   file).** New **`admin/src/admin-ui-layout.unit.test.ts`** covers `renderAdminPage()`'s
   title escaping, `extraStyles`/`headExtra`/`bodyEnd` ordering, and `bodyClass` handling,
   plus a new loop-test assertion (folded into the existing admin-UI smoke suite,
   `admin-ui.smoke.test.ts` extended +9/-lines) that all 22 render sites emit exactly one
   `DOCTYPE` and one viewport-meta tag. **Reuse**, new file backing a pure-extraction
   refactor of already-covered logic — the underlying functional surface (server-rendered
   admin page shell) was already migrated as part of the `admin-ui.ts`/`admin-ui-pages.ts`/
   `admin-ui-styles.ts` composite row; this is a relocation, not a new boundary.
6. **CFB-2.3 — live progress bubbles and e2e test suite.** New admin-ui-pages.ts
   exports for live-progress rendering: `ERROR_KIND_LABELS`, `errorKindLabel()`,
   `LIVE_STATUS_BUBBLE_ID`, `LIVE_STATUS_ELAPSED_ID`, `LIVE_STATUS_MILESTONE_ID`,
   `STALL_INDICATOR_CLASS`, `STALL_WARN_AFTER_MS`, `ABSOLUTE_MAX_MS`, and
   `renderChatMessageBubble()`. Two new admin e2e spec files,
   **`admin/e2e/chat-progress.e2e.ts`** and **`admin/e2e/chat-degraded.e2e.ts`** (359
   lines combined), assert: (a) the live elapsed ticker increments with zero network
   dependency (Layer-1 wall-clock guarantee), (b) stall warnings appear after the
   threshold when progressSeq freezes, (c) degraded mode renders correctly when chatClient
   is absent. Extended **`admin/e2e/test-server.ts`** with live-progress fixtures
   (`MOCK_CHAT_PENDING_MESSAGES`, `CHAT_PENDING_MODE`, `CHAT_CLIENT_ABSENT` switches).
   **Reuse**, all new files backing new production code in the same PR, correct canonical
   layers (e2e for behavior-driven assertions on real browser/elapsed-ticker guarantees,
   not implementation detail).
7. **`b4c0feb9` (#2914) — mobile responsive defects across admin console (CFB-3.1).**
   Nine confirmed mobile defects fixed (iOS zoom-on-focus, broken thread-page height,
   padding specificity, above-the-fold thread header, non-wrapping composer, sub-44px
   touch targets, missing `image/heic`/`image/heif` allowlist entries), verified via
   **new Playwright e2e assertions** (computed font-size, absence of horizontal overflow,
   computed touch-target height) added to the two new e2e spec files from item 6 above,
   rather than string-matching rendered HTML. Three now-redundant unit-test assertions
   were **removed** in the same commit in favor of the more faithful e2e coverage — a
   deliberate, documented layer migration (brittle string-match unit assertions replaced
   by behavior-accurate e2e assertions proving the actual rendered/computed style), not an
   unexplained coverage loss. `admin/src/attachment-validation.unit.test.ts` extended
   (+33 lines) to cover the new allowlist entries. **Reuse** — the e2e migration is a
   correct layer move (CSS media-query/computed-style behavior is more faithfully proven
   in a real browser than by string-matching server-rendered HTML), not a Delete-redundant
   or Rebuild finding, since the replaced unit assertions were testing implementation
   detail (literal style strings) rather than a property lower-layer tests can't reach.
8. Re-ran the isolation-contract grep sweep (`mock.module(`, `global.fetch =`,
   `global.console =`, `DATABASE_URL` inside `*.unit.test.ts`) across the current suite —
   same result as every prior cycle: 0 real violations. No new singleton-ref files added
   this cycle to check by hand.
9. Pulled a fresh live Tier 1 speed + Step 4c coverage measurement from CI (`gh run view`
   against the latest green `lint / typecheck / test` run at this cycle's exact HEAD,
   `ac0ec6a8`, run [33271383502](https://github.com/app-vitals/shipwright/actions/runs/33271383502)) — see Speed measurement below.

## Prior cycle's method note (2026-08-28, preserved for continuity)

This pass diffed the current file tree against the 2026-08-25 pass's findings (baseline
doc committed at `d4bfce02`) and re-verified the delta directly against current repo
state — 42 non-routine commits since `d4bfce02` (69 total, minus chart-version-bump/
plugin-version-sync churn and this pipeline's own prior docs-refresh commits), the largest
window in several cycles:

1. **`70722bbb` (#2865) — DB-aware readiness check.** New `task-store/src/
   app.readiness.smoke.test.ts` (125 lines, new file) exercises `GET /ready` against a
   real Postgres connection (200 when reachable) and a deliberately broken connection
   string (503), via Hono's in-process `app.request()` — correctly at the smoke layer
   per this repo's route-contract convention, no mocked DB. `main.unit.test.ts` gained
   26 lines covering the new `PrismaClient` wiring into the route factory. **Reuse**, new
   file backing new production code, landed in the same PR, correct layer.
2. **`6a61dd5d`/`e3d199f3`/`14d9d8bf` (#2843/#2844/#2878) — downstream-compat script +
   cloud-native chart/minikube work.** `scripts/check-downstream-compat.smoke.test.ts`
   (175 lines, new) extracts and executes the real bash script's `render_variant`/
   `copy_wrapper`/restore-guard logic against synthetic `Chart.lock`-only wrapper
   fixtures, explicitly regression-testing the restore-guard bug fixed in the same PR
   window (checked the post-delete destination path instead of the source wrapper path).
   **Reuse**, new file backing new production code, correct layer per this design's
   Step-2 watch-item note (see `test-system.md`'s 2026-08-28 delta). The cloud-native
   chart additions (bundled ingress-nginx/traefik/cert-manager subcharts, cert-manager
   bootstrap hook) are covered entirely by `helm unittest` YAML fixtures
   (`cert_manager_bootstrap_job_test.yaml`, `bundled_subcharts_test.yaml`,
   `cloud_native_profile_test.yaml`, `test-cert-issuer.yaml`, extended
   `ingress_traefik_test.yaml`/`test-connection.yaml`) — outside this document's
   `bun test`-scanned scope per the existing, repeatedly-applied `charts/shipwright/`
   exclusion; not bucketed below. `scripts/minikube.ts`'s cloud-native profile support
   extends `scripts/minikube.unit.test.ts` (existence-only signal, already-migrated row,
   including a byte-identical regression pin for `task minikube:up`'s emitted command
   list) — **reuse**, no new file.
3. **`56217d3b`/`4abd283b`/`31a1a0b8` (#2881/#2852/#2829) — Renovate exclusion + policy
   plain-YAML parsing fixes.** `check-review.unit.test.ts` gained 29 lines (renovate-
   mirrored all-renovate/mixed-set/requested-reviewer-does-not-override cases);
   `check-helpers.unit.test.ts` (both `agent/src/` and `plugins/shipwright/scripts/`
   copies) gained 40+8 lines covering the plain-YAML-frontmatter parse path; existing
   `check-deploy.unit.test.ts`/`check-helpers.unit.test.ts` cases updated in place for
   the `allow_self_review` default flip to `false`. All in-place extensions to
   already-migrated, already-correct-layer unit rows. **Reuse**, no new file.
4. **`d6599c8d` (#2851) — live policy-flag reads (PLR-1.1).** `check-deploy.unit.test.ts`
   (+84/-lines), `check-review.unit.test.ts` (+50), `pr-state-reconciler.unit.test.ts`
   (+55) all extended to assert the getter-function conversion actually re-reads on
   every call (not just once at construction) — a meaningfully deeper assertion than the
   prior static-boolean tests could express, but still within the existing unit-test
   files, not a new file. **Reuse**, no layer/framework change.
5. **`3261f023` (#2825) — phase-scoped redispatch cooldown (PRC-1.1).**
   `loop-orchestrator.unit.test.ts` gained 34 lines proving two different phases on the
   same PR no longer share a cooldown window. **Reuse**, in-place extension.
6. **`1498a716` (#2849) — ScheduleWakeup/backgrounding guardrail.**
   `dev-task.content.test.ts` gained a new 59-line `describe` block, correctly at the
   canonical `content` layer (markdown/prompt assertion, no I/O). **Reuse**, no new file.
7. **`63cb4921` (SUX-1.1, #2864) — cron-table single-source-of-truth.** New
   `plugins/shipwright/test/cron-tables.content.test.ts` (150 lines, new file) is a
   regression guard asserting every manifest cron appears exactly once in
   `cron-jobs.mdx`'s table and zero times in `the-agent.mdx`. Correctly at the `content`
   layer (markdown-table cross-reference assertion, no I/O boundary). **Reuse**, new
   file backing new production-doc-consistency logic, landed in the same PR.
8. **`10ce19ed` (#2833) — `/docs` landing page removed, replaced by a redirect.**
   `site/tests/docs-landing.spec.ts` **deleted** in the same commit as its subject page
   (`site/src/pages/docs/index.astro`); `site/tests/docs-redirect.spec.ts` (32 lines,
   new) and `docs-platform.spec.ts`/`config.spec.ts`/`home.spec.ts` updated in place to
   assert the new redirect behavior instead. A clean one-for-one swap, not a
   delete-without-replacement — **reuse** (the new file), not a delete-candidate finding.
9. **`26d28ec9`/`60eeba9d`/`f815be7a` (#2826/#2835/#2863) — new security docs page +
   two new Astro components (`SecurityArchDiagram`, `StepFlow`).** New
   `site/tests/docs-security.spec.ts` (70 lines, new file, extended 4 more times in the
   same window by `60eeba9d`/`2f290939`/`bec31c77`/`f1270a21` as the page's content grew)
   and new `site/tests/docs-slack-integration.spec.ts` (147 lines, new — `f815be7a`
   applied the new `StepFlow` component to `slack-integration.mdx` and
   `the-shipwright-loop.mdx`; the latter's own spec, `docs-the-shipwright-loop.spec.ts`,
   predates this window (added by an earlier, unrelated commit) and was only extended
   +56 lines here — verified via `git log --diff-filter=A`, not counted as new to this
   cycle). Both `SecurityArchDiagram.astro` and `StepFlow.astro` are presentational-only
   with no dedicated component-level test, per the existing convention already applied
   to `ComparisonTable.astro`/`ConfigTable.astro`/`ChooseWhenGrid.astro` (covered by the
   astro build + brand-lint CI gate) — correctly un-migrated, not a gap. **Reuse**, new
   files backing new production doc pages, landed in the same PRs.
10. Re-ran the isolation-contract grep sweep (`mock.module(`, `global.fetch =`,
    `global.console =`, `DATABASE_URL` inside `*.unit.test.ts`) across the current suite
    — same result as every prior cycle: 0 real violations (all hits are doc-comment
    references to the convention, not actual violations). The singleton-mutation pattern
    widening this document has recommended since 2026-08-22 (scanning for `.set(`/
    `.reset(` calls on named singleton-ref exports with no paired `afterEach`) still has
    not been implemented as an automated sweep — remains a manual read-through finding,
    carried forward as a process-improvement recommendation. No new singleton-ref files
    added this cycle to check by hand.
11. Pulled a fresh live Tier 1 speed + Step 4c coverage measurement from CI (`gh run view
    --log` against the latest green `lint / typecheck / test` run at this cycle's HEAD,
    `7a688190`) — see Speed measurement below.

## Prior cycle's method note (2026-08-25, preserved for continuity)

This pass diffed the current file tree against the 2026-08-23 pass's findings (baseline
doc committed at `be5bfa59`) and re-verified the delta directly against current repo
state — 5 substantive commits since `be5bfa59` (19 total, the rest chart-version-bump/
plugin-version-sync/docs-sync-anchor churn):

1. **`da1aa278` (#2811), `31278dbd` (#2810), `acec7edd` (#2803) — principles.md-override
   content additions to `dev-task.md`, `review.md`/`code-reviewer.md`, `plan-session.md`.**
   Read each paired content-test diff directly: `dev-task.content.test.ts` (+36 lines, new
   `describe` block asserting Step 6 checks `.claude/shipwright/principles.md` before
   falling back and cites the security domain), `plan-session.content.test.ts` (+31 lines,
   same pattern for Step 5), `review.content.test.ts` (+18 net lines — hoists an existing
   `extractStep7Section` helper to module scope and adds a new `describe` asserting Step
   7's `testReadinessContext` fallback note checks the override path first),
   `code-reviewer.unit.test.ts` (+50 lines, asserts the Shipwright-specific Rules preamble
   checks the override path and that Rules 6/7/8 reference an "override-aware principles
   source" rather than a hardcoded `references/principles.md` path). All four are
   content-only assertions on markdown/prompt text — correctly at the canonical `content`
   layer, no I/O boundary, no layer mismatch. **Reuse**, no new row (extends the
   already-migrated `commands/*.content.test.ts` / `agents/code-reviewer.unit.test.ts`
   pairs).
2. **`da6e21df` (#2804) — cron-prompt chaining fix + golden-fixture update.** The
   `error-patrol-maintenance` cron prompt gained an explicit chaining instruction; the
   paired `admin/src/agent-cron-jobs.integration.test.ts` golden-fixture string was
   updated in the same commit to match. Read the diff directly — the `GOLDEN_CODING_CRONS`
   fixture's `prompt` field is a literal string compared against the live cron
   registration, so the test *must* change in lockstep with the prompt or it would fail;
   this is expected co-evolution, not drift. **Reuse**, already-canonical integration-style
   golden-fixture assertion.
3. **`72d84950` (#2801) — `check-consolidation-patrol.ts` bootstrap-deadlock fix.** Read
   `check-consolidation-patrol.unit.test.ts`'s diff directly: the existing test case
   `"exits 1 when the ledger is missing"` was renamed and its assertions flipped in place
   (`expect(result.exit).toBe(1)` → `toBe(0)`, `expect(result.output).toBe("")` →
   `expect(result.output.length).toBeGreaterThan(0)`) to match the corrected behavior — a
   one-for-one update of an existing case, not a new one. Correctly at the canonical unit
   layer (pure function, `Deps` injected). **Reuse**, no layer/framework change.
4. **`6b6709cf` (#2798) — `deepmerge-ts` CVE dependency bump.** `package.json`/`bun.lock`
   only, no test-file change, no source row per existing convention (dependency-version
   churn, not a code unit).
5. Re-ran the isolation-contract grep sweep (`mock.module(`, `global.fetch =`,
   `global.console =`, `DATABASE_URL` inside `*.unit.test.ts`) across the current suite —
   same result as every prior cycle: 0 real violations (all hits are doc-comment
   references to the convention, not actual violations). The singleton-mutation pattern
   widening this document has recommended since 2026-08-22 (scanning for `.set(`/`.reset(`
   calls on named singleton-ref exports with no paired `afterEach`) still has not been
   implemented as an automated sweep — remains a manual read-through finding, carried
   forward as a process-improvement recommendation. No new singleton-ref files added this
   cycle to check by hand.
6. Pulled a fresh live Tier 1 speed + Step 4c coverage measurement from CI (`gh run view
   --log` against the latest green `lint / typecheck / test` run at this cycle's HEAD,
   `d4bfce02`) — see Speed measurement below.
7. **`agent-types/coding/manifest.yaml`'s cron-manifest mirror, extended alongside
   `da6e21df` above.** `manifest.content.test.ts` (pre-existing file, +26 lines) gained a
   new case asserting the `error-patrol-maintenance` cron entry in the agent-type
   manifest's own `crons` list also carries the chaining-instruction keywords — the same
   content already asserted against the raw cron prompt in item 2 above, now also checked
   at its second copy (the agent-type manifest mirrors cron definitions independently of
   the admin-service golden fixture). Content-layer, correctly thin, no new row. No other
   files changed since `be5bfa59` beyond chart-version-bump/plugin-version-sync/
   docs-sync-anchor churn — none of which touch test infrastructure.

## 2026-08-23 cycle's method note (preserved for continuity)

This pass diffed the current file tree against the 2026-08-22 pass's findings (baseline
doc committed at `ea846283`... actually merged as `be5bfa59`) and re-verified the delta
directly against current repo state — exactly 2 substantive commits since `ea846283`:

1. **`3df0539e` (PR #2788) — SMR enforcement gate in `agent/src/slack.ts`.** Read
   `agent/src/slack.unit.test.ts` directly: 13 new assertions cover
   `isAllowedSlackSender()` (fail-open when unsynced, fail-open when `restrict:false`,
   case-insensitive email match, reject on no match) and `shouldRejectSlackSender()`
   (resolves sender email then delegates), plus per-handler wiring assertions across all
   3 call sites (`app.message()`, `app.event("app_mention")`, `app.event("reaction_added")`).
   Correctly extends the already-migrated `slack.unit.test.ts`/`slack.integration.test.ts`
   pair at their existing canonical layers (unit for the pure predicate + handler wiring,
   integration for the Bolt-app-level `slack.integration.test.ts` and
   `slack-chat-conversation.integration.test.ts` suites) — **Reuse**, no new row. The
   same PR's two companion fix-commits (injecting a fresh, unsynced `membershipRef` into
   `slack.integration.test.ts` and `slack-chat-conversation.integration.test.ts` instead
   of falling through to the process-wide singleton default) are exactly the isolation
   fix this document's own 2026-08-22 pass recommended widening the audit's pattern set
   to catch (see the 2026-08-22 Isolation contract audit note, preserved below) — closes
   2 of the 3 call sites that pattern-set gap could have missed.
2. **`192437ed` (PR #2790, T-084) — resolves this document's own carried Promote/deepen
   item.** Adds the `afterEach` reset to `agent-slack-membership-ref.unit.test.ts`'s
   singleton-scoped describe block that the 2026-08-22 pass flagged as missing (see that
   pass's Method note step 1 and Risk callouts, preserved below). Read the diff directly:
   the reset restores `agentSlackMembershipRef` to `{restrict: false, emails: []}` after
   each test in that block, exactly as recommended. Combined with item 1's two companion
   fixes, all 3 known call sites of the singleton-mutation leak (the unit test itself,
   plus the two integration suites that defaulted to reading the same shared singleton)
   are now closed. **This item moves from Promote/deepen back to Reuse as-is** — the
   document's own prediction ("recommend the next cycle confirm PR #2788 lands with a
   proper `afterEach` reset... and, if so, move this item back to Reuse as-is") is
   confirmed, modulo the fix landing as a separate PR (#2790) rather than inside #2788
   itself as originally anticipated — re-verified directly rather than assumed.
3. Re-ran the isolation-contract grep sweep (`mock.module(`, `global.fetch =`,
   `global.console =`, `DATABASE_URL` inside `*.unit.test.ts`) — same results as
   2026-08-22 (0 real violations); the widened singleton-mutation pattern this document
   previously recommended (scanning for `.set(`/`.reset(` calls on named singleton-ref
   exports with no paired `afterEach`) still has not been implemented as an actual
   automated sweep — it remains a manual read-through finding, carried forward as a
   process-improvement recommendation, not yet a script. No new instances found this
   cycle among the repo's other singleton refs (`agentReposRef`,
   `reviewAuthorAllowlistRef`, `loopJobsRef`).
4. Pulled a fresh live Tier 1 speed + Step 4c coverage measurement from CI (`gh run view
   --log` against the latest green `lint / typecheck / test` run at this cycle's HEAD,
   `be5bfa59`) — see Speed measurement below.
5. No other files changed since `ea846283` beyond chart-version-bump/plugin-version-sync/
   docs-sync-anchor churn (`efb98a08`, `b080d823`, `4269ceb2`, `ee616a1a`) — none of which
   touch test files or test infrastructure.

## 2026-08-22 cycle's method note (preserved for continuity)

This pass diffed the current file tree against the 2026-08-18 pass's findings (baseline
doc committed at `f901803d`) and re-verified the delta directly against current repo
state:

1. Enumerated new test files via `git diff --stat --diff-filter=A f901803d..HEAD --
   '*.unit.test.ts' '*.integration.test.ts' '*.smoke.test.ts' '*.content.test.ts'
   '*.spec.ts' '*.e2e.ts'` — **five** new files: `admin/src/okta-auth-client.integration.test.ts`
   (263 lines), `admin/src/github-app-provisioning-client.unit.test.ts` (89 lines),
   `admin/src/github-app-provisioning-client.integration.test.ts` (149 lines),
   `agent/src/agent-slack-membership-ref.unit.test.ts` (197 lines),
   `task-store/src/pr-transition-diff.unit.test.ts` (161 lines). Read each directly:
   - **`okta-auth-client.integration.test.ts`** — 10 cases across `getAuthorizationUrl`,
     `exchangeCode`, `getUserInfo`, and default construction; injected-`fetchFn` cassette
     replay (`fixtures/okta-auth-cassette.json`), mirrors `google-auth-client.integration.test.ts`'s
     established pattern exactly. Covers happy path, missing-optional-fields, non-ok
     error responses, and issuer-trailing-slash normalization for both the authorize and
     token endpoints. Correctly at the canonical integration layer — **Reuse**.
   - **`github-app-provisioning-client.unit.test.ts`** — 13 cases for the pure
     `buildAgentAppManifest` builder (permission-scope lock-in, `public: false`,
     conditional `redirect_url`/`setup_url`, explicit "no `hook_attributes`, no webhook
     receiver" assertion matching the source file's own design note). **Reuse**.
   - **`github-app-provisioning-client.integration.test.ts`** — 7 cases for
     `exchangeManifestCode`: happy path with narrowed-field return, an explicit
     **"never stores or returns `webhook_secret`"** assertion (directly proving the
     source file's header-comment claim, not just asserting it in prose), 404/422/missing-field
     error paths, default construction. Canonical integration layer, genuinely proves the
     security-relevant discard behavior — **Reuse**.
   - **`pr-transition-diff.unit.test.ts`** — 9 `test()` cases (not `it()`, consistent with
     this file's style) covering both design notes from Phase 1/2 directly:
     `heartbeatAt`-only change → no events, a real field change alongside an incidental
     `heartbeatAt` change → only the real field's entry, system columns
     (`createdAt`/`updatedAt`/`id`) never diffed, null↔value transitions, numeric/boolean
     stringification. Pure, canonical unit layer — **Reuse**.
   - **`agent-slack-membership-ref.unit.test.ts`** — 16 cases across
     `createAgentSlackMembershipRef` (including a "multiple independent ref instances
     don't share state" case using a factory-created instance), the process-wide
     `agentSlackMembershipRef` singleton, and `resolveSlackMembership`'s null/undefined
     defaulting. **Found a real isolation gap while reading this file directly** (not
     inferred from the filename): the singleton-scoped `describe` block's one test case
     calls `agentSlackMembershipRef.set({ restrict: true, emails: ["alice@example.com"] })`
     directly on the shared, process-wide singleton and has **no `afterEach`/cleanup**
     resetting it back to the fail-open default afterward. No other test file in this
     baseline imports `agentSlackMembershipRef`, so this specific commit does not yet
     produce an observable cross-suite failure — but it is the exact shape of leak this
     repo's isolation contract (`CLAUDE.md`'s "no `global.*` overrides — Bun shares the
     test process, so leaked globals break sibling suites") exists to prevent, and per
     project memory a fix for precisely this file-order-dependent leak (PR #2788) lands
     just after this cycle's diff window closes at `ea846283`. Bucketed **Promote /
     deepen** — see Risk callouts and the new open item below; not Rebuild, since the
     assertions themselves are correct and the fix is additive (an `afterEach` reset),
     not a layer or framework change.
2. Cross-referenced Phase 1's delta list (84 commits / 138 files since `f901803d`)
   against existing test files for the remaining new/extended surfaces: `prs.smoke.test.ts`
   / `routes/prs.openapi.smoke.test.ts` (extended for `GET /:id/events`, PSA-1.2 — read
   directly, exercises the new route's response shape including an empty-events case for
   a PR with no recorded transitions yet), `pull-request.integration.test.ts` (new,
   exercises `recordTransition()` persisting `PullRequestEvent` rows inside the same
   transaction as the source update — the transactional-atomicity property the design
   doc's local-substitute row prescribes), `check-review.unit.test.ts` (extended across
   the 8-commit review-candidacy fix cluster — spot-checked 3 of the 8 diffs directly;
   each adds a case matching its commit's stated fix, no gap), `admin-dev-login.smoke.test.ts`
   / `provision-callback.smoke.test.ts` (extended for the Okta login-route + GitHub App
   manifest-flow route wiring — route-contract coverage only, business logic already
   covered by the client-layer unit/integration tests above, correctly thin per the
   canonical-layer rule). No layer mismatches found.
3. Re-ran the isolation-contract grep sweep (`mock.module(`, `global.fetch =`,
   `global.console =`, `DATABASE_URL` inside `*.unit.test.ts`) across the full current
   suite — **one new finding**: `agent-slack-membership-ref.unit.test.ts`'s singleton
   test doesn't match any of those literal greps (it mutates an exported ref object, not
   a JS global), so the sweep's existing pattern set doesn't catch this class of leak.
   Recommend widening the sweep in a future cycle to also flag direct `.set()`/mutation
   calls on any singleton-named export (`reviewAuthorAllowlistRef`, `agentRposRef`,
   `agentSlackMembershipRef`, etc.) with no paired `afterEach` in the same file — see
   Risk callouts.
4. Pulled a fresh live Tier 1 speed + Step 4c coverage measurement from CI (`gh run view
   --log` against the latest green `lint / typecheck / test` run at this cycle's HEAD,
   `ea846283`) — see Speed measurement below.
5. Confirmed the never-decrease coverage gate's removal (`2ab1a4cb`) has no migration
   impact: `scripts/check-coverage-no-decrease.unit.test.ts` and
   `.integration.test.ts` were deleted in the same commit as their source file — a clean
   paired deletion, not an orphaned test. Removed from the Tests — bucketed table below.
6. Spot-checked the prior cycle's carried-forward non-gap items against current file
   contents: `admin/src/clock.ts` (still no dedicated unit test file, unchanged, still a
   non-gap per the repo's established `Clock`-file convention).

One open item carried forward unresolved from the 2026-08-18 pass is addressed by this
cycle's own new finding rather than a separate action (see item 1's
`agent-slack-membership-ref.unit.test.ts` note above). No other open items were carried
forward — the 2026-08-18 cycle's delta introduced no gaps beyond what's already noted.

## Prior cycle's method note (2026-08-18, preserved for continuity)

This pass diffed the current file tree against the 2026-08-16 pass's findings (baseline
doc committed at `ca3c8c3f`) and re-verified the small delta directly against current
repo state:

1. Enumerated new test files via `git diff --stat --diff-filter=A ca3c8c3f..origin/main
   -- '*.unit.test.ts' '*.integration.test.ts' '*.smoke.test.ts' '*.content.test.ts'
   '*.spec.ts' '*.e2e.ts'` — exactly **one** new file:
   `task-store/src/pr-finding.integration.test.ts` (234 lines). Read it directly (not
   just the filename): it round-trips a `PrFinding` row against real Postgres (via
   `DATABASE_URL_SHIPWRIGHT_TASK_STORE_TEST`, `describeOrSkip`-gated per the repo's usual
   convention), covers the FK relation to `PullRequest`, and exercises the model's
   `disposition`/`source` enum persistence. Correctly at the canonical integration
   layer per `test-inventory.md`'s row for the new `PrFinding` model — no layer mismatch.
2. Cross-referenced Phase 1's delta list (16 commits / 53 files since `ca3c8c3f`) against
   existing test files for the four in-place fixes: `admin/src/agent-manifest.unit.test.ts`
   (extended, ephemeral-storage constant), `agent/src/setup.integration.test.ts`
   (extended, +38 lines, cache-pinning assertions), `task-store/src/state-filter.smoke.test.ts`
   (new file, but a smoke-layer route-contract test, not counted as a "new inventory
   coverage" item since it's incremental coverage on the already-migrated
   `routes/tasks.ts` row — the `hitl` filter is a filter-combination extension, not a new
   functional unit), `scripts/minikube.unit.test.ts` (extended, +158/-25 lines, dev-login
   URL/auto-open builder cases). Read each diff directly — all land at their already-
   migrated canonical layer, no rebuild/promote signal.
3. `task-store/src/prs.smoke.test.ts` (+277 lines) and `task-store/src/routes/prs.openapi.smoke.test.ts`
   (+4 lines) extended to cover the new `POST /:id/findings` route, including the
   `source:"patch"` + non-`"rejected"` disposition 400 authority-violation case. Read
   directly — this is exactly the smoke-layer route-contract coverage the design
   blueprint prescribes for a new Hono route; no gap.
4. `task-store/src/pull-request-service.unit.test.ts` (+112 lines) and
   `pull-request-service.integration.test.ts` (+107 lines) extended for
   `appendFinding()` — unit tests cover input validation, integration tests cover the
   real-Postgres INSERT path (findUnique-then-create) and the `NotFoundError` path for a
   nonexistent `prId`. No gap.
5. Re-ran the isolation-contract grep sweep (`mock.module(`, `global.fetch =`,
   `global.console =`, `DATABASE_URL` inside `*.unit.test.ts`) across the full current
   suite — no new violations (the delta's only new file, `pr-finding.integration.test.ts`,
   correctly uses `DATABASE_URL_SHIPWRIGHT_TASK_STORE_TEST`, a `.integration.test.ts` file,
   not a unit-file violation).
6. Pulled a fresh live Tier 1 speed + Step 4c coverage measurement from CI (`gh run view
   --log` against the green `lint / typecheck / test` run at this cycle's exact HEAD,
   `f901803d`) — see Speed measurement below.
7. Spot-checked the prior cycle's carried-forward non-gap items against current file
   contents: `admin/src/clock.ts` (still no dedicated unit test file, unchanged, still a
   non-gap per the repo's established `Clock`-file convention).

No open items were carried forward from the 2026-08-16 pass — that cycle closed its two
longest-standing open items (HITL bootstrap Net-new, `check-review.ts` combinatorial
Promote/deepen) and this cycle's delta introduced no new gaps.

## Prior cycle's method note (2026-08-16, preserved for continuity)

This pass diffed the current file tree against the 2026-08-14 pass's findings (baseline
doc committed at `297fb61e`) and re-verified every open item directly against current
repo state — per this cycle's standing instruction not to carry forward "gap"/"missing"
claims without re-checking them, with particular scrutiny on the two carried-forward open
items from last cycle (the `check-review.ts` combinatorial-coverage Promote/deepen item,
and the HITL local dev-loop bootstrap Net-new item) and this cycle's new dual
coverage-gate tooling delta:

1. Enumerated every test file via the Phase 2 framework matrix's glob patterns: 132
   `*.unit.test.ts` (+5), 65 `*.integration.test.ts` (+2), 43 `*.smoke.test.ts`
   (unchanged), 41 `*.content.test.ts` (+2) (281 `bun test`-scanned files, up from 272),
   plus 21 `site/tests/*.spec.ts` (unchanged) and 3 Playwright `*.e2e.ts` (2 `admin/e2e/`,
   1 `metrics/e2e/`, unchanged) = **305 test files total** — +9 from the 2026-08-14 pass,
   confirmed via `git diff --stat 297fb61e..HEAD --diff-filter=A -- '*.unit.test.ts'
   '*.integration.test.ts' '*.smoke.test.ts' '*.content.test.ts' '*.spec.ts' '*.e2e.ts'`
   (exactly 9 additions, 0 deletions, each individually confirmed absent at the `297fb61e`
   baseline via `git show 297fb61e:<path>`, not just inferred from the diff stat):
   `plugins/shipwright/skills/repo-config/SKILL.content.test.ts`,
   `plugins/shipwright/skills/test-inventory/SKILL.content.test.ts`,
   `scripts/check-coverage-gate.unit.test.ts`,
   `scripts/check-coverage-no-decrease.integration.test.ts`,
   `scripts/check-coverage-no-decrease.unit.test.ts`,
   `scripts/check-coverage.unit.test.ts` (genuinely new — the file did not exist before
   this cycle, despite `check-coverage.ts` itself being a long-standing row),
   `scripts/coverage-tool-dispatch.unit.test.ts`, `scripts/hitl.bootstrap.unit.test.ts`,
   `scripts/hitl.integration.test.ts`.
2. Cross-referenced Phase 1's delta list (46 commits / 57 files since the `297fb61e`
   baseline, ~13 non-doc/non-version-bump/non-chart source files) against the test-file
   glob to confirm each has matching coverage, reading the actual test body for the
   files with the most consequential changes: the new dual-coverage-gate cluster
   (`check-coverage-gate.ts`, `check-coverage-no-decrease.ts`, `coverage-tool-dispatch.ts`),
   `scripts/check-coverage.ts`'s `CoverageParser` refactor, `scripts/hitl.ts`'s
   planner/executor split, `scripts/check-config-docs.ts`'s `collectTsFiles` export.
3. **Directly investigated the carried-forward HITL local dev-loop bootstrap Net-new
   item** (open since 2026-07-31 — the pure planning helpers were fully unit-tested but
   no integration-level test exercised the full clone→seed→boot→poll sequence).
   Confirmed **resolved this cycle**: T-080 (PR #2644, part of MTC-1.7's #2659) split
   `scripts/hitl.ts` into pure planners (`buildHitlConfig`, `buildPreflightSteps`,
   `buildServiceSpecs`) plus a thin injected executor (`runSteps`), covered by the new
   `scripts/hitl.bootstrap.unit.test.ts` (593 lines) at the **unit** layer — not the
   integration layer this doc's prior cycles had prescribed. This was a deliberate,
   human-reviewed HITL decision recorded in the newly-populated
   `.claude/shipwright/test-readiness-decisions.md` ("scripts/hitl.ts bootstrap sequence
   — planner/executor split, unit layer not integration"), not a classifier default: the
   registry entry's own rationale is that "proven correct end-to-end" and "no real …
   long-running service processes spawned" are self-contradictory acceptance criteria,
   so the honest layer for what's actually testable without Postgres is unit. Separately,
   `scripts/hitl.integration.test.ts` (308 lines, also landed via #2659) drives the real
   `buildPreflightSteps()` clone sub-sequence through `runPreflight()` and covers a
   failing-clone-step-stops-the-sequence case — the thin executor half, at the layer this
   doc's prior cycles expected. Read both new test files directly (not just the
   filenames) to confirm they exercise composed sequencing and failure propagation, not
   just individual planner-function unit shape. **This closes the repo's longest-standing
   open Net-new item** (carried forward across every cycle since 2026-07-31).
4. **Directly investigated the carried-forward `check-review.ts` combinatorial-coverage
   Promote/deepen item** (open since 2026-08-14 — pairwise "even when X" combination
   coverage existed, but no test exercised 3+ of the fixed exclusion checks
   simultaneously). Confirmed **resolved this cycle**: T-082 (PR #2641) added 3 new test
   cases to `check-review.unit.test.ts` (now 101 test cases) stacking 3+ of the flagged
   exclusion conditions — stale `commitSha`/`reviewedCommitSha` divergence,
   review-thread-reply freshness, and task-blocked state — in single fixtures, per the
   commit's own description: "proving the documented precedence order holds under
   combination, not just individually." Read the new test cases directly to confirm they
   genuinely combine 3+ conditions in one fixture rather than three separate two-condition
   cases. **This closes the repo's second-longest-standing open item** (Promote/deepen
   since 2026-08-14, originally flagged by Phase 2's gap #18 on 2026-08-11/14).
5. **New this cycle: bucketed the dual coverage-gate test cluster.** All 6 new test files
   backing the coverage-gate/dispatch feature (`check-coverage-gate.unit.test.ts`,
   `check-coverage-no-decrease.{unit,integration}.test.ts`,
   `coverage-tool-dispatch.unit.test.ts`, plus the extended `check-coverage.unit.test.ts`)
   were read for layer-correctness: the pure comparison/computation logic
   (`evaluateCoverageDelta`, `computeCoverageGate`, `extractCoverageTool`/`selectParser`,
   the five `CoverageParser` implementations) is unit-tested; the base-ref
   worktree-checkout driver in `check-coverage-no-decrease.ts` has a dedicated, correctly
   thin `*.integration.test.ts` (22 lines) rather than being folded into the unit file.
   Both land at the canonical layer per the inventory's own split classification (see
   `test-inventory.md`). No layer mismatch found.
6. Re-ran the isolation-contract grep sweep (`mock.module(`, `global.fetch =`,
   `global.console =`, `DATABASE_URL` inside `*.unit.test.ts`) across the full current
   suite (305 files) — zero new violations. 41 `mock.module(` string matches across 24
   files, all manually confirmed to be inside `*` or `//` comment lines documenting the
   *absence* of mocking, not executable calls. Zero `global.fetch =`/`global.console =`
   assignments. Same three pre-existing non-violation `DATABASE_URL` string-literal
   matches as every prior pass (`admin/src/main.unit.test.ts`,
   `admin/src/schema.unit.test.ts`, `scripts/dev-tmux.unit.test.ts`).
7. Pulled a fresh live Tier 1 speed + Step 4c coverage measurement from CI (`gh run view
   --log` against the green `lint / typecheck / test` run at this cycle's exact HEAD,
   `ca3c8c3f`) — see Speed measurement below.
8. Spot-checked every other carried-forward gap item (#1-#17, #19 from prior cycles)
   that Phase 2 restated unchanged, directly against current file contents rather than a
   prior doc's snapshot: `browser.integration.test.ts` (present, unchanged),
   `admin/src/clock.ts` (still no dedicated unit test file, unchanged, still a non-gap
   per the repo's established `Clock`-file convention), and the
   `pr-state-reconciler.ts` orphan-reconciliation removal (PR #2527, closed as resolved
   in a prior cycle — reconfirmed via grep that zero references to the removed functions
   have resurfaced).

**Speed measurement: re-run this pass via CI history (Tier 1 + Step 4c coverage).** No
local Postgres in this sandbox, so a local `bun test` run was not attempted — Tier 1's
aggregate pull and Step 4c's coverage-percentage pull both need only `gh run view --log`
against the most recent green `lint / typecheck / test` job, which this sandbox does
have, at this cycle's exact HEAD commit `ca3c8c3f`.

## The `pr-state-reconciler.ts` orphan-reconciliation removal — resolved (2026-08-11)

Closed in the 2026-08-11 pass: the orphan-task reconciliation pass removal (547 lines of
production + test code) was confirmed a deliberate, reviewed, well-tested clean removal
(PR #2527, DTC-1.1), superseded by `StaleClaimReaper` + the ready-set FIFO +
`dev-task.md` Step 4's Branch/PR Reality Check. Not restated in full here — see the
2026-08-11 version of this document (`git log -p -- docs/test-readiness/test-migration.md`)
for the full investigation writeup. No new evidence this cycle changes that verdict.

## Summary

| Bucket | Test count | Infra count | Effort |
|---|---|---|---|
| Reuse as-is | ~403 | 7 | — |
| Promote / deepen | 1 | 0 | trivial |
| Rebuild | 0 | 0 | — |
| Delete (redundant) | 0 | 0 | — |
| Net-new | 1 | 0 | trivial, optional |

**Total existing test files discovered (this cycle, 2026-09-21, ground-truth recount via
`find . -name "*.<suffix>.test.ts"`, cross-checked against this cycle's live CI run's own
"370 files" scan report — see Speed measurement below):** 187 unit + 83 integration + 54
smoke + 46 content = **370 `bun test`-scanned files**, + 26 `site/tests/*.spec.ts` + 7
Playwright `*.e2e.ts` (6 `admin/e2e/`, 1 `metrics/e2e/`) = **403 test files total** — up
from the prior pass's 401 (+2 net): `agent/src/pr-origin-backfill.unit.test.ts` (new,
backing the new POB-1.1 inventory row) and `agent/src/sessions.unit.test.ts` (new, backing
the DTW-1.3 companion prune-wiring fix) — both verified directly via `git log
--diff-filter=A` against the `eff5b503..08cab0b33` range, matching exactly (no other new
test files this cycle, consistent with the small 6-non-routine-commit delta).

**Promote/deepen gains its first finding in eight cycles this pass** — see Method note
step 3 above and Tests — bucketed below. Rebuild remains empty.

**Headline finding:** the one genuinely new inventory item this cycle
(`agent/src/pr-origin-backfill.ts`) landed with adequate, correctly-layered test coverage
in the same PR as the production code, continuing the pattern observed in every prior
pass. The three touched-in-place rows (dashboard KPI swap, Sessions Follow button,
dev-task resume-loop wiring) were each verified to have their paired test file extended
in the same commit, not merely diff-stat-inferred. The one real finding this cycle is
narrower and process-facing rather than coverage-facing: the `#3545` worktree-path
fallback fix closed a bug that had recurred 5+ times, but landed with no accompanying
regression-guard assertion in `dev-task.content.test.ts` to prevent a sixth recurrence —
flagged as Promote/deepen, not a gap in this cycle's own delta (the fix itself is correct
and this document's job is coverage of current behavior, not retroactively grading past
incidents), but cheap enough to fix now while the context is fresh. No rebuild
candidates, no delete/trim candidates, no new isolation-contract regressions (see
Isolation contract audit below — grep counts for `mock.module(`/`DATABASE_URL` are
byte-for-byte unchanged from the 2026-09-18 pass).

### Prior cycle's summary narrative (2026-09-18, preserved for continuity)

| Bucket | Test count | Infra count | Effort |
|---|---|---|---|
| Reuse as-is | ~400 | 7 | — |
| Promote / deepen | 0 | 0 | — |
| Rebuild | 0 | 0 | — |
| Delete (redundant) | 0 | 0 | — |
| Net-new | 1 | 0 | trivial, optional |

**Total existing test files discovered (this cycle, 2026-09-18, ground-truth recount via
`find . -name "*.<suffix>.test.ts"`, matching CI's own "368 files" scan report exactly):**
185 unit + 83 integration + 54 smoke + 46 content = **368 `bun test`-scanned files**, +
26 `site/tests/*.spec.ts` + 7 Playwright `*.e2e.ts` (6 `admin/e2e/`, 1 `metrics/e2e/`) =
**401 test files total** — up from the prior pass's 349 (+52 net, spanning the 16-day/
246-commit gap): ~22 new files backing new Phase 1 inventory rows across the clusters in
this cycle's Method note (Sessions alone contributes 7), the two new content-category
reference-doc tests (`dependency-patch.content.test.ts`,
`dependency-risk-analysis.content.test.ts`), 2 new marketing-site specs
(`autonomy.spec.ts`, `vs-openhands.spec.ts`), 1 new `.claude/commands/docs-sync.content.test.ts`,
minus 1 deleted (`triage-dependency-bot-pr/SKILL.content.test.ts`, retired alongside its
skill, DBR-3.4) — plus a handful of first-time layer additions to already-inventoried
rows not separately itemized here (see the per-cluster rows above for detail). The prior
pass's 349 total itself included two novel dependency-runtime regression-test files with
no corresponding source-code row (`agent/src/slack-bolt-client.integration.test.ts`,
`agent/src/slack-socket-mode-ping.integration.test.ts`) — unchanged this cycle, still
correctly bucketed Reuse.

**Promote/deepen and Rebuild remain empty for a seventh consecutive cycle** — no open
items were carried in from the 2026-09-02 pass (it closed clean).

**Headline finding:** despite this cycle's unusually large 246-commit/16-day gap — by far
the largest window this pipeline has processed — every real source change already has
matching test coverage at the correct layer, landed in the same commits/PRs as the
production changes, continuing the pattern observed in every prior pass. The one
noteworthy verification finding (`task-store/src/pr-origin-derivation.ts` initially
looked like it might lack a dedicated test file) turned out, on direct inspection, to
have comprehensive coverage colocated in a sibling service's test file — a minor
file-naming-convention deviation, not a coverage gap (see Method note step 2). No open
items carried in, no new open items raised, no rebuild candidates, no unexplained
trim/delete candidates beyond the accounted-for dependency-bot-triage retirement (Method
note above), no redundant-coverage pattern, no new isolation-contract regressions.

### Prior cycle's summary narrative (2026-09-02, preserved for continuity)

**Total existing test files discovered (2026-09-02 cycle):** 157 unit + 71
integration + 47 smoke + 43 content = 318 `bun test`-scanned files, + 24
`site/tests/*.spec.ts` + 7 Playwright `*.e2e.ts` (6 `admin/e2e/`, 1 `metrics/e2e/`) =
**349 test files total** — net +26/−1 from the 2026-08-30 pass's 324 (26 added, verified
via `git diff --diff-filter=A ac0ec6a8..HEAD`: the 19 files backing new inventory rows
from the six feature clusters in that cycle's Method note, plus 4 first-time unit-test
files for already-inventoried source rows that previously had only integration/smoke
coverage — `agent/src/cron-handler.unit.test.ts`, `chat/src/message-service.unit.test.ts`,
`metrics/src/providers/task-store-provider.unit.test.ts`,
`admin/src/http-chat-client.unit.test.ts` — plus 2 genuinely novel dependency-runtime
regression-test files with no corresponding source-code row:
`agent/src/slack-bolt-client.integration.test.ts` and
`agent/src/slack-socket-mode-ping.integration.test.ts`, both added as direct regression
coverage for two real, already-shipped-and-fixed incidents in the installed
`@slack/bolt`/`@slack/socket-mode` npm packages themselves (a Bolt-internal nested
`@slack/web-api` resolution gap, #2997/#3003, and Bun's `undici` polyfill lacking the
`ping()`/diagnostics_channel API Bolt 5's socket-mode heartbeat depends on, SSB-1.1,
#3006/c7f9d03b) — a new, narrow pattern flagged at the time: these two files test
*installed dependency behavior under this repo's exact runtime* rather than first-party
source code, so they have no Phase 1 inventory row of their own by design — **Reuse**,
correctly scoped as integration-layer dependency-contract tests, not miscategorized.
−1 removed: `check-dependabot-triage.unit.test.ts`, a rename (not a deletion) paired
with `398c71ed`'s script rename.

**Headline finding (2026-09-02):** every real source change in that cycle's unusually
large 51-commit Phase 1 delta already had matching test coverage at the correct layer,
landed in the same commits/PRs as the production changes.

### Prior cycle's summary narrative (2026-08-30, preserved for continuity)

**Total existing test files discovered (2026-08-30 cycle):** 139 unit + 67
integration + 45 smoke + 42 content = 293 `bun test`-scanned files, + 24
`site/tests/*.spec.ts` + 5 Playwright `*.e2e.ts` (4 `admin/e2e/`, 1 `metrics/e2e/`) =
**324 test files total.** This pass added `admin/e2e/chat-progress.e2e.ts` and
`admin/e2e/chat-degraded.e2e.ts` for CFB-2.3 live-progress assertions (elapsed ticker
and stall-warning behavior) plus extended `admin/e2e/test-server.ts` with live-progress
fixtures. All new/extended files back new production code at the correct canonical layer
(e2e for behavior-driven browser assertions on elapsed-ticker and stall guarantees).
**Promote/deepen and Rebuild remain empty for a fifth consecutive cycle** — no open items
were carried in from the 2026-08-28 pass (it closed clean).

### Prior cycle's summary narrative (2026-08-28, preserved for continuity)

**Total existing test files discovered (this cycle, 2026-08-28):** 135 unit + 67
integration + 45 smoke + 42 content = 289 `bun test`-scanned files, + 24
`site/tests/*.spec.ts` + 3 Playwright `*.e2e.ts` (2 `admin/e2e/`, 1 `metrics/e2e/`) =
**316 test files total** — net +7 from the 2026-08-25 pass's 309 (+8 new, verified via
`git diff --diff-filter=A d4bfce02..7a688190`: `task-store/src/app.readiness.smoke.test.ts`,
`task-store/src/main.unit.test.ts`, `scripts/check-downstream-compat.smoke.test.ts`,
`plugins/shipwright/test/cron-tables.content.test.ts`, `site/tests/docs-redirect.spec.ts`,
`site/tests/docs-security.spec.ts`, `site/tests/docs-slack-integration.spec.ts`,
`site/tests/vs-factory.spec.ts`; −1 deleted: `site/tests/docs-landing.spec.ts`, paired
with its subject page's removal). All 8 new files fold into "Reuse as-is" — each backs
new production code or content landed in the same commit/PR, at the correct canonical
layer. **Promote/deepen and Rebuild remain empty for a fourth consecutive cycle** — no
open items were carried in from the 2026-08-25 pass (it closed clean).

### Prior cycle's summary narrative (2026-08-25, preserved for continuity)

**Total existing test files discovered (2026-08-25 cycle):** unchanged file count from
the 2026-08-23 pass's 309 — this cycle's 5-commit delta added assertions to 5
already-existing files (`dev-task.content.test.ts`, `plan-session.content.test.ts`,
`review.content.test.ts`, `code-reviewer.unit.test.ts`, `manifest.content.test.ts`) and
edited one existing case in-place (`check-consolidation-patrol.unit.test.ts`) rather than
adding or removing test files. **Promote/deepen and Rebuild remain empty for a second
consecutive cycle** — no open items were carried in from the 2026-08-23 pass (it closed
clean).

### Prior cycle's summary narrative (2026-08-23, preserved for continuity)

**Total existing test files discovered (2026-08-23 cycle):** unchanged file count from
the 2026-08-22 pass's 309 — this cycle's 2-commit delta added assertions to 2
already-existing files (`agent/src/slack.unit.test.ts`,
`agent-slack-membership-ref.unit.test.ts`) rather than adding or removing test files.
**Promote/deepen returned to empty** — the sole item carried from the 2026-08-22 pass
(`agent-slack-membership-ref.unit.test.ts`'s missing `afterEach` reset) resolved that
cycle by PR #2790 (T-084).

### Prior cycle's summary narrative (2026-08-22, preserved for continuity)

**Total existing test files discovered (2026-08-22 cycle):** 134 unit + 67
integration + 43 smoke + 41 content = 285 `bun test`-scanned files, + 21
`site/tests/*.spec.ts` + 3 Playwright `*.e2e.ts` (2 `admin/e2e/`, 1 `metrics/e2e/`) =
**309 test files total** — net +2 from the 2026-08-18 pass's 307 (+5 new:
`okta-auth-client.integration.test.ts`, `github-app-provisioning-client.{unit,integration}.test.ts`,
`agent-slack-membership-ref.unit.test.ts`, `pr-transition-diff.unit.test.ts`; −2 deleted:
`check-coverage-no-decrease.{unit,integration}.test.ts`, paired with their source
file's removal, `2ab1a4cb`). 4 of the 5 new files fold into "Reuse as-is"; one moved to
Promote/deepen (the singleton-leak finding this cycle resolves — see above).

### Prior cycle's summary narrative (2026-08-18, preserved for continuity)

**Total existing test files discovered (this cycle, 2026-08-18):** 132 unit + 66
integration + 44 smoke + 41 content = 283 `bun test`-scanned files, + 21
`site/tests/*.spec.ts` + 3 Playwright `*.e2e.ts` (2 `admin/e2e/`, 1 `metrics/e2e/`) =
**307 test files total** — +2 from the 2026-08-16 pass
(`task-store/src/pr-finding.integration.test.ts`, backing the new `PrFinding` model,
correctly landed at the integration layer alongside its production code, and
`task-store/src/state-filter.smoke.test.ts`, backing the new `?hitl` filter, whose
count was previously omitted from the smoke bucket — corrected here to match the
CI-reported 283-file total). Both fold into "Reuse as-is" (already-correct,
already-passing coverage for this cycle's delta). The Promote/deepen and Rebuild
buckets remain empty for a second consecutive cycle (first reached 2026-08-16). Only
the long-standing, explicitly-optional `admin/src/clock.ts` item remains in Net-new.

**Headline finding:** every real source change in this cycle's 16-commit / 53-file Phase
1 delta already has matching test coverage at the correct layer, landed in the same
commits/PRs as the production changes — continuing the pattern observed in every prior
pass. No open items were carried in from the 2026-08-16 pass (it closed its own two
longest-standing items). No new isolation-contract regression, no new redundant-coverage
pattern, no rebuild candidates, no trim/delete candidates.

### Prior cycle's summary narrative (2026-08-16, preserved for continuity)

**Total existing test files discovered:** 132 unit + 65 integration + 43 smoke + 41
content = 281 `bun test`-scanned files, + 21 `site/tests/*.spec.ts` + 3 Playwright
`*.e2e.ts` (2 `admin/e2e/`, 1 `metrics/e2e/`) = **305 test files total** — +9 from the
2026-08-14 pass (the dual-coverage-gate test cluster: `check-coverage-gate.unit.test.ts`,
`check-coverage-no-decrease.{unit,integration}.test.ts`,
`coverage-tool-dispatch.unit.test.ts`, `check-coverage.unit.test.ts`, plus
`hitl.bootstrap.unit.test.ts`, `hitl.integration.test.ts`, and two `.content.test.ts`
companions to skill-doc extensions), all new files backing new production code or closing
a previously-flagged gap, correctly landed — not open Net-new work items. These 9 fold
into "Reuse as-is" (already-correct, already-passing coverage for this cycle's delta).
**The Promote/deepen and Net-new buckets both drop to their smallest counts in this
document's history this cycle**: the `check-review.ts` combinatorial-coverage item
(Promote/deepen since 2026-08-14) resolved via T-082 and moved to Reuse as-is (Method
note step 4); the HITL local dev-loop bootstrap item (Net-new since 2026-07-31, the
longest-standing open item in this document) resolved via T-080/#2659 and moved to Reuse
as-is (Method note step 3). Only the long-standing, explicitly-optional
`admin/src/clock.ts` item remains in Net-new.

## Isolation contract audit

Grepped the full current suite (403 files) for the three hard-rule signals from root
`CLAUDE.md`'s Test isolation section and `references/principles.md`'s `t1_no_global_mocking`:

| Signal | Files matched | Real violations |
|---|---|---|
| `mock.module(` | 53 files — **byte-for-byte unchanged from the 2026-09-18 pass**, re-verified by direct recount; neither new file this cycle (`pr-origin-backfill.unit.test.ts`, `sessions.unit.test.ts`) adds a hit | **0** — every hit re-verified line-by-line to be a comment (`*`/`//`-prefixed) documenting compliance ("no `mock.module()`..."), not an executable call. Confirmed via `grep -vE ':\s*(\*\|//)'` returning zero matches. |
| `global.fetch = ` (assignment) | 0 | **0** |
| `global.console = ` (assignment) | 0 | **0** |
| `DATABASE_URL` inside `*.unit.test.ts` | 6 — **unchanged from the 2026-09-18 pass**, re-verified by direct recount | **0** — unchanged from the prior pass's spot-check findings. |

**No new isolation-contract findings this cycle (2026-09-21)** — `pr-origin-backfill.unit.test.ts`'s
own header comment explicitly states "no `mock.module()`, no `gh` CLI, no
`global.fetch`/`global.*` overrides" (verified true by direct read); `sessions.unit.test.ts`
uses only injected store/`setInterval` doubles, no globals. Both grep signals are
byte-for-byte unchanged from the 2026-09-18 pass. Neither new file introduces a
module-level singleton requiring an `afterEach` reset. The widened pattern-set
recommendation (scanning for `.set(`/`.reset(` calls on named singleton-ref exports with
no paired `afterEach`) remains **not implemented as an automated sweep** — carried
forward unchanged as a standing process-improvement recommendation (see Next step below).

**Prior cycle's isolation-contract note (2026-09-18, preserved for continuity).** All new
hits that cycle (mostly Sessions-feature test files) were comment-only compliance
documentation or consistent with the established injected-fake pattern, confirmed by
direct recount against the then-current 401-file suite, not assumed carried-forward. The
shared-singleton mutation leak flagged 2026-08-22 and resolved 2026-08-23 remains closed
— no new singleton-ref files (a mutable box with module-level state, like
`agent-slack-membership-ref.ts`) were added that cycle: `admin/src/session-scope.ts` and
the new `*-ref.ts` files (`patch-author-allowlist-ref.ts`) were checked and are either
pure stateless predicates or per-call-constructed, not module-level singletons requiring
an `afterEach` reset.

## Delete (redundant assertion) analysis

**Zero delete/trim candidates identified this cycle (2026-09-21)** — the two new test
files (`pr-origin-backfill.unit.test.ts`, `sessions.unit.test.ts`) were checked against
their nearest structural siblings (`pr-census.unit.test.ts`, `sessions.integration.test.ts`
respectively) for overlap: `pr-origin-backfill.unit.test.ts` explicitly delegates
classification to `pr-census.ts` rather than reimplementing it (see Method note step 1)
and asserts only its own orchestration/chunking/error-isolation logic — no duplicate
assertion of `pr-census.ts`'s own classification-precedence tests; `sessions.unit.test.ts`
asserts the new `prune()`-wiring call path (injected store/timer), distinct from
`sessions.integration.test.ts`'s real-file-I/O-backed store behavior. Same finding as
every prior pass.

### Prior cycle's redundant-assertion analysis (2026-09-18, preserved for continuity)

The Sessions feature's new test files (`session-service`,
`session-rollup`, `session-retention-reaper`, `session-alert-sweeper`,
`session-follow-service`, `session-scope`) were spot-checked pairwise for overlap given
they span two workspaces (task-store + admin) covering related concepts — each asserts
a distinct property (task-store's `session-service.ts` owns persistence/rollup
aggregation; admin's `session-alert-sweeper.ts`/`session-follow-service.ts` own
notification delivery and subscription state) with no duplicate assertion found. The
`pr-origin-derivation.ts`/`pull-request-service.unit.test.ts` colocation (Method note
step 2) was also checked for redundancy against `pull-request-service.unit.test.ts`'s
own `claim()`-level origin-stamping tests — confirmed complementary, not duplicate: the
`deriveOrigin()` describe block proves the pure precedence table in isolation, while the
`claim()` tests prove it's wired in correctly end-to-end, per this document's own
layer-hierarchy criterion (higher-layer tests are kept only when they assert what the
lower layer cannot).

### Prior cycle's redundant-assertion analysis (2026-09-02, preserved for continuity)

This cycle's four
first-time-unit-test-coverage additions for already-inventoried files
(`cron-handler.unit.test.ts`, `message-service.unit.test.ts`,
`task-store-provider.unit.test.ts`, `http-chat-client.unit.test.ts`) were each spot-checked
against their existing integration/smoke siblings — every case confirmed to exercise a
distinct property (pure/injected-fake logic at the unit layer vs. real-boundary behavior
at the higher layer already in place), not a re-assertion of what the higher layer already
proves. `stall-reaper.unit.test.ts` was spot-checked against
`task-store/src/stale-claim-reaper.unit.test.ts` (its structural sibling) for the same
reason — proportionate depth confirmed, not a shallower duplicate. The two new
dependency-runtime regression files (`slack-bolt-client.integration.test.ts`,
`slack-socket-mode-ping.integration.test.ts`) assert properties no other file in the
suite touches (real Bolt/socket-mode client behavior under Bun's runtime) — no overlap
with `slack.unit.test.ts`/`slack-progress.unit.test.ts`, which exercise this repo's own
wrapper logic against fakes, not the installed dependency's real behavior.

## Tests — bucketed

### Reuse as-is

The large majority of the suite, unchanged in shape from the 2026-08-11 pass aside from
the narrow delta below. Grouped by cluster; each cluster was checked against Phase 1's
delta list, not re-read in full where unchanged.

| Cluster | Test files | Layer(s) | Functional unit | Notes |
|---|---|---|---|---|
| `lib/` | 13 files (re-verified via `find lib -maxdepth 1 -name "*.test.ts"`; +`cli-flags.unit.test.ts`, +`graceful-shutdown.unit.test.ts`, new) | unit | Cross-service pure helpers | **This cycle (2026-09-18):** new `cli-flags.ts` (`parseFlags`, CFH-1.2) and `graceful-shutdown.ts` (SIGTERM/SIGINT coordinator) each covered by a paired `.unit.test.ts` — **Reuse**, correct canonical unit layer, see Method note step 6. **Prior cycle (2026-09-02):** new `chat-notify.ts` (shared pure chat-reply-notification payload-shaping helper, CFB-4.3) covered by new **`chat-notify.unit.test.ts`** — **Reuse**, new file backing new production code, correct canonical unit layer, see Method note step 6. **Prior cycle (2026-08-30):** new `progress-phases.ts` (closed 11-phase `ProgressPhase` enum, CFB-2.1) covered by new **`progress-phases.unit.test.ts`** — **Reuse**, new file backing new production code, correct canonical unit layer, see Method note step 1. |
| `metrics/src/*` | 10 files at top-level (re-verified via `find metrics/src -maxdepth 1 -name "*.test.ts"`; +`api.merged-prs.smoke.test.ts`, new) plus `lib/`/`providers/`/`dashboard/` subdirectories | unit/integration/smoke | Formatters, provider selection, HTTP clients, `/metrics/*` routes, request coalescing, dashboard feature-grouping, **merged-PRs-by-repo panel/endpoint (new, POM-4.1)**, **Shipwright-PRs-Merged KPI (replaces Estimation Accuracy, MPC-1.1)** | **This cycle (2026-09-21):** `task-store-provider.integration.test.ts` gained a targeted fixture (two matching PRs + a near-miss for each predicate conjunct, MPC-1.1/`#3534`) after an initial review finding that the only existing assertion for the new `shipwright_prs_merged` aggregation passed regardless of predicate correctness (an existence-only false-confidence signal, closed in the same PR) — **Reuse**, in-place strengthening confirmed via `git log`, no bucket change, see Method note step 3. **Prior cycle (2026-09-18):** new `api.merged-prs.smoke.test.ts` covers the new merged-PRs-by-repo endpoint (`f2b8768a`) backing the new dashboard panel (`0feb6428`) — **Reuse**, correct canonical smoke layer for a route wire-contract test. **Prior cycle (2026-09-02):** `metrics/src/providers/task-store-provider.ts` gains its first unit-layer coverage via new **`task-store-provider.unit.test.ts`** (218 lines), landed alongside PTL-2.2's fix deriving dashboard feature-grouping from tasks directly rather than the deprecated `pr.taskId` — **Reuse**, a genuine layer addition (the existing `.integration.test.ts` also extended in place), not a duplicate; the split proves the pure grouping-derivation logic in isolation vs. the integration file's real task-store-backed behavior. |
| `agent/src/*` | 65 files (re-verified via `find agent/src -maxdepth 1 -name "*.test.ts"`; +`pr-origin-backfill.unit.test.ts`, +`sessions.unit.test.ts` — 2 new files this cycle) | unit/integration/smoke | Work selection, check-\* precondition scripts, Slack Bolt handlers, `claude.ts` spawner, chat/task-store/GitHub clients, chat-poller loop, headless-browser launcher, **PR-origin historical backfill (new, POB-1.1)**, **session-store prune wiring (new, DTW-1.3 companion)** | **This cycle (2026-09-21):** see Method note steps 1 (`pr-origin-backfill.ts`) and 2 (`sessions.ts` prune wiring) for the two new files — both **Reuse**, correct canonical unit layer, paired test verified directly for each; `check-helpers.unit.test.ts` extended in place for the DTW-1.3 resume-loop wiring (`#3526`), confirmed via `git log` — **Reuse**, see Method note step 3. **Prior cycle (2026-09-18):** see Method note steps 3 for the five new files (`check-plan.ts`, `github-auth-startup.ts`/`github-auth-deps.ts`, `patch-author-allowlist-ref.ts`, `slack-startup.ts`, `pr-census.ts`) — all **Reuse**, correct canonical layer, paired test verified directly for each. `review-author-allowlist-ref.ts` (renamed from `agent-author-allowlist-ref.ts`, DBR-1.x/2.x) — existing row extended/renamed in place, no new row. **Prior cycle (2026-09-02):** new `agent/src/claim-invariant-reconciler.ts` (task `status`/`claimedBy` self-heal pass, defense-in-depth alongside a new DB CHECK constraint) covered by new **`claim-invariant-reconciler.unit.test.ts`** — **Reuse**, canonical integration-equivalent-but-unit-tested layer matching the sibling `pr-state-reconciler.ts` precedent, see Method note step 4. New `agent/src/slack-progress.ts`/`slack-thread-status-tracker.ts` (SAV-1.1, Slack Bolt 5.x upgrade + migration to `agents.sessions.setStatus`, superseding the CFB-2.3/2.4/5.1-era in-line thread-message-editing logic) covered by new **`slack-progress.unit.test.ts`** (extended twice more within this window as the migration landed in stages) and **`slack-thread-status-tracker.unit.test.ts`** — **Reuse**, both new files backing new production code, correct canonical layers, see Method note step 5. Two novel dependency-runtime regression files, **`slack-bolt-client.integration.test.ts`** and **`slack-socket-mode-ping.integration.test.ts`**, added as direct regression coverage for two real production incidents in the installed `@slack/bolt`/`@slack/socket-mode` npm packages under Bun's runtime (a nested `@slack/web-api` resolution gap, and Bun's `undici` polyfill lacking the `ping()`/diagnostics_channel API Bolt 5's heartbeat needs) — **Reuse**, correctly scoped integration-layer dependency-contract tests with no first-party source row of their own by design, see Summary note above. `agent/src/cron-handler.ts` gains an injectable spawner for its `preCheck` invocation (de-flaking), covered by new **`cron-handler.unit.test.ts`** (first unit-layer coverage for this previously smoke-only-tested file — a genuine layer addition, not a duplicate, since the new file exercises `preCheck`'s spawner-injection logic in isolation while the existing smoke test still exercises the full HTTP wire contract) — **Reuse**. **Prior cycle (2026-08-30):** new `progress-milestones.ts` (`phaseForBlock()`, CFB-2.1) covered by new **`progress-milestones.unit.test.ts`**, including an explicit assertion that `block.input` is never read (the file's load-bearing anti-leak invariant) — **Reuse**, new file backing new production code, correct canonical unit layer, see Method note step 1. `claude.unit.test.ts` extended (+222/-lines) for the widened `ProgressCallback`. `pr-state-reconciler.unit.test.ts` extended (+176/-lines) for the batch+rotating-cursor scan (#2893); `check-deploy.unit.test.ts` extended (+56/-lines) for case-insensitive workflow-name matching (#2896); `chat-poller.unit.test.ts` extended (+220/-lines, the largest single-file extension this cycle) for the heartbeat-driven reply timeout (#2899/#2902) — all in-place extensions to already-migrated, already-correct-layer unit rows, see Method note steps 1-4. **Prior cycle (2026-08-28):** `check-review.ts` gains `EXCLUDED_BOT_AUTHORS` (Renovate exclusion, generalizing the prior single-hardcoded-`app/dependabot` check), `check-helpers.ts`/`check-deploy.ts`/`pr-state-reconciler.ts` gain live getter-function policy-flag reads (PLR-1.1), `loop-orchestrator.ts` gains phase-scoped redispatch cooldown keys (PRC-1.1) — all in-place extensions to already-migrated, already-correct-layer unit rows (`check-review.unit.test.ts` +29, `check-helpers.unit.test.ts` +40, `check-deploy.unit.test.ts` +84, `pr-state-reconciler.unit.test.ts` +55, `loop-orchestrator.unit.test.ts` +34) — see Method note steps 3-5. **Prior cycle (2026-08-23):** `slack.ts` extended with the SMR enforcement gate (`isAllowedSlackSender`/`shouldRejectSlackSender`, PR #2788), 13 new assertions in `slack.unit.test.ts` — **Reuse**, no new row (extends the already-migrated route-equivalent row). `agent-slack-membership-ref.unit.test.ts` moves back to **Reuse** — its singleton-leak Promote/deepen finding from last cycle is resolved (PR #2790, T-084) — see Method note. **Prior cycle (2026-08-22):** `check-review.ts`/`compute-unaddressed-findings.ts` extended across an 8-commit review-candidacy fix cluster (author-reply scoping, `headRefOid` re-fetch race, ledger-timestamp trigger, unresolved-threads author-reply exclusion) — spot-checked 3 of 8, each adds a matching case, no gap, stays Reuse. **2026-08-18 cycle:** `setup.ts`'s `runMiseStartup` gains GOPATH/GOMODCACHE/npm/bun cache-pinning (companion to the `agent-manifest.ts` resource-limit fix above), covered by extended `setup.integration.test.ts` (+38 lines) — no new row. **2026-08-16 cycle:** `check-review.ts`'s combinatorial exclusion-check coverage completed — `check-review.unit.test.ts` now 101 test cases (+3), each new case stacking 3+ of the prior cycle's fixed exclusion conditions in a single fixture (T-082, #2641). `check-deploy.unit.test.ts`, `check-patch.unit.test.ts` extended with further `buildProductionDeps` coverage (companion to MTC-1.7's coverage-floor raise, existence-only signal, no new row). |
| `admin/src/*` | 85 files (re-verified via `find admin/src -maxdepth 1 -name "*.test.ts"`; +`session-alert-sweeper.unit.test.ts`, +`session-follow-service.{unit,integration}.test.ts`, +`session-scope.unit.test.ts`, +`agent-work-queue-merge.unit.test.ts`, +`prisma-client.{unit,integration}.test.ts` — plus new `admin-ui-session*.smoke.test.ts` — ~9 new files this cycle) | unit/integration/smoke | Agent/Env/Cron/Tool/Token CRUD, K8s client, provisioning clients, auth clients, admin UI, chat-token daily rollup, work-queue snapshots (+cross-agent merge, new), **Sessions (new, follow/alert-sweep/admin-actions)**, Prisma 7 client extraction (new) | **This cycle (2026-09-21):** `admin-ui-sessions-list.smoke.test.ts` extended in place for the Sessions list Follow button wiring (FLW-1.1, `#3533`, replaces the disabled follow-toggle stub with a live per-row Follow/Following toggle reusing the existing `POST /admin/sessions/:slug/{follow,unfollow}` routes; a review-round fix makes the row fail open if `listByUser()` errors) — confirmed via `git log`, **Reuse**, correct canonical smoke layer for the wired route, no new row, see Method note step 3. **Prior cycle (2026-09-18):** see Method note steps 1 (Sessions), 4 (`agent-work-queue-merge.ts`), 5 (`prisma-client.ts`) — all **Reuse**, paired test verified directly for each new file. **Prior cycle (2026-09-02):** new `admin/src/slack-provisioning-service.ts`/`github-provisioning-service.ts` (UAP-1.1/1.2, pure extraction of previously-inline `/admin/provision/*` handler logic — no behavior change per both files' own header comments, now also backing new per-agent connect-later routes) covered by new **`slack-provisioning-service.unit.test.ts`+`.integration.test.ts`**, **`github-provisioning-service.unit.test.ts`+`.integration.test.ts`** — **Reuse**, all four new files backing an extraction landed in the same PR at the correct unit(orchestration)/integration(OAuth calls) split, see Method note step 1. New `admin/src/pwa.ts`/`push-*.ts` (CFB-4.2, PWA shell + Web Push) covered by 9 new test files (`pwa.unit.test.ts`, `pwa-https-gating.smoke.test.ts`, `pwa-routes.smoke.test.ts`, `pwa-icons.unit.test.ts`, `push-content.unit.test.ts`, `push-crypto.unit.test.ts`, `push-sender.unit.test.ts`, `push-service.unit.test.ts`, `push-toggle.unit.test.ts`) — **Reuse** across all, each at its correct canonical layer per the Phase 2 blueprint's new Web Push local-execution-architecture row; `push-sender.unit.test.ts` verified directly to use an injected `fetchImpl` fake, never `global.fetch`, matching the isolation contract, see Method note step 2. `admin/src/http-chat-client.ts` gains its first unit-layer coverage via new **`http-chat-client.unit.test.ts`** (companion to the CFB-2.3 live-progress cluster landed in the prior 2026-08-30 window, first observed as a new file in this cycle's diff range) — **Reuse**, a genuine layer addition alongside existing integration coverage, not a duplicate. **Prior cycle (2026-08-30):** new `admin-ui-layout.ts` (shared `renderAdminPage()` extraction + viewport-meta normalization across 22 sites, #2905) covered by new **`admin-ui-layout.unit.test.ts`** plus a new loop-test assertion in `admin-ui.smoke.test.ts` (+9/-lines) — **Reuse**, new file backing a pure-extraction refactor, folds into the already-migrated admin-UI composite row (relocation, not a new boundary). Chat page inline-styles-to-classes migration (CFB-1.3, #2908) covered by new **`admin-ui-styles.unit.test.ts`** (53 lines). Mobile-responsive fixes (CFB-3.1, #2914) verified via new Playwright e2e assertions rather than unit string-matching — three now-redundant unit assertions removed in the same commit, a deliberate layer migration (see Method note step 7). `attachment-validation.unit.test.ts` extended (+33 lines) for the new HEIC/HEIF allowlist entries — see Method note steps 5-7. **Prior cycle (2026-08-22):** new `okta-auth-client.ts` (mirrors `google-auth-client.ts`, 10 cases in `okta-auth-client.integration.test.ts`) and `github-app-provisioning-client.ts` (13 unit cases for the pure manifest builder + 7 integration cases for the code-exchange client, incl. an explicit webhook-secret-discard assertion) — both **Reuse**, canonical layer, see Method note step 1. `admin-ui-pages.ts`/`admin-ui.ts` extended for the `restrictSlackToMembers` toggle UI and `/admin/agents/new` CTA/no-Slack-provisioning changes — existence-only signal, already-inventoried row. **Prior cycle (2026-08-18):** `agent-manifest.ts`'s ephemeral-storage constant (1Gi→4Gi) fix, covered by extended `agent-manifest.unit.test.ts` (+11 lines) — no new row. **2026-08-16 cycle:** `agents-api.ts`/`agent-cron-runs.ts` gained server-side `itemId`/`phaseId` cron-run filters, with `agent-cron-runs.integration.test.ts` extended; `admin-ui-pages.ts`'s blocked-task badge/detail render-crash fix, with a new `BlockedByEntry` parity assertion in `admin-ui-pages.unit.test.ts`. `blocked-by-type-parity.check.ts` has zero `bun test` coverage by design (compile-time-only, wired into `admin/package.json`'s `typecheck` script via `tsconfig.type-parity.json`) — confirmed passing in CI, not a migration-bucket gap. |
| `task-store/src/*` | 48 files (re-verified via `find task-store/src -maxdepth 1 -name "*.test.ts"`; +`session-service.{unit,integration}.test.ts`, +`session-rollup.unit.test.ts`, +`session-retention-reaper.integration.test.ts`, +`webhook-dispatcher.unit.test.ts`, +`webhook-timeout-buffer-check.unit.test.ts`, +`prisma-client.unit.test.ts` — 7 new files this cycle; `pr-origin-derivation.ts`'s tests colocate in `pull-request-service.unit.test.ts`, see Method note step 2 — not a new file) | unit/integration/smoke | Task/PR/Token/**Session (new)** services, dependency-resolution rules, routes (+`sessions.ts`, new), PR-origin derivation (new, colocated tests), **outbound webhook dispatcher (new)**, Prisma 7 client extraction (new) | **This cycle (2026-09-18):** see Method note steps 1 (Sessions), 2 (`pr-origin-derivation.ts` — verified comprehensive coverage despite no dedicated file), 4 (webhook dispatcher/timeout-buffer-check), 5 (`prisma-client.ts`) — all **Reuse**. **Prior cycle (2026-09-02):** new `task-store/src/task-transition-diff.ts` (`computeTaskTransitionDiff`, TCS-1.1, mirrors the existing `pr-transition-diff.ts` pure-diff-over-allowlist pattern applied to `Task`) covered by new **`task-transition-diff.unit.test.ts`** — **Reuse**, canonical unit layer, see Method note step 3. `task-service.ts`'s claim/release/complete paths now write `TaskEvent` rows inside the same interactive `$transaction`, and `stale-claim-reaper.ts` now also emits one when it reaps a stale claim (TCS-1.3) — both covered by substantially extended `task-service.unit.test.ts` and `stale-claim-reaper.unit.test.ts`/`.integration.test.ts` — **Reuse**, in-place extensions. New DB `CHECK` constraint (migration `20260831000000_add_task_pending_claimed_by_invariant`) formalizing the `status`/`claimedBy` invariant at the schema level covered by new **`task-claim-status-invariant.integration.test.ts`** (110 lines, asserts a direct raw-SQL constraint-violating insert is rejected) — **Reuse**, correct integration layer for a real DB-level assertion. **Prior cycle (2026-08-28):** `GET /ready` now runs a real Prisma DB round-trip (503 on failure, was a static 200) — covered by new **`app.readiness.smoke.test.ts`** (125 lines, real-connection 200 case + broken-connection-string 503 case, both via Hono's in-process `app.request()`) and extended `main.unit.test.ts` (+26, `PrismaClient` wiring into the route factory) — **Reuse**, new file backing new production code at the canonical smoke layer, see Method note step 1. **Prior cycle (2026-08-22):** new `pr-transition-diff.ts` (`computePrTransitionDiff`, pure diff over an explicit `AUDITED_FIELDS` allowlist, `heartbeatAt` deliberately excluded) — 9 cases in `pr-transition-diff.unit.test.ts` directly proving both design notes (heartbeat exclusion, system-column exclusion) — **Reuse**, canonical unit layer. `pull-request-service.ts`'s `recordTransition()` (persists `PullRequestEvent` rows transactionally) covered by new `pull-request.integration.test.ts` — **Reuse**. New route `GET /prs/:id/events` covered by extended `prs.smoke.test.ts`/`routes/prs.openapi.smoke.test.ts`, incl. an empty-events case — **Reuse**. `pull-request-service.ts` also gains a nullable `agentId` on `PrFinding`, threaded through `patch.md`/`appendFinding()` — existence-only signal, already-inventoried row. **Prior cycle (2026-08-18):** new `PrFinding` model (PFL-1.1/PFL-2.1) — `pull-request-service.ts`'s `appendFinding()` covered by extended `pull-request-service.unit.test.ts` (+112, validation) and `pull-request-service.integration.test.ts` (+107, real-Postgres INSERT + FK + `NotFoundError` path) plus the new dedicated **`pr-finding.integration.test.ts`** (234 lines, model round-trip). New route `POST /prs/:id/findings` covered by extended `prs.smoke.test.ts` (+277, incl. the `source:"patch"`+non-`"rejected"` 400 authority-violation case) and `routes/prs.openapi.smoke.test.ts` (+4). `task-service.ts`/`routes/tasks.ts` gain a `?hitl=true/false` filter (HTF-1.1) forwarded to `listReady()`/`listBlocked()`, covered by the new **`state-filter.smoke.test.ts`** (117 lines) plus extended `task-service.unit.test.ts`. All land at their already-correct canonical layer — see Method note steps 1-4. **Prior cycle (2026-08-16):** `task-service.ts`'s `listReady()`/`listBlocked()` gained full session/repo/org/source/claimedBy/pr/branch/assignee filter support (ATB-1.3 shared post-filter extraction) plus own-assignee fallback for repos-absent agent tokens. `task-service.ts`'s `claim()` concurrent-claim regression test (T-081, PR #2560) landed in `task-service.integration.test.ts`. `Task.requiresHumanApproval` column dropped via a companion Prisma migration — confirmed no test file references the removed field. |
| `chat/src/*` | 15 files (re-verified via `find chat/src -maxdepth 1 -name "*.test.ts"`; +`prisma-client.{unit,integration}.test.ts`, new) | unit/integration/smoke | Message/thread/token services, routes, OpenAPI schemas, spec generator, outbound reply-notification webhook, stall reaper, Prisma 7 client extraction (new) | **This cycle (2026-09-18):** new `prisma-client.ts` (Prisma 7 migration, pure extraction) covered by paired `.unit.test.ts` + `.integration.test.ts` — **Reuse**, see Method note step 5. **Prior cycle (2026-09-02):** new `chat/src/reply-notifier.ts` (optional per-agent-configured outbound webhook, CFB-4.3) covered by new **`reply-notifier.unit.test.ts`**; new `chat/src/stall-reaper.ts` (reaps chat claims stalled >300s, mirrors `task-store/src/stale-claim-reaper.ts`'s established pattern) covered by new **`stall-reaper.unit.test.ts`**, spot-checked against its task-store sibling's test shape — both assert the same three cases (stalled claim reaped, fresh claim untouched, boundary-exact heartbeat-age case), proportionate depth, not a shallower reimplementation — **Reuse** for both, see Method note step 6. `chat/src/message-service.ts` gains its first unit-layer coverage via new **`message-service.unit.test.ts`** (companion to persisting last-write-wins Slack progress directly on the `Message` row) — **Reuse**, a genuine layer addition alongside existing integration coverage. **Prior cycle (2026-08-30):** new **`messages.smoke.test.ts`** (111 lines) covers the heartbeat-driven reply-timeout wire contract at the smoke layer, alongside extended `message-service.integration.test.ts` (+32/-lines) and `test-fakes.ts` (+24/-lines) — **Reuse**, new file backing new production behavior in the same PR (#2899/#2902), correct canonical layer, folds into the already-migrated chat-service composite row. |
| `mcp-server/src/*` | 8 files | unit/integration/smoke | Tool allowlist, tool-caller proxy, MCP wire protocol, inbound bearer auth, Streamable HTTP transport + idle-session reaper, app-factory composition, fail-closed entrypoint guard | Unchanged this cycle — no files in Phase 1's delta touch `mcp-server/`. |
| `brand/*` | 2 files | unit | Hex-color lint, CSS token generation | Unchanged. |
| `scripts/*` | 29 files (was 23, re-verified via `find scripts -maxdepth 1 -name "*.test.ts"`; +`find-hidden-dir-tests.unit.test.ts`, new — remaining delta is prior-cycle files not previously recounted, e.g. `agent-workspace-pull.*`, `docs-source-map.unit.test.ts`, `lib/clone-plan.unit.test.ts`) | unit/integration/smoke | Coverage gate(s), config-docs checker, dev supervisors, seed scripts, version sync, agent-provisioning poll loop (`agent-workspace-pull.ts`), Minikube stack orchestration, HITL bootstrap, downstream chart-compatibility checker, **hidden-dir test finder (new)** | **This cycle (2026-09-18):** new `find-hidden-dir-tests.ts` (finds `bun test`-invisible dot-directory test files, wired into `task test`/`task ci`) covered by paired `.unit.test.ts` — **Reuse**, see Method note step 7. **Prior cycle (2026-08-28):** new `check-downstream-compat.sh` (CNH-8.3) — a bash script rendering a downstream wrapper chart's Shipwright dependency 3 ways and diffing them — covered by new **`check-downstream-compat.smoke.test.ts`** (175 lines, extracts and executes the real `render_variant`/`copy_wrapper`/restore-guard shell logic against synthetic `Chart.lock`-only fixtures, explicitly regression-testing the same-PR restore-guard bug fix) — **Reuse**, new file backing new production code, see Method note step 2 and `test-system.md`'s flagged watch item on this file's `.smoke.test.ts` suffix choice. `scripts/minikube.ts` gains `cloud-native-nginx`/`cloud-native-traefik` profiles via a per-profile `PROFILES` config object, covered by extended `minikube.unit.test.ts` (incl. a byte-identical regression pin for `task minikube:up`'s emitted command list) — existence-only signal, no new row, row remains ranked out-of-scope dev-only tooling. **Prior cycle (2026-08-18):** `minikube.ts` gains `buildAccessUrls().devLogin`, `buildOpenCommand()`, `hostsEntryPresent()` pure builders (dev-login-URL/auto-open fix), covered by extended `minikube.unit.test.ts` (+158/-25 lines) — no new row; the row remains ranked out-of-scope dev-only tooling. **2026-08-16 cycle:** the dual coverage-gate cluster — `check-coverage-gate.ts` (`computeCoverageGate`, six-field block + self-consistency guard), `check-coverage-no-decrease.ts` (`evaluateCoverageDelta` pure comparison + base-ref worktree-checkout driver), `coverage-tool-dispatch.ts` (`extractCoverageTool`/`selectParser`) — each with its own new test file(s) at the correct layer (see Method note step 5). `scripts/check-coverage.ts` refactored to a pluggable `CoverageParser` interface (5 implementations), covered by the new `check-coverage.unit.test.ts`. `scripts/hitl.ts` split into pure planners + thin executor per `test-readiness-decisions.md`'s T-080 entry, now covered by `hitl.bootstrap.unit.test.ts` (unit, planners) + `hitl.integration.test.ts` (integration, executor) — **resolves the repo's longest-standing open Net-new item**, see Method note step 3. |
| `plugins/shipwright/scripts/*` | 17 files (was 13, re-verified via `find plugins/shipwright/scripts -maxdepth 1 -name "*.test.ts"`; +`is-ci-green.unit.test.ts`, +`compute-unresolved-comment-check.unit.test.ts`, +`resolve-dependency-watched-paths.unit.test.ts`, +`slack-say.unit.test.ts`, +`check-site-docs-freshness.unit.test.ts` — 5 new files; `check-dependabot-triage.unit.test.ts` retired outright this cycle alongside its skills, DBR-3.4, not merely renamed as the prior cycle's DBT-1.1 note assumed) | unit | `check-*.ts` cron prechecks (incl. new `check-site-docs-freshness.ts`), `classify_test_layer.ts`, `check-helpers.ts`, `compute-review-verdict.ts` + new `compute-unresolved-comment-check.ts`, `compute-unaddressed-findings.ts`, `diagnose-review-candidacy.ts`, new `is-ci-green.ts`, `resolve-dependency-watched-paths.ts`, `slack-say.ts` | **This cycle (2026-09-18):** see Method note step 7 for all five new files — **Reuse**, correct canonical unit layer, paired test verified directly for each. `check-dependency-bot-triage.ts` + `.unit.test.ts` **deleted outright** (DBR-3.4, `e42a6cf9d`) now that `review.md` Step 5.8 supersedes it — a genuine deletion. Net this cycle: 13 (prior) + 5 new − 1 deleted = 17, matching the ground-truth `find` count. **Prior cycle (2026-09-02):** `398c71ed` renames `check-dependabot-triage.ts`/`.unit.test.ts` → `check-dependency-bot-triage.ts`/`.unit.test.ts` (453 lines, up from 281) — **Reuse**, a rename carries its prior `Reuse` bucket forward unchanged, see Method note step 8. `ddafdc57` removes now-redundant self-review heuristics from `compute-unaddressed-findings.ts`; `compute-unaddressed-findings.unit.test.ts` shrinks in step (net −388 lines), confirmed 1:1 against removed production logic, not a coverage-loss red flag — **Reuse**, correct in-place shrink, see Method note step 9. **Prior cycle:** `diagnose-review-candidacy.ts`'s CLI wrapper around `check-review.ts`'s exported `traceReviewCandidacyDecision()` — `diagnose-review-candidacy.unit.test.ts` (269 lines) covers argument parsing and trace-output formatting against fixed fixture data, reusing `check-review.ts`'s existing recorded-GitHub-review-data fixtures rather than introducing new ones. (Baseline corrected from a prior-cycle miscounting bug: the 2026-08-11 doc's "18 files (was 15, ...)" row for this directory was already inflated at the time — ground-truth `git ls-tree` against the `0428fe71` baseline commit shows 11 `.unit.test.ts` files, not 18; corrected here to the verified 11→13 delta.) |
| `plugins/shipwright/{commands,agents,skills,references,test}/*` | 40 dedicated `*.content.test.ts` files (re-verified via `find plugins/shipwright -name "*.content.test.ts"`; +`dependency-patch.content.test.ts`, +`dependency-risk-analysis.content.test.ts`, new; −`triage-dependency-bot-pr/SKILL.content.test.ts`, deleted alongside its retired skill, DBR-3.4; `.claude/commands/docs-sync.content.test.ts` also new, counted separately as it lives outside `plugins/shipwright/`) | content/unit/integration | Command/skill/agent markdown prose assertions | **This cycle (2026-09-21):** `dev-task.content.test.ts` checked against the `#3545` worktree-path fallback fix (`${SHIPWRIGHT_REPO_DIR:-repos}` → `${SHIPWRIGHT_REPO_DIR:-$HOME/src}` in the stale-bundle re-add path) — the 1-line fix commit itself touched no test file, and no existing assertion sweeps every `worktree add`/`worktree remove` invocation in the file for the absolute-default-fallback pattern, so a future line could reintroduce the same relative-default typo (already recurred 5+ times per this repo's own incident history) undetected — **Promote / deepen**, see Method note step 3 and Tests — bucketed below. **Prior cycle (2026-09-18):** new `dependency-patch.content.test.ts`/`dependency-risk-analysis.content.test.ts` pair with the new reference docs of the same name — **Reuse**, canonical content layer, see Method note step 9. `triage-dependency-bot-pr/SKILL.content.test.ts` deleted alongside its retired skill (DBR-3.4) — correct, no orphaned assertions against removed content. New `.claude/commands/docs-sync.content.test.ts` covers the `docs-sync.md` rewrite (#3428) — **Reuse**. **Prior cycle (2026-08-28):** new `plugins/shipwright/test/cron-tables.content.test.ts` (150 lines, SUX-1.1) — regression guard asserting every manifest cron appears exactly once in `cron-jobs.mdx`'s table and zero times in `the-agent.mdx` — **Reuse**, new file backing new production-doc-consistency logic, canonical content layer, see Method note step 7. `dev-task.content.test.ts` (+59, ScheduleWakeup guardrail), `deploy.content.test.ts`/`merge.content.test.ts` (branch-protection-block detection), `review.content.test.ts` (Step 5.5 fix) extended in place — existence-only signal, no new row. **Prior cycle (2026-08-25):** `repo-config/SKILL.content.test.ts` and `test-inventory/SKILL.content.test.ts`, backing this cycle's `repo-config/SKILL.md` and `test-inventory/SKILL.md` (Step 3b feature-grouping) extensions. |
| `site/tests/*.spec.ts` | 26 files (was 24, re-verified via `find site/tests -maxdepth 1 -name "*.spec.ts"`; +`autonomy.spec.ts`, +`vs-openhands.spec.ts`, new) | e2e | Marketing pages, incl. `docs/*` page specs, `/autonomy` staged-rollout page (new), `/vs/openhands` comparison page (new) | **This cycle (2026-09-18):** new `autonomy.spec.ts` covers the `/autonomy` staged-rollout page; new `vs-openhands.spec.ts` covers the new `/vs/openhands` comparison page (which also fixed a stale OpenHands row on `compare.astro` in the same PR, #89c512cc2) — both **Reuse**, same Playwright tooling, same marketing-site journey row. **Prior cycle (2026-08-28):** new `docs-security.spec.ts` (70 lines, SDP-1.1, extended 4× in the same window as `security.mdx` grew) covers the new public security-architecture docs page; new `docs-slack-integration.spec.ts` (147 lines, SUX-2.1/2.2) covers the new `StepFlow.astro` component's application to `slack-integration.mdx`; `docs-landing.spec.ts` **deleted** in the same commit as its subject page (`/docs` landing page removed, #2833), replaced one-for-one by new `docs-redirect.spec.ts` (32 lines) — net file-count change +3 (4 added, 1 deleted) — see Method note steps 8-9. All **Reuse**, same Playwright tooling, same user-journey row. **Prior cycle (2026-08-25):** new `vs-factory.spec.ts` (full Factory comparison page E2E coverage, ~130 lines, 13 assertions); `compare.spec.ts`/`self-hosted.spec.ts` extended with one assertion each for the new "Factory" comparison-table row. |
| `admin/e2e/*.e2e.ts`, `metrics/e2e/*.e2e.ts` | 7 files (re-verified via `find admin/e2e metrics/e2e -name "*.e2e.ts"` — corrected from the prior cycle's stale "5 files" count; no new files added this cycle, the ground-truth count already included `chat-degraded.e2e.ts`/`chat-progress.e2e.ts`/`login-page.e2e.ts` that a prior cycle's narrative approximation missed) | e2e | Admin login/agents/chat pages (incl. mobile-responsive assertions), metrics dashboard | **This cycle (2026-09-18):** no new files; ground-truth recount only (see left column). **Prior cycle (2026-08-30):** new **`admin/e2e/chat-page.e2e.ts`** and **`admin/e2e/chat-thread-page.e2e.ts`** (118 lines) cover the chat-page style-class migration (#2908) and the nine mobile-responsive fixes (#2914) via real computed-style/viewport assertions (font-size, overflow, touch-target height) — **Reuse**, both new files backing new production code/refactor in the same PRs, folding into the existing admin e2e user-journey row per its established collective treatment, see Method note steps 6-7. |

### Promote / deepen

**One item this cycle, ending an eight-cycle empty streak:**

| Test file | Gap | Fix | Effort |
|---|---|---|---|
| `plugins/shipwright/commands/dev-task.content.test.ts` | No sweeping assertion that every `git -C ... worktree add`/`worktree remove` invocation in `dev-task.md` uses the `${VAR:-$HOME/...}` absolute-default fallback pattern. The `#3545` fix corrected one instance (the stale-bundle re-add path) that had silently landed worktrees nested inside `repos/<repo>/worktrees/...` instead of the top-level `worktrees/` dir — a class of bug that recurred 5+ times before being root-caused, per this repo's own incident history. | Add one content-layer assertion: extract every `worktree add`/`worktree remove` line via regex and assert each uses `${SHIPWRIGHT_REPO_DIR:-$HOME/src}`/`${SHIPWRIGHT_WORKTREE_DIR:-$HOME/worktrees}` (or the equivalent absolute-fallback form), never a bare relative default. Mirrors the file's own existing narrower assertion at line ~712 (`"no longer uses raw {repo} for ... {repo} style local paths"`), just widened to cover *every* invocation rather than one historical instance. | trivial — one new `it()` block in an already-existing, already-passing content test file; no production code change. |

Not urgent (the underlying production bug is already fixed and this doesn't touch
currently-red behavior), but cheap enough to close now given the recurrence history — see
Method note step 3 above. No open items were carried in from the 2026-09-18 pass (it
closed clean). The sole other carried-forward item from earlier in this document's
history, `agent-slack-membership-ref.unit.test.ts`'s missing `afterEach` reset on the
shared singleton, resolved via PR #2790 (T-084) — see the 2026-08-30 method note
(preserved above) and the `agent/src/*` row.

### Rebuild

None. No test file's layer, framework, or fundamentals were found wrong this pass.

### Delete (redundant)

None. See Delete (redundant assertion) analysis above.

### Net-new

One item, carried forward unchanged, trivial/optional. The repo's longest-standing real
Net-new item (Local HITL dev-loop bootstrap, journey #5, open since 2026-07-31) resolved
this cycle — see Method note step 3 and the `scripts/*` row above:

| Inventory item | Prescribed layer | Criticality | Estimated effort |
|---|---|---|---|
| `admin/src/clock.ts` (`Clock`/`SystemClock`/`FixedClock` implementation for admin) — carried forward, unrelated to this cycle's Phase 1 delta | unit | low | trivial, optional — carried forward unchanged from every prior pass. All sibling `Clock` files across the repo lack a dedicated test file too; near-zero logic (two one-line methods), proven correct via consumers per the repo's established convention. |

## Infrastructure — bucketed

### Reuse as-is

- **Bun test runner config** (`bunfig.toml`'s `pathIgnorePatterns`) — re-verified this
  pass: `["**/site/**", "**/metrics/e2e/**", "**/admin/e2e/**", "**/*.canary.test.ts"]`,
  unchanged and correct.
- **CI workflow shape** (`.github/workflows/ci.yml`) — matches Phase 2's documented
  shape; unchanged this cycle.
- **Postgres service container for admin/task-store/chat** — real DB per test run, all
  three `DATABASE_URL_SHIPWRIGHT_*` vars wired in `ci.yml`.
- **`Clock` injection helpers** — implementations across the repo plus
  `plugins/shipwright/scripts/test-helpers/doubles.ts` — still the strongest, most
  consistently-applied convention in the repo. No new consumer landed this cycle that
  deviates from the pattern.
- **Recorded-fixture doubles** — `RecordedKubernetesClient`,
  `metrics/src/providers/task-store-recorded.ts`,
  `metrics/src/fixtures/task-store-fixtures.ts`, `agent/src/shipwright-config-client.ts` —
  unchanged this cycle; pattern is stable and extending cleanly to new clients.
- **`scripts/check-coverage.ts`** — the 90/89 line/function-threshold gate (raised from
  80/80 by MTC-1.7, then to 90% lines by CLG-1.1, and refactored to a pluggable `CoverageParser` interface
  previously — see the `scripts/*` row above). Confirmed passing this cycle's live CI run at
  90.90%/89.75%.
- **`scripts/check-coverage-no-decrease.ts`** (new in Phase 1, PR #2657; retired after
  stage-3 promotion, no longer in CI pipeline) — was a companion coverage-must-not-decrease
  gate wired into `.github/workflows/ci.yml` for PRs. Removed when stage-3 promotion made
  the never-decrease check no longer required.
- **`.github/PULL_REQUEST_TEMPLATE.md`** — confirmed present directly, matches
  `test-system.md`'s Repo configuration section's note that this is resolved, not a gap.
- **`docs/test-readiness/speed-baseline.md`** — not re-refreshed this cycle; its numbers
  remain directionally consistent with this pass's live CI pull (see Speed measurement
  below) — steady growth, no budget concern.

### Promote / deepen

None. The design-level backlog item (extending `Recorded*Client` fixture-double coverage
to the remaining `Http*Client` modules named in `test-system.md`'s Local execution
architecture table) remains a Phase 2 design-level item, not a migration-bucket verdict on
any existing infra file — carried in `test-system.md`'s gaps summary, not restated here.

### Rebuild

None.

### Delete (redundant)

None.

### Net-new

None. No new infrastructure gap identified this cycle. The new
`pull-request-service.integration.test.ts`'s concurrent-caller race harness
(`Promise.allSettled` racing two `claim()` calls) is a reusable pattern for the
`task-service.ts` Net-new item above, but requires no new shared cross-workspace
infrastructure — it's colocated with the service's own integration-test file, per Phase
2's Shared helpers section's own guidance for this exact harness shape.

## Risk callouts

No test currently passing (green) received a `delete` or `rebuild` verdict this pass, so
the SKILL's mandatory risk-callout requirement has no entries to populate for the test
suite as it exists today.

**No new risk callouts this cycle (2026-09-21).** No test received a `delete` or
`rebuild` verdict this pass either — the one Promote/deepen finding
(`dev-task.content.test.ts`'s missing worktree-path-fallback sweep, see Method note step
3 and Tests — bucketed above) is explicitly **not** a risk callout: the SKILL's mandatory
risk-callout requirement applies to a currently-*passing* test getting a `delete`/
`rebuild` verdict (false-confidence coverage), and no test received either verdict this
cycle. No new isolation-contract violation surfaced (grep counts for `mock.module(`/
`DATABASE_URL` are byte-for-byte unchanged from the 2026-09-18 pass — see Isolation
contract audit above).

**Prior cycle's risk-callout note (2026-09-18, preserved for continuity).** No test
received a `delete` or `rebuild` verdict, and no new isolation-contract violation
surfaced (all new `mock.module(` grep hits confirmed comment-only false positives). The
`pr-origin-derivation.ts` verification (that cycle's Method note step 2) was not a risk
callout either — it turned out to be a naming-convention nitpick on top of genuinely
comprehensive coverage, not a false-confidence or missing-coverage finding.

**Earlier cycle's risk-callout note (2026-09-02, preserved for continuity).** No test received a `delete` or
`rebuild` verdict, and no new isolation-contract violation surfaced (the one new
`mock.module(` grep hit was confirmed a comment-only false positive — see Isolation
contract audit above). The `agent-slack-membership-ref.unit.test.ts` singleton leak
flagged 2026-08-22 and resolved 2026-08-23 (PR #2790/T-084 + companion fix-commits in PR
#2788) remains closed — no currently-red test was ever involved.

**Adjacent risk note (not a delete/rebuild verdict, but worth flagging given the
magnitude):** the `pr-state-reconciler.ts` orphan-reconciliation removal (PR #2527)
deleted 547 lines of passing, green test assertions alongside their production code.
This audit confirmed the removal was deliberate, reviewed, and the superseding mechanisms
(`StaleClaimReaper`, ready-set FIFO, `dev-task.md` Step 4) are real and tested — so no
action is recommended. Flagging only so a future cycle doesn't need to re-derive this
finding from scratch if the same pattern (large deletion of tested code) recurs elsewhere
without a PR reference this clear.

## Speed measurement

### Tier 1 — aggregate (measured this pass, 2026-09-21)

- **Run:** [35557736074](https://github.com/app-vitals/shipwright/actions/runs/35557736074)
  (CI on commit `08cab0b33`, this cycle's exact HEAD), 2026-09-21T03:30:17Z. Fully green —
  fetched directly via `gh run list`/`gh run view --log`, not assumed.
- **Job wall-clock:** 1m37s (`lint / typecheck / test`, 03:30:34Z–03:32:11Z), of which the
  `bun test` step itself ran 23.04s.
- **Tests:** 9,271 tests across 370 files (per the runner's own summary line, "Ran 9271
  tests across 370 files.") — +52 tests / +2 files vs. the 2026-09-18 pass, tracking this
  cycle's two new test files (`pr-origin-backfill.unit.test.ts`,
  `sessions.unit.test.ts`) plus their in-line case counts, proportionate to the small
  6-commit delta.
- **Coverage (from this same run's `test:coverage` step log):** Lines 92.46%
  (35715/38629), Functions 91.01% (2965/3258) — against the 90%/89% thresholds
  (`✅ Coverage gate passed`, logged directly). Both comfortably clear their respective
  gates, essentially flat vs. the 2026-09-18 pass (92.47%/91.02%) — consistent with a
  small, proportionate top-up rather than a coverage shift.
- **vs. budget:** 1m37s against the <15 min Full-PR-pipeline budget — **~11% of budget,
  well within**, consistent with the 2026-09-18 pass's ~10% reading. Not trending toward
  the escalation trigger (aggregate wall-clock >7.5 min, sustained across 2 consecutive
  measurements) — nowhere near tripped.

Test-count growth trajectory (continued from below): …→ 7,870 (2026-09-02) → 9,219
(2026-09-18) → **9,271 (2026-09-21, this pass)** — +52 tests, proportionate to this
cycle's small 6-commit delta, in sharp contrast to the prior pass's +1,349-test outlier
jump. No test-count anomaly, no padding.

### Tier 2 — per-layer breakdown (conditional)

- **Tier 2 triggered:** no — Tier 1's aggregate is at ~11% of the <15 min budget this
  pass, far below the 50% escalation threshold.

Tier 2 not triggered — suite is comfortably within the Tier 1 aggregate budget. Per the
SKILL's explicit guidance, this is a valid, complete outcome, not a gap requiring
escalation. No per-layer numbers fabricated.

### Prior pass's Tier 1 reading (2026-09-18, preserved for continuity)

- **Run:** [35295193216](https://github.com/app-vitals/shipwright/actions/runs/35295193216)
  (CI on commit `eff5b503`, that cycle's exact HEAD), 2026-09-18T01:24:34Z. Fully green —
  fetched directly via `gh run list`/`gh run view --log`, not assumed.
- **Job wall-clock:** 1m29s (`lint / typecheck / test`, 01:24:49Z–01:26:18Z), of which the
  `bun test` step itself ran 23.38s.
- **Tests:** 9,219 tests across 368 files (9,218 pass, 1 skip, 0 fail, 5 snapshots,
  21,605 `expect()` calls) — per the runner's own summary line ("Ran 9219 tests across
  368 files.").
- **Coverage (from this same run's `test:coverage` step log):** Lines 92.47%
  (35494/38386), Functions 91.02% (2941/3231) — against the 90%/89% thresholds. Both
  comfortably clear their respective gates (`check-coverage-gate.ts` logged "✅ Coverage
  gate passed"). `feature_coverage_pct`: **100% (24/24)**, carried forward from
  `test-inventory.md` (one new feature, Sessions, this cycle — see `test-inventory.md`).
- **vs. budget:** 1m29s against the <15 min Full-PR-pipeline budget — **~10% of budget,
  well within**, actually faster than every prior cycle's ~13-18% readings despite the
  16-day/246-commit gap and +1,349 tests. Not trending toward the escalation trigger.
  The escalation formula (aggregate wall-clock >7.5 min, sustained across 2 consecutive
  measurements) is nowhere near tripped.

Test-count growth trajectory: 5,762 (2026-07-28 `speed-baseline.md`) → 6,041-6,042
(2026-08-01) → 6,070 (2026-08-05) → 6,071 (2026-08-06) → 6,177 (2026-08-09) → 6,288
(2026-08-11) → 6,501 (2026-08-14) → 6,864 (2026-08-16) → 6,920 (2026-08-18) → 7,190
(2026-08-22) → 7,211 (2026-08-23) → 7,228 (2026-08-25) → 7,326 (2026-08-28) →
7,444 (2026-08-30) → 7,870 (2026-09-02) → **9,219 (2026-09-18, this pass)** — +1,349
tests, by far the largest single-cycle jump on record, tracking this cycle's unusually
large 246-commit/16-day gap: the Sessions feature alone (7 new test files across
task-store + admin) plus ~15 more new test files across the remaining clusters in the
Method note above, on top of substantial in-place extensions (author-allowlist
rename/split cascade, TaskKind/autonomousPlanSession migration churn, admin Tasks/PRs
board redesign). Growth is proportionate to the inventory delta — no test-count anomaly,
no padding. The job got *faster* despite this growth (1m29s vs. the prior pass's 2m34s),
consistent with normal CI infra variance rather than a real speed regression or
improvement signal worth investigating on its own.

### Prior pass's Tier 2 breakdown (2026-09-18, preserved for continuity)

- **Tier 2 triggered:** no — Tier 1's aggregate was at ~10% of the <15 min budget that
  pass, far below the 50% escalation threshold.

Tier 2 was not triggered — suite was comfortably within the Tier 1 aggregate budget.

### Prior pass's Tier 1 reading (2026-08-30, preserved for continuity)

- **Run:** [33271383502](https://github.com/app-vitals/shipwright/actions/runs/33271383502)
  (CI on commit `ac0ec6a8`), 2026-08-29T19:38:10Z. Fully green.
- **Job wall-clock:** 2m41s, `bun test` step 44.54s. 7,444 tests pass across 294 files.
  Coverage: Lines 91.32% (28102/30772), Functions 89.79% (2330/2595). ~18% of budget.

### Earlier pass's Tier 1 reading (2026-08-28, preserved for continuity)

- **Run:** [33124011158](https://github.com/app-vitals/shipwright/actions/runs/33124011158)
  (CI on commit `7a688190`), 2026-08-27T22:49:38Z. Fully green.
- **Job wall-clock:** 2m32s, `bun test` step 40.92s. 7,326 tests pass across 290 files.
  Coverage: Lines 91.23% (27698/30361), Functions 89.79% (2314/2577). ~17% of budget.

### Earlier pass's Tier 1 reading (2026-08-25, preserved for continuity)

- **Run:** [32804399527](https://github.com/app-vitals/shipwright/actions/runs/32804399527)
  (CI on commit `d4bfce02`), 2026-08-25T03:14:08Z. Fully green.
- **Job wall-clock:** 2m22s, `bun test` step 42.09s. 7,228 tests pass, 1 skipped, across
  286 files. Coverage: Lines 91.29% (27607/30240), Functions 89.95% (2308/2566). ~16% of
  budget.

### Earlier pass's Tier 1 reading (2026-08-23, preserved for continuity)

- **Run:** [32559075040](https://github.com/app-vitals/shipwright/actions/runs/32559075040)
  (CI on commit `be5bfa59`), 2026-08-22T07:14:37Z. Fully green.
- **Job wall-clock:** 2m45s, `bun test` step 42.73s. 7,210 tests pass, 0 fail, across 286
  files. Coverage: Lines 91.29% (27604/30237), Functions 89.95% (2308/2566). ~18% of
  budget.

### Earlier pass's Tier 1 reading (2026-08-22, preserved for continuity)

- **Run:** [32550364804](https://github.com/app-vitals/shipwright/actions/runs/32550364804)
  (CI on commit `ea846283`), 2026-08-22T03:56:54Z. Fully green.
- **Job wall-clock:** 2m38s, `bun test` step 41.53s. 7,190 tests pass, 0 fail, across 286
  files. Coverage: Lines 91.28% (27548/30181), Functions 90.00% (2304/2560). ~18% of
  budget.

### Earlier pass's Tier 1 reading (2026-08-16, preserved for continuity)

- **Run:** [31871862234](https://github.com/app-vitals/shipwright/actions/runs/31871862234)
  (CI on commit `ca3c8c3f`), 2026-08-15T07:24:20Z. Fully green.
- **Job wall-clock:** 2m28s, `bun test` step 40.06s. 6,864 tests pass, 0 fail, across 282
  files. Coverage: Lines 90.90% (26490/29143), Functions 89.75% (2224/2478). ~16% of
  budget.

## Next step

Run `/test-roadmap` to synthesize this migration audit with the Phase 1 inventory and
Phase 2 blueprint into the executable roadmap.

**Candidate Phase 4 inputs from this document (2026-09-21 cycle):**

1. **One Promote/deepen item, ready for a trivial roadmap task:**
   `plugins/shipwright/commands/dev-task.content.test.ts` needs one new `it()` block
   asserting every `git -C ... worktree add`/`worktree remove` invocation in
   `dev-task.md` uses the `${VAR:-$HOME/...}` absolute-default fallback pattern (see
   Tests — bucketed → Promote / deepen above). No production code change — content-test
   file only. Trivial effort, recommend sequencing into the next roadmap milestone
   opportunistically rather than as a standalone urgent task (the underlying bug is
   already fixed; this only prevents a *future* recurrence).
2. **No open items carried in from the 2026-09-18 pass — it closed clean.**
3. **`agent/src/pr-origin-backfill.ts` (POB-1.1) needs no Phase 4 action** — landed with
   adequate test coverage in the same PR, bucketed Reuse as-is (see Method note step 1
   above).
4. **Isolation-contract audit's pattern set still needs to widen to catch
   singleton-mutation leaks as an automated sweep** — carried forward unchanged, same
   standing recommendation as every prior cycle (no new singleton-ref files this cycle to
   re-check by hand).
5. **Speed measurement re-confirmed in budget** — Tier 1 aggregate ~11% of budget on a
   fresh data point at this cycle's exact HEAD (92.46%/91.01% coverage, both comfortably
   above threshold). No escalation.

### Prior cycle's candidate Phase 4 inputs (2026-08-25, preserved for continuity)

1. **No open items carried in from the prior pass — the 2026-08-23 cycle closed clean
   (0 open items).** Nothing to resolve or re-check this cycle beyond the standing
   isolation-sweep-widening recommendation (item 2 below).
2. **Isolation-contract audit's pattern set still needs to widen to catch
   singleton-mutation leaks as an automated sweep, not just literal `global.*`
   overrides.** Carried forward unchanged across every cycle since 2026-08-22 — still a
   manual read-through finding, not yet a script. Recommend a Phase 4 task to add a
   fourth grep signal (`.set(`/`.reset(` calls on named singleton-ref exports with no
   paired `afterEach` in the same file). No new singleton-ref files this cycle to
   re-check by hand.
3. **All 5 substantive commits this cycle landed with matching test coverage at the
   correct layer in the same commit** (3 principles.md-override content additions, 1
   cron-chaining content fix + golden-fixture update, 1 bootstrap-deadlock logic fix with
   its existing test case updated in lockstep) — no Phase 4 action needed for any of
   them.
4. **Speed measurement re-confirmed in budget** — Tier 1 aggregate ~16% of budget on a
   fresh data point at this cycle's exact HEAD (91.29%/89.95% coverage, at/above the
   90/89 floor), consistent with the growth trajectory (+17 tests over this cycle's
   5-commit delta). No M1 task needed.
5. **0 net-new infra items, 0 rebuild candidates, 0 delete/trim candidates, 0 new
   isolation-contract regressions, 0 open Promote/deepen items** — a clean cycle, same
   as 2026-08-23: nothing opened, nothing to close.

### 2026-08-23 cycle's candidate inputs (preserved for continuity)

1. **This cycle's sole open item from the prior pass is now resolved — no carry-forward
   action needed.** `agent-slack-membership-ref.unit.test.ts`'s missing `afterEach`
   landed via PR #2790 (T-084), confirmed by direct diff read. If a Phase 4 task was
   already filed for this in a prior roadmap cycle, it should be closed as done.
2. **Isolation-contract audit's pattern set still needs to widen to catch
   singleton-mutation leaks as an automated sweep, not just literal `global.*`
   overrides.** Carried forward unchanged from last cycle.
3. **SMR enforcement (PR #2788) landed with matching test coverage at the correct layer
   in the same commit** — no Phase 4 action needed.
4. **Speed measurement re-confirmed in budget** — Tier 1 aggregate ~18% of budget on a
   fresh data point at this cycle's exact HEAD (91.29%/89.95% coverage, at/above the
   90/89 floor), consistent with the growth trajectory (+21 tests over this cycle's
   2-commit delta). No M1 task needed.
5. **0 net-new infra items, 0 rebuild candidates, 0 delete/trim candidates, 0 new
   isolation-contract regressions, 0 open Promote/deepen items** — a clean, fully
   resolved cycle: the one item flagged last cycle is closed, nothing new opened.

### 2026-08-22 cycle's candidate inputs (preserved for continuity)

1. **One new Promote/deepen item that cycle: `agent-slack-membership-ref.unit.test.ts`'s
   singleton-scoped test needed an `afterEach` reset.** Resolved this cycle (see above).
2. **Isolation-contract audit's pattern set should widen to catch singleton-mutation
   leaks, not just literal `global.*` overrides.** Still open — see item 2 above.
3. **Four of five new functional surfaces landed with matching test coverage at the
   correct layer in the same commits** (Okta auth, GitHub App provisioning, PR
   audit-trail events/PSA-1.2, plus the review-candidacy fix cluster) — no Phase 4
   action needed for those.
4. **Speed measurement re-confirmed in budget** — ~18% of budget (91.28%/90.00%
   coverage). No M1 task needed.

### 2026-08-18 cycle's candidate inputs (preserved for continuity)

1. **0 open items carried in, 0 new gaps found.** The one net-new functional surface
   this cycle (the `PrFinding` model — PFL-1.1/PFL-2.1) landed with matching test
   coverage at the correct layer in the same commits (unit + integration for the
   service, smoke for the route, including the `source:"patch"` authority-violation
   negative case). The four in-place fixes (`agent-manifest.ts` resource limit,
   `setup.ts` cache pinning, `tasks.ts`'s `?hitl` filter, `minikube.ts`'s dev-login
   builders) all extended their already-migrated test files at the already-correct
   layer. No Phase 4 action needed.
2. **Speed measurement re-confirmed in budget** — Tier 1 aggregate ~15% of budget on a
   fresh data point at this cycle's exact HEAD (90.95%/89.68% coverage, above the 90/89
   floor), consistent with the growth trajectory (+56 tests over the 1-day gap). No M1
   task needed.
3. **0 net-new infra items, 0 rebuild candidates, 0 delete/trim candidates, 0
   isolation-contract regressions, 0 Promote/deepen items** — the empty-Promote/deepen
   steady state (first reached 2026-08-16) holds for a second consecutive cycle.

### 2026-08-16 cycle's candidate inputs (preserved for continuity)

1. **2 open items resolved and closed out this cycle** — the repo's two
   longest-standing open migration items, both closed:
   - The HITL local dev-loop bootstrap Net-new item (open since 2026-07-31) resolved via
     T-080/#2659: `scripts/hitl.ts` split into pure planners + thin executor, covered by
     new `hitl.bootstrap.unit.test.ts` (unit) + `hitl.integration.test.ts` (integration),
     per a newly-recorded `test-readiness-decisions.md` entry.
   - The `agent/src/check-review.unit.test.ts` combinatorial-coverage Promote/deepen item
     (open since 2026-08-14) resolved via T-082/#2641: 3 new multi-condition test cases
     stacking 3+ of the prior cycle's fixed exclusion checks in single fixtures.
   No Phase 4 action needed for either.
2. **1 low-priority/optional carried-forward item** (`admin/src/clock.ts` — still a
   non-gap by the repo's own convention). No Phase 4 action needed.
3. **New this cycle: dual coverage-gate tooling fully covered at landing** — the
   `check-coverage-gate.ts`/`check-coverage-no-decrease.ts`/`coverage-tool-dispatch.ts`
   cluster and `check-coverage.ts`'s `CoverageParser` refactor all shipped with matching
   test coverage at the correct layer in the same commits. No Phase 4 action needed.
4. **`test-system.md`'s coverage-floor references corrected this cycle** (80/80 → 89/89 → 90/89,
   updated to reflect CLG-1.1 line-threshold raise) — a Phase 2 (not
   Phase 4) correction, already applied in this cycle's `test-system.md` refresh.
5. **Speed measurement re-confirmed in budget** — Tier 1 aggregate ~16% of budget on a
   fresh data point at this cycle's exact HEAD (90.90%/89.75% coverage, well above the
   90/89 floor), consistent with the growth trajectory (+363 tests over the 2-day gap).
   No M1 task needed.
6. **0 net-new infra items, 0 rebuild candidates, 0 delete/trim candidates, 0
   isolation-contract regressions, 0 Promote/deepen items** — the first cycle with an
   empty Promote/deepen bucket since this document's inception (2026-07-21). The
   steady-state finding holds and strengthens this cycle.
