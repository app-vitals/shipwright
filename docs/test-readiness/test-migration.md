# Test Migration

> Phase 3 of the test-readiness pipeline. Reconciles existing tests and infrastructure
> against the Phase 1 inventory ([`test-inventory.md`](./test-inventory.md), refreshed at
> `24bf90f3`) and the Phase 2 blueprint ([`test-system.md`](./test-system.md), refreshed at
> `24bf90f3`), bucketing each existing test file and each inventory item into one of five
> categories.
>
> Generated by `/test-migration` on 2026-07-25 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-20260725`, HEAD `24bf90f3`).

## Method note

This is the fifth `/test-migration` pass (prior: 2026-07-16, 2026-07-19, 2026-07-21,
2026-07-23 — committed `0c84bbd4`). Rather than re-read all ~267 test files cold, this
pass diffed the current file tree against the 2026-07-23 pass's findings and re-verified
each open item directly, then did a fresh full-suite discovery pass to catch anything the
diff approach would miss:

1. Enumerated every test file via the Phase 2 framework matrix's glob patterns (`bun
   test`'s default discovery for `*.unit.test.ts` / `*.integration.test.ts` /
   `*.smoke.test.ts` / `*.content.test.ts`, plus `site/**/*.spec.ts`, plus
   `{admin,metrics}/e2e/**/*.e2e.ts`).
2. Re-checked every open item from the 2026-07-23 report (1 net-new test —
   `admin/src/clock.ts`, low-priority/optional; 1 real net-new gap —
   `mcp-server/src/main.ts`'s fail-closed guard; 0 net-new infra) against the current
   tree by direct file-existence and content checks — not assumed from memory.
3. Cross-referenced Phase 1's "new since the 2026-07-23 inventory" file list (Agent Type
   manifest system: `admin/src/agent-type-registry.ts` +
   `admin/src/agent-type-manifest-loader.ts`; plus `scripts/generate-agent-type-schema.ts`,
   `scripts/wait-for-agent.ts`, `agent/workspace/CLAUDE-HITL.md.template`; two deletions,
   `admin/src/system-crons.ts` and `lib/agent-default-tools.ts`) against the test-file
   glob to find newly-introduced coverage gaps or orphaned test files for deleted source.
4. Re-ran the isolation-contract grep sweep (`mock.module(`, `global.fetch =`,
   `global.console =`, `DATABASE_URL` inside `*.unit.test.ts`) across the full current
   suite, not just the delta, to catch any regression introduced by unrelated PRs since
   the last pass.
5. Confirmed `.github/PULL_REQUEST_TEMPLATE.md` and `bunfig.toml`'s `pathIgnorePatterns`
   directly rather than trusting the prior doc's snapshot.
6. Read `admin/src/agent-type-manifest-loader.ts` and its
   `agent-type-manifest-loader.unit.test.ts` sibling in full to verify the file's actual
   I/O shape against its claimed `.unit.test.ts` layer, given Phase 1/2 both flag this
   component's canonical layer as **integration** (real file I/O) — this surfaced this
   pass's one Promote finding (see below).
7. Verified every consumer of the new `AgentTypeManifestResolver` interface
   (`admin/src/agent-cron-jobs.ts`, `scripts/seed-dev-agent.ts`,
   `admin/src/agents-api.ts`) tests against an **injected fake resolver**, not the real
   filesystem — confirmed via direct grep of each test file's fixture setup.
8. Checked `admin/e2e/agents-page.e2e.ts` directly for the required-`typeName`-picker
   assertion Phase 1/2 flagged as needing re-validation (not assumed still-passing
   because the file exists, per `test-system.md`'s Gap #2).
9. Checked for orphaned test files following the two deletions this cycle
   (`admin/src/system-crons.ts`, `lib/agent-default-tools.ts`) — confirmed neither had a
   dedicated test file to remove (both already covered as pure config/constant, absorbed
   into the new manifest-loader tests).

**Speed measurement: not re-run this pass**, same reasoning as all four prior passes — no
local Postgres in this sandbox (`DATABASE_URL_SHIPWRIGHT_*` unset), so a `bun test` run
here would silently skip every DB-backed integration test and produce misleading timings.
Static review (line counts, import/mock shape) was used instead; no evidence found of any
test breaching a hard cap. A real per-layer measurement continues to exist at
`docs/test-readiness/speed-baseline.md` on `main` (unchanged since 2026-07-15, already
cross-referenced into `test-system.md`'s Speed budgets section since the 2026-07-19 pass
per T-067/PR #1930 — no further correction needed this cycle). See
`project_shipwright_metrics_full_suite_flake` and `project_shipwright_admin_full_suite_flake`
in memory for the two known pre-existing full-suite flakes (unrelated to layer/speed, not
relevant to this audit).

**This measurement-only item has now been carried forward unactioned across 5
consecutive `/test-migration` cycles** (2026-07-16, 2026-07-19, 2026-07-21, 2026-07-23,
2026-07-25 — this pass), crossing the SKILL's 3-cycle escalation threshold. Per
`SKILL.md`'s "Failure modes to avoid" section, this must be surfaced explicitly so
`/test-roadmap` places it as the first Milestone 1 (M1) task by construction, the same
way the naming-convention task is guaranteed an M1 slot. **Action for Phase 4: schedule a
real `bun test --coverage` (or equivalent per-layer `time`-wrapped) run against a
sandbox/CI environment with `DATABASE_URL_SHIPWRIGHT_*` wired, and refresh
`speed-baseline.md` from live numbers** — the current baseline is 10 days stale
(2026-07-15) relative to substantial in-place logic growth across `agent/src/check-*.ts`,
`task-store/src/task-service.ts`, and now the full Agent Type manifest system landing
this cycle. Static review continues to find no hard-cap violations, but static review
cannot detect a test that has silently grown slow within its existing budget headroom.

## Summary

| Bucket | Test count | Infra count | Effort |
|---|---|---|---|
| Reuse as-is | ~264 | 6 | — |
| Promote / deepen | 1 | 0 | small |
| Rebuild | 0 | 0 | — |
| Trim (redundant assertion) | 0 | 0 | — |
| Net-new | 1 | 0 | trivial (optional) |

**Total existing test files discovered:** 117 unit + 56 integration + 43 smoke + 28
content = 244 `bun test`-scanned files, + 20 `site/` Playwright `*.spec.ts` + 3 Playwright
`*.e2e.ts` (2 `admin/e2e/`, 1 `metrics/e2e/`) = **267 test files total** — up from 249 at
the 2026-07-23 pass (+18: the Agent Type manifest system's 4 new test files
(`admin/src/agent-type-registry.unit.test.ts`,
`admin/src/agent-type-registry.schema.unit.test.ts`,
`admin/src/agent-type-manifest-loader.unit.test.ts`, plus expanded
`admin/src/agent-cron-jobs.{unit,integration}.test.ts` coverage), `scripts/hitl.unit.test.ts`
(new — the 739-line worker-loop expansion landed with tests),
`scripts/wait-for-agent.unit.test.ts` (new), plus growth inside already-existing files
(`admin/src/agents.unit.test.ts`, `admin/src/agents-api.{smoke,integration}.test.ts`,
`admin/src/admin-ui.smoke.test.ts`, `admin/src/admin-ui-pages.unit.test.ts`,
`admin/src/openapi-schemas.unit.test.ts`, `admin/e2e/agents-page.e2e.ts`,
`scripts/dev-tmux.unit.test.ts`) that added `typeName`/`missingRequiredEnv`/no-implicit-
agent-creation assertions without new files. Net file-count delta driven mostly by the
manifest system's genuinely new modules, not 1:1 with every changed source file — several
delta items landed as in-place growth in already-existing test files (see Resolved
section).

**Headline finding: every open item from the 2026-07-23 report is now closed, and the
Agent Type manifest system's full four-layer coverage (unit schema validation,
integration-shaped loader tests, unit-injected consumer tests, smoke happy+4xx paths, and
e2e picker assertion) is in place — one layer-naming mismatch found within it.** The one
Promote finding: `admin/src/agent-type-manifest-loader.unit.test.ts` is filed as
`.unit.test.ts` but 2 of its 14 test cases (`"loads the real committed coding manifest
from disk via the default reader"` and `"listTypes discovers the real committed
agent-types/ dir via the default list fn"`) deliberately exercise the real filesystem via
`defaultManifestReader`/`defaultTypeDirLister`, with no injected double — a real I/O
boundary crossing inside a unit-suffixed file. See Promote section below. No
trim/rebuild/delete candidates found. See Resolved-since-last-pass and Net-new sections
below.

## Resolved since the 2026-07-23 pass

Verified directly against the current tree (not carried forward from memory):

| Prior item | Verdict then | Status now |
|---|---|---|
| `mcp-server/src/main.ts` (fail-closed guard on missing `SHIPWRIGHT_MCP_SERVER_TOKEN`) — no test | net-new (real gap) | **Unchanged, still open.** No new test file added for `mcp-server/src/main.ts` this cycle — not part of this cycle's Phase 1 delta (no `mcp-server/` changes landed since 2026-07-23), so nothing regressed, but the gap identified two passes ago remains unaddressed. Carried forward below as this pass's sole real Net-new item — see Net-new section. |
| `admin/src/clock.ts` — no dedicated test file | net-new (non-gap, established `Clock` convention) | **Unchanged.** Still no dedicated test file; still a non-gap by the repo's established convention (near-zero logic, proven via consumers) — carried forward, see Net-new section. |
| Agent Type manifest system (anticipated by this cycle's own Phase 1/2 docs as a fresh delta) | not yet landed at 2026-07-23 | **Landed with test coverage across 4 of 4 prescribed layers**, per `test-system.md` Gap #1's scoping: `admin/src/agent-type-registry.{unit,schema.unit}.test.ts` (unit — schema/validation), `admin/src/agent-type-manifest-loader.unit.test.ts` (mostly-integration-shaped, injected-DI — see Promote finding above for the 2 real-disk cases), `admin/src/agent-cron-jobs.{unit,integration}.test.ts` + `scripts/seed-dev-agent.unit.test.ts` (unit, confirmed injected `AgentTypeManifestResolver` fakes, zero filesystem touches), `admin/src/agents-api.{smoke,integration}.test.ts` (smoke — happy-path `typeName` plus the 400-on-unknown-type rejection path at `agents-api.smoke.test.ts:1607`). |
| Agent provisioning E2E needs re-validation against the new required-picker flow, not assumed passing (Gap #2) | open question | **Confirmed re-validated.** `admin/e2e/agents-page.e2e.ts:267-274` asserts `a required select[name=type] is present` with the `required` attribute — matches the #2205 UI change (implicit agent creation removed, `typeName` now mandatory at creation). |
| `admin/src/agent-type-manifest-loader.ts`'s fallback-to-"coding" contract needs a dedicated assertion, not folded silently into a generic loader test (Gap #4) | open design ask | **Already resolved, pre-dates this pass's ask.** `agent-type-manifest-loader.unit.test.ts:54` — `"falls back to the coding manifest with a warning for an unknown typeName"` — asserts the resolved manifest, the warning count, and warning content (contains both the unknown and fallback type names). A second case (line 72) separately asserts the hard-failure contract (`"throws (does not silently no-op) when the coding fallback itself cannot load"`). Both cases already existed when Phase 2 wrote Gap #4 — the gap was speculative, not a real absence; noted here to close the loop explicitly. |
| `admin/src/server.ts` composition entrypoint needs exactly one thin smoke test (Gap #9, carried forward since 2026-07-19/PR #1935) | open question ("Phase 3 should confirm whether this test already landed") | **Confirmed landed, unchanged.** `admin/src/server.smoke.test.ts:227` — `describe("server.ts composed app mounts correctly")` with 3 cases confirming the UI app, agent-runtime app, and admin CRUD app all mount under one Hono instance with correct auth boundaries. |
| `admin/src/system-crons.ts`, `lib/agent-default-tools.ts` (deleted this cycle) — orphaned test check | n/a (new deletions) | **No orphaned test files found.** Neither deleted file had a dedicated test file — both were small hardcoded-constant modules whose behavior is now exercised transitively through the new manifest-loader/registry tests and their consumers' tests (`agent-cron-jobs`, `seed-dev-agent`). Nothing to remove. |

## Isolation contract audit

Grepped the full current suite (267 files) for the three hard-rule signals from root
`CLAUDE.md`'s Test isolation section and `references/principles.md`'s `t1_no_global_mocking`:

| Signal | Files matched | Real violations |
|---|---|---|
| `mock.module(` | 37 files (up from 36; all header-comment prose, e.g. `"No mock.module()..."`) | **0** — grepped for any match *not* immediately preceded by a `*` or `//` comment marker on the same line; zero results. The one new match this pass is a documentation comment in one of the Agent Type manifest system's new test files, same "we deliberately don't do this" convention as every other match. No executable call anywhere in the suite. |
| `global.fetch = ` (assignment) | 0 | **0** |
| `global.console = ` (assignment) | 0 | **0** |
| `DATABASE_URL` inside `*.unit.test.ts` | 3 (same three as every prior pass: `admin/src/main.unit.test.ts`, `admin/src/schema.unit.test.ts`, `scripts/dev-tmux.unit.test.ts`) | **0** — re-verified line-by-line this pass: `main.unit.test.ts` only asserts the early-return-when-unset branch via a saved/restored env var (never opens a connection), `schema.unit.test.ts` asserts the Prisma schema's datasource line contains the env-var name as a string literal, `dev-tmux.unit.test.ts` asserts spawned-process env/command strings contain the var name — none opens a real connection. |

No isolation-contract regression found since the 2026-07-23 pass despite +18 new/grown
test files this cycle — the Agent Type manifest system's tests all use the repo's
injected-double/fake-resolver conventions (`agent-cron-jobs.unit.test.ts` and
`seed-dev-agent.unit.test.ts` both inject a fake `AgentTypeManifestResolver` rather than
touching the filesystem or mocking a module).

## Trim analysis

**Zero trims identified**, same finding as all four prior passes. No new
duplicate-assertion pattern was introduced by this pass's delta — each new/grown test
file asserts a distinct concern for its layer: `agent-type-registry.unit.test.ts` covers
Zod schema validation in isolation, `agent-type-registry.schema.unit.test.ts` covers the
JSON-Schema-generator's output shape (a distinct property from validation — not
duplicative), `agent-type-manifest-loader.unit.test.ts` covers resolve/fallback/cache
behavior via injected reader (the 2 real-disk cases are a layer question, not a
duplication question — see Promote), `agent-cron-jobs.{unit,integration}.test.ts` and
`seed-dev-agent.unit.test.ts` cover their own consumers' use of the injected resolver
interface (not re-asserting the loader's own resolve logic — they assert *their own*
behavior given a manifest, which the loader tests don't cover), and
`agents-api.{smoke,integration}.test.ts`'s `typeName` cases assert the wire-level 400
rejection and persisted-row shape (properties no lower layer can prove). No E2E test was
reconsidered for trimming (the one E2E change this pass — the required-picker assertion
in `agents-page.e2e.ts` — proves a DOM/wire property no unit or integration test can:
that the browser-rendered form actually has the `required` HTML attribute).

## Tests — bucketed

### Reuse as-is

The large majority of the suite, effectively unchanged in shape from the 2026-07-23 pass
plus the newly-landed Agent Type manifest system coverage. Grouped by cluster; every
cluster was re-verified this pass by checking for new files against Phase 1's delta list,
not re-read in full where unchanged.

| Cluster | Test files | Layer(s) | Functional unit | Notes |
|---|---|---|---|---|
| `lib/` | `pricing`, `org-repo`, `request-context`, `sentry`, `web/toolbar`, `claim-ttl`, `task-store-types` (`.unit.test.ts`) | unit | Cross-service pure helpers | Unchanged this pass; `lib/agent-default-tools.ts`'s deletion (this cycle) had no test file to remove (already excluded — see Resolved section). |
| `metrics/src/*` | ~9 top-level + ~7 `lib/` + 2 `providers/` + 1 `dashboard/` | unit/integration/smoke | Formatters, provider selection, HTTP clients, `/metrics/*` routes, request coalescing | Unchanged this pass — no `metrics/` source changes in this cycle's Phase 1 delta. |
| `agent/src/*` | ~44 files | unit/integration/smoke | Work selection, check-\* precondition scripts, Slack Bolt handlers, `claude.ts` spawner, chat/task-store/GitHub clients, chat-poller loop | File count unchanged this pass; `agent/src/check-review.ts`'s bundle-completeness gate (D2198) landed inside the already-existing `check-review.unit.test.ts`, no new file needed. |
| `admin/src/*` | ~56 files (up from ~52 — Agent Type manifest system's 3 new files, `agent-type-registry.unit.test.ts` + `agent-type-registry.schema.unit.test.ts` + `agent-type-manifest-loader.unit.test.ts`) | unit/integration/smoke | Agent/Env/Cron/Tool/Token CRUD, K8s client, provisioning clients, admin UI (now agent-type-aware), chat-token daily rollup, work-queue snapshots, server composition, Agent Type registry + manifest resolution | `agent-type-registry.{unit,schema.unit}.test.ts` are both Reuse — right layer (pure schema validation / pure schema-generator output), no I/O, no fixture concerns. `agent-type-manifest-loader.unit.test.ts` is 12/14 tests Reuse-grade (clean injected-DI) with 2 tests flagged Promote (see below) rather than the whole file — the file itself stays open as Promote per the SKILL's file-level bucketing, but the majority of its content is correctly shaped. |
| `task-store/src/*` | ~24 files | unit/integration/smoke | Task/PR/Token services, dependency-resolution rules, routes | Unchanged this pass; no `task-store/` source changes in this cycle's Phase 1 delta. |
| `chat/src/*` | 10 files | unit/integration/smoke | Message/thread/token services, routes, OpenAPI schemas, spec generator | Unchanged this pass. |
| `mcp-server/src/*` | 8 files | unit/integration/smoke | Tool allowlist, tool-caller proxy, MCP wire protocol, inbound bearer auth, Streamable HTTP transport + idle-session reaper, app-factory composition | Unchanged this pass; no `mcp-server/` source changes in this cycle's delta. `mcp-server/src/main.ts` remains the one carried-forward Net-new gap (see below). |
| `brand/*` | 2 files | unit | Hex-color lint, CSS token generation | Unchanged. |
| `scripts/*` | ~16 files (up from 13 — `hitl.unit.test.ts` and `wait-for-agent.unit.test.ts` new, `dev-tmux.unit.test.ts` grew in place, `seed-dev-agent.unit.test.ts`/`seed-task-store-token.unit.test.ts` grew in place) | unit/integration/smoke | Coverage gate, config-docs checker, dev supervisors, seed scripts, version sync, HITL dev-worker loop, agent-provisioning wait-loop | `hitl.unit.test.ts` (new — 398 lines covering all 7 non-interface exports of the 739-line `hitl.ts`: `buildTaskCommand`, `parseHitlRepos`, `parseHitlAuthors`, `computeProvisionPlan`, `ensureHitlAgent`, `parseTasksResponse`, `buildClaudeSpawnEnv` — all pure/injected-fetch-double logic, matching the file's own header comment's isolation claim) and `wait-for-agent.unit.test.ts` (new, 174 lines for a 120-line source file) both land with the code, correctly injected, no filesystem/network touches. `dev-tmux.unit.test.ts` grew to assert the no-implicit-agent-creation behavior change (`"agent pane script waits for a developer-created agent before starting docker (no implicit agent — ATS-6.1)"`, `"buildStackCommands never invokes seed-dev-agent.ts — zero agents created implicitly"`) — confirms Phase 1's #2205 delta item has matching test coverage. `scripts/generate-agent-type-schema.ts` (new, 24-line thin CLI wrapper) has no dedicated test file, matching the established sibling-script convention (`generate-admin-spec.ts`, `generate-task-store-spec.ts`, `generate-chat-spec.ts` also have none) — the pure function it wraps (`buildAgentTypeJsonSchema`) is already unit-tested in `agent-type-registry.schema.unit.test.ts`. Non-gap, not separately bucketed. |
| `plugins/shipwright/scripts/*` | 15 files | unit | `check-*.ts` cron prechecks, `classify_test_layer.ts`, `check-helpers.ts` | Unchanged this pass. |
| `plugins/shipwright/{commands,agents,skills,references,test}/*` | ~30 files (28 dedicated `*.content.test.ts`) | content/unit/integration | Command/skill/agent markdown prose assertions | Command count grew from ~19 (2026-07-21 baseline) to 28 across intervening cycles per Phase 1; `.content.test.ts` count (28) tracks the growth — confirmed no orphaned content-test files for a deleted command/skill. |
| `site/tests/*.spec.ts` | 20 files (up from 12 at 2026-07-19 baseline, per direct Phase 1 re-count this cycle) | e2e | Marketing pages | Unchanged file set this pass; no new outage-linked commits found on re-check. Count correction reflects Phase 1's fresh direct enumeration, not a regression or new finding by this pass. |
| `admin/e2e/*.e2e.ts`, `metrics/e2e/*.e2e.ts` | 3 files | e2e | Admin login/agents pages, metrics dashboard | `admin/e2e/agents-page.e2e.ts` grew in place to assert the required-`typeName`-picker (`select[name=type]` with the `required` attribute) — matches #2205's UI change, re-validated per Gap #2 rather than assumed still-passing. `login-page.e2e.ts` and `metrics/e2e/`'s dashboard suite unchanged. |

### Promote / deepen

| Test file | Issue | Fix | Effort |
|---|---|---|---|
| `admin/src/agent-type-manifest-loader.unit.test.ts` | Filed as `.unit.test.ts` (12/14 cases correctly injected-DI, zero I/O), but 2 cases — `"loads the real committed coding manifest from disk via the default reader"` (line 100) and `"listTypes discovers the real committed agent-types/ dir via the default list fn"` (line 244) — construct `new AgentTypeRegistry()` with **no injected `read`/`list`**, which falls through to `defaultManifestReader`/`defaultTypeDirLister` and hits the real filesystem (`readFileSync`/`readdirSync` against `agent-types/coding/manifest.yaml` on disk). This is a genuine I/O-boundary crossing inside a file whose suffix promises none — exactly the failure mode `test-system.md`'s Gap #1 and `SKILL.md`'s "don't infer layer from filename" warning both call out. The intent (catching a broken repo-root path) is sound and worth keeping; the layer label is wrong for those 2 cases. | Extract the 2 real-disk cases into a new `admin/src/agent-type-manifest-loader.integration.test.ts` (matches Phase 1/2's prescribed canonical layer for this component — "integration, real file I/O, both happy-path resolve and fallback-to-coding cases," `test-system.md` line 64/493). Leave the remaining 12 injected-DI cases in the `.unit.test.ts` file — they're correctly shaped as-is. Not a rebuild: the assertions themselves are fine, only the file placement/suffix is wrong for 2 of 14 cases. | small — a file split + 2 test relocations, no logic changes, no new fixtures needed (the 2 cases already exercise the committed real `agent-types/coding/manifest.yaml`, which the integration layer's "real dependency behavior via recorded fixtures / injected doubles" convention already accommodates for local-filesystem boundaries per `test-system.md`'s Local execution architecture table). |

No other promote-shaped gap was found in this pass's review of the Agent Type manifest
system delta or the substantial in-place growth inside `scripts/hitl.ts`,
`scripts/dev-tmux.ts`, `admin/src/agents.ts`/`agents-api.ts`/`admin-ui.ts`/`admin-ui-pages.ts`
(all of which gained new `typeName`/`missingRequiredEnv`/no-implicit-creation logic this
cycle but landed with correctly-layered tests in the same PRs).

### Rebuild

None. No test file's layer, framework, or fundamentals were found wrong this pass (the
one layer mismatch found — the manifest-loader's 2 real-disk cases — is a Promote/split,
not a Rebuild: the assertions are sound, only 2 of 14 cases need to move files).

### Trim (redundant assertion)

None. See Trim analysis above.

### Net-new

Sorted by inventory criticality. No new non-test source files landed this cycle without
matching coverage — both open Net-new items are carried forward unchanged from the
2026-07-23 pass, neither part of this cycle's Phase 1 delta.

| Inventory item | Prescribed layer | Criticality | Estimated effort |
|---|---|---|---|
| `mcp-server/src/main.ts` (process entrypoint — fail-closed guard on missing `SHIPWRIGHT_MCP_SERVER_TOKEN`, then `Bun.serve`) — carried forward, not part of this cycle's Phase 1 delta | smoke (entrypoint) | critical (per Phase 1) | **Real gap, unchanged from the 2026-07-23 pass.** The fail-closed guard (`if (!token) throw new Error(...)`, `mcp-server/src/main.ts:27-32`) runs at module top level, before `createApp` is invoked — `mcp-server/src/index.smoke.test.ts` calls `createApp` directly with an injected token and never imports or executes `main.ts`, so it cannot exercise this guard. No test file in the repo references `SHIPWRIGHT_MCP_SERVER_TOKEN`. Re-verified this pass by direct grep — still zero matches. Trivial effort — a few lines exercising the module in isolation (e.g. via a subprocess or a dynamic `import()` with the env var deleted). |
| `admin/src/clock.ts` (`Clock`/`SystemClock`/`FixedClock` implementation for admin) — carried forward, not part of this cycle's Phase 1 delta | unit | low | trivial, optional — carried forward unchanged since the 2026-07-19 pass (now its 4th consecutive appearance). The sibling `Clock` files (`agent/src/clock.ts`, `metrics/src/lib/clock.ts`, `plugins/shipwright/scripts/clock.ts`, `mcp-server/src/clock.ts`) all lack a dedicated test file too; near-zero logic (two one-line methods), proven correct via consumers per the repo's established convention. Not escalated to a Net-new gap — this is a stable non-gap, distinct from the measurement-only speed item escalated above (which is escalated precisely because it depends on infra this sandbox lacks, not because it's trivial). |

## Infrastructure — bucketed

### Reuse as-is

- **Bun test runner config** (`bunfig.toml`'s `pathIgnorePatterns`) — re-verified this
  pass: `["**/site/**", "**/metrics/e2e/**", "**/admin/e2e/**", "**/*.canary.test.ts"]`,
  unchanged and correct.
- **CI workflow shape** (`.github/workflows/ci.yml`) — matches Phase 2's documented shape;
  Postgres service container wiring for admin/task-store/chat unchanged.
- **Postgres service container for admin/task-store/chat** — real DB per test run, all
  three `DATABASE_URL_SHIPWRIGHT_*` vars wired in `ci.yml`.
- **`Clock` injection helpers** — five implementations
  (`agent/src/clock.ts`, `metrics/src/lib/clock.ts`, `plugins/shipwright/scripts/clock.ts`,
  `admin/src/clock.ts`, `mcp-server/src/clock.ts`) plus
  `plugins/shipwright/scripts/test-helpers/doubles.ts` — unchanged this pass, still the
  strongest, most consistently-applied convention in the repo.
- **`AgentTypeManifestResolver` injected-fake pattern (new convention this cycle)** —
  `admin/src/agent-cron-jobs.unit.test.ts` and `scripts/seed-dev-agent.unit.test.ts` both
  define a lightweight fake implementing the `AgentTypeManifestResolver` interface rather
  than touching the filesystem or mocking a module — a clean extension of the repo's
  existing "mock the client interface, not the I/O primitive" convention (mirrors the
  `Recorded*Client` pattern) applied to a new local-filesystem boundary. Worth naming
  explicitly as its own reusable pattern for Phase 4, since `test-system.md`'s Local
  execution architecture table (line 78) prescribes exactly this shape and it's now
  proven out in two independent consumers.
- **Recorded-fixture doubles** — `RecordedKubernetesClient`, `metrics/src/providers/task-store-recorded.ts`,
  `metrics/src/fixtures/task-store-fixtures.ts`, `agent/src/shipwright-config-client.ts` —
  unchanged this pass.
- **`scripts/check-coverage.ts`** — the 80/80 lcov-threshold gate, unchanged.

### Promote / deepen

None at the infrastructure level this pass. (The one Promote finding this pass —
`agent-type-manifest-loader.unit.test.ts`'s 2 real-disk cases — is bucketed under Tests
above, not Infrastructure, since it's a test-file layer-placement issue, not a shared
helper/config/fixture issue.) The prior passes' "recorded-fixture coverage breadth" item
(extending `Recorded*Client` to ~9 remaining `Http*Client` modules) remains a Phase 2
design-level backlog item, not a migration-bucket verdict on any existing file — carried
in `test-system.md`'s Gap #5, not restated here as a bucket entry to avoid
double-tracking.

### Rebuild

None.

### Trim (redundant assertion)

None.

### Net-new

None. No new infrastructure gap identified this pass — the Bun test runner config, CI
workflow shape, Postgres service containers, `Clock` helpers, recorded-fixture doubles,
and coverage gate are all confirmed unchanged and correct (see Reuse as-is above).
`.github/PULL_REQUEST_TEMPLATE.md` remains resolved (closed at the 2026-07-19 pass, no
regression this cycle). The new `AgentTypeManifestResolver` fake-injection pattern is
bucketed under Reuse (a working, already-proven pattern), not Net-new (it's not a gap —
it's newly-observed infrastructure that already exists and works).

## Risk callouts

No test currently passing (green) received a `delete` or `rebuild` verdict this pass, so
the SKILL's mandatory risk-callout requirement has no entries to populate — same as all
four prior passes. The one Promote finding (`agent-type-manifest-loader.unit.test.ts`'s
file split) carries no regression risk: it moves 2 already-passing, already-correct test
cases to a differently-suffixed file with no assertion changes.

## Speed measurement

- **Measured:** no local re-measurement this pass (no local Postgres available in this
  sandbox; see Method note above) — same as every prior pass. **This is now the 5th
  consecutive cycle this measurement-only item has gone unactioned — see the escalation
  in the Method note above; Phase 4 must place it in Milestone 1.**
- **Current per-layer p95:** not measured *by this pass*, but the real, `time`-wrapped
  per-layer measurement at `docs/test-readiness/speed-baseline.md` (committed `8a037efc`,
  PR #1580, merged 2026-07-15) remains valid and is correctly cross-referenced from
  `test-system.md`'s Speed budgets section — no regression found, but the baseline itself
  is now 10 days stale relative to this cycle's substantial in-place logic growth.
- **Tests over hard cap:** none identified via static review, consistent with
  `speed-baseline.md`'s measured numbers. `admin/src/admin-ui.smoke.test.ts` and
  `admin/src/admin-ui-pages.unit.test.ts` remain the two largest files by line count
  (unchanged, both large due to test-case count, not per-test slowness). This pass's new
  files (`admin/src/agent-type-registry.{unit,schema.unit}.test.ts`,
  `agent-type-manifest-loader.unit.test.ts`, `scripts/hitl.unit.test.ts`,
  `scripts/wait-for-agent.unit.test.ts`) are all reasonably sized, single-concern files —
  no size/slowness signal from static review, but static review is exactly the kind of
  check that cannot catch a test that has silently grown slow within its existing budget
  headroom (the reason the speed-measurement item above is being escalated, not just
  re-noted).

## Next step

Run `/test-roadmap` to synthesize this migration audit with the Phase 1 inventory and
Phase 2 blueprint into the executable roadmap. Candidate Phase 4 inputs from this
document:

1. **Escalated (must land in Milestone 1 per SKILL.md's 3-cycle carry-forward rule):** a
   real per-layer speed measurement against a Postgres-enabled environment, refreshing
   the 10-day-stale `speed-baseline.md` — unactioned for 5 consecutive cycles.
2. **Small, real:** split `admin/src/agent-type-manifest-loader.unit.test.ts`'s 2
   real-disk test cases into a new `.integration.test.ts` file, matching the component's
   prescribed canonical layer.
3. **Trivial, real:** add a smoke-adjacent test for `mcp-server/src/main.ts`'s
   fail-closed guard on missing `SHIPWRIGHT_MCP_SERVER_TOKEN` — carried forward unchanged
   for 2 cycles now.
4. **Trivial, optional, non-gap:** `admin/src/clock.ts` — carried forward for
   documentation continuity only, not a real gap.

0 net-new infra items, 0 trim candidates, 0 rebuild candidates this cycle. The Agent Type
manifest system — this cycle's headline Phase 1 delta — landed with matching test
coverage across all four of its prescribed layers (unit schema validation,
integration-shaped resolver behavior, unit-injected consumer tests, smoke + e2e
happy/4xx/picker paths), with only the one file-placement Promote finding above.
