# Test Migration

> Phase 3 of the test-readiness pipeline. Reconciles existing tests and infrastructure
> against the Phase 1 inventory ([`test-inventory.md`](./test-inventory.md), refreshed at
> `297fb61e`) and the Phase 2 blueprint ([`test-system.md`](./test-system.md), refreshed
> at `297fb61e`), bucketing each existing test file and each inventory item into one of
> five categories.
>
> Generated by `/test-migration` on 2026-08-14 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-20260814`).

## Method note

This pass diffed the current file tree against the 2026-08-11 pass's findings (baseline
doc committed at `0428fe71`) and re-verified every open item directly against current
repo state — per this cycle's standing instruction not to carry forward "gap"/"missing"
claims without re-checking them, with particular scrutiny on the carried-forward
`task-service.ts` claim-race gap (net-new item from last cycle) and Phase 2's two new
gap items (#18 `check-review.ts` fix-density, #19 the new compile-time-only check
pattern):

1. Enumerated every test file via the Phase 2 framework matrix's glob patterns: 127
   `*.unit.test.ts` (+2), 63 `*.integration.test.ts` (+1), 43 `*.smoke.test.ts`
   (unchanged), 39 `*.content.test.ts` (+1) (272 `bun test`-scanned files, up from 268),
   plus 21 `site/tests/*.spec.ts` (unchanged) and 3 Playwright `*.e2e.ts` (2 `admin/e2e/`,
   1 `metrics/e2e/`, unchanged) = **296 test files total** — +4 from the 2026-08-11 pass,
   confirmed via `git diff --stat 0428fe71..HEAD --diff-filter=A -- '*.unit.test.ts'
   '*.integration.test.ts' '*.smoke.test.ts' '*.content.test.ts' '*.spec.ts' '*.e2e.ts'`
   (exactly 4 additions, 0 deletions): `plugins/shipwright/scripts/compute-unaddressed-findings.unit.test.ts`,
   `plugins/shipwright/scripts/diagnose-review-candidacy.unit.test.ts`,
   `plugins/shipwright/skills/review-staged/SKILL.content.test.ts`,
   `task-store/src/task-service.integration.test.ts`.
2. Cross-referenced Phase 1's delta list (87 commits / 111 files since the `0428fe71`
   baseline, ~35 non-doc/non-version-bump/non-chart source files) against the test-file
   glob to confirm each has matching coverage, reading the actual test body for the
   files with the most consequential changes: `agent/src/check-review.ts` (5 fixes),
   `plugins/shipwright/scripts/diagnose-review-candidacy.ts` (RCO-1.4, new),
   `admin/src/blocked-by-type-parity.check.ts` (new, no `bun test` coverage by design —
   see below), `task-store/src/task-service.ts` (filter-honoring + assignee-fallback),
   `agent/src/worktree-reaper.ts` (org/repo normalization), `agent/src/check-patch.ts`
   (stale-CI rerun), `agent/src/loop-orchestrator.ts` (Sentry tags),
   `agent/src/pr-state-reconciler.ts` (per-pass logging), `site/src/pages/{compare,self-hosted}.astro`.
3. **Directly investigated the carried-forward `task-service.ts` claim-race net-new item**
   (flagged last cycle as genuinely open — the `pull-request-service.ts` sibling had a
   real concurrent-caller integration test, but `task-service.ts` only had mocked-double,
   sequential coverage). Confirmed **resolved this cycle**: PR **#2560** ("test: add
   TaskService.claim() concurrent-race integration test (T-081)") landed a new
   `task-store/src/task-service.integration.test.ts` file (132 lines) with a dedicated
   real-Postgres test — `"concurrent claims against the SAME seeded pending row: exactly
   one wins, the other gets ConflictError"` — using `Promise.allSettled` to race two
   genuinely concurrent `claim()` calls against a seeded row, the exact harness shape the
   prior cycle's Net-new entry called for as a same-shape port from
   `pull-request-service.integration.test.ts`. Read the file directly (not just the
   filename) to confirm the assertion actually races two calls rather than sequential
   single-caller checks — it does. **This closes last cycle's sole Net-new item that
   wasn't a long-standing carry-forward.**
4. **Directly investigated Phase 2's two new gap items (#18, #19) against actual test
   file contents:**
   - **#18, `agent/src/check-review.ts`'s five-fix-in-3-days density** — read
     `check-review.unit.test.ts` in full (2267 lines, 98 `test()` cases across 4
     `describe` blocks). Found meaningful "even when"-shaped combination coverage already
     exists for several of the flagged interactions — e.g. `"excludes a PR whose record
     has staged:true and matching reviewedCommitSha, even when reviewState reads pending"`
     (line 597), `"isAuthorAllowed does not re-exclude a non-allowlisted author once a
     PrRecord already exists (AAL-3.2)"` (line 774), `"a live terminal review at head
     still excludes the PR even when the task-store record independently says eligible"`
     (line 1051), plus an explicit precedence-ordering test (`"check precedence:
     already-reviewed-live is checked before task-blocked"`, line 2096) and four
     dedicated review-thread-reply cases (lines 1762-1941) covering the exact
     author-reply-vs-terminal-skip interaction Phase 2 was most concerned about. **This
     softens but does not fully close Phase 2's concern**: the coverage is real and
     substantive, but it's pairwise (one flagged fix's interaction with one other check
     at a time), not a full combinatorial matrix across all five of this cycle's fixes
     simultaneously (e.g. no single test simultaneously exercises a stale-`commitSha`
     PR + a review-thread reply + a non-allowlisted author together). Downgraded from
     Phase 2's "possible coverage gap" framing to a **Promote/deepen** item, not a
     Net-new gap — see below.
   - **#19, `admin/src/blocked-by-type-parity.check.ts`'s new pattern** — confirmed this
     file has, by design, zero `bun test` coverage: it's a `tsc --noEmit`-only
     exhaustiveness check. Verified it IS wired into CI: `admin/package.json`'s
     `typecheck` script includes `tsc --noEmit -p tsconfig.type-parity.json` alongside
     the default `tsconfig.json` check, and this cycle's live CI run (`297fb61e`)
     confirms the `lint / typecheck / test` job passed with both projects included. **No
     migration-bucket action needed** — this isn't a test file to bucket (it produces no
     `bun test` output), and Phase 2 already declined to formalize a new layer for it
     pending a second instance. Not carried forward as an open item here since there is
     no coverage gap, only a taxonomy question already tracked in `test-system.md`.
5. Re-ran the isolation-contract grep sweep (`mock.module(`, `global.fetch =`,
   `global.console =`, `DATABASE_URL` inside `*.unit.test.ts`) across the full current
   suite (296 files) — zero new violations. 38 `mock.module(` string matches across 22
   files, all manually confirmed to be inside `*` or `//` comment lines documenting the
   *absence* of mocking, not executable calls. Zero `global.fetch =`/`global.console =`
   assignments. Same three pre-existing non-violation `DATABASE_URL` string-literal
   matches as every prior pass (`admin/src/main.unit.test.ts`,
   `admin/src/schema.unit.test.ts`, `scripts/dev-tmux.unit.test.ts`).
6. Pulled a fresh live Tier 1 speed measurement from CI (`gh run view --log` against the
   green `lint / typecheck / test` run at this cycle's exact HEAD, `297fb61e`) — see
   Speed measurement below.
7. Spot-checked every other carried-forward gap item (#1-#14, #17 from prior cycles)
   that Phase 2 restated unchanged, directly against current file contents rather than a
   prior doc's snapshot: `browser.integration.test.ts` (present, unchanged),
   `admin/src/clock.ts` (still no dedicated unit test file, unchanged, still a non-gap
   per the repo's established `Clock`-file convention), `scripts/hitl.ts`/`hitl.unit.test.ts`
   (still only the pure-planning half tested; no `hitl.integration.test.ts`), and the
   `pr-state-reconciler.ts` orphan-reconciliation removal (PR #2527, closed as resolved
   last cycle — reconfirmed via grep that zero references to the removed functions have
   resurfaced).

**Speed measurement: re-run this pass via CI history (Tier 1 only).** No local Postgres
in this sandbox, so a local `bun test` run was not attempted — Tier 1's aggregate pull
needs only `gh run view --log` against the most recent green `lint / typecheck / test`
job, which this sandbox does have, at this cycle's exact HEAD commit `297fb61e`.

## The `pr-state-reconciler.ts` orphan-reconciliation removal — resolved (2026-08-11)

Closed in the 2026-08-11 pass: the orphan-task reconciliation pass removal (547 lines of
production + test code) was confirmed a deliberate, reviewed, well-tested clean removal
(PR #2527, DTC-1.1), superseded by `StaleClaimReaper` + the ready-set FIFO +
`dev-task.md` Step 4's Branch/PR Reality Check. Not restated in full here — see the
2026-08-11 version of this document (`git log -p -- docs/test-readiness/test-migration.md`)
for the full investigation writeup. No new evidence this cycle changes that verdict.

## Summary

| Bucket | Test count | Infra count | Effort |
|---|---|---|---|
| Reuse as-is | ~294 | 7 | — |
| Promote / deepen | 1 | 0 | small |
| Rebuild | 0 | 0 | — |
| Delete (redundant) | 0 | 0 | — |
| Net-new | 2 | 0 | small (scoping/case additions, not large new files) |

**Total existing test files discovered:** 127 unit + 63 integration + 43 smoke + 39
content = 272 `bun test`-scanned files, + 21 `site/tests/*.spec.ts` + 3 Playwright
`*.e2e.ts` (2 `admin/e2e/`, 1 `metrics/e2e/`) = **296 test files total** — +4 from the
2026-08-11 pass (`compute-unaddressed-findings.unit.test.ts`,
`diagnose-review-candidacy.unit.test.ts`, `review-staged/SKILL.content.test.ts`,
`task-service.integration.test.ts`), all new files backing new production code or
closing a previously-flagged gap, correctly landed — not open Net-new work items. These 4
fold into "Reuse as-is" (already-correct, already-passing coverage for this cycle's
delta). The Net-new bucket's count of 2 (down from 3) reflects: the `task-service.ts`
claim-race item resolved this cycle (see Method note step 3) and moved to Reuse as-is;
one item (**`check-review.ts`'s combinatorial guard-condition coverage**) moved from a
would-be new Net-new candidate to Promote/deepen instead, since substantial pairwise
combination coverage already exists (see Method note step 4) — it's a depth gap, not a
zero-coverage gap.

**Headline finding:** every real source change in this cycle's 87-commit / 111-file
Phase 1 delta (~35 non-doc/non-version-bump/non-chart source files) already has matching
test coverage at the correct layer, landed in the same commits/PRs as the production
changes — continuing the pattern observed in every prior pass. The one open item carried
from last cycle (`task-service.ts`'s claim-race regression test) is now resolved (PR
#2560, T-081). The new `admin/src/blocked-by-type-parity.check.ts` compile-time-only
check is confirmed wired into CI's `typecheck` step with no coverage gap — it simply
falls outside the `bun test` bucketing model by design, not a migration finding. The
`check-review.ts` fix-density flagged by Phase 2 is real but the concern is now
better-scoped: meaningful pairwise combination coverage exists, but no test yet exercises
three-or-more of this cycle's fixed exclusion checks simultaneously — a Promote/deepen
item, not a coverage hole. No new isolation-contract regression, no new
redundant-coverage pattern, no rebuild candidates, no trim/delete candidates.

## Isolation contract audit

Grepped the full current suite (296 files) for the three hard-rule signals from root
`CLAUDE.md`'s Test isolation section and `references/principles.md`'s `t1_no_global_mocking`:

| Signal | Files matched | Real violations |
|---|---|---|
| `mock.module(` | 22 files / 38 matches, all in header-comments (`grep -rn "mock\.module(" --include="*.test.ts"`, e.g. `admin/src/agent-deletion.unit.test.ts:7` — `"...contract — no mock.module(), no global overrides."`) | **0** — every match manually confirmed to start with `*` or `//` (a comment line), not an executable call |
| `global.fetch = ` (assignment) | 0 | **0** |
| `global.console = ` (assignment) | 0 | **0** |
| `DATABASE_URL` inside `*.unit.test.ts` | 3 (same three as every prior pass: `admin/src/main.unit.test.ts`, `admin/src/schema.unit.test.ts`, `scripts/dev-tmux.unit.test.ts`) | **0** — unchanged from prior findings; all assert on the env-var name as a string literal or an early-return guard, none opens a real connection |

No isolation-contract regression found since the 2026-08-11 pass. Match count grew by
exactly 1 (37→38), consistent with the +4 new test files this cycle (one new file's
header comment references `mock.module(` in its own isolation-contract doc-comment,
same pattern as all 37 prior matches).

## Delete (redundant assertion) analysis

**Zero delete/trim candidates identified**, same finding as every prior pass. No new
duplicate-assertion pattern was introduced by this cycle's delta. This cycle's new
`compute-unaddressed-findings.unit.test.ts` (test-file companion to the already-existing
production module, extended with `diagnose-review-candidacy.unit.test.ts`),
`diagnose-review-candidacy.unit.test.ts`, `review-staged/SKILL.content.test.ts`, and
`task-service.integration.test.ts` each assert distinct properties not covered anywhere
else, correctly non-duplicative per layer. `task-service.integration.test.ts`'s
concurrent-claim-race test and `task-service.unit.test.ts`'s existing mocked-double
sequential tests are a unit-vs-real-Postgres pair proving distinct properties (WHERE-clause
SQL shape vs. real-DB race arbitration), not duplicate coverage — same pattern already
established for `pull-request-service.ts`'s equivalent pair last cycle.

## Tests — bucketed

### Reuse as-is

The large majority of the suite, unchanged in shape from the 2026-08-11 pass aside from
the narrow delta below. Grouped by cluster; each cluster was checked against Phase 1's
delta list, not re-read in full where unchanged.

| Cluster | Test files | Layer(s) | Functional unit | Notes |
|---|---|---|---|---|
| `lib/` | `pricing`, `org-repo`, `request-context`, `sentry`, `web/toolbar`, `claim-ttl`, `github-login`, `task-store-types`, `http` | unit | Cross-service pure helpers | Unchanged this cycle. |
| `metrics/src/*` | ~9 top-level + ~7 `lib/` + 2 `providers/` + 1 `dashboard/` | unit/integration/smoke | Formatters, provider selection, HTTP clients, `/metrics/*` routes, request coalescing | Unchanged this cycle. |
| `agent/src/*` | ~49 files | unit/integration/smoke | Work selection, check-\* precondition scripts, Slack Bolt handlers, `claude.ts` spawner, chat/task-store/GitHub clients, chat-poller loop, author-allowlist ref, headless-browser launcher | This cycle: `check-review.ts`'s 5 candidate-exclusion correctness fixes, each with a dedicated guard-condition case added to `check-review.unit.test.ts` (now 98 test cases, 2267 lines — see Method note step 4 for the combination-coverage finding, downgraded to Promote/deepen below); `check-patch.ts`'s stale-cancelled-CI rerun-before-escalate logic; `loop-orchestrator.ts`'s Sentry dispatch tags (observability-only, no dedicated assertion per repo convention); `pr-state-reconciler.ts`'s per-pass summary logging (observability-only); `worktree-reaper.ts`'s org/repo-normalization fix to `resolveOwningScopedRepo`, one new guard case in `worktree-reaper.unit.test.ts`. |
| `admin/src/*` | ~57 files (+`blocked-by-type-parity.check.ts`, no test suffix by design) | unit/integration/smoke | Agent/Env/Cron/Tool/Token CRUD, K8s client, provisioning clients, admin UI, chat-token daily rollup, work-queue snapshots, server composition, agent-type manifest resolution | This cycle: `agents-api.ts`/`agent-cron-runs.ts` gain server-side `itemId`/`phaseId` cron-run filters, with `agent-cron-runs.integration.test.ts` extended; `admin-ui-pages.ts`'s blocked-task badge/detail render-crash fix, with a new `BlockedByEntry` parity assertion in `admin-ui-pages.unit.test.ts`. New file `blocked-by-type-parity.check.ts` has zero `bun test` coverage by design (compile-time-only, wired into `admin/package.json`'s `typecheck` script via `tsconfig.type-parity.json`) — confirmed passing in this cycle's live CI run, not a migration-bucket gap. |
| `task-store/src/*` | ~28 files (was ~27, +`task-service.integration.test.ts`) | unit/integration/smoke | Task/PR/Token services, dependency-resolution rules, routes | This cycle: `task-service.ts`'s `listReady()`/`listBlocked()` gain full session/repo/org/source/claimedBy/pr/branch/assignee filter support (ATB-1.3 shared post-filter extraction) plus own-assignee fallback for repos-absent agent tokens — incremental filter-combination cases added to `task-service.unit.test.ts`. `task-service.ts`'s `claim()` concurrent-claim regression test (T-081, PR #2560) landed in the **new `task-service.integration.test.ts`** — resolves last cycle's sole open Net-new item (see Method note step 3). `Task.requiresHumanApproval` column dropped via a companion Prisma migration — confirmed no test file references the removed field. |
| `chat/src/*` | 10 files | unit/integration/smoke | Message/thread/token services, routes, OpenAPI schemas, spec generator | Unchanged this cycle. |
| `mcp-server/src/*` | 8 files | unit/integration/smoke | Tool allowlist, tool-caller proxy, MCP wire protocol, inbound bearer auth, Streamable HTTP transport + idle-session reaper, app-factory composition, fail-closed entrypoint guard | Unchanged this cycle — no files in Phase 1's delta touch `mcp-server/`. |
| `brand/*` | 2 files | unit | Hex-color lint, CSS token generation | Unchanged. |
| `scripts/*` | 17 files | unit/integration/smoke | Coverage gate, config-docs checker, dev supervisors, seed scripts, version sync, agent-provisioning poll loop, Minikube stack orchestration | This cycle: `minikube.ts`'s Apple-Silicon/shared-ingress reliability fix, one guard case added to `minikube.unit.test.ts`. `scripts/hitl.ts`/`hitl.unit.test.ts` unchanged — still only the pure-planning half tested; see Net-new below for the open integration-harness gap. |
| `plugins/shipwright/scripts/*` | 13 files (was 11, +`compute-unaddressed-findings.unit.test.ts`, +`diagnose-review-candidacy.unit.test.ts`) | unit | `check-*.ts` cron prechecks, `classify_test_layer.ts`, `check-helpers.ts`, `compute-review-verdict.ts`, `compute-unaddressed-findings.ts`, **`diagnose-review-candidacy.ts` (RCO-1.4, new)** | New this cycle: `diagnose-review-candidacy.ts`'s CLI wrapper around `check-review.ts`'s exported `traceReviewCandidacyDecision()` — `diagnose-review-candidacy.unit.test.ts` (269 lines) covers argument parsing and trace-output formatting against fixed fixture data, reusing `check-review.ts`'s existing recorded-GitHub-review-data fixtures rather than introducing new ones. (Baseline corrected from a prior-cycle miscounting bug: the 2026-08-11 doc's "16 files (was 15, ...)" row for this directory was already inflated at the time — ground-truth `git ls-tree` against the `0428fe71` baseline commit shows 11 `.unit.test.ts` files, not 18; corrected here to the verified 11→13 delta.) |
| `plugins/shipwright/{commands,agents,skills,references,test}/*` | 39 dedicated `*.content.test.ts` files (was 38, +`review-staged/SKILL.content.test.ts`) | content/unit/integration | Command/skill/agent markdown prose assertions | New this cycle: `review-staged/SKILL.content.test.ts`, backing an extension to the pre-existing `review-staged/SKILL.md`. `code-reviewer.md`'s PVD-1.2 extension (surfacing prior CURRENT_USER review bodies) covered by its existing `code-reviewer.unit.test.ts` extension. `investigate-cron/SKILL.md`'s extension covered by its existing `SKILL.content.test.ts`/`SKILL.unit.test.ts` pair. `deploy.md`'s `requiresHumanApproval` merge-gate removal covered by its existing `deploy.content.test.ts` extension. |
| `site/tests/*.spec.ts` | 21 files | e2e | Marketing pages, incl. 11 `docs/*` page specs | This cycle: `compare.spec.ts`/`self-hosted.spec.ts` extended with one assertion each for the new "Factory" comparison-table row, same file count. |
| `admin/e2e/*.e2e.ts`, `metrics/e2e/*.e2e.ts` | 3 files | e2e | Admin login/agents pages, metrics dashboard | Unchanged this cycle. |

### Promote / deepen

One item this cycle — downgraded from what could have been framed as a Net-new gap,
since substantial coverage already exists and this is a depth/combination concern, not a
zero-coverage one:

| Test file | Current state | Target state | Effort |
|---|---|---|---|
| `agent/src/check-review.unit.test.ts` (`getReviewCandidates`/`traceReviewCandidacyDecision`) | 98 test cases exercising each of this cycle's 5 fixed exclusion checks individually, plus several pairwise "even when X" combination cases (e.g. staged+pending, allowlist+existing-record, terminal-review+task-store-eligible) | Add 2-3 explicit multi-condition matrix cases combining 3+ of: stale-`commitSha`/`reviewedCommitSha` divergence, review-thread-reply freshness, author-allowlist status, and task-blocked state — proving the checks compose correctly in the order `traceReviewCandidacyDecision` documents, not just pairwise | small — the fixture-building helpers and `traceReviewCandidacyDecision`'s existing trace-output assertions already exist; this is additional cases in the same file using the same harness, not new tooling |

### Rebuild

None. No test file's layer, framework, or fundamentals were found wrong this pass.

### Delete (redundant)

None. See Delete (redundant assertion) analysis above.

### Net-new

Sorted by criticality. Both items are carried forward unchanged from prior cycles — no
new Net-new item surfaced this cycle (the `task-service.ts` claim-race item resolved,
see Reuse as-is above):

| Inventory item | Prescribed layer | Criticality | Estimated effort |
|---|---|---|---|
| **Local HITL dev-loop bootstrap** (journey #5) — the full `SHIPWRIGHT_HITL_REPOS` clone → `agent-policy.md` seed → task-store/admin boot → poll → dispatch sequence has no integration-level test exercising the sequence end-to-end with an injected `gh` exec fake, even though the pure planning helpers (`computeMissingClones`, `computeProvisionPlan`) are already fully unit-tested | integration-composed (per Phase 2's Test-data strategy — no browser, single-process orchestration with an injected exec fake) | medium (developer-experience path, not customer-facing, but blocks the team's own local dev-loop if it regresses) | small — the hardest planning logic is already isolated and tested; the remaining work is wiring an injectable `gh`/process-exec seam into `scripts/hitl.ts`'s orchestration function (if not already present) and one sequencing test |
| `admin/src/clock.ts` (`Clock`/`SystemClock`/`FixedClock` implementation for admin) — carried forward, unrelated to this cycle's Phase 1 delta | unit | low | trivial, optional — carried forward unchanged from every prior pass. All sibling `Clock` files across the repo lack a dedicated test file too; near-zero logic (two one-line methods), proven correct via consumers per the repo's established convention. |

## Infrastructure — bucketed

### Reuse as-is

- **Bun test runner config** (`bunfig.toml`'s `pathIgnorePatterns`) — re-verified this
  pass: `["**/site/**", "**/metrics/e2e/**", "**/admin/e2e/**", "**/*.canary.test.ts"]`,
  unchanged and correct.
- **CI workflow shape** (`.github/workflows/ci.yml`) — matches Phase 2's documented
  shape; unchanged this cycle.
- **Postgres service container for admin/task-store/chat** — real DB per test run, all
  three `DATABASE_URL_SHIPWRIGHT_*` vars wired in `ci.yml`.
- **`Clock` injection helpers** — implementations across the repo plus
  `plugins/shipwright/scripts/test-helpers/doubles.ts` — still the strongest, most
  consistently-applied convention in the repo. No new consumer landed this cycle that
  deviates from the pattern.
- **Recorded-fixture doubles** — `RecordedKubernetesClient`,
  `metrics/src/providers/task-store-recorded.ts`,
  `metrics/src/fixtures/task-store-fixtures.ts`, `agent/src/shipwright-config-client.ts` —
  unchanged this cycle; pattern is stable and extending cleanly to new clients.
- **`scripts/check-coverage.ts`** — the 80/80 lcov-threshold gate, unchanged. Confirmed
  passing this cycle's CI run at 81.32%/82.23%.
- **`.github/PULL_REQUEST_TEMPLATE.md`** — confirmed present directly, matches
  `test-system.md`'s Repo configuration section's note that this is resolved, not a gap.
- **`docs/test-readiness/speed-baseline.md`** — not re-refreshed this cycle; its numbers
  remain directionally consistent with this pass's live CI pull (see Speed measurement
  below) — steady growth, no budget concern.

### Promote / deepen

None. The design-level backlog item (extending `Recorded*Client` fixture-double coverage
to the remaining `Http*Client` modules named in `test-system.md`'s Local execution
architecture table) remains a Phase 2 design-level item, not a migration-bucket verdict on
any existing infra file — carried in `test-system.md`'s gaps summary, not restated here.

### Rebuild

None.

### Delete (redundant)

None.

### Net-new

None. No new infrastructure gap identified this cycle. The new
`pull-request-service.integration.test.ts`'s concurrent-caller race harness
(`Promise.allSettled` racing two `claim()` calls) is a reusable pattern for the
`task-service.ts` Net-new item above, but requires no new shared cross-workspace
infrastructure — it's colocated with the service's own integration-test file, per Phase
2's Shared helpers section's own guidance for this exact harness shape.

## Risk callouts

No test currently passing (green) received a `delete` or `rebuild` verdict this pass, so
the SKILL's mandatory risk-callout requirement has no entries to populate for the test
suite as it exists today.

**Adjacent risk note (not a delete/rebuild verdict, but worth flagging given the
magnitude):** the `pr-state-reconciler.ts` orphan-reconciliation removal (PR #2527)
deleted 547 lines of passing, green test assertions alongside their production code.
This audit confirmed the removal was deliberate, reviewed, and the superseding mechanisms
(`StaleClaimReaper`, ready-set FIFO, `dev-task.md` Step 4) are real and tested — so no
action is recommended. Flagging only so a future cycle doesn't need to re-derive this
finding from scratch if the same pattern (large deletion of tested code) recurs elsewhere
without a PR reference this clear.

## Speed measurement

### Tier 1 — aggregate (measured this pass)

- **Run:** [31760943817](https://github.com/app-vitals/shipwright/actions/runs/31760943817)
  (CI on commit `297fb61e`, this cycle's exact HEAD), 2026-08-14T01:32:53Z. Fully green —
  no failing jobs.
- **Job wall-clock:** 2m39s (`lint / typecheck / test`, 01:33:09Z–01:35:48Z), of which the
  `bun test` step itself ran 41.19s.
- **Tests:** 6,501 tests pass, 0 fail, across 273 files — per the runner's own summary
  line ("Ran 6501 tests across 273 files."). Coverage held at 88.84% lines / 88.00%
  functions (threshold 80%/80%).
- **vs. budget:** 2m39s against the <15 min Full-PR-pipeline budget — **~18% of budget,
  well within**, consistent with every prior cycle's ~13-18% readings. Not trending
  toward the escalation trigger. The escalation formula (aggregate wall-clock >7.5 min,
  sustained across 2 consecutive measurements) is nowhere near tripped.

Test-count growth trajectory: 5,762 (2026-07-28 `speed-baseline.md`) → 6,041-6,042
(2026-08-01) → 6,070 (2026-08-05) → 6,071 (2026-08-06) → 6,177 (2026-08-09) → 6,288
(2026-08-11) → 6,501 (2026-08-14, this pass) — steady, expected growth (+213 tests over
the 3-day gap since 2026-08-11), consistent with this cycle's 4-new-file delta plus a
substantial number of incremental cases added to existing files (5 `check-review.ts`
exclusion-check fixes, `task-service.ts`'s filter-honoring cases, the new
`task-service.integration.test.ts`'s concurrent-race test, `worktree-reaper.ts`'s
normalization guard case, `minikube.ts`'s reliability-fix case).

### Tier 2 — per-layer breakdown (conditional)

- **Tier 2 triggered:** no — Tier 1's aggregate is at ~18% of the <15 min budget this
  pass, far below the 50% escalation threshold.

Tier 2 not triggered — suite is comfortably within the Tier 1 aggregate budget. Per the
SKILL's explicit guidance, this is a valid, complete outcome, not a gap requiring
escalation. No per-layer numbers fabricated.

## Next step

Run `/test-roadmap` to synthesize this migration audit with the Phase 1 inventory and
Phase 2 blueprint into the executable roadmap. Candidate Phase 4 inputs from this
document:

1. **1 open item resolved and closed out this cycle**: `task-store/src/task-service.ts`'s
   `claim()` concurrent-race regression test (carried forward as Net-new since
   2026-08-11) landed via PR #2560 (T-081) — the new `task-service.integration.test.ts`
   ports the exact `pull-request-service.integration.test.ts` harness. No Phase 4 action
   needed.
2. **1 real Promote/deepen item, new this cycle**: `agent/src/check-review.unit.test.ts`
   has substantial pairwise combination coverage for this cycle's 5 candidate-exclusion
   fixes, but no test yet exercises 3+ of the fixed checks simultaneously. Small effort —
   2-3 additional multi-condition matrix cases in the same file, same harness. This
   corrects Phase 2's gap #18's "possible coverage gap" framing to a scoped, small,
   depth-only item.
3. **1 real net-new item, unchanged since 2026-07-31**: the HITL local dev-loop bootstrap
   (journey #5) needs an integration-level sequencing test with an injected `gh` exec
   fake; the pure planning logic is already fully unit-tested.
4. **1 low-priority/optional carried-forward item** (`admin/src/clock.ts` — still a
   non-gap by the repo's own convention).
5. **Phase 2's gap #19 (`admin/src/blocked-by-type-parity.check.ts`'s new pattern)
   confirmed not a coverage gap** — the compile-time-only check is wired into CI's
   `typecheck` step and passing; the only open question is a taxonomy one (whether to
   formalize a "type-parity" layer), already tracked in `test-system.md`, not a Phase 4
   coverage task.
6. **Speed measurement re-confirmed in budget** — Tier 1 aggregate ~18% of budget on a
   fresh data point at this cycle's exact HEAD, consistent with the growth trajectory
   (+213 tests over the 3-day gap). No M1 task needed.
7. **0 net-new infra items, 0 rebuild candidates, 0 delete/trim candidates, 0
   isolation-contract regressions** — the steady-state finding holds for another cycle.
