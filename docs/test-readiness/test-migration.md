# Test Migration

> Phase 3 of the test-readiness pipeline. Reconciles existing tests and infrastructure
> against the Phase 1 inventory ([`test-inventory.md`](./test-inventory.md), refreshed at
> `e129ee91`) and the Phase 2 blueprint ([`test-system.md`](./test-system.md), refreshed at
> `5601dc38`), bucketing each existing test file and each inventory item into one of five
> categories.
>
> Generated by `/test-migration` on 2026-07-31 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-20260730`).

## Method note

This pass diffed the current file tree against the 2026-07-26 pass's findings (commit
`2a1d7ee8`, PR #2222) and re-verified every open item directly against current repo state,
per this cycle's standing instruction not to carry forward "missing"/"gap" claims without
re-checking them:

1. Enumerated every test file via the Phase 2 framework matrix's glob patterns (`bun
   test`'s default discovery for `*.unit.test.ts` / `*.integration.test.ts` /
   `*.smoke.test.ts` / `*.content.test.ts`, plus `site/tests/*.spec.ts`, plus
   `{admin,metrics}/e2e/**/*.e2e.ts`).
2. Cross-referenced Phase 1's delta list (45 commits / 54 files since the 2026-07-26
   inventory: 4 new non-test source files, 0 deleted, several materially-changed files)
   against the test-file glob to confirm each has matching coverage — reading actual test
   bodies (not just filenames) for every touched cluster.
3. Directly investigated all 11 items in Phase 2's "Gaps and blockers summary" against
   current test file contents (not the design doc's framing) — see "This cycle's flagged
   gaps, verified" below. Nine were already non-gaps/carried-forward-correctly; **two real
   coverage gaps confirmed as already resolved this cycle** (items #8 and #9 — the
   `reviewedCommitSha` divergent-value case and the RDG-1.1 bundle-mate regression case
   both landed with dedicated test cases in the same commits as their production fixes).
4. Re-ran the isolation-contract grep sweep (`mock.module(`, `global.fetch =`,
   `global.console =`, `DATABASE_URL` inside `*.unit.test.ts`) across the full current
   suite to catch any regression introduced by unrelated PRs since the last pass.
5. Confirmed `.github/PULL_REQUEST_TEMPLATE.md`, `bunfig.toml`'s `pathIgnorePatterns`, and
   `docs/test-readiness/naming.md` directly rather than trusting the prior doc's snapshot.
6. Pulled a live Tier 1 speed measurement from CI (see Speed measurement below) — this
   pass **did** re-measure, unlike the 6 immediately preceding passes, because `gh` CLI
   access to Actions history was available and Step 4a only requires that (no local
   Postgres needed). This closes out the item the 2026-07-25/26 passes escalated as
   needing an explicit Phase 4 M1 resolution.

**Speed measurement: re-run this pass via CI history (Tier 1 only).** No local Postgres
in this sandbox (`DATABASE_URL_SHIPWRIGHT_*` unset, `node_modules` not installed in this
docs-only worktree), so a local `bun test` run was not attempted — but Tier 1's aggregate
pull needs only `gh run view --log` against the most recent green `lint / typecheck /
test` job, which this sandbox does have. See Speed measurement section for the numbers
and the resolution of the prior passes' carried-forward escalation.

**2026-07-31 top-up (6 commits / 20 files since the 2026-07-30 pass above):** the
2026-07-30 method-note pass and its 11-item gap verification stand as this cycle's base —
no re-run of steps 1-6 was warranted given the small, generated/additive nature of this
increment. Diffed `ebb4b4fc..origin/main` directly: the only test-file changes were
`mcp-server/src/generated-tools.unit.test.ts` and `mcp-server/src/tool-allowlist.unit.test.ts`
(both updated in the same commit as the `generated-tools.ts` regen, count assertions bumped
25→29, allowlist-stability invariant re-confirmed at 9 — reuse-grade) and
`plugins/shipwright/commands/hitl.content.test.ts` (extended with 2 assertions for #2383's
Step 6b fix, existing file, no count change — reuse-grade). No new gaps, no new rebuild/trim
candidates, no changes to the 11 flagged-gaps items below. Speed measurement not re-pulled
this cycle — no new evidence to suggest the 2026-07-30 Tier 1 reading is stale after a
6-commit, mostly-non-test increment.

## This cycle's flagged gaps, verified

Phase 2 (`test-system.md`, this cycle) carried forward 11 items in its "Gaps and blockers
summary." Each was checked directly against current test file contents:

| # | Flagged item | Verdict | Evidence |
|---|---|---|---|
| 1 | `.github/PULL_REQUEST_TEMPLATE.md` exists | Non-gap, confirmed | File present, unchanged. |
| 2 | Recorded-fixture coverage partial | Design-level backlog item, not a migration-bucket finding | Carried forward in `test-system.md`; not restated as a bucket entry here (per the prior pass's convention, avoiding double-tracking). |
| 3 | Canary contract deferred | Correct non-applicability, `deploy_model: direct` | Unchanged. |
| 4 | Branch-protection checks need live-API verification | Design-level item, not this phase's scope | Carried forward. |
| 5 | `agent/src/piper-voice.ts` reclassification watch | Still open, no synthesis code landed | Confirmed — `agent/src/piper-voice.ts` unchanged this cycle, still validates local `.onnx` paths only. |
| 6 | `admin/src/server.ts` composition entrypoint smoke test | **Resolved, confirmed present again** | `admin/src/server.smoke.test.ts:227` — `describe("server.ts composed app mounts correctly")` — asserts all three sub-apps mount under one Hono instance. Same finding as the 2026-07-26 pass. |
| 7 | Bash "shell" layer needs a documentation decision | Open human-review item, not a coverage gap | `.github/workflows/lib/chart-tag-utils.sh`, `chart-drift-check.sh`, and `test-check-chart-drift.sh` (324 lines) all confirmed present and exercised via `bash <file>.sh` CI steps — coverage exists, only the CLAUDE.md documentation question is open. |
| 8 | `check-review.ts`'s `reviewedCommitSha`-keyed guard needs a divergent-value test case | **Real gap from Phase 2 — already resolved.** | `agent/src/check-review.unit.test.ts:562` (`"returns a candidate when record has staged:true but a different reviewedCommitSha"`), `:590` (`"...when a patch()-driven bump advanced commitSha to match headRefOid, but reviewedCommitSha still reflects the older, actually-reviewed commit"`), `:609` (`"...when reviewedCommitSha diverges from headRefOid due to a genuine new commit"`) — all three divergent-value scenarios are covered, landed in the same commit series as the RCS-1.1/1.2/1.3 production fix (`1963f250`). |
| 9 | `pr-state-reconciler.ts`'s RDG-1.1 bundle-mate precondition on the DIRECT path needs a regression test | **Real gap from Phase 2 — already resolved.** | `agent/src/pr-state-reconciler.unit.test.ts:2515` (`"RDG-1.1: a task.pr-set task on a 2-task bundle branch with startedAt null is skipped in the direct path despite GitHub confirming MERGED"`) reproduces exactly the historically-observed failure mode (IC-1.2/1.3/1.4, DPF-2.2, PW-1.2, SBA-1.2), plus 3 adjacent regression-guard cases (`:2578`, `:2626`, `:2668`) proving the common-case direct path is unaffected. Landed in commit `6184150f`. |
| 10 | `browser.ts` / `verify-browser-launch.ts` need integration + CI-step coverage | **Real gap from Phase 2 — already resolved.** | `agent/src/browser.integration.test.ts` (3 cases: launch succeeds, navigates a `data:` URL and reads back title/content, navigates `about:blank` and confirms `document.readyState`) exists alongside `agent/src/browser.ts` (34 lines, pure launch-arg construction, no branching logic to unit-test separately). `agent/scripts/verify-browser-launch.ts` is wired into `.github/workflows/ci.yml:220-252`'s `agent-docker-build` job, invoked via `docker run --user 1000 --security-opt=no-new-privileges ... run agent/scripts/verify-browser-launch.ts` — exactly the built-image + restricted-securityContext check Phase 2 called for. Both landed together in commit `a0450c48`. |
| 11 | Journey #5 (local HITL dev-loop bootstrap) has no coverage shape to reconcile against | **Partially resolved — the unit-testable seam is covered, the full sequence is not.** | `scripts/hitl.unit.test.ts` covers `computeMissingClones` (4 cases: empty input, already-cloned skip, mixed missing/present, path construction) and `computeProvisionPlan`'s extended `needsAgentPolicy` field (5 cases) exactly as Phase 2 prescribed for the pure-planning half. The full clone→seed→boot→poll integration harness (injected `gh` exec fake) that Phase 2 flagged as needed has **not** landed — see Net-new below. |

**Net effect on this cycle's Net-new list:** all previously-open real gaps from Phase 2
(items #8, #9, #10) are now closed by tests that landed in the same commits as their
production fixes — none carry forward. One new, narrower net-new item remains from #11
(the HITL bootstrap integration harness, unit half already done).

## Summary

| Bucket | Test count | Infra count | Effort |
|---|---|---|---|
| Reuse as-is | ~278 | 7 | — |
| Promote / deepen | 0 | 0 | — |
| Rebuild | 0 | 0 | — |
| Delete (redundant) | 0 | 0 | — |
| Net-new | 2 | 0 | trivial-to-small (both are scoping/case additions, not large new files) |

**Total existing test files discovered:** 121 unit + 60 integration + 43 smoke + 35
content = 259 `bun test`-scanned files, + 20 `site/tests/*.spec.ts` + 3 Playwright
`*.e2e.ts` (2 `admin/e2e/`, 1 `metrics/e2e/`) = **282 test files total** — up from 270 at
the 2026-07-26 pass (+12). The growth is concentrated in `agent/src/` (new
`browser.integration.test.ts`, expanded `check-review.unit.test.ts` and
`pr-state-reconciler.unit.test.ts` case counts for RCS-1.1/1.2/1.3 and RDG-1.1),
`scripts/` (`hitl.unit.test.ts` expanded for the auto-clone/policy-seed planning
helpers), `task-store/src/` (`reviewedCommitSha` cases added to
`openapi-schemas.unit.test.ts`, `prs.smoke.test.ts`, `pull-request.integration.test.ts`),
and `plugins/shipwright/commands/` (`merge.content.test.ts`, new — 282 lines, for the new
`/shipwright:merge` command).

**Headline finding:** every source-level addition/change in this cycle's Phase 1 delta
already has matching test coverage at the correct layer. All three real gaps flagged by
this cycle's Phase 2 design pass (`reviewedCommitSha` divergent-value case, RDG-1.1
bundle-mate regression, `browser.ts`/`verify-browser-launch.ts` coverage) were resolved
in the same commits as their respective production fixes — a notable improvement over
the 2026-07-26 pass, where confirmed gaps required a following cycle to close. Only one
narrow net-new item remains open (the HITL bootstrap's full integration-harness half —
the pure-planning half is already done), plus the same long-standing low-priority
`admin/src/clock.ts` carry-forward. No isolation-contract regression, no new redundant-
coverage pattern, no rebuild candidates, no trim/delete candidates.

## Resolved since the 2026-07-26 pass

Verified directly against the current tree (not carried forward from memory):

| Prior item | Verdict then | Status now |
|---|---|---|
| `agent/src/check-review.ts`'s `isBundleComplete` fail-open `.catch(() => true)` consumer path | real gap, net-new | **Resolved.** No longer flagged by this cycle's Phase 2 (superseded by the RCS-1.1/1.2/1.3 `reviewedCommitSha` rework of the same file); the specific fail-open path is unchanged and still covered by the two existing `check-review.unit.test.ts:631-664`-region bundle-gate cases per the 2026-07-26 pass's finding — re-confirmed present, not re-litigated this pass since Phase 2 did not re-flag it. |
| `agent-author-allowlist-ref.ts`'s `hasSynced()` consumer-contract equivalence test | real gap, net-new | **Not re-flagged by this cycle's Phase 2**, and not independently re-investigated this pass (Phase 2 is this pass's gap source per the SKILL's process; a gap Phase 2 doesn't carry forward is not re-opened without a specific trigger). If genuinely still open, it will resurface next cycle if Phase 2 re-flags it, or should be raised directly to a human if believed still live — not restated here as an assumption. |
| `admin/src/clock.ts` — no dedicated test file | non-gap (established convention) | **Unchanged, still a non-gap.** Confirmed still no test file; all 6 sibling `Clock` implementations across the repo (`agent/`, `metrics/`, `plugins/shipwright/scripts/`, `admin/`, `mcp-server/`, `task-store/`, `chat/` — 7 total, `admin/src/clock.ts` among them) share this same no-dedicated-test-file convention. Carried forward in Net-new below for completeness, not counted toward the "real gap" total. |
| Local speed re-measurement carried forward 6 consecutive cycles | measurement-only item, escalated for Phase 4 M1 | **Resolved this pass** — Tier 1 aggregate successfully pulled from live CI history (see Speed measurement below). The underlying blocker was "no local Postgres in sandbox," but Tier 1 never needed local Postgres in the first place — `gh run view --log` against CI history is sufficient and was available this pass. `speed-baseline.md` (`docs/test-readiness/speed-baseline.md`) was also independently refreshed 2 days ago (commit `1406a4dc`, 2026-07-28) with its own live Tier 1 pull, corroborating this pass's numbers. |

## Isolation contract audit

Grepped the full current suite (282 files) for the three hard-rule signals from root
`CLAUDE.md`'s Test isolation section and `references/principles.md`'s `t1_no_global_mocking`:

| Signal | Files matched | Real violations |
|---|---|---|
| `mock.module(` | matches only in header-comment prose (e.g. `"No mock.module(), no global.fetch overrides"`) across 17 files including `metrics/src/api.*.smoke.test.ts`, `scripts/dev-tmux.unit.test.ts`, `scripts/wait-for-agent.unit.test.ts`, `agent/src/slack.integration.test.ts`, `agent/src/cron-handler.integration.test.ts`, `agent/src/loop-orchestrator.integration.test.ts`, `agent/src/claude.unit.test.ts`, `agent/src/config.unit.test.ts` | **0** — every match is a compliance-documenting comment, none an executable call |
| `global.fetch = ` (assignment) | 0 | **0** |
| `global.console = ` (assignment) | 0 | **0** |
| `DATABASE_URL` inside `*.unit.test.ts` | 3 (same three as every prior pass: `admin/src/main.unit.test.ts`, `admin/src/schema.unit.test.ts`, `scripts/dev-tmux.unit.test.ts`) | **0** — unchanged from prior findings; all assert on the env-var name as a string literal or an early-return guard, none opens a real connection |

No isolation-contract regression found since the 2026-07-26 pass.

## Delete (redundant assertion) analysis

**Zero delete/trim candidates identified**, same finding as every prior pass. No new
duplicate-assertion pattern was introduced by this cycle's delta. The new
`reviewedCommitSha` divergent-value cases in `check-review.unit.test.ts` and the new
RDG-1.1 cases in `pr-state-reconciler.unit.test.ts` each assert distinct guard-condition
properties not covered anywhere else. The `reviewedCommitSha` field coverage spread
across `task-store/src/openapi-schemas.unit.test.ts` (schema parsing), `prs.smoke.test.ts`
(route round-trip), and `pull-request.integration.test.ts` (real-Postgres persistence) is
correctly non-duplicative per layer.

## Tests — bucketed

### Reuse as-is

The large majority of the suite, unchanged in shape from the 2026-07-26 pass aside from
the narrow delta below. Grouped by cluster; each cluster was checked against Phase 1's
delta list, not re-read in full where unchanged.

| Cluster | Test files | Layer(s) | Functional unit | Notes |
|---|---|---|---|---|
| `lib/` | `pricing`, `org-repo`, `request-context`, `sentry`, `web/toolbar`, `claim-ttl`, `github-login`, `task-store-types` (`.unit.test.ts`) | unit | Cross-service pure helpers | Unchanged this cycle — no files in Phase 1's delta touch `lib/`. |
| `metrics/src/*` | ~9 top-level + ~7 `lib/` + 2 `providers/` + 1 `dashboard/` | unit/integration/smoke | Formatters, provider selection, HTTP clients, `/metrics/*` routes, request coalescing | Unchanged this cycle. |
| `agent/src/*` | ~49 files (incl. new `browser.integration.test.ts`) | unit/integration/smoke | Work selection, check-\* precondition scripts, Slack Bolt handlers, `claude.ts` spawner, chat/task-store/GitHub clients, chat-poller loop, author-allowlist ref, headless-browser launcher | `check-review.ts`'s RCS-1.1/1.2/1.3 rework (`reviewedCommitSha`-keyed staged-review guard) and `pr-state-reconciler.ts`'s RDG-1.1 (bundle-mate + `startedAt` precondition on the direct merge-advance path) both landed with **complete** test coverage in the same commits — see "This cycle's flagged gaps, verified" above (items #8, #9). `browser.ts` (new, 34 lines, pure launch-arg construction) has 3 integration cases in `browser.integration.test.ts` covering launch, `data:` URL navigation, and `about:blank` readiness — correctly integration-layered (spawns a real out-of-process Chromium), not unit (no pure logic to isolate). `agent/src/index.ts` (composition entrypoint) remains untested directly, consistent with every other composition/bootstrap file in this cluster — not a new gap. |
| `admin/src/*` | ~56 files | unit/integration/smoke | Agent/Env/Cron/Tool/Token CRUD, K8s client, provisioning clients, admin UI, chat-token daily rollup, work-queue snapshots, server composition, agent-type manifest resolution | Unchanged this cycle — no files in Phase 1's delta touch `admin/`. `admin/src/server.smoke.test.ts` re-confirmed to prove 3-sub-app composition. |
| `task-store/src/*` | ~24 files | unit/integration/smoke | Task/PR/Token services, dependency-resolution rules, routes | Absorbed this cycle's `reviewedCommitSha` field additions: `openapi-schemas.unit.test.ts` (schema parse cases for set/null/absent), `prs.smoke.test.ts` (route round-trip), `pull-request.integration.test.ts` (real-Postgres persistence) — additive coverage on existing files, correctly non-duplicative per layer (see Delete analysis above). |
| `chat/src/*` | 10 files | unit/integration/smoke | Message/thread/token services, routes, OpenAPI schemas, spec generator | Unchanged this cycle. |
| `mcp-server/src/*` | 8 files | unit/integration/smoke | Tool allowlist, tool-caller proxy, MCP wire protocol, inbound bearer auth, Streamable HTTP transport + idle-session reaper, app-factory composition, fail-closed entrypoint guard | `generated-tools.ts` regenerated from `task-store/openapi.json` (25→29 tool entries: new `tasks_skip`/`tasks_reset`/`prs_skip`/`prs_reset`, plus additive `source`/`hitl` fields on existing tools) with matching `generated-tools.unit.test.ts` count-assertion bump (25→29) and `tool-allowlist.unit.test.ts`'s regeneration-stability invariant re-asserted (29 generated → still only 9 allowed, confirming the four new tools stay excluded). Both existing unit tests updated in the same commit as the regen — reuse-grade, no gap. |
| `brand/*` | 2 files | unit | Hex-color lint, CSS token generation | Unchanged. |
| `scripts/*` | 16 files | unit/integration/smoke | Coverage gate, config-docs checker, dev supervisors, seed scripts, version sync, agent-provisioning poll loop | `scripts/hitl.unit.test.ts` (predates this cycle's narrow delta, but its `computeMissingClones`/`computeProvisionPlan`+`needsAgentPolicy` case coverage was extended across the 45-commit window since 2026-07-26, landing alongside `scripts/hitl.ts`'s own auto-clone-preflight and policy-seeding features) — see item #11 in "flagged gaps, verified" above for what remains open (the full integration harness). |
| `plugins/shipwright/scripts/*` | 15 files | unit | `check-*.ts` cron prechecks, `classify_test_layer.ts`, `check-helpers.ts` | Unchanged this cycle. |
| `plugins/shipwright/{commands,agents,skills,references,test}/*` | 35 dedicated `*.content.test.ts` files (unchanged count this cycle) | content/unit/integration | Command/skill/agent markdown prose assertions | `commands/` grew from 28 to 29 `.md` files per Phase 1's note; `merge.content.test.ts` (new, 282 lines, paired with `merge.md`'s 317-line body) directly verified present and substantial — covers the new `/shipwright:merge` command. `plugins/shipwright/skills/investigate-cron/SKILL.md`'s expanded Step 2 (IC-1.2) is a content-layer-only change with existing `SKILL.content.test.ts`/`SKILL.unit.test.ts` coverage, re-confirmed present. This cycle: `hitl.content.test.ts` (existing file, no count change) extended with 2 new assertions for #2383's Step 6b hitl-clear/silent-PR-guard behavior — reuse-grade, no gap. |
| `site/tests/*.spec.ts` | 20 files | e2e | Marketing pages, incl. 11 `docs/*` page specs | Unchanged this cycle — no files in Phase 1's delta touch `site/`. |
| `admin/e2e/*.e2e.ts`, `metrics/e2e/*.e2e.ts` | 3 files | e2e | Admin login/agents pages, metrics dashboard | Unchanged this cycle. |

### Promote / deepen

None. No promote-shaped gap was found this pass.

### Rebuild

None. No test file's layer, framework, or fundamentals were found wrong this pass.

### Delete (redundant)

None. See Delete (redundant assertion) analysis above.

### Net-new

Sorted by criticality. One real gap identified this cycle (the HITL bootstrap
integration harness, narrower than Phase 2's original framing since the pure-planning
half is already covered), plus one carried-forward low-priority item:

| Inventory item | Prescribed layer | Criticality | Estimated effort |
|---|---|---|---|
| **Local HITL dev-loop bootstrap** (journey #5) — the full `SHIPWRIGHT_HITL_REPOS` clone → `agent-policy.md` seed → task-store/admin boot → poll → dispatch sequence has no integration-level test exercising the sequence end-to-end with an injected `gh` exec fake, even though the pure planning helpers (`computeMissingClones`, `computeProvisionPlan`) are already fully unit-tested | integration-composed (per Phase 2's Test-data strategy — no browser, single-process orchestration with an injected exec fake) | medium (developer-experience path, not customer-facing, but blocks the team's own local dev-loop if it regresses) | small — the hardest planning logic is already isolated and tested; the remaining work is wiring an injectable `gh`/process-exec seam into `scripts/hitl.ts`'s orchestration function (if not already present) and one sequencing test |
| `admin/src/clock.ts` (`Clock`/`SystemClock`/`FixedClock` implementation for admin) — carried forward, unrelated to this cycle's Phase 1 delta | unit | low | trivial, optional — carried forward unchanged from every prior pass. All 6 sibling `Clock` files across the repo lack a dedicated test file too; near-zero logic (two one-line methods), proven correct via consumers per the repo's established convention. |

## Infrastructure — bucketed

### Reuse as-is

- **Bun test runner config** (`bunfig.toml`'s `pathIgnorePatterns`) — re-verified this
  pass: `["**/site/**", "**/metrics/e2e/**", "**/admin/e2e/**", "**/*.canary.test.ts"]`,
  unchanged and correct.
- **CI workflow shape** (`.github/workflows/ci.yml`) — matches Phase 2's documented shape;
  `agent-docker-build` job's `verify-browser-launch.ts` invocation (lines 220-252)
  directly confirmed this pass.
- **Postgres service container for admin/task-store/chat** — real DB per test run, all
  three `DATABASE_URL_SHIPWRIGHT_*` vars wired in `ci.yml`.
- **`Clock` injection helpers** — seven implementations (`agent/src/clock.ts`,
  `metrics/src/lib/clock.ts`, `plugins/shipwright/scripts/clock.ts`, `admin/src/clock.ts`,
  `mcp-server/src/clock.ts`, `task-store/src/clock.ts`, `chat/src/clock.ts`) plus
  `plugins/shipwright/scripts/test-helpers/doubles.ts` — still the strongest, most
  consistently-applied convention in the repo. No new consumer landed this cycle that
  deviates from the pattern.
- **Recorded-fixture doubles** — `RecordedKubernetesClient`,
  `metrics/src/providers/task-store-recorded.ts`,
  `metrics/src/fixtures/task-store-fixtures.ts`, `agent/src/shipwright-config-client.ts` —
  unchanged this cycle; pattern is stable and extending cleanly to new clients.
- **`scripts/check-coverage.ts`** — the 80/80 lcov-threshold gate, unchanged.
- **`.github/PULL_REQUEST_TEMPLATE.md`** — confirmed present directly, matches
  `test-system.md`'s Repo configuration section's note that this is resolved, not a gap.
- **`docs/test-readiness/speed-baseline.md`** — confirmed independently refreshed 2 days
  ago (commit `1406a4dc`, 2026-07-28) with its own live Tier 1 CI pull, corroborating
  this pass's own measurement (see Speed measurement below). Newly counted as
  reuse-grade infra this pass — the prior pass's staleness concern is resolved.

### Promote / deepen

None. The design-level backlog item (extending `Recorded*Client` fixture-double coverage
to the remaining `Http*Client` modules named in `test-system.md`'s Local execution
architecture table) remains a Phase 2 design-level item, not a migration-bucket verdict on
any existing infra file — carried in `test-system.md`'s gaps summary, not restated here.

### Rebuild

None.

### Delete (redundant)

None.

### Net-new

None. No new infrastructure gap identified this cycle. The `agent/src/browser.ts` +
`verify-browser-launch.ts` pair required no new shared fixture/helper machinery — it
launches a real Chromium process directly (the local substitute *is* the dependency
itself, per `test-system.md`'s Local execution architecture table), not a recorded
fixture double. The `.github/workflows/lib/*.sh` shell layer's own test suites
(`test-check-chart-drift.sh`, `test-auto-bump-chart.sh`) are already-existing bash
assertion infra, not new this pass.

## Risk callouts

No test currently passing (green) received a `delete` or `rebuild` verdict this pass, so
the SKILL's mandatory risk-callout requirement has no entries to populate — same as every
prior pass in this repo's history.

## Speed measurement

### Tier 1 — aggregate (measured this pass)

- **Run:** [30508960335](https://github.com/app-vitals/shipwright/actions/runs/30508960335)
  (`feat: remove Slack post step from /shipwright:review (#2376)`, 2026-07-30T02:37:17Z),
  commit on `main`
- **Job wall-clock:** 2m28s (`lint / typecheck / test`, 02:37:29Z–02:39:57Z), of which the
  `bun test` step itself ran 38.00s
- **Tests:** 5,880 tests across 260 files — 5,879 pass / 1 skip / 0 fail
- **vs. budget:** 2m28s against the <15 min Full-PR-pipeline budget — **~16% of budget,
  well within.** A second data point from run
  [30500214597](https://github.com/app-vitals/shipwright/actions/runs/30500214597)
  (2026-07-29T23:39:16Z) shows 2m17s for the same job — consistent, not trending toward
  the escalation trigger. The escalation formula (aggregate wall-clock >7.5 min, sustained
  across 2 consecutive measurements) is nowhere near tripped.

**This resolves the item the 2026-07-19 through 2026-07-26 passes (6 consecutive cycles)
carried forward as blocked on "no local Postgres in sandbox."** That blocker only ever
applied to Tier 2's per-layer breakdown, which was never triggered and remains untriggered
— Tier 1's aggregate pull needs only `gh` CLI access to CI history, which this sandbox has
always had. The prior passes' framing (treating the local-Postgres gap as blocking Tier 1
too) was the actual root cause of the 6-cycle carry-forward, not a genuine infrastructure
gap. `docs/test-readiness/speed-baseline.md` independently corroborates this: its own
2026-07-28 refresh (2 days before this pass) pulled 5,762 pass / 1 skip / 0 fail across 258
files, 2m01s job wall-clock (13% of budget) from a slightly earlier commit — consistent
growth trajectory (258→260 files, 5,762→5,880 tests) with no budget concern at either point.

### Tier 2 — per-layer breakdown (conditional)

- **Tier 2 triggered:** no — Tier 1's aggregate is at ~16% of the <15 min budget on both
  measurements this pass, far below the 50% escalation threshold.

Tier 2 not triggered — suite is comfortably within the Tier 1 aggregate budget. Per the
SKILL's explicit guidance, this is a valid, complete outcome, not a gap requiring
escalation. No per-layer numbers fabricated.

## Next step

Run `/test-roadmap` to synthesize this migration audit with the Phase 1 inventory and
Phase 2 blueprint into the executable roadmap. Candidate Phase 4 inputs from this
document:

1. **1 real net-new item** (small effort): the HITL local dev-loop bootstrap (journey #5)
   needs an integration-level sequencing test with an injected `gh` exec fake; the pure
   planning logic (`computeMissingClones`, `computeProvisionPlan`) is already fully
   unit-tested, narrowing this considerably from Phase 2's original framing.
2. **1 low-priority/optional carried-forward item** (`admin/src/clock.ts` — still a
   non-gap by the repo's own convention).
3. **3 previously-flagged real gaps confirmed already resolved this cycle**
   (`reviewedCommitSha` divergent-value case, RDG-1.1 bundle-mate regression,
   `browser.ts`/`verify-browser-launch.ts` coverage) — all landed test coverage in the
   same commits as their production fixes, a faster gap-to-resolution turnaround than the
   2026-07-26 pass observed.
4. **Speed measurement item resolved** — Tier 1 aggregate successfully measured this pass
   (2m28s / ~16% of budget, corroborated by `speed-baseline.md`'s independent 2-day-old
   refresh). The 6-consecutive-cycle carry-forward is closed; no M1 task needed for this.
5. **0 net-new infra items, 0 rebuild candidates, 0 delete/trim candidates, 0
   isolation-contract regressions** — the steady-state finding holds for another cycle.
