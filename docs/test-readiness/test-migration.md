# Test Migration

> Phase 3 of the test-readiness pipeline. Reconciles existing tests and infrastructure
> against the Phase 1 inventory ([`test-inventory.md`](./test-inventory.md), committed at
> `ebde3ca1`) and the Phase 2 blueprint ([`test-system.md`](./test-system.md), committed at
> `8cfc3683`), bucketing each existing test file and each inventory item into one of five
> categories.
>
> Generated by `/test-migration` on 2026-07-16 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-20260716`).

## Method note

This repo has **~220 test files (~130K lines including source) / ~110K lines of test
code** across 8 workspaces plus `scripts/`, `brand/`, `plugins/shipwright/`, and `site/`.
Reading every file in full was not feasible within this pass. The approach taken:

1. Enumerated every test file via the Phase 2 framework matrix's glob patterns (`bun
   test`'s default discovery for `*.unit.test.ts` / `*.integration.test.ts` /
   `*.smoke.test.ts` / `*.content.test.ts`, plus `**/*.spec.ts` for `site/`, plus
   `**/e2e/**/*.e2e.ts` for `admin/`/`metrics/` Playwright).
2. Measured every file's line count as a first-pass layer-mismatch signal (a "unit" test
   with real DB/process I/O tends to also be disproportionately large or complain in its
   own header comment).
3. Read file headers + representative bodies for every workspace cluster, weighted toward
   files whose name/inventory pairing looked ambiguous (e.g. `*.integration.` on a file
   with no I/O; `*.unit.` on a file wrapping an external client).
4. Grepped the **entire** test suite for the isolation contract's hard signals
   (`mock.module(`, `global.fetch =`, `global.console =`, `DATABASE_URL` inside
   `*.unit.test.ts`) to find real violations vs. false positives (comments documenting
   compliance).
5. Cross-referenced every Phase 1 inventory row against the test-file glob to find
   zero-coverage items.
6. Checked `git log --follow` for every `site/tests/*.spec.ts` and `*/e2e/*.e2e.ts` file
   for outage-linked commits before considering any trim.

**Speed measurement: not run.** No local Postgres is available in this environment
(`DATABASE_URL_SHIPWRIGHT_*` unset, `pg_isready` absent) — running `bun test` here would
silently skip every DB-backed integration test and produce misleading "fast" timings for
files that actually hit real Postgres in CI. Per SKILL Step 4 ("optional but
recommended"), this was skipped rather than produce a misleading measurement; line-count
and import-based static signals were used instead as the layer-mismatch heuristic. CI's
own historical full-suite timings (referenced in Phase 2's Speed budgets section: "low
minutes" wall time, comfortably under the 15-minute budget) are the source of truth for
actual timing — no evidence found of any test individually breaching a hard cap.

**What this pass did *not* do:** open all ~220 files in full. For the largest clusters
(admin, agent, task-store — each with 15-40 test files), every file's header comment and
declared strategy were read, and a sample of full bodies per functional group (auth,
CRUD service, HTTP route, client wrapper) was read in depth. No file was bucketed on
filename/directory alone — every bucket assignment below is backed by an actual read,
even if abbreviated for very large or very repetitive files.

## Summary

| Bucket | Test count | Infra count | Effort |
|---|---|---|---|
| Reuse as-is | ~205 | 6 | — |
| Promote / deepen | 3 | 1 | small each |
| Rebuild | 1 | 0 | small |
| Trim (redundant assertion) | 0 | 0 | — |
| Net-new | 6 | 2 | mixed (see below) |

**Total existing test files discovered:** 209 `bun test`-scanned files (105 unit + 46
integration + 39 smoke + 15 content, excluding `pathIgnorePatterns`) + 12 `site/`
Playwright `*.spec.ts` + 3 Playwright `*.e2e.ts` (2 `admin/e2e/`, 1 `metrics/e2e/`) = **224
test files total.**

The overwhelmingly dominant finding: **this repo's existing suite is unusually
well-aligned with its own blueprint.** The isolation contract (`Clock` injection,
recorded-fixture/injected-double clients, no `mock.module()`/`global.*` overrides) is
followed with near-total consistency — every apparent "violation" found by grep turned out
to be a header comment *documenting* compliance, not an actual violation (see Isolation
contract audit below). No trims were identified — see Trim analysis below for why.

## Isolation contract audit

Grepped the entire suite (224 files) for the three hard-rule signals from root
`CLAUDE.md`'s Test isolation section and `references/principles.md`'s `t1_no_global_mocking`:

| Signal | Files matched | Real violations |
|---|---|---|
| `mock.module(` | 27 (all in header-comment prose, e.g. `"no mock.module()"`) | **0** |
| `global.fetch = ` (assignment) | 0 | **0** |
| `global.console = ` (assignment) | 0 | **0** |
| `DATABASE_URL` inside `*.unit.test.ts` | 3 (`admin/src/main.unit.test.ts`, `admin/src/schema.unit.test.ts`, `scripts/dev-tmux.unit.test.ts`) | **0** — all three assert on the env-var *name* as a string literal (schema text, command-string builder) or test an early-return guard when the var is unset; none open a real connection |

No file needed a `rebuild` verdict on isolation-contract grounds. This is the strongest
positive signal in this audit — see `t2_clock_injection`/`t3_recorded_fixture_pattern` in
`references/principles.md`; both are already the repo's dominant pattern (`Clock`
injection in `agent/src/clock.ts`, `plugins/shipwright/scripts/clock.ts`,
`metrics/src/lib/clock.ts`; recorded-fixture doubles per the Phase 2 Local execution
architecture table).

## Trim analysis

**Zero trims identified.** The SKILL's trim bucket requires a *specific, cited* duplicate
assertion — a higher-layer test re-asserting a business rule already proved by a
lower-layer test for the same module. Sampling across every workspace found the opposite
pattern consistently: each layer's test file asserts a genuinely distinct concern for its
module (e.g. `admin/src/admin-ui-pages.unit.test.ts` asserts pure render-function output;
its sibling `admin/src/admin-ui.smoke.test.ts` asserts the routed HTTP wrapper around those
same render functions — different concern, not duplicate coverage). Two candidate pairs
were checked in depth and found non-duplicative:

- `plugins/shipwright/commands/dev-task.content.test.ts` (markdown-prose assertions about
  the `/dev-task` command's Step 1 orphan-check behavior) vs.
  `plugins/shipwright/commands/dev-task.unit.test.ts` (pure parsing/scoring logic in a
  colocated helper) — no assertion overlap, different subjects entirely despite the shared
  filename stem.
- `plugins/shipwright/agents/learning-dreamer.integration.test.ts` vs.
  `plugins/shipwright/agents/learning-dreamer.unit.test.ts` — both read the same
  `learning-dreamer.md`, but each extracts and asserts on a different section (mining-table
  vs. review-mode); no duplicate assertion found.

No E2E test (`site/tests/*.spec.ts`, `admin/e2e/*.e2e.ts`, `metrics/e2e/*.e2e.ts`) was
considered for trimming — per the SKILL's hard rule, E2E tests are never deleted, and none
were found re-asserting only what a lower layer already proves (all 12 site specs assert
page-level rendering/navigation, which by definition is not provable at unit/integration
layer). All 15 E2E-layer files' git history was checked (`git log --follow`); none carry
an outage-linked commit, but that only matters for E2E *retention* decisions (per SKILL:
"if it has an outage-linked commit, mark it reuse and leave it") — since no E2E file was a
trim/delete candidate in the first place, this check served as confirmation, not a
deciding factor.

## Tests — bucketed

### Reuse as-is

The large majority of the suite. Rather than list all ~205 files individually (most
already match the SKILL's reuse criteria — right layer, right framework, adequate depth,
no external network, within budget, canonical owner), grouped by cluster with
representative files cited. Every cluster below was verified by reading file headers +
sampled bodies; none was bucketed on filename alone.

| Cluster | Test files | Layer(s) | Functional unit | Notes |
|---|---|---|---|---|
| `lib/` | `pricing.unit.test.ts`, `request-context.unit.test.ts`, `sentry.unit.test.ts`, `web/toolbar.unit.test.ts` | unit | Cross-service pure helpers | Clean pure-function tests, no I/O. `sentry.unit.test.ts` correctly tests only the scrub-rule logic, not SDK `init()` (matches Phase 2's split modeling). |
| `metrics/src/*` | 9 top-level + 5 `lib/` + 2 `providers/` + 1 `dashboard/` | unit/integration/smoke | Formatters, provider selection, `HttpTaskStoreClient`/`accounts-client`/`admin-metrics-client`, `/metrics/*` routes | `task-store-client.unit.test.ts` uses an injected `FetchLike` double for pure pagination/date-window/repo-filter logic — correctly unit-scoped even though the client's *own* recorded-fixture integration coverage is a separate net-new gap (see below). |
| `agent/src/*` | 39 files | unit/integration/smoke | Work selection, check-\* precondition scripts, Slack Bolt handlers, `claude.ts` spawner, chat/task-store/GitHub clients | Every client wrapper (`http-chat-service-client.unit.test.ts`, `shipwright-runtime-client.integration.test.ts`) uses an injected fetch/spawn double, not a real network call. `claude.unit.test.ts` (995 lines) injects the process spawner itself — no real `claude -p` subprocess ever runs; legitimately unit-testable given full DI, consistent with the repo's isolation contract, even though the inventory models the *component* `claude.ts` as "service-boundary/integration" at the file-classification level. `slack.integration.test.ts` (2728 lines, largest file in the suite) composes `createSlackApp`'s injected params — no real Bolt socket. |
| `admin/src/*` | 47 files | unit/integration/smoke | Agent/Env/Cron/Tool/Token CRUD, K8s client, provisioning clients, admin UI (render + routed) | `admin-ui-pages.unit.test.ts` (4379 lines) tests pure `render*Page` functions (string→string, no Hono); `admin-ui.smoke.test.ts` (4822 lines) tests the routed HTTP surface around those same functions — distinct concerns, not duplicated (see Trim analysis). `kubernetes-client.integration.test.ts` exercises the existing `RecordedKubernetesClient` cassette double named in Phase 2. |
| `task-store/src/*` | 21 files + `routes/*` | unit/integration/smoke | Task/PR/Token services, `blocked-by.ts`, routes, OpenAPI schemas | `blocked-by.ts` has both a `.unit.test.ts` and a `.smoke.test.ts` — verified non-duplicative (unit covers the pure dependency-satisfaction algorithm, smoke covers its wiring into the route response). `ready.ts` (`resolveReadyTasks`) has **no** test file — flagged net-new below, not reuse. |
| `chat/src/*` | 7 files | unit/smoke | Message/thread/token services, routes, OpenAPI schemas | No `*.integration.test.ts` exists for chat's Prisma-backed service layer — confirms Phase 2's flagged CI-wiring gap at the test-file level too; flagged net-new below (mirrors the Phase 2 gap, now audited at the test-inventory level). |
| `mcp-server/src/*` | 5 files | unit/smoke | Tool allowlist, tool-caller proxy, MCP wire protocol | `tool-caller.unit.test.ts` builds a proxy request/response mapping with no real HTTP call — legitimately unit despite the inventory's "integration (recorded fixture)" label; a companion recorded-fixture integration test for the real proxied call is a smaller-scope net-new gap, not called out separately below (low criticality per inventory). |
| `brand/*` | 2 files | unit | Hex-color lint, CSS token generation | Small, clean pure-function tests. |
| `scripts/*` | 13 files | unit/integration/smoke | Coverage gate, config-docs checker, dev supervisors, seed scripts, version sync | `dev-tmux.unit.test.ts` (985 lines) is the largest script test — builds and asserts on command/env strings for a 6-pane tmux session without spawning real tmux or Docker; correctly unit-scoped (pure builder function under test, matches its own CLAUDE.md-documented pattern). |
| `plugins/shipwright/scripts/*` | 14 files | unit | `check-*.ts` cron prechecks, `classify_test_layer.ts`, `check-helpers.ts` | All precondition scripts use injected `ghJson`/task-store client doubles; `isCleanApproveBody` and other pure parsing helpers are directly unit-tested. |
| `plugins/shipwright/{commands,agents,skills,references,test}/*`, `CLAUDE.unit.test.ts`, `assets/rubrics/*` | 28 files | content/unit/integration | Command/skill/agent markdown prose assertions, principles-wiring checks | Content-layer tests correctly assert on markdown structure/prose rather than executing anything — matches the repo's fifth "content" layer convention. `learning-dreamer.integration.test.ts` is a misnomer worth a light look (see Promote below) but not wrong enough to rebuild. |
| `site/tests/*.spec.ts` | 12 files | e2e | Marketing pages: home, architecture, compare, config, docs-\*, self-hosted, vs-devin, booking-cta | Real-browser Playwright, matches Phase 2's E2E framework choice exactly. All checked for outage-linked history — none found, all are additive feature commits. |
| `admin/e2e/*.e2e.ts`, `metrics/e2e/*.e2e.ts` | 3 files | e2e | Admin login/agents pages, metrics dashboard | Same Playwright pattern, separate CI jobs per Phase 2's CI pipeline shape (`e2e`, `admin-e2e`). |

### Promote / deepen

| Test file | Layer | Gap | Fix |
|---|---|---|---|
| `plugins/shipwright/agents/learning-dreamer.integration.test.ts` | filed as integration | File reads a local markdown file via `readFileSync` — no real I/O boundary (no network, no DB, no subprocess). This is functionally a content-layer test misfiled under the `.integration.` suffix rather than `.content.test.ts`. Not wrong enough to warrant a full rebuild (the assertions themselves are fine and the file is tiny — 67 lines), but the layer label is misleading for anyone auditing coverage by suffix. | Rename to `learning-dreamer.content.test.ts` (or merge into the existing `.unit.test.ts` sibling, which does the same markdown-read pattern) — small effort, no behavior change. |
| `metrics/src/lib/task-store-client.unit.test.ts` | unit (correctly) | Covers pagination/date-filtering/repo-filtering logic via an injected `FetchLike` double — good unit coverage. But per Phase 2's Local execution architecture table, `HttpTaskStoreClient` is also supposed to have a recorded-fixture **integration** test (the `Recorded*Client` pattern extended per the gaps-summary item 3). No such file exists. | Add `metrics/src/lib/task-store-client.integration.test.ts` using a `metrics/src/fixtures/task-store/*.json` recorded response, per the existing `RecordedKubernetesClient`/`task-store-recorded.ts` pattern — small effort, additive (doesn't change the unit test). |
| `lib/task-store-types.integration.test.ts` | filed as integration | Same misfiling pattern as `learning-dreamer` above: reads a local generated `.ts` file via `readFileSync` and asserts on its string content (`"export interface paths"`, `'"/tasks"'`). No real I/O boundary — this is a codegen-freshness check, unit-shaped at most, not integration. 24 lines. | Rename to `lib/task-store-types.unit.test.ts` — trivial effort, no behavior change. Listed here rather than under Rebuild because the fix is a rename, not a rewrite (see Rebuild criteria: "wrong layer" only routes to rebuild when the *test itself* needs restructuring — this one just needs its filename corrected). |
| Infra: `plugins/shipwright/agents/learning-dreamer.{unit,integration}.test.ts` pairing | — | Two files with overlapping subject matter (same source markdown) and one arguably-mislabeled suffix (see row above) create minor audit friction — a reviewer scanning by suffix would misclassify coverage. | Same fix as the content-layer rename above; no separate infra action needed. |

### Rebuild

| Test file | Current layer | Should be | Reason | Effort |
|---|---|---|---|---|
| `lib/task-store-types.integration.test.ts` | integration (by filename) | unit or content | Listed under both Promote and Rebuild tables is avoided — this is a rename-only fix (see Promote table above for the full reasoning), so it is **not** double-counted in the bucket totals. It appears in this table only for completeness of the "wrong layer" pattern-matching per Step 5's matrix; the actual bucket assigned is Promote (small effort, rename). | small |

**Note on bucket counting:** the row above is the same item as the third row in Promote —
included in this table for matrix-completeness per SKILL Step 5's "right-layer? → if not,
→ REBUILD" branch, but assigned to the Promote bucket in the Summary counts above because
the fix is a rename (no assertion or fixture rewrite), which the SKILL's Rebuild
definition ("wrong fundamentals... migration cost is justified") does not describe. The
Rebuild bucket count in the Summary table is 1 to reflect this row existing in the matrix,
with 0 *additional* distinct rebuild-effort items found beyond what Promote already
covers.

### Trim (redundant assertion)

None identified. See the Trim analysis section above for the two candidate pairs checked
and found non-duplicative, and the E2E-history check performed as a precondition (none
needed, since no E2E file was a trim candidate).

### Net-new

Sorted by inventory criticality (critical first).

| Inventory item | Prescribed layer | Criticality | Estimated effort |
|---|---|---|---|
| `task-store/src/ready.ts` (`resolveReadyTasks`) | unit | critical | small — pure dependency-resolution function, no fixture setup needed |
| `chat/*` Prisma-backed service layer (`message-service.ts`, `thread-service.ts`, `token-service.ts`) — DB integration coverage | integration | high | medium — blocked on Phase 2's flagged CI-wiring gap (no `shipwright_chat_test` DB in `ci.yml`); the test-writing effort itself is small once the DB is wired, but is sequenced after that infra fix, matching Phase 2's own sequencing note |
| `lib/org-repo.ts` (`isOrgRepo`) | unit | medium | small — single pure predicate, a handful of valid/invalid-string cases |
| `task-store/src/statuses.ts` (`CLOSED_STATUSES`/`OPEN_STATUSES` and any status-transition helpers) | unit | critical | small — currently only indirectly exercised as an import inside `blocked-by`/`task-service` tests; needs its own direct assertion of the lifecycle list/rules per the inventory's "status lifecycle rules" framing |
| `lib/secret-env-vars.ts` (`SECRET_ENV_VARS` constant) | unit | high | small — currently only used as a test fixture input inside `lib/sentry.unit.test.ts`'s scrub-rule assertions; needs its own direct test asserting list contents/no-duplicates, since it's also consumed by `admin/src/agent-envs.ts` for masking and that consumer path doesn't test the constant's own contract either |
| `metrics/src/lib/task-store-client.ts` recorded-fixture integration test | integration | high | small — see Promote table; listed here too because it is a genuine coverage gap (no existing file), while the Promote entry documents the specific fix |

## Infrastructure — bucketed

### Reuse as-is

- **Bun test runner config** (`bunfig.toml`'s `pathIgnorePatterns`) — correctly excludes
  `site/**`, `metrics/e2e/**`, `admin/e2e/**`, and reserves `*.canary.test.ts` per Phase
  2's canary-applicability finding (not yet used, correctly left unused).
- **CI workflow shape** (`.github/workflows/ci.yml`) — matches Phase 2's documented CI
  pipeline shape exactly (single combined `ci` job for unit+integration+smoke, separate
  `e2e`/`admin-e2e` jobs, `site` job) — no infra rebuild needed for the shape itself.
- **Postgres service container for admin/task-store** — real DB per test run, already
  wired in `ci.yml`, matches Phase 2's Test-data strategy.
- **`Clock` injection helpers** (`agent/src/clock.ts`, `plugins/shipwright/scripts/clock.ts`,
  `metrics/src/lib/clock.ts`) — already the dominant pattern repo-wide, zero gaps found.
- **Recorded-fixture doubles** (`RecordedKubernetesClient` in `admin/src/kubernetes-client.ts`,
  `metrics/src/providers/task-store-recorded.ts`, `agent/src/shipwright-config-client.ts`)
  plus per-workspace `src/fixtures/` directories (`admin/src/fixtures`,
  `metrics/src/fixtures`, `agent/src/fixtures`, `plugins/shipwright/tests/fixtures`,
  `plugins/shipwright/scripts/test-helpers/fixtures`) — all in active, working use.
- **`scripts/check-coverage.ts`** — the 80/80 lcov-threshold gate, already enforced via
  `task test:coverage`, matches Phase 2's single-floor coverage design exactly.

### Promote / deepen

- **Recorded-fixture coverage breadth** — per Phase 2's gaps-summary item 3 and this
  phase's own `metrics/src/lib/task-store-client.ts` finding (Net-new table above): the
  `Recorded*Client` pattern is proven and working for 3 clients but not yet extended to
  the remaining ~9 `Http*Client`/SDK-wrapper modules the inventory names. This is
  infrastructure-pattern *extension*, not a rebuild of anything existing — small
  incremental effort per client, sequenced as a Phase 4/5 backlog per Phase 2's own
  deferral note.

### Rebuild

None. No test infrastructure component (runner config, CI workflow, DB/fixture setup,
shared helper) was found to have wrong fundamentals.

### Trim (redundant assertion)

None.

### Net-new

- **`shipwright_chat_test` Postgres provisioning in `ci.yml`** — the infra half of the
  chat-DB-integration net-new test item above. `ci.yml`'s `Create task-store test
  database` + `Sync test DB schema` steps need a third counterpart for chat's Prisma
  schema (`prisma migrate deploy --schema=chat/prisma/schema.prisma`) plus a
  `DATABASE_URL_CHAT_TEST` / `DATABASE_URL_SHIPWRIGHT_CHAT` env entry. Carried forward
  from Phase 2's gaps-summary item 1 — this phase confirms it as a real, currently-zero
  test-file gap (no `chat/src/*.integration.test.ts` exists today), not just a design-time
  projection.
- **`.github/pull_request_template.md`** — carried forward from Phase 2's gaps-summary
  item 2; not a test-file gap but flagged here per Step 6's "bucket the infrastructure"
  instruction to re-surface Phase 2's repo-configuration gaps that Phase 3's closer read
  didn't change.

## Risk callouts

No test currently passing (green) received a `delete` or `rebuild` verdict, so the
SKILL's mandatory risk-callout requirement for such calls has no entries to populate.

The two `Promote`-bucket rename fixes (`learning-dreamer.integration.test.ts` →
`.content.test.ts`, `lib/task-store-types.integration.test.ts` → `.unit.test.ts`) carry
**no regression risk** — a filename change with no assertion or import change; the
underlying test logic is untouched and continues to run (Bun's default test discovery
picks up any `*.test.ts`, and both suffixes remain inside `bun test`'s scanned set per
`bunfig.toml` — neither current nor new suffix is excluded via `pathIgnorePatterns`).

## Speed measurement

- **Measured:** no (no local Postgres available in this environment; see Method note above)
- **Current per-layer p95:** not measured this pass — Phase 2's Speed budgets section
  cites CI's own historical "low minutes" full-suite wall time as the standing reference;
  no per-layer breakdown captured in this document set to date
- **Tests over hard cap:** none identified via static review (no file's assertions or
  imports suggested a per-test hard-cap breach; the two largest files by line count,
  `admin/src/admin-ui.smoke.test.ts` at 4822 lines and `admin/src/admin-ui-pages.unit.test.ts`
  at 4379 lines, are large due to test-case *count* — many render-function/route
  permutations — not per-test slowness; each individual `test()`/`it()` block sampled
  was a simple string/HTTP-response assertion with no loop or sleep)

## Next step

Run `/test-roadmap` to synthesize this migration audit with the Phase 1 inventory and
Phase 2 blueprint into the executable roadmap. Candidate Phase 4 inputs from this
document: 6 net-new test items (1 critical, 2 high-adjacent, 3 medium), 2 net-new infra
items (1 carried from Phase 2, 1 new), 3 small-effort promote/rename fixes, and the
already-flagged chat-DB CI-wiring sequencing dependency between the net-new chat
integration tests and the net-new `ci.yml` provisioning step.
