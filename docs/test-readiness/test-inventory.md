# Test Inventory

> Phase 1 of the test-readiness pipeline. The source-of-truth ledger of what should be tested in this repo, at which layer, with what priority.
>
> Generated by `/test-inventory` on 2026-08-30 against `app-vitals/shipwright` (worktree: `docs/test-readiness-refresh-20260830`, commit `ac0ec6a8`).

## Stack profile

- **Language(s):** TypeScript (Bun runtime), plus Astro/Tailwind for the marketing site; a small but real bash cluster for CI chart-tag logic (`.github/workflows/lib/*.sh`)
- **Package manager:** Bun workspaces (`bun.lock`), 8-workspace monorepo (`lib`, `plugins/shipwright`, `metrics`, `agent`, `admin`, `task-store`, `chat`, `mcp-server`) plus non-workspace supporting surfaces (`site/`, `brand/`, `scripts/`, `charts/` Helm chart, `demo/`, `.github/workflows/`)
- **Primary framework(s):** Hono (all HTTP services: metrics, admin, task-store, chat), Prisma + Postgres (admin, task-store, chat), Zod/`@hono/zod-openapi` for schema validation, Astro (site), Playwright (site + admin + metrics E2E, plus a headless-Chromium agent runtime capability), Bun's built-in test runner throughout
- **Detected source roots:** `lib/`, `metrics/src/`, `agent/src/`, `agent/scripts/`, `admin/src/`, `task-store/src/`, `chat/src/`, `mcp-server/src/`, `plugins/shipwright/{commands,agents,skills,scripts,references}/`, `scripts/`, `brand/`, `site/src/`, `.github/workflows/lib/`

**CLAUDE.md layer-structure declaration:** the root CLAUDE.md's `## Test conventions` section declares five concrete layer names via filename suffix, used verbatim below instead of the rubric's generic unit/integration/smoke/E2E labels (unchanged since the 2026-07-21 inventory):

| Suffix | Layer | Covers |
|---|---|---|
| `*.unit.test.ts` | unit | pure logic, no I/O |
| `*.integration.test.ts` | integration | real dependency behavior via recorded fixtures / injected doubles |
| `*.smoke.test.ts` | smoke | HTTP route contracts — in-process `app.request()` (real `Bun.serve()` boot only when there's no Hono app-factory seam) |
| `*.spec.ts` / `*.e2e.ts` | e2e | real-browser Playwright (`site/` uses `*.spec.ts`; `admin/e2e/` uses `*.e2e.ts`) |
| `*.content.test.ts` | content | markdown/prompt-content assertions (commands, skills, agent prompts) — no I/O boundary, not one of the four canonical layers but a first-class fifth layer in this repo |

Not declared in CLAUDE.md but observed since 2026-08-06: `.github/workflows/test-*.sh` — plain-bash assertion scripts (no suffix convention, run via `bash <file>.sh` rather than `bun test`) covering CI workflow logic that lives outside the Bun test runner's reach entirely. Treated as a repo-specific sixth "shell" layer alongside the five above — unchanged this cycle, see Notes and Ambiguous.

**CLAUDE.md deploy-model declaration:** root CLAUDE.md declares `## Deploy model` as **`direct`** — "ships via Helm chart + GHCR-pinned image tags, with no staging/production GitHub Environments and no deploy-staging→canary→promote-to-production pipeline." Unchanged since the last inventory (re-verified directly against `CLAUDE.md` at this commit). Per Step 5 of the SKILL, canary-eligibility tagging is skipped entirely for this repo (see Notes).

## Sampling

- **Files inspected:** the 2026-07-31 cycle's ~40-file breadth-first base is carried forward unchanged below; every cycle since has been a targeted top-up diffed against the prior cycle's baseline commit, per SKILL.md's diff-based refresh convention. This cycle (2026-08-30) tops up the 2026-08-28 baseline (`5b586beb`) — a smaller window (28 commits, 13 non-routine) consisting entirely of correctness fixes/refactors to already-inventoried rows, one new small pure-logic feature (generic progress phases), and no new user journeys.
- **Sampling strategy (this cycle, 2026-08-30):** diffed `git log --oneline 5b586beb..ac0ec6a8` (28 commits / 69 files). Excluding chart-version-bump churn (7 `chore(chart): bump chart version` commits) and plugin-version-sync churn (2 `chore(plugin): sync version` commits, CI-generated metadata) and doc-only/sync-anchor commits (`b098dab3`, `e5dbec89`, `04cca096`), the real delta is **13 commits**, all reviewed via diff read:
  - **New feature: generic progress phases derived from the Claude stream (`lib/progress-phases.ts`, `agent/src/progress-milestones.ts`, `agent/src/claude.ts` `ProgressCallback` widened, CFB-2.1, #2906)** — a closed 11-phase enum (`lib/progress-phases.ts`) plus a pure mapper (`agent/src/progress-milestones.ts`'s `phaseForBlock()`) that derives a generic `ProgressPhase` (`reading`/`searching`/`web`/`editing`/`running`/`delegating`/`planning`/`thinking`/`writing`/`tool`) from an assistant message's `content` block `type`/tool `name` only — `block.input` is never read, a load-bearing invariant documented in the file header to prevent secrets/filesystem paths embedded in tool inputs from ever leaking through this path. `claude.ts`'s `_consumeStream`/`handleLine` walks `message.content` before the existing usage guard so a `tool_use`-only line can still fire a phase update; repeated identical phases collapse to a single fire per run. New rows for both files — category 1 (pure business logic), canonical layer unit, high (agent-observability surface, not itself revenue/security-path but the explicit secret-non-leakage invariant warrants above-`medium` attention). `claude.ts`'s existing row is extended (`ProgressCallback` widened additively with an optional phase arg) — no new row for `claude.ts` itself, no layer change. New test files observed: `lib/progress-phases.unit.test.ts` (new), `agent/src/progress-milestones.unit.test.ts` (new), `agent/src/claude.unit.test.ts` (extended, +222/-lines) — canonical-layer coverage present at inventory time.
  - **`agent/src/pr-state-reconciler.ts` (batch+rotating-cursor scan replaces `updatedSince` exclusion, #2893)** — `reconcilePrOpenTasks()` previously filtered its task-store scan by a rolling recency window, but nothing bumps a `pr_open` task's `updatedAt` while it sits waiting on human review, so a slow-review PR permanently fell out of scope after the window elapsed, even after merging on GitHub. Replaced with a fixed per-tick batch cap (`RECONCILER_PR_OPEN_TASK_BATCH_SIZE`, default 200, overridable via `SHIPWRIGHT_RECONCILER_PR_OPEN_TASK_BATCH_SIZE`) plus a module-level rotating offset cursor that wraps to 0, so a round-robin sweep across ticks eventually covers every `pr_open` task. Correctness fix to the already-inventoried, already-critical `pr-state-reconciler.ts` row — no new category or layer. `pr-state-reconciler.unit.test.ts` extended (+176/-lines) — existence-only signal, canonical-layer coverage remains present.
  - **`agent/src/check-deploy.ts` (case-insensitive CI/Deploy workflow-name matching, #2896)** — `isCiGreen()`'s exact-case match on workflow `name: "CI"` (and the busy-repo guard's match on `"Deploy"`) permanently excluded any repo whose workflow uses a different case (e.g. lowercase `ci`) from ever qualifying as a deploy candidate. Correctness fix to the already-inventoried, already-critical `check-deploy.ts` row — no new category or layer. `check-deploy.unit.test.ts` extended (+56/-lines) — existence-only signal.
  - **`agent/src/chat-poller.ts`, `chat/src/message-service.ts`, `chat/src/routes/messages.ts` (heartbeat-driven reply timeout replaces flat 90s UI cutoff, #2899 + #2902)** — chat replies spanning multiple Claude turns were finishing successfully but the admin UI showed "Request timed out" after a flat 90s poll cutoff. The agent now sends a throttled heartbeat while working a claimed message; the UI extends its timeout whenever `claimedAt`/`heartbeatAt` advances instead of tripping the same fixed cutoff. `#2902` follow-up makes the heartbeat interval-driven and owner-scoped (guards against an unclaimed/already-replied message heartbeating). Correctness fix to already-inventoried, already-`high`/`smoke` rows (`chat-poller.ts` category 2 integration; `message-service.ts`/`routes/messages.ts` fold into the existing chat-service composite row) — no new category or layer. New/extended test files observed: `agent/src/chat-poller.unit.test.ts` (+220/-lines), `chat/src/message-service.integration.test.ts` (extended), `chat/src/messages.smoke.test.ts` (new, 111 lines — a new smoke-test *file* within the already-inventoried chat-route row, not a new row), `chat/src/test-fakes.ts` (extended fake) — canonical-layer coverage present.
  - **`admin/src/admin-ui-pages.ts`/`admin-ui-layout.ts` (shared page-head extraction + viewport-meta normalization, #2905)** — extracts `renderAdminPage()` into a new file `admin-ui-layout.ts` as a dumb string wrapper (title/extraStyles/headExtra/bodyClass/body/bodyEnd), and normalizes two drifted viewport-meta spellings to `"width=device-width, initial-scale=1, viewport-fit=cover"` (required for `env(safe-area-inset-*)` on iOS) across all 22 render sites. A refactor-extraction of logic already covered by the existing `admin/src/admin-ui.ts`, `admin-ui-pages.ts`, `admin-ui-styles.ts` composite row (server-rendered UI, category 3, smoke) — no new row for `admin-ui-layout.ts`, since it's the same functional surface relocated, not a new boundary. New content-loop assertion (all 22 sites emit exactly one DOCTYPE + one viewport meta tag) plus new `renderAdminPage()` unit tests (title escaping, ordering, `bodyClass` handling) — existence-only signal, canonical-layer coverage remains present.
  - **`d37719b4` (chat page inline styles → classes, CFB-1.3, #2908) + `b4c0feb9` (mobile responsive fixes across admin console, CFB-3.1, #2914)** — `d37719b4` migrates `renderChatThreadPage()`'s ~10 inline `style=` attributes into a new `chatThreadStyles` const so the existing `@media (max-width:640px)` rules can actually take effect (inline styles previously always outranked them), and creates two new admin e2e spec files, `admin/e2e/chat-page.e2e.ts` and `admin/e2e/chat-thread-page.e2e.ts`. `b4c0feb9` fixes nine confirmed mobile defects (iOS zoom-on-focus below 16px, a broken `calc(100vh - 52px)` thread-page height, mobile-padding specificity loss, an above-the-fold thread header eating ~280px, a non-wrapping composer row, sub-44px touch targets, and a missing `image/heic`+`image/heif` attachment-allowlist entry that hard-rejected iPhone photo-library picks), adds a 641–960px tablet breakpoint band, and exports `BREAKPOINT_MOBILE_MAX`/`BREAKPOINT_TABLET_MAX` as shared constants; verified via new Playwright e2e assertions (computed font-size, absence of horizontal overflow, computed touch-target height) rather than string-matching rendered HTML, with three now-redundant unit tests removed in favor of the e2e coverage. Both are correctness fixes / test-layer migrations within the already-inventoried admin-UI composite row (category 3, smoke) and the already-inventoried admin e2e user-journey row (`admin/e2e/agents-page.e2e.ts`'s row, extended to also cover the chat-page/chat-thread-page journeys) — no new row. `admin/src/attachment-validation.ts`'s allowlist extension is a correctness fix to an already-inventoried row (`attachment-validation.unit.test.ts` extended, +33 lines). New e2e spec files `admin/e2e/chat-page.e2e.ts` (new), `chat-thread-page.e2e.ts` (new, 118 lines) — existence-only signal, folding into the existing admin e2e user-journey row rather than creating new rows (consistent with the row already being a collective across admin e2e specs, mirroring the `site/src/pages/*.astro` marketing-journey row's treatment of multiple page files as one row).
  - Also reviewed, no inventory impact: `admin/src/http-chat-client.ts` (+3 lines, wiring for the heartbeat feature above, folds into the existing admin-UI composite row), `renovate.json` (`5150bd14`, enables GitHub-native platform automerge for minor/patch Renovate PRs — declarative bot config, not a code unit, excluded per the existing top-level-config exclusion), `charts/shipwright/{Chart.yaml,CHANGELOG.md,values.yaml}` (chart-version-bump churn, folded per usual), `charts/shipwright/README.md`, `templates/NOTES.txt`, `templates/_helpers.tpl`, `templates/admin-deployment.yaml`, `tests/admin_workload_test.yaml` (`90e36b1b`'s `shipwright.admin.appBaseUrl` derivation-from-public-host helper for ingress/gateway installs — declarative Helm chart content + `helm-unittest` YAML assertions, excluded per the existing `charts/shipwright/` exclusion), `chat/openapi.json`, `chat/prisma/schema.prisma` + migration, `chat/src/generate-spec.ts`, `chat/src/openapi-schemas.ts` (generated/schema wiring following the heartbeat feature above, folded into the existing chat-service row), `docs/agent.md`, `docs/chat.md`, `docs/configuration.md`, `docs/deploy-kubernetes.md`, `docs/helm-repo.md` (doc-only, several syncing the appBaseUrl/heartbeat/progress-phases additions above), `lib/package.json` (new `lib/progress-phases.ts` file wiring, non-code), `state/docs-last-synced.json` (sync anchor bump), `bun.lock`, `agent/package.json`, `metrics/package.json`, `package.json`, `plugins/shipwright/.claude-plugin/plugin.json`, `plugins/shipwright/package.json`, `version.txt` (version-sync churn), `plugins/shipwright/commands/deploy.md`, `merge.md` + paired `merge.content.test.ts` (content-only, already-inventoried `content` category rows). No files were deleted this cycle. No new user journeys. `.claude/shipwright/test-readiness-decisions.md` re-checked per Step 0 — both existing entries (`site/src/lib/html-escape.ts`, `scripts/hitl.ts` bootstrap) remain applicable, no revisit conditions met, no new entries added since last cycle. `deploy_model` re-verified as `direct` directly against `CLAUDE.md` at this commit (`ac0ec6a8`) — unchanged, canary eligibility tagging remains skipped for the entire inventory.
- **2026-08-28 cycle's sampling strategy** (below, preserved for continuity): diffed `git diff --stat d4bfce02..7a688190` (69 commits / 136 files). Excluding chart-version-bump churn (11 `chore(chart): bump chart version` commits) and plugin-version-sync churn (11 `chore(plugin): sync version` commits, CI-generated metadata) and this pipeline's own prior docs-refresh commits (`988c566f`, `7f4a4119`, `1f739ac3`, `800b8b51`, and `d6cef04f`'s `docs/test-readiness/*.md` hunk), the real delta is **42 commits**, of which the code-surface-relevant ones are:
  - **New feature: task-store DB-aware readiness check (`task-store/src/app.ts`, `main.ts`, `app.readiness.smoke.test.ts`, `main.unit.test.ts`, new, #2865)** — `GET /ready` (previously a bare liveness 200) now runs a real `SELECT 1`-equivalent Prisma round-trip and returns 503 when the DB is unreachable, so a Kubernetes readiness probe actually gates traffic on DB connectivity instead of always passing. `main.ts` wires a `PrismaClient` into the route factory; the Helm chart's `task-store-deployment.yaml` readiness probe path/config is unchanged (still `/ready`, now backed by real behavior). Folds into the existing Task store composite row (`task-store/src/app.ts`, `main.ts`, `index.ts`) — no new row, but the row's canonical layer note is extended: the readiness path is now smoke-tested against a real Postgres round-trip (`app.readiness.smoke.test.ts`, new file) rather than being a static 200. New test files observed: `task-store/src/app.readiness.smoke.test.ts` (125 lines, new), `main.unit.test.ts` (+26) — canonical-layer coverage present at inventory time.
  - **New feature: downstream chart-compatibility render-diff script (`scripts/check-downstream-compat.sh`, `check-downstream-compat.smoke.test.ts`, new, CNH-8.3, #2843)** — a bash script rendering a downstream "wrapper" chart's Shipwright dependency three ways (as currently pinned, at a published gh-pages baseline, and from this repo's working tree) and diffing them, so a chart change that would break a real downstream consumer is caught pre-merge; the published Helm repo URL is derived at runtime (never hardcoded) from `--repo-url`, the wrapper's `Chart.yaml` dependency entry, or this repo's own git remote. New row — category 2 (service-boundary: git/helm/filesystem orchestration), canonical layer **smoke** (the repo's established bash-script convention for CI-invoked shell orchestration is a `*.smoke.test.ts` extracting and executing the real script logic against synthetic fixtures — see `check-downstream-compat.smoke.test.ts`, which does exactly this for both the digest-change and plain-tag-change cases — not the bare `.sh`-assertion "shell" layer used by the `.github/workflows/lib/*.sh` cluster below, since this script ships as a first-class `Taskfile.yml` target (`helm:downstream-compat`) for downstream consumers to run directly, not CI-internal-only), high (protects downstream chart consumers from silent breakage, not itself on this repo's own revenue/security path). A fix landed in the same PR window for a restore-guard bug (checked the post-delete destination path instead of the source wrapper path) — the new smoke test explicitly regression-tests that exact restore condition. New test file observed: `scripts/check-downstream-compat.smoke.test.ts` (175 lines, new) — canonical-layer coverage present at inventory time.
  - **New feature cluster: cloud-native networking hardening (`charts/shipwright/templates/{cert-manager-bootstrap-job,cert-manager-bootstrap-rbac}.yaml`, bundled `ingress-nginx`/`traefik`/`cert-manager` subcharts, `scripts/minikube.ts` cloud-native profiles, CNH-2.1/CNH-4.1/CNH-5.1/CNH-6.1/CNH-7.1/CNH-8.1, PRs #2819–#2824/#2844)** — six PRs building a fully self-contained "helm install and you have ingress+TLS" path: Ingress TLS + cert-manager ingress-shim helpers (nginx), Traefik ingress controller support, a chart-created cert-manager `Issuer` for externally-managed cert-manager, three optional bundled subcharts (`ingress-nginx`, `traefik`, `cert-manager`, each `<name>.enabled`-gated and vendored with `Chart.lock`, default off — byte-identical default render preserved), and a post-install/post-upgrade bootstrap hook Job (`cert-manager-bootstrap-job.yaml` + RBAC) solving the CRD-not-yet-installed-at-apply-time problem when cert-manager is bundled in the same release. All of this is declarative Helm chart content + `helm-unittest` YAML assertions (`tests/cert_manager_bootstrap_job_test.yaml`, `bundled_subcharts_test.yaml`, `cloud_native_profile_test.yaml`, `test-cert-issuer.yaml`, extended `ingress_traefik_test.yaml`/`test-connection.yaml`) — excluded from per-file inventory rows per the existing, repeatedly-applied `charts/shipwright/` exclusion (no `bun test`-classifiable code units; chart-level `helm unittest` assertions get the same non-inventoried treatment as all prior chart work). The one genuine source-code row here is **`scripts/minikube.ts`'s cloud-native profile support** (#2878) — refactors the file to a per-profile `PROFILES: Record<Profile, ProfileConfig>` config object consumed by the existing pure command-builder, adding `cloud-native-nginx`/`cloud-native-traefik` profiles alongside the original `addon` default; extends the already-inventoried, already-out-of-scope `scripts/minikube.ts` row's existing split classification (unit builders / integration driver) — no new row, no layer change. New/extended test coverage observed: `scripts/minikube.unit.test.ts` (extended, including a byte-identical regression pin for `task minikube:up`'s emitted command list) — existence-only signal.
  - **`agent/src/check-review.ts`, `check-helpers.ts`, `plugins/shipwright/scripts/check-helpers.ts` (Renovate exclusion + plain-YAML policy-parsing fix, #2881/#2852/#2829)** — (a) `56217d3b` generalizes the single hardcoded `app/dependabot` string comparison in `getReviewCandidates()` into an `EXCLUDED_BOT_AUTHORS` list covering both Dependabot and Renovate (now installed alongside it), and generalizes `ReviewCandidacyTrace`'s `"dependabot"` case to `"excluded-bot-author"`; (b) `4abd283b` fixes `parseAllowSelfReview`/`parseCleanupMergedWorktrees` (previously only recognizing markdown-table or bold-colon formats) to also parse the plain-YAML-frontmatter format `state/agent-policy.md` actually ships in, closing a bug where `allow_self_review: true` was silently read as `false`; (c) `31a1a0b8` changes the *default* (missing/unparseable policy) fallback for `allow_self_review` from `true` to `false`, a fail-closed correctness fix. All three are correctness fixes to the already-inventoried, already-critical `check-review.ts`/`check-helpers.ts` rows — no new category or layer, consistent with this cluster's historical fix-density noted in prior cycles.
  - **`agent/src/check-deploy.ts`, `check-review.ts`, `pr-state-reconciler.ts` (live policy-flag reads, PLR-1.1, #2851)** — `isSelfReviewAllowed` and `isCleanupMergedWorktreesEnabled` were plain booleans captured once inside the memoized `buildProductionDeps()`, so an edit to `state/agent-policy.md` never took effect until an agent restart; converted to getter functions (`() => boolean`) invoked fresh on every call, mirroring the existing live-read pattern `cleanup_after_days` and `worktree-reaper.ts` already used. Correctness fix to already-inventoried, already-critical rows — no new category or layer.
  - **`agent/src/loop-orchestrator.ts` (phase-scoped redispatch cooldown, PRC-1.1, #2825)** — the redispatch-cooldown key was previously keyed by PR ID alone, so a cooldown set by one phase (e.g. review) suppressed dispatch of an unrelated phase (e.g. patch) on the same PR; rescoped to `${pr.id}:${phase}` so each phase's cooldown history is tracked independently. Correctness fix to the already-inventoried, already-critical `loop-orchestrator.ts` row — no new category or layer.
  - **`plugins/shipwright/commands/dev-task.md` (ScheduleWakeup/backgrounding prohibition guardrail, #2849)** — Steps 8 and 9b.2 now explicitly require long-running waits to run synchronously or backgrounded-and-polled within the same session, closing the gap that let a dev-task session background a CI-polling loop via a scheduled wakeup that never resumed (the real incident this cites, task AGH-1.1, is the same failure class as this pipeline's own documented `ScheduleWakeup` guidance). Content-only extension to the already-inventoried `content` category row (`plugins/shipwright/commands/*.md` + paired `dev-task.content.test.ts`) — no new row.
  - **`plugins/shipwright/commands/deploy.md`, `merge.md` (branch-protection-block detection, #2834) and `review.md` (Step 5.5 GraphQL variable-extraction fix, #2832)** — both content-only command-prompt extensions, already-inventoried `content` category rows (each with paired `.content.test.ts` extensions) — no new row.
  - **`plugins/shipwright/test/cron-tables.content.test.ts` (new, SUX-1.1, #2864)** — a new regression-guard content test asserting every manifest cron appears exactly once in `cron-jobs.mdx`'s table and zero times in `the-agent.mdx` (which now links to it instead of re-documenting the table), fixing drift between the two docs pages and `agent-types/coding/manifest.yaml`. New test file within the already-inventoried `content` category (a new content-test file, not a new source row).
  - **`site/src/components/{SecurityArchDiagram,StepFlow}.astro` (new, #2835/#2863) and `site/src/content/docs/security.mdx` (SDP-1.1 new page, #2826, plus 8 follow-up hardening/correction commits)** — a new public security-architecture docs page (deployment topology, network/egress posture, human access, K8s RBAC, secrets/auth, GitHub App + Slack App scopes, compliance posture, FAQ) and two new zero-client-JS presentational Astro components (a static security architecture diagram; a reusable numbered-step/flow component reusing the same box-and-arrow visual language, unblocking future linear-process docs pages). Both components are presentational-only with "no dedicated test, covered by the astro build + brand-lint CI gate" per the existing convention already applied to `ComparisonTable.astro`/`ConfigTable.astro`/`ChooseWhenGrid.astro` — no new row for the components themselves. `security.mdx` is a new page within the already-inventoried `site/src/pages/*.astro` (page content, not `.astro` itself, but same user-journey row) — folded into the existing marketing-site user-journey row per the established pattern for new/extended docs pages (e.g. the 2026-08-14 cycle's `/vs/factory` page); its Playwright coverage (`site/tests/docs-security.spec.ts`, new + extended 5 times across the window) is existence-only signal, consistent with that row's e2e canonical layer.
  - **`site/src/pages/docs/index.astro` removed, replaced by a redirect (#2833)** — the standalone `/docs` landing page is deleted; `/docs` now redirects to `/introduction` via `site/src/content/docs/introduction.mdx` frontmatter. A genuine deletion (`docs-landing.spec.ts` deleted, replaced by `docs-redirect.spec.ts`), but the deleted page was itself a member of the existing `site/src/pages/*.astro` marketing-site journey row, not an independently inventoried row — no row removed from the Summary table, since this row was always a collective ("marketing pages: index, ... docs/*") rather than one row per page.
  - **Heavy `site/` docs prose-to-table conversion cluster (10 commits: `2a388eca`, `7d255b6a`, `63fef533`, `b0419798`, `49c2e423`, `cfa42175`, `55671276`, `2f290939`, `bec31c77`, `120dedc6`, `d65d732c`, `9ba8b684`, `e05af294`)** — a dense run of docs-only rewrites converting bullet/prose lists to tables across `security.mdx`, `the-agent.mdx`, `cron-jobs.mdx`, `configuring-autonomy.mdx`, `getting-started.mdx`, `deploying-to-cloud.mdx`, `admin-console.mdx`, `running-locally.mdx`, plus `docs/agent.md`/`docs/observability.md` sync passes. All content-only, all within the already-inventoried marketing-site journey row and `content` category — no new rows. `2f290939`/`bec31c77`/`f1270a21` also extend `site/tests/docs-security.spec.ts` alongside their `security.mdx` edits — existence-only signal, no independent classification.
  - Also reviewed, no inventory impact: `docs/helm-repo.md`, `docs/architecture.md`, `docs/task-store.md`, `docs/configuration.md`, `docs/deploy-kubernetes.md` (doc-only, several syncing the DB-readiness-check and downstream-compat-script additions above), `state/docs-last-synced.json` (sync anchor bump), `CONTRIBUTING.md` (downstream-compat-script + subchart-pin-bump procedure documentation, non-code), `Taskfile.yml` (`helm:downstream-compat`, `minikube:cloud-native*` target wiring, declarative), `.github/workflows/{helm,chart-release}.yml` (CI wiring for the cloud-native chart work, declarative), `charts/shipwright/{Chart.yaml,Chart.lock,CHANGELOG.md,values.yaml,values.schema.json,CONTRIBUTING.md}` (chart-version-bump + cloud-native-networking chart wiring, already folded into the CNH cluster above), `bun.lock`, `admin/package.json`, `chat/package.json`, `metrics/package.json`, `task-store/package.json` (`a8f9d547`'s `workspace:*` dependency-declaration fix — Renovate-lookup-warning correctness fix, not a source-code row, consistent with the existing exclusion of `package.json`/`bun.lock` version-sync churn), `docs/test-readiness/*.md` (this pipeline's own artifacts, regenerated by phases 2–5). No files were deleted this cycle other than the `/docs` landing-page/spec pair noted above (not an independently inventoried row). No new user journeys. `.claude/shipwright/test-readiness-decisions.md` re-checked per Step 0 — both existing entries (`site/src/lib/html-escape.ts`, `scripts/hitl.ts` bootstrap) remain applicable, no revisit conditions met, no new entries added since last cycle. `deploy_model` re-verified as `direct` directly against `CLAUDE.md` at this commit (`7a688190`) — unchanged, canary eligibility tagging remains skipped for the entire inventory.
- **2026-08-25 cycle's sampling strategy** (below, preserved for continuity): diffed `git diff --stat be5bfa59..d4bfce02` (19 commits / 31 files). Excluding chart-version-bump churn (`9b246132`, `4dd5cc90`, `88311ff4`, `bc571f8d`, `c8638efd`), plugin-version-sync churn (`4f538992`, `9aebf20e`, `939d6bdb`, `c5645b88`, `f78cdd26`), and this pipeline's own prior docs-refresh merge (`4d948eaf`) plus its docs-sync-anchor bump (`19812c30`), the real delta is **five commits, all either content-only or infra/dependency fixes to already-inventoried rows — zero new source-code rows, zero deletions, zero new user journeys**:
  - **`da1aa278` (feat: add principles.md override check + security domain citation to dev-task, #2811)**, **`31278dbd` (feat: add principles.md override check to review/code-reviewer, #2810)**, **`acec7edd` (feat: add principles.md override check + security domain citation to plan-session, #2803)** — three content-only extensions wiring the same `.claude/shipwright/principles.md`-override-then-fallback pattern (already established by `entropy-scan/SKILL.md`) into `dev-task.md`, `review.md`/`code-reviewer.md`, and `plan-session.md` respectively, each citing the security domain alongside architecture/testing. All three land inside already-inventoried `content` category rows (the `plugins/shipwright/commands/*.md` + paired `*.content.test.ts` rows), each gaining new content-test assertions (`dev-task.content.test.ts`, `plan-session.content.test.ts`, `review.content.test.ts`, `code-reviewer.unit.test.ts` all extended) — no new row, no layer change.
  - **`da6e21df` (feat: add explicit chaining instruction to error-patrol-maintenance cron prompt, #2804)** — adds an explicit "continue automatically through all three commands" instruction to the `error-patrol-maintenance` cron's prompt (between the three `/shipwright:X` lines and the "After the chain completes" line), fixing a measured 0/16 historical chaining rate for that cron vs. 5/5 for the analogous `security-patrol-maintenance` cron. Content-only cron-prompt change; the paired golden-fixture test (`admin/src/agent-cron-jobs.integration.test.ts`) was updated in the same commit to match the new prompt string — both already-inventoried rows (cron-prompt content + the `agent-cron-jobs.integration.test.ts` golden-fixture assertions), no new row.
  - **`72d84950` (fix: bootstrap consolidation-patrol precheck instead of permanent deadlock, #2801)** — `plugins/shipwright/scripts/check-consolidation-patrol.ts`'s missing-ledger branch changes from `exit 1` ("nothing to do") to `exit 0` ("bootstrap unlock"), fixing a confirmed permanent deadlock (the `consolidation-patrol-maintenance` cron has never fired since 2026-07-18, because the ledger it gates on is only ever written by the scan the precheck itself blocks). Correctness fix to the already-inventoried, already-`medium`-criticality `check-consolidation-patrol.ts` row (category 2, integration) — no new category or layer. `check-consolidation-patrol.unit.test.ts` extended alongside — existence-only signal, canonical-layer coverage remains present.
  - **`6b6709cf` (fix: bump deepmerge-ts to 8.0.2 to patch stack-exhaustion DoS, CVE-2026-40345, #2798)** — adds a root `package.json` bun override pinning the transitive `deepmerge-ts` dependency (pulled in exactly-pinned by `@prisma/config`) to the patched version. Dependency-version bump only, no source-code row (consistent with the existing exclusion of `bun.lock`/`package.json` version churn from per-file inventory rows) — flagged here only because it's a security fix, not routine version-sync noise.
  - Also reviewed, no inventory impact: `agent-types/coding/manifest.yaml` + new `manifest.content.test.ts` (content-only manifest field addition, paired with a new content-test file — first observed this cycle but the `agent-types/coding/manifest.yaml` surface itself is not yet a distinct inventory row; folding into the existing `content` category note rather than adding a new row, since it's schema/prompt content, not executable logic), `.claude-plugin/marketplace.json`, `agent/package.json`, `metrics/package.json`, `plugins/shipwright/package.json`, `plugins/shipwright/.claude-plugin/plugin.json`, `bun.lock`, `version.txt` (version-sync churn), `charts/shipwright/{Chart.yaml,CHANGELOG.md,values.yaml}` (chart-version-bump churn), `docs/consolidation.md`, `docs/extending.md` (doc-only, documenting the principles.md-override additions above), `state/docs-last-synced.json` (sync anchor bump), `docs/test-readiness/*.md` (this pipeline's own artifacts, regenerated by phases 2–5). No files were deleted this cycle. No new user journeys. `.claude/shipwright/test-readiness-decisions.md` re-checked per Step 0 — both existing entries (`site/src/lib/html-escape.ts`, `scripts/hitl.ts` bootstrap) remain applicable, no revisit conditions met, no new entries added since last cycle. `deploy_model` re-verified as `direct` directly against `CLAUDE.md` at this commit (`d4bfce02`) — unchanged, canary eligibility tagging remains skipped for the entire inventory.
- **2026-08-23 cycle's sampling strategy** (below, preserved for continuity): diffed `git diff --stat ea846283..be5bfa59` (5 commits). Excluding chart-version-bump churn (`efb98a08`), plugin-version-sync churn (`b080d823`, `4269ceb2`), and this pipeline's own doc-only commits (`ee616a1a` docs-last-synced anchor bump; `be5bfa59` itself, the prior cycle's docs-refresh PR merge — same commit this file's own prior header cited as its generation point), the real delta is **exactly 2 commits, both already resolving the prior cycle's open watch item**:
  - **`3df0539e` (feat: enforce Slack membership check across message, mention, and reaction handlers, #2788)** — adds `isAllowedSlackSender()` (pure predicate: fails open when the membership ref hasn't synced or `restrict` is false; otherwise a case-insensitive email match against the synced member list) and `shouldRejectSlackSender()` (resolves the sender's email via `resolveUserEmail`/`resolveSlackMembership` then checks it against `isAllowedSlackSender()`) in `agent/src/slack.ts`, called at the top of `app.message()`, `app.event("app_mention")`, and `app.event("reaction_added")` before `runner()` is invoked — non-members and unresolved emails are rejected silently (debug log only, no reply, no runner call). This is the enforcement half of the SMR (Slack-membership restriction) feature whose config-plumbing half (`agent-slack-membership-ref.ts`, admin UI toggle, runtime config lookup) was already inventoried last cycle as a new row — `isAllowedSlackSender`/`shouldRejectSlackSender` extend the existing `agent/src/slack.ts` row (category 3, HTTP-route-equivalent, smoke, critical) rather than creating a new one, since they're additional wire-contract-gating logic within the same already-inventoried file, not a new functional surface with its own boundary. Two follow-up commits in the same PR fixed a real test-isolation gap the new code introduced: `createSlackApp()`'s new `membershipRef` parameter defaulted to the process-wide `agentSlackMembershipRef` singleton, which `agent/src/slack.integration.test.ts` and `agent/src/slack-chat-conversation.integration.test.ts` both fell through to — since Bun shares the test process, either suite's outcome depended on file-execution order whenever `agent-slack-membership-ref.unit.test.ts` had already mutated the singleton to `restrict:true` (rejecting every sender). Fixed by injecting a fresh, unsynced ref per test in both integration suites. New test coverage observed: `agent/src/slack.unit.test.ts` gained 13 assertions covering `isAllowedSlackSender`/`shouldRejectSlackSender` (fail-open unsynced, fail-open `restrict:false`, case-insensitive match, unresolved-email reject, non-member reject, per-handler wiring across all 3 call sites) — canonical-layer coverage present at inventory time.
  - **`192437ed` (test: reset agentSlackMembershipRef singleton after test, T-084, #2790)** — adds an `afterEach` to `agent-slack-membership-ref.unit.test.ts`'s process-wide-singleton describe block, resetting `agentSlackMembershipRef` back to its fail-open default after each test. This **resolves the prior cycle's Ambiguous/watch item** (flagged 2026-08-22: "watch for the singleton test-leak fix... PR #2788, lands just after this window") — the fix landed as a *separate* PR (#2790, not #2788 as the prior note anticipated) but addresses exactly the flagged risk: the file-order-dependent CI-only leak where `agent-slack-membership-ref.unit.test.ts` mutating the shared singleton to `restrict:true` could pollute a sibling suite that ran after it in the same Bun test process. Combined with #2788's own two fix-commits above (which independently discovered and closed the *other* two call sites of the same leak — `slack.integration.test.ts` and `slack-chat-conversation.integration.test.ts` — that this `afterEach` alone doesn't reach, since those suites construct their own `createSlackApp()` instance rather than reading the module-level singleton directly), the singleton's cross-suite isolation risk is now closed at all 3 known call sites. **Removing this Ambiguous item** from the list below — resolved, not carried forward, per project memory confirming PR #2788 "fixed a missed direct call site the wrapper fix didn't cover."
- **2026-08-22 cycle's sampling strategy** (below, preserved for continuity): diffed `git diff --stat f901803d..HEAD` — 84 commits / 138 files. Excluding chart-version-bump churn (18 `chore(chart): bump chart version` commits touching `charts/shipwright/Chart.yaml`/`values.yaml`/`CHANGELOG.md`/`README.md`/`values.schema.json`/`templates/*.yaml`/`tests/*.yaml` where the diff is purely the new Okta/SMR-related chart wiring covered below, not independent drift) and plugin/agent/metrics/task-store/lib/mcp-server/chat/site `package.json`/`.claude-plugin/marketplace.json`/`plugin.json`/`bun.lock`/`version.txt` version-sync churn (11 `chore(plugin): sync version` commits, CI-generated metadata), plus doc-only churn (`docs/*.md`, `state/docs-last-synced.json`, `planning/*/PLAN.md`, `planning/*/PRODUCT-SPEC.md`) and this pipeline's own regenerated artifacts (`docs/test-readiness/*.md`), the real delta is **four new feature clusters (Okta OIDC auth, GitHub App manifest-flow provisioning, Slack-membership-restriction (SMR), PR audit-trail events (PSA-1.2)) plus one deletion (the never-decrease coverage gate, fully retired) plus several correctness fixes to already-inventoried review/patch candidacy rows**. Reviewed via diff read (full or targeted, per file):
  - **New feature: Okta OIDC auth (`admin/src/okta-auth-client.ts`, `admin/src/main.ts` login-route wiring, `charts/shipwright/{values.yaml,values.schema.json,templates/admin-secret.yaml,templates/admin-deployment.yaml}`, PRs #2723/#2724/#2727/#2733/#2742)** — `OktaAuthClient` interface + `HttpOktaAuthClient` implementation (typed OIDC authorize/token-exchange/userinfo client, endpoints derived from a caller-supplied issuer URL per Okta's well-known conventions — no live `.well-known` discovery call, fully synchronous/side-effect-free construction), mirroring the existing `google-auth-client.ts` row's shape exactly. `b590fe63` extracts shared login logic so both Google and Okta share one code path; `143e99ee` adds the "Sign in with Okta" login-page button; `9187d565` extends the Helm chart's `auth.mode` enum with `okta` (mirrors the existing `google` mode: `auth.okta.{issuer,clientId,clientSecret,allowedEmails,existingSecret}` + wiring into `admin-secret.yaml`/`admin-deployment.yaml`); `3a4d71f5` adds Okta-authenticated-access smoke tests. New row (parallel to the existing `google-auth-client.ts` row) — category 5 (external integration), canonical layer integration (recorded fixture, new `admin/src/fixtures/okta-auth-cassette.json`), critical (auth boundary). New test files observed: `admin/src/okta-auth-client.integration.test.ts` (new) — existence confirms the row is covered at its canonical layer.
  - **New feature: GitHub App manifest-flow provisioning (`admin/src/github-app-provisioning-client.ts`, `admin/src/provision-callback.smoke.test.ts` route wiring, PRs #2761/#2772)** — `GithubAppProvisioningClient` interface + `HttpGithubAppProvisioningClient`: drives the one-time "create a GitHub App from a manifest" flow (redirect → GitHub-hosted manifest form → one-time `code` → unauthenticated `POST app-manifests/{code}/conversions` → app credentials returned directly). The conversion response's `webhook_secret` field is intentionally discarded (no webhook receiver in this codebase) — never stored, never returned; worth flagging since a future webhook feature must not assume it was captured. `632dc1ce` wires the manifest-flow routes + provision-form UI. New row — category 5 (external integration, one-time OAuth-adjacent exchange), canonical layer integration (recorded fixture, new `admin/src/fixtures/github-app-manifest-cassette.json`), critical (one-time credential-issuing boundary). New test files observed: `admin/src/github-app-provisioning-client.unit.test.ts` + `.integration.test.ts` (both new) — canonical-layer coverage present at inventory time.
  - **New feature: Slack-membership restriction, SMR (`agent/src/agent-slack-membership-ref.ts`, `admin/src/admin-ui-pages.ts`/`admin-ui.ts` restrictSlackToMembers UI, `admin/prisma/schema.prisma` + migration `20260821000000_add_agent_restrict_slack_to_members`, PRs #2763/#2762/#2782/#2785)** — `AgentSlackMembershipRef`: a small mutable box holding the agent's most-recently-synced Slack membership restriction config (`{restrict: boolean, emails: string[]}`), mirroring the existing `agent-author-allowlist-ref.ts` row's pure/zero-I/O pattern deliberately so it's unit-testable in isolation. Callers needing to distinguish "never synced" from "synced to deliberately unrestricted" must call `hasSynced()` rather than inferring it from the default value — both states return the same `{restrict: false, emails: []}` shape, a real footgun worth flagging for reviewers of any new consumer. `bbdf5cfb` adds the `restrictSlackToMembers` toggle + admin UI checkbox/warning banner (`admin-ui-pages.ts`/`admin-ui.ts`, already-inventoried rows — no new row, extends existing); `38c766d2` caches Slack sender email alongside display name (SMR-2.2); `85e981dc` adds the `users:read.email` Slack scope; `85353d45` exposes `restrictSlackToMembers` + member emails on the runtime config lookup; `86633b1c` wires `agentSlackMembershipRef` into the config-sync tick. New row for `agent-slack-membership-ref.ts` — category 1 (pure business logic), canonical layer unit, critical (access-control boundary — this directly gates who can message a restricted agent). New test file observed: `agent/src/agent-slack-membership-ref.unit.test.ts` (new) — canonical-layer coverage present. Per the project memory note on this feature (SMR membership singleton test leak, PR #2788, landed just after this diff window closes at `ea846283` — not yet in this baseline): flag as an **Ambiguous/watch item** for the next cycle once that PR's commit lands, since it fixed a file-order-dependent CI-only test leak in this exact area.
  - **New feature: PR audit-trail events, PSA-1.2 (`task-store/prisma/schema.prisma` new `PullRequestEvent` model + migration `20260818000000_add_pull_request_event`, `task-store/src/pr-transition-diff.ts`, `routes/prs.ts` new `GET /prs/:id/events`, PRs #2713/#2718)** — `computePrTransitionDiff` is a pure, deliberately I/O-free diff function comparing a `PullRequest` record's before/after state and returning one entry per changed, auditable field; `PullRequestService.recordTransition()` wraps it to persist the resulting entries as `PullRequestEvent` rows inside the same transaction as the source update. Two design notes worth preserving for future classification: (1) `heartbeatAt` is excluded from the audit trail entirely (a bare liveness ping carries no diagnostic value and would dominate write volume — a heartbeat-only update yields zero event rows, even when it lands alongside a real field change, in which case only the real field's entry is produced); (2) only scalar, application-meaningful columns are diffed via an explicit allowlist (`AUDITED_FIELDS`), not a denylist — a newly-added scalar column defaults to NOT audited until deliberately opted in, favoring silence over silently auditing (and potentially leaking) a new field. New route `GET /prs/:id/events` (`96646400`) surfaces the event log read-only. New row for `pr-transition-diff.ts` — category 1 (pure business logic, diff computation), canonical layer unit, high (audit-trail correctness, not itself revenue/security-path but feeds review/patch decision-tracing). The route + service-layer persistence fold into the existing Task store composite smoke/integration row (same pattern as the PFL-1.1/PFL-2.1 PrFinding row from the 2026-08-18 cycle) — no new category there. New test files observed: `task-store/src/pr-transition-diff.unit.test.ts` (new), `task-store/src/pull-request.integration.test.ts` (new) — canonical-layer coverage present. Companion: `7f574ffa` adds a nullable `agentId` to `PrFinding` and threads it through `patch.md`/`appendFinding()` — extends the existing PrFinding row from the prior cycle, no new row.
  - **Deletion: never-decrease coverage gate fully retired (`2ab1a4cb`, "remove the never-decrease coverage check")** — after the 3-stage coverage-promotion lifecycle completed, `scripts/check-coverage-no-decrease.ts` and both its test files (`*.unit.test.ts`, `*.integration.test.ts`) were deleted outright, along with the `coverage-no-decrease` CI job and doc references. **Removes the row added in the 2026-08-16 cycle** (`scripts/check-coverage-no-decrease.ts`, previously category 2/1 split-classification, high) — this is a genuine deletion, not a rename/refactor, so the row is removed from the Summary table below rather than marked stale. Companion: `5bb3cf6d` raises `check-coverage.ts`'s line-coverage threshold from 89 to 90 (matches `CLAUDE.md`'s `task test:coverage` doc, already reflecting 90/89 — no drift). Existing `check-coverage.ts` row's threshold constant changed, no new row.
  - **`agent/src/check-review.ts`, `check-patch.ts`, `review.md` review/patch-candidacy correctness fixes (33e69283, 668638a2, dca470d3, 5f17f4b5, 34552115, fad53537, 0577938a, 46206d38)** — a dense cluster of fixes to the already-inventoried, already-critical `check-review.ts`/`compute-unaddressed-findings.ts`/`review.md` candidacy-decision rows this cycle: (a) `33e69283` scopes `isAddressedByAuthorReply` to the real PR author rather than `currentUser` (self-review false-negative fix); (b) `668638a2` re-fetches `headRefOid` immediately before the review POST to abort on a stale head (closes a race where the head moved mid-review); (c) `dca470d3` removes a release-before-patch race in `review.md`'s terminal-skip path; (d) `5f17f4b5` makes Step 14's terminal-review check state-aware; (e) `34552115` enforces findings-ledger-before-complete ordering in `review.md`; (f) `fad53537` excludes PR-author-reply-addressed threads from the unresolved-threads gate (extends `compute-unaddressed-findings.ts`'s `isThreadAddressedByAuthorReply`, added last cycle, from structurally-inert-on-`check-patch.ts`'s-path toward actually being consulted); (g) `0577938a` adds a ledger-timestamp candidacy trigger to `check-review.ts` (PFL-3.1); (h) `46206d38` clarifies Step 7 dispatch synchronicity and guards against `ScheduleWakeup` misuse in `review.md`. All are correctness fixes to already-inventoried, already-critical rows — no new category or layer, consistent with this cluster's historical fix-density (this row has now absorbed fix clusters in the 2026-08-14, 2026-08-16, and this cycle's inventories).
  - **`admin/src/admin-ui-pages.ts`/`admin-ui.ts` (agent-create/provisioning UX, f8ed3e11, 994c0e27, 9e6639c8)** — `f8ed3e11` makes `/admin/agents/new` the primary agent-create CTA; `994c0e27` relabels the toolbar "Provision" nav link to "Slack"; `9e6639c8` lets `/admin/agents/new` provision an in-cluster agent without Slack (companion to the SMR feature above — an agent can now exist with Slack access deliberately unconfigured, not just restricted). UX/routing fixes to the already-inventoried, already-high admin-UI row — no new category or layer.
  - **`7d1911f1` (fix(chart): emit a 64-char-hex SHIPWRIGHT_ENCRYPTION_KEY, chart-only)** and **`8c249651` (suppress confirmed false-positive gitleaks findings, new `.gitleaksignore`)** — both infrastructure/CI-config fixes, not source-code rows; consistent with the existing exclusion of chart/CI-config churn from per-file inventory rows.
  - Also reviewed, no inventory impact: `docs/agent-api.md`, `docs/agent.md`, `docs/architecture.md`, `docs/configuration.md`, `docs/deploy-kubernetes.md`, `docs/migration.md`, `docs/quickstart.md`, `docs/task-store.md`, `docs/testing.md` (doc-only), `state/docs-last-synced.json` (sync anchor bump), `planning/dual-coverage-gates/{PLAN.md,PRODUCT-SPEC.md}` (pre-implementation/retrospective design docs, not shipped code), `docs/test-readiness/*.md` (this pipeline's own artifacts, regenerated by phases 2–5), `plugins/shipwright/commands/{patch,review}.md` + paired `.content.test.ts` (content-only, already-inventoried `content` category rows), `plugins/shipwright/scripts/compute-unaddressed-findings.ts` + `.unit.test.ts` (extended per the fix cluster above, already-inventoried row, no new row), `admin/openapi.json`, `admin/src/openapi-schemas.ts`, `task-store/openapi.json`, `task-store/src/{app,index,generate-spec,openapi-schemas}.ts` (generated/wiring churn following the new routes above, folded into their existing rows), `lib/admin-types.ts` (generated types), `lib/web/toolbar.ts` + `.unit.test.ts` (extended for the "Provision"→"Slack" relabel, already-inventoried row), `metrics/src/dashboard/__snapshots__/dashboard-page.unit.test.ts.snap` (snapshot churn, no independent source change), `scripts/minikube.ts`, `scripts/hitl.ts` + `hitl.unit.test.ts`/`.integration.test.ts` (extended — see new `hitl-log-file-tee-target.ts` row below), `.claude-plugin/marketplace.json`, `agent/package.json`, `metrics/package.json`, `package.json`, `plugins/shipwright/package.json`, `plugins/shipwright/.claude-plugin/plugin.json`, `bun.lock`, `version.txt` (version-sync churn), `charts/shipwright/{Chart.yaml,CHANGELOG.md,README.md,values.yaml,values.schema.json,templates/NOTES.txt,templates/admin-deployment.yaml,templates/admin-secret.yaml,tests/*.yaml}` (chart-version-bump + Okta/SMR chart wiring, already folded into the Okta/SMR rows above), `.github/workflows/ci.yml` (coverage-no-decrease job removal, folded into the deletion note above), `19df7334` docs-only pre-commit-checklist scrub, `6b558c52`/`aa20035a`/`4046276d` docs-sync-anchor commits (doc-only). One new file with no test: `scripts/hitl-log-file-tee-target.ts` — see new row + Notes (deliberate, documented exclusion). No files were deleted this cycle other than the never-decrease coverage cluster noted above. No new user journeys. `deploy_model` re-verified as `direct` directly against `CLAUDE.md` at this commit (`ea846283`) — unchanged, canary eligibility tagging remains skipped for the entire inventory.
- **2026-08-18 cycle's sampling strategy** (below, preserved for continuity): diffed `git diff --stat ca3c8c3f..origin/main` — 16 commits / 53 files. Excluding `CHANGELOG.md`, chart-version-bump (`charts/shipwright/Chart.yaml`, `values.yaml`, `charts/shipwright/CHANGELOG.md`, `charts/shipwright/examples/values-minikube.yaml`), and plugin/agent/metrics/task-store/lib/mcp-server/chat/site `package.json`/`.claude-plugin/marketplace.json`/`plugin.json`/`bun.lock`/`version.txt` version-sync churn (all CI-generated metadata with no source-code content, consistent with prior cycles), plus `planning/pr-findings-ledger/PLAN.md` (pre-implementation design doc, not shipped code) and the doc-only `docs/test-readiness/*.md`/`docs/configuration.md`/`docs/deploy-kubernetes.md`/`docs/task-store.md`/`state/docs-last-synced.json` churn, the real delta is **one new feature cluster (the PR findings ledger, PFL-1.1/PFL-2.1) plus 4 in-place fixes to already-inventoried rows**. Reviewed via diff read (full or targeted, per file):
  - **New feature: PR findings ledger (`task-store/prisma/schema.prisma`, `pull-request-service.ts`, `routes/prs.ts`, `openapi-schemas.ts`, PFL-1.1 PR #2688 + PFL-2.1 PR #2693)** — a new `PrFinding` model (id, `prRecordId` FK to `PullRequest`, `ref`, `disposition` enum {resolved, superseded, rejected}, `source` enum {review, patch}, `evidence`, `at`), deliberately modeled as a dedicated table with a plain-INSERT append (`PullRequestService.appendFinding`) rather than a JSON-blob read-modify-write on `PullRequest`, for race-safety under concurrent review/patch writers. New route `POST /prs/:id/findings` enforces server-side source/disposition authority: `source:"patch"` may only submit `disposition:"rejected"`; `source:"review"` may submit any disposition — a 400 otherwise. `PullRequest.list()`/`get()` now `include: { findings: true }`. This is a genuinely new functional surface (not a fix to an existing row) — added as a new sub-row under the existing `task-store/src/routes/{tasks,prs,tokens}.ts` / `task-service.ts`/`pull-request-service.ts`/`token-service.ts` composite row (schema + service + route all land in the same already-critical, already-`smoke`/`integration`-layered Task store row — no new category, but the row's rationale note is extended). New test files observed: `task-store/src/pr-finding.integration.test.ts` (234 lines, new), `pull-request-service.unit.test.ts` (+112), `pull-request-service.integration.test.ts` (+107), `prs.smoke.test.ts` (+277), `routes/prs.openapi.smoke.test.ts` (+4) — existence-only signal, consistent with the row already being canonically integration (service) + smoke (route).
  - **`task-store/src/routes/tasks.ts`, `task-service.ts` (`?hitl=true/false` filter, HTF-1.1, #0c2fc620)** — extends the same session/repo/org/source/claimedBy/pr/branch/assignee filter set (already covered by the ATB-1.2/TRF-1.1 rows from prior cycles) to also forward a `hitl` boolean filter to both `listReady()` and `listBlocked()`. Correctness/coverage extension to the already-inventoried, already-critical `task-service.ts`/`ready.ts`-family row — no new category or layer. New `task-store/src/state-filter.smoke.test.ts` (117 lines, new file) observed — existence-only signal; still lands within the same already-`smoke`-layered `routes/tasks.ts` row (a new test file, not a new source row).
  - **`admin/src/agent-manifest.ts` (ephemeral-storage 1Gi → 4Gi, #1ef11f4f)** — raises the agent pod's `AGENT_CONTAINER_RESOURCES` ephemeral-storage request/limit from 1Gi to 4Gi; the 1Gi Autopilot default left no room for a build step's scratch space and agents were evicted mid-run. Correctness fix (resource-limit constant change) to the already-inventoried, already-critical `admin/src/agent-manifest.ts` row (pure wire-object builder) — no new category or layer.
  - **`agent/src/setup.ts` (`runMiseStartup` pins GOPATH/GOMODCACHE/npm/bun caches to the PVC, companion to the above, #1ef11f4f)** — three package managers (Go, npm, Bun) ignore `XDG_CACHE_HOME` and default to paths under the container's ephemeral overlay; `GOMODCACHE` alone can pull ~1.7GiB for a Terratest module. Pins all three to the PVC-backed home dir. Correctness fix to the already-inventoried, already-high `agent/src/setup.ts` row (workspace bootstrap) — no new category or layer. New `agent/src/setup.integration.test.ts` assertions (+38 lines) observed alongside — existence-only signal.
  - **`scripts/minikube.ts` (dev-login URL + auto-open, #333212ad)** — adds `buildAccessUrls().devLogin` (the `/admin/dev-login` dead-end fix — `/` redirects to a Google-sign-in page this profile never configures), `buildOpenCommand()` (best-effort OS-native URL opener, darwin/linux/null), and `hostsEntryPresent()` (whole-field, non-commented `/etc/hosts` line match) plus a `printNextSteps()` rewrite to surface only the one link that works. All are pure builders extending the already-inventoried `scripts/minikube.ts` row's existing split classification (unit builders / integration driver) — no new category or layer; the row remains ranked "low-priority / out of scope (dev-only tooling, not shipped)". `scripts/minikube.unit.test.ts` extended (+158/-25 lines) — existence-only signal.
  - Also reviewed, no inventory impact: `docs/configuration.md`, `docs/deploy-kubernetes.md`, `docs/task-store.md` (doc-only), `state/docs-last-synced.json` (sync anchor bump), `planning/pr-findings-ledger/PLAN.md` (pre-implementation design doc, not shipped code), `docs/test-readiness/test-inventory.md`, `test-migration.md`, `test-readiness-plan.md`, `test-system.md` (this pipeline's own artifacts, regenerated by phases 2–5, not a source-code delta), `plugins/shipwright/commands/patch.md` + `patch.content.test.ts` (content-only, already-inventoried `content` category row), `.claude-plugin/marketplace.json`, `agent/package.json`, `metrics/package.json`, `package.json`, `plugins/shipwright/package.json`, `version.txt` (version-sync churn), `admin/src/agent-manifest.unit.test.ts` (extended, no independent source change beyond the ephemeral-storage constant above), `task-store/src/generate-spec.ts`, `task-store/src/app.ts`, `task-store/src/index.ts` (openapi/route-registration wiring for the new `/prs/:id/findings` route — no independent classification, folded into the PR-findings-ledger row above). No files were deleted this cycle. No new user journeys. `deploy_model` re-verified as `direct` directly against `CLAUDE.md` at this commit (`f901803d`) — unchanged, canary eligibility tagging remains skipped for the entire inventory.
- **2026-08-16 cycle's sampling strategy** (below, preserved for continuity):
  - **`plugins/shipwright/skills/test-inventory/SKILL.md` (PR #2647, "add feature-grouping step to test-inventory")** — adds a new **Step 3b — group into features** and a `{{FEATURES_ROWS}}`/`{{FEATURE_COVERAGE_PCT}}` template requirement (companion change: `assets/templates/test-inventory.md.tmpl` gained the `## Features` section). This landed *after* the 2026-08-14 cycle's artifact was generated, so the artifact never picked it up — the prior cycle's inventory was missing the Features section entirely. This cycle adds it for the first time (see new `## Features` section below). Process/template change, not a source-code row.
  - **New file cluster: dual coverage gates (`scripts/check-coverage-gate.ts`, `scripts/check-coverage-no-decrease.ts`, `scripts/coverage-tool-dispatch.ts`, PRs #2648/#2652/#2653/#2656/#2658/#2660)** — `check-coverage.ts` (already-inventoried row) is refactored to export a `CoverageParser` interface plus four concrete parsers (`LcovParser`, `JacocoParser`, `IstanbulParser`/c8-nyc, `CoveragePyParser`, `GoCoverParser`); `coverage-tool-dispatch.ts` (new, pure) selects the right parser per repo from a `**Coverage toolchain:**` marker recorded in `test-system.md`; `check-coverage-gate.ts` (new, pure) computes the six-field `coverage_gate` block (feature + line coverage, sources, verdict, targets) with a self-consistency guard that throws on a caller-supplied verdict that doesn't match the recomputed one; `check-coverage-no-decrease.ts` (new) fails CI if aggregate line coverage regressed vs. the PR's base branch, by checking out the base ref into an isolated temp git worktree and re-running coverage there. New rows for the three new files; `check-coverage.ts`'s existing row's rationale note extended (parser now pluggable, not lcov-only).
  - **`scripts/check-config-docs.ts` (companion to the coverage-gate work, no PR number captured in this diff window)** — `collectTsFiles` (recursive `*.ts` walker, previously module-private) is exported so it can be unit-tested directly against a real scratch directory tree. No behavior change, no new row — existing row's canonical layer (unit, pure parsing) still holds since the newly-exported function is itself a real filesystem walk already implicit in the file's classification.
  - **`scripts/hitl.ts` bootstrap split (T-080, PR #4a25c890 / #2644)** — per the newly-populated `.claude/shipwright/test-readiness-decisions.md` entry "scripts/hitl.ts bootstrap sequence — planner/executor split, unit layer not integration": `hitl.ts` (631 lines, +446/-185 vs. the 2026-08-14 baseline) is split into pure planner functions (`buildHitlConfig`, `buildPreflightSteps`, `buildServiceSpecs`, `runSteps`) plus a thin injected executor, mirroring the existing `scripts/dev-tmux.ts` pure-builder pattern. Covered by new `scripts/hitl.bootstrap.unit.test.ts` (unit layer) rather than the task's originally-prescribed `scripts/hitl.integration.test.ts` (integration layer) — a deliberate, human-reviewed HITL decision, not a classifier default. This inventory updates the existing `scripts/hitl.ts` row's canonical layer from integration to **unit (planners) / integration (thin executor + `hitl.integration.test.ts`, which does exist per the diff and covers the executor's real spawn/poll behavior)** — a split classification, annotated per the registry entry. See Notes and the updated Ambiguous item.
  - **`agent/src/check-review.ts` fix-density Ambiguous item — addressed.** T-082 (PR #29379c7f / #2641) adds 3 new test cases to `check-review.unit.test.ts` that stack 3+ of the exclusion conditions the prior cycle's 5-commit fix run touched (stale `commitSha`/`reviewedCommitSha` divergence, review-thread-reply freshness, task-blocked state) in single fixtures, proving the documented precedence order holds under combination — not just individually. This directly answers the 2026-08-14 cycle's open question ("does existing coverage exercise the exclusion checks in combination, not just individually") with "yes, now it does." **Removing this Ambiguous item** — it's resolved, not carried forward. No row/layer change to `check-review.ts` itself (still category 2, integration, critical).
  - **`.github/workflows/ci.yml` (+53 lines)** — CI pipeline wiring for the new `check-coverage-gate.ts`/`check-coverage-no-decrease.ts` scripts (invoked as part of `task ci`'s coverage-gate step). Declarative CI config, not a code unit — consistent with the existing exclusion of workflow YAML from per-file inventory rows (the `.sh`/`.yml` chart-drift cluster is the sole exception, already flagged in Ambiguous for its own reasons).
  - **`plugins/shipwright/commands/review.md`, `plugins/shipwright/skills/repo-config/SKILL.md`, `plugins/shipwright/skills/test-design/SKILL.md`, `plugins/shipwright/skills/test-migration/SKILL.md`, `plugins/shipwright/skills/test-fix/SKILL.md`, `plugins/shipwright/skills/test-roadmap/SKILL.md`, `plugins/shipwright/TESTING.md`** — content-only extensions (each gained a paired `.content.test.ts`), wiring the dual-coverage-gate feature and the feature-grouping step through the rest of the pipeline's prompts. Already-inventoried `content` category rows, no new row.
  - Also reviewed, no inventory impact: `docs/agent.md`, `docs/testing.md`, `CLAUDE.md` (doc-only), `state/docs-last-synced.json` (sync anchor bump), `planning/dual-coverage-gates/PLAN.md`, `planning/patch-comment-findings/PLAN.md` (pre-implementation design docs, not shipped code), `.claude/shipwright/test-readiness-decisions.md` (+40 lines — first two entries populated; consumed narratively above and by the `hitl.ts` row's split classification, though the skill's own automatic consumption step remains "not yet implemented" per the registry file's own header — this cycle applied it by manual read, same as Step 0 prescribes as the interim path), `.claude-plugin/marketplace.json`, `agent/package.json`, `metrics/package.json`, `package.json`, `plugins/shipwright/package.json`, `bun.lock`, `version.txt` (version-sync churn), `agent/src/check-deploy.unit.test.ts`, `agent/src/check-patch.unit.test.ts`, `agent/src/check-review.unit.test.ts` (existence-only signal beyond T-082 above — the check-deploy/check-patch additions are further coverage extensions to already-inventoried, already-critical rows, no new row), `metrics/src/lib/accounts-client.integration.test.ts` (extended, no source change). No files were deleted this cycle (`scripts/hitl.ts`'s internal restructure is not a deletion — the file remains, its exports changed). No new user journeys. One Ambiguous item resolved (`check-review.ts` fix density); one Ambiguous item updated (`scripts/hitl.ts`, now resolved via the decisions registry rather than open). `deploy_model` re-verified as `direct` directly against `CLAUDE.md` at this commit (`ca3c8c3f`) — unchanged, canary eligibility tagging remains skipped for the entire inventory.
- **2026-08-14 cycle's sampling strategy** (below, preserved for continuity):
  - **New file: `admin/src/blocked-by-type-parity.check.ts`** (+ `admin/tsconfig.type-parity.json`, PR #2565) — a compile-time-only exhaustiveness check (`tsc --noEmit` via a dedicated `tsconfig.type-parity.json` project) proving admin's hand-mirrored `BlockedByEntry` union (in `admin-ui-pages.ts`, kept deliberately decoupled from `@shipwright/task-store`) stays a permissive superset of the real `BlockedByEntry` union in `task-store/src/blocked-by.ts`. Zero runtime behavior, never imported by application code, no `bun test` execution at all — a genuinely new "compile-time parity check" pattern not covered by the five-plus-shell layer taxonomy. New row, flagged in Ambiguous (a build-time TS-compiler assertion, neither unit nor content nor shell).
  - **New file: `plugins/shipwright/scripts/diagnose-review-candidacy.ts`** (+ `diagnose-review-candidacy.unit.test.ts`, PR #2600) — an on-demand CLI diagnostic: given `org/repo#123`, fetches live GitHub PR/review state + the task-store PR record and runs it through `check-review.ts`'s exported `traceReviewCandidacyDecision()` (RCO-1.3) to print exactly which exclusion check would reject the PR right now. Deliberately imports the trace function directly from `agent/src/check-review.ts` (a documented, narrow exception to the "plugins/shipwright is repo-agnostic, duplicate rather than import agent/src" convention — `plugins/shipwright/tsconfig.json`'s `include` is extended with `../../agent/src` to satisfy TS's rootDir constraint). The imported `traceReviewCandidacyDecision` call is service-boundary (live GitHub + task-store fetches); the script itself is a thin CLI wrapper. New row, category 2 (service-boundary, live-fetch CLI), canonical layer integration (recorded-fixture) — replaces what a stuck-PR incident (referenced in the file's own header comment) previously required multi-hour historical-log reconstruction to diagnose.
  - **`agent/src/check-review.ts` (RCO-1.3, RCO-1.4 precursor + #2613, #cb198d42, #931a8de5, #e1b0e897, #846b5156)** — several correctness fixes land on `getReviewCandidates()`/`traceReviewCandidacyDecision()` this cycle: (a) exclude review candidates with *any* review at head commit, not just terminal ones (#911ef03e); (b) key the terminal-skip check on `reviewedCommitSha` instead of `commitSha` (#cb198d42); (c) add skip-reason tracing + structured logging (#931a8de5); (d) treat review-thread replies as a fresh-author-reply signal in dedup (#e1b0e897); (e) don't re-gate an already-reviewed PR on the author allowlist (#846b5156). All are correctness fixes to an already-inventoried, already-critical service-boundary row (`check-review.ts` is part of the `check-*.ts` native-candidate-provider cluster) — no new category or layer, but a meaningful density of bug-fixes to this cycle's window (5 of the 87 commits target this one file), consistent with the file's `critical` ranking.
  - **`agent/src/check-patch.ts` (#1314fa02, #83692479)** — stale-cancelled-CI detection (rerun before escalating to CI-fix) and a structured skip-reason tag on patch.md's Step 3d no-op-at-dispatch bail. Correctness/observability fixes to the same already-inventoried, already-critical row DRO-1.2 touched last cycle — no new category or layer.
  - **`agent/src/loop-orchestrator.ts` (#b26e80a1)** — adds a `task_id`/`pr_number` Sentry tag to `dispatch()` for cross-referencing dispatch failures back to their originating task/PR in Sentry. Observability-only addition to an already-inventoried, already-critical row — no new category or layer.
  - **`agent/src/pr-state-reconciler.ts` (#2d46d50d)** — adds per-pass summary logging (RCO-1.5) to the reconciler's heal-pass loop. Observability-only, no behavior change — no new category or layer.
  - **`agent/src/worktree-reaper.ts` (#c66450b7)** — fixes `resolveOwningScopedRepo`'s longest-prefix match to normalize `org/repo`-formatted scoped-repo entries (via the shared `splitOrgRepo()` helper) to their bare repo name before comparing against a worktree dirname, closing a gap where a scoped-repos list containing `"org/repo"` strings (the production format per `agent-repos-ref.ts`) would never match any worktree dirname (which uses the bare-name convention). Correctness fix to an already-inventoried, already-critical row — no new category or layer.
  - **`task-store/src/task-service.ts`, `task-store/src/routes/tasks.ts` (#1eb54c1b, #fc8afab5, #3646c029, #da3a98b2)** — `listReady()`/`listBlocked()` gain full session/repo/org/source/claimedBy/pr/branch/assignee filter support (previously only some filters were honored), a shared post-filter helper is extracted (`ATB-1.3`) to keep the two list methods' filter logic from drifting, and a fallback to own-assignee matching is added for agent tokens with no `repos` scope configured. All are correctness/coverage fixes to the already-inventoried, already-critical `task-service.ts`/`ready.ts`-family row (filter-honoring gaps, not new functional surface) — no new category or layer. New `task-store/src/tasks.integration.test.ts` file (226 lines) and substantially extended `task-service.unit.test.ts` (+905/-lines, including #2560's `claim()` concurrent-race integration test, T-081) observed alongside — existence-only signal.
  - **`task-store/prisma/schema.prisma` + migration `20260811000000_drop_requires_human_approval`** — drops the `requiresHumanApproval` column from `Task`, companion to `#4334e481` (`deploy.md`'s `requiresHumanApproval` merge-gate removal, a content-only command change). Schema-level removal of a field this inventory never gave its own row (it was part of the `task-service.ts`/`statuses.ts` composite row) — no row change, noted here for completeness since it's a destructive schema migration.
  - **`admin/src/agents-api.ts`, `admin/src/agent-cron-runs.ts` (#66ead786)** — adds server-side `itemId`/`phaseId` filters to the cron-runs API. Coverage addition to an already-inventoried, already-critical/high row (`admin/src/agents-api.ts` critical, `agent-cron-runs.ts` high per the Prisma-backed-services row) — no new category or layer.
  - **`admin/src/admin-ui-pages.ts` (#69a27fd3)** — fixes a blocked-task badge/detail render crash (the proximate bug `blocked-by-type-parity.check.ts` above now guards against structurally). Correctness fix to an already-inventoried, already-high row — no new category or layer.
  - **`lib/sentry.ts`** — touched incidentally by the Sentry-tag addition above; no `lib/sentry.ts`-specific behavior change beyond what's already captured in the `loop-orchestrator.ts` row.
  - **`site/src/pages/compare.astro`, `site/src/pages/self-hosted.astro`, `site/src/pages/vs/factory.astro` (new) (#a164358c, #7c65df1e, #vfc-1)** — add "Factory" to the self-hosted architecture comparison table and the landscape table, and ship a new dedicated `/vs/factory` comparison page (Factory architectural parity, licensing, enterprise-security tradeoffs, choose-when guidance). Content/data changes to the already-inventoried `site/src/pages/*.astro` user-journey row (marketing site, journey #4) — no new category or layer; `site/tests/compare.spec.ts`/`self-hosted.spec.ts` extended alongside, and new `site/tests/vs-factory.spec.ts` (existence-only signal).
  - **`.github/workflows/auto-bump-chart.yml`, `.github/workflows/lib/` (implied by `test-auto-bump-chart.sh` diff), (#916c583a)** — retries required-check failures before giving up on chart-bump auto-merge. Correctness fix to the already-inventoried `auto-bump-chart.yml` row (category 2, integration-equivalent) — no new category or layer.
  - **`renovate.json` (new file, #49783367)** — Renovate bot config prioritizing vulnerability remediation. Declarative config, not a code unit — excluded from inventory per the same reasoning as other top-level config files (Helm chart, `demo/`).
  - **`charts/shipwright/templates/mcp-server-{deployment,service,serviceaccount}.yaml` (#3d1ddcf3)** — new Helm chart artifacts for the mcp-server workload (disabled by default). Declarative Helm templates — excluded from inventory per the existing `charts/shipwright/` Ambiguous-items exclusion (no code units to classify); their companion Helm unit tests (`charts/shipwright/tests/mcp_server_workload_test.yaml`) are chart-level `helm unittest` assertions, not `bun test` — same treatment as the rest of `charts/shipwright/tests/`.
  - **`scripts/minikube.ts` (#94ff2a9d)** — reliability fix for `task minikube:up` on Apple Silicon and behind the shared ingress rewrite-target. Correctness fix to the already-inventoried, already-out-of-scope-for-shipping `scripts/minikube.ts` row (dev-only tooling) — no new category or layer.
  - Also reviewed, no inventory impact: `plugins/shipwright/commands/{deploy,patch,plan-session,review}.md` (content-only rewrites/extensions, each already anchored by a corresponding code-layer row above or in the prior cycle — `deploy.md`'s `requiresHumanApproval` gate removal is the content-layer twin of the Prisma migration above), `plugins/shipwright/agents/code-reviewer.md` (content extension, PVD-1.2 — surfaces prior CURRENT_USER review bodies to the subagent), `plugins/shipwright/skills/investigate-cron/SKILL.md`, `plugins/shipwright/skills/review-staged/SKILL.md` (both pre-existing skills, extended — skill count still 27), `.claude/skills/verify/SKILL.md` (new project-level skill under `.claude/`, not `plugins/shipwright/` — out of this inventory's scope, which covers the shipped plugin + services, not the reference-clone's own dev tooling), `docs/agent-api.md`, `docs/agent.md`, `docs/configuration.md`, `docs/deploy-kubernetes.md`, `docs/helm-repo.md`, `docs/migration.md`, `docs/observability.md`, `docs/task-store.md` (doc-only, no inventory impact), `admin/tsconfig.json`, `plugins/shipwright/tsconfig.json` (config changes supporting the two new-file rows above, no independent classification), `task-store/openapi.json`, `admin/src/openapi-schemas.ts`, `task-store/src/openapi-schemas.ts` (generated/schema-definition churn following the route filter changes above, folded into their existing schema-definition rows), `lib/task-store-types.ts` (generated OpenAPI types — already excluded). No files were deleted this cycle. No new user journeys. Two new ambiguous items (`blocked-by-type-parity.check.ts`'s compile-time-only check pattern; noted above). `deploy_model` re-verified as `direct` directly against `CLAUDE.md` at this commit (`297fb61e`) — unchanged, canary eligibility tagging remains skipped for the entire inventory.
- **Prior cycles' sampling strategy** (2026-07-21 through 2026-08-11) is preserved verbatim in this file's git history; not restated here to keep this document from growing unboundedly. See the Notes section's per-cycle delta log for a condensed summary of each prior cycle's changes.

## Summary

| Code area | Category | Canonical layer | Criticality | Canary-eligible |
|---|---|---|---|---|
| `lib/pricing.ts` (`calculateCost`, `normalizeModelToRateKey`) | 1. Pure business logic | unit | high | n/a |
| `lib/org-repo.ts` (`isOrgRepo`) | 1. Pure business logic | unit | medium | n/a |
| `lib/request-context.ts` (`callerLabel`) | 1. Pure business logic | unit | medium | n/a |
| `lib/secret-env-vars.ts` (constant list) | 1. Pure business logic | unit | high | n/a |
| `lib/claim-ttl.ts` (claim TTL constants, `DEFAULT_CLAIM_TTL_MS`) | 1. Pure business logic | unit | critical | n/a |
| `lib/github-login.ts` (`isGithubLogin`) | 1. Pure business logic | unit | medium | n/a |
| `lib/http.ts` (`readJson` — new shared home for a previously triplicated request-body parser, new) | 1. Pure business logic | unit | medium | n/a |
| `lib/sentry.ts` (scrubbing config) | 1. Pure business logic (scrub rules) / 5. external integration (Sentry SDK init) | unit (scrub rules) / integration (init wiring) | high | n/a |
| `lib/web/toolbar.ts` (shared HTML render) | 1. Pure business logic | unit | medium | n/a |
| `lib/progress-phases.ts` (closed 11-phase `ProgressPhase` enum + human-readable labels, CFB-2.1, new) | 1. Pure business logic | unit | high | n/a |
| `lib/admin-types.ts`, `lib/task-store-types.ts` (generated OpenAPI types) | (generated code — excluded) | — | — | n/a |
| `agent/src/fixtures/stream-json/*.ts` (recorded `claude -p` stream-JSON transcripts) | (test fixture data — excluded) | — | — | n/a |
| `metrics/src/formatters.ts` (date range + envelope formatting) | 1. Pure business logic | unit | high | n/a |
| `metrics/src/select-provider.ts` (backend mode selector) | 1. Pure business logic | unit | high | n/a |
| `metrics/src/metrics-provider.ts` (query interface + date range types) | 1. Pure business logic | unit | high | n/a |
| `metrics/src/schemas.ts`, `types.ts` (Zod schemas / types) | 1. Pure business logic | unit | medium | n/a |
| `metrics/src/lib/clock.ts`, `env.ts` | 1. Pure business logic | unit | medium | n/a |
| `metrics/src/lib/api-auth.ts` (key parsing) | 1. Pure business logic | unit | high | n/a |
| `metrics/src/lib/api-schemas.ts`, `api-utils.ts` | 1. Pure business logic | unit | medium | n/a |
| `metrics/src/lib/web/toolbar.ts` (re-export shim of `lib/web/toolbar.ts`) | 1. Pure business logic (re-export) | unit | low | n/a |
| `metrics/src/lib/errors.ts` (typed HTTP error classes) | 4. Error / failure-path (logic-only) | unit | high | n/a |
| `metrics/src/lib/coalescing-cache.ts` (single-flight + short-TTL cache, injected Clock) | 1. Pure business logic (deterministic given injected Clock) | unit | medium | n/a |
| `metrics/src/lib/session-middleware.ts` (cookie/JWT verify) | 2. Service-boundary code (JWT crypto + redirect) | integration | critical | n/a |
| `metrics/src/lib/task-store-client.ts` (`HttpTaskStoreClient`) | 2. Service-boundary code | integration (recorded fixture) | high | n/a |
| `metrics/src/lib/accounts-client.ts`, `admin-metrics-client.ts` | 2. Service-boundary code | integration (recorded fixture) | high | n/a |
| `metrics/src/lib/test-helpers.ts` | (test infra — excluded from inventory) | — | — | n/a |
| `metrics/src/providers/task-store-provider.ts` | 2. Service-boundary code (aggregation over client) | integration | critical | n/a |
| `metrics/src/providers/task-store-recorded.ts` (fixture-backed provider) | 2. Service-boundary code | integration | medium | n/a |
| `metrics/src/fixtures/task-store-fixtures.ts` (offline cassette-backed provider for `METRICS_OFFLINE`) | 2. Service-boundary code (composes recorded doubles) | integration | medium | n/a |
| `metrics/src/secrets.ts` (env-first, GCP Secret Manager fallback) | 2. Service-boundary code / 5. external integration | integration (recorded fixture) | high | n/a |
| `metrics/src/api.ts` (`/metrics/*` Hono sub-app, 5 endpoints) | 3. HTTP route | smoke | critical | n/a |
| `metrics/src/server.ts` (process entrypoint, mode wiring) | 3. HTTP route (entrypoint) | smoke | critical | n/a |
| `metrics/src/dashboard/dashboard-page.ts` (server-rendered HTML) | 3. HTTP route (rendered response body) | smoke | high | n/a |
| `agent/src/work-selector.ts` (`selectNextWorkItem`) | 1. Pure business logic | unit | critical | n/a |
| `agent/src/loop-cron-classifier.ts` | 1. Pure business logic | unit | high | n/a |
| `agent/src/format.ts`, `markers.ts`, `log-prefix.ts` | 1. Pure business logic | unit | medium | n/a |
| `agent/src/pricing.ts` (re-export of lib/pricing) | 1. Pure business logic (re-export) | unit | medium | n/a |
| `agent/src/clock.ts` | 1. Pure business logic | unit | medium | n/a |
| `agent/src/agent-repos-ref.ts` (mutable box for synced scoped-repos list) | 1. Pure business logic | unit | medium | n/a |
| `agent/src/loop-jobs-ref.ts` (mutable box for live cron-job enabled flags) | 1. Pure business logic | unit | high | n/a |
| `agent/src/agent-author-allowlist-ref.ts` (mutable box for synced author-allowlist) | 1. Pure business logic | unit | high | n/a |
| `agent/src/agent-slack-membership-ref.ts` (`AgentSlackMembershipRef` — mutable box for synced Slack membership-restriction config `{restrict, emails}`; `hasSynced()` distinguishes "never synced" from "synced unrestricted", SMR, new) | 1. Pure business logic | unit | critical | n/a |
| `agent/src/piper-voice.ts` (voice-name validation + `.onnx` path resolver) | 1. Pure business logic (injectable readdir) | unit | medium | n/a |
| `agent/src/browser.ts` (`launchBrowser` — headless Chromium via Playwright, sandbox disabled for restricted pod securityContext) | 2. Service-boundary code (spawns/drives a real browser process) | integration | medium | n/a |
| `agent/scripts/verify-browser-launch.ts` (CI-only: proves Chromium launches under the built image's restricted securityContext) | 4. Error/failure-path (environment-capability gate, not exercised by normal `bun test`) | integration (CI-only shell-out check, no `bun test` suffix — see Ambiguous) | medium | n/a |
| `agent/src/loop-orchestrator.ts` (`createLoopOrchestrator`; redispatch-cooldown key now phase-scoped `${pr.id}:${phase}` instead of PR-ID-only, PRC-1.1, new) | 2. Service-boundary code (orchestrates claim/dispatch across task-store + GitHub) | integration | critical | n/a |
| `agent/src/check-deploy.ts`, `check-dev-task.ts` (native candidate providers) | 2. Service-boundary code | integration | critical | n/a |
| `agent/src/check-patch.ts` (native candidate provider; new `isSupersededBySelfReview` predicate wired into `plugins/shipwright/scripts/compute-unaddressed-findings.ts`'s `hasUnaddressedFindings` exclusion — a prior self-review's finding no longer counts as unaddressed once a later, genuinely clean self-review supersedes it, DRO-1.2) | 2. Service-boundary code | integration | critical | n/a |
| `agent/src/check-review.ts` (native candidate provider; staged-review guard keys on `reviewedCommitSha`) | 2. Service-boundary code | integration | critical | n/a |
| `agent/src/check-helpers.ts` (shared GitHub/task-store helpers, `isCleanApproveBody`) | 1. Pure business logic (parsing helpers) + 2. service-boundary (`ghJson` etc.) | unit (pure parts) / integration (I/O parts) | critical | n/a |
| `agent/src/pr-state-reconciler.ts` (self-heal stale PR state; orphan-task reconciliation pass — `listOrphanCandidateTasks`, `ghListOpenPrsForBranch`, `reconcileOrphanedTask(s)` — removed this cycle as a scoped simplification; retains `reconcilePrOpenTask`'s direct + branch-fallback heal paths) | 2. Service-boundary code | integration | critical | n/a |
| `agent/src/crypto.ts` (AES-256-GCM) | 1. Pure business logic (deterministic given key/IV) | unit | critical | n/a |
| `agent/src/github-app-auth.ts` (installation token cache/refresh) | 5. External integration | integration (recorded fixture) | critical | n/a |
| `agent/src/claude.ts` (`createRunClaude` — spawns `claude -p`, captures/surfaces a `session_id`; `ProgressCallback` widened with an optional `ProgressPhase` arg, CFB-2.1) | 2. Service-boundary code | integration | critical | n/a |
| `agent/src/progress-milestones.ts` (`phaseForBlock` — maps an assistant stream-json content block's `type`/tool `name` to a generic `ProgressPhase`; `block.input` deliberately never read, load-bearing anti-leak invariant, CFB-2.1, new) | 1. Pure business logic | unit | high | n/a |
| `agent/src/entrypoint.ts`, `entrypoint-main.ts` (container startup sequence) | 2. Service-boundary code | integration | critical | n/a |
| `agent/src/setup.ts`, `setup-github-auth.ts` (workspace bootstrap) | 2. Service-boundary code | integration | high | n/a |
| `agent/src/slack.ts` (Bolt event handlers; **new this cycle:** `isAllowedSlackSender()`/`shouldRejectSlackSender()` — SMR enforcement gate called at the top of `app.message()`/`app.event("app_mention")`/`app.event("reaction_added")`, fails open when unsynced or `restrict:false`) | 3. HTTP route (event-driven, not URL-routed, but owns the wire contract with Slack) | smoke (mocked Bolt app) | critical | n/a |
| `agent/src/voice.ts` (Whisper/ElevenLabs wrappers) | 5. External integration | integration (recorded fixture) | medium | n/a |
| `agent/src/chat-poller.ts` (`createChatPoller` — poll chat-svc → Claude → reply) | 2. Service-boundary code | integration | high | n/a |
| `agent/src/startup-dm.ts` | 2. Service-boundary code | integration | medium | n/a |
| `agent/src/cron-handler.ts` (`handleCronRequest`) | 3. HTTP route (thin — orchestrates Claude run + Slack post) | smoke | critical | n/a |
| `agent/src/health.ts` (`/health` Hono route) | 3. HTTP route | smoke | high | n/a |
| `agent/src/cron-run-reporter.ts`, `chat-token-reporter.ts`, `work-queue-reporter.ts` | 2. Service-boundary code | integration | medium | n/a |
| `agent/src/cron-failure-reporter.ts` (turns thrown cron-tick errors into an AgentCronRun row + Sentry capture) | 4. Error/failure-path (involving real boundary) | integration | high | n/a |
| `agent/src/analytics.ts` (file-backed JSON event store) | 2. Service-boundary code | integration | medium | n/a |
| `agent/src/cutover-validate.ts` | 4. Error/failure-path (validation gate) | unit | medium | n/a |
| `agent/src/http-chat-service-client.ts`, `shipwright-config-client.ts`, `shipwright-runtime-client.ts` | 2. Service-boundary code | integration (recorded fixture) | high | n/a |
| `agent/src/sessions.ts`, `users.ts` (user display name and email resolver with unified cache, SMR-2.2) | 2. Service-boundary code | integration | medium | n/a |
| `agent/src/config.ts`, `cli-args.ts`, `test-env.ts` | 1. Pure business logic | unit | medium | n/a |
| `agent/src/index.ts` (composition entrypoint) | 2. Service-boundary code (composition + sync wiring) | integration | critical | n/a |
| `admin/src/agents-api.ts` (CRUD API for Agent/Env/Cron/Tool/Token/Plugin/Member) | 3. HTTP route | smoke | critical | n/a |
| `admin/src/api.ts` (agent runtime API: config/crons) | 3. HTTP route | smoke | critical | n/a |
| `admin/src/server.ts` (Hono entrypoint — mounts UI + CRUD API + runtime API) | 3. HTTP route (entrypoint, composition-only) | smoke | critical | n/a |
| `admin/src/admin-ui.ts`, `admin-ui-pages.ts`, `admin-ui-styles.ts` (server-rendered UI) | 3. HTTP route (server-rendered UI) | smoke | high | n/a |
| `admin/src/agent-provisioner.ts` (composes token+manifest+k8s client) | 2. Service-boundary code | integration | critical | n/a |
| `admin/src/kubernetes-client.ts` (`HttpKubernetesClient`) | 5. External integration (in-cluster K8s API) | integration (recorded fixture) | critical | n/a |
| `admin/src/agent-manifest.ts` (pure Deployment/Secret wire-object builders) | 1. Pure business logic | unit | critical | n/a |
| `admin/src/admin-ui-pages.ts`'s `renderRepoOrgFilterFields`, `toFilterArray` (pure HTML-fragment builders for the Tasks/PRs org/repo multiselect) | 1. Pure business logic (pure string-in/HTML-out given filters+suggestions) | unit | medium | n/a |
| `admin/src/agent-deletion.ts`, `agent-deletion-checklist.ts` | 2. Service-boundary code | integration | high | n/a |
| `admin/src/agent-tokens.ts`, `token-crypto.ts`, `chat-markers.ts` | 1. Pure business logic (hash/encrypt given key) | unit | critical | n/a |
| `admin/src/agent-envs.ts`, `agent-cron-jobs.ts`, `agent-cron-runs.ts`, `agent-cron-run-stats.ts`, `agent-tools.ts`, `agent-plugins.ts`, `agent-members.ts` (Prisma-backed services) | 2. Service-boundary code | integration | high | n/a |
| `admin/src/agents.ts` (`AgentService`) | 2. Service-boundary code | integration | high | n/a |
| `admin/src/agent-chat-tokens.ts` (`AgentChatTokenService` — daily rollup, atomic upsert) | 2. Service-boundary code | integration | high | n/a |
| `admin/src/agent-work-queue.ts` (`AgentWorkQueueService` — latest work-queue snapshot per agent) | 2. Service-boundary code | integration | medium | n/a |
| `admin/src/clock.ts` | 1. Pure business logic | unit | medium | n/a |
| `admin/src/api-auth.ts` (session-or-bearer combined auth) | 2. Service-boundary code (JWT verify) | integration | critical | n/a |
| `admin/src/dev-auth-guard.ts` (pure predicate over injected env) | 1. Pure business logic | unit | critical | n/a |
| `admin/src/crypto.ts` | 1. Pure business logic | unit | critical | n/a |
| `admin/src/google-auth-client.ts` | 5. External integration | integration (recorded fixture) | critical | n/a |
| `admin/src/okta-auth-client.ts` (`OktaAuthClient`/`HttpOktaAuthClient` — OIDC authorize/token-exchange/userinfo, endpoints derived from a caller-supplied issuer URL, no live `.well-known` discovery call, new) | 5. External integration | integration (recorded fixture) | critical | n/a |
| `admin/src/github-app-provisioning-client.ts` (`GithubAppProvisioningClient`/`HttpGithubAppProvisioningClient` — one-time GitHub App manifest-flow credential exchange; `webhook_secret` deliberately discarded, never stored, new) | 5. External integration | integration (recorded fixture) | critical | n/a |
| `admin/src/slack-provisioning-client.ts`, `chat-service-provisioning-client.ts`, `task-store-provisioning-client.ts`, `http-chat-client.ts` | 5. External integration / 2. service-boundary (internal HTTP) | integration (recorded fixture) | high | n/a |
| `admin/src/attachment-validation.ts` (pure size/MIME checks) | 1. Pure business logic | unit | medium | n/a |
| `admin/src/openapi-schemas.ts` | 1. Pure business logic (schema defs) | unit | medium | n/a |
| `agent-types/coding/manifest.yaml` + `admin/src/agent-type-manifest-loader.ts` (registry/fallback, `listTypes()`, `tryGetManifest()`) | 1. Pure business logic (data + resolution) | unit | medium | n/a |
| `admin/src/errors.ts` | 4. Error/failure-path (logic-only) | unit | high | n/a |
| `task-store/src/routes/tasks.ts`, `prs.ts`, `tokens.ts` (both `tasks.ts`/`prs.ts` now import the shared `readJson` from `@shipwright/lib/http`; `prs.ts`'s `/skip/reset` route doc updated for the new auto-unblock behavior — no shape change; `prs.ts` gains `POST /:id/findings` — appends a `PrFinding` row with server-side `source`/`disposition` authority enforcement; `tasks.ts` gains a `?hitl=true/false` filter forwarded to both `listReady()`/`listBlocked()`, HTF-1.1; **new this cycle: `prs.ts` gains `GET /:id/events`** — read-only surface over the new `PullRequestEvent` audit trail, PSA-1.2) | 3. HTTP route | smoke | critical | n/a |
| `task-store/src/task-service.ts`, `pull-request-service.ts`, `token-service.ts` (Prisma-backed services; `task-service.ts`'s `claim()` now also gates its UPDATE on `claimedBy IS NULL`; `pull-request-service.ts`'s `claim()` re-validates its conflict conditions inside the UPDATE's own WHERE clause instead of a pre-update JS check, closing a lost-update race — CRF-1.1; `resetSkip()` now also clears `blocked`/`blockedReason` when the block was set by the skip-auto-block mechanism specifically; `pull-request-service.ts` gains `appendFinding()` — a race-safe plain-INSERT append backing the `PrFinding` model, PFL-1.1/PFL-2.1; `list()`/`get()` now `include: { findings: true }`; **new this cycle: `pull-request-service.ts`'s `recordTransition()` wraps `computePrTransitionDiff()` to persist `PullRequestEvent` rows inside the same transaction as each source update, PSA-1.2**; `nullable agentId` added to `PrFinding`, threaded through `patch.md`/`appendFinding()`) | 2. Service-boundary code | integration | critical | n/a |
| `task-store/src/pr-transition-diff.ts` (`computePrTransitionDiff` — pure before/after diff over an explicit `AUDITED_FIELDS` allowlist; `heartbeatAt` deliberately excluded from the audit trail, new) | 1. Pure business logic | unit | high | n/a |
| `task-store/src/lib/repo-org-filter.ts` (`buildRepoOrgWhere` — pure `{orgs?, repos?}` → Prisma-where-fragment builder) | 1. Pure business logic | unit | high | n/a |
| `task-store/src/ready.ts` (`resolveReadyTasks`; same-branch exclusivity guard + injectable `now` param) | 1. Pure business logic | unit | critical | n/a |
| `task-store/src/blocked-by.ts` (`computeBlockedBy`; two independent, accumulating gates — `{type:"hitl"}` and `{type:"blocked",reason}`) | 1. Pure business logic | unit | critical | n/a |
| `task-store/src/statuses.ts` (status lifecycle rules) | 1. Pure business logic | unit | critical | n/a |
| `task-store/src/stale-claim-reaper.ts` | 2. Service-boundary code | integration | critical | n/a |
| `task-store/src/claim-ttl-buffer-check.ts` (`checkClaimTtlBuffer` — startup TTL headroom sanity check) | 1. Pure business logic (opt-in, given injected env values) | unit | medium | n/a |
| `task-store/src/auth.ts` (bearer middleware) | 2. Service-boundary code | integration | critical | n/a |
| `task-store/src/validate.ts` (re-export) | 1. Pure business logic | unit | medium | n/a |
| `task-store/src/app.ts`, `main.ts`, `index.ts` (composition/entrypoint; **new this cycle: `GET /ready` now runs a real Prisma DB round-trip and returns 503 on failure, instead of a static 200**, so the Kubernetes readiness probe actually gates on DB connectivity) | 3. HTTP route (entrypoint) | smoke | critical | n/a |
| `task-store/src/generate-spec.ts`, `openapi-schemas.ts` (schema definitions) | 1. Pure business logic | unit | medium | n/a |
| `chat/src/routes/messages.ts`, `threads.ts`, `tokens.ts` (`messages.ts` now imports the shared `readJson` from `@shipwright/lib/http` instead of a local duplicate — no shape change) | 3. HTTP route | smoke | high | n/a |
| `chat/src/message-service.ts`, `thread-service.ts`, `token-service.ts` (Prisma-backed) | 2. Service-boundary code | integration | high | n/a |
| `chat/src/auth.ts` | 2. Service-boundary code | integration | critical | n/a |
| `chat/src/routes/utils.ts` | 1. Pure business logic | unit | medium | n/a |
| `chat/src/generate-spec.ts` (OpenAPI 3.1 doc generator, pure given stub deps) | 1. Pure business logic | unit | medium | n/a |
| `chat/src/test-fakes.ts` | (test infra — excluded from inventory) | — | — | n/a |
| `chat/src/app.ts`, `main.ts`, `index.ts` | 3. HTTP route (entrypoint) | smoke | high | n/a |
| `mcp-server/src/mcp-server.ts` (`tools/list`, `tools/call`) | 3. HTTP route (MCP wire protocol) | smoke | high | n/a |
| `mcp-server/src/tool-caller.ts` (proxy build + fetch) | 2. Service-boundary code | integration (recorded fixture) | high | n/a |
| `mcp-server/src/tool-allowlist.ts` (pure filter) | 1. Pure business logic | unit | high | n/a |
| `mcp-server/src/serve.ts` (stdio entrypoint) | 3. HTTP route (transport entrypoint) | smoke | medium | n/a |
| `mcp-server/src/index.ts` (Hono app factory — mounts auth + HTTP transport) | 3. HTTP route (entrypoint, composition-only) | smoke | high | n/a |
| `mcp-server/src/main.ts` (`Bun.serve` process entrypoint, fails closed with no token) | 3. HTTP route (entrypoint) | smoke | critical | n/a |
| `mcp-server/src/http-transport.ts` (`mountMcpHttpTransport` — Streamable HTTP, stateful sessions, idle-session reaper) | 2. Service-boundary code (session lifecycle over injected transport) | integration | high | n/a |
| `mcp-server/src/auth.ts` (inbound bearer middleware, constant-time compare) | 2. Service-boundary code (security-critical middleware) | integration | critical | n/a |
| `mcp-server/src/clock.ts` (injectable `Clock`, mirrors `task-store/src/clock.ts`) | 1. Pure business logic | unit | medium | n/a |
| `scripts/check-coverage.ts` (multi-format coverage parser — `CoverageParser` interface + `LcovParser`/`JacocoParser`/`IstanbulParser`/`CoveragePyParser`/`GoCoverParser` + threshold gate, was lcov-only; **line-coverage threshold raised 89 → 90 this cycle**, matching `CLAUDE.md`'s documented 90/89 lines/functions gate) | 1. Pure business logic (parsing) | unit | high | n/a |
| `scripts/coverage-tool-dispatch.ts` (`extractCoverageTool`/`selectParser` — per-repo coverage-tool selection from a `test-system.md` marker, new) | 1. Pure business logic | unit | medium | n/a |
| `scripts/check-coverage-gate.ts` (`computeCoverageGate` — six-field dual feature+line coverage_gate block with self-consistency guard, new) | 1. Pure business logic (with a throwing consistency guard) | unit | high | n/a |
| `scripts/check-config-docs.ts` (env-var doc-coverage checker; `collectTsFiles` now exported for direct unit testing) | 1. Pure business logic | unit | medium | n/a |
| `scripts/sync-version.ts` | 2. Service-boundary code (file writes) | integration | medium | n/a |
| `scripts/check-downstream-compat.sh` (renders a downstream wrapper chart's Shipwright dependency 3 ways — pinned, published gh-pages baseline, working tree — and diffs them; `helm:downstream-compat` Taskfile target, new) | 2. Service-boundary code (git/helm/filesystem orchestration) | smoke (`check-downstream-compat.smoke.test.ts` extracts and executes the real script logic against synthetic diff fixtures) | high | n/a |
| `scripts/dev.ts`, `dev-tmux.ts` (local dev supervisors) | 2. Service-boundary code (process spawning) | integration | low-priority / out of scope (dev-only tooling, not shipped) | n/a |
| `scripts/minikube.ts` (`task minikube:up`/`minikube:down` orchestration; pure builders + injected-exec driver) | 1. Pure business logic (command-list builders) + 2. service-boundary (injected-exec driver) | unit (builders) / integration (driver) | low-priority / out of scope (dev-only tooling, not shipped) | n/a |
| `scripts/wait-for-agent.ts` (poll loop for the first admin-created Agent row) | 2. Service-boundary code | integration | low-priority / out of scope (dev-only tooling) | n/a |
| `scripts/seed-dev-agent.ts` (standalone convenience seeder) | 2. Service-boundary code | integration | low-priority / out of scope (dev-only tooling) | n/a |
| `scripts/seed-*.ts` (other dev-only DB seeders) | 2. Service-boundary code | integration | medium (dev-only) | n/a |
| `scripts/generate-*-spec.ts`, `generate-mcp-*.ts` | 2. Service-boundary code (codegen, file I/O) | integration | medium | n/a |
| `scripts/hitl.ts` (HITL runner; split into pure planners — `buildHitlConfig`, `buildPreflightSteps`, `buildServiceSpecs`, `computeMissingClones` — plus a thin injected `runSteps` executor, per `test-readiness-decisions.md`) | 1. Pure business logic (planners) / 2. service-boundary (executor) | unit (`hitl.bootstrap.unit.test.ts`, per registry decision — not the originally-prescribed integration layer) / integration (`hitl.integration.test.ts`, executor's real spawn/poll) | high (gates local HITL dev-loop correctness) | n/a |
| `scripts/hitl-log-file-tee-target.ts` (real `LogFileTeeTarget` — patches `process.stdout.write`/`stderr.write`/`console.*`; split out of `hitl.ts` specifically so it can be excluded from the coverage gate without hiding `hitl.ts`'s own pure logic, new) | 2. Service-boundary code (global-state mutation, deliberately untestable in-process per this repo's isolation contract) | integration (exercised only via a subprocess fixture in `hitl.integration.test.ts` — no dedicated test file of its own, by design; see Notes) | medium (dev-only tooling wiring) | n/a |
| `plugins/shipwright/scripts/check-deploy.ts`, `check-review.ts`, `check-patch.ts`, `check-dev-task.ts` (cron prechecks) | 2. Service-boundary code | integration | critical | n/a |
| `plugins/shipwright/scripts/check-helpers.ts` (`isCleanApproveBody`, GitHub wrappers) | 1. Pure business logic (parsing) + 2. service-boundary (`ghJson`) | unit (pure parts) / integration (I/O parts) | critical | n/a |
| `plugins/shipwright/scripts/check-banned-strings.ts` (`scanForBannedStrings`) | 1. Pure business logic | unit | high | n/a |
| `plugins/shipwright/scripts/check-docs-freshness.ts`, `check-dependency-bot-triage.ts`, `check-error-patrol.ts`, `check-learn-dream.ts`, `check-review-patch.ts`, `check-test-readiness.ts` (cron prechecks, multi-repo) | 2. Service-boundary code | integration | high | n/a |
| `plugins/shipwright/scripts/check-consolidation-patrol.ts` (ledger-driven cron precheck) | 2. Service-boundary code | integration | medium | n/a |
| `plugins/shipwright/scripts/classify_test_layer.ts` (`classifyPath`) | 1. Pure business logic | unit | high | n/a |
| `plugins/shipwright/scripts/compute-review-verdict.ts` (`computeVerdict`/`validateReviewVerdict` — mechanizes review.md Step 10's verdict truth table, DRO-1.1) | 1. Pure business logic | unit | high | n/a |
| `plugins/shipwright/scripts/diagnose-review-candidacy.ts` (CLI wrapper around `check-review.ts`'s `traceReviewCandidacyDecision`, RCO-1.4, new) | 2. Service-boundary code (live GitHub + task-store fetch) | integration (recorded fixture) | medium | n/a |
| `admin/src/blocked-by-type-parity.check.ts` (compile-time-only `BlockedByEntry` union parity check, new) | (compile-time TS assertion — see Ambiguous) | build-time (not `bun test`) | high | n/a |
| `plugins/shipwright/scripts/ci-checks.ts` (`parseActionsChecks`) | 1. Pure business logic | unit | medium | n/a |
| `plugins/shipwright/scripts/clock.ts`, `test-helpers/doubles.ts` (`FixedClock`) | 1. Pure business logic | unit | medium | n/a |
| `plugins/shipwright/commands/*.md` (30 slash-command prompt files, up from 29 — new: `unblock.md`) | content (prompt/markdown) | content | high | n/a |
| `plugins/shipwright/agents/*.md` (code-reviewer, docs-refresher, learning-dreamer, researcher) | content (prompt/markdown) | content | high | n/a |
| `plugins/shipwright/skills/*/SKILL.md` (27 skill prompt files; `security-scan/SKILL.md` and `test-readiness/SKILL.md` both substantially extended this cycle — content-only) | content (prompt/markdown) | content | high | n/a |
| `plugins/shipwright/commands/references/escalation-pattern.md` (new — human-facing triage-decision pattern backing `unblock.md`) | content (prompt/markdown) | content | medium | n/a |
| `plugins/shipwright/references/user-guide.md`, `*.md` (principles, toolchain-patterns, post-review-guide, etc.) | content (prompt/markdown) | content | high | n/a |
| `.github/workflows/lib/chart-tag-utils.sh` (`service_for_tag`, `values_paths_for_service`, `highest_semver_tag`) | 1. Pure business logic (pure string/tag parsing, no network) | unit-equivalent (bash test script, no `bun:test` suffix — see Notes) | high | n/a |
| `.github/workflows/lib/chart-drift-check.sh` (`read_pinned_tag`, mismatch computation — read-only) | 1. Pure business logic (pure `values.yaml` parsing given a string) | unit-equivalent (bash test script) | high | n/a |
| `.github/workflows/test-check-chart-drift.sh`, `.github/workflows/test-auto-bump-chart.sh` (bash assertion suites for the above, run via `bash <file>.sh`, not `bun test`) | (test infra — excluded from inventory) | — | — | n/a |
| `.github/workflows/check-chart-drift.yml` (periodic workflow: compares pinned `values.yaml` tags against the highest git release tag per service, on a schedule) | 3. HTTP route-equivalent (scheduled CI job owning its own "contract": what triggers it, what it reports) | smoke-equivalent (CI-level, not app-level — see Ambiguous) | medium | n/a |
| `.github/workflows/auto-bump-chart.yml` (release-tag-triggered chart-version bump; sources `lib/chart-tag-utils.sh`) | 2. Service-boundary code (writes to `values.yaml` + opens a PR) | integration-equivalent (covered by `test-auto-bump-chart.sh`) | high | n/a |
| `brand/brand-lint.ts` (`scanFile` hex-color check) | 1. Pure business logic | unit | medium | n/a |
| `brand/build-brand-css.ts` (tokens.json → CSS generator) | 1. Pure business logic (string generation) | unit | medium | n/a |
| `site/src/content.config.ts` (Astro content collection schema) | 1. Pure business logic (schema def) | unit | low | n/a |
| `site/src/data/config-vars.ts` (static config-var data) | 1. Pure business logic (data) | unit | low | n/a |
| `site/src/consts.ts` (nav-link data, sidebar-links data) | 1. Pure business logic (data) | unit | low | n/a |
| `site/src/lib/html-escape.ts` (per test-readiness-decisions.md - see 'site/src/lib/html-escape.ts — indirect e2e coverage accepted') | 1. Pure business logic | e2e (covered indirectly via `compare.spec.ts`, `self-hosted.spec.ts`, `vs-devin.spec.ts`) | high | n/a |
| `site/src/pages/*.astro` (marketing pages: index, agent-model, service-architecture, compare, self-hosted, vs/devin, story, docs/*) | 6. User journey (rendered page + nav flow) | e2e (Playwright `*.spec.ts`) | high | n/a |
| `site/src/layouts/*.astro`, `components/*.astro` | 3. HTTP route (rendered fragment, no standalone contract) | e2e (covered via page-level spec, not independently) | medium | n/a |
| `admin/e2e/agents-page.e2e.ts`'s subject: admin agent-detail page (GET `/admin/agents/:id`) rendered UI journey (auth redirect, env/cron/token visibility, secret masking, mobile viewport) | 6. User journey (single-page, but exercises auth + multi-widget render as a flow) | e2e (Playwright `*.e2e.ts`) | high | n/a |
| **User journey: dev-task → review → patch → merge/deploy pipeline** (task claim → PR open → review → patch → merge → deploy, spanning agent + task-store + admin + GitHub; includes an explicit human-invoked `/shipwright:merge` sub-step and, new this cycle, a human-invoked `/shipwright:unblock` triage sub-step for stuck items) | 6. User journey | e2e / smoke-composed (no browser; cross-service integration + GitHub fixtures) | critical | n/a |
| **User journey: agent provisioning** (admin creates Agent → resolves type manifest → seeds repos/tools/plugins/members/authorAllowlist → K8s Deployment/Secret → task-store/chat token minting → agent boots and reports health) | 6. User journey | e2e / smoke-composed | critical | n/a |
| **User journey: Slack chat with an agent** (DM/mention → chat-svc queue → `chat-poller` claim → Claude session → Slack post) | 6. User journey | e2e / smoke-composed | high | n/a |
| **User journey: marketing site visitor → docs → self-hosted quickstart** | 6. User journey | e2e (Playwright) | high | n/a |
| **User journey: local HITL dev-loop bootstrap** (`task hitl` → auto-clone configured repos → seed `agent-policy.md` → boot task-store/admin → poll for ready work → dispatch Claude Code) | 6. User journey | integration-composed (no browser, single-process orchestration, not yet a literal E2E) | medium | n/a |

## Critical-path roster (canary suite definition)

**Not applicable.** `deploy_model` is declared `direct` in this repo's CLAUDE.md — there is no staging environment and no canary-execution contract to define a roster against. See Notes.

## Features

A coarser, separate grouping of the units above — for coverage rollup and prioritization
only, not a per-file criticality→CI mapping. Every code unit above is assigned to exactly
one feature. Importance is prioritization-only and never excludes a feature from the
`feature_coverage_pct` denominator. **New this cycle** — the prior (2026-08-14) inventory
predates PR #2647 (feature-grouping step added to `test-inventory/SKILL.md`) and never had
this section; this is the first cycle it's populated. Grouped primarily by top-level
workspace/directory (the strongest, most consistent signal across this repo's 8-workspace
monorepo layout), with a few cross-cutting groupings called out where directory alone would
split a genuinely single feature.

| Feature name | Member units | Required layer | Importance | Covered (y/n) |
|---|---|---|---|---|
| `lib/` shared helpers | `lib/pricing.ts`, `org-repo.ts`, `request-context.ts`, `secret-env-vars.ts`, `claim-ttl.ts`, `github-login.ts`, `http.ts`, `sentry.ts`, `web/toolbar.ts`, `progress-phases.ts` | unit | core | y |
| Metrics dashboard | `metrics/src/*` (formatters, providers, clients, `api.ts`, `server.ts`, `dashboard-page.ts`, `secrets.ts`) | smoke | core | y |
| Agent — work selection & dispatch | `agent/src/work-selector.ts`, `loop-cron-classifier.ts`, `loop-orchestrator.ts`, `check-deploy.ts`, `check-dev-task.ts`, `check-patch.ts`, `check-review.ts`, `check-helpers.ts`, `pr-state-reconciler.ts`, `worktree-reaper.ts` | integration | revenue-path | y |
| Agent — runtime & lifecycle | `agent/src/entrypoint.ts`, `entrypoint-main.ts`, `setup.ts`, `setup-github-auth.ts`, `index.ts`, `config.ts`, `cli-args.ts`, `test-env.ts`, `clock.ts`, `crypto.ts`, `github-app-auth.ts`, `claude.ts`, `cutover-validate.ts`, `progress-milestones.ts` | integration | core | y |
| Agent — Slack & chat surface | `agent/src/slack.ts`, `chat-poller.ts`, `startup-dm.ts`, `voice.ts`, `piper-voice.ts`, `http-chat-service-client.ts` | smoke | core | y |
| Agent — cron & reporting | `agent/src/cron-handler.ts`, `health.ts`, `cron-run-reporter.ts`, `chat-token-reporter.ts`, `work-queue-reporter.ts`, `cron-failure-reporter.ts`, `analytics.ts`, `sessions.ts`, `users.ts`, `shipwright-config-client.ts`, `shipwright-runtime-client.ts`, `browser.ts`, `agent-repos-ref.ts`, `loop-jobs-ref.ts`, `agent-author-allowlist-ref.ts`, `agent-slack-membership-ref.ts` | smoke | auxiliary | y |
| Admin — agent CRUD & provisioning | `admin/src/agents-api.ts`, `api.ts`, `server.ts`, `admin-ui*.ts`, `agent-provisioner.ts`, `kubernetes-client.ts`, `agent-manifest.ts`, `agent-deletion*.ts`, `agent-tokens.ts`, `token-crypto.ts`, `chat-markers.ts`, `agent-envs.ts`, `agent-cron-*.ts`, `agent-tools.ts`, `agent-plugins.ts`, `agent-members.ts`, `agents.ts`, `agent-chat-tokens.ts`, `agent-work-queue.ts`, `agent-type-manifest-loader.ts`, `blocked-by-type-parity.check.ts`, `github-app-provisioning-client.ts` | smoke | revenue-path | y |
| Admin — auth & security | `admin/src/api-auth.ts`, `dev-auth-guard.ts`, `crypto.ts`, `google-auth-client.ts`, `okta-auth-client.ts`, `slack-provisioning-client.ts`, `chat-service-provisioning-client.ts`, `task-store-provisioning-client.ts`, `http-chat-client.ts`, `attachment-validation.ts`, `clock.ts`, `openapi-schemas.ts`, `errors.ts` | integration | security-path | y |
| Task store — task/PR lifecycle | `task-store/src/routes/{tasks,prs,tokens}.ts`, `task-service.ts`, `pull-request-service.ts`, `token-service.ts`, `repo-org-filter.ts`, `ready.ts`, `blocked-by.ts`, `statuses.ts`, `stale-claim-reaper.ts`, `claim-ttl-buffer-check.ts`, `auth.ts`, `validate.ts`, `app.ts`, `main.ts`, `index.ts`, `generate-spec.ts`, `openapi-schemas.ts`, `pr-transition-diff.ts` | smoke | revenue-path | y |
| Chat service | `chat/src/routes/*.ts`, `message-service.ts`, `thread-service.ts`, `token-service.ts`, `auth.ts`, `routes/utils.ts`, `generate-spec.ts`, `app.ts`, `main.ts`, `index.ts` | smoke | core | y |
| MCP server | `mcp-server/src/mcp-server.ts`, `tool-caller.ts`, `tool-allowlist.ts`, `serve.ts`, `index.ts`, `main.ts`, `http-transport.ts`, `auth.ts`, `clock.ts` | smoke | core | y |
| Coverage tooling & CI gates | `scripts/check-coverage.ts`, `coverage-tool-dispatch.ts`, `check-coverage-gate.ts`, `check-config-docs.ts` | integration | core | y |
| Dev-only local tooling | `scripts/sync-version.ts`, `dev.ts`, `dev-tmux.ts`, `minikube.ts`, `wait-for-agent.ts`, `seed-dev-agent.ts`, `seed-*.ts`, `generate-*-spec.ts`, `generate-mcp-*.ts`, `hitl.ts`, `hitl-log-file-tee-target.ts` | integration | auxiliary | y |
| Downstream chart-compatibility check | `scripts/check-downstream-compat.sh` | smoke | high | y |
| Shipwright plugin — cron prechecks & scripts | `plugins/shipwright/scripts/check-*.ts`, `classify_test_layer.ts`, `compute-review-verdict.ts`, `diagnose-review-candidacy.ts`, `ci-checks.ts`, `clock.ts`, `test-helpers/doubles.ts`, `compute-unaddressed-findings.ts` | integration | revenue-path | y |
| Shipwright plugin — commands, agents & skills (content) | `plugins/shipwright/commands/*.md`, `agents/*.md`, `skills/*/SKILL.md`, `commands/references/*.md`, `references/*.md` | content | revenue-path | y |
| CI chart-tag automation | `.github/workflows/lib/chart-tag-utils.sh`, `chart-drift-check.sh`, `check-chart-drift.yml`, `auto-bump-chart.yml`, their `test-*.sh` siblings | shell/smoke-equivalent | auxiliary | y |
| Brand tooling | `brand/brand-lint.ts`, `build-brand-css.ts` | unit | auxiliary | y |
| Marketing site | `site/src/pages/*.astro`, `layouts/*.astro`, `components/*.astro`, `content.config.ts`, `data/config-vars.ts`, `consts.ts`, `lib/html-escape.ts` | e2e | core | y |
| **User journey: dev-task → review → patch → merge/deploy pipeline** | (cross-cutting — see User journeys #1) | e2e/smoke-composed | revenue-path | y |
| **User journey: agent provisioning** | (cross-cutting — see User journeys #2) | e2e/smoke-composed | revenue-path | y |
| **User journey: Slack chat with an agent** | (cross-cutting — see User journeys #3) | e2e/smoke-composed | core | y |
| **User journey: marketing site visitor → docs → self-hosted quickstart** | (cross-cutting — see User journeys #4) | e2e | core | y |
| **User journey: local HITL dev-loop bootstrap** | (cross-cutting — see User journeys #5) | integration-composed | auxiliary | y |

**feature_coverage_pct: 100% (23/23)** — every feature's member units all have at least one test at their prescribed layer per this repo's existing, mature test suite (task ci's coverage gate enforces 90/89 lines/functions repo-wide as of CLG-1.1; no feature has a known, unaddressed gap at inventory time). This is a rollup of *layer presence*, not a claim that every individual unit's coverage is qualitatively ideal — Phase 3 (test-migration) evaluates coverage quality/completeness per unit, this is a coarser presence check. The 5 cross-cutting user-journey rows are marked covered on the same basis as the existing "e2e/smoke-composed" per-journey coverage already documented in the User journeys section below (composed of the underlying feature rows' own tests, not a separate literal end-to-end suite for 3 of the 5).

## Per-category detail

### 1. Pure business logic

The largest category by file count. Concentrated in:
- **`lib/`** — shared cross-service pure helpers (`pricing.ts`, `org-repo.ts`, `request-context.ts`, `secret-env-vars.ts`, `claim-ttl.ts`, `github-login.ts`), imported by `agent/`, `admin/`, `metrics/`, `task-store/`. **New this cycle: `lib/http.ts`'s `readJson`** — a request-body parser that was previously identically duplicated across `chat/src/routes/messages.ts`, `task-store/src/routes/tasks.ts`, and `task-store/src/routes/prs.ts`, now consolidated into one shared, exported module (`@shipwright/lib/http`) with all three call sites switched over. This is a dedup of already-tested logic, not new functional surface, but it does collapse what was previously three near-identical inline unit-test-able helpers into one canonical row.
- **Decision/classification logic** — `agent/src/work-selector.ts` (FIFO candidate picker), `task-store/src/ready.ts` / `blocked-by.ts` / `statuses.ts` (dependency-satisfaction rules), `plugins/shipwright/scripts/classify_test_layer.ts`, `plugins/shipwright/scripts/check-helpers.ts`'s `isCleanApproveBody`, and **new this cycle: `agent/src/check-patch.ts`'s `isSupersededBySelfReview`** (pure predicate — given a review, the full review list, and a `currentUser`, determines whether a later self-review supersedes an earlier one).
- **New this cycle: `plugins/shipwright/scripts/compute-review-verdict.ts`'s `computeVerdict`/`validateReviewVerdict`** — a pure 3-boolean-input truth-table function plus a regex-based verdict-label extractor/validator, mechanizing `review.md` Step 10's documented verdict logic. No I/O; deterministic given its typed input object.
- **Mutable-box refs** — `agent/src/agent-repos-ref.ts`, `agent/src/loop-jobs-ref.ts`, `agent/src/agent-author-allowlist-ref.ts` (unchanged this cycle).
- **Pure wire-object builders** — `admin/src/agent-manifest.ts` (K8s Deployment/Secret shapes), `admin/src/dev-auth-guard.ts`.
- **Crypto helpers treated as pure** — `agent/src/crypto.ts`, `admin/src/crypto.ts`, `admin/src/token-crypto.ts`: deterministic given key/IV inputs, no ambient I/O; canonical layer is unit even though the category-classifier's "no I/O" signal is borderline (uses `node:crypto`, which is in-process, not a process boundary).
- **Parsing/validation utilities** — `scripts/check-coverage.ts` (multi-format coverage parsing, now pluggable via `CoverageParser`), `scripts/coverage-tool-dispatch.ts`, `scripts/check-coverage-gate.ts` (both new this cycle), `scripts/check-config-docs.ts`, `brand/brand-lint.ts`, `brand/build-brand-css.ts`, `admin/src/attachment-validation.ts`, `agent/src/piper-voice.ts` (voice-name validation with injectable readdir).
- **New this cycle: `scripts/hitl.ts`'s planner functions** (`buildHitlConfig`, `buildPreflightSteps`, `buildServiceSpecs`) — pure command/config builders extracted from what was previously an undifferentiated integration-only script, mirroring `scripts/dev-tmux.ts`'s existing pure-builder pattern. See `test-readiness-decisions.md` and the updated row above.
- **`task-store/src/claim-ttl-buffer-check.ts`** — an opt-in startup sanity check that is pure given its injected env-value inputs; no network/DB call, so it stays unit despite living next to task-store's mostly-integration service layer.
- **`agent-types/coding/manifest.yaml` + `admin/src/agent-type-manifest-loader.ts`**, **`scripts/minikube.ts`'s pure builders**, **`.github/workflows/lib/chart-tag-utils.sh` and `chart-drift-check.sh`**, **`task-store/src/lib/repo-org-filter.ts`'s `buildRepoOrgWhere`**, **`admin/src/admin-ui-pages.ts`'s `renderRepoOrgFilterFields`/`toFilterArray`** — all unchanged this cycle.

These are already the repo's dominant unit-test surface (many have `*.unit.test.ts` siblings observed during enumeration, not read; `lib/http.unit.test.ts` and `compute-review-verdict.unit.test.ts` new the 2026-08-11 cycle).

### 2. Service-boundary code

Every workspace has a Prisma-backed service layer (task-store: `task-service.ts`/`pull-request-service.ts`/`token-service.ts`; chat: `message-service.ts`/`thread-service.ts`/`token-service.ts`; admin: `agent-envs.ts`/`agent-cron-jobs.ts`/`agent-chat-tokens.ts`/`agent-work-queue.ts`/etc.) plus internal HTTP clients (`metrics/src/lib/task-store-client.ts`, `agent/src/shipwright-*-client.ts`, `admin/src/*-provisioning-client.ts`). The agent's `check-*.ts` / `loop-orchestrator.ts` family is a distinctive service-boundary cluster: it composes GitHub API reads with task-store writes and is the operational core of the dev-task/review/patch/deploy loop — ranked critical.

**2026-08-18 cycle — PR findings ledger and cache/resource-limit fixes:**

1. **`task-store/prisma/schema.prisma` + `pull-request-service.ts` + `routes/prs.ts` — new `PrFinding` model (PFL-1.1/PFL-2.1).** A dedicated table (not a JSON blob on `PullRequest`) so review/patch can append findings concurrently via a plain race-safe INSERT (`appendFinding()`); the route enforces server-side authority (`source:"patch"` may only submit `disposition:"rejected"`) rather than trusting caller-supplied prose. Genuinely new functional surface, folded into the existing Task store composite row rather than a new row (schema + service + route all already live inside that row's scope) — see Summary row and Sampling note.
2. **`admin/src/agent-manifest.ts` + `agent/src/setup.ts` — ephemeral-storage 1Gi→4Gi + GOPATH/GOMODCACHE/npm/bun cache pinning.** Two halves of one fix: the pod's ephemeral-storage budget was too small for a single build step's scratch space, and three cache dirs (Go module cache being the worst offender at ~1.7GiB) were leaking onto that same budget by ignoring `XDG_CACHE_HOME`. No new row — correctness fixes to already-inventoried, already-critical/high rows.
3. **`task-store/src/routes/tasks.ts` / `task-service.ts` — `?hitl=true/false` filter (HTF-1.1).** Extends the existing multi-filter set (session/repo/org/source/claimedBy/pr/branch/assignee) with a `hitl` boolean, forwarded to both `listReady()` and `listBlocked()`. No new row — same pattern as prior cycles' filter-honoring extensions (ATB-1.2/TRF-1.1).

**2026-08-16 cycle — dual coverage-gate tooling and a resolved test-layer ambiguity:**

1. **`scripts/check-coverage-no-decrease.ts` (new)** — its comparison logic (`evaluateCoverageDelta`) is pure/unit, but the driver that checks out the base ref into an isolated temp git worktree and re-runs coverage there is a real service-boundary operation (filesystem + git + subprocess) — split classification, see Summary row.
2. **`scripts/hitl.ts` re-classification** — per `test-readiness-decisions.md`'s T-080 entry, the bootstrap sequence (clone→seed→boot→poll) is now covered at the unit layer via extracted pure planners, with only the thin executor remaining integration-covered. This is the registry's first entry to actually change a row's classification in this inventory (previously documentation-only per the registry's own "not yet implemented" consumption note).

**2026-08-14 cycle — dense bug-fix window on `check-review.ts`, new diagnostic CLI, and filter-honoring fixes:**

1. **`agent/src/check-review.ts` (5 of this cycle's 87 commits)** — a concentrated run of correctness fixes to `getReviewCandidates()`/`traceReviewCandidacyDecision()`: excluding candidates with any review at head (not just terminal ones), keying the terminal-skip check on `reviewedCommitSha` instead of `commitSha`, adding skip-reason tracing/structured logging, treating review-thread replies as a fresh-author-reply signal, and not re-gating an already-reviewed PR on the author allowlist. No new row — same already-critical service-boundary row, but flagged here given the fix density (see Ambiguous note on whether this signals a structurally under-tested area).
2. **New file: `plugins/shipwright/scripts/diagnose-review-candidacy.ts` (RCO-1.4)** — an on-demand CLI that fetches live GitHub PR/review + task-store PR state for one PR and runs it through `check-review.ts`'s exported `traceReviewCandidacyDecision()`. New row, category 2 (service-boundary via live fetch), canonical layer integration (recorded fixture).
3. **`task-store/src/task-service.ts` / `routes/tasks.ts` — filter-honoring + assignee-fallback fixes.** `listReady()`/`listBlocked()` gain full session/repo/org/source/claimedBy/pr/branch/assignee filter support (previously partial), a shared post-filter helper is extracted to prevent the two methods' filter logic from drifting (ATB-1.3), and agent tokens with no `repos` scope configured now fall back to matching on their own assignee. Correctness/coverage fixes to the already-inventoried, already-critical row — no new category or layer.
4. **`agent/src/worktree-reaper.ts`** — fixes `resolveOwningScopedRepo` to normalize `org/repo`-formatted scoped-repo entries to their bare name before matching against worktree dirnames (previously never matched in production, where scoped-repos entries are `org/repo`-formatted). Correctness fix to the already-inventoried, already-critical row.
5. **`agent/src/check-patch.ts`, `loop-orchestrator.ts`, `pr-state-reconciler.ts`** — stale-cancelled-CI detection + rerun-before-escalate (check-patch.ts), a `task_id`/`pr_number` Sentry dispatch tag (loop-orchestrator.ts), and per-pass summary logging (pr-state-reconciler.ts). Observability/correctness fixes, no new rows.

**2026-08-11 cycle — two independent claim-race closes and a reconciler scope reduction:**

1. **`task-store/src/pull-request-service.ts`'s `claim()` (CRF-1.1)** — moves its two conflict-detection conditions (modern same-phase guard, legacy review guard) from a pre-update JS `if` check against a `findUnique()` snapshot into the UPDATE statement's own `WHERE` clause via a negated `NOT: { OR: conflictConditions }`. Under the prior code, two concurrent claimers reading the same stale snapshot under READ COMMITTED could both pass the JS `if`, and the second writer would silently clobber the first's claim (a lost-update race). Now, Postgres — holding the row lock at execution time — is the sole arbiter: a losing writer's UPDATE matches 0 rows, Prisma throws `P2025`, and the catch block disambiguates `ConflictError` (row still exists, a concurrent claim now conflicts — the expected/desired outcome) from `NotFoundError` (row was deleted). `resetSkip()` also gained a companion UX fix: it now clears `blocked`/`blockedReason` in the same update, but only when the PR was auto-blocked by the skip-count mechanism specifically (`blockedReason` contains `"consecutive skips"`) — a block set by a different mechanism (e.g. the CI-failure-streak auto-block in `patch()`) is left untouched.
2. **`task-store/src/task-service.ts`'s `claim()`** — the same class of race is closed for tasks: the conditional UPDATE's `WHERE` clause gains an explicit `AND "claimedBy" IS NULL` (previously gated only on `status = 'pending'`).
3. **`agent/src/pr-state-reconciler.ts` — orphan-task reconciliation pass removed.** `listOrphanCandidateTasks`, `ghListOpenPrsForBranch`, and the `reconcileOrphanedTask`/`reconcileOrphanedTasks` functions are removed from both the `PrStateReconcilerDeps` interface and the reconciler body (a 547-line reduction in the paired unit-test file). The remaining `reconcilePrOpenTask`'s direct-path and branch-fallback heal logic, and their `startedAt`/BBR-1.1 headcount preconditions, are unchanged and re-documented in place to reflect that they're now the file's only two heal paths. This is a scope reduction to an already-inventoried, already-critical row — no new row, but a meaningful shrink of what this module's canonical-layer integration tests need to cover (a future Phase 3 migration pass should confirm the orphan-reconciliation-specific test cases were removed alongside the code, not left as dead assertions).
4. **`plugins/shipwright/scripts/compute-unaddressed-findings.ts` (PVD-1.1, URT-1.1)** — extracted from `check-patch.ts`, new `hasUnaddressedFindings` implementation + its six helper/predicate functions (`isSelfCleanApprove`, `isAddressedByAuthorReply`, `isSupersededBySelfReview`, `isResolvedByPriorFindingsStatus`, `isResolvedByLedger`, and the new `isThreadAddressedByAuthorReply`) exposed for CLI use by both `check-patch.ts` (native candidate qualification) and `review.md` Step 9.5 (mechanical hard gate). Integration-layer half of the DRO-1.2 correction + URT-1.1's inline-thread author-reply exclusion: the exclusions' cumulative effect on `hasUnaddressedFindings`'s overall boundary behavior (which reviews `check-patch.ts` treats as still-blocking when composing its candidate list, and which `review.md` Step 9.5 must also compute mechanically to decide verdict). Note that the sixth exclusion, `isThreadAddressedByAuthorReply`, is only actually populated on `review.md`'s Step 9.5 path today: `check-patch.ts`'s own GraphQL query (`fetchPrReviews`, `agent/src/check-patch.ts:562`) still fetches `comments(first: 1)` with no `createdAt` field on reviewThreads, so this exclusion's inputs never resolve on `check-patch.ts`'s real candidate-qualification path — structurally inert there until that query is widened to match `review.md`'s Step 5.5 query.

Companion, no-classification-impact changes: `chat/src/routes/messages.ts`, `task-store/src/routes/{tasks,prs}.ts` switch their local `readJson` duplicate to the new shared `lib/http.ts` import (see category 1); `agent/src/loop-orchestrator.ts` gained a header-comment-only note on `PR_REDISPATCH_COOLDOWN_MS`'s per-phase load-bearing status, no code change.

Earlier cycles' changes (HITL/blocked split, session-id threading, multi-repo/org filtering, CI-failure-streak auto-block, requested-reviewer inclusion, same-branch exclusivity guard, skip-reason taxonomy) are preserved in this file's git history — all were correctness/coverage fixes to already-inventoried, already-critical rows, no new category or layer introduced. `admin/src/agents.ts` / `agents-api.ts`, the K8s/manifest cluster, `agent/src/browser.ts`, and `scripts/minikube.ts` are unchanged since their respective introduction cycles.

### 3. HTTP routes / public API

Every service (metrics, admin, task-store, chat, mcp-server) exposes a Hono app factory with a route table documented in its own header comment. This cycle: `task-store/src/routes/{tasks,prs}.ts` and `chat/src/routes/messages.ts` each switch a locally-duplicated `readJson` helper for the new shared `@shipwright/lib/http` import — no route shape or classification change. `prs.ts`'s `/skip/reset` route's summary doc-comment updated to describe the new conditional auto-unblock behavior (implemented at the service layer — see category 2). `agent/src/cron-handler.ts` and `agent/src/slack.ts` are treated as route-equivalent (own a wire contract with an external caller). Canonical layer is smoke throughout, consistent with the CLAUDE.md convention (`*.smoke.test.ts` — in-process `app.request()`).

`.github/workflows/check-chart-drift.yml` remains flagged as route-*equivalent* rather than a literal HTTP route (see Ambiguous, carried forward unchanged).

### 4. Error / failure paths

Thin category here — most error handling is folded into the service/route layer rather than living in dedicated retry/circuit-breaker modules (no queue/retry infrastructure observed). Distinct logic-only error modules: `metrics/src/lib/errors.ts`, `admin/src/errors.ts`, `task-store/src/errors.ts`, `chat/src/errors.ts` (typed HTTP error classes — unit), and `agent/src/cutover-validate.ts` (validation gate — unit). `task-store/src/stale-claim-reaper.ts` and `agent/src/pr-state-reconciler.ts` are boundary-involving error/self-heal paths — integration (`pr-state-reconciler.ts`'s scope narrowed this cycle, see category 2). `agent/src/cron-failure-reporter.ts` is a boundary-involving error path: it catches a thrown cron-tick error, writes an AgentCronRun row via the admin API, and captures to Sentry — ranked high given it is the only thing standing between a swallowed cron crash and operator visibility. `agent/scripts/verify-browser-launch.ts` (environment-capability gate, CI-only, no `bun test` suffix) is unchanged this cycle.

No new failure-path modules landed this cycle. The two claim-race closes in category 2 (`pull-request-service.ts`, `task-service.ts`) are arguably error-path-adjacent (they change what a losing concurrent writer observes — `ConflictError`/`P2025` handling), but are classified at their home service-boundary row rather than split into a separate category-4 entry, consistent with how `claim()`'s pre-existing conflict-error paths were already folded into the service rows in prior cycles.

### 5. External integrations

- `admin/src/kubernetes-client.ts` — in-cluster K8s API (critical; already has a `RecordedKubernetesClient` cassette double per its own header comment).
- `admin/src/google-auth-client.ts` — Google OAuth2 (critical, auth path).
- `agent/src/github-app-auth.ts` — GitHub App installation tokens (critical).
- `agent/src/voice.ts` — Groq Whisper / ElevenLabs (medium). `agent/src/piper-voice.ts` is *not* grouped here — it only validates/resolves baked local `.onnx` file paths (no network call); actual Piper synthesis is a future task per its own header comment, so it stays category 1 (unit) for now.
- `admin/src/slack-provisioning-client.ts` — Slack `apps.manifest.create` (high, one-time provisioning flow).
- `lib/sentry.ts` — Sentry SDK wiring (high, observability-critical but not user-facing).
- Third-party SDKs used directly in entrypoints (`@sentry/bun`, `@octokit/*`, `@slack/bolt`, `playwright`'s `chromium`) are not separately inventoried beyond the wrapper modules that own them.

No new external-integration surfaces landed this cycle.

### 6. User journeys

Five top-level product journeys — unchanged in count this cycle, but journey #1 gained a scope note:

1. **dev-task → review → patch → merge/deploy pipeline** — the core product loop. Critical — this is the product. **New this cycle: `/shipwright:unblock` (30th command file) adds an explicit human-invoked triage sub-step** for `status: 'blocked'` tasks and `blocked: true` PRs — discover → infer originating phase → retry/redirect/abandon, one at a time, human-gated. Like `/shipwright:merge` before it, `unblock` is explicitly exempt from the Candidate Selection Contract and never runs unattended, so it extends the journey's *human-recovery* surface without becoming a new autonomous pipeline phase. This cycle's CRF-1.1 (claim-race close, both PR and task claim paths), DRO-1.2 (self-review supersession), and the `pr-state-reconciler.ts` orphan-pass removal are all within-journey correctness/scope properties — best proven at the integration layer where `check-patch.ts`/`pull-request-service.ts`/`task-service.ts`/`pr-state-reconciler.ts` already live, not new journeys.
2. **Agent provisioning** — admin creates an Agent record (type-aware manifest resolution, seeded repos/tools/plugins/members/authorAllowlist), which cascades through `agent-provisioner.ts` → K8s Deployment/Secret → task-store/chat-service token minting → the agent container boots and reports `/health`. Critical. Unchanged this cycle.
3. **Slack chat with an agent** — DM/mention → chat-svc queue → `agent/src/chat-poller.ts`'s poll/claim loop → Claude session → Slack reply. High. Unchanged this cycle (its `chat/src/routes/messages.ts` gained the shared `readJson` import swap, a no-op refactor from this journey's perspective).
4. **Marketing site visitor journey** — landing → docs → self-hosted quickstart, already covered by `site/`'s existing Playwright `*.spec.ts` suite per CLAUDE.md. High. Unchanged this cycle.
5. **Local HITL dev-loop bootstrap** — `task hitl` → auto-clone missing repos → seed `agent-policy.md` → boot task-store + admin locally → poll for ready work → dispatch Claude Code. Medium. Unchanged this cycle.

No journey is browser-driven except #4 (plus #2's detail-page rendering step). #1, #3, and #5 are best proven by cross-service integration tests composing recorded fixtures / injected doubles rather than a literal Playwright E2E, since there is no UI for the pipeline or the HITL bootstrap itself — noted as "e2e / smoke-composed" or "integration-composed" in the summary table to flag that the canonical E2E *tooling* (browser automation) doesn't apply, but the *layer* (multi-step, multi-process state) still does.

## Ambiguous items (human review needed)

- **`.github/workflows/lib/chart-tag-utils.sh` / `chart-drift-check.sh` and their `test-*.sh` siblings** — classified as a repo-specific "shell" layer alongside the five CLAUDE.md-declared suffixes, since these are genuinely meaningful, tested code units (pure tag/YAML parsing logic with 300+ line bash assertion suites) that fall entirely outside the `bun test` runner and the five-suffix convention. Recommend a human either (a) formally document this as a sixth layer in CLAUDE.md's `## Test conventions` table, given it's now used by two workflow files with real test coverage, or (b) confirm it should stay an unofficial exception. Carried forward unchanged — still unresolved.
- **`.github/workflows/check-chart-drift.yml` and `auto-bump-chart.yml`** — modeled as HTTP-route-equivalent (category 3, "smoke-equivalent") and service-boundary (category 2, "integration-equivalent") respectively, by analogy to the classifier's route/service distinction, even though neither is an HTTP endpoint — they're scheduled/triggered CI workflows. Recommend a human confirm whether CI workflow files belong in this inventory at all, or should live in a separate "CI infrastructure" appendix outside the four-layer model entirely. Carried forward unchanged.
- **`agent/scripts/verify-browser-launch.ts`** — classified category 4 (error/failure-path, environment-capability gate) rather than category 2/3, on the basis that its sole job is to fail loudly if a specific runtime precondition doesn't hold, and it cannot be meaningfully exercised by a normal in-process `bun test`. No `*.test.ts` suffix applies to it at all. Carried forward unchanged.
- **`agent/src/browser.ts`** — classified category 2 (service-boundary: spawns/drives a real Chromium process) rather than category 5 (external integration), on the reasoning that Chromium here is a bundled build-time dependency the agent fully controls (baked into the image), not a third-party network service. Carried forward unchanged.
- **`agent/src/crypto.ts` / `admin/src/crypto.ts` / `admin/src/token-crypto.ts`** — classified as category 1 (pure business logic, unit layer) on the basis that `node:crypto` is in-process rather than a network/DB boundary. Carried forward unchanged.
- **`agent/src/slack.ts` and `agent/src/cron-handler.ts`** — classified as category 3 (HTTP route equivalent) despite not being URL-routed. Carried forward unchanged.
- **`agent/src/piper-voice.ts`** — currently category 1 (unit); re-classify to category 5 once synthesis is implemented. Carried forward unchanged — still no synthesis landed.
- **`lib/sentry.ts`** — straddles category 1 (scrub-rule logic) and category 5 (SDK `init()` call). Carried forward unchanged.
- **`admin/src/server.ts`** — classified alongside the other pure-composition entrypoints (category 3, smoke). Carried forward unchanged.
- **`site/src/layouts/*.astro`, `site/src/components/*.astro`** — not independently testable via a wire contract; modeled as "covered via page-level spec, not independently." Carried forward unchanged.
- **`site/src/lib/html-escape.ts`** — **Resolved**, per `.claude/shipwright/test-readiness-decisions.md` ("site/src/lib/html-escape.ts — indirect e2e coverage accepted"). Carried forward unchanged. Revisit if a 4th call site is added without a matching `*.spec.ts`, or if `site/` adopts a `bun:test` unit convention.
- **`scripts/dev.ts`, `scripts/dev-tmux.ts`, `scripts/wait-for-agent.ts`, `scripts/seed-dev-agent.ts`, `scripts/minikube.ts`** — pure local-developer tooling, never shipped/deployed. Human should confirm whether dev-only tooling belongs in this ledger at all, or a separate "tooling" appendix. `scripts/hitl.ts` remains deliberately **not** grouped here despite also being dev-only — it's ranked high/journey-level given its expanded scope. Carried forward unchanged. **Note (this cycle):** `scripts/hitl.ts`'s test-layer ambiguity itself (unit vs. integration for the bootstrap sequence) is now **resolved** via `.claude/shipwright/test-readiness-decisions.md` — see the updated row above and this cycle's Sampling note. What remains open here is only the broader "does dev-only tooling belong in this ledger" question, not `hitl.ts`'s specific layer.
- **`plugins/shipwright/skills/*/SKILL.md` and `commands/*.md`** — batch-classified as "content" layer by convention rather than individually read in full. `commands/` grew from 29 to 30 files this cycle (one new file: `unblock.md`, plus its own `references/escalation-pattern.md`); neither was read in full beyond the header/opening sections shown in the sampling-strategy note above. Flagged for the human reviewer to spot-check during Phase 3, with particular attention to `unblock.md` given it's a wholly new, interactive, human-facing command with its own 305-line body, 225-line content-test suite, and a new 95-line reference doc. `security-scan/SKILL.md` and `test-readiness/SKILL.md`'s substantial extensions this cycle were likewise not read in full — recommend a spot-check of both during Phase 3.
- **`charts/shipwright/` (Helm chart) and `demo/`** — top-level directories, declarative/config or media assets, not TypeScript source with a test-suffix convention. Excluded from this inventory as out of scope (no code units to classify). Carried forward unchanged.
- **`mcp-server/src/index.ts` and `main.ts`** — classified as category 3 composition entrypoints (smoke), same reasoning as `admin/src/server.ts`. Carried forward unchanged.
- **`agent/src/pr-state-reconciler.ts`'s removed orphan-reconciliation pass** — carried forward unresolved from the 2026-08-11 cycle. The removal (`listOrphanCandidateTasks`, `ghListOpenPrsForBranch`, `reconcileOrphanedTask(s)`) looks like a clean scope reduction from the diff alone, but this inventory did not verify *why* the pass was removed (e.g. whether it was subsumed by another mechanism, found to be unreachable/redundant, or simply descoped) or whether its 547 lines of removed unit-test assertions represented coverage that's now genuinely unneeded versus coverage that quietly disappeared. Recommend a human confirm the removal's rationale before Phase 3 treats the reduced `pr-state-reconciler.ts` surface as fully accounted for.
- **`admin/src/blocked-by-type-parity.check.ts`** — new this cycle. A genuinely new pattern: a `*.check.ts`-suffixed file with zero runtime behavior, type-checked via a dedicated `tsconfig.type-parity.json` project, never executed by `bun test`. It doesn't fit any of the five CLAUDE.md-declared suffixes or the unofficial sixth "shell" layer (which at least runs via `bash`). Recommend a human decide whether "compile-time parity check" deserves formal recognition as a seventh convention (there may be more of these as cross-package type-mirroring patterns spread) or should just be noted as a one-off. Not yet resolved.
- ~~**`agent/src/check-review.ts`'s fix density (5 of 87 commits, 2026-08-14 cycle)**~~ — **Resolved.** T-082 (PR #2641) added 3 test cases to `check-review.unit.test.ts` stacking 3+ of the prior fix cycle's exclusion conditions (stale commit-SHA divergence, review-thread-reply freshness, task-blocked state) in single fixtures, proving the documented precedence order holds in combination.
- ~~**`agent/src/agent-slack-membership-ref.ts` — singleton test-leak fix (flagged 2026-08-22 cycle)**~~ — **Resolved this cycle.** `192437ed` (T-084, PR #2790) adds an `afterEach` to `agent-slack-membership-ref.unit.test.ts`'s singleton describe block, resetting `agentSlackMembershipRef` to its fail-open default after each test. `3df0539e` (PR #2788, landed alongside) independently closed the same leak's other two call sites (`slack.integration.test.ts`, `slack-chat-conversation.integration.test.ts`) by injecting a fresh, unsynced ref per test instead of falling through to the singleton default. All 3 known call sites of the leak are now closed. Removed from the open list — see this cycle's Sampling note for detail.

## Notes

- **Canary eligibility skipped for this entire inventory.** `deploy_model: direct` — canary eligibility not applicable (no staging environment). Per Step 5 of the `test-inventory` SKILL, no items are tagged `canary-eligible: true`; the `Canary-eligible` column above is `n/a` throughout and the Critical-path roster section is empty by design. Re-verified directly against `CLAUDE.md` at this commit (`ac0ec6a8`) — unchanged.
- This inventory is **deduplicated** per the canonical-layer rule. Each functional unit appears exactly once at its lowest-sufficient layer.
- Higher-layer tests, if needed, only assert what the canonical layer cannot (boundary behavior, wire contract, multi-step state).
- The repo's own test-layer convention (`*.unit.test.ts` / `*.integration.test.ts` / `*.smoke.test.ts` / `*.spec.ts` / `*.e2e.ts` / `*.content.test.ts`) maps cleanly onto this rubric's four layers plus a fifth "content" layer for markdown/prompt assertions, plus the unofficial sixth "shell" layer for `.github/workflows/test-*.sh`, plus a still-unresolved seventh "compile-time parity check" pattern (`*.check.ts`, see Ambiguous).
- **`## Features` section** (SKILL.md Step 3b, PR #2647) — 23 features grouped primarily by workspace/directory, `feature_coverage_pct` remains 100% (23/23) this cycle since every feature's member units (including the two new `progress-phases.ts`/`progress-milestones.ts` units, folded into the existing `lib/` shared helpers and Agent — runtime & lifecycle features respectively) already have prescribed-layer test coverage. See the Features section above for the full table and coverage-pct methodology note.
- **Delta since the 2026-08-28 inventory (this cycle, 2026-08-30, 13 substantive commits out of 28 total):** 2 new inventory rows (`lib/progress-phases.ts`, `agent/src/progress-milestones.ts` — the new generic-progress-phase feature, CFB-2.1, #2906), 0 removed. Both new rows fold into existing features (`lib/` shared helpers; Agent — runtime & lifecycle) — no new feature, `feature_coverage_pct` unchanged at 100% (23/23), both new units already have unit-test coverage (`lib/progress-phases.unit.test.ts`, `agent/src/progress-milestones.unit.test.ts`, both new). `agent/src/claude.ts`'s existing row extended in place (`ProgressCallback` widened additively) — no new row. Eleven further commits are correctness fixes/refactors to already-inventoried rows, no new category or layer: `pr-state-reconciler.ts` (batch+rotating-cursor scan replacing a recency-window exclusion that permanently dropped slow-review PRs, #2893); `check-deploy.ts` (case-insensitive CI/Deploy workflow-name matching, #2896); `chat-poller.ts`/`message-service.ts`/`routes/messages.ts` (heartbeat-driven reply timeout replacing a flat 90s UI cutoff, #2899 + #2902); `admin-ui-pages.ts`/new `admin-ui-layout.ts` (shared page-head extraction + viewport-meta normalization across 22 render sites, #2905 — refactor-relocation within the existing admin-UI composite row, no new row); `admin-ui-pages.ts`'s chat-page inline-styles-to-classes migration plus two new admin e2e spec files (`admin/e2e/chat-page.e2e.ts`, `chat-thread-page.e2e.ts`, #2908); nine mobile-responsive defect fixes across the admin console chat pages plus an attachment-allowlist extension, verified via new Playwright e2e assertions rather than string-matched unit tests (#2914). One config-only change (`renovate.json`'s native-automerge enablement for minor/patch Renovate PRs, #5150bd14) — no source row, per the existing top-level-config exclusion. No deletions, no new user journeys. No Ambiguous items resolved or newly added this cycle — both carried-forward entries (`blocked-by-type-parity.check.ts`'s compile-time-only pattern; the `.github/workflows/test-*.sh` unofficial "shell" layer) remain open. `.claude/shipwright/test-readiness-decisions.md` re-checked per Step 0 — both existing entries remain applicable, no revisit conditions met. `deploy_model` re-verified as `direct` directly against `CLAUDE.md` at this commit (`ac0ec6a8`) — unchanged, canary eligibility tagging remains skipped for the entire inventory.
- **Delta since the 2026-08-23 inventory (2026-08-25 cycle, 5 substantive commits out of 19 total, preserved for continuity):** 0 new inventory rows, 0 removed. Three commits (#2811, #2810, #2803) extended already-inventoried `content` category rows (`dev-task.md`, `review.md`/`code-reviewer.md`, `plan-session.md`) with a `.claude/shipwright/principles.md`-override-then-fallback pattern and a security-domain citation — content-only, no row/layer change. One commit (#2804) is a content-only cron-prompt fix (`error-patrol-maintenance` chaining instruction) plus its paired golden-fixture test update (`agent-cron-jobs.integration.test.ts`) — both already-inventoried rows. One commit (#2801) is a correctness fix (missing-ledger branch `exit 1`→`exit 0`) to the already-inventoried, already-`medium`-criticality `check-consolidation-patrol.ts` row, fixing a confirmed permanent-deadlock bug (the `consolidation-patrol-maintenance` cron had never fired since 2026-07-18) — no new category or layer. One CVE dependency bump (`deepmerge-ts` 7.1.5→8.0.2, #2798) — `package.json`/`bun.lock` only, no source row per existing convention. `feature_coverage_pct` unchanged at 100% (23/23) — no new feature, no new row. No Ambiguous items resolved or newly added this cycle — all carried forward unchanged (see list above). No new user journeys. `.claude/shipwright/test-readiness-decisions.md` re-checked per Step 0 — both existing entries remain applicable, no revisit conditions met. `deploy_model` re-verified as `direct` directly against `CLAUDE.md` at this commit (`d4bfce02`) — unchanged, canary eligibility tagging remains skipped for the entire inventory.
- **Delta since the 2026-08-22 inventory (2026-08-23 cycle, 2 substantive commits out of 5 total, preserved for continuity):** 0 new inventory rows, 0 removed. `agent/src/slack.ts`'s existing row extended in place with the SMR enforcement gate (`isAllowedSlackSender`/`shouldRejectSlackSender`, PR #2788) — the config-plumbing half of SMR was already a new row last cycle (`agent-slack-membership-ref.ts`); this cycle's PR #2788 is the enforcement half, folded into the already-inventoried `slack.ts` row rather than a new one, since it's additional gating logic within an already-inventoried file's wire contract, not a new boundary. `feature_coverage_pct` unchanged at 100% (23/23) — no new feature, no new row, both changed files (`slack.ts`, `agent-slack-membership-ref.unit.test.ts`) already had prescribed-layer coverage before this cycle and gained more. One Ambiguous item **resolved**: the SMR singleton test-leak watch item flagged last cycle (PR #2790, T-084, plus two companion fix-commits inside #2788 closing the other 2 call sites) — all 3 known leak call sites now closed. No new Ambiguous items. No new user journeys. `deploy_model` re-verified as `direct` directly against `CLAUDE.md` at this commit (`be5bfa59`) — unchanged, canary eligibility tagging remains skipped for the entire inventory.
- **Delta since the 2026-08-18 inventory (2026-08-22 cycle, 84 commits / 138 files, spanning a 4-day gap, preserved for continuity):** 5 new inventory rows (`admin/src/okta-auth-client.ts`, `admin/src/github-app-provisioning-client.ts`, `agent/src/agent-slack-membership-ref.ts`, `task-store/src/pr-transition-diff.ts`, `scripts/hitl-log-file-tee-target.ts`), folded into 3 existing features (Admin — auth & security; Admin — agent CRUD & provisioning; Agent — cron & reporting; Task store — task/PR lifecycle; Dev-only local tooling) — no new feature added, `feature_coverage_pct` unchanged at 100% (23/23), all 5 new units have prescribed-layer coverage at inventory time (`hitl-log-file-tee-target.ts` by deliberate design has no dedicated test file of its own, exercised only via `hitl.integration.test.ts`'s subprocess fixture — counted as covered per that documented, intentional exception). 1 row **removed**: `scripts/check-coverage-no-decrease.ts` (added in the 2026-08-16 cycle) was deleted outright this cycle (`2ab1a4cb`, 3-stage coverage-promotion lifecycle complete) along with both its test files and its CI job — a genuine deletion, not a refactor. `scripts/check-coverage.ts`'s line-coverage threshold raised 89→90 (matches `CLAUDE.md`'s already-documented 90/89 gate, no drift). One dense correctness-fix cluster to the already-critical `check-review.ts`/`compute-unaddressed-findings.ts`/`review.md` review-candidacy rows (8 commits this cycle — the largest single-area fix density observed across any cycle to date, consistent with this area's history of absorbing fix clusters). One new Ambiguous/watch item added (SMR singleton test-leak fix, PR #2788, lands just after this window). No Ambiguous items resolved this cycle. No new user journeys. `deploy_model` re-verified as `direct` directly against `CLAUDE.md` at this commit (`ea846283`) — unchanged, canary eligibility tagging remains skipped for the entire inventory.
- **Delta since the 2026-08-16 inventory (2026-08-18 cycle, 16 commits / 53 files, spanning a 1-day gap, preserved for continuity):** No new inventory rows added — the one new functional surface this cycle (`PrFinding` model: `task-store/prisma/schema.prisma` + `pull-request-service.ts`'s `appendFinding()` + `routes/prs.ts`'s `POST /:id/findings`, PFL-1.1/PFL-2.1) is folded into the existing, already-critical Task store composite row rather than getting its own row, since schema+service+route for a single small feature within an already-inventoried service module doesn't warrant row-splitting per the dedup rule. 3 further in-place fixes to already-inventoried rows: `admin/src/agent-manifest.ts`'s ephemeral-storage constant (1Gi→4Gi) and `agent/src/setup.ts`'s cache-pinning (GOPATH/GOMODCACHE/npm/bun) are two halves of one pod-eviction fix; `task-store/src/routes/tasks.ts`/`task-service.ts` gained a `?hitl=true/false` filter (HTF-1.1); `scripts/minikube.ts` gained dev-login-URL/auto-open pure builders. No deletions, no new user journeys, no new Ambiguous items, no Ambiguous items resolved this cycle. `feature_coverage_pct` unchanged at 100% (23/23) — no new feature introduced; `PrFinding` is a new capability within the existing "Task store — task/PR lifecycle" feature, which was already `smoke`/`integration`-required and covered. `deploy_model` re-verified as `direct` directly against `CLAUDE.md` at this commit (`f901803d`) — unchanged, canary eligibility tagging remains skipped for the entire inventory.
- **Delta since the 2026-08-14 inventory (2026-08-16 cycle, 46 commits / 57 files, ~13 non-doc/non-version-bump source files, spanning a 2-day gap, preserved for continuity):** 3 new source files with their own inventory rows (`scripts/coverage-tool-dispatch.ts`, `scripts/check-coverage-gate.ts`, `scripts/check-coverage-no-decrease.ts` — the new dual feature+line coverage-gate cluster), no deletions. `scripts/check-coverage.ts`'s existing row extended in place (lcov-only → pluggable `CoverageParser` interface with 5 concrete parsers: lcov/Jacoco/Istanbul(c8-nyc)/coverage.py/go-cover). `scripts/hitl.ts`'s existing row re-classified from pure-integration to a **split unit(planners)/integration(executor)** layer, per a newly-populated `test-readiness-decisions.md` entry (T-080) — the task's originally-prescribed integration layer was deliberately not used; this is the registry's first operational effect on this inventory (applied by manual read this cycle, since the skill's automatic consumption step is still not implemented per the registry file's own header). One Ambiguous item **resolved** (`check-review.ts`'s fix density — T-082 added multi-condition exclusion-check test coverage answering the open question). No new user journeys, no new "seventh layer" pattern instances beyond the one already flagged. `deploy_model` re-verified as `direct` directly against `CLAUDE.md` at this commit (`ca3c8c3f`) — unchanged, canary eligibility tagging remains skipped for the entire inventory.
- **Prior cycles' delta log (2026-07-21 through 2026-08-14)** is preserved in this file's git history and is not restated here — see the 2026-08-14 version of this document (or `git log -p -- docs/test-readiness/test-inventory.md`) for the full chain.
- `docs/test-readiness/test-system.md` (existing) is the authoritative test blueprint (layer matrix, boundary rules, per-component budgets, CI pipeline shape) — this inventory is upstream of it and does not restate its content; consult it for the isolation contract referenced throughout this document.
- `docs/test-readiness/naming.md` (existing) is the source of truth for the suffix convention used above.
- Phase 2 (`/test-system-design`) consumes this inventory to design the ideal test system.
