# Test System Design

> Phase 2 of the test-readiness pipeline. The greenfield blueprint for the ideal test
> system that would satisfy the Phase 1 inventory
> ([`test-inventory.md`](./test-inventory.md)). Does NOT account for existing tests —
> that's Phase 3 (`/test-migration`).
>
> Generated by `/test-design` on 2026-07-16 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-20260716`), consuming the Phase 1 inventory committed at
> `ebde3ca1`.

## Stack profile (from inventory)

- **Language(s):** TypeScript (Bun runtime), plus Astro/Tailwind for the marketing site.
- **Package manager:** Bun workspaces (`bun.lock`), 8-workspace monorepo (`lib`,
  `plugins/shipwright`, `metrics`, `agent`, `admin`, `task-store`, `chat`, `mcp-server`)
  plus non-workspace supporting surfaces (`site/`, `brand/`, `scripts/`, `demo/`).
- **Primary framework(s):** Hono (all HTTP services), Prisma + Postgres (admin,
  task-store, chat), Zod / `@hono/zod-openapi`, Astro (site), Playwright (site + admin +
  metrics E2E), Bun's built-in test runner throughout.
- **Deploy model:** `direct` (root `CLAUDE.md`) — Helm chart + GHCR-pinned image tags,
  no staging/production GitHub Environments, no deploy-staging→canary→promote pipeline.
  This shapes Step 4 below (canary contract) and the repo-configuration section.
- **Repo's own layer convention (used verbatim, per CLAUDE.md fallback):** this repo
  already declares five concrete layer names by filename suffix — `*.unit.test.ts`,
  `*.integration.test.ts`, `*.smoke.test.ts`, `*.spec.ts` (site only), and
  `*.content.test.ts` — mapping cleanly onto the rubric's unit/integration/smoke/e2e plus
  a repo-specific fifth "content" layer for markdown/prompt assertions. This design
  reuses those names rather than introducing the rubric's generic labels.

## Framework matrix per layer

The stack-matrix default for "TypeScript (Bun)" is `bun test` for unit/integration,
supertest/Hono-test-client for smoke, Playwright for E2E. The repo already implements
all four choices in production, so no override is needed anywhere — the design below
documents the existing choices as the target, not a hypothetical.

| Layer | Framework | Rationale |
|---|---|---|
| Unit | `bun test` | Matches the stack matrix default for TypeScript (Bun). Already the repo's exclusive unit runner (`*.unit.test.ts`, no `pathIgnorePatterns` entry — discovered by the default root scan). No I/O, no framework boot — Bun's runner is fast enough that no alternative is justified. |
| Integration | `bun test` + recorded-fixture doubles (hand-authored JSON, e.g. `RecordedKubernetesClient`, `RecordedTaskStoreClient`, `RecordedGithubClient`) for internal/third-party HTTP; **real Postgres via a GitHub Actions `postgres:16` service container** for all three Prisma-backed DB integration suites (admin, task-store, chat) | Matches the stack matrix default (`bun test` + testcontainers-equivalent). The repo already runs real Postgres for DB integration tests via a CI service container rather than testcontainers proper — functionally equivalent (real DB per test run) and cheaper in CI (no per-job container-runtime bootstrap beyond the `services:` block Actions already manages). Chat's Prisma layer now has this pattern wired too (see below — resolved on `main` via PR #1687, ahead of this doc's own base commit). |
| Smoke | `bun test` + Hono `app.request()` (in-process, no real socket); `Bun.serve()` + `fetch()` only where no Hono app-factory seam exists (e.g. `agent/src/health.ts`) | Matches the stack matrix's "supertest / Hono test client" cell. `app.request()` is strictly cheaper than supertest (no socket, no port allocation) and is already the repo's documented convention (`docs/testing.md`, this repo's own `test-system.md` predecessor). The real-socket exception is narrow and already named in CLAUDE.md's Test conventions table. |
| E2E | Playwright (`@playwright/test`) | Matches the stack matrix default. Already wired for three surfaces: `site/` (`site/playwright.config.ts`, `*.spec.ts`), `metrics/` (`metrics/playwright.config.ts`, `metrics/e2e/*.e2e.ts`), `admin/` (`admin/playwright.config.ts`, `admin/e2e/*.e2e.ts`). All three are excluded from the root `bun test` scan via `bunfig.toml` `pathIgnorePatterns` and run via dedicated `task e2e` / `task e2e:admin` / `cd site && npm test` entry points. |
| Content (repo-specific fifth layer) | `bun test` | Not in the rubric's stack matrix — a repo-specific layer for markdown/prompt-content assertions (commands, skills, agent prompts, reference docs). No I/O boundary beyond static file reads (`existsSync`/`readFileSync`); no separate framework needed. Runs in the same `bun test` invocation as unit (no `pathIgnorePatterns` entry, discovered by default). |

## Local execution architecture

Every external dependency named in the Phase 1 inventory, with its local substitute per
the SKILL's substitution table. No test in this system may require a hosted external
service or a developer manually starting one.

| Dependency | Local substitute | Setup file |
|---|---|---|
| Postgres (admin, task-store) | Real DB per test run — GitHub Actions `postgres:16` service container in CI; `task db:provision` (`prisma migrate deploy`) locally against a developer-run Postgres. **Pattern already in place:** Prisma calls are wrapped in a typed service layer (`admin/src/agent-envs.ts` etc., `task-store/src/task-service.ts` / `pull-request-service.ts` / `token-service.ts`); integration tests hit real Postgres through that layer; unit tests never touch Prisma. | `.github/workflows/ci.yml` (`services.postgres`, `DATABASE_URL_ADMIN_TEST`, `DATABASE_URL_SHIPWRIGHT_TASK_STORE_TEST`); local equivalent documented in `docs/task-store.md` / `docs/agent.md`. |
| Postgres (chat) | **Resolved.** `chat/src/message-service.ts` / `thread-service.ts` / `token-service.ts` are Prisma-backed per the inventory, and `chat/prisma/schema.prisma` now has matching `DATABASE_URL_SHIPWRIGHT_CHAT` wiring in `ci.yml`'s `postgres` service container alongside `admin` and `task-store` — this landed via PR #1687 ("feat: provision shipwright_chat_test DB and run chat prisma migrations in CI"), which is an ancestor of this branch's own base commit. `chat/src/message-service.integration.test.ts`, `chat/src/thread-service.integration.test.ts`, and `chat/src/token-service.integration.test.ts` already exist and exercise this. | **No action needed.** `ci.yml` creates `shipwright_chat_test` and runs `prisma migrate deploy --schema=chat/prisma/schema.prisma` alongside the existing admin/task-store steps; `DATABASE_URL_SHIPWRIGHT_CHAT` is set. |
| Redis | Not named in the inventory — no service in this repo uses Redis. No substitute needed. | — |
| S3 / blob storage | Not named in the inventory. No substitute needed. | — |
| Internal HTTP service (task-store client, GitHub App API, K8s API, chat-service client, accounts client, admin-metrics client) | Recorded fixture doubles (hand-authored JSON + a `Recorded*Client` implementing the same interface as the `Http*Client`), per the repo's existing pattern — already implemented for `admin/src/kubernetes-client.ts` (`RecordedKubernetesClient`), `metrics/src/providers/task-store-recorded.ts`, `agent/src/shipwright-config-client.ts`. This design extends the same pattern to every remaining `Http*Client` named in the inventory (`metrics/src/lib/task-store-client.ts`, `metrics/src/lib/accounts-client.ts`, `metrics/src/lib/admin-metrics-client.ts`, `agent/src/github-app-auth.ts`, `agent/src/http-chat-service-client.ts`, `agent/src/shipwright-runtime-client.ts`, `admin/src/*-provisioning-client.ts`, `mcp-server/src/tool-caller.ts`). | `<workspace>/src/fixtures/` — already exists per-workspace (`admin/src/fixtures`, `metrics/src/fixtures`, `agent/src/fixtures`, `plugins/shipwright/tests/fixtures`, `plugins/shipwright/scripts/test-helpers/fixtures`). No new directory needed; new fixtures land alongside existing ones. |
| Third-party HTTP API (Sentry SDK init, Slack `apps.manifest.create`, Google OAuth2, GitHub App tokens, Groq Whisper / ElevenLabs) | Recorded fixture doubles — never live. Same `Recorded*Client` pattern as internal HTTP. `lib/sentry.ts`'s scrub-rule logic is unit-testable directly (pure); its SDK `init()` wiring is integration-tested against a recorded/no-op transport, never a live Sentry project. | Per-workspace `src/fixtures/`, same as above. Requires the deferred recorded-fixture maintenance loop (see Repo configuration, and `repo-config/SKILL.md`) once this expands past hand-maintained fixtures — not yet implemented, do not block on it. |
| SMTP / email | Not named in the inventory. No substitute needed. | — |
| Webhook receiver | Not named in the inventory as a distinct component — GitHub App webhook-equivalent traffic is inbound via polling (`agent/src/loop-orchestrator.ts`, `check-*.ts`), not a receiver endpoint. No substitute needed. | — |

**Blockers (no clean local substitute):** none from the inventory's external-dependency
list itself. The chat Postgres CI-wiring gap previously noted here is **resolved on
`main`** (PR #1687, ancestor of this branch's base commit) — the substitute technique
proven for admin/task-store is now also applied to chat, with real DB integration
coverage running in CI for all three Prisma-backed services.

## Canary execution contract

Per Step 4 of `test-design/SKILL.md`, the canary-execution contract
(`${CLAUDE_PLUGIN_ROOT}/skills/canary-execution/SKILL.md`) is included verbatim below.

**Applicability finding (carried forward from Phase 1):** root `CLAUDE.md` declares
`## Deploy model` as **`direct`** — "ships via Helm chart + GHCR-pinned image tags, with
no staging/production GitHub Environments and no deploy-staging→canary→promote-to-production
pipeline." A canary suite validates a **post-deploy, pre-promotion** environment
(`staging`) before promoting to `production`; this repo has no such intermediate
environment for the contract to gate. **The canary execution contract therefore does not
apply to this repo today.** This section documents the contract as reference material
(so a future move to a staged deploy model has zero rediscovery cost) and records what
changes to make it apply — it does not prescribe building `test:canary` now.

### The contract, verbatim (for reference)

- **Entry point:** a dedicated `test:canary` npm script or a dedicated Playwright
  project (e.g. `playwright.canary.config.ts`) — must never reuse `test:smoke` / the
  local smoke runner. CI wires two distinct jobs, `smoke` (pre-merge, boots local deps)
  and `canary` (post-deploy, sets `TEST_TARGET_URL`) — they must not share a script.
- **Mode selection:** every canary-eligible smoke/E2E test reads `TEST_TARGET_URL` at
  process start. Unset → local mode (boot deps inline, hit `localhost`). Set → canary
  mode (skip local boot, hit `$TEST_TARGET_URL` directly, canary-only auth via
  `TEST_AUTH_TOKEN`). Test code is identical; only the setup-fixture layer branches.
- **Eligibility rules (all must hold):** (1) layer ∈ {smoke, E2E}; (2) criticality ∈
  {critical, high}; (3) read-only OR self-cleaning (try/finally teardown even on
  assertion failure); (4) no destructive side effects, canary auth scoped to reject even
  attempts; (5) tolerates eventual consistency via retry-with-backoff; (6) zero
  DB-dependent local setup — a test that boots a local DB is never canary-eligible.
- **Budget:** per-test 95p <5s smoke / <30s E2E; **suite wall-time hard cap 60s total**.
- **Gating:** failing in local mode blocks the PR (local mode is the primary contract).
  Failing in canary mode blocks promotion staging → production. Canary is a **deploy
  gate, never a merge gate**.
- **Canary safety lint:** before promoting a test file to the canary suite, grep it for
  write patterns (`.create`/`.update`/`.delete`/`.insert`/`.upsert`/`POST`/`PUT`/`PATCH`/`DELETE`)
  and classify every match as setup-only-with-teardown, canary-scoped, or unsafe. Any
  unsafe match disqualifies the file — see `canary-execution/SKILL.md` for the full
  step-by-step lint.

### What would need to change for this contract to apply here

1. Stand up a `staging` deploy target (or equivalent pre-production environment) with a
   stable `TEST_TARGET_URL` and a scoped `TEST_AUTH_TOKEN` issuance path.
2. Add a promotion step between "deploy to staging" and "deploy to production" for the
   canary job to gate.
3. Only then: define the critical-path roster (Phase 1's "Critical-path roster (canary
   suite definition)" section, currently empty by design), reserve the already-defined
   `.canary.test.ts` suffix (see `docs/test-readiness/naming.md` — reserved but unused),
   and wire the `smoke` / `canary` CI jobs per the contract above.
4. Until then: **no `test:canary` script, no `.canary.test.ts` files, and no `canary`
   CI job should be created.** The `.canary.test.ts` suffix stays reserved-but-unused in
   `bunfig.toml`'s `pathIgnorePatterns` (already the case) as a naming placeholder only.

**Net effect on this design:** Step 4's usual output (a populated canary CI job, a
`test:canary` script name, a critical-path roster) is replaced with the applicability
finding above. Every other step (frameworks, local substitutes, test-data strategy, CI
pipeline shape, coverage, speed budgets, shared helpers, repo configuration) proceeds
unchanged — none of them depend on canary applying.

## Test-data strategy

- **Per-service DBs vs. shared:** per-service, already the case — each Prisma-backed
  service (`admin`, `task-store`, `chat`) reads its own `DATABASE_URL_SHIPWRIGHT_<SERVICE>`
  (root `CLAUDE.md`'s "Database env vars" table); no shared connection string anywhere.
  This matches the modern-microservice-convention default and requires no change.
- **Real DB per test run vs. transaction rollback:** real DB per test run. Each DB
  integration test's `beforeEach` spins up a brand-new `PrismaClient` (its own pooled
  connection, `connection_limit` bounding the pool) and `afterEach` disconnects it —
  already the documented pattern in `ci.yml`'s comments. Transaction-rollback isolation
  was considered and rejected: it requires wrapping the service layer's own transaction
  boundaries in an outer test transaction, which the current service-layer interfaces
  don't expose, and real-DB-per-run at this test volume (per Phase 1: 3 Prisma-backed
  services, not dozens) doesn't yet justify the added complexity.
- **Factories vs. fixtures:** both, split by purpose — factories (in-line builder
  functions returning valid entity shapes with overridable fields) for DB integration
  tests needing variability across many test cases; hand-authored fixture JSON (the
  existing `src/fixtures/` convention) for recorded-fixture doubles representing a
  specific external-service response shape (stable golden cases, not meant to vary).
- **Seed strategy:** explicit, named seed sets per test file — no reliance on
  migration-time seed data. `scripts/seed-*.ts` (noted in the inventory as dev-only DB
  seeders) are for local `task stack` development, not test seeding; tests seed their
  own state directly via the service layer in `beforeEach`.
- **API-first seeding:** seed through the public API / service layer wherever it exists
  (e.g. `taskService.create(...)` rather than a raw Prisma `create` call in the test),
  per the SKILL's rule that direct DB writes bypass validations and derived-field logic
  that accumulate over time. Direct DB writes are the explicit fallback only for
  corrupt-state or legacy-data tests that the service layer cannot express (e.g.
  `task-store/src/stale-claim-reaper.ts`'s abandoned-claim scenarios, which by
  definition represent a state the service layer's own API wouldn't produce).
- **Eventual consistency:** the inventory found no queue/retry infrastructure in this
  repo (Phase 1, category 4 detail: "no queue/retry infrastructure observed") and no
  async worker/event-bus component. The one async-shaped path is `agent/src/loop-orchestrator.ts`'s
  poll loop (task-store claim → dispatch → status update) and the Slack chat poll loop
  (`chat-svc` queue → agent poll → reply) — both are tested by directly awaiting the
  orchestration function under test with an injected `Clock`, not by polling for
  eventual state, since the "worker" in both cases is the function under test itself,
  not a detached background process. No wait/poll-with-backoff pattern is needed
  anywhere in this repo's test suite today.

## CI pipeline shape

```
┌──────────────────────┐
│  changes (path-diff)  │  gates whether app jobs run at all
└──────────┬─────────────┘
           │
   ┌───────┴────────────────────────────────────────┐
   │                                                  │
┌──▼───────────────────────────┐   ┌──────────────────▼───────┐
│ ci: lint → check-strings →   │   │ site: build → brand-lint  │
│ typecheck → check-config-docs│   │  → Playwright smoke       │
│ → unit+integration+smoke     │   │  (site/*.spec.ts)         │
│  (task test:coverage, all    │   └────────────────────────────┘
│  8 workspaces, one combined  │
│  job) → secret-scan          │   ┌────────────────────────────┐
│  [needs real Postgres        │   │ admin-docker-build          │
│   service container]         │   │ agent-docker-build          │
└──────────┬────────────────────┘   │ metrics-docker-build        │
           │                        └────────────────────────────┘
           │ (needs: ci)
  ┌────────┴─────────┐
  │                   │
┌─▼──────────┐  ┌─────▼────────┐
│ e2e         │  │ admin-e2e    │   ← Playwright, parallel, post-ci
│ (metrics    │  │ (admin UI)   │
│  dashboard) │  │              │
└─────────────┘  └──────────────┘
           │
     merge → main → (direct deploy; no canary/promotion stage)
```

This is the **existing** `.github/workflows/ci.yml` shape, confirmed against the file —
documented here as the target because it already matches the SKILL's prescription
(below) with no changes needed to the shape itself, only to what's inside two of the
boxes (see gaps noted throughout this doc).

- **Layer order:** unit → integration → smoke, all inside the single `ci` job (fail-fast
  is intra-job: `bun test` stops the whole `task test:coverage` run on first failure by
  default within a package, and `task ci`'s step order means lint/typecheck fail before
  the test step ever runs). E2E (`e2e`, `admin-e2e`) runs `needs: ci` — post-merge-gate,
  pre-merge-CI, i.e. still required to merge but only after the unit/integration/smoke
  combined job is green. Content tests run alongside unit inside the same `bun test`
  invocation — no separate CI stage, matching Step 6's guidance that content has no I/O
  boundary distinct enough to warrant its own job.
  **Canary does not appear** — see the Canary execution contract section; there is no
  post-deploy stage in a `direct` deploy model for a canary job to occupy.
- **Fail-fast:** a unit failure fails the whole `ci` job before `e2e`/`admin-e2e` run
  (`needs: ci`), matching "integration failures skip smoke" by construction — they're
  the same job, same process, same `bun test` invocation.
- **Parallelism (per-workspace matrix — evaluated against Step 6's override
  conditions):** the `ci` job runs unit+integration+smoke as **one combined job across
  all 8 workspaces** (`task test:coverage` → `bun test --coverage` at the repo root),
  not a per-workspace matrix. Checked against the three override conditions:
  - **(a) Budget miss:** no — current full-suite wall time comfortably fits the <15min
    PR budget (see Speed budgets below; the repo's own historical full-suite runs are
    on the order of low minutes, well under budget).
  - **(b) Documented flake/slowness history:** two pre-existing full-suite flakes are on
    record (`metrics/` — ~15 pre-existing test failures reproducing on clean `main`, and
    `admin/` — a 1-test flake that varies which test) but neither is isolated by
    workspace-matrix sharding today, and sharding would not fix either (both are
    order/environment flakes, not slowness). Not a justification for a matrix split.
  - **(c) Overhead-justified parallel savings:** no — with 8 workspaces at the current
    per-workspace test volume (per Phase 1: largest single-purpose clusters are still
    well under what would make an 8-way matrix's fixed per-job overhead (checkout,
    `bun install`, Postgres container init × 8) pay for itself 2x over versus one
    combined job's single setup cost).
  
  **Conclusion: keep the single combined `ci` job.** Default to the cheaper shape per
  Step 6 — none of the three override conditions hold. E2E is already split into two
  jobs (`e2e` for metrics, `admin-e2e` for admin) — that split is justified under (b)/(c)
  implicitly (they're different Playwright projects against different running services,
  not a cost-only sharding decision) and is left as-is; it is not a "layer sharded by
  workspace" instance of the pattern Step 6 gates, since each targets a genuinely
  different browser-driven surface.
- **Budget:** <15min total per PR — see Speed budgets below for the measured/target
  breakdown per stage.

## Coverage targets

Single coverage floor per Step 7 — no per-tier CI thresholds. This matches the repo's
existing gate exactly: `scripts/check-coverage.ts` already enforces an aggregate 80/80
line/function floor via `task test:coverage`, with no per-file or per-criticality
carve-out. No change needed to the enforcement mechanism.

| Layer | Minimum CI floor | Notes |
|---|---|---|
| unit | ≥80% line, ≥80% branch | Already enforced repo-wide via `scripts/check-coverage.ts`'s lcov-threshold gate (`task test:coverage`). Critical-path files (`agent/src/work-selector.ts`, `task-store/src/ready.ts`/`blocked-by.ts`/`statuses.ts`, `admin/src/agent-manifest.ts`, etc., per Phase 1) should reach ≥90% — directed by team practice during Phase 5 task prioritization, not a separate CI gate. |
| integration | every named boundary covered | Measured by code path (every `Recorded*Client` / DB-service-layer method has at least one integration test), not by line count. Phase 3 (`/test-migration`) is where this gets audited against the Phase 1 inventory's per-file boundary list. |
| smoke | every public route 200/4xx covered | Route coverage — every Hono route table entry in `metrics/src/api.ts`, `admin/src/agents-api.ts`/`api.ts`, `task-store/src/routes/*.ts`, `chat/src/routes/*.ts`, `mcp-server/src/mcp-server.ts` needs at least a happy-path 200 and one representative 4xx (auth failure or validation failure) smoke test. |
| E2E | every top-5 journey covered | Phase 1 identified 4 journeys (below the top-5 threshold — this product surface doesn't have 5). Only journey #4 (marketing-site visitor) is genuinely browser-driven; #1–#3 are "e2e / smoke-composed" (cross-service integration + fixtures, no browser) per Phase 1's finding — journey coverage for those three means integration-test coverage of the multi-step state transition, not a Playwright spec. |

**Why a single floor (per SKILL Step 7):** per-tier thresholds require a maintained
criticality mapping in CI that decays as files are added/moved/reweighted. A single
floor enforced by `scripts/check-coverage.ts`'s built-in lcov parsing avoids that
maintenance burden while criticality still drives *what gets written first* during
Phase 4/5 planning.

## Speed budgets

Per Step 8, the default budget table from `speed-budgets/SKILL.md`, included verbatim —
this repo does not tighten any budget below the default.

| Layer | Per-test 95p | Per-test hard cap | Suite target |
|---|---|---|---|
| Unit | <50 ms | <200 ms | <30 s |
| Integration | <2 s | <10 s | <3 min |
| Smoke | <5 s | <15 s | <30 s sequential / <60 s full |
| E2E | <30 s | <90 s | <10 min |
| Full PR pipeline | — | — | **<15 min** |
| Canary suite | — | — | **<60 s** (not applicable — see Canary execution contract) |

**Parallelization plan:**

- **Unit + integration + smoke (the combined `ci` job):** Bun's test runner
  parallelizes within a single process by default; no explicit worker-count flag is
  set in `task test` / `task test:coverage` today, and none is being newly prescribed —
  the single-job shape (see CI pipeline shape above) means the whole layer stack runs
  as one `bun test` invocation with Bun's own internal concurrency, not a CI-level
  worker matrix. This is sufficient at current suite size to hit the <15min full-PR
  budget with headroom (Bun's runner reports low-minutes wall time for this repo's
  ~267 source files' worth of tests).
- **E2E (`e2e`, `admin-e2e`):** two separate CI jobs, run in parallel with each other
  (both `needs: ci`, no dependency between them) — this is the sharding, and it's
  justified because they exercise genuinely different Playwright projects/servers, not
  a cost-only split of one logical suite. Within each job, Playwright's own
  default worker parallelism applies (no custom `workers:` override observed in
  `metrics/playwright.config.ts` / `admin/playwright.config.ts` / `site/playwright.config.ts`
  — default to CPU-count-based parallelism, which is sufficient at 1-2 spec files per
  surface today).
- **If suite growth later threatens the <15min budget:** re-evaluate the per-workspace
  matrix override conditions in the CI pipeline shape section above — specifically
  condition (a) (budget miss) is the trigger to revisit, not a fixed test-count
  threshold.

## Shared helpers / utilities to build

Per Step 9 — most of these already exist; only additions/extensions are called out
explicitly.

- **HTTP request builders (auth tokens, default headers)** — exists per-service already
  (e.g. `admin/src/api-auth.ts`, `task-store/src/auth.ts`, `chat/src/auth.ts` define the
  auth contract; smoke tests build request headers directly against `app.request()`).
  No new shared cross-workspace helper needed — auth shapes differ enough per service
  (session-or-bearer for admin, bearer-only for task-store/chat) that a shared builder
  would abstract away meaningful differences.
- **Test-user / test-agent factory** — needed for `admin`'s Agent/Env/Cron/Tool/Token
  CRUD integration tests and `task-store`'s Task/PullRequest/TaskToken tests. Build as
  in-line builder functions (per the factories-vs-fixtures split above) colocated with
  each service's `*.integration.test.ts` files, not a shared cross-workspace package —
  the entity shapes are service-specific (Prisma models differ per schema).
  **New, not yet confirmed to exist** — flag for Phase 3 to verify.
- **DB reset helper** — `beforeEach`/`afterEach` PrismaClient connect/disconnect pattern
  already documented in `ci.yml`'s comments (real DB per test run, no rollback). No
  shared truncate-helper needed if each test seeds only what it needs and Prisma's
  cascading FK constraints keep state scoped — Phase 3 should confirm there's no
  cross-test pollution today given 3 separate schemas.
- **Time freezer (`Clock` injection)** — already a hard rule in root `CLAUDE.md`'s Test
  isolation section ("inject time via a `Clock` interface... `FixedClock(t)` in tests").
  Already implemented per-workspace (`agent/src/clock.ts`, `plugins/shipwright/scripts/clock.ts`,
  `metrics/src/lib/clock.ts` per Phase 1). No new work — this is the strongest
  already-enforced convention in the repo.
- **Recorded-fixture loader** — already exists as the `Recorded*Client` pattern
  (`admin/src/kubernetes-client.ts`'s `RecordedKubernetesClient`,
  `metrics/src/providers/task-store-recorded.ts`, `agent/src/shipwright-config-client.ts`)
  plus per-workspace `src/fixtures/` directories. This design extends the pattern to
  every remaining `Http*Client` in the Local execution architecture table above — same
  loader shape, more fixture files, no new infrastructure.

## Repo configuration

Per Step 10 (`repo-config/SKILL.md`). Without this section, the test pipeline above is
advisory — CI can run and still not gate `main`.

### Branch protection (`main`)

- **Required PR** — no direct pushes (assume already true; verify in Phase 4 against the
  live GitHub branch-protection API, not assumed from this doc).
- **Required status checks:** `ci` (lint/typecheck/unit/integration/smoke/secret-scan),
  `site` (build/brand-lint/Playwright), `admin-docker-build`, `agent-docker-build`,
  `metrics-docker-build`, `e2e`, `admin-e2e` — every job in `.github/workflows/ci.yml`
  that isn't itself gated to skip on version-bump-only changes (the `changes` job's
  `app==true` condition is fine to leave as-is; a required check that's skipped due to
  the path filter still reports a passing/neutral state to the merge gate, which is the
  intended fail-open behavior documented in `ci.yml`'s own comments).
- **Require branches up to date before merging** — yes.
- **Require conversation resolution before merging** — yes.
- **Required approving reviews:** ≥1 — small team; ≥2 would deadlock a repo whenever
  the sole other reviewer is the PR author. Keep at 1.
- **Enforce on admins:** yes, recommended — document a break-glass procedure (direct
  push only for a CI outage or emergency rollback, immediately followed by a
  retroactive PR) rather than leaving admins permanently exempt.
- **Canary status check:** not applicable / not included — see Canary execution
  contract section; there is no canary job to include or exclude.

### Required secrets

- `TEST_CANARY_API_KEY` — **not required today.** Per the Canary execution contract
  section, canary doesn't apply under the `direct` deploy model. Do not provision this
  secret until a staging environment exists.
- Per-external-service auth tokens for **recording** new fixtures (not for running
  tests — recorded fixtures replay offline): a GitHub App token (for `RecordedGithubClient`
  recordings), a K8s service-account token (for `RecordedKubernetesClient` recordings),
  Groq/ElevenLabs API keys (for `agent/src/voice.ts` recordings), Google OAuth2 test
  credentials (for `admin/src/google-auth-client.ts` recordings), Slack app credentials
  (for `admin/src/slack-provisioning-client.ts` recordings). These are **not currently
  needed as GitHub Secrets** unless/until the deferred recorded-fixture maintenance loop
  (see below) is implemented — today, fixture recording/updates are a manual,
  local, one-off developer action, not a CI-triggered one.
- Deploy credentials (GHCR push token, Helm/K8s cluster credentials) — already exist per
  the repo's `direct` deploy model; out of scope for this test-system doc (owned by the
  deploy pipeline, not the test pipeline).

### Required GitHub Environments

- **`staging` / `production`:** **not applicable under the current `direct` deploy
  model.** The repo's `CLAUDE.md` explicitly states no staging/production GitHub
  Environments exist. Do not create these as part of test-readiness work — that would
  be scope creep into the deploy pipeline, which is out of this document's charter.
- **`recorded-fixture-refresh`:** deferred, not created — see below. Only relevant once
  the recorded-fixture maintenance loop is actually implemented (currently a documented
  target design per `repo-config/SKILL.md`, explicitly not built).

### PR template

Add `.github/pull_request_template.md` with a Closing Checklist requiring the
verification-command output (e.g. `task ci` output, or a CI run link) to be pasted into
the PR description before merge. Not currently present in this repo (no
`.github/pull_request_template.md` found) — a Phase 4 task.

### Recorded-fixture maintenance loop — deferred

Per `repo-config/SKILL.md` §5: **not implemented.** This repo already has several
recorded-fixture doubles in production use (`RecordedKubernetesClient`,
`metrics/src/providers/task-store-recorded.ts`, `agent/src/shipwright-config-client.ts`)
maintained by hand. Do not wire up the scheduled auto-recording GitHub Actions workflow
described in `repo-config/SKILL.md` until the manual, hand-authored-fixture workflow
this design extends (more `Recorded*Client` doubles per the Local execution architecture
table above) is running and tuned across all the newly-covered clients. Flagging this
explicitly so Phase 4 doesn't schedule "build the fixture-refresh cron" alongside the
initial fixture-authoring tasks — sequence it after.

## Gaps and blockers summary

For Phase 4 (`test-roadmap`) triage — every item below is a **design decision or gap**,
not an existing-test audit finding (that's Phase 3):

1. ~~Chat service has no DB-integration CI wiring~~ **Resolved on `main`** (PR #1687,
   ancestor of this branch's base commit) — `ci.yml`'s Postgres service container now
   also creates `shipwright_chat_test` and runs
   `prisma migrate deploy --schema=chat/prisma/schema.prisma`, and the chat integration
   test files (`chat/src/{message,thread,token}-service.integration.test.ts`) already
   exist. No further action needed here.
2. **`.github/pull_request_template.md` does not exist.** Add per Step 10.
3. **Recorded-fixture coverage is partial.** Three `Recorded*Client` implementations
   exist; the inventory names roughly a dozen more `Http*Client`/SDK-wrapper modules
   that need the same pattern extended to them (full list in the Local execution
   architecture table above).
4. **Canary execution contract is fully deferred**, contingent on a future staged-deploy
   model — not a gap in this design, a correct non-applicability finding carried
   forward from Phase 1 and re-confirmed here after reading the full canary contract.
5. **Branch-protection required-checks list should be verified against the live GitHub
   API**, not assumed from `ci.yml`'s job list alone — Phase 4/Step 10's pairing rule
   depends on this being accurate.

## Next step

Run `/test-migration` (Phase 3) to reconcile this blueprint against the existing test
suite — which tests already satisfy this design, which need to move layers, which need
to be rebuilt, and which gaps above already have partial coverage this design doc
wasn't able to see (Phase 2 does not read existing tests, per the SKILL's failure-modes
guidance).
