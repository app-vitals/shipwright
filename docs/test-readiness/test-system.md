# Test System Design

> Phase 2 of the test-readiness pipeline. The greenfield blueprint for the ideal test
> system that would satisfy the Phase 1 inventory
> ([`test-inventory.md`](./test-inventory.md)). Does NOT account for existing tests —
> that's Phase 3 (`/test-migration`).
>
> Generated by `/test-design` on 2026-07-25 against `app-vitals/shipwright` (worktree:
> `docs/test-readiness-refresh-20260725`), consuming the Phase 1 inventory refreshed at
> `24bf90f3`.

## Stack profile (from inventory)

- **Language(s):** TypeScript (Bun runtime), plus Astro/Tailwind for the marketing site.
- **Package manager:** Bun workspaces (`bun.lock`), 8-workspace monorepo (`lib`,
  `plugins/shipwright`, `metrics`, `agent`, `admin`, `task-store`, `chat`, `mcp-server`)
  plus non-workspace supporting surfaces (`site/`, `brand/`, `scripts/`, `charts/` Helm
  chart, `demo/`, `agent-types/`).
- **Primary framework(s):** Hono (all HTTP services), Prisma + Postgres (admin,
  task-store, chat), Zod / `@hono/zod-openapi`, Astro (site), Playwright (site + admin +
  metrics E2E), Bun's built-in test runner throughout.
- **Deploy model:** `direct` (root `CLAUDE.md`) — Helm chart + GHCR-pinned image tags,
  no staging/production GitHub Environments, no deploy-staging→canary→promote pipeline.
  Unchanged since the last design pass. This shapes the canary contract section and the
  repo-configuration section below.
- **Repo's own layer convention (used verbatim, per CLAUDE.md fallback):** this repo
  already declares five concrete layer names by filename suffix — `*.unit.test.ts`,
  `*.integration.test.ts`, `*.smoke.test.ts`, `*.spec.ts` (site only) / `*.e2e.ts`
  (`metrics/e2e/`, `admin/e2e/`), and `*.content.test.ts` — mapping cleanly onto the
  rubric's unit/integration/smoke/e2e plus a repo-specific fifth "content" layer for
  markdown/prompt assertions. This design reuses those names rather than introducing the
  rubric's generic labels.
- **Delta since the 2026-07-21 design (this pass, 32 commits landed against Phase 1's
  refreshed inventory):** the headline change is the **Agent Type manifest system**
  landing end-to-end — `admin/src/agent-type-registry.ts` (Zod schema + `.strict()`
  trust-boundary validation + JSON-Schema generator, new category-1/unit item) and
  `admin/src/agent-type-manifest-loader.ts` (`AgentTypeRegistry` — resolves `typeName` →
  parsed manifest from `agent-types/{typeName}/manifest.yaml`, new category-2/integration
  item — real file I/O) replace the previously hardcoded `admin/src/system-crons.ts` and
  `lib/agent-default-tools.ts` (**both deleted this cycle**). The admin UI/API now
  require a `typeName` at agent-creation time, with the "coding" manifest as the
  universal fallback. This materially touches the **agent provisioning** user journey —
  worth a fresh E2E/integration pass rather than a fixture refresh, per Phase 1. `scripts/hitl.ts`
  grew from a thin wrapper to a 739-line full worker loop (workspace provisioning +
  service boot + polling + Claude Code dispatch), promoted to its own user-journey row
  in the inventory and flagged for a criticality re-review (currently medium, dev-only).
  `agent/src/check-review.ts` gained a bundle-completeness gate (D2198) — no layer
  change. `scripts/generate-agent-type-schema.ts` and `scripts/wait-for-agent.ts` are new,
  both category-2 dev/codegen tooling. **None require a new framework, local-substitute
  pattern, or CI-shape change** — every addition slots into the matrix and substitution
  table below unchanged; only the Local execution architecture, Shared helpers, and
  Coverage-targets sections need new rows/callouts for the manifest system.

## Framework matrix per layer

The stack-matrix default for "TypeScript (Bun)" is `bun test` for unit/integration,
supertest/Hono-test-client for smoke, Playwright for E2E. The repo already implements
all four choices in production, so no override is needed anywhere — the design below
documents the existing choices as the target, not a hypothetical.

| Layer | Framework | Rationale |
|---|---|---|
| Unit | `bun test` | Matches the stack matrix default for TypeScript (Bun). Already the repo's exclusive unit runner (`*.unit.test.ts`, no `pathIgnorePatterns` entry — discovered by the default root scan). No I/O, no framework boot — Bun's runner is fast enough that no alternative is justified. New unit-layer item this pass: `admin/src/agent-type-registry.ts` (Zod schema definition + `.strict()` parse/validate + JSON-Schema generator — pure given an in-memory YAML-derived object, no I/O of its own; the file read happens in the loader, category 2). High criticality per Phase 1 — a manifest-schema regression silently breaks every agent-type resolution downstream. |
| Integration | `bun test` + recorded-fixture doubles (hand-authored JSON, e.g. `RecordedKubernetesClient`, `RecordedTaskStoreClient`, `RecordedGithubClient`) for internal/third-party HTTP; **real Postgres via a GitHub Actions `postgres:16` service container** for all three Prisma-backed DB integration suites (admin, task-store, chat) | Matches the stack matrix default (`bun test` + testcontainers-equivalent). The repo already runs real Postgres for DB integration tests via a CI service container rather than testcontainers proper — functionally equivalent (real DB per test run) and cheaper in CI. Confirmed still wired in `.github/workflows/ci.yml`: `DATABASE_URL_ADMIN_TEST`, `DATABASE_URL_SHIPWRIGHT_TASK_STORE_TEST`, and `DATABASE_URL_SHIPWRIGHT_CHAT` are all present against the single `postgres:16` service container. New integration-layer item this pass: `admin/src/agent-type-manifest-loader.ts` (`AgentTypeRegistry` — real file I/O reading `agent-types/{typeName}/manifest.yaml`, no DB, no network) — needs a real-filesystem-backed integration test asserting both the happy-path resolve and the documented fallback-to-"coding" behavior on an unknown/missing `typeName`; no new infra required, fits the existing injected-dependency pattern (`admin/src/agent-cron-jobs.ts` now takes an injected `AgentTypeManifestResolver`, so its own tests can inject a fake resolver rather than touching the filesystem). |
| Smoke | `bun test` + Hono `app.request()` (in-process, no real socket); `Bun.serve()` + `fetch()` only where no Hono app-factory seam exists (e.g. `agent/src/health.ts`) | Matches the stack matrix's "supertest / Hono test client" cell. `app.request()` is strictly cheaper than supertest (no socket, no port allocation) and is already the repo's documented convention (`docs/testing.md`). No new smoke-layer items this pass — `admin/src/agents-api.ts` and `admin/src/admin-ui.ts`/`admin-ui-pages.ts` gained `typeName`/`missingRequiredEnv` fields and the required agent-type picker respectively, but both are additive changes to already-smoke-tested routes/pages, not new route surface. |
| E2E | Playwright (`@playwright/test`) | Matches the stack matrix default. Already wired for three surfaces: `site/` (`site/playwright.config.ts`, `*.spec.ts`), `metrics/` (`metrics/playwright.config.ts`, `metrics/e2e/*.e2e.ts`), `admin/` (`admin/playwright.config.ts`, `admin/e2e/*.e2e.ts`). All three are excluded from the root `bun test` scan via `bunfig.toml` `pathIgnorePatterns` and run via dedicated `task e2e` / `task e2e:admin` / `cd site && npm test` entry points. The agent-provisioning journey's `admin/e2e/` suite is the one most directly affected by this cycle's manifest-driven redesign — the new-agent form's required `typeName` picker changes the happy-path flow through that suite (see Coverage targets / Gaps below). |
| Content (repo-specific fifth layer) | `bun test` | Not in the rubric's stack matrix — a repo-specific layer for markdown/prompt-content assertions (commands, skills, agent prompts, reference docs, and declarative manifest data). No I/O boundary beyond static file reads (`existsSync`/`readFileSync`); no separate framework needed. Runs in the same `bun test` invocation as unit (no `pathIgnorePatterns` entry, discovered by default). `agent-types/coding/manifest.yaml` (now the sole source of truth for default crons/plugins/tools, per this cycle's deletions) keeps its existing `.content.test.ts` sibling asserting structure/wording — no new content-layer work needed, just re-confirmed still load-bearing. `agent-admin`, `error-scan`, and `test-readiness` `SKILL.md` bodies were edited this cycle (small diffs); existing content tests for those skills cover structure/frontmatter presence, not full prose — no gap introduced by small wording edits. |

## Local execution architecture

Every external dependency named in the Phase 1 inventory, with its local substitute per
the SKILL's substitution table. No test in this system may require a hosted external
service or a developer manually starting one.

| Dependency | Local substitute | Setup file |
|---|---|---|
| Postgres (admin, task-store, chat) | Real DB per test run — GitHub Actions `postgres:16` service container in CI; `task db:provision` (`prisma migrate deploy`) locally against a developer-run Postgres. Pattern already in place for all three services, confirmed in the current `ci.yml`: Prisma calls are wrapped in a typed service layer; integration tests hit real Postgres through that layer; unit tests never touch Prisma. No change this pass — the Agent Type manifest system reads YAML from disk, not from Postgres, so it doesn't add a new DB-boundary surface. | `.github/workflows/ci.yml` (`services.postgres`, `DATABASE_URL_ADMIN_TEST`, `DATABASE_URL_SHIPWRIGHT_TASK_STORE_TEST`, `DATABASE_URL_SHIPWRIGHT_CHAT`); local equivalent documented in `docs/task-store.md` / `docs/agent.md` / `docs/chat.md`. |
| Local filesystem (Agent Type manifests) | **New this pass.** `admin/src/agent-type-manifest-loader.ts` reads `agent-types/{typeName}/manifest.yaml` directly off disk — not a hosted dependency, but still a real I/O boundary that must not be mocked at the `fs` level in a way that hides a malformed-YAML or missing-file regression. Local substitute: exercise the loader against real fixture manifest directories checked into the test tree (e.g. a temp dir with a valid manifest, a temp dir with a deliberately malformed one, and a temp dir with a missing `typeName`) rather than mocking `readFileSync`/`readdir` — this is cheap, deterministic, and tests the actual YAML-parsing + Zod-validation path end to end. `admin/src/agent-cron-jobs.ts` and `scripts/seed-dev-agent.ts`, which now consume the loader via an injected `AgentTypeManifestResolver` interface, mock *that* interface in their own unit/integration tests rather than touching the filesystem — matches this SKILL's "prefer mocking the client interface over the wire/IO protocol" rule. | `admin/src/fixtures/agent-types/` (new directory to create — mirrors the `agent-types/` layout at repo root with a small set of fixture manifests: one valid, one malformed, one missing-required-field). |
| Redis | Not named in the inventory — no service in this repo uses Redis. No substitute needed. | — |
| S3 / blob storage | Not named in the inventory. No substitute needed. | — |
| Internal HTTP service (task-store client, GitHub App API, K8s API, chat-service client, accounts client, admin-metrics client) | Recorded fixture doubles (hand-authored JSON + a `Recorded*Client` implementing the same interface as the `Http*Client`), per the repo's existing pattern — confirmed still present for `admin/src/kubernetes-client.ts` (`RecordedKubernetesClient`), `metrics/src/providers/task-store-recorded.ts`, `metrics/src/fixtures/task-store-fixtures.ts`, `agent/src/shipwright-config-client.ts`. This design extends the same pattern to every remaining `Http*Client` named in the inventory (`metrics/src/lib/task-store-client.ts`, `metrics/src/lib/accounts-client.ts`, `metrics/src/lib/admin-metrics-client.ts`, `agent/src/github-app-auth.ts`, `agent/src/http-chat-service-client.ts`, `agent/src/shipwright-runtime-client.ts`, `agent/src/chat-poller.ts`'s underlying chat-service client, `admin/src/*-provisioning-client.ts`, `mcp-server/src/tool-caller.ts`). No new items this pass. | `<workspace>/src/fixtures/` — already exists per-workspace (`admin/src/fixtures`, `metrics/src/fixtures`, `agent/src/fixtures`, `plugins/shipwright/tests/fixtures`, `plugins/shipwright/scripts/test-helpers/fixtures`). No new directory needed beyond the manifest fixtures row above; new fixtures land alongside existing ones. |
| Third-party HTTP API (Sentry SDK init, Slack `apps.manifest.create`, Google OAuth2, GitHub App tokens, Groq Whisper / ElevenLabs) | Recorded fixture doubles — never live. Same `Recorded*Client` pattern as internal HTTP. `lib/sentry.ts`'s scrub-rule logic is unit-testable directly (pure); its SDK `init()` wiring is integration-tested against a recorded/no-op transport, never a live Sentry project. `agent/src/piper-voice.ts` stays out of this row today — it only validates/resolves local `.onnx` file paths with an injectable `readdir`, no network call; move it into this row once Piper synthesis lands (carried forward from Phase 1's Ambiguous items, unchanged this cycle). | Per-workspace `src/fixtures/`, same as above. Requires the deferred recorded-fixture maintenance loop (see Repo configuration, and `repo-config/SKILL.md`) once this expands past hand-maintained fixtures — not yet implemented, do not block on it. |
| SMTP / email | Not named in the inventory. No substitute needed. | — |
| Webhook receiver | Not named in the inventory as a distinct component — GitHub App webhook-equivalent traffic is inbound via polling (`agent/src/loop-orchestrator.ts`, `check-*.ts`) and the chat-service poll loop (`agent/src/chat-poller.ts`), not a receiver endpoint. No substitute needed. | — |

**Blockers (no clean local substitute):** none from the inventory's external-dependency
list itself. Every dependency named in Phase 1 — including the new local-filesystem
manifest-read boundary — has a working local substitute either already implemented or a
direct extension of an already-implemented pattern.

## Canary execution contract

Per Step 4 of `test-design/SKILL.md`, the canary-execution contract
(`${CLAUDE_PLUGIN_ROOT}/skills/canary-execution/SKILL.md`) is included verbatim below.

**Applicability finding (carried forward from Phase 1, re-confirmed this pass):** root
`CLAUDE.md` declares `## Deploy model` as **`direct`** — "ships via Helm chart +
GHCR-pinned image tags, with no staging/production GitHub Environments and no
deploy-staging→canary→promote-to-production pipeline." A canary suite validates a
**post-deploy, pre-promotion** environment (`staging`) before promoting to
`production`; this repo has no such intermediate environment for the contract to gate.
**The canary execution contract therefore does not apply to this repo today.** This
section documents the contract as reference material (so a future move to a staged
deploy model has zero rediscovery cost) and records what changes to make it apply — it
does not prescribe building `test:canary` now.

### The contract, verbatim (for reference)

- **Entry point:** a dedicated `test:canary` npm script or a dedicated Playwright
  project (e.g. `playwright.canary.config.ts`) — must never reuse `test:smoke` / the
  local smoke runner. CI wires two distinct jobs, `smoke` (pre-merge, boots local deps)
  and `canary` (post-deploy, sets `TEST_TARGET_URL`) — they must not share a script.
- **Mode selection:** every canary-eligible smoke/E2E test reads `TEST_TARGET_URL` at
  process start. Unset → local mode (boot deps inline, hit `localhost`). Set → canary
  mode (skip local boot, hit `$TEST_TARGET_URL` directly, canary-only auth via
  `TEST_AUTH_TOKEN`). Test code is identical; only the setup-fixture layer branches.
- **Eligibility rules (all must hold):** (1) layer ∈ {smoke, E2E}; (2) criticality ∈
  {critical, high}; (3) read-only OR self-cleaning (try/finally teardown even on
  assertion failure); (4) no destructive side effects, canary auth scoped to reject even
  attempts; (5) tolerates eventual consistency via retry-with-backoff; (6) zero
  DB-dependent local setup — a test that boots a local DB is never canary-eligible.
- **Budget:** per-test 95p <5s smoke / <30s E2E; **suite wall-time hard cap 60s total**.
- **Gating:** failing in local mode blocks the PR (local mode is the primary contract).
  Failing in canary mode blocks promotion staging → production. Canary is a **deploy
  gate, never a merge gate**.
- **Canary safety lint:** before promoting a test file to the canary suite, grep it for
  write patterns (`.create`/`.update`/`.delete`/`.insert`/`.upsert`/`POST`/`PUT`/`PATCH`/`DELETE`)
  and classify every match as setup-only-with-teardown, canary-scoped, or unsafe. Any
  unsafe match disqualifies the file — see `canary-execution/SKILL.md` for the full
  step-by-step lint.

### What would need to change for this contract to apply here

1. Stand up a `staging` deploy target (or equivalent pre-production environment) with a
   stable `TEST_TARGET_URL` and a scoped `TEST_AUTH_TOKEN` issuance path.
2. Add a promotion step between "deploy to staging" and "deploy to production" for the
   canary job to gate.
3. Only then: define the critical-path roster (Phase 1's "Critical-path roster (canary
   suite definition)" section, currently empty by design), reserve the already-defined
   `.canary.test.ts` suffix (see `docs/test-readiness/naming.md` — reserved but unused),
   and wire the `smoke` / `canary` CI jobs per the contract above.
4. Until then: **no `test:canary` script, no `.canary.test.ts` files, and no `canary`
   CI job should be created.** The `.canary.test.ts` suffix stays reserved-but-unused in
   `bunfig.toml`'s `pathIgnorePatterns` (already the case) as a naming placeholder only.

**Net effect on this design:** Step 4's usual output (a populated canary CI job, a
`test:canary` script name, a critical-path roster) is replaced with the applicability
finding above. Every other step (frameworks, local substitutes, test-data strategy, CI
pipeline shape, coverage, speed budgets, shared helpers, repo configuration) proceeds
unchanged — none of them depend on canary applying, and the Agent Type manifest system
landing this cycle doesn't change that (it's a provisioning-time concern, not a
deployed-environment concern).

## Test-data strategy

- **Per-service DBs vs. shared:** per-service, already the case — each Prisma-backed
  service (`admin`, `task-store`, `chat`) reads its own `DATABASE_URL_SHIPWRIGHT_<SERVICE>`
  (root `CLAUDE.md`'s "Database env vars" table); no shared connection string anywhere.
  This matches the modern-microservice-convention default and requires no change.
- **Real DB per test run vs. transaction rollback:** real DB per test run. Each DB
  integration test's `beforeEach` spins up a brand-new `PrismaClient` (its own pooled
  connection, `connection_limit` bounding the pool) and `afterEach` disconnects it —
  already the documented pattern in `ci.yml`'s comments. Transaction-rollback isolation
  was considered and rejected: it requires wrapping the service layer's own transaction
  boundaries in an outer test transaction, which the current service-layer interfaces
  don't expose, and real-DB-per-run at this test volume (per Phase 1: 3 Prisma-backed
  services, not dozens) doesn't yet justify the added complexity. Unchanged this pass.
- **Factories vs. fixtures:** both, split by purpose — factories (in-line builder
  functions returning valid entity shapes with overridable fields) for DB integration
  tests needing variability across many test cases; hand-authored fixture JSON/YAML (the
  existing `src/fixtures/` convention) for recorded-fixture doubles and — new this pass —
  Agent Type manifest fixtures representing stable golden cases (a valid manifest, a
  malformed one, a missing-field one), not meant to vary across test cases.
- **Seed strategy:** explicit, named seed sets per test file — no reliance on
  migration-time seed data. `scripts/seed-*.ts` (dev-only DB seeders; `seed-dev-agent.ts`
  materially changed this cycle to resolve Plugin/Tool rows from the Agent Type manifest
  instead of hardcoded defaults) are for local `task stack` development, not test
  seeding; tests seed their own state directly via the service layer in `beforeEach`.
  The manifest-loader's own tests seed via real fixture manifest files (see Local
  execution architecture above), not via the DB.
- **API-first seeding:** seed through the public API / service layer wherever it exists
  (e.g. `taskService.create(...)` rather than a raw Prisma `create` call in the test),
  per the SKILL's rule that direct DB writes bypass validations and derived-field logic
  that accumulate over time. Direct DB writes are the explicit fallback only for
  corrupt-state or legacy-data tests that the service layer cannot express (e.g.
  `task-store/src/stale-claim-reaper.ts`'s abandoned-claim scenarios, and any test
  exercising `lib/claim-ttl.ts`'s boundary constants against a claim artificially aged
  past `DEFAULT_CLAIM_TTL_MS`). For the new agent-creation flow, "API-first" means
  creating an agent via `admin/src/agents-api.ts`'s CRUD endpoint with a real `typeName`,
  not hand-inserting an `Agent` row with a `typeName` that bypasses the registry's
  validation — the whole point of this cycle's change is that `typeName` is now a
  required, validated field, and tests should exercise that validation path rather than
  route around it.
- **Eventual consistency:** the inventory found no queue/retry infrastructure in this
  repo and no async worker/event-bus component — unchanged this cycle. The two
  async-shaped paths (`agent/src/loop-orchestrator.ts`'s poll loop and
  `agent/src/chat-poller.ts`'s poll loop) are tested by directly awaiting the
  orchestration function under test with an injected `Clock`, not by polling for
  eventual state. The Agent Type manifest resolution path is synchronous file I/O (no
  async worker involved), so it introduces no new eventual-consistency test pattern
  either. No wait/poll-with-backoff pattern is needed anywhere in this repo's test suite
  today.

## CI pipeline shape

```
┌──────────────────────┐
│  changes (path-diff)  │  gates whether app jobs run at all
└──────────┬─────────────┘
           │
   ┌───────┴────────────────────────────────────────┐
   │                                                  │
┌──▼───────────────────────────┐   ┌──────────────────▼───────┐
│ ci: lint → check-strings →   │   │ site: build → brand-lint  │
│ typecheck → check-config-docs│   │  → Playwright smoke       │
│ → unit+integration+smoke     │   │  (site/*.spec.ts)         │
│  (task test:coverage, all    │   └────────────────────────────┘
│  8 workspaces, one combined  │
│  job) → secret-scan          │   ┌────────────────────────────┐
│  [needs real Postgres        │   │ admin-docker-build          │
│   service container:         │   │ agent-docker-build          │
│   admin+task-store+chat]     │   │ metrics-docker-build        │
└──────────┬────────────────────┘   └────────────────────────────┘
           │
           │ (needs: ci)
  ┌────────┴─────────┐
  │                   │
┌─▼──────────┐  ┌─────▼────────┐
│ e2e         │  │ admin-e2e    │   ← Playwright, parallel, post-ci
│ (metrics    │  │ (admin UI)   │
│  dashboard) │  │              │
└─────────────┘  └──────────────┘
           │
     merge → main → (direct deploy; no canary/promotion stage)
```

This is the **existing** `.github/workflows/ci.yml` shape, re-confirmed against the
current file this pass (job list: `changes`, `ci`, `site`, `admin-docker-build`,
`agent-docker-build`, `metrics-docker-build`, `e2e`, `admin-e2e` — identical to the last
design pass) — documented here as the target because it already matches the SKILL's
prescription (below) with no changes needed to the shape itself. The Agent Type manifest
system landing this cycle adds test volume inside the existing `ci` job (unit +
integration) and inside `admin-e2e` (the new-agent form's required picker); it does not
require a new job or a new job dependency edge.

- **Layer order:** unit → integration → smoke, all inside the single `ci` job (fail-fast
  is intra-job: `bun test` stops the whole `task test:coverage` run on first failure by
  default within a package, and `task ci`'s step order means lint/typecheck fail before
  the test step ever runs). E2E (`e2e`, `admin-e2e`) runs `needs: ci` — post-merge-gate,
  pre-merge-CI, i.e. still required to merge but only after the unit/integration/smoke
  combined job is green. Content tests run alongside unit inside the same `bun test`
  invocation — no separate CI stage, matching Step 6's guidance that content has no I/O
  boundary distinct enough to warrant its own job.
  **Canary does not appear** — see the Canary execution contract section; there is no
  post-deploy stage in a `direct` deploy model for a canary job to occupy.
- **Fail-fast:** a unit failure fails the whole `ci` job before `e2e`/`admin-e2e` run
  (`needs: ci`), matching "integration failures skip smoke" by construction — they're
  the same job, same process, same `bun test` invocation.
- **Parallelism (per-workspace matrix — evaluated against Step 6's override
  conditions):** the `ci` job runs unit+integration+smoke as **one combined job across
  all 8 workspaces** (`task test:coverage` → `bun test --coverage` at the repo root),
  not a per-workspace matrix. Checked against the three override conditions:
  - **(a) Budget miss:** no — current full-suite wall time (34.23s measured baseline,
    see Speed budgets below) comfortably fits the <15min PR budget. The Agent Type
    manifest system adds a small, bounded number of new tests (schema validation, loader
    resolve/fallback, injected-resolver consumers) — not enough to threaten the budget.
  - **(b) Documented flake/slowness history:** two pre-existing full-suite flakes remain
    on record (`metrics/` — ~15 pre-existing test failures reproducing on clean `main`,
    and `admin/` — a 1-test flake that varies which test) but neither is isolated by
    workspace-matrix sharding today, and sharding would not fix either (both are
    order/environment flakes, not slowness). Not a justification for a matrix split.
  - **(c) Overhead-justified parallel savings:** no — with 8 workspaces at the current
    per-workspace test volume, an 8-way matrix's fixed per-job overhead (checkout,
    `bun install`, Postgres container init × 8) still would not pay for itself 2x over
    versus one combined job's single setup cost.

  **Conclusion: keep the single combined `ci` job.** Default to the cheaper shape per
  Step 6 — none of the three override conditions hold. E2E is already split into two
  jobs (`e2e` for metrics, `admin-e2e` for admin) — that split is justified under (b)/(c)
  implicitly (they're different Playwright projects against different running services,
  not a cost-only sharding decision) and is left as-is.
- **Budget:** <15min total per PR — see Speed budgets below for the measured/target
  breakdown per stage.

### Naming convention and runner-exclusion config

Per Step 6's requirement that a naming convention and its enforcing exclusion config are
inseparable — both already exist in this repo, confirmed against `docs/test-readiness/naming.md`
and `bunfig.toml`, unchanged this pass:

1. **File-naming convention** (repo's own five-suffix scheme, per CLAUDE.md):
   - `*.unit.test.ts` — unit
   - `*.integration.test.ts` — integration
   - `*.smoke.test.ts` — smoke
   - `*.spec.ts` (site only) / `*.e2e.ts` (metrics/admin) — e2e
   - `*.content.test.ts` — content
   - `*.canary.test.ts` — **reserved, unused** (canary contract not applicable — see above)
2. **Runner-exclusion config:**
   - `bun test` (unit/integration/smoke/content) discovers by default root scan; no
     `pathIgnorePatterns` entry needed for these four.
   - `bunfig.toml`'s `pathIgnorePatterns` excludes `site/**`, `metrics/e2e/**`,
     `admin/e2e/**`, and `**/*.canary.test.ts` from the root `bun test` scan — confirmed
     unchanged this pass.
   - Playwright `testMatch` in each of the three `playwright.config.ts` files scopes to
     that surface's own spec files only.
   - Coverage config (`scripts/check-coverage.ts` + Bun's native lcov output) excludes
     test files themselves, fixtures, and generated files (`lib/*-types.ts` OpenAPI
     types) from the line/branch denominator. New fixture files this pass (Agent Type
     manifest fixtures under `admin/src/fixtures/agent-types/`) fall under the same
     fixtures exclusion — no config change needed, just confirm the glob still matches
     the new subdirectory once created.
3. **Verification:** `bun test --dry-run 2>&1 | grep -v '\.unit\.\|\.content\.'` should
   return only integration/smoke files if run scoped to those two layers; the inverse
   check works per-layer. (Per project memory: `bun test`'s `--filter`/`--dry-run` flags
   are not always reliable in this sandbox — treat as a CI-only verification, not a
   local one.)

## Coverage targets

Single coverage floor per Step 7 — no per-tier CI thresholds. This matches the repo's
existing gate exactly: `scripts/check-coverage.ts` already enforces an aggregate 80/80
line/function floor via `task test:coverage`, with no per-file or per-criticality
carve-out. No change needed to the enforcement mechanism.

| Layer | Minimum CI floor | Notes |
|---|---|---|
| unit | ≥80% line, ≥80% branch | Already enforced repo-wide via `scripts/check-coverage.ts`'s lcov-threshold gate (`task test:coverage`). Critical-path files (`agent/src/work-selector.ts`, `task-store/src/ready.ts`/`blocked-by.ts`/`statuses.ts`, `admin/src/agent-manifest.ts`, `lib/claim-ttl.ts`, and — new this pass — `admin/src/agent-type-registry.ts` per Phase 1's "high criticality" tag) should reach ≥90% — directed by team practice during Phase 5 task prioritization, not a separate CI gate. |
| integration | every named boundary covered | Measured by code path, not line count. Phase 3 (`/test-migration`) is where this gets audited against the Phase 1 inventory's per-file boundary list, including the new critical item this pass — `admin/src/agent-type-manifest-loader.ts` — which needs both the happy-path resolve and the fallback-to-"coding" behavior covered as distinct boundary cases, not just one happy-path test. |
| smoke | every public route 200/4xx covered | Route coverage — every Hono route table entry needs at least a happy-path 200 and one representative 4xx. `admin/src/agents-api.ts`'s create-agent route now has a new validation dimension (`typeName` required, must resolve to a known or fallback-eligible manifest) — its 4xx case should include a request with a missing/invalid `typeName`, not just the pre-existing auth-failure case. |
| E2E | every top-5 journey covered | Phase 1 identified 5 journeys this cycle (four top-level product journeys plus the dev-only HITL loop, newly promoted to its own row). Only journey #4 (marketing-site visitor) is genuinely browser-driven; #1–#3 and #5 are "e2e / smoke-composed" (cross-service integration + fixtures, no browser) per Phase 1's finding. **Journey #2 (agent provisioning) is the one most affected by this cycle's change** — the manifest-driven redesign changes what "correct" looks like end-to-end (required `typeName`, no more implicit agent creation), so its existing integration/E2E coverage should be treated as needing a fresh look in Phase 3, not assumed still-valid from before the redesign. |

**Why a single floor (per SKILL Step 7):** per-tier thresholds require a maintained
criticality mapping in CI that decays as files are added/moved/reweighted. A single
floor enforced by `scripts/check-coverage.ts`'s built-in lcov parsing avoids that
maintenance burden while criticality still drives *what gets written first* during
Phase 4/5 planning.

## Speed budgets

Per Step 8, the default budget table from `speed-budgets/SKILL.md`, included verbatim —
this repo does not tighten any budget below the default.

| Layer | Per-test 95p | Per-test hard cap | Suite target |
|---|---|---|---|
| Unit | <50 ms | <200 ms | <30 s |
| Integration | <2 s | <10 s | <3 min |
| Smoke | <5 s | <15 s | <30 s sequential / <60 s full |
| E2E | <30 s | <90 s | <10 min |
| Full PR pipeline | — | — | **<15 min** |
| Canary suite | — | — | **<60 s** (not applicable — see Canary execution contract) |

**Parallelization plan:**

- **Unit + integration + smoke (the combined `ci` job):** Bun's test runner
  parallelizes within a single process by default; no explicit worker-count flag is
  set in `task test` / `task test:coverage` today, and none is being newly prescribed —
  the single-job shape (see CI pipeline shape above) means the whole layer stack runs
  as one `bun test` invocation with Bun's own internal concurrency, not a CI-level
  worker matrix. This is sufficient at current suite size to hit the <15min full-PR
  budget with headroom. The new manifest-system tests this pass are a small, bounded
  addition and don't change this calculus.
- **E2E (`e2e`, `admin-e2e`):** two separate CI jobs, run in parallel with each other
  (both `needs: ci`, no dependency between them) — justified because they exercise
  genuinely different Playwright projects/servers, not a cost-only split of one logical
  suite. Within each job, Playwright's own default worker parallelism applies.
- **If suite growth later threatens the <15min budget:** re-evaluate the per-workspace
  matrix override conditions in the CI pipeline shape section above — specifically
  condition (a) (budget miss) is the trigger to revisit, not a fixed test-count
  threshold. This cycle's ~5-file delta is not, on its own, that trigger.

**Measured baseline (T-067, folding in T-002):** real per-layer numbers exist in
[`speed-baseline.md`](./speed-baseline.md), measured across all five workspaces (plugin,
metrics dashboard, agent, admin, site) — unchanged as an artifact this pass (Phase 2
does not re-run the measurement; see that file for the full per-package breakdown,
methodology, and exact verification-command output). Full-suite figure at last
measurement: **34.23s wall-clock** (3842 pass, 242 skip, 4084 tests across 198 files),
well within the <15 min full-PR budget. Future `/test-migration` passes should
re-measure once the Agent Type manifest system's new tests land, and cite the refreshed
baseline rather than this cycle's carried-forward number.

## Shared helpers / utilities to build

Per Step 9 — most of these already exist; only additions/extensions are called out
explicitly.

- **HTTP request builders (auth tokens, default headers)** — exists per-service already
  (e.g. `admin/src/api-auth.ts`, `task-store/src/auth.ts`, `chat/src/auth.ts` define the
  auth contract; smoke tests build request headers directly against `app.request()`).
  No new shared cross-workspace helper needed.
- **Test-user / test-agent factory** — needed for `admin`'s Agent/Env/Cron/Tool/Token
  CRUD integration tests and `task-store`'s Task/PullRequest/TaskToken tests. **New this
  pass:** the agent factory should be extended (or a sibling factory added) to build a
  valid `typeName`-bearing agent-creation payload by default, since `typeName` is now
  required — tests that previously built an agent payload without `typeName` need that
  factory updated, not each call site patched individually. Build as in-line builder
  functions colocated with each service's `*.integration.test.ts` files, not a shared
  cross-workspace package — the entity shapes are service-specific.
- **Agent Type manifest fixture builder (new this pass)** — a small helper that writes a
  temp-directory manifest fixture (valid / malformed / missing-field variants) for
  `admin/src/agent-type-manifest-loader.ts`'s integration tests, and an in-memory
  `AgentTypeManifestResolver` fake for unit-testing consumers (`admin/src/agent-cron-jobs.ts`,
  `scripts/seed-dev-agent.ts`) without touching the filesystem at all. Colocate with
  `admin/src/fixtures/agent-types/` (see Local execution architecture above).
- **DB reset helper** — `beforeEach`/`afterEach` PrismaClient connect/disconnect pattern
  already documented in `ci.yml`'s comments (real DB per test run, no rollback). No
  shared truncate-helper needed if each test seeds only what it needs.
- **Time freezer (`Clock` injection)** — already a hard rule in root `CLAUDE.md`'s Test
  isolation section. Already implemented per-workspace (`agent/src/clock.ts`,
  `plugins/shipwright/scripts/clock.ts`, `metrics/src/lib/clock.ts`, `admin/src/clock.ts`,
  `mcp-server/src/clock.ts`). No new work this pass — the manifest loader's file-read
  path has no time-dependent logic to freeze.
- **Recorded-fixture loader** — already exists as the `Recorded*Client` pattern
  (`admin/src/kubernetes-client.ts`'s `RecordedKubernetesClient`,
  `metrics/src/providers/task-store-recorded.ts`, `metrics/src/fixtures/task-store-fixtures.ts`,
  `agent/src/shipwright-config-client.ts`) plus per-workspace `src/fixtures/`
  directories. No new work this pass beyond the manifest-fixture builder above, which is
  a sibling pattern (real-file fixtures, not recorded-HTTP fixtures) rather than an
  extension of this exact loader.
- **Thin codegen-wrapper scripts are conventionally untested (class-level decision,
  T-079) — narrowly scoped to three scripts.** `scripts/generate-agent-type-schema.ts`,
  `scripts/generate-chat-spec.ts`, and `scripts/generate-task-store-spec.ts` are thin
  CLI wrappers around already-unit-tested logic (e.g. `admin/src/agent-type-registry.ts`'s
  `buildAgentTypeJsonSchema()`, covered by `agent-type-registry.unit.test.ts`) — the
  only untested surface per script is a `writeFileSync` call and the
  `import.meta.main` CLI-entry guard. Human decision (2026-07-28): this repo does not
  test these three scripts. This is a narrow, named-file convention, not a glob-based
  or repo-wide one — do not re-litigate these three files individually in future
  test-readiness cycles, but do not extend the exemption to other `generate-*.ts`
  scripts by pattern-matching the name.
  - **Not covered by this exemption — real, open gaps:** `scripts/generate-admin-spec.ts`
    has no `import.meta.main` guard; its OpenAPI merge and colon-to-brace path-rewrite
    logic runs unconditionally at module load and isn't extracted into a tested
    function (`admin/src/admin-spec.smoke.test.ts` tests the live Hono `/doc` format,
    not this script's output, so it doesn't exercise that logic).
    `scripts/generate-mcp-tools.ts` has no test file and exports several substantial,
    I/O-free pure functions (`deriveToolName`, `resolveRef`, `resolveBodySchema`,
    `inlineObjectSchema`, `generateTools`) that are straightforward to unit test.
    Both should be tracked as follow-up test-readiness tasks, not swept under this
    convention.
  - **Already covered, not part of the untested class:** `scripts/generate-mcp-docs.ts`
    has `generate-mcp-docs.unit.test.ts` covering its `renderMcpToolsDoc()` render
    logic.

## Repo configuration

Per Step 10 (`repo-config/SKILL.md`). Without this section, the test pipeline above is
advisory — CI can run and still not gate `main`.

### Branch protection (`main`)

- **Required PR** — no direct pushes (assume already true; verify in Phase 4 against the
  live GitHub branch-protection API, not assumed from this doc).
- **Required status checks:** `ci` (lint/typecheck/unit/integration/smoke/secret-scan),
  `site` (build/brand-lint/Playwright), `admin-docker-build`, `agent-docker-build`,
  `metrics-docker-build`, `e2e`, `admin-e2e` — every job in `.github/workflows/ci.yml`,
  unchanged this pass.
- **Require branches up to date before merging:** **no.**
- **Require conversation resolution before merging:** **no.**
- **Required approving reviews:** **0.**
- **Enforce on admins:** **no.**

`shipwright-deploy` is enabled for this repo, so most PRs are agent-authored and
agent-reviewed, and `deploy.md` Step 4b merges every PR with `gh pr merge --admin` by
design, not as an emergency exception. `--admin` bypasses branch protection wholesale, so
all four settings above are moot for the actual merge path — see `repo-config/SKILL.md`'s
Branch protection section for the full reasoning (GitHub blocks self-APPROVE via the API,
`enforce_admins: true` would block every automated merge, "up to date" burns real CI
minutes on every sibling PR each time main advances, and conversation resolution isn't
reliably enforced by the pipeline either). These are the standard values for every
shipwright repo, one flat default rather than a per-repo choice.
- **Canary status check:** not applicable / not included — see Canary execution
  contract section; there is no canary job to include or exclude.

### Required secrets

- `TEST_CANARY_API_KEY` — **not required today.** Canary doesn't apply under the
  `direct` deploy model. Do not provision this secret until a staging environment
  exists.
- Per-external-service auth tokens for **recording** new fixtures (not for running
  tests): a GitHub App token (`RecordedGithubClient`), a K8s service-account token
  (`RecordedKubernetesClient`), Groq/ElevenLabs API keys (`agent/src/voice.ts`), Google
  OAuth2 test credentials (`admin/src/google-auth-client.ts`), Slack app credentials
  (`admin/src/slack-provisioning-client.ts`). **Not currently needed as GitHub Secrets**
  unless/until the deferred recorded-fixture maintenance loop is implemented. The new
  Agent Type manifest loader reads local YAML files, not a third-party API, so it
  introduces no new secret requirement.
- Deploy credentials (GHCR push token, Helm/K8s cluster credentials) — already exist per
  the repo's `direct` deploy model; out of scope for this test-system doc.

### Required GitHub Environments

- **`staging` / `production`:** **not applicable under the current `direct` deploy
  model.** Do not create these as part of test-readiness work.
- **`recorded-fixture-refresh`:** deferred, not created — see below.

### PR template

**Already exists — confirmed present this pass** at `.github/PULL_REQUEST_TEMPLATE.md`
(916 bytes, uppercase canonical filename). No Phase 4 task needed.

### Recorded-fixture maintenance loop — deferred

Per `repo-config/SKILL.md` §5: **not implemented.** This repo already has several
recorded-fixture doubles in production use, maintained by hand. Do not wire up the
scheduled auto-recording GitHub Actions workflow until the manual, hand-authored-fixture
workflow is running and tuned across all newly-covered clients. The Agent Type manifest
fixture pattern introduced this pass (real local files, not recorded HTTP) is explicitly
a different mechanism and is not in scope for this deferred loop.

## Gaps and blockers summary

For Phase 4 (`test-roadmap`) triage — every item below is a **design decision or gap**,
not an existing-test audit finding (that's Phase 3):

1. **Agent Type manifest system needs full-stack test-layer coverage scoped correctly
   (new this pass).** `admin/src/agent-type-registry.ts` (unit — schema/validation),
   `admin/src/agent-type-manifest-loader.ts` (integration — real file I/O, both
   happy-path resolve and fallback-to-"coding" cases), `admin/src/agent-cron-jobs.ts`
   and `scripts/seed-dev-agent.ts` (unit, via the injected `AgentTypeManifestResolver`
   fake — not the filesystem), and `admin/src/agents-api.ts`/`admin-ui.ts` (smoke — the
   new required-`typeName` validation path, both happy-path and 4xx). Flag for Phase 4 so
   this isn't scoped as one trivial task — it spans four layers.
2. **Agent provisioning E2E/integration coverage needs a fresh pass, not a fixture
   refresh.** Per Phase 1: the manifest-driven redesign changes what "correct" looks
   like end-to-end for journey #2 (implicit agent creation removed, `typeName` now
   mandatory). Phase 3 should treat any existing `admin/e2e/agents-page.e2e.ts` coverage
   of the new-agent form as needing re-validation against the new required-picker flow,
   not assumed still passing because the file exists.
3. **`scripts/hitl.ts` criticality/layer is an open question (carried forward from
   Phase 1's Ambiguous items).** Grew from a thin wrapper to a 739-line full worker loop
   this cycle with no criticality change (still "medium, dev-only"). If a human confirms
   it should be "high," its test-layer treatment should mirror `agent/src/entrypoint.ts`
   / `loop-orchestrator.ts` (integration-layer, injected exec/fs/network) rather than
   being left as an implicitly-untested dev script. No test-system action needed until
   that sign-off happens — flagged here so Phase 4 doesn't silently skip it.
4. **`admin/src/agent-type-manifest-loader.ts`'s fallback-to-"coding" contract is an
   error-handling policy embedded in an integration-layer file (Phase 1 Ambiguous
   items).** Recommend Phase 4 scope a dedicated test asserting the "never throws on
   unknown typeName, falls back with a logged warning" behavior as its own case, not
   folded silently into a generic loader test — a stale/renamed `typeName` failing open
   is a meaningful operational property.
5. **Recorded-fixture coverage is partial (carried forward, unchanged this pass).** Four
   `Recorded*`/offline-fixture implementations exist; roughly a dozen more
   `Http*Client`/SDK-wrapper modules need the same pattern extended, per the Local
   execution architecture table above.
6. **Canary execution contract is fully deferred**, contingent on a future staged-deploy
   model — not a gap in this design, a correct non-applicability finding carried forward
   from Phase 1 and re-confirmed here.
7. **Branch-protection required-checks list should be verified against the live GitHub
   API**, not assumed from `ci.yml`'s job list alone — Phase 4/Step 10's pairing rule
   depends on this being accurate.
8. **`agent/src/piper-voice.ts` is a pending reclassification watch item** (unit today,
   category-5/integration once Piper synthesis lands) — carried forward from Phase 1's
   Ambiguous items, still open this pass (no synthesis code landed yet). No test-system
   action needed until that lands.
9. **`admin/src/server.ts` (composition entrypoint)** needs exactly one thin smoke test
   confirming the three sub-apps mount correctly under one Hono instance — carried
   forward; Phase 3 should confirm whether this test already landed.

## Next step

Run `/test-migration` (Phase 3) to reconcile this blueprint against the existing test
suite — which tests already satisfy this design, which need to move layers, which need
to be rebuilt, and which gaps above already have partial coverage this design doc
wasn't able to see (Phase 2 does not read existing tests, per the SKILL's failure-modes
guidance).
