# site

Static (Astro SSG) marketing site for Shipwright Harness — dark-premium, no runtime JS. Theme derives from `brand/tokens.json`. Self-contained: `bun install`, `bunx astro build`, `bunx astro preview`, smoke via `bunx playwright test`.
